---
title: Learnable Token Sparsification for Efficient Gigapixel Whole Slide Image Reasoning
title_zh: 可学习的令牌稀疏化方法用于高效十亿像素全切片图像推理
authors: "Jingzhi Chen, Landi He, Zhuo Chen, Shawn Young, Lijian Xu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08641v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 全切片图像VLM的可学习令牌稀疏化
tldr: "针对十亿像素全切片图像视觉令牌过多问题，提出可学习令牌稀疏化方法，通过解耦路由架构和SparseLearn组件（含可微Soft Top-K与对角注意力去噪器）实现端到端训练，推理时仅保留32个令牌（0.78%原始长度），在SlideBench上达到73.32%准确率，显著优于基线方法，并具备强零样本泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08641v1/fig-001.webp\", \"caption\": \"Table 1: Main results on SlideBench (TCGA) and zero-shot generalization on SlideBench (BCNB) and WSI-VQA*.\", \"page\": 6, \"index\": 1, \"width\": 964, \"height\": 492}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-002.webp\", \"caption\": \"Figure 3: Accuracy and token efficiency. Our framework achieves competitive diagnostic accuracy using only 32 tokens, which is 128 times fewer than the SlideChat [51] baseline. Furthermore, it significantly outperforms traditional patch-based VLM methods that demand over 17.2K tokens.\", \"page\": 7, \"index\": 2, \"width\": 960, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-003.webp\", \"caption\": \"Figure 1: Illustration of WSI sparsity and pruning methods. The left panel highlights the inherent sparsity of WSIs, where critical tumor regions are submerged in redundant normal tissue. The right panel contrasts training-free sampling with learning-based pruning, noting that the latter leverages loss feedback for better generalization in LLMs.\", \"page\": 3, \"index\": 3, \"width\": 976, \"height\": 501}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-004.webp\", \"caption\": \"Figure 4: Performance stability across varying sequence lengths. The interval accuracy (orange line) remains highly stable near the global average (73.32%) despite exponential variations in WSI token counts (blue bars). This demonstrates that the model’s reasoning capability is inherently insensitive to the input scale.\", \"page\": 8, \"index\": 4, \"width\": 976, \"height\": 587}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-005.webp\", \"caption\": \"Figure 5: UMAP visualizations of the latent feature space across three diverse WSIs. The selected tokens consistently cluster at topological extremities, demonstrating the model’s precision in isolating sparse, discriminative morphological outliers without semantic dilution.\", \"page\": 8, \"index\": 5, \"width\": 976, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-006.webp\", \"caption\": \"Figure 2: Overview of our framework. Frozen encoders extract visual and textual features. To mitigate LLM context bottlenecks, a learnable sparsity mechanism dynamically reduces visual tokens via decoupled routing. Training path: To enable continuous gradients for discrete pruning, the SparseLearn module computes token importance (si) via a Scorer, mapped to continuous weights (α) using Soft Top-K. A VP Noise Gate injects α-scaled VP noise for continuous optimization without token removal, while a Denoiser recovers perturbed representations. Inference path: SparseLearn is detached. Guided solely by the trained Scorer, a Hard Top-K operator executes deterministic token pruning, retaining critical pathology features with zero computational overhead. Fire and snowflake icons denote trainable and frozen modules.\", \"page\": 4, \"index\": 6, \"width\": 976, \"height\": 467}]"
motivation: 现有令牌剪枝方法依赖固定启发式或空间下采样，会丢弃稀疏但关键的病理模式。
method: 将令牌减少建模为可训练稀疏化问题，利用SparseLearn组件（方差保持噪声门+可微Soft Top-K与对角注意力去噪器）实现梯度传播，推理时采用Hard Top-K保留32个令牌。
result: "在SlideBench（TCGA）上达到73.32%总体准确率，令牌压缩至0.78%，并在BCNB和WSI VQA*上展现强零样本泛化。"
conclusion: 该框架通过可学习稀疏化解决了视觉上下文瓶颈，防止诊断证据稀释，为全切片图像推理提供了高效范式。
---

## 摘要
在视觉语言模型中处理十亿像素全切片图像时，由于视觉令牌数量过多而面临重大困难。现有解决方案通常依赖于空间下采样或无需训练的启发式剪枝策略，这些方法往往会丢弃细微但具有临床意义的模式，因为病理证据在组织中不规则地散布。为克服这一局限，我们将全切片图像中的令牌缩减重新构建为可训练的稀疏化问题，使模型能够学习最优选择策略，而非遵循固定启发式规则。我们提出了一种解耦路由架构。为在训练过程中实现不可微剪枝操作的梯度传播，我们引入了一个名为SparseLearn的组件。该组件通过可微分的Soft Top-K算子，使用方差保留噪声门控来调节每个斑块的信息流，并结合对角注意力去噪器恢复受扰表示而不泄露空间信息。在推理阶段，SparseLearn模块被完全丢弃，训练好的评分器应用确定性的Hard Top-K算子仅保留得分最高的32个令牌，不引入额外计算。通过将视觉序列压缩至仅32个令牌的稀疏集合（仅占原始长度的0.78%），我们的框架在SlideBench（TCGA）上实现了73.32%的总体准确率，一致超越基于采样的基线和通用视觉语言模型。在SlideBench（BCNB）和WSI VQA*上也展现了强大的零样本泛化能力。通过解决视觉上下文瓶颈并防止稀疏诊断证据的稀释，本工作为端到端十亿像素全切片图像推理提供了一种高效范式。

## Abstract
The processing of gigapixel whole slide images within vision language models faces a major difficulty due to an excessive number of visual tokens. Existing solutions typically rely on spatial downsampling or heuristic pruning strategies that operate without training, and these methods often discard subtle but clinically meaningful patterns because pathological evidence is scattered irregularly across the tissue. To overcome this limitation, we reformulate token reduction in whole slide images as a trainable sparsification problem, allowing the model to learn an optimal selection strategy instead of following fixed heuristics. We propose a decoupled routing architecture. To enable gradient propagation through the nondifferentiable pruning operation during training, we introduce a component called SparseLearn. This component uses a variance-preserving noise gate that regulates the information flow of each patch via a differentiable Soft Top-K operator, together with a diagonal attention denoiser that recovers perturbed representations without leaking spatial information. At inference time, the SparseLearn module is entirely discarded, and the trained scorer applies a deterministic Hard Top-K operator to keep only the highest scoring 32 tokens, incurring no extra computation. By compressing the visual sequence down to a sparse set of just 32 tokens, which represents as little as 0.78% of the original length, our framework achieves 73.32% overall accuracy on SlideBench (TCGA), consistently surpassing sampling-based baselines and general-purpose vision language models. It also demonstrates strong zero shot generalization on SlideBench (BCNB) and WSI VQA*. By resolving the visual context bottleneck and preventing the dilution of sparse diagnostic evidence, this work provides a highly efficient paradigm for end to end gigapixel whole slide image reasoning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

在计算病理学中，视觉语言模型（VLM）处理十亿像素全切片图像（WSI）时，面临严重的计算瓶颈。一张标准WSI通常生成超过10⁵个图像块（patches），这些视觉令牌在送入大语言模型（LLM）时，会导致二次复杂度的自注意力计算和显存消耗，使得密集视觉输入不可行。现有方法主要依赖两种策略：
- **空间下采样**：均匀或随机丢弃大部分patch，压缩到固定窗口；
- **免训练剪枝**：基于注意力幅度或视觉相似性等代理度量过滤令牌。

然而，这些方法本质上将压缩视为识别背景噪声，忽略了病理数据的极端稀疏性——关键诊断证据（如微转移灶）往往只占据全片的不到1%。因此，这些方法容易丢弃细微但临床意义重大的模式。

本工作将WSI令牌剪枝重新定义为**可学习的稀疏化问题**，让模型通过连续优化自主学会最优的令牌选择策略，避免固定启发式的信息丢失。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
提出**解耦路由架构**，将训练与推理分离：

- **训练路径**：引入 **SparseLearn 训练模块**，使用可微分的 **Soft Top-K** 算子配合**方差保持噪声门控**（VP noise gate）和**对角注意力去噪器**（Diagonal-Attention Denoiser），在不移除令牌的情况下实现连续梯度传播。
- **推理路径**：丢弃 SparseLearn 模块，仅用训练好的**评分器**（Scorer）执行确定性的 **Hard Top-K** 剪枝，保留得分最高的32个令牌，零额外计算开销。

### 关键技术细节

1. **系统形式化**  
   - 给定WSI I，提取 N 个patch的特征序列 X_v ∈ ℝ^{N×d_v}。
   - 在视觉编码器 E_v 和投影层 P_{v→ℓ} 之间加入可学习剪枝框架。

2. **学习型评分器与 Soft Top-K**  
   - 评分器 S_θ 由轻量Transformer编码块+线性投影组成，为每个令牌计算重要性分数 s ∈ ℝ^N。
   - **Soft Top-K 算子 Φ_K** 对分数进行 z-score 归一化后，通过温度缩放映射为连续权重 α ∈ [0,1]^N，满足 ∑α_i ≈ K（K=32为固定稀疏预算）。温度参数 τ 从 τ_start 余玄退火到 τ_end，逐渐逼近二进制掩码。

3. **方差保持噪声门控（VP Noise Gate）**  
   - 对于第 i 个令牌 x_i，噪声注入表示为：  
     ˜x_i = √α_i · x_i + √(1-α_i) · ε_i，其中 ε_i ~ N(0, I_{d_v})。  
   - 当 α_i→1 时保留原特征；α_i→0 时信号淹没在噪声中。  
   - 系数设计确保 Var(˜x_i) ≈ Var(x_i)，防止分布偏移。

4. **对角注意力去噪器**  
   - 轻量单层Transformer编码块，使用**对角注意力掩码**（每个令牌只能关注自身），避免高置信度令牌的信息泄漏到低置信度令牌。  
   - 仅通过值投影和前馈网络恢复噪声带来的表示扰动，不引入空间泄漏。推理时完全丢弃。

5. **优化目标**  
   - 联合优化评分器参数 θ、去噪器参数 ω 和 LLM 的 LoRA 参数 φ，采用负对数似然损失（NLL）进行下一令牌预测。

## 3. 实验设计

### 数据集与评估协议

| 数据集 | 用途 | 说明 |
|--------|------|------|
| SlideBench (TCGA) | 训练与主评估 | 报告总体准确率，并细分为 Microscopy（低层形态/染色描述）和 Diagnosis（组织学推理/临床亚型）两个子类 |
| SlideBench (BCNB) | 零样本泛化测试 | 外部乳腺数据集，评估分布外鲁棒性 |
| WSI-VQA* | 零样本泛化测试 | 外部视觉问答数据集 |

### 对比方法

- **上界无压缩模型**：SlideChat（保留全片证据，无令牌缩减）
- **计算匹配的空间采样方法**：Random Baseline（随机保留32令牌）
- **通用VLM**：GPT-4o、LLaVA-Med、Quilt-LLaVA、MedDr（均为计算量相当的配置）
- **消融变体**：替换VP噪声门控为缩放门控、替换对角注意力为全局注意力

### 主要结果（表1）

| 方法 | FLOPs | TCGA整体 | TCGA-Diagnosis | BCNB零样本 | WSI-VQA*零样本 |
|------|-------|----------|----------------|------------|----------------|
| SlideChat（上界） | 133.3T | 81.17% | 73.27% | 54.14% | 60.18% |
| Ours（32令牌） | 1.73T | **73.32%** | **70.09%** | **56.89%** | **60.76%** |
| MedDr（1.70T） | 1.70T | 67.70% | 57.78% | 33.67% | 54.36% |
| GPT-4o | – | 57.91% | 46.69% | 41.43% | 30.41% |

## 4. 资源与算力

文中明确说明：
- **GPU型号与数量**：2块 **NVIDIA A6000 GPU**
- **训练策略**：两阶段渐进训练
  - 阶段1：LLM严格冻结，优化 slide encoder、SparseLearn 模块、跨模态投影器。
  - 阶段2：冻结 slide encoder，继续更新 SparseLearn 和投影器，同时使用 LoRA 对 LLM 进行参数高效微调。
- **训练时长**：文中未明确给出具体训练小时数，仅说明使用了上述两阶段协议。

## 5. 实验数量与充分性

论文进行了多组实验，整体较为充分：

1. **主实验**：在 SlideBench (TCGA) 上对比了5种基线方法（含上界、随机、4种VLM），报告了总准确率和诊断子类准确率。
2. **零样本泛化**：在2个外部数据集（BCNB、WSI-VQA*）上评估，与所有基线（含GPT-4o）对比。
3. **效率分析**：
   - 图3展示不同输入规模下的准确率与令牌数量关系，突出32令牌下的128倍压缩。
   - 图4展示从64到10000+ patch的序列长度下准确率稳定性（波动极小）。
   - UMAP可视化（图5）定性展示选定令牌聚集在拓扑极值区域。
4. **消融实验**（表2）：
   - 替换VP噪声门控为缩放门控 → 准确率下降2.92%（73.32%→70.40%）。
   - 替换对角注意力为全局注意力 → 准确率下降1.70%（73.32%→71.62%）。
   - 说明两个组件均不可或缺。

**充分性评价**：
- 覆盖了同类方法的全面对比（含计算匹配的VLM、空间采样基线），且设置了无压缩上界。
- 消融实验针对核心设计点进行，逻辑清晰。
- 零样本泛化测试验证了分布外鲁棒性。
- 不足：未在不同稀疏预算（如16、64令牌）下进行系统消融，仅固定K=32；未与其他可学习剪枝方法（如LearnPruner等）直接对比（可能因研究时间接近或适用场景不同）。

## 6. 论文的主要结论与发现

1. **可学习稀疏化有效**：将WSI令牌剪枝转化为可训练问题，在严格32令牌预算下达到73.32%总体准确率，保留上界模型（SlideChat）96%的诊断推理能力（Diagnosis 70.09% vs 73.27%），计算开销仅1.3%。
2. **零样本泛化超越基线**：在BCNB和WSI-VQA*上，该方法甚至超过了无压缩的SlideChat，说明学习语义重要性而非固定空间锚点有助于提取域不变形态特征。
3. **尺度不变性**：模型在64到10000+ patch的输入长度下准确率稳定在73%左右，说明推理由内在形态特征驱动而非物理尺寸。
4. **连续梯度传播方案有效**：VP噪声门控+对角注意力去噪器成功模拟了离散剪枝的信息限制行为，避免了STE梯度近似带来的优化崩溃。

## 7. 优点

1. **新颖的问题重新定义**：将传统固定启发式剪枝转化为端到端可学习的稀疏化问题，突破了训练与推理之间的不可微鸿沟。
2. **精巧的可微分代理设计**：VP噪声门控通过保留方差确保了LLM输入分布稳定，对角注意力防止了信息泄漏，两者协同实现了可靠的连续梯度流。
3. **极致的计算效率**：仅保留32个令牌（压缩128倍以上），推理时无任何额外计算，FLOPs仅1.73T，而上界SlideChat需133.3T。
4. **强泛化能力**：在外部数据集上表现甚至超过无压缩模型，说明选择机制学会了真正的病理相关特征，而不是过度拟合训练集。
5. **模块化架构**：SparseLearn训练阶段可插入、推理阶段可完全移除，不对冻结的LLM进行任何结构性侵入。

## 8. 不足与局限

1. **局部评分器的局限性**：评分器仅基于单个patch的特征独立评估重要性，缺乏早期跨令牌通信。对于需要宏观形态模式或弥散性边缘的病变（如弥漫肿瘤浸润），可能遗漏更广泛的关联上下文。
2. **固定的稀疏预算**：强制使用32个令牌，未探索动态预算分配。不同临床任务可能需要不同的信息密度（如分型需更多区域、检测微转移只需少数区域），静态预算限制了灵活性。
3. **未与其他可学习剪枝方法直接对比**：论文主要对比了空间采样和通用VLM，未与同类的可学习令牌缩减方法（如LearnPruner、Token Merging等）进行实验，尽管可能因领域差异（WSI vs 自然图像）难以直接移植，但缺少这一维度的比较削弱了结果说服力。
4. **消融实验覆盖范围有限**：仅在K=32下进行消融，未测试不同K值的影响；未对温度τ的调度策略进行敏感性分析。
5. **训练成本未充分披露**：仅提到使用2×A6000，但未给出训练轮数、时间或收敛数据，可复现性信息不完整。
6. **下游任务单一**：仅在分类/问答任务上评估，未在分割、生存分析等更具挑战性的任务上验证泛化性。
7. **数据来源限制**：主要基于TCGA数据集训练，虽然BCNB是外部测试，但两个数据集均来自公开癌症基因组图谱领域，对于非癌组织等场景的泛化性未知。

（完）
