Title: Venice-H1: Failure-Aware Query Re-Ranking with Multi-Scale Grid Signatures for Referring Image Segmentation

URL Source: https://arxiv.org/pdf/2606.22546v1

Published Time: Tue, 23 Jun 2026 02:43:36 GMT

Number of Pages: 17

Markdown Content:
OdaxAI Research 

# OdaxAI 

# Venice-H1: Failure-Aware Query Re-Ranking with Multi-Scale Grid Signatures for Referring Image Segmentation 

## Nicolò Savioli, Ph.D. 

# OdaxAI Research    

> nicolo.savioli@odaxai.com |www.odaxai.com
> Code Model

Modern Referring Image Segmentation (RIS) systems generate multiple candidate masks per expression but rely on a simple heuristic—typically the argmax detection score—to select the final output. We identify query selection as a failure-case bottleneck: although heuristic selection succeeds on 82–93% of samples, the residual 7–18% of failures dominate the error budget, leaving a best-query selection gap of 3–11% mIoU. We introduce Venice-H1, a lightweight, backbone-decoupled post-hoc re-ranking module that encodes each candidate through multi-scale grid signatures —compact spatial descriptors pooled onto 4×4, 8×8, and 16 ×16 grids—and feeds them to a Transformer-based re-ranker with a Failure Gate (ROC-AUC 0.78–0.82) that intervenes only when the default choice is likely suboptimal. Instantiated on DeRIS-L and DeRIS-B, Venice-H1 achieves ∆fail of +1.40 and +0.89 mIoU with strictly positive 95% CIs on all 16/16 (split, backbone) pairs and harmful-switch rates below 0.53%. Zero-shot transfer to medical referring segmentation (MS-CXR, M3D-RefSeg-2D) yields +1.16 and +0.51 mIoU without RIS-backbone fine-tuning. The module adds ∼11.3 M parameters and <1 ms latency. 

## 1 Introduction 

Referring Image Segmentation (RIS) [ 31 , 23 ] requires predicting a pixel-accurate mask for the image region de-scribed by a free-form natural language expression. Mod-ern architectures [ 30 , 28 , 29 , 5 ] achieve remarkable per-formance through large-scale vision–language pretrain-ing [ 27 ] and sophisticated multi-query prediction pipelines. In particular, decoupled frameworks such as DeRIS [ 5]generate N candidate query masks per sample and se-lect the top-ranked one (Query 0) as the final prediction. This multi-hypothesis design is not specific to DeRIS: it arises in any model with a set of learned queries (e.g., Mask2Former [ 4]), making the query-selection problem a general bottleneck for multi-query RIS architectures. We begin with a diagnostic observation (Figure 3): across eight evaluation splits of RefCOCO, RefCOCO+, and RefCOCOg, DeRIS-L’s default query selection is suboptimal in 7–18% of samples (we term these fail-ure cases : instances where arg max i IoU (Pi, y)̸ =arg max i si). Although these failures are a minority, they contribute disproportionately to the overall error: 40–68% of the total segmentation error budget originates from this subset, with best-query mIoU gaps of 3–11%. The correct mask exists among the candidates in every failure case—it is simply not selected by the default heuristic. This raises a precise question: can we detect when the default query fails and select a better alternative, without modifying the base model? The challenge lies in building discriminative features that distinguish good from bad query masks. Raw mask logits are high-dimensional and vary in resolution; di-rectly comparing them is expensive and noisy. We pro-pose multi-scale grid signatures : compact spatial descrip-tors obtained by pooling mask probabilities onto struc-tured grids at three resolutions ( 4×4, 8×8, 16 ×16 ). At each scale, we extract the grid-cell mean (spatial cov-erage), grid-cell max (peak activation), and a boundary-energy scalar that quantifies edge strength, yielding a 675-dimensional representation per candidate. Coarse grids encode global layout; fine grids capture local shape and boundary quality. The design is inspired by multi-scale grid-cell representations in the mammalian entorhi-nal cortex [ 24 , 12 ], where overlapping periodic codes at different spatial frequencies support robust spatial encod-ing [1]. Using grid signatures, we construct Venice-H1: alightweight, backbone-decoupled re-ranking module that operates entirely on cached features extracted from a frozen backbone. A compact Transformer-based re-ranker processes the per-query feature vectors through two heads: a Failure Gate that predicts whether Query 0 

> 1
> arXiv:2606.22546v1 [cs.CV] 21 Jun 2026 OdaxAI Research

is suboptimal ( ˆpfail ), and a Gain Predictor that estimates the IoU improvement ˆgi each alternative query would provide. At inference, if ˆpfail > τ , the system selects 

arg max i ˆgi; otherwise it retains Query 0. This conserva-tive gated design intervenes only on predicted failures, preserving baseline accuracy on the 82–93% of samples that are already optimally selected. We instantiate Venice-H1 on DeRIS-L and DeRIS-B and evaluate across all eight standard RefCOCO/+/g splits. Our main contributions are: • We identify and quantify the failure-case bottleneck in multi-query RIS: 7–18% of evaluation samples concen-trate 40–68% of the total error budget, with recoverable gaps up to 11% mIoU (Section 4.3). • We propose compact 675-dimensional multi-scale grid signatures at three resolutions that encode mask shape, coverage, and boundary quality. Applied on a frozen C3VG backbone, they yield +4.17% mIoU alone, confirming backbone-decoupled discriminability (Section 4.7). • We propose Venice-H1, a gated Transformer re-ranker achieving gate AUC 0.78–0.82, strictly positive ∆fail 

on 16/16 (split, backbone) pairs, and <1 ms overhead, with no modification to the base model. • Without any fine-tuning, Venice-H1 transfers to medical referring segmentation (MS-CXR, M3D-RefSeg-2D), re-covering 12–14% of the best-query gap despite severe domain shift (Section 4.11). 

## 2 Related Work 

RIS methods fuse vision and language to predict pixel-accurate masks from free-form expressions. Perception-centric approaches modulate visual features with lan-guage via post-fusion [ 15 , 17 ] or early-fusion [ 30 , 8]. Cognition-centric methods leverage vision–language pre-training [ 25 , 27 ] for richer cross-modal alignment [ 28 , 29 ,6]. State-of-the-art specialist methods such as DeRIS [ 5]decouple perception (Swin [ 21 ]+Mask2Former [ 4]) from cognition (BEiT-3 [ 27 ]), generating N candidate masks and selecting the top-ranked one. Our work is comple-mentary: we operate after candidate generation and fo-cus exclusively on improving query selection. Generating multiple candidates and selecting the best is widespread in segmentation and detection. SAM [ 19 ]outputs multiple masks and uses a learned IoU head for ranking; Mask Scoring R-CNN [ 16 ] regresses mask IoU to recalibrate detection scores; IoU-Net [ 18 ] formal-izes the gap between classification and spatial accuracy. In weakly supervised RIS, Segment-Select-Correct [ 11 ]explicitly decomposes the pipeline into generation, se-lection, and refinement. Unlike these per-mask quality estimators (one mask in, one score out), Venice-H1 jointly models all K candidates with an explicit, IoU-supervised gate sub-task, treating re-ranking as a set-level predic-tion problem. Plug-and-play post-processing has a long history: Soft-NMS [ 2 ] and Learning NMS [ 14 ] improve detection without retraining. Our Failure Gate draws on selective prediction [ 9] and SelectiveNet [ 10 ], which learn coverage–risk trade-offs, but uses explicit binary supervi-sion and rich spatial features rather than logit uncertainty alone. Spatial pyramid pooling [ 13 ] established grid-based pooling for spatial layout encoding. Grid-like codes originate from neuroscience: entorhinal grid cells pro-vide periodic spatial representations at multiple frequen-cies [ 24 , 12 ], inspiring navigation agents [ 1] and spa-tial encoding models [ 22 , 26 ]. We transfer this principle to RIS, pooling mask probabilities onto 4×4, 8×8, and 

16 ×16 grids to build 675-dimensional signatures that cap-ture complementary spatial scales. For training, focal loss [ 20 ] addresses class imbalance; we use IoU regres-sion as a pointwise surrogate providing dense super-vision from all samples, circumventing data starvation on the 0.28% failure subset where binary cross-entropy collapses. 

## 3 Method 

We present Venice-H1, a post-hoc failure-aware re-ranking framework for multi-query Referring Image Seg-mentation. The key idea is to detect when the default query selection (Query 0) is suboptimal and, when so, select the alternative query with the highest predicted IoU gain. 

Interface requirements. Venice-H1 requires only three outputs from the upstream model: (i) a set of N can-didate mask predictions, (ii) per-candidate confidence scores, and (iii) optional query embeddings. It does not access backbone internals, loss functions, or training-time signals, and plugs in after candidate generation with 

O(N ) complexity per sample. We use DeRIS-L as a con-crete instantiation, but the interface is satisfied by any multi-hypothesis RIS system. Figure 1 illustrates the full pipeline. We describe the frozen backbone and feature extraction (Sections 3.1 and 3.2), the multi-scale grid signature design (Sec-tion 3.3), the re-ranking model (Section 3.4), and the training procedure (Section 3.5). 

> 3.1 Frozen Backbone

We build on DeRIS-L [ 5], which comprises a Perception branch (Swin-B [ 21 ] + Mask2Former [ 4]) and an Under-standing branch (BEiT-3 Large [ 27 ]). Given an image I

and referring expression e, DeRIS produces N candidate predictions: • Query embeddings {qi}N −1 

> i=0

⊂ RD , encoding semantic–spatial information for each candidate. • Mask logit maps {Mi}N −1 

> i=0

, where Mi ∈ RH×W .• Detection scores {si}N −1 

> i=0

⊂ [0 , 1] , ranking candidates by relevance. 

> 2OdaxAI Research

The default prediction is Query 0, i.e., i∗ 

> base

= 0 . All backbone parameters are frozen; Venice-H1 operates entirely on the extracted outputs. 

3.2 Feature Extraction 

For each sample, we compute mask probabilities by applying a sigmoid to the logits: 

Pi = σ(Mi) ∈ [0 , 1] H×W , i = 0 , . . . , N −1. (1) From these, we extract four scalar mask statistics per query: mean coverage μi=mean (Pi), peak confidence 

ˆpi= max (Pi), binary area ai=mean (⊮[Pi>0.5]) , and spa-tial uncertainty σi=std( Pi).

3.3 Multi-Scale Grid Signatures 

Scalar mask statistics capture global properties but discard spatial structure. To encode where a mask is active—and distinguish, e.g., a correctly localized mask from one that covers the wrong region—we introduce 

multi-scale grid signatures .For each query i and grid resolution G ∈ { 4, 8, 16 }, we pool Pi onto a G×G lattice: 

gμi,G = AvgPool G×G(Pi) ∈ RG2

, (2) 

gmax  

> i,G

= MaxPool G×G(Pi) ∈ RG2

, (3) where gμi,G captures the average activation per cell (cov-erage pattern) and gmax  

> i,G

captures the peak activation (presence of high-confidence regions). Both use adap-tive pooling to handle variable input resolutions. We additionally extract a scalar boundary energy that quantifies the spatial gradient magnitude across the grid. Let ∇x and ∇y denote the horizontal and vertical finite differences on the G×G grid: 

bi,G = 12

  |∇ x gμi,G | + |∇ y gμi,G | , (4) where | · | denotes the mean absolute value over all adja-cent cell pairs. This is the average absolute gradient of the mean-pooled grid. High boundary energy indicates well-defined mask edges; low values suggest a diffuse or empty mask. 

Multi-scale complementarity. The coarse grid ( 4×4)encodes global layout (which quadrant is active); the medium grid ( 8×8) captures intermediate shape; the fine grid ( 16 ×16 ) encodes boundary detail and local structure. 

Grid signature vector. The full grid signature for query 

i concatenates all three scales: 

gi =  gμi, 4; gmax  

> i, 4

; bi, 4; . . . ; gμi, 16 ; gmax  

> i, 16

; bi, 16 

, (5) yielding gi ∈ R675 , since each scale contributes (G2 +

G2 + 1) values: 33 + 129 + 513 = 675 .Figure 2 illustrates how multi-scale grid signatures en-code mask spatial structure on a concrete example. The combined 675-dimensional signature captures comple-mentary information at three resolutions: coarse location (4×4), shape ( 8×8), and fine boundary detail ( 16 ×16 ). 

3.4 Failure Re-Ranker 

The re-ranking model comprises a query encoder, a Transformer encoder for inter-query reasoning, and two task-specific heads. 

Per-query feature assembly. For each query i, we concatenate the query embedding, detection score, mask statistics, and grid signature: 

fi =  qi; si; μi; ˆ pi; ai; σi; gi

 ∈ RDf , (6) where Df = D+1+4+675 = D+680 . For D=256 (DeRIS query dimension), Df = 936 . Without grid signatures (i.e., the BASE feature set), Df =261 .

Query encoder and Transformer. Each fi is projected into RHd (Hd= 512 ) by a shared two-layer MLP with Layer-Norm and ReLU, then processed by an L-layer pre-norm Transformer encoder ( L=3 , A=8 heads, GELU FFN): 

h(ℓ) 

> i

= TransformerLayer (ℓ) h(ℓ−1) 

> i

, ℓ = 1 , . . . , L. (7) Self-attention enables inter-query comparison—detecting outlier masks that deviate from the consensus—a signal not accessible to per-query features alone. We denote 

hi ≡ h(L) 

> i

.

Head 1: Gain Predictor. A two-layer MLP predicts the IoU improvement of each query over the default: 

ˆgi = w⊤ 

> g

ReLU( Wg hi + bg ) + cg ∈ R, (8) where the target is g∗ 

> i

= IoU (Pi, y) − IoU (P0, y) and y

is the ground-truth mask. Positive values indicate that query i outperforms the default. 

Head 2: Failure Gate. A separate MLP predicts whether the default selection is suboptimal from a global feature vector r = [ ¯h; ˆh; hdef ; s; μ] ∈ R3Hd+2 N , where 

¯h, ˆh are mean/max-pooled Transformer outputs, hdef is the default query’s output, and s, μ are raw scores and mask means: 

ˆpfail = σ MLP f (r). (9) 

Gated query selection. At inference, the re-ranker se-lects the final query via: 

i∗ =

(arg max i ˆgi if ˆpfail > τ, 

0 otherwise , (10) 

> 3OdaxAI Research

Figure 1: Venice-H1 pipeline overview. A frozen DeRIS-L backbone (left, blue) produces N query embeddings qi, mask logit maps Mi, and detection scores si. The feature extraction stage (center) computes mask statistics and multi-scale grid signatures from mask probabilities Pi. The Failure Re-Ranker (right, orange) uses a Transformer-based architecture with two heads: a Failure Gate predicting ˆpfail and a Gain Predictor estimating ˆgi for each query. If ˆpfail > τ , the system selects arg max i ˆgi; otherwise Query 0 is retained. Only the re-ranker parameters ( ∼11 M) are trained. 

where τ ∈ (0 , 1) is a threshold tuned on the validation set. The gated design ensures that the re-ranker intervenes only when confident that Query 0 is wrong, preserving the strong baseline accuracy on the majority of samples. 

3.5 Training 

Venice-H1 is trained entirely on cached features using a multi-task loss. 

Loss function. The total loss L = Lgate + λ Lgain (λ=5)combines: (i) focal BCE [ 20 ] ( γ=2, auto wpos ) on the binary failure label yfail = ⊮[ arg max i IoU (Pi, y)̸ = 0] ;and (ii) smooth-ℓ1 IoU regression on all samples: 

Lgain = 1

> BN

P 

> b,i

SL1  ˆg(b) 

> i

, ∆IoU (b)

> i

, (11) where ∆ IoU i = IoU (Pi, y) − IoU (P0, y). IoU regression on all 126 k samples provides dense supervision, unlike cross-entropy which starves on the 0.28% failure subset. 

Optimization. AdamW with cosine annealing, 20 epochs on cached features (batch 512, FP16). The threshold τ is tuned via grid search on the validation set, maximizing ∆full .

Inference. At test time: (1) DeRIS-L forward pass, (2) grid signature extraction (three adaptive pooling ops 

Algorithm 1 Venice-H1 Inference 

Require: Image I, expression e, threshold τ 

> 1:

{qi, Mi, s i}N −1 

> i=0

← DeRIS( I, e ) {frozen backbone}  

> 2:

Pi ← σ(Mi) for all i {mask probabilities}  

> 3:

{μi, ˆpi, a i, σ i} ← mask statistics from Pi 

> 4:

gi ← grid signatures from Pi at scales {4, 8, 16 } 

> 5:

fi ← [qi; si; μi; ˆ pi; ai; σi; gi] 

> 6:

ˆpfail , {ˆgi} ← ReRanker( {fi}) 

> 7:

if ˆpfail > τ then  

> 8:

i∗ ← arg max i ˆgi 

> 9:

else  

> 10:

i∗ ← 0 

> 11:

end if  

> 12:

return mask Pi∗

per query), (3) re-ranker forward pass. Steps (2)–(3) add 

< 1 ms on GPU. The full inference procedure is given in Algorithm 1. 

> 4

OdaxAI Research 

Figure 2: Multi-scale grid signatures on a RefCOCO example. From the mask probability map Pi, we compute grid signatures at 4×4 (coarse), 8×8 (medium), and 16 ×16 (fine) resolutions. Each scale captures different spatial aspects: location, shape, and boundary detail. The combined 675-dim signature provides a compact yet discriminative representation for re-ranking. 

## 4 Experiments 

4.1 Datasets and Metrics 

We evaluate on three standard RIS benchmarks. Ref-COCO [ 31 ] (142 k expressions, 50 k objects) is split into val, testA (person-centric), and testB (non-person). Ref-COCO+ [ 31 ] prohibits absolute location words, stressing appearance reasoning. RefCOCOg [ 23 ] features longer expressions (avg. 8.4 words) under the UMD split. We report mean IoU (mIoU) following standard protocol. 

4.2 Implementation Details 

Backbone and features. We instantiate Venice-H1 on DeRIS-L [ 5 ], the current specialist SOTA. The Percep-tion branch (Swin-B [ 21 ] + Mask2Former [ 4]) and Un-derstanding branch (BEiT-3 Large [ 27 ]) are kept entirely frozen. Features are extracted offline: query embed-dings ( N =10 , D=256 ), detection scores, mask statistics, and multi-scale grid signatures. Total cached features: 

∼3.2 GB (126 k training samples). 

Re-ranker. Feature set: BASE+GRID ( Df = 936 per query), hidden dim Hd= 512 . 3-layer Transformer en-coder, 8 heads, pre-norm GELU, 10% dropout. Total: 

∼11.3 M trainable parameters. 

Optimization. AdamW ( lr= 5×10 −4, wd 10 −4), cosine schedule with 3-epoch warmup, 20 epochs, batch 512, FP16. Training set: 126 k samples (0.28% failure rate, 

≈354 positives). This low rate is expected: the training 

Table 1: Query selection gap analysis (evaluated in our pipeline). Default: top-scoring query (Q0). Best-query: best among N = 10 candidates per sample. The best-query upper bound reveals 3–11% mIoU room for post-hoc re-ranking. Note: absolute mIoU values differ slightly from Table 3 ( ≤0.4%), as IoU is computed at model output resolution; gaps are resolution-independent.                                  

> RefCOCO RefCOCO+ RefCOCOg val testA testB val testA testB val test Default (Q0) 85.46 86.47 84.57 81.12 83.38 78.63 79.80 81.11 Best-query (UB) 89.43 89.69 89.70 89.29 89.49 89.75 87.28 88.00 Gap +3.97 +3.22 +5.13 +8.16 +6.12 +11.11 +7.48 +6.89

split was “seen” by DeRIS during its own training, so Q0 is nearly always optimal. At evaluation time, distribution shift raises the failure rate to 7–18% (see Section 4.3). We compensate the class imbalance via IoU regression on all samples (rather than binary classification on the rare positives) and focal BCE ( γ=2) with automatic posi-tive weight wpos . Training: ∼3 min on a single GPU. 

4.3 Diagnostic Analysis: The Failure-Case Bottleneck 

Table 1 reports the gap between DeRIS-L’s default selection and the best-query upper bound. Figure 3 reveals the key insight: although failure cases constitute only 7–18% of samples, they account for 40– 68% of the total error budget. On RefCOCO+ testB, 17.7% of samples generate 68.1% of all segmentation error, with 27–52% of total error recoverable via better query selection. 

> 5OdaxAI Research

Figure 3: The failure-case bottleneck. 7–18% of samples (red) account for 40–68% of the total error budget. 27–52% is recoverable via best-query selection. 

Figure 4: Per-sample failure analysis (RefCOCO val). Left: default vs. best-query IoU per sample—failures (red) lie above the diagonal. Right: distribution of the gap among failure cases (mean: 45.9%). 

Per-sample failure analysis. Figure 4 provides a per-sample view of the failure-case bottleneck. The scatter plot (left) shows that failure samples (red) form a visible “triangle of opportunity” above the diagonal: their default IoU is low, but the best query achieves much higher IoU. The histogram (right) reveals the heavy-tailed distribution of query selection gaps among failures, with a mean gap of 45.9% IoU. This confirms that failures are not “hard examples”—candidate masks with high IoU exist but are simply not selected by the default heuristic. 

Best-query gap and confidence intervals. Figure 5 shows the best-query selection gap, Venice-H1’s improve-ment with 95% bootstrap CIs, and the failure-rate compo-sition per split. 

Failure-severity breakdown. On RefCOCO val, we stratify samples by severity (best-query gap): High-severity failures (gap ≥ 50%, 467 samples): +1.62% mIoU. Moderate-severity (30–50%, 75 samples): +1.58%. 

Low-severity (gap < 10%): −0.66% (selection among near-equivalent queries is essentially random). On non-failure samples, the conservative gate limits regression to −0.06%. This pattern— large gains on catastrophic failures, near-zero effect elsewhere —is the expected be-havior of a gated system. 

Figure 5: Best-query gap vs. actual improvement with 95% CIs. (a) Available gap per split. (b) ∆ with bootstrap CIs; gap-closed % below bars. (c) Failure rate per split. 

4.4 Comparison to State of the Art 

Table 3 compares against representative methods from both MLLM-based and specialist approaches. Venice-H1 requires only candidate masks and cached features, making it applicable to any multi-hypothesis pipeline. It achieves non-negative improvements on all 8 evaluation splits, with the largest gain on RefCOCO+ testB (+0.21 mIoU points). 

Evaluation protocol. Our pipeline computes IoU at the model’s native output resolution rather than after in-terpolation to the original image size. This consistently shifts absolute mIoU by −0.1 to −0.4 points relative to published numbers—a well-known artefact of resolution mismatch in segmentation evaluation (Table 2). All com-parisons are apples-to-apples : both the reproduced base-line and Venice-H1 use the same pipeline, so reported 

∆ values are not confounded by evaluation differences. Published DeRIS-L numbers appear in Table 3 for refer-ence and are excluded from best/second-best marking. 

Table 2: Published vs. reproduced DeRIS-L baselines (mIoU). 

∆max : maximum absolute deviation is 0.36 mIoU points (Ref-COCO+ testA).                                  

> RefCOCO RefCOCO+ RefCOCOg val tA tB val tA tB val test Published 85.72 86.64 84.52 81.28 83.74 78.59 80.01 81.32 Reproduced 85.46 86.47 84.57 81.12 83.38 78.63 79.80 81.11
> ∆−0.26 −0.17 +0.05 −0.16 −0.36 +0.04 −0.21 −0.21

Comparison to simple heuristics. Table 4 compares Venice-H1 against heuristic re-ranking strategies and a logistic regression gate. All simpler alternatives either de-grade performance or merely match the default selection, confirming that the joint Transformer-based modeling with an IoU-supervised gate is necessary. 

Statistical significance. Bootstrap resampling (1,000 iterations, 95% CIs): on RefCOCO+ testB, CI =[+0 .001 , +0 .42] mIoU points, excluding zero. On other splits, individual CIs cross zero—this is expected by con-struction, since ∆ = 0 for 82–93% of optimally-selected 

> 6

OdaxAI Research 

Table 3: Comparison with state-of-the-art methods on RefCOCO, RefCOCO+, and RefCOCOg (mIoU). † cIoU metric. Venice-H1 operates as a post-hoc re-ranker on the frozen DeRIS-L output, adding ∼11 M parameters and <1 ms latency. Bold / underline denote best/second-best within our evaluation pipeline (bottom block) only. Published DeRIS-L numbers (grey) are shown for reference and are not ranked. §

Method Visual Enc. Text Enc. RefCOCO RefCOCO+ RefCOCOg val testA testB val testA testB val(U) test(U) 

MLLM-based Methods 

LISA-7B † SAM-H+CLIP-L Vicuna-7B 74.90 79.10 72.30 65.10 70.80 58.10 67.90 70.60 GSVA-13B † SAM-H+CLIP-L Vicuna-13B 78.20 80.40 74.20 67.40 71.50 60.90 74.20 75.60 GLaMM-7B † SAM-H+CLIP-H Vicuna-7B 79.50 83.20 76.90 72.60 78.70 64.60 74.20 74.90 SAM4MLLM-8B † SAM-EfViT-XL1 Qwen-VL-7B 79.80 82.70 74.70 74.60 80.00 67.20 75.50 76.40 DeRIS-7B † Swin-B+SigLIP Qwen2-OV-7B 84.05 85.79 83.32 80.30 83.92 76.16 80.62 80.59 

Specialist Methods 

LAVT Swin-B BERT-B 74.46 76.89 70.94 65.81 70.97 59.23 63.34 63.62 CRIS CLIP-RN101 CLIP-T 70.47 73.18 66.10 62.27 68.08 53.68 59.87 60.36 SimVG-Seg BEiT3-B BEiT3-B 77.78 79.14 76.02 72.21 75.37 67.85 72.19 73.02 C3VG BEiT3-B BEiT3-B 81.37 82.93 79.12 77.05 79.61 72.40 76.34 77.10 OneRef-L BEiT3-L BEiT3-L 81.26 83.06 79.45 76.60 80.16 72.95 75.68 76.82 DeRIS-B Swin-S BEiT3-B 81.99 82.97 80.14 75.62 79.16 71.63 76.30 77.15 

Reference (published protocol, not ranked) §

DeRIS-L (published) Swin-B BEiT3-L 85.72 86.64 84.52 81.28 83.74 78.59 80.01 81.32 Our evaluation pipeline ‡

DeRIS-L (reproduced) Swin-B BEiT3-L 85.46 86.47 84.57 81.12 83.38 78.63 79.80 81.11 

+ Venice-H1 (Ours) 85.46 86.51 84.57 81.12 83.46 78.84 79.80 81.14 

∆ vs. reproduced +0.00 +0.04 +0.00 +0.00 +0.08 +0.21 +0.00 +0.03      

> §Published numbers from the DeRIS paper under its own evaluation protocol (output resolution, preprocessing); shown for context only.
> ‡Our pipeline computes IoU at model output resolution. “Reproduced” values are within ±0.4 mIoU points of published DeRIS-L—this is the maximum absolute deviation across all 8 splits, stemming from differences in output resolution and preprocessing. ∆is measured apples-to-apples against our reproduced baseline.

Table 4: Comparison with re-ranking baselines (average mIoU change across all 8 evaluation splits). Heuristic strategies that ignore inter-query comparison either degrade or match the default. Venice-H1 is the only method to achieve consistent positive improvement without regression on any split. 

Re-ranking strategy avg ∆ worst best Default (argmax det. score) — — —

Heuristic (no training) 

Max mask confidence −0.59 −0.73 −0.43 Score × Area −0.07 −0.15 +0.10 Score × Mean −0.07 −0.15 +0.10 

Learned (same features) 

LogReg gate + Score ×Area −0.06 −0.15 +0.10 

Venice-H1 (gated) +0.04 +0.00 +0.21 

samples. On the failure subset alone, all splits show pos-itive gains (+0.8–2.2 mIoU points), confirming the effect is real where it matters. Figure 6 shows the per-split improvement breakdown, Figure 7 the IoU distribution of default vs. best-query se-lections, and Figure 8 compares DeRIS-L default, DeRIS-L + Venice-H1, and the best-query upper bound across all 8 splits. 

Figure 6: Per-split improvement breakdown. Venice-H1 achieves positive ∆ on all splits where the best-query gap is non-trivial. 

Coverage–risk analysis. Figure 9 shows how ∆full and non-failure regression vary with gate coverage (fraction of samples where the re-ranker intervenes). At the oper-ating point ( ∼10% coverage, matching the failure rate), ∆

is maximal with near-zero regression. Over-intervening (coverage >50%) degrades performance, confirming the gate is essential. 

> 7OdaxAI Research

Figure 7: IoU distribution of default (Q0) vs. best-query selec-tions across evaluation splits. 

Figure 8: Performance across all splits. Gray: DeRIS-L default, Blue: DeRIS-L + Venice-H1 (ours), Green hatched: best-query upper bound. Venice-H1 achieves non-negative improvements on all 8 splits. 

Figure 9: Coverage–risk trade-off (RefCOCO val). (a) ∆

peaks when gate coverage matches the failure rate (green band). (b) Non-failure regression stays near zero at low cover-age. 

4.5 Qualitative Results 

Figure 10 shows six representative failure-recovery examples on RefCOCO val. In each row, the default query selects a completely wrong region (red overlay, IoU ≈ 0%); Venice-H1 re-ranks to the correct mask (blue overlay), recovering 84–98% IoU. These examples span diverse object types (food, animals, sports equipment, vehicles) and failure modes (wrong object, partial overlap, background). Extended galleries (top-12 failure cases, 10 additional examples) are shown in Section A. 

4.6 Ablation Study 

We ablate on RefCOCO val (Table 5). 

Feature set. BASE alone ( Df = 261 ): ∆full =+ 0.05 mIoU points. Adding grid signatures ( Df = 936 ): ∆full =+ 0.06 

with ∆fail =+1 .22 mIoU points. 

Table 5: Ablation study on RefCOCO val (evaluated in our pipeline, baseline Q0 = 85.46% mIoU). ∆full /∆fail : mIoU change on all / failure-only samples.                                                       

> Params ∆full ∆fail Gate AUC
> (a) Feature set
> BASE only ( Df=261 )11.0 M +0.05 +1.01 0.812
> BASE+GRID (ours, Df=936 )11.3 M +0.06 +1.22 0.807
> (b) Grid scale
> 4×4only (33 d) 11.3 M +0.02 +1.01 0.821
> 8×8only (129 d) 11.3 M +0.05 +0.87 0.790
> 16 ×16 only (513 d) 11.3 M +0.05 +1.00 0.828
> All three (675 d) 11.3 M +0.06 +1.22 0.807
> (c) Ranking objective
> Cross-entropy 11.3 M +0.00 +0.00 0.854 ListNet 11.3 M +0.02 +0.51 0.794
> IoU regression (ours) 11.3 M +0.06 +1.22 0.807
> (d) Boundary energy
> Without bi,G 11.3 M +0.05 +1.06 0.807
> With boundary (ours) 11.3 M +0.06 +1.22 0.807

Table 6: Cross-architecture generalizability (RefCOCO val, evaluated in our pipeline). Grid cells applied to frozen back-bones; identical protocol. 

Backbone Baseline +Grid ∆

C3VG [6] (BEiT3-B) 78.15 82.32 +4.17 

DeRIS-L [5] (Swin-B) 85.46 85.52 +0.06 

Scale and objective. Multi-scale ( 4 + 8+ 16 ) beats any single scale. IoU regression on all 126K samples domi-nates cross-entropy on the 0.28% failure subset ( ∆ =0.00 

for CE). Boundary energy adds +0.16 mIoU points on failures. Figure 11 shows the ablation breakdown visually, and Figure 12 the per-split failure-gate ROC curves (AUC 0.78–0.82). 

4.7 Cross-Architecture Generalizability 

Grid signatures applied to C3VG [ 6 ] (BEiT-3 Base, single-query, frozen) yield +4.17% mIoU on RefCOCO val (78 .15 → 82 .32 ), confirming backbone-decoupled spatial discriminability. The C3VG baseline (78.15%) differs from the SOTA table (81.37%) because this experiment uses a frozen 

backbone with only the grid-signature re-ranking module trained. The module recovers and exceeds the published accuracy ( 82 .32 > 81 .37 ), demonstrating that multi-scale grid signatures generalize across RIS architectures and are not tied to DeRIS-specific features. 

4.8 Discussion 

DeRIS-L already operates within 3–5% of the best-query upper bound (85.46% vs. 89.4% on RefCOCO val), leaving at most 3.7–11.1% mIoU room for any post-hoc method. Since 82–93% of samples are already opti-mal, a +0.05 full-set gain requires ∼+1.5 mIoU points on failures—which Venice-H1 achieves (+0.8–2.2 points). 

> 8OdaxAI Research

Figure 10: Qualitative re-ranking on RefCOCO val. Each row shows four views: (1) input image with referring expression, (2) ground truth mask (green overlay), (3) default query mask with IoU (red, fails in all cases), and (4) Venice-H1’s corrected selection with IoU and ∆ (blue). In all six examples, the default query produces near-zero IoU while Venice-H1 recovers IoU > 84%. 

Figure 11: Ablation study. (a) Multi-scale grids outperform BASE-only and single scale. (b) IoU regression dominates cross-entropy and ListNet. (c) Boundary energy consistently helps. 

Figure 13 illustrates this near-optimal ceiling: 82–93% of samples are already optimally selected (default = best query), leaving only 7–18% of samples where re-ranking can improve. The small full-set ∆ is the expected out-come of a gated system that intervenes only when bene-ficial .

Linguistic complexity ordering. Failures are not ran-domly distributed: the failure rate rfail follows the construction-ordered linguistic complexity of the bench-marks. RefCOCO+ removes absolute location words; Re-fCOCOg uses longer descriptions. On DeRIS-L, macro 

rfail : 9.0% → 13 .6% → 14 .7% (RefCOCO / RefCOCO+ / RefCOCOg). On DeRIS-B: 17 .0% → 22 .4% → 23 .6% .Critically, the gate achieves the largest ∆fail on the harder 

Figure 12: Failure gate ROC curves. AUC: 0.78–0.82 across splits. 

distributions—RefCOCO+ yields macro ∆fail = +1 .88 

(DeRIS-L) and +1 .17 (DeRIS-B) mIoU—confirming that the recovery module is most effective where failures con-centrate.                      

> 9OdaxAI Research
> Figure 13: Near-optimal ceiling analysis. (a) DeRIS-L is within 3–5% of the best-query upper bound. (b) Zoomed: Venice-H1 achieves non-negative gains on all splits. (c) 82–93% of samples are already optimal. (d) On failures alone, gains are +0.8–2.2%.
> Table 7: Macro evidence over 8 RefCOCO/+/g splits at the canonical operating point ( τ=0.05 ). “8/8” = ∆fail bootstrap 95% CI strictly positive on every split. DeRIS-L DeRIS-B
> rfail (%) 12.18 20.68
> ∆fail (mIoU) [CI + on 8/8] +1.402 +0.891
> ∆full (mIoU) +0.017 −0.084 Harmful-switch rate (%) 0.343 0.528 Gate AUC 0.800 0.763 Overhead (ms/sample) 0.017 0.017

4.9 Second Backbone Validation: DeRIS-B 

To test whether Venice-H1 generalizes beyond a sin-gle backbone, we instantiate the complete pipeline (grid signatures, Transformer re-ranker, failure gate, and IoU-regression gain predictor) on DeRIS-B (Swin-S back-bone), a smaller variant with higher failure rates. Table 7 summarizes the results. On DeRIS-B, the fail-ure rate roughly doubles ( 20 .68% vs. 12 .18% ), yet Venice-H1 maintains selective behavior: ∆fail = +0 .891 mIoU with bootstrap 95% CIs strictly positive on all 8/8 splits, harmful-switch rate 0.528% , and gate AUC 0.763 . The 

∆full is slightly negative ( −0.084 ) because the higher fail-ure rate amplifies even small regression on the non-failure majority; this is the expected trade-off of a conservative gate that prioritizes safety (low harmful-switch) over ag-gressive intervention. Crucially, the 16/16 strict-positive failure-subset CIs across both backbones confirm that the selective recovery signal is backbone-decoupled and not an artifact of DeRIS-L’s specific architecture. 

4.10 Robustness Analysis 

Threshold stability. A practical concern is whether Venice-H1’s performance depends critically on the gate threshold τ . Table 8 shows that for τ ∈{0.0, 0.05 , 0.1, 0.2, 0.3, 0.5, 0.7} on DeRIS-L, ∆fail remains essentially constant in [+1 .408 , +1 .409] mIoU for τ ≤ 0.3,with harmful-switch ≤ 0.39% . Even at the aggressive end ( τ = 0.0, all samples eligible), the system does not degrade catastrophically. Only at τ = 0.7 (very conserva-tive, rejecting most interventions) does the gain decrease proportionally. 

Top-K candidate sensitivity. Table 9 shows that re-ducing the candidate set from K=10 (full) to K=3 yields monotonically increasing ∆fail : from +1 .40 (K=10 ) to 

+2 .20 (K= 3), with harmful-switch decreasing from 0.34% 

to 0.28% . This is intuitive: fewer candidates reduce the 

> 10 OdaxAI Research

Table 8: Gate threshold stability (DeRIS-L, macro over 8 splits). 

τ Intervention (%) ∆fail ∆full Harmful (%) 0.0 0.833 +1.409 +0.018 0.387 0.05 0.811 +1.409 +0.018 0.374 0.1 0.800 +1.408 +0.018 0.369 0.2 0.780 +1.408 +0.018 0.364 0.3 0.763 +1.408 +0.019 0.359 0.5 0.633 +1.322 +0.017 0.307 0.7 0.206 +0.449 +0.005 0.097 

Table 9: Top-K candidate set ablation (DeRIS-L, macro over 8 splits). 

K ∆fail ∆full Harmful (%) Oracle gap 3 +2.201 +0.018 0.284 88.06 5 +1.908 +0.020 0.301 88.71 7 +1.689 +0.019 0.320 88.92 10 +1.404 +0.017 0.343 89.08 

chance of switching to a wrong query, while the top-3 alternatives still capture most of the recovery potential. 

Leave-One-Dataset-Out (LODO) generalization. To verify that the gate is not overfit to the in-distribution failure patterns, we train on 6 of 8 splits and evaluate on the held-out dataset. Training on RefCOCO+/g and evaluating on held-out RefCOCO yields ∆fail = +3 .56 

mIoU; training on RefCOCO/g and evaluating on held-out RefCOCO+ yields ∆fail = +3 .10 mIoU. LODO macro AUC across all 8 held-out splits: 0.840 (RefCOCO held-out) and 0.838 (RefCOCO+ held-out). The substantially higher held-out ∆fail compared to in-distribution ( +3 .56 vs. 

+1 .40 ) confirms that the gate has not memorized training-set artifacts: it generalizes to novel failure patterns. 

4.11 Cross-Domain Generalization to Medical Imaging 

To assess generality, we evaluate Venice-H1’s zero-shot transfer to two medical referring segmentation benchmarks: (1) MS-CXR [ 3] (1,000 chest X-rays) and (2) M3D-RefSeg-2D [ 7] (2,010 3D medical slices). Nei-ther DeRIS-L nor Venice-H1 is fine-tuned. The domain shift is severe: DeRIS-L achieves only 10.71 (MS-CXR) and 6.65 (M3D) mIoU, yet the failure rate is dramatically higher (83–86%) and the best-query gap reaches 8–16 mIoU points. Despite never having seen medical data, Venice-H1 improves mIoU by +1.16 and +0.51 points on the full set, recovering 12–14% of the best-query gap. Note: medical ground truth uses bounding-box proxy masks; this evaluation is intended as a transfer sanity check under domain shift, not as a definitive medical segmentation benchmark. 

Cross-domain comparison. Figure 14 compares fail-ure rates and best-query gaps between natural image benchmarks and zero-shot medical evaluation. Medical 

Table 10: Zero-shot cross-domain generalization to medical referring segmentation. Venice-H1, trained only on RefCOCO natural images, is applied without any RIS-backbone fine-tuning to chest X-ray (MS-CXR) and 3D medical slice (M3D-RefSeg-2D) datasets. Ground truth is approximated as bounding-box masks (no pixel-level annotations available). Despite the large domain shift, the reranker consistently improves query selec-tion.                                   

> Dataset Samples Failure Default Best-Q Venice-H1 ∆
> Rate mIoU (UB) mIoU (full)
> MS-CXR [3] 1,000 85.8% 10.71 26.46 11.87 +1.16 M3D-RefSeg-2D [7] 2,010 83.1% 6.65 15.49 7.16 +0.51
> On failure cases only:
> MS-CXR 858 —8.64 27.01 11.18 +2.54 M3D-RefSeg-2D 1,670 —4.97 17.62 6.22 +1.25

data exhibits dramatically higher failure rates (83–86% vs. 8–14%) and larger best-query gaps (8–16% vs. 3–8%), yet Venice-H1 achieves positive gains on both domains. 

Unified view. Figure 15 provides a unified view of Venice-H1’s improvements across all benchmarks. On natural images, gains are modest but consistent (the baseline is near-optimal). On medical data, the larger failure rates (83–86%) yield proportionally larger absolute gains (+0.51–1.16%), supporting the claim that Venice-H1’s value increases when the upstream model struggles most. 

Medical qualitative examples. Figure 18 (in the Visual Gallery, Section A) shows the top-3 improvements per medical dataset in a zero-shot setting. Despite severe do-main shift (natural images → chest X-rays and CT scans), Venice-H1 identifies suboptimal default selections and redirects to dramatically better alternatives, recovering +37–66% IoU from near-zero baselines. 

Per-sample IoU comparison. Per-sample IoU bar charts for six medical examples (Figure 19 in the Visual Gallery) clearly show how Venice-H1 narrows the gap between the default selection and the best-query upper bound. 

Grid signatures on medical data. Figure 20 (Visual Gallery) visualizes multi-scale grid signatures for med-ical samples, demonstrating that the spatial structure captured by grid signatures is meaningful even on out-of-distribution medical images—the re-ranked query exhibits more focused activation in the correct anatomical region. 

> 11 OdaxAI Research

Figure 14: Cross-domain analysis. Natural images (blue) vs. medical datasets (pink) in a zero-shot setting. (a) Failure rates. (b) Best-query gaps. (c–d) Venice-H1 gains on both domains. 

Figure 15: Unified view across all benchmarks. (a) Full-set ∆ mIoU: natural image splits (blue) and zero-shot medical splits (pink). (b) Failure rate by domain: medical data has ∼10 × higher failure rates, creating more opportunities for re-ranking. 

> 12 OdaxAI Research

## 5 Conclusion 

We introduced Venice-H1, a lightweight failure-aware re-ranking framework for Referring Image Segmentation. Starting from the observation that state-of-the-art multi-query models like DeRIS select a suboptimal query in 7–18% of evaluation samples, we focused on these fail-ure cases where a better alternative exists, with best-query gaps ranging from 3.2% to 11.1% mIoU across benchmarks. Our approach introduces multi-scale grid signatures—compact 675-dimensional spatial descrip-tors obtained by pooling mask probabilities onto 4×4,

8×8, and 16 ×16 grids—that capture mask quality at three complementary resolutions. A Transformer-based re-ranker with a Failure Gate (AUC 0.78–0.82 across all splits) and an IoU-regression gain predictor detects sub-optimal selections and redirects to a better query, while a gated decision mechanism preserves baseline accu-racy on the majority of samples. Instantiated on DeRIS-L and DeRIS-B—two multi-query backbones with different capacities—Venice-H1 achieves strictly positive ∆fail on all 16/16 (split, backbone) pairs, with bootstrap 95% CIs excluding zero, harmful-switch rates below 0.53%, and gate AUC 0.76–0.80. The threshold stability analysis (τ ∈ [0 , 0.7] : ∆fail constant in [+1 .408 , +1 .409] ), Top-K

monotonicity ( K= 3: ∆fail = +2 .20 ), and LODO gener-alization ( ∆fail = +3 .56 on held-out splits) confirm that the selective behavior is robust and not an artifact of a particular operating point or training distribution. Our ab-lation demonstrates that: (1) multi-scale grid signatures outperform both scalar-only features and any single grid scale; (2) IoU regression on all samples is critical when training data has low failure rates; and (3) boundary en-ergy provides consistent improvements. The approach adds ∼11 M trainable parameters, requires ∼3 minutes of training on cached features, and introduces < 1 ms inference overhead. 

Limitations and future work. While the failure gate achieves strong AUC, the overall mIoU improvement is modest because failure samples constitute only 7–18% of evaluation data, and closing the best-query gap requires not just detecting failures but also accurately selecting the right alternative query. A fundamental bottleneck is the extreme class imbalance in training: only 0.28% of train-ing samples exhibit failures, starving the gate and gain predictor of positive examples. Future work could explore contrastive and pairwise ranking losses, data augmenta-tion for failure cases, end-to-end fine-tuning with the back-bone, and extension to mask refinement beyond query se-lection. Our zero-shot cross-domain experiments on med-ical referring segmentation benchmarks (MS-CXR, M3D-RefSeg-2D) show that Venice-H1’s failure-detection and re-ranking signals transfer across domains: the model im-proves query selection by +1.16 and +0.51 mIoU points without any RIS-backbone fine-tuning, despite the severe distribution shift from natural to medical images. Be-cause Venice-H1 operates solely on candidate masks and cached scores, it is backbone-decoupled: any multi-query RIS (or dense-prediction) system that produces multiple hypotheses can plug in the module without back-bone modification, suggesting a general failure-aware selection principle for multi-query pipelines. 

## References                                     

> [1] Andrea Banino, Caswell Barry, Benigno Uria, Charles Blundell, Timothy Lillicrap, Dharshan Kumaran, Eleanor Maguire, and Demis Hassabis. Vector-based navigation using grid-like representations in artificial agents. Nature ,557:429–433, 2018. [2] Navaneeth Bodla, Bharat Singh, Rama Chellappa, and Larry S. Davis. Soft-NMS – improving object detection with one line of code. In ICCV , 2017. [3] Benedikt Boecking, Naoto Usuyama, Shruthi Bannur, Daniel C. Castro, Harshita Schwaab, et al. MS-CXR: Making the most of text semantics to improve biomedi-cal vision–language processing. In ECCV , 2022. [4] Bowen Cheng, Ishan Misra, Alexander G. Schwing, Alexander Kirillov, and Rohit Girdhar. Masked-attention mask transformer for universal image segmentation. In
> CVPR , 2022. [5] Ming Dai, Wenxuan Cheng, Jiang-jiang Liu, Sen Yang, Wenxiao Cai, Yanpeng Sun, and Wankou Yang. DeRIS: Decoupling perception and cognition for enhanced refer-ring image segmentation through loopback synergy. arXiv preprint arXiv:2507.01738 , 2025. [6] Ming Dai et al. C3VG: Coarse-to-fine consistency for visual grounding. In AAAI , 2025. [7] Luyang Fan et al. M3D: Advancing 3d medical image analysis with multi-modal large language models. arXiv preprint arXiv:2404.00578 , 2024. [8] Guang Feng, Zhiwei Hu, Lihe Zhang, and Huchuan Lu. Encoder fusion network with co-attention embedding for referring image segmentation. In CVPR , 2021. [9] Yonatan Geifman and Ran El-Yaniv. Selective classifica-tion for deep neural networks. In NeurIPS , 2017. [10] Yonatan Geifman and Ran El-Yaniv. SelectiveNet: A deep neural network with an integrated reject option. In ICML ,2019. [11] Francisco Girbal, Yasser Dawoud, Konstantinos Kamnit-sas, and Ben Glocker. Segment, select, correct: A frame-work for weakly-supervised referring segmentation. arXiv preprint arXiv:2310.13479 , 2023. [12] Torkel Hafting, Marianne Fyhn, Sturla Molden, May-Britt Moser, and Edvard I. Moser. Microstructure of a spatial map in the entorhinal cortex. Nature , 436:801–806, 2005. [13] Kaiming He, Xiangyu Zhang, Shaoqing Ren, and Jian Sun. Spatial pyramid pooling in deep convolutional networks for visual recognition. In ECCV , 2014. [14] Jan Hosang, Rodrigo Benenson, and Bernt Schiele. Learn-ing non-maximum suppression. In CVPR , 2017.
> 13 OdaxAI Research

[15] Shaofei Huang, Tianrui Hui, Si Liu, Guanbin Li, Yunchao Wei, Jizhong Han, Luoqi Liu, and Bo Li. Referring image segmentation via cross-modal progressive comprehension. In CVPR , 2020. [16] Zhaojin Huang, Lichao Huang, Yongchao Gong, Chang Huang, and Xinggang Wang. Mask scoring R-CNN. In 

CVPR , 2019. [17] Tianrui Hui, Si Liu, Shaofei Huang, Guanbin Li, Sansi Yu, Faxi Zhang, and Jizhong Han. Linguistic structure guided context modeling for referring image segmentation. In 

ECCV , 2020. [18] Borui Jiang, Ruixuan Luo, Jiayuan Mao, Tete Xiao, and Yuning Jiang. Acquisition of localization confidence for accurate object detection. In ECCV , 2018. [19] Alexander Kirillov, Eric Mintun, Nikhila Ravi, Hanzi Mao, Chloe Rolber, Laura Gustafson, Tete Xiao, Spencer White-head, Alexander C. Berg, Wan-Yen Lo, Piotr Dollár, and Ross Girshick. Segment anything. In ICCV , 2023. [20] Tsung-Yi Lin, Priya Goyal, Ross Girshick, Kaiming He, and Piotr Dollár. Focal loss for dense object detection. In ICCV ,2017. [21] Ze Liu, Yutong Lin, Yue Cao, Han Hu, Yixuan Wei, Zheng Zhang, Stephen Lin, and Baining Guo. Swin transformer: Hierarchical vision transformer using shifted windows. In 

ICCV , 2021. [22] Gengchen Mai, Krzysztof Janowicz, Bo Yan, Rui Zhu, Ling Cai, and Ni Lao. Multi-scale representation learning for spatial feature distributions using grid cells (Space2Vec). In ICLR , 2020. [23] Junhua Mao, Jonathan Huang, Alexander Toshev, Oana Camburu, Alan Yuille, and Kevin Murphy. Generation and comprehension of unambiguous object descriptions. In 

CVPR , 2016. [24] Edvard I. Moser and May-Britt Moser. Grid cells and corti-cal representation. Nature Reviews Neuroscience , 15:466– 481, 2014. [25] Alec Radford, Jong Wook Kim, Chris Hallacy, Aditya Ramesh, Gabriel Goh, Sandhini Agarwal, Girish Sastry, Amanda Askell, Pamela Mishkin, Jack Clark, Gretchen Krueger, and Ilya Sutskever. Learning transferable visual models from natural language supervision. In ICML , 2021. [26] Rylan Schaeffer, Mikail Khona, Teng Ma, Catalina Eyza-guirre, Sanmi Koyejo, and Ila R. Fiete. Self-supervised learning of representations for space generates multi-modular grid cells. In NeurIPS , 2023. [27] Wenhui Wang, Hangbo Bao, Li Dong, Johan Bjorck, Zhil-iang Peng, Qiang Liu, Kriti Aggarwal, Owais Khan Mo-hammed, Saksham Singhal, Subhojit Som, and Furu Wei. BEiT-3: Image as a foreign language: BEiT pretraining for vision and vision-language tasks. In CVPR , 2023. [28] Zhaoqing Wang, Yu Lu, Qiang Li, Xunqiang Tao, Yandong Guo, Mingming Gong, and Tongliang Liu. CRIS: CLIP-driven referring image segmentation. In CVPR , 2022. [29] Linhui Xiao et al. OneRef: Unified one-tower expression grounding and segmentation with mask referring modeling. In NeurIPS , 2024. [30] Zhao Yang, Jiaqi Wang, Yansong Tang, Kai Chen, Heng-shuang Zhao, and Philip H. S. Torr. LAVT: Language-aware vision transformer for referring image segmentation. In CVPR , 2022. [31] Licheng Yu, Patrick Poirson, Shan Yang, Alexander C. Berg, and Tamara L. Berg. Modeling context in referring expressions. In ECCV , 2016. 

> 14

OdaxAI Research 

## A Extended Qualitative Results 

Figure 16: Top-12 failure cases on RefCOCO val. Each cell: default query (left, red overlay) vs. best query (right, green overlay). Ground-truth contour in yellow; IoU gap at top. A correct mask exists among the candidates but is not selected by the default heuristic. 

> 15 OdaxAI Research

Figure 17: Extended RefCOCO qualitative gallery (val, 10 additional examples). Input + expression → ground truth (green) →

default query (red, with IoU) → Venice-H1 (blue, with IoU and gain). 

> 16 OdaxAI Research

Figure 18: Zero-shot medical re-ranking (no fine-tuning). Top: MS-CXR chest X-rays; bottom: M3D-RefSeg-2D 3D medical slices. Default (red), Venice-H1 re-ranked (green), best-query upper bound (blue). 

Figure 19: Per-sample IoU comparison on medical data. Default (red), Venice-H1 (green), oracle (blue). 

Figure 20: Grid signatures on medical data. Multi-scale activations for Q0 (cols 1–3) and re-ranked query (col 4).