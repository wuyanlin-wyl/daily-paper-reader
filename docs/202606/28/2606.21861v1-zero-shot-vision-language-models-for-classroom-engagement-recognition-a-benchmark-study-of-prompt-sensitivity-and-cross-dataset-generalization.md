---
title: "Zero-Shot Vision-Language Models for Classroom Engagement Recognition: A Benchmark Study of Prompt Sensitivity and Cross-Dataset Generalization"
title_zh: 零样本视觉-语言模型用于课堂参与度识别：提示敏感性与跨数据集泛化的基准研究
authors: "Aman Goyal, Kshama Nitin Shah, Kemmannu Vineet Venkatesh Rao"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21861v1"
tags: ["query:vl"]
score: 9.0
evidence: 零样本视觉语言模型在参与度识别上的基准测试
tldr: "本研究首次系统评估了五种视觉-语言模型（CLIP、BLIP-VQA、GPT-4o、LLaVA-1.5-7B、Qwen2.5VL-7B）在零样本条件下识别课堂参与度的能力，基于DAiSEE（单个学生视频）和SCB（场景级图像）两个数据集，测试三种提示词变体。结果显示：个体学生识别近乎随机（kappa<0.10）、严重的类别坍缩（85-100%预测集中于单一水平）以及极端的提示敏感性（准确率波动达32%）。但在场景级分类中，CLIP和GPT-4o配合行为锚定提示可达到kappa≈0.60。GPT-4o的安全过滤器拒绝了98%涉及学生面部的链式思考请求。研究为教育观察系统中使用VLM提供了校准基线和关键设计考量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21861v1/fig-001.webp\", \"caption\": \"Figure 5. t-SNE projection of CLIP ViT-B/32 image embeddings for all 300 DAiSEE frames, coloured by ground-truth engagement label. The four engagement classes intermix within every cluster, confirming that CLIP organises frames by subject identity rather than engagement state.\", \"page\": 6, \"index\": 1, \"width\": 470, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2606.21861v1/fig-002.webp\", \"caption\": \"Table 2. Full prompt-sensitivity results on DAiSEE. Pred. Dist. shows the fraction of predictions in each class (GT: 1%–28%–35%– 35%).\", \"page\": 4, \"index\": 2, \"width\": 492, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2606.21861v1/fig-003.webp\", \"caption\": \"Figure 3. Fraction of the 300 DAiSEE samples predicted at each engagement level, for every model-prompt combination and the ground truth distribution. Warm colours indicate high concentration. Each model collapses to 1–2 dominant classes, confirming class collapse rather than genuine multi-class discrimination.\", \"page\": 5, \"index\": 3, \"width\": 480, \"height\": 546}]"
motivation: 零样本视觉-语言模型在自动化课堂参与度识别中的适用性尚未被充分探索。
method: 在DAiSEE和SCB数据集上，对五种VLM使用三种提示变体（最小、评分锚定、链式思考）进行基准测试。
result: 个体分类表现差（kappa<0.10），场景分类表现较好（kappa≈0.60），存在严重的提示敏感性和类别坍缩。
conclusion: 当前零样本VLM不适用于个体学生参与度识别，但场景级分类有潜力；设计时需考虑提示工程和安全限制。
---

## 摘要
自动化课堂参与度识别对于可扩展的学习分析具有重大前景，但在零样本条件下，现代视觉-语言模型（VLM）对此任务的适用性尚未得到充分探索。我们提出了一个系统基准，评估了五种广泛使用的VLM：CLIP、BLIP-VQA、GPT-4o、LLaVA-1.5-7B和Qwen2.5VL-7B-Instruct，在两个互补的教育数据集上：DAiSEE（个体学生视频数据集，300个采样测试片段）和学生课堂行为数据集（SCB，1168张场景级图像）。每个模型使用三种提示变体进行测试，涵盖最小提示、基于评分标准的提示和思维链提示。我们的实验揭示了零样本VLM在参与度识别中的三个主要失败模式：（1）个体学生上的表现接近随机，DAiSEE上的Cohen's kappa从未超过0.10；（2）严重的类别坍缩，模型将85-100%的预测分配给同一个参与度水平，而不考虑视觉内容；（3）极端的提示敏感性，相同图像上的准确率波动高达32个百分点，仅取决于提示措辞。值得注意的是，SCB上的场景级分类明显更易处理：当使用基于行为的评分标准提示时，CLIP和GPT-4o的kappa达到约0.60。我们还记录了一个实际部署障碍：GPT-4o的安全过滤器拒绝了98%涉及个体学生面部的思维链请求。我们的研究结果为在教育观察系统中使用VLM提供了校准的基线和关键设计考量。

## Abstract
Automated classroom engagement recognition holds substantial promise for scalable learning analytics, yet the suitability of modern Vision-Language Models (VLMs) for this task under zero-shot conditions remains largely unexplored. We present a systematic benchmark that evaluates five widely-used VLMs: CLIP, BLIP-VQA, GPT-4o, LLaVA-1.5-7B, and Qwen2.5VL-7B-Instruct across two complementary educational datasets: DAiSEE, an individual-student video dataset (300 sampled test clips), and the Student Classroom Behaviour dataset (SCB, 1,168 scene-level images). Each model is probed with three prompt variants spanning minimal, rubric-anchored, and chain-of-thought designs. Our experiments reveal three primary failure modes of zero-shot VLMs for engagement recognition: (1) near-random performance on individual students, with Cohen's kappa never exceeding 0.10 on DAiSEE; (2) severe class collapse, where models assign 85-100% of predictions to a single engagement level regardless of visual content; and (3) extreme prompt sensitivity, with accuracy swings of up to 32 percentage points on identical images depending solely on prompt phrasing. Remarkably, scene-level classification on SCB is substantially more tractable: CLIP and GPT-4o achieve kappa approximately 0.60 when prompted with behaviorally-grounded rubrics. We also document a practical barrier for deployment: GPT-4o's safety filters reject 98% of chain-of-thought requests involving individual student faces. Our findings provide a calibrated baseline and surface critical design considerations for the use of VLMs in educational observation systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：自动化课堂参与度识别对于大规模学习分析至关重要，但传统监督方法需要大量标注数据，难以跨情境迁移。现代视觉-语言模型（VLM）支持零样本预测，有望作为即插即用的教育观察工具。然而，VLM在零样本条件下对于课堂参与度识别的适用性、提示敏感性及跨数据集泛化能力尚未被系统研究。
- **整体含义**：本文首次为五种代表性VLM提供了系统的零样本基准，揭示了其在个体学生水平的严重失败模式（近随机性能、类别坍缩、极端提示敏感性），同时发现场景级识别更可行，为教育AI系统设计提供了关键约束和方向。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在零样本设置下，直接使用预训练VLM对课堂图像进行参与度分类，不进行任何微调，仅通过设计不同提示来探索模型能力。
- **模型选择**：
  - **CLIP (ViT-B/32)**：判别式对比模型，通过计算图像嵌入与文本模板的余弦相似度分类。
  - **BLIP-VQA**：判别式VQA模型，输入图像+问题，生成自由文本回答后映射到类别。
  - **GPT-4o**：闭源多模态生成模型，通过API调用，温度为0.0（主运行）和0.7（一致性运行）。
  - **LLaVA-1.5-7B**：开源指令微调模型，本地4-bit量化运行（约4GB内存），温度设置同GPT-4o。
  - **Qwen2.5-VL-7B-Instruct**：开源VLM，bfloat16精度加载，温度跟随协议。
- **提示设计（三种变体）**：
  - **P1（最小提示）**：仅提供四分类数值标度，无额外指导。
  - **P2（评分锚定提示）**：补充每类别的具体行为描述（如“看向别处、打哈欠”对应0级）。
  - **P3（思维链提示）**：要求模型先描述面部表情和身体姿势，再给出评分。
- **评估指标**：准确率、F1 Macro、Cohen's κ（加权二次）、MSE；同时报告预测分布、混淆矩阵、特征空间分析（t-SNE、余弦相似度分布）。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **DAiSEE**：个体学生视频数据集，采集112名受试者在在线学习中的10秒片段，标注四类参与度。本文从测试集中分层采样300个片段（保留全部0级和1级样本，各采样106个2、3级样本），仅使用第5秒的静态帧。
  - **SCB（学生课堂行为数据集）**：教室场景图像，包含1168张验证图像，每个图像有逐学生边界框和六类行为标注。本文通过聚合策略将行为映射到四类参与度，形成场景级标签（分布高度不平衡：54.5%为3级，1.9%为0级）。
- **基准方法**：对比文献中的监督方法：ResNet+TCN（58.8%准确率）、Huang等人（62.3%准确率，κ=0.37）、CavT（MSE=0.038）。注意这些监督方法在完整测试集上报告，而VLM评估在子样本上，本文仅作趋势对比。
- **对比方法**：五种VLM在相同提示下比较；同时分析不同提示下的性能差异。

### 4. 资源与算力

- 论文未明确说明训练所用GPU型号、数量和时长（因为所有模型均为零样本，无需训练）。
- 本地部署的说明：
  - LLaVA-1.5-7B：4-bit量化，本地机器16GB RAM，通过Ollama服务。
  - Qwen2.5-VL-7B：bfloat16精度，通过Hugging Face Transformers库。
  - GPT-4o：通过OpenAI API，图像以base64编码，使用“低”细节分辨率。
- 未提供具体推理时间或计算量统计。

### 5. 实验数量与充分性

- **实验组数**：
  - 主要评估了5个模型 × 3种提示 × 2个数据集 = 30个配置（每个配置在DAiSEE的300样本和SCB的1168样本上运行）。
  - 自一致性分析：对GPT-4o和LLaVA在最优提示下重复3次（温度0.7）。
  - 特征空间分析：CLIP的t-SNE投影和逐类余弦相似度分布。
- **充分性与客观性**：
  - 实验覆盖了不同架构（对比式/生成式、开源/闭源）、不同提示类型，以及两个差异化的数据集（个体 vs 场景）。
  - 指标全面（包括Cohen's κ、F1 Macro等），并报告了预测分布等细粒度信息。
  - 局限性：仅使用单帧静态图像，未利用时间信息；SCB标签通过行为映射间接得到，可能不完全反映真实参与度；DAiSEE样本量较小（300），主体仅21个；开源模型量化可能造成性能偏差。整体实验较为充分，但提示敏感性未提供鲁棒统计量（如多次采样）。

### 6. 论文的主要结论与发现

- **个体学生识别失败**：所有模型在DAiSEE上的Cohen's κ ≤ 0.10，准确率接近或低于多数类基线（35.3%），为近随机水平。
- **类别坍缩**：模型将85–100%的预测集中在单一类别（多为L2或L1），从不预测L3（高度参与）。
- **极端提示敏感性**：同一模型相同图像因提示不同准确率波动可达32.3个百分点（LLaVA在DAiSEE上），提示工程不可靠。
- **场景级识别可行**：SCB上CLIP（κ=0.60）和GPT-4o（κ=0.58）在使用评分锚定或情感模板时表现良好，接近“中等一致性”。
- **GPT-4o安全限制**：98%的个体级思维链请求被安全过滤器拒绝（涉及面部描述），阻碍实际部署。
- **自一致性差**：生成式VLM在温度0.7下仅68–73%的图像在三轮中预测一致，单个运行的κ不可靠。

### 7. 优点

- **系统性基准**：首次对多种VLM在零样本教育参与度识别上进行统一评估，覆盖不同模型类型和提示设计。
- **深入诊断**：通过特征空间分析（t-SNE、余弦相似度）揭示了失败的根本原因（特征空间按身份而非参与度组织，文本-图像相似度范围极窄）。
- **关注实际部署问题**：明确指出了GPT-4o的安全过滤器、提示敏感性的实际影响以及自一致性问题。
- **设计启示**：提出“场景优先”范式，与教师实际观察方式一致，且隐私更优。

### 8. 不足与局限

- **仅限零样本**：未探索微调（如线性探测、LoRA）是否能缩小差距，错过评估VLM可适应性的机会。
- **忽略时间维度**：只使用单帧，而参与度是动态过程，文献表明时空特征有优势。
- **SCB标签间接性**：参与度标签从行为类别映射而来，可能不完全符合主观评级，结果泛化需谨慎。
- **数据集规模与多样性**：DAiSEE仅21个主体，SCB图像来源有限，跨学校、文化场景的泛化未验证。
- **模型量化影响**：LLaVA使用4-bit量化可能影响性能，与GPT-4o的对比不完全公平。
- **提示敏感性分析不够深入**：仅报告了波动值，未分析提示的语义空间或提供选择提示的鲁棒策略。

（完）
