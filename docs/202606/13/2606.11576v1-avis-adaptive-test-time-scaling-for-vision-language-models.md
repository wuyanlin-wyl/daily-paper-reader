---
title: "AVIS: Adaptive Test-Time Scaling for Vision-Language Models"
title_zh: AVIS：面向视觉语言模型的自适应测试时缩放
authors: "Ahmadreza Jeddi, Minh Ngoc Le, Amirhossein Kazerouni, Hakki Can Karaimer, Hue Nguyen, Iqbal Mohomed, Michael Brudno, Alex Levinshtein, Konstantinos G. Derpanis, Babak Taati, Radek Grzeszczuk"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11576v1"
tags: ["query:vl"]
score: 9.0
evidence: 提出AVIS自适应缩放机制用于视觉语言模型
tldr: 现代视觉语言模型（VLM）在链式思维和测试时缩放中获益，但视觉上下文和大解码链导致高推理成本。现有方法通常优化一个轴（视觉上下文缩放VCS或视觉推理缩放VRS），缺少联合分配。本文提出自适应视觉推理缩放（AVIS），通过键多样性视觉剪枝（KDV）和自适应自一致性（利用难度预测器选择推理次数），实现VCS和VRS的轻量联合自适应。在多种基准上，AVIS相比单轴基线改善了准确率-计算权衡，对RL后训练的VLM依然有效，且兼容共享预填充推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11576v1/fig-001.webp\", \"caption\": \"Figure 4: Distribution of predicted rollout counts K. Histogram of the learned difficulty-aware policy’s K assignments on representative image and video benchmarks. Because the binning rule reflects the inverted-U marginal utility of majority voting, K=1 is assigned at both the easy and very-hard extremes, while K=5 and K=7 are reserved for intermediate hard-but-solvable examples. The resulting distribution is dataset-dependent, shifting from K=1-dominated on MMBench to K=5-dominated on Video-TT.\", \"page\": 21, \"index\": 1, \"width\": 814, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2606.11576v1/fig-002.webp\", \"caption\": \"Table 1: Performance across inference strategies. We compare VRS-only scaling, KDV-based VCS pruning, fixed joint VCS–VRS configurations, and AVIS, which adaptively selects both ρ and K at test time. AVIS achieves the best overall compute–performance trade-off across 12 image and 6 video benchmarks. #F denotes FLOPs relative to Vanilla (ρ=0,K=1); lower #F is better.\", \"page\": 7, \"index\": 2, \"width\": 824, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2606.11576v1/fig-003.webp\", \"caption\": \"Table 8: KDV pruning on Qwen-2.5-VL-Instruct. We report absolute scores and percentages relative to the full-token (Vanilla) baseline. For this ablation, we do not enforce the <think> prompting format. Results are shown on GQA [85], MMBench-DEV-EN [63], MME [61] (perception+reasoning sum), POPE [65] (overall accuracy), ScienceQA-Image [82], CV-Bench-2D [64], SeedBench-Image [81], and MMStar [62]. We compare against Vanilla and representative trainingfree pruning baselines: FastV [10] (K=2), VisionZip [44] (dominant tokens = 22%, context = 3%), PDrop [42] (L=[8, 16, 24], R=0.5), and KeyDiff [54].\", \"page\": 22, \"index\": 3, \"width\": 823, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2606.11576v1/fig-004.webp\", \"caption\": \"Figure 2: Adaptive Visual Inference Scaling (AVIS). Given a query x = (I,Q), we first apply an adaptive, training-free key-diversity pruning rule to remove redundant visual tokens and produce a compact visual prefix. We then run self-consistency with K shared-prefill rollouts and aggregate answers by majority vote. A lightweight difficulty predictor estimates per-sample difficulty and maps it to an appropriate rollout count K, enabling adaptive compute allocation across the workload.\", \"page\": 3, \"index\": 4, \"width\": 822, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2606.11576v1/fig-005.webp\", \"caption\": \"Figure 5: Adaptive KDV pruning. Qualitative examples of our adaptive KDV policy selecting different pruning ratios ρ per image. In each pair, the left panel shows the original image and the right panel shows the retained visual tokens after pruning. Images are from \\\"Are We on the Right Way for Evaluating Large Vision-Language Models?\\\" by Chen et al., published in NeurIPS 2024.\", \"page\": 23, \"index\": 5, \"width\": 606, \"height\": 806}, {\"url\": \"assets/figures/arxiv/2606.11576v1/fig-006.webp\", \"caption\": \"Table 4: AVIS on RL post-trained VLMs. We compare each checkpoint under Vanilla, fixed joint scaling (ρ=75%,K=5), and AVIS with adaptive ρ and K. Across 6 image benchmarks, AVIS preserves or improves accuracy with substantially lower FLOPs, showing that adaptive VCS–VRS allocation transfers to RL post-trained VLMs.\", \"page\": 8, \"index\": 6, \"width\": 825, \"height\": 329}]"
motivation: 现有方法单独优化视觉上下文或推理缩放，未探索两者的联合计算分配，导致效率欠佳。
method: AVIS采用键多样性视觉剪枝（KDV）在预填充前移除冗余视觉令牌，并通过学习难度预测器自适应选择推理自一致性次数。
result: 在图像和视频推理基准上，AVIS提升了准确率-计算权衡，且在强化学习后训练的VLM上保持高效。
conclusion: AVIS是一种部署友好、兼容共享预填充的轻量策略，能有效联合分配计算资源。
---

## 摘要
现代视觉语言模型（VLM）受益于思维链提示和测试时缩放，但这些增益往往由于大量的视觉上下文和长的解码链而带来高昂的推理成本。我们从两个耦合的维度审视这一成本：视觉上下文缩放（VCS），它控制传递给语言模型的视觉证据量；以及视觉推理缩放（VRS），它控制推理时间内搜索的计算量。现有方法通常一次只优化一个维度，而跨这些维度的计算联合分配尚未得到充分探索。我们引入自适应视觉推理缩放（AVIS），这是一种轻量级策略，能够根据每个查询自适应调整VCS和VRS。AVIS通过关键多样性视觉（KDV）剪枝实现VCS，这是一种无需训练的$O(N)$基于键的规则，用于在预填充前移除冗余视觉标记；并通过自适应自一致性实现VRS，使用学习到的难度预测器来选择推理展开的次数。AVIS部署友好，兼容共享预填充推理，其中所有展开复用单个预填充阶段和KV缓存。在多样化的图像和视频推理基准上，AVIS相比仅VCS和仅VRS的基线提升了准确率-计算权衡，并在RL后训练的VLM上保持有效，同时保持较低的计算量和延迟。

## Abstract
Modern Vision-Language Models (VLMs) benefit from chain-of-thought prompting and test-time scaling, but these gains often come with prohibitive inference cost due to large visual contexts and long decoding chains. We view this cost through two coupled axes: Visual Context Scaling (VCS), which controls how much visual evidence is passed to the language model, and Visual Reasoning Scaling (VRS), which controls how much inference-time reasoning search is performed. Existing methods typically optimize one axis at a time, leaving the joint allocation of compute across these axes underexplored. We introduce Adaptive Visual Inference Scaling (AVIS), a lightweight policy that adapts both VCS and VRS per query. AVIS realizes VCS through Key Diversity Visual (KDV) pruning, a training-free $O(N)$ key-based rule for removing redundant visual tokens before prefilling, and realizes VRS through adaptive self-consistency, using a learned difficulty predictor to select the number of reasoning rollouts. AVIS is deployment-friendly and compatible with shared-prefill inference, where all rollouts reuse a single prefilling pass and KV cache. Across diverse image and video reasoning benchmarks, AVIS improves the accuracy--compute trade-off relative to VCS-only and VRS-only baselines, and remains effective on top of RL post-trained VLMs while keeping compute and latency low.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现代视觉语言模型（VLM）通过思维链（CoT）提示和测试时缩放显著提升了推理能力，但伴随高昂的推理成本——高分辨率图像/视频产生大量视觉上下文（大序列长度），CoT推理需要长解码链或多轮采样（如Best-of-N、自一致性）。现有方法通常**独立优化**两个关键维度：**视觉上下文缩放（VCS）**（控制传递给语言模型的视觉证据量）和**视觉推理缩放（VRS）**（控制推理时搜索的算力投入），而忽视了它们之间的**联合分配**与交互耦合关系。
- **动机**：如何根据每个输入实例（图像/视频+问题）**自适应地**在“看”（减少冗余视觉令牌）和“想”（增加推理展开次数）之间分配计算资源，从而在提升准确率的同时控制整体推理成本，是当前VLM部署面临的核心挑战。
- **研究意义**：提出一个统一的视角，将VCS和VRS视为耦合的推理时资源，通过轻量级策略AVIS实现自适应联合分配，显著改善精度-计算权衡，且兼容共享预填充推理（共享KV缓存）。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将每个查询的推理配置表示为 `θ = (ρ, K)`，其中 `ρ` 是视觉令牌剪枝比例（控制VCS），`K` 是自一致性推理展开的次数（控制VRS）。目标是找到一个样本依赖的策略 `π(x)`，在总计算预算约束下最大化准确率。
- **技术细节**：
  - **VCS实现：Key Diversity Visual (KDV) 剪枝**  
    - 一种**无需训练**的、基于注意力键（Key）多样性的令牌选择规则，在预填充前运行，时间复杂度O(N)。  
    - 对每个视觉令牌，计算其与各注意力头平均键方向的余弦相似度（取负值作为分数），分数越高表示该令牌越不冗余。保留分数超过阈值 `τ`（实验中取 π/4）的令牌，从而实现每个样本的自适应剪枝比例 `ρ(x)`。
  - **VRS实现：自适应自一致性**  
    - 使用**自一致性**（self-consistency）：对每个样本采样K个独立的CoT推理轨迹，通过多数投票聚合答案。  
    - 训练一个**轻量级难度预测器**（由1D卷积+GroupNorm+SiLU+全局平均池化+MLP组成），输入视觉编码器的嵌入，输出预测的“可解性”概率 `ˆp(x)`。  
    - 根据预测概率将样本映射到离散的K值（1/3/5/7），映射规则呈倒U型：对中间难度（hard-but-solvable）分配较大K，对极容易或极难样本分配较小K。边界值通过校准集全局计算预算选定。
  - **共享预填充推理**：所有K次展开复用一次视觉特征计算和预填充KV缓存，使得增加K主要增加解码计算，而不重复视觉编码和预填充，显著降低边际成本。
- **整体流程**：输入 (I, Q) → 视觉编码器得到视觉嵌入 → KDV自适应剪枝保留关键令牌 → 难度预测器预测 `ˆp(x)` → 根据映射规则确定K → 执行K次共享预填充的自一致性推理 → 多数投票输出最终答案。

## 3. 实验设计
- **使用的数据集/场景**：
  - **图像基准（12个）**：数学推理（MathVista, MathVerse, MathVision）、OCR（DocVQA）、多学科推理（MMMU-Pro）、通用VQA（MME, MMStar, MMBench, CVBench-2D, POPE, BLINK, TreeBench）。
  - **视频基准（6个）**：Video-MME, TempCompass, Video-TT, MVBench, Q-Bench-Video, Video-MMMU。
- **对比方法**：
  - **Vanilla基线**：无剪枝，K=1。
  - **VRS-only**：固定K=3/5/7，无剪枝。
  - **VCS-only**：固定剪枝比例ρ=75%，K=1。
  - **固定联合配置**：ρ=75%, K=5等。
  - **其他剪枝方法**：FastV, VisionZip, PDrop, KeyDiff（仅消融KDV时）。
  - **RL后训练模型**：VL-Rethinker, Vision-R1, OpenVLThinker。
- **评估指标**：准确率（或分数）+ 归一化FLOPs（相对于Vanilla）。延迟（wall-clock time）。

## 4. 资源与算力
- **硬件**：单块NVIDIA L40 GPU（48 GB内存），用于延迟测量。
- **推理框架**：使用vLLM v0.10.0实现共享预填充。
- **训练难度预测器**：使用5000个样本的校准集（4000图像+1000视频），标注通过运行Kmax=10次自一致性获得。预测器本身训练开销很小（文中未给出具体时长，但提到其FLOPs仅占总推理的<0.1%）。
- **算力说明**：论文未明确报告训练VLM或RL后训练模型的算力，但强调AVIS本身是轻量级的。

## 5. 实验数量与充分性
- **实验数量**：非常充分。
  - 主实验：Table 1涵盖12个图像+6个视频基准，对比了Vanilla、VRS-only、VCS-only、固定联合及AVIS共5种策略。
  - 匹配FLOPs比较：Table 2。
  - 延迟测量：Table 3。
  - RL后训练VLM：Table 4（4个模型，6个基准）。
  - 消融实验：自适应vs固定分配（Table 5）、VCS-VRS权衡热力图（Fig 3）、难度预测器架构/大小消融（Table 7、Fig 4）、KDV与其他剪枝方法对比（Table 8）、自适应剪枝可视化（Fig 5）。
- **充分性与公平性**：
  - 基准覆盖广泛（数学、OCR、VQA、视频推理等），且统一使用Qwen2.5-VL-7B作为骨干，确保对比公平。
  - 实验设计考虑了不同计算预算下的比较（匹配FLOPs），以及自适应策略在各轴上的单独消融。
  - 对RL后训练模型的评估表明AVIS具有泛化性。
  - 不足：未给出多次运行的误差棒（受限于计算成本），但多数结果趋势一致。

## 6. 论文的主要结论与发现
1. **联合VCS-VRS优于单轴优化**：固定联合配置（ρ=75%, K=5）已经能在部分基准上以更少的FLOPs达到比Vanilla更高的准确率。
2. **AVIS实现最佳权衡**：相比Vanilla，AVIS在图像基准上平均准确率提升3%，计算减少52%；视频基准上准确率提升2.4%，计算减少66%。相比最佳固定联合配置（ρ=75%, K=5），AVIS在计算减少40%的同时准确率更高。
3. **自适应分配带来非平凡收益**：在匹配FLOPs条件下，AVIS比固定联合策略平均高3.7%。
4. **共享预填充降低并行搜索成本**：实际延迟测量显示，AVIS（757秒）优于Vanilla（795秒）和固定联合（789秒），同时准确率更高。
5. **对RL后训练模型有效**：在VL-Rethinker、Vision-R1等模型上，AVIS同样能在大幅降低FLOPs（约40-60%）的同时保持或提升准确率。
6. **KDV剪枝有效保留关键视觉信息**：在75%剪枝率下，KDV平均保留97.99%的Vanilla准确率，优于其他训练无关剪枝方法。

## 7. 优点
- **方法论亮点**：
  - 首次将VCS和VRS作为耦合轴统一建模，提出联合自适应分配。
  - 训练无关的KDV剪枝（O(N)）高效且部署友好。
  - 难度预测器利用视觉嵌入在预填充前决策，与共享预填充天然兼容。
  - 自一致性的倒U型边际收益被显式利用，避免浪费计算在极难/极易样本上。
- **实验亮点**：
  - 覆盖面广（图像+视频共18个基准，含数学、OCR、VQA、视频理解等）。
  - 对比基线全面（单轴、固定联合、其他剪枝方法、RL后训练模型）。
  - 提供多种消融（轴别自适应、预测器架构、剪枝策略、预算边界等）和延迟实测。
  - 考虑了计算公平性（匹配FLOPs比较）。

## 8. 不足与局限
- **解耦决策**：VCS和VRS当前是顺序执行的独立决策，未建模两者的交互（例如剪枝后样本难度可能改变，进而影响最优K值）。论文承认这是一个开放问题。
- **难度预测器仅使用视觉嵌入**：未利用文本问题信息，可能降低预测精度。若使用LM表示则需要部分预填充，影响计算节省。
- **未与其他测试时缩放轴组合**：如顺序细化、Tree搜索、Best-of-N等。仅使用自一致性一种VRS实现。
- **校准集依赖**：需要一份代表性的校准集来训练预测器和确定预算边界；分布漂移下可能需重新校准。
- **实验误差棒缺失**：由于计算成本，未报告多次运行的统计误差。但多数对比趋势明显，影响有限。
- **计算资源限制**：仅使用单块L40 GPU和Qwen2.5-VL-7B，可能无法完全代表更大模型或分布式部署场景。

（完）
