---
title: "++nnU-Net: Scaling nnU-Net with Prefix-Based Data Augmentation"
title_zh: "++nnU-Net: 基于前缀的数据增强扩展nnU-Net"
authors: "Ana Sofia Santos, André Ferreira, Gijs Luijten, Naida Solak, Lisle Faray de Paiva, Behrus Hinrichs-Puladi, Jens Kleesiek, Jan Egger, Victor Alves"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10713v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 提出++nnU-Net用于医学分割，带数据增强
tldr: "nnU-Net在医学分割中表现优异，但依赖大量标注数据。++nnU-Net提出基于图像配准的数据增强模块，在预处理前生成变形图像和分割，提升数据多样性。在5个2D数据集上，Dice得分最高提升22%，证明配准增强在数据有限场景下的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10713v1/fig-001.webp\", \"caption\": \"Table 1: Overview of the datasets and their properties used in the study (*).\", \"page\": 3, \"index\": 1, \"width\": 964, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2606.10713v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the augmentation pipeline: (a) data are converted to NIfTI format, renamed, stored in respective image and segmentation directories and split in training and test cohorts; (b) initial stages of the data augmentation process; (c) iteration process between images to be registered and respective transformation to generate segmentation masks; (d) final step of conversion to 3D format before training by nnU-Net.\", \"page\": 3, \"index\": 2, \"width\": 979, \"height\": 633}, {\"url\": \"assets/figures/arxiv/2606.10713v1/fig-003.webp\", \"caption\": \"Table 2: Overall performance comparison of Baseline and ++nnU-Net on DSC and HD95.\", \"page\": 5, \"index\": 3, \"width\": 964, \"height\": 337}]"
motivation: 解决医学影像数据标注困难、获取受限的问题，通过数据增强提升分割性能。
method: 提出++nnU-Net，包含两阶段图像配准模块，在预处理和训练前生成变换图像及对应分割。
result: "在五个2D数据集上，Dice得分平均提升，最高达22%，优于nnU-Net基线。"
conclusion: 基于配准的数据增强能有效提升2D医学图像分割性能，++nnU-Net提供可扩展的解决方案。
---

## 摘要
nnU-Net在医疗分割任务中持续展现出成功，这高度依赖于注释生物医学数据的可用性和多样性。然而，由于隐私法规和注释成本等多种因素，收集医学影像数据集仍然具有挑战性。因此，数据增强在增加数据可用性同时保持解剖学可行性方面发挥着关键作用。为此，我们提出了++nnU-Net，一种基于图像配准的新型数据增强模块，它在预处理和训练之前运行。我们的框架在五个不同的二维数据集上进行了评估。在此工作流程中，图像数据经过两阶段配准过程，生成新的扭曲图像。然后，变换应用于相应的分割结果。此外，该流程计算可用磁盘空间，生成补充的二元合成掩膜并生成检查点。我们证明，++nnU-Net优于nnU-Net基线，在Dice相似系数得分上有所提升。在最显著的情况下，我们观察到性能提升约22%。这些发现凸显了基于配准的数据增强的有效性，特别是对于二维医学影像数据集，并表明++nnU-Net为在数据受限环境下提升分割性能提供了一种实用且可扩展的方法。++nnU-Net的源代码可在以下网址获取：https://github.com/sofia-adelie/plusplusnnunet.git

## Abstract
The nnU-Net has demonstrated continuous success in medical segmentation tasks, which heavily rely on the availability and diversity of annotated biomedical data. However, assembling medical imaging cohorts remains challenging due to numerous factors such as privacy regulations and annotation costs. As a result, data augmentation plays a crucial role in increasing data availability while maintaining anatomical feasibility. Hence, we propose the ++nnU-Net, a novel data augmentation module based on image registration that operates prior to preprocessing and training take place. Our framework was evaluated across five different 2D datasets. In this workflow, image data go through a two-stage registration process, generating new warped images. The transformations are then applied to the respective segmentation. In addition, the pipeline computes available disk space, generates supplementary binary synthetic masks and generates checkpoints. We demonstrate that the ++nnU-Net outperforms the nnU-Net baseline, yielding improvements in Dice Similarity Coefficient scores. In the most prominent cases, we observe performance gains of approximately 22\%. These findings highlight the effectiveness of registration-based data augmentation, particularly for 2D medical imaging datasets and suggest that the ++nnU-Net provides a practical and scalable approach for enhancing segmentation performance in data-limited settings. The source code for the ++nnU-Net is available at: https://github.com/sofia-adelie/plusplusnnunet.git

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
医学图像分割任务严重依赖于大量、多样化的标注数据，然而由于隐私法规、标注成本高昂等因素，获取大规模高质量数据集极具挑战。数据增强是缓解数据稀缺、提升模型泛化能力的重要手段。已有工作表明基于图像配准的非刚性增强在3D场景（如CT颅骨、脑肿瘤分割）中效果显著，但针对2D医学图像的系统性研究较少。本文旨在将这种配准增强方法扩展到2D，并集成到广泛使用的nnU-Net框架中，提出**++nnU-Net**，在预处理和训练之前对训练数据进行自动化的两阶段配准增强，以提升分割性能。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在标准nnU-Net之前增加一个模块化的“前缀式”数据增强管道，对原始训练图像进行基于图像配准的变形，生成新的训练样本及其对应的分割掩膜。
- **关键技术流程**：
  1. **数据格式统一**：将2D图像转换为NIfTI格式，按nnU-Net要求命名并分为80/20训练/测试集。
  2. **磁盘空间估算**：根据用户磁盘剩余空间（80%可用）和平均文件大小，估算可生成的增强样本数量上限。
  3. **临时合成掩膜生成**：在每幅图像中心创建一个半径为图像宽度75%的圆形二值掩膜，限制配准变形只发生在该区域内，避免边缘失真。
  4. **两阶段配准**：使用ANTs的`antsRegistrationSyNQuick.sh`脚本，依次进行刚性配准和可变形对称归一化（SyN）配准。每次迭代从训练集中随机选取一对图像（固定图与运动图）。
  5. **有效性检查**：最多尝试5次。条件：(a) 最大正交距离 ≤ 最小图像尺寸的一半；(b) 变形后图像中心仍在原图边界内。通过后保存并应用相同变换到分割掩膜。
  6. **质量检查**：若变换后分割掩膜为空（无标签），则删除对应图像和分割。
  7. **检查点恢复**：存储当前最高ID、配准对、已处理索引，支持中断后从断点继续增强。
  8. **最终处理**：将增强后的2D图像和分割转换为z=1的3D格式（nnU-Net v1需要），然后送入标准nnU-Net训练。

### 3. 实验设计
- **使用数据集**：5个公开2D数据集：
  - **PLMUS**（超声，99张）
  - **BUSI**（超声，647张，排除正常类和多个标注图像）
  - **ARCADE**（X光，1297张，二进制分割）
  - **PENGWIN**（X光，250张随机选取，多类分割含背景共4类）
  - **PDRD**（全景牙科X光，598张，二进制分割）
- **基准方法**：标准nnU-Net（v1）训练，无任何改动。
- **对比方案**：++nnU-Net分别生成1k和10k个增强样本（ARCADE因原始近1k仅测试10k），除数据增加外训练配置完全相同。
- **评估指标**：Dice相似系数（DSC）和Hausdorff距离（HD95）。

### 4. 资源与算力
- **GPU**：NVIDIA RTX A6000
- **框架**：PyTorch 2.4.1，CUDA 12.1
- **训练设置**：5折交叉验证，110个epoch
- **未明确说明**：GPU数量、单次训练时长、总计算量等信息。

### 5. 实验数量与充分性
- **实验组数**：总计5个数据集 × （baseline + 至少两个增强规模） ≈ 15组主要结果。PENGWIN为多类，每个类单独报告，因此表中包含多条记录。
- **消融分析**：无系统的消融实验（如不同配准策略、不同区域限制比例等）。仅对比了增强数量（1k vs 10k）的影响。
- **公平性与客观性**：训练超参数（batch size, patch size等）与baseline完全一致，差异仅来自数据增强，对比公平。但缺少对增强参数（如配准强度、掩膜尺寸）的敏感性分析。
- **覆盖广度**：涵盖两种模态（超声、X光）和多种解剖结构（胰腺、乳腺、冠脉、骨盆、牙齿），但未包括CT、MRI等模态，也未在3D数据上验证。

### 6. 论文的主要结论与发现
- ++nnU-Net在所有5个数据集上均取得了比baseline更高的DSC，最大提升达**22.08%**（PLMUS数据集，10k增强）。
- 对于初始性能较低的数据集（如PLMUS、ARCADE），提升尤为显著（22%和13.69%）。
- **HD95略有增加**，符合预期——形状变异性增加可能带来边界偏差。
- 对于X光数据集（PDRD、PENGWIN），存在一个最优增强数量：超过10k后DSC反而下降，提示需要平衡增强规模。
- 基于配准的增强在**小数据集**上效果最突出，有助于缓解数据稀缺问题。

### 7. 优点
- **创新性**：首次将基于配准的增强系统性地集成到nnU-Net中，并专为2D医学图像设计，填补了该领域在2D上的空白。
- **自动化与鲁棒性**：管道自动处理格式转换、磁盘估算、临时掩膜、有效性检查、中断恢复，降低用户使用门槛。
- **有效性验证充分**：在5个不同模态、不同大小的数据集上验证，结果一致且显著。
- **代码开源**：提供了完整源代码，便于复现和扩展。

### 8. 不足与局限
- **模态覆盖有限**：仅测试了超声和X光，未涉及CT、MRI等3D模态，而3D场景对医学分割更常见。
- **增强数量需调优**：未提供自动确定最优增强规模的方法，不同数据集可能需要手动尝试。
- **HD95略高**：边界精度有轻微下降，可能在某些要求高精度的任务中需权衡。
- **缺乏消融研究**：未分析不同配准类型（仅刚性 vs 仅SyN vs 两阶段）、掩膜尺寸、有效性阈值等参数的影响。
- **多类分割处理简单**：PENGWIN为多类但方法中未特殊处理，直接将变换应用于所有类，可能引入类间重叠或错位风险。
- **计算成本未评估**：配准过程本身耗时，且增强可能显著增加训练数据量，文中未比较训练时间或资源消耗。

（完）
