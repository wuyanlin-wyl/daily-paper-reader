---
title: "PRPO: Perception-Reinforced Policy Optimization via Token-Level Dynamic Advantage Reshaping"
title_zh: PRPO：通过令牌级动态优势重塑的感知增强策略优化
authors: "Qiming Li, Tianlun Li, Xiaolong Cheng, Hangyu Li, Ruiyan Gong, Kangning Niu, Kaitao Jiang, Mu Xu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08708v1"
tags: ["query:vl"]
score: 9.0
evidence: 大视觉语言模型的强化学习
tldr: "针对多模态推理中粗粒度轨迹级奖励导致关键感知token监督不足的问题，提出感知增强策略优化（PRPO）框架，通过鲁棒视觉依赖（RVD）指标识别感知关键token，并设计token级感知优势重塑（PAR）方法增强其学习信号。在7个多模态推理基准上，3B和7B模型分别平均提升23.3%和21.1%，实现SOTA且泛化性强。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08708v1/fig-001.webp\", \"caption\": \"Figure 1: Error analysis of multimodal reasoning failures using Qwen2.5-VL-7B on the MathVerse benchmark. Analysis results indicate that LVLMs are limited by perceptual bottlenecks within reasoning trajectories rather than insufficient reasoning capabilities.\", \"page\": 2, \"index\": 1, \"width\": 825, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-002.webp\", \"caption\": \"Table 1: Main results (Rollout = 8) on various benchmarks. The best results are bolded, and the second-best results are underlined. Comparison is performed separately within 3B and 7B groups.\", \"page\": 7, \"index\": 2, \"width\": 814, \"height\": 430}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-003.webp\", \"caption\": \"Table 5: Key hyperparameters for training and evaluation.\", \"page\": 14, \"index\": 3, \"width\": 540, \"height\": 765}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-004.webp\", \"caption\": \"Figure 5: Training dynamics.\", \"page\": 8, \"index\": 4, \"width\": 830, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of our proposed PRPO framework. PRPO consists of two core components: (i) Robust Visual Dependency (RVD), which identifies pivotal perceptual tokens through fine-grained trajectory analysis, and (ii) Perceptual Advantage Reshaping (PAR) algorithm, which applies a synaptic S-shaped curve to reshape uniform trajectory-level advantages, dynamically amplifying pivotal perceptual tokens while preserving stable optimization gradients for reasoning tokens.\", \"page\": 4, \"index\": 5, \"width\": 826, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-006.webp\", \"caption\": \"Table 6: Ablation study of heuristic metrics on Qwen2.5-VL-7B (Rollout = 8).\", \"page\": 17, \"index\": 6, \"width\": 744, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-007.webp\", \"caption\": \"Figure 6: Comparison of the generation results between PRPO the baseline using Qwen2.5-VL-7B.\", \"page\": 9, \"index\": 7, \"width\": 826, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-008.webp\", \"caption\": \"Table 3: Ablation study of key hyperparameters in Perceptual Advantage Reshaping (PAR) on Qwen2.5-VL-7B (Rollout = 8). The best results are bolded.\", \"page\": 9, \"index\": 8, \"width\": 798, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2606.08708v1/fig-009.webp\", \"caption\": \"Figure 3: Distribution of Visual Dependency (VD) and Visual Inconsistency (VI) on Qwen2.5-VL-7B.\", \"page\": 5, \"index\": 9, \"width\": 459, \"height\": 316}]"
motivation: 现有强化学习方法仅基于轨迹级奖励，无法有效区分因果依赖于视觉证据的稀疏感知token，导致其被语言先验或推理模板token淹没。
method: 提出RVD指标识别视觉依赖且扰动稳定的感知token，并设计PAR方法在保持非感知token梯度稳定的同时，放大感知token的优势信号。
result: "在7个多模态推理基准上，3B和7B模型分别平均提升23.3%和21.1%，达到SOTA并提升训练效率和跨任务泛化能力。"
conclusion: 细粒度token级信用分配对可扩展的多模态强化学习至关重要，PRPO通过显式强化感知token有效提升了LVLM的推理性能。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为提升大型视觉语言模型（LVLMs）推理能力的有效范式。然而，现有RLVR方法主要依赖于轨迹级结果奖励，为所有生成的令牌分配相同的学习信号。这种粗粒度的信用分配本质上与多模态推理不匹配，因为只有稀疏的子集令牌在因果上基于视觉证据。因此，这些关键的感知令牌受到弱监督，往往被语言先验或推理模板令牌所淹没。为了解决这一局限性，我们提出了感知增强策略优化（PRPO），这是一种令牌级强化学习框架，明确识别并加强长程多模态推理轨迹中的关键感知令牌。PRPO引入了鲁棒视觉依赖（RVD），这是一种原则性度量，用于识别其预测既基于视觉又具有扰动稳定性的令牌，过滤掉脆弱或噪声视觉令牌。基于RVD，我们进一步提出了感知优势重塑（PAR），这是一种令牌级信用分配技术，用于放大感知信息丰富的令牌，同时为非感知令牌保持稳定梯度。在七个多模态推理基准上的大量实验表明，PRPO在3B和7B模型规模上均持续优于强LVLM基线，分别实现了23.3%和21.1%的平均增益。PRPO以更高的训练效率和更强的跨任务泛化能力实现了最先进的性能。我们的发现强调了细粒度信用分配对于可扩展多模态强化学习的重要性。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become an effective paradigm for improving the reasoning capability of Large Vision-Language Models (LVLMs). However, existing RLVR methods primarily rely on trajectory-level outcome rewards, which assign identical learning signals across all generated tokens. This coarse-grained credit assignment is fundamentally mismatched to multimodal reasoning, where only a sparse subset of tokens is causally grounded in visual evidence. Consequently, these pivotal perceptual tokens receive weak supervision and are often overwhelmed by language priors or reasoning-template tokens. To address this limitation, we propose Perception-Reinforced Policy Optimization (PRPO), a token-level reinforcement learning framework that explicitly identifies and reinforces pivotal perceptual tokens within long-horizon multimodal reasoning trajectories. PRPO introduces Robust Visual Dependency (RVD), a principled metric that identifies tokens whose predictions are both visually grounded and perturbation-stable, filtering out brittle or noisy visual tokens. Based on RVD, we further propose Perceptual Advantage Reshaping (PAR), a token-level credit assignment technique that amplifies perceptually informative tokens while preserving stable gradients for non-perceptual tokens. Extensive experiments on seven multimodal reasoning benchmarks demonstrate that PRPO consistently outperforms strong LVLM baselines across both 3B and 7B model scales, achieving average gains of 23.3% and 21.1%, respectively. PRPO achieves state-of-the-art performance with improved training efficiency and stronger cross-task generalization. Our findings highlight the importance of fine-grained credit assignment for scalable multimodal reinforcement learning.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将根据您提供的论文文本，对《PRPO: Perception-Reinforced Policy Optimization via Token-Level Dynamic Advantage Reshaping》进行结构化、深入且客观的中文总结。

### 论文总结

#### 1. 核心问题与研究动机

*   **核心问题**：当前多模态推理大语言模型的强化学习方法存在严重的“**感知信用分配问题**”（Perceptual Credit Assignment Problem）。

*   **背景与动机**：
    *   现有 RLVR 方法（如 GRPO, DAPO）继承自纯文本领域，使用**轨迹级的结果奖励**，即所有生成的 token 获得相同的优化信号。
    *   然而，在多模态推理中，只有一小部分 token（如坐标、数值、几何线索）是因果依赖于视觉证据的“**关键感知 token**”，而绝大多数 token 由语言先验或推理模板驱动。
    *   论文通过实验分析（图1）证实，大多数推理失败并非源于推理能力不足，而是由中间步骤的**感知错误**（如看错区域、读错数字）导致的。轨迹级奖励无法有效识别和强化这些关键但稀疏的感知 token，导致其监督不足，被海量的语言 token 淹没。
    *   因此，核心挑战在于如何从长轨迹中精准定位并强化稀疏、关键的感知 token，实现比轨迹级更细粒度的信用分配。

#### 2. 方法论：PRPO 框架

PRPO 是一个动态的令牌级强化学习框架，旨在解决上述感知信用分配问题。其核心包含两大组件：

*   **组件一：鲁棒视觉依赖指标**
    *   **核心思想**：设计一个双指标协同的方法，精准识别**既具有强烈视觉依赖性，又对数值噪声鲁棒**的关键感知 token。
    *   **关键技术细节**：
        1.  **视觉依赖**：计算每个 token 在原始图像和**强扰动图像**（如随机黑块，严重破坏视觉信息）下的 KL 散度。值高表示 token 强烈依赖视觉证据。
        2.  **视觉不一致**：计算每个 token 在原始图像和**弱扰动图像**（如微小像素抖动，不改变语义）下的 KL 散度。值高表示 token 对数值噪声敏感，是“脆弱锚点”。
        3.  **鲁棒视觉依赖**：通过一个非线性信息过滤器，结合上述两个指标：
            *   `RVD = VD * ReLU(1 - VI / (VD + η))`
            *   其作用是：**放大**那些 `VD` 高且 `VI` 低的“关键感知 token”；**抑制**那些 `VD` 和 `VI` 都高（即信号脆弱）的“脆弱锚点”；**忽略**那些 `VD` 低的“语言先验 token”。

*   **组件二：感知优势重塑**
    *   **核心思想**：在识别出关键感知 token 后，需要一个有效的方法来重新分配学习信号。直接使用鲁棒视觉依赖指标会导致梯度崩溃和优化不均。
    *   **关键技术细节**：
        1.  **突触S型重塑函数**：提出一个基于广义逻辑函数的非线性映射，将原始的 RVD 分数转换为一个边界稳定的令牌级权重：
            *   `T(x) = m + (M - m) / (1 + exp(-k * (x - μ)))`
            *   其中，`μ` 是激活阈值（设为 RVD 分数的第 25 个百分位数），`M` 是上饱和界，`m` 是下抑制基线（确保非感知 token 也有非零梯度），`k` 是陡度。
        2.  **令牌级动态优势重塑**：将原始轨迹级优势 `Ai` 与上述重塑后的权重相乘，得到最终的令牌级优势 `Ai_PAR`。
            *   `Ai_PAR = Ai * T(RVD)`
            *   该机制使得：对于超过阈值的感知 token，其学习信号被**放大**；对于未超过阈值的 token，其信号被**轻微抑制**，但仍保留非零梯度以防止训练僵化。
        3.  **训练目标**：将新的令牌级优势代入 DAPO 的裁剪代理目标中，进行最终优化。

#### 3. 实验设计

*   **数据集/场景**：在 **ViRL39K** 数据集上进行训练。
*   **Benchmark**：在 7 个广泛使用的多模态推理基准上进行评估：
    *   数学推理: `Geo3k`, `MathVista`, `We-Math`, `MMK12`, `MathVerse` (含其 Vision-centric 子集)。
    *   逻辑推理: `LogicVista`。
    *   高级理解: `MMMU-Pro`。
*   **对比方法**：
    *   基础模型: `Qwen2.5-VL` (3B 和 7B)。
    *   强化学习基线: `GRPO`, `DAPO`。
    *   多模态强化学习基线: `PAPO`, `VPPO`。
    *   其他 SOTA 方法: `MM-Eureka`, `VL-Rethinker`, `NoisyRollout`, `R1-ShareVL`。

#### 4. 资源与算力

*   **硬件**：8 × NVIDIA A100 80GB GPUs。
*   **软件/框架**：PyTorch 及相关深度学习库（未明确说明具体框架名称）。
*   **训练时长**：未给出 PRPO 训练一次的确切总时长，但文中提到 DAPO 基线训练约需 32.4 小时，PRPO 因计算 RVD 增加了开销，完成等量训练步数需约 38.2 小时。

#### 5. 实验数量与充分性

*   **实验数量与类型**：
    *   **主要结果（表1）**：在两个模型规模（3B, 7B）上，对比了 10+ 种方法在 7 个基准上的表现。
    *   **消融实验**：
        1.  **核心组件消融（表2）**：分别移除 Visual Inconsistency 和 PAR 机制。
        2.  **PAR 参数消融（表3）**：系统性地分析了上界 `M`、下界 `m`、陡度 `k` 的影响。
        3.  **RVD 阈值消融（表4）**：探讨不同百分位数的 `μ` 值对性能的影响。
        4.  **指标消融（表6）**：比较 RVD 与 JSD、Top-1 概率差、单一 KL 散度等指标的性能。
        5.  **扰动策略分析（表7）**：探讨信息破坏扰动（掩码）的不同比例 `p` 的影响。
    *   **效率分析**：
        1.  **等预算分析（表8）**：在相同计算时间下，对比 PRPO 与 DAPO 的性能。
    *   **案例研究（图6，附录F）**：提供了多个定性案例，展示 PRPO 如何修正感知错误，生成更短、更准确的推理路径。
*   **充分性与公平性评估**：
    *   **实验非常充分且设计严谨**。不仅与多种 SOTA 方法对比，还对自身每个关键设计（RVD, PAR, 及其内部参数）进行了消融，验证了每个组件的必要性。
    *   “等预算分析”（表8）客观证明了 PRPO 的性能提升来源于其更优的学习机制，而非单纯的计算开销增加。
    *   由于未提供代码和详细的数据集划分，复现性稍显不足，但方法论描述足够详细。

#### 6. 主要结论与发现

*   PRPO 在所有 7 个基准测试上均取得了最先进的（SOTA）结果。
*   在 3B 和 7B 模型上，相比于基础模型，PRPO 分别实现了 **23.3%** 和 **21.1%** 的平均性能提升。
*   相比于最强的 RL 基线 VPPO，PRPO 在 3B 和 7B 模型上分别提升了 1.9% 和 1.7%。
*   PRPO 具有更高的**训练效率**（等预算下性能远超 DAPO）和更强的**跨任务泛化能力**。
*   论文证实了**细粒度的令牌级信用分配**对于多模态强化学习至关重要。

#### 7. 优点

*   **问题洞察深刻**：精准识别出“感知信用分配问题”是当前多模态 RLVR 的关键瓶颈，并通过详实的误差分析提供了有力证据。
*   **方法设计精巧**：
    *   提出的 RVD 指标同时考虑“依赖性”和“鲁棒性”，有效过滤噪声，这是比 VPPO 等单指标方法更先进的思路。
    *   设计的 PAR 模块使用连续S形函数而非简单缩放或离散选择，既能放大关键感知信号，又能为非关键信号维持稳定梯度，防止梯度崩溃和表示坍缩，这是一个非常巧妙的工程设计。
*   **实验全面严谨**：涵盖了多个主流基准、多种对比方法和详尽的消融实验，并进行了公平的“等预算”分析，极大增强了结论的说服力。
*   **理论支撑**：在附录 B.1 中，从信息论和信息几何角度为 RVD 的双指标度量提供了理论基础，提升了方法的可信度。

#### 8. 不足与局限

*   **计算开销**：计算 RVD 需要对每个轨迹进行两次额外的前向传播（一次强扰动，一次弱扰动），这增加了训练成本（约 18% 的时间开销）。虽然等预算分析证明了其高效性，但在资源极其有限的情况下，这可能是一个负担。
*   **参数敏感性**：PAR 的 S 型函数参数（μ, M, m, k）对性能有一定影响，需要精细调优。虽然论文给出了最优参数，但结论是否对模型规模和数据集具有普适性仍有待验证。
*   **依赖双扰动推理**：RVD 的计算依赖于对扰动图像进行推理，这要求在训练流程中整合两个并行推理流，增加了系统的工程复杂度。如何更高效地计算 RVD 或设计无需扰动的方法，是未来的研究方向。
*   **未探索其他RL范式**：论文的优化器建立在 DAPO 基础上，未探索与 RLHF、DPO 等其他范式结合的可能性，其方法的通用性存在一定的局限。

（完）
