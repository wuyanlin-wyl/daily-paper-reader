---
title: "ScAle: Attention Head Scaling as a Minimal Adapter for Spatial Reasoning in Vision Language Models"
title_zh: ScAle：注意力头缩放作为视觉语言模型中空间推理的最小适配器
authors: "Rahul Chowdhury, Timothy A Rupprecht, Xuan Shen, Pu Zhao, Yanzhi Wang"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29579v1"
tags: ["query:vl"]
score: 9.0
evidence: 针对视觉语言模型的空间推理适应方法
tldr: "空间推理是视觉语言模型的挑战。本文提出ScAle，仅学习少量标量系数调整最后token的注意力和MLP激活，在冻结骨干上实现轻量适配。在SpatialEval和真实VQA上，仅用1K参数获得134.1%相对提升，接近标准PEFT性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29579v1/fig-001.webp\", \"caption\": \"Table 3: Test accuracy on WhatsUp-VLM and POPE benchmarks.\", \"page\": 11, \"index\": 1, \"width\": 442, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-002.webp\", \"caption\": \"Table 2: SpatialEval evaluation with different models under 20% training data.\", \"page\": 10, \"index\": 2, \"width\": 704, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-003.webp\", \"caption\": \"Fig. 3: Learned scaling factors for (a) LLaVA-7B and (b) Qwen2.5-VL-3B.\", \"page\": 14, \"index\": 3, \"width\": 652, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-004.webp\", \"caption\": \"Fig. 1: Examples of SpatialEval: (a) Spatial-Grid, (b) Maze-Nav, and (c) Spatial-Map.\", \"page\": 4, \"index\": 4, \"width\": 652, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-005.webp\", \"caption\": \"Table 4: Cross-task evaluation with Qwen2.5-VL-3B.\", \"page\": 12, \"index\": 5, \"width\": 658, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-006.webp\", \"caption\": \"Table 1: SpatialEval evaluation with LLaVA-7B under different training data size.\", \"page\": 9, \"index\": 6, \"width\": 706, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-007.webp\", \"caption\": \"Fig. 2: Accuracy heatmaps for multiplying one scalar to the last-token MLP activations in one single layer. Different scalars ranging from −10 to +10 are applied to different layers to obtain the heatmap. We evaluate LLaVA-Next on three SpatialEval tasks.\", \"page\": 5, \"index\": 7, \"width\": 652, \"height\": 221}]"
motivation: 初步分析发现重新缩放选定层激活可显著影响性能，促使探索更轻量适配方法。
method: ScAle学习少量标量系数，调制冻结VLM中最后一层token的注意力和MLP激活。
result: "在空间推理和VQA基准上，仅用1K参数实现高达134.1%相对准确率提升，优于LoRA等标准PEFT。"
conclusion: 有界激活重加权是简单、架构无关、极参数高效的预训练VLM适配替代方案。
---

## 摘要
空间推理仍然是许多视觉语言模型（VLM）面临的持续挑战，而改进它通常需要微调大量额外参数。我们的初步分析表明，在不修改预训练权重的情况下，重新缩放选定Transformer层中的激活值可以显著影响下游性能。受此观察的启发，我们提出了ScAle，一种超轻量级的适配方法，它学习一组小的标量系数来调节完全冻结主干中的最后一个token注意力和MLP激活值。我们在合成空间推理基准SpatialEval以及多个模型系列的真实世界VQA数据集（COCOQA和VGQA）上评估了我们的方法。我们的方法ScAle仅使用1K可训练参数，就能实现高达134.1%的相对准确率提升，而无需像LoRA等标准PEFT方法那样需要数百万可训练参数。尽管极其紧凑，我们的方法在保持强大的非空间VQA准确率的同时，恢复了标准PEFT性能的很大一部分。这些结果表明，有界激活重新加权为适配预训练VLM提供了一种简单、与架构无关且高度参数高效的替代方案。

## Abstract
Spatial reasoning remains a persistent challenge for many vision language models (VLMs), and improving it typically requires fine-tuning with substantial additional parameters. Our preliminary analysis reveals that rescaling activations in selected transformer layers-without modifying pretrained weights-can significantly influence downstream performance. Motivated by this observation, we propose ScAle, an ultra-lightweight adaptation method that learns a small set of scalar coefficients to modulate last-token attention and MLP activations in a fully frozen backbone. We evaluate our method on the synthetic spatial reasoning benchmark SpatialEval and on real-world VQA datasets (COCOQA and VGQA) across multiple model families. Our method, ScAle, achieves up to 134.1% relative accuracy gains using only 1K trainable parameters without requiring millions of trainable parameters as in standard PEFT methods such as LoRA. Despite its extreme compactness, our approach recovers a substantial fraction of standard PEFT performance while preserving strong non-spatial VQA accuracy. These results demonstrate that bounded activation reweighting provides a simple, architecture-agnostic, and highly parameter-efficient alternative for adapting pretrained VLMs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉语言模型（VLM）在空间推理任务（如几何关系、导航、地图理解）上表现持续不佳，即使是大模型也存在显著缺陷。传统的改进方法需要大规模微调，引入大量额外参数，成本高且可能损害通用能力。
- **研究动机**：初步实验发现，仅对冻结VLM中**单一层的最后一个token的MLP激活值**进行手动缩放（不修改预训练权重），就能显著影响空间推理准确率。这表明内部激活的重新加权可以促进已有但被压制的空间推理模式。
- **整体含义**：本文提出一种**超轻量级适配方法ScAle**，通过学习少量有界标量系数来调制最后一层token的注意力和MLP激活，在保持主干完全冻结的情况下，以极低参数开销（约1K）实现接近标准PEFT（如LoRA）的性能，且不降低非空间VQA能力。

## 2. 方法论

### 核心思想
- 为每个Transformer层的每个**注意力头**以及每个**MLP块**的最后一个token输出学习一个**有界缩放因子**，仅作用于最后一个token（因为该token聚合了全部上下文信息，适合做下游分类）。
- 缩放函数使用 **tanh** 限制范围：`eff(u) = 1 + s_max * tanh(u)`，其中 `u` 是可学习参数，`s_max` 控制缩放幅度（实验中设为10），保证数值稳定性。
- 训练时，所有预训练权重完全冻结，仅优化这些标量系数，通过反向传播跨层协调调整。

### 关键技术细节
- **注意力头缩放**：每层有H个注意力头，每个头对应一个标量 `u_attn_{l,h}`，输出 `z_{l,h}` 缩放为 `tilde{z}_{l,h} = eff(u_attn_{l,h}) * z_{l,h}`。参数量 = 层数 × 头数。
- **MLP缩放**：每层整个MLP输出共享一个标量 `u_MLP_l`，参数量 = 层数。
- **三种变体**：仅注意力头缩放、仅MLP缩放、两者结合。实验表明注意力头缩放效果更好。
- **损失函数**：交叉熵损失 + L2正则化（对缩放参数本身），优化器使用AdamW，学习率1e-3，正则化系数1e-4。

### 与现有方法的对比优势
- 相比LoRA（每层插入低秩矩阵），ScAle参数减少2500倍以上。
- 相比(IA)^3（每维度学习一个缩放因子），ScAle每头/每层只学习一个全局标量，参数更少。
- 相比Prefix-tuning（插入连续软token），ScAle不改变输入表示，更简洁。

## 3. 实验设计

### 使用的数据集与基准
- **SpatialEval**（合成空间推理）：包含三个子任务：
  - Maze-Nav（导航推理）
  - Spatial-Grid（坐标化位置推理）
  - Spatial-Map（地图关系推理）
  每个任务1500样本，划分出5%、10%、20%作为训练集，其余测试。
- **WhatsUp-VLM**（真实世界空间VQA）：包含COCOQA和VGQA数据集。
- **POPE**（幻觉鲁棒性基准）：评估是否产生对象幻觉。
- **跨任务评估**：在Qwen3B上训练ScAle后，测试其对不同任务（包括GLUE中的SST-2、MRPC）的迁移能力。

### 对比方法
- 冻结基线（Frozen）
- LoRA-all（全模型，秩1）
- LoRA-last（仅最后一层，秩1）
- (IA)^3（学习每维度缩放向量）

### 模型
- LLaVA-Next（7B, 13B）
- Qwen2.5-VL（3B, 7B）

## 4. 资源与算力

**文中未明确说明**使用的具体GPU型号、数量、训练时长或总计算量。仅提到微批大小为1，训练5个epoch。由于参数量极少（约1K），推断训练成本极低，但无具体数字。

## 5. 实验数量与充分性

- **实验组数较多**：
  - 4种模型 × 3个SpatialEval任务 × 3种训练数据比例 = 36组核心结果。
  - 另加2个真实VQA数据集、POPE、跨任务评估（7个迁移目标）、消融（attn / mlp / both）、与LoRA组合、可视化等。
- **充分性评估**：
  - 实验设计**充分**：覆盖合成与真实、多尺度模型、多种训练数据量、多种PEFT对比，并做了跨任务和组合实验。
  - **公平性**：所有方法使用相同的固定训练/测试划分，超参数一致（除了各自的学习率等微调）。但未对LoRA等进行广泛的超参数搜索，可能略有偏差。
  - **客观性**：结果报告完整，包括失败情况（如MLP缩放效果差），并分析了原因。

## 6. 主要结论与发现

1. **ScAle在SpatialEval上大幅提升**：以LLaVA-7B为例，仅用1,024个注意力头缩放参数，在Maze-Nav上从31.7%提升到68.9%（相对+117%），在Spatial-Grid上从29.3%到68.6%（+134%），在Spatial-Map上从22.7%到45.0%（+98%）。
2. **参数效率极高**：对比LoRA-all（2.65M参数）和(IA)^3（664K），ScAle仅需约1K参数（2500倍更少），且恢复LoRA大部分性能。
3. **注意力头缩放优于MLP缩放**：注意力缩放是主要贡献者，MLP缩放效果微弱。
4. **跨模型泛化**：在LLaVA-7B/13B、Qwen3B/7B上均取得一致提升，表明架构无关。
5. **真实场景有效**：在WhatsUp-VLM（VGQA、COCOQA）上准确率从约56%提升至90%以上；在POPE幻觉基准上也从82.1%提升至88.2%。
6. **跨任务迁移好**：在Spatial-Grid上训练的缩放因子能提升VGQA（84.5%→92.2%）和GLUE/MRPC（66.7%→73.8%），且不退化通用语言理解。
7. **与LoRA互补**：将LoRA-QKV与ScAle结合，可进一步超越全LoRA（例如Maze-Nav达到78.9% vs LoRA-all 71.6%），参数仅为LoRA-all的36%。

## 7. 优点

- **超轻量级**：仅需约1K可训练参数，训练极快，内存开销几乎为零，适合资源受限场景（如移动端、边缘设备）。
- **架构无关**：在LLaVA和Qwen家族多个尺度上均有效，无需特定设计。
- **可解释性**：可视化学习的缩放因子，可分析哪些层/头对空间推理关键（例如中间层责任更大）。
- **保持通用能力**：不损害甚至提升非空间VQA和NLU性能，避免灾难性遗忘。
- **组合灵活**：可与LoRA等PEFT方法结合，进一步提升效率或性能。
- **实现简单**：仅需在模型前向时在指定位置乘以标量，易于集成到现有代码。

## 8. 不足与局限

- **实验范围有限**：仅使用SpatialEval（合成）和WhatsUp-VLM/POPE（真实）。缺乏更多样化的空间推理基准（如VCR、CLEVR等），以及更多语言理解基准的全面评估。
- **模型规模不够大**：最大仅13B，未测试30B/70B或更大模型。极轻量化的适配在大模型上可能效果减弱（因为大模型本身已较好？）。
- **未与传统全微调比较**：仅与PEFT方法对比，未报告全参数微调的性能上限。ScAle能否达到或接近全微调仍是未知。
- **训练数据量偏小**：最多仅20%数据（300样本），虽然展示了小样本能力，但在极低数据下可能存在过拟合风险，且未报告变化曲线。
- **缩放因子范围限制**：s_max=10是手工设定，可能不是最优；未做s_max的消融。
- **仅作用于最后token**：假设最后token足够代表空间推理需求，但对某些需要逐token精细推理的任务（如详细描述）可能不适用。
- **跨任务泛化分析不足**：虽然做了跨任务测试，但未解释为什么某些缩放因子能迁移而某些不能（如Maze-Nav训练对SST-2有提升但对MRPC无显著影响）。
- **应用限制**：作为适配器，ScAle不增加新能力，只是重新加权已有模式。若模型本身缺乏空间知识的表示，则无法通过简单缩放创造新知识。

（完）
