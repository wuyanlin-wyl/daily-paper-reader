---
title: Training-free Cross-domain Few-shot Segmentation via Robust Semantic Representation and Matching
title_zh: 免训练跨域小样本分割：基于鲁棒语义表征与匹配
authors: "Sujun Sun, Mingwu Ren, Haofeng Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24297v1"
tags: ["query:vlmmed"]
score: 7.0
evidence: 无训练跨域小样本分割方法
tldr: 跨域小样本分割（CD-FSS）旨在用少量标注样本分割目标域新类。现有方法依赖训练易过拟合，引入视觉大模型后提升有限。本文提出无训练框架，基于DINOv3编码器，通过语义重融合、自适应增强和混合原型匹配模块，在四个数据集上实现SOTA，避免训练开销和过拟合。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24297v1/fig-001.webp\", \"caption\": \"Fig. 4: Parameter studies. (a) The mIoU curves over different numbers of sampled thresholds in ASE. (b) The mIoU curves over different coefficients α in ASE. (c) The mIoU curves over different numbers of foreground regional prototypes in HPM.\", \"page\": 25, \"index\": 1, \"width\": 692, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2606.24297v1/fig-002.webp\", \"caption\": \"Fig. 2: Visualization of the raw attention outputs and fused outputs after residual connections in each DINOv3 block. Layers focusing on semantic patterns and local consistency appear alternately, and the position-aware features that emphasize local consistency cause the fused output Fl sum to also overly focus on local consistency, which persists to the final output.\", \"page\": 21, \"index\": 2, \"width\": 710, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2606.24297v1/fig-003.webp\", \"caption\": \"Fig. 3: Variance proportion of position-sensitive channels in each layer across different DINOv3 architectures.\", \"page\": 21, \"index\": 3, \"width\": 696, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2606.24297v1/fig-004.webp\", \"caption\": \"Fig. 1: (a) Existing CD-FSS methods across three paradigms all involve different trainable parameters, while our method requires no additional training. (b) When DINOv3 is incorporated into three existing CD-FSS paradigms, their performance shows only marginal improvement or even degrades. In contrast, our training-free method eliminates overfitting risk and achieves state-of-the-art performance.\", \"page\": 2, \"index\": 4, \"width\": 715, \"height\": 186}]"
motivation: 消除训练或微调带来的高计算成本和过拟合风险，利用预训练视觉模型实现无需额外训练的跨域小样本分割。
method: 基于无训练DINOv3编码器，设计语义感知特征重融合（SAFR）、自适应支持增强（ASE）和混合原型匹配（HPM）三个模块。
result: 在四个目标域数据集上取得最优性能，无需任何训练或微调。
conclusion: 所提无训练框架有效避免过拟合，显著提升跨域小样本分割效果，优于现有需训练的方法。
---

## 摘要
跨域小样本分割旨在将从源域学到的知识迁移到不同的目标域，仅用少量标注样本即可分割未见过的目标类别。尽管现有方法取得了显著进展，但它们仍依赖训练或微调过程，这会带来高计算成本并存在过拟合风险。我们观察到，当将强大的通用视觉基础模型融入这些方法时，由于过拟合，其性能仅获得边际提升甚至下降。为此，我们消除可训练参数，提出了一种免训练框架，以避免训练开销和过拟合。该框架基于自监督视觉编码器DINOv3，通过三个核心模块应对跨域挑战。首先，语义感知特征重构模块识别并重构强调语义模式的特征，生成具有增强语义判别性的表征。此外，自适应支持增强模块通过鲁棒的查询信息聚合缩小支持集与查询集之间的语义差距。最后，混合原型匹配模块整合来自不同原型的匹配结果，以适应各域不同的语义复杂度。在四个目标域数据集上的大量实验表明，我们的方法在无需任何训练的情况下实现了CD-FSS的最优性能。

## Abstract
Cross-domain Few-shot Segmentation (CD-FSS) aims to transfer knowledge learned from source domain to distinct target domains, segmenting unseen target classes with only a few annotated samples. Although existing methods have made significant progress, they still rely on training or fine-tuning processes, which incur high computational costs and risk overfitting. We observe that when powerful and general-purpose vision foundation models are incorporated into these methods, their performance shows only marginal improvement or even degrades due to overfitting. To address this, we eliminate trainable parameters and propose a training-free framework to avoid both training overhead and overfitting. Built upon the self-supervised vision encoder DINOv3, our framework addresses cross-domain challenges through three core modules. First, the Semantic-aware Feature Re-fusion (SAFR) module identifies and re-fuses features that emphasize semantic patterns, generating representations with enhanced semantic discriminability. Additionally, the Adaptive Support Enhancement (ASE) module narrows semantic gaps between support and query through robust query information aggregation. Finally, the Hybrid Prototype Matching (HPM) module integrates matching results from diverse prototypes to adapt to varying semantic complexity across domains. Extensive experiments on four target domain datasets demonstrate that our method achieves state-of-the-art performance in CD-FSS without any training.