---
title: An Effective Router for Vision-Language Model Selection
title_zh: 视觉语言模型选择的有效路由器
authors: "Can Wang, Shengwei Wang, Bolin Zhang, Zhiying Tu, Dianhui Chu"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.08970v1"
tags: ["query:vl"]
score: 9.0
evidence: 视觉语言模型选择路由器
tldr: "针对视觉语言模型(VLM)选择困难的问题，本文构建了包含7个主流VLM在32,626个查询上输出的数据集，并提出路由器ARMS。ARMS通过VLM增强输入信号和简单有效的架构改进查询与VLM能力表示，采用增量训练和独立训练策略适应新VLM。实验表明，仅800M参数的ARMS在分布内外测试集上表现优异，甚至超越百倍规模GPT-4o。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08970v1/fig-001.webp\", \"caption\": \"Figure 4: Impact of data amount and training epochs on the performance of ARMS in in-distribution (ID) and out-of-distribution (OOD) scenarios. The best results are annotated below the corresponding points.\", \"page\": 6, \"index\": 1, \"width\": 1042, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-002.webp\", \"caption\": \"Figure 5: Main Results of our router ARMS with different training strategies and selection spaces (S). Φ+ΔΦ denotes the incremental training strategy and Φ+Θ denotes the independent training strategy. The best and the second-best results are annotated above the corresponding bars, the best results are in red. |S| = 4 denotes that S contains only four open-source VLMs, while |S| = 7 means that S includes four open-source models and three commercial VLMs\", \"page\": 6, \"index\": 2, \"width\": 1054, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-003.webp\", \"caption\": \"Table 1: Comparison with existing datasets\", \"page\": 2, \"index\": 3, \"width\": 504, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-004.webp\", \"caption\": \"Figure 2: A data sample in M2. <Question> and <Image> are combined as the input to the VLM claude3.5-sonnet, where the blue text denotes the added prompt for output standardization. The correct answer is in green, while the incorrect answer from the VLM is in red.\", \"page\": 3, \"index\": 4, \"width\": 452, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-005.webp\", \"caption\": \"Figure 6: Embeddings visualization of the fused image and text vectors from ARMS with t-SNE [26].\", \"page\": 8, \"index\": 5, \"width\": 1018, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-006.webp\", \"caption\": \"Figure 7: Comparison between two training strategies. The best results on Accuracy, Recall, Precision and F1-score are annotated below the corresponding points. The best results on EM are annotated above the corresponding bars.\", \"page\": 8, \"index\": 6, \"width\": 1054, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2606.08970v1/fig-007.webp\", \"caption\": \"Figure 3: Architecture of ARMS, consists of five modules: a text encoder, an image encoder, a gatemodule, fusion experts, and a classifier.\", \"page\": 4, \"index\": 7, \"width\": 502, \"height\": 292}]"
motivation: 现有VLM众多，但缺乏有效选择方法，面临数据缺失、特征表示低效和模型空间僵化等挑战。
method: 构建多模态数据集，提出ARMS路由器，利用VLM特征增强输入，采用增量/独立训练策略实现快速适应新VLM。
result: ARMS（800M）在分布内外测试集上超越GPT-4o等百倍规模模型。
conclusion: ARMS通过有效表示和灵活训练策略，实现了高效准确的VLM选择，且易于扩展。
---

## 摘要
具有不同性能和资源需求的视觉语言模型（VLM）被广泛部署，使得用户难以在众多VLM候选中选择最合适的一个。现有工作揭示了语言模型中的性能悖论现象，并专注于路由方法来解决它。然而，为VLM选择开发一个路由器仍然是一个关键且具有挑战性的问题，主要面临：1）缺乏专门数据，2）特征表示无效，以及3）模型空间僵化和适应成本高。在本文中，我们构建了一个用于VLM选择的多模态数据集，包含七个主流VLM在32,626个独特图像-文本查询上的输出。然后我们提出了ARMS，一个用于VLM选择的路由器。ARMS通过VLM配置文件增强输入信号，采用简单但有效的架构来改善查询和VLM能力的表示。为了提高ARMS对新VLM的适应能力，我们提出了两种扩展训练策略：增量训练和独立训练。在分布内和分布外测试集上的实验结果表明了ARMS的有效性。特别是，使用我们的训练策略，ARMS（仅800M大小）能够适应更广泛的VLM空间，并击败规模大数百倍的商业模型如GPT-4o。我们的代码、模型和数据集可在匿名仓库中获得。

## Abstract
Vision-language models (VLMs) with varying performance and resource requirements are widely deployed, making it difficult for users to select the most appropriate one among numerous VLM candidates. Existing work reveals the performance paradox phenomenon in language models and focuses on routing methods to solve it. However, developing a router for VLM selection is still a critical yet challenging problem, which primarily faces: 1) lack of specialized data, 2) ineffective feature representation, and 3) rigid model space and costly adaptation. In this paper, we construct a multimodal dataset for VLM selection, containing the outputs of seven mainstream VLMs on 32,626 unique image-text queries. We then propose ARMS, a router for VLM selection. ARMS enhances input signals with VLM profiles, employs a simple but effective architecture to improve representations of queries and VLM capabilities. To improve ARMS' adaptation to new VLMs, we propose two extension training strategies: incremental training and independent training. Experimental results on both in-distribution and out-of-distribution test sets demonstrate the effectiveness of ARMS. In particular, using our training strategy, ARMs (only 800M in size) can adapt to a broader VLM space and defeat commercial models like GPT-4o that are hundreds of times larger in scale. Our code, models, and datasets are available in the anonymous repository.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉语言模型（VLM）种类繁多，性能和资源需求各异，用户难以准确选择最合适的模型来回答给定的图像-文本查询。现有研究揭示了语言模型中的“性能悖论”——更大、更贵的模型并非总是优于更小、更便宜的模型。作者通过VHELM基准上的实证研究证实，该悖论同样存在于VLM中（例如GPT-4o答错的查询中有43.18%被免费的开源模型Qwen2.5-VL-7B-Instruct正确回答）。因此，亟需一种能够根据查询动态选择最优VLM的路由器。
- **现有方法不足**：已有路由器研究主要针对纯文本的大语言模型（LLM），无法直接应用于多模态查询；且存在三大障碍：①缺乏专门用于VLM选择的大规模多模态训练数据；②特征表示不充分，无法有效利用多模态输入和模型信息；③模型空间僵化，扩展新VLM需完全重训练，成本高、可扩展性差。

## 2. 论文提出的方法论

### 核心思想
构建一个多模态路由器ARMS（a router for VLM selection），通过混合专家（MoE）架构动态融合图像和文本查询特征，并利用VLM配置文件增强输入信号，从而判断给定VLM是否能正确回答某查询。同时提出两种扩展训练策略，使路由器能够高效适应新VLM。

### 关键技术细节
- **ARMS架构**（如图3所示）：
  - **文本编码器**：基于DistilBERT，编码用户查询文本 \(Q_i\) 和VLM配置文件 \(M_j\)（含名称、参数量、架构等）。
  - **图像编码器**：基于CLIP-ViT-B/32，编码图像 \(G_i\)。
  - **门控网络（Gate Network）**：将查询文本向量 \(q_i\) 和图像向量 \(g_i\) 拼接后，通过投影和Softmax得到专家选择概率 \(p_i\)，用于激活不同的融合专家。
  - **融合专家（Fusion Experts）**：每个专家为一个Transformer块，以 \(q_i\) 为查询、\(g_i\) 为键和值进行注意力对齐，再经过LN和FFN得到融合向量 \(e_i\)。
  - **分类器（Classifier）**：将 \(e_i\) 与 \(q_i\) 拼接后LN，计算与VLM配置文件向量 \(m_j\) 的余弦相似度 \(s_{i,j}\)，再经 \(\text{arctanh}\)、裁剪（clamp）和Sigmoid映射到[0,1]作为置信度得分 \(c_{i,j}\)，阈值0.5决定预测标签。损失函数为交叉熵（使用logits经裁剪后输入）。

- **训练目标**：最大化路由器参数 \(\Phi\)，使得对于训练集中每个查询和VLM，预测标签与真实标签（VLM是否回答正确）的似然最大（公式(1)）。

### 扩展训练策略
1. **增量训练（Incremental Training）**：在原始路由器 \(\Phi\) 上，使用新VLM的样本继续训练部分参数（公式(7)）。
2. **独立训练（Independent Training）**：分别训练两个路由器 \(\Phi\)（基于原始VLM集S）和 \(\Theta\)（基于新VLM集 \(S_{\text{new}}\)），然后通过阈值 \(\theta\) 级联系统选择VLM（算法1）：先用 \(\Phi\) 选出最高置信度VLM，若超过阈值则调用；否则用 \(\Theta\) 计算新VLM的置信度，比较后选出更高置信度的VLM。

## 3. 实验设计

### 数据集与场景
- **M2数据集**（论文自行构建）：
  - 来源：三个大规模VLM基准（ALM-bench、MMT-Bench、MME-RealWorld），共32,626个图像-文本查询。
  - 运行4个开源VLM（Qwen2.5-VL-7B-Instruct、Idefics2-8B、Llama3.2-11B-Vision-Instruct、SmolVLM2-2B-Instruct），得到130,504个样本（32,626×4）。
  - 从中选7,000个查询再运行3个商业VLM（GPT-4o、Claude3.5 Sonnet、Gemini1.5 Flash），得到21,000个样本（7,000×3）。
  - 另从VHELM基准中选10,000个查询作为OOD测试集（最终有效：7,084个至少一个免费VLM答对，9,397个至少任意VLM答对）。
  - 数据集分为ID（训练+测试）和OOD（仅测试），VLM选择空间大小|S|为4或7。总计训练集35,126个查询，测试集20,981个查询。

### Benchmark与对比方法
- **评价指标**：Accuracy、Precision、Recall、F1-Score；以及EM（Exact Match）= 选择模型正确回答的查询数/总查询数。
- **基线**：七个单独VLM（GPT-4o、Claude3.5 Sonnet、Gemini1.5 Flash、Qwen2.5-VL-7B-Instruct、Idefics2-8B、Llama3.2-11B-Vision-Instruct、SmolVLM2-2B-Instruct）。
- **Oracle上界**：总是选择能正确回答的VLM，EM=1.0。

### 实验分组
- **RQ1**：数据量（10%-100%）和训练轮数（2-10）对ID和OOD性能的影响。
- **RQ2**：ARMS在|S|=4和|S|=7下的ID/OOD性能，对比单个VLM（图5）。
- **RQ3**：消融实验（冻结BERT/CLIP、使用简单拼接、移除VLM配置文件等），以及t-SNE可视化。
- **RQ4**：比较增量训练和独立训练策略在不同数据比例和置信度阈值下的表现（图7）。

## 4. 资源与算力

- **训练硬件**：4× NVIDIA RTX 4090 GPU，CPU为16核Xeon(R) Gold 6430。
- **模型大小**：ARMS约为800M参数（基于DistilBERT和CLIP-ViT-B/32）。
- **训练细节**：batch size=64，学习率8e-5，权重衰减1e-2，优化器AdamW，训练2-10个epoch。
- **未明确说明**：具体训练时长未给出，但可推断在多卡4090上训练ID数据（约35k查询）的epoch规模在合理时间内。

## 5. 实验数量与充分性

- **实验组数**：包含四大研究问题下的多组实验：
  - RQ1：不同数据量（6个比例）× 不同epoch（5个值）→ 约30个子实验。
  - RQ2：两种选择空间（|S|=4/7）× 两种扩展策略（增量/独立）× ID/OOD → 8个主要结果。
  - RQ3：6个消融变体（冻结BERT/CLIP、拼接、无配置文件、完整ARMS），以及t-SNE可视化。
  - RQ4：增量训练（5个初始数据比例）× 独立训练（6个阈值）→ 11个子实验。
  - 总计约50+次实验，覆盖了数据规模、训练轮次、模块消融、扩展策略等关键维度。
- **充分性**：实验设计较为全面，包括ID和OOD泛化、消融研究、扩展策略对比，符合学术规范。但缺少与其他路由方法的直接对比（因现有方法主要针对LLM且非多模态，对比有限），作者已说明理由。
- **客观公平**：Oracle上界（EM=1.0）清晰表明数据集具有挑战性；所有VLM均在统一硬件上推理或有官方API结果；实验中控制了随机种子等（文中未明确说明，但通常假设已随机化）。

## 6. 论文的主要结论与发现

1. **M2数据集的有效性**：随着训练数据量增加，EM分数持续提升，说明M2提供了稳定且有信息量的监督信号。
2. **ARMS优于单个VLM**：在ID和OOD设置下，ARMS在EM上均优于所有四个开源VLM；当扩展选择空间至7个VLM（含商业模型）后，ARMS（独立训练）进一步超越所有单独VLM，包括GPT-4o。
3. **模块贡献**：消融实验表明每个模块均有正向贡献，其中文本编码器最关键（冻结BERT导致EM从0.5721降至0.3480）；注意力融合优于简单拼接；VLM配置文件显著提升性能。
4. **扩展策略的互补性**：增量训练适用于模型空间渐进演化的场景；独立训练更鲁棒于分布偏移，且在高置信度阈值下表现更好。
5. **泛化能力**：训练2个epoch的ARMS在OOD上EM为0.5551（|S|=4），表明学到了可迁移的表示。

## 7. 优点

- **首次构建VLM选择专用大规模多模态数据集**：M2包含56k+查询和7个VLM的输出，填补了空白。
- **路由器架构设计合理**：MoE门控网络动态选择专家，能处理不同查询和VLM能力差异；余弦相似度+arctanh+裁剪的logits转换稳定。
- **扩展训练策略实用**：增量训练和独立训练无需全重训练，支持新VLM的高效引入。
- **充分对比与消融**：多种实验设置验证了数据量、模块、策略的有效性。
- **开源代码、模型和数据集**：促进后续研究。

## 8. 不足与局限

- **与现有路由方法缺乏直接比较**：尽管作者说明已有方法主要针对LLM且非多模态，但未尝试将方法适配后对比，略显遗憾。
- **数据集构建偏差**：仅包含三个基准（ALM-bench、MMT-Bench、MME-RealWorld）和七个VLM，覆盖任务领域有限；OOD测试集来自VHELM，可能存在任务分布重叠。
- **忽略成本与延迟**：论文将选择空间限制为免费开源VLM，声称不考虑商业模型的成本/延迟，但实际应用中成本是重要因素。若扩展至付费VLM，路由器需综合多维度目标。
- **阈值设定依赖经验**：级联系统中的阈值 \(\theta\) 在不同场景需手动选择（文中通过实验确定，但未提供自适应机制）。
- **模型泛化边界**：OOD上EM最高仅0.5551（|S|=4），与Oracle上界1.0差距大；对全新领域（如医疗、遥感）的泛化性未验证。
- **可重复性**：商业VLM的API输出可能随时间变化，影响实验复现。

（完）
