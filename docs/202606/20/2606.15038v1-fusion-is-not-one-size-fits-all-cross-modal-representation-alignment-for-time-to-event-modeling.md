---
title: "Fusion is not one-size-fits-all: Cross-Modal Representation Alignment for Time-to-Event Modeling"
title_zh: 融合并非一刀切：面向事件时间建模的跨模态表示对齐
authors: "Zhemin Zhang, Weijie Chen, David Le, Amara Tariq, Alex Wallace, Matthew Stib, Juan Maria Farina, Chadi Ayoub, Reza Arsanjani, Imon Banerjee"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15038v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: CT影像和电子病历之间的跨模态对齐用于医学预测
tldr: "针对多模态临床数据在时间事件预测中的模态不平衡和分布偏移问题，本文提出一种基于基础模型的跨模态表征对齐框架，将CT影像和纵向EHR数据通过域特定模型独立编码，并采用四种融合策略（晚期融合、对比对齐、交叉注意力、共注意力）对齐到共享潜空间。在肺栓塞死亡率和心血管疾病结局两个任务上，多机构队列实验显示融合策略相比单模态基线一致性指数提升1.5-5.4%，其中对比融合性能最稳健。研究确立了任务感知多模态对齐作为鲁棒泛化和临床部署的必要设计原则。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15038v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of the multimodal survival framework and fusion strategies. Cross-modal alignment of chest CT and longitudinal EHR using domain-specific foundation encoders to produce shared embeddings for time-to-event prediction, with image-only and EHR-only baselines derived from the respective encoders. Fusion variants in latent space— (a) Traditional concatenation of embedding, Proposed - (b) contrastive learning, (c) co-attention, and (d) symmetric crossattention.\", \"page\": 4, \"index\": 1, \"width\": 588, \"height\": 510}, {\"url\": \"assets/figures/arxiv/2606.15038v1/fig-002.webp\", \"caption\": \"Table 1: Prediction performance for PE and MACE reported as mean (95% CI) for Internal (Institution A) and External (Inspect) cohorts. Unimodal baselines (Imageonly and EHR-only) are shown separately from multimodal fusion models. ∗ indicates p < 0.05 versus Image-only, and + indicates p < 0.05 versus linear concatenation fusion as ref. baseline. Best performance within each cohort and outcome is shown in bold.\", \"page\": 7, \"index\": 2, \"width\": 720, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2606.15038v1/fig-003.webp\", \"caption\": \"Fig. 2: Comparative saliency maps for MACE and pulmonary embolism (PE) outcome prediction using image-only and multimodal fusion models. P denotes the predicted risk score for patients who subsequently developed adverse events.\", \"page\": 8, \"index\": 3, \"width\": 508, \"height\": 273}]"
motivation: 现有时间事件预测模型在处理多模态临床数据时面临模态不平衡和分布偏移问题，缺乏统一且可泛化的跨模态对齐方法。
method: 使用域特定基础模型独立编码CT和EHR模态，通过晚期融合、对比对齐、交叉注意力和共注意力四种策略在共享潜空间进行表征对齐。
result: "在多机构肺栓塞和心血管疾病数据集上，融合策略一致性指数提升1.5-5.4%，对比融合最稳健，交叉注意力和共注意力分别在不同任务上最优。"
conclusion: 任务感知的多模态对齐是提升模型泛化性和临床部署鲁棒性的关键设计原则。
---

## 摘要
由于模态不平衡和分布偏移，从多模态临床数据中准确预测事件时间（TTE）仍然具有挑战性。我们提出了一种基础模型驱动的框架，用于CT成像与纵向EHR数据之间的跨模态表示对齐，旨在跨任务和机构进行泛化。CT和EHR模态分别使用领域特定的基础模型进行编码，并通过四种原则性融合策略在共享潜在空间中对齐：后期融合、对比对齐、交叉注意力和协同注意力。我们在大规模多机构队列（PE：训练集N=3,099；内部测试1,098；外部测试435；CVD：训练集N=2,951；内部测试837；外部测试682）上评估了两个临床不同的TTE任务：肺栓塞（PE）死亡率和心血管疾病（CVD）结局。当模态贡献相当时，融合始终比单模态基线提高一致性指数1.5-5.4%。总体而言，对比多模态融合，特别是使用CLMBR表示，提供了最一致且统计上稳健的改进，尤其对于PE死亡率预测。对于MACE，交叉注意力（独热编码）实现了最高的内部性能，而图像引导的协同注意力实现了最佳外部性能。因此，我们引入了一个可泛化的基于基础模型的跨模态对齐框架，并首次系统分析了TTE预测中模态不平衡下的融合行为。我们的结果确立了任务感知的多模态对齐作为稳健泛化和可扩展临床部署的必要设计原则。

## Abstract
Accurate time-to-event (TTE) prediction from multimodal clinical data remains challenging due to modality imbalance and distribution shift. We introduce a foundation model-driven framework for cross-modal representation alignment between CT imaging and longitudinal EHR data, designed to generalize across tasks and institutions. CT and EHR modalities are encoded independently using domain-specific foundation models and aligned in a shared latent space through four principled fusion strategies: late fusion, contrastive alignment, cross-attention, and co-attention. We evaluate two clinically distinct TTE tasks: pulmonary embolism (PE) mortality and cardiovascular disease (CVD) outcomes, on large-scale multi-institutional cohorts (PE: N=3,099 train; 1,098 internal; 435 external; CVD: N=2,951 train; 837 internal; 682 external). Fusion consistently improves concordance index by 1.5-5.4% over unimodal baselines when modalities contribute comparably. Overall, contrastive multimodal fusion, particularly with CLMBR representations, provided the most consistent and statistically robust improvements, especially for PE mortality prediction. For MACE, cross-attention (one-hot) achieved the highest internal performance and image-guided co-attention achieved the best external performance. We therefore introduce a generalizable foundation model-based cross-modal alignment framework and provide the first systematic analysis of fusion behavior under modality imbalance in TTE prediction. Our results establish task-aware multimodal alignment as a necessary design principle for robust generalization and scalable clinical deployment.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：多模态临床数据（CT影像 + 纵向EHR）在时间事件预测（TTE）中面临模态不平衡和分布偏移，现有方法多启发式选择融合策略，且基础模型预训练目标与生存预测任务不匹配，缺乏对跨模态对齐与生存目标交互的系统研究。
- **整体含义**：本文旨在建立一种可泛化的基础模型驱动的跨模态对齐框架，系统对比多种融合策略在TTE任务上的表现，证明任务感知的多模态对齐是提升泛化性和临床部署鲁棒性的必要设计原则。

## 2. 论文提出的方法论

### 2.1 核心思想
- 使用冻结的领域特定基础模型分别编码CT（MedImageInsight）和EHR（CLMBR-T-base 或手工独热编码）得到独立嵌入，然后在共享潜空间通过不同融合策略对齐，最后用MLP预测头进行TTE建模（负对数似然生存损失）。
- 仅在融合层和预测头反向传播梯度，基础编码器冻结。

### 2.2 关键技术细节
- **图像编码**：MedImageInsight（2D医学影像基础模型），对CT轴向切片（选择合适的窗口）进行编码，然后沿z维平均得到1×1024嵌入。
- **EHR编码**：两种表示：
  - CLMBR-T-base：自监督预训练的Transformer，输出768维患者级嵌入。
  - 手工1-hot编码：基于任务特定临床特征（人口统计学、实验室、药物、诊断等）二值化。
- **融合策略**（四种）：
  - **传统拼接（晚期融合）**：直接拼接图像和EHR嵌入。
  - **对比对齐（双步训练）**：先通过对称对比损失（CLIP风格）对齐图像和EHR嵌入（正对：同一样本的两种模态），然后使用融合嵌入训练TTE模型。
  - **交叉注意力（双向）**：端到端学习，EHR->图像和图像->EHR两个方向的交叉注意力，输出汇聚后拼接，TTE损失优化。
  - **协同注意力**：一种模态作查询，另一种作键值，输出加权后的表示再拼接，TTE损失优化。可指定引导模态（图像引导或EHR引导）。

### 2.3 损失函数
- 对比对齐：对称对比损失（公式含温度参数τ）。
- TTE损失：负对数似然NLL（考虑右删失），加L2正则化。

## 3. 实验设计

### 3.1 数据集与任务
- **任务1**：肺栓塞（PE）死亡率预测（1年死亡率）
  - 训练集：3,099患者（私有，多机构）
  - 内部测试：1,098患者
  - 外部测试：435患者（INSPECT公开数据集）
- **任务2**：主要不良心血管事件（MACE）预测
  - 训练集：2,951患者（多机构，46%事件率）
  - 内部测试：837患者
  - 外部测试：682患者（急诊科非心脏CT患者，28%事件率）

### 3.2 基准与对比方法
- **单模态基线**：图像only（CT）、EHR only（两种表示：1-hot和CLMBR）
- **多模态融合基线**：传统拼接（线性串联）
- **对比方法**：对比对齐、交叉注意力、协同注意力（图像引导和EHR引导），每种又区分1-hot和CLMBR两种EHR表示。
- **评估指标**：AUC（报告为均值及95%置信区间），统计显著性检验（p<0.05）。

### 3.3 实验充分性
- 覆盖两个临床任务、多机构内外部验证。
- 消融：不同融合策略、不同EHR表示、不同引导方向。
- 额外分析：梯度显著性图（saliency maps）比较融合前后模型关注的图像区域。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长。仅提到基础模型为冻结预训练模型，训练仅限于融合层和MLP头，计算成本相对较低。

## 5. 实验数量与充分性

- **实验数量**：两个任务共形成约16组主要实验结果（表1：跨任务、跨融合策略、跨EHR表示、跨数据集），每个结果含95%置信区间和显著性标记。
- **充分性评价**：
  - **优点**：覆盖了不同模态组合、不同融合策略、内外部验证，对比全面；显著性分析和消融实验增强了结论可信度。
  - **不足**：
    - 未与其他复杂的TTE基础模型（如MOTOR）或更先进的生存模型进行比较。
    - 缺乏对超参数（如对比温度τ、学习率、MLP层宽）的敏感性分析。
    - 未报告C-index或Brier score等其他生存指标，仅用AUC（虽摘要提及concordance index提高，但表中实际为AUC）。
    - 缺乏对不同事件率、删失比例条件下的鲁棒性测试。

## 6. 论文的主要结论与发现

1. **融合普遍优于单模态**：当模态贡献相当时，融合提升AUC 1.5-5.4%。
2. **对比融合最稳健**：尤其结合CLMBR表示的对比对齐，在PE任务内部和外部均取得最佳性能（内部AUC 0.862，外部0.743）。
3. **最优策略任务依赖**：MACE任务中交叉注意力（1-hot表示）内部最佳，图像引导协同注意力外部最佳。
4. **EHR表示影响**：手工1-hot特征（基于PREVENT风险因素）更适合MACE；CLMBR基础模型表示更适合PE（捕获更广泛的共病模式）。
5. **注意力引导可改善图像关注区域**：融合模型将注意力转移到任务相关的解剖结构（如PE的栓塞负荷、MACE的肺动脉扩张和心脏形态）。
6. **外部泛化仍存差距**：提示需要领域自适应与数据协调策略。

## 7. 优点

- **系统性比较**：首次在同一框架内对不同融合策略（对比、交叉注意力、协同注意力）进行公平、系统的TTE性能对比。
- **基础模型驱动**：利用冻结的域特定基础模型，避免大规模重新训练，提高效率并降低过拟合风险。
- **多任务多机构验证**：两个临床场景、内外部数据集增强了结论的泛化性。
- **可解释性**：通过显著性图展示融合对模型注意力的影响，提供机制层面的理解。
- **实践指导**：揭示了“一刀切”融合不可取，为临床部署选择融合策略提供依据。

## 8. 不足与局限

- **外部泛化差距**：内外性能差异较大（如PE外部AUC比内部低约0.12），受成像协议、人群特征、EHR完整性差异影响，未提出有效缓解方法。
- **样本量与事件率**：中等样本量（单机构内部测试约1000人），PE事件率低（7%），可能导致统计效力有限。
- **评估指标单一**：仅报告AUC，未使用生存分析专用指标（如C-index、Brier score、D-calibration），无法全面评估校准和区分能力。
- **计算资源未报告**：缺乏训练硬件、时间等成本信息，难以评估可复现性。
- **对比基线限制**：未与更复杂的生存模型（如DeepSurv、DeepHit、MOTOR）或端到端多模态方法（如直接训练联合编码器）对比。
- **回顾性数据偏差**：残余混杂和缺失数据模式可能影响结果。
- **缺乏校准与临床效用分析**：未评估预测概率的校准性或决策曲线分析，临床转化证据不足。

（完）
