---
title: "CoAgent: Concurrency Control for Multi-Agent Systems"
title_zh: "CoAgent: 多智能体系统的并发控制"
authors: "Hongtao Lyu, Dingyan Zhang, Mingyu Wu, Xingda Wei, Haibo Chen"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15376v1"
tags: ["query:vlmmed"]
score: 7.0
evidence: 多智能体LLM系统的并发控制
tldr: 多智能体LLM系统并行运行时共享状态变异引发并发控制挑战，经典机制因长事务、宽读集、即时写等不适用。本文提出MTPO协议，由LLM判断冲突并修复操作，通过工具注册逆操作实现原地写和通知重判。CoAgent实现该协议，在10个负载上保持近串行正确性，速度提升1.4倍，成本接近串行，相比2PL和OCC显著提升并发效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15376v1/fig-001.webp\", \"caption\": \"Figure 5. Five protocols on ten contended cells: correctness, speedup, token cost, and Pareto front, all normalized to serial.\", \"page\": 11, \"index\": 1, \"width\": 1048, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2606.15376v1/fig-002.webp\", \"caption\": \"Figure 6. The same contended cell under four protocols.\", \"page\": 11, \"index\": 2, \"width\": 492, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2606.15376v1/fig-003.webp\", \"caption\": \"Figure 2. Unconditional broadcast on the task pair 𝑥 ← 𝑦/2 ∥ 𝑦 ← 𝑥/2. Every write invalidates the other agent’s premise and every heal issues a new write: the run never terminates, although either serial order would succeed.\", \"page\": 7, \"index\": 3, \"width\": 506, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2606.15376v1/fig-004.webp\", \"caption\": \"Figure 7. (a) Online tool growth; (b) bash agent vs CoAgent ToolSmith–Worker.\", \"page\": 12, \"index\": 4, \"width\": 492, \"height\": 430}, {\"url\": \"assets/figures/arxiv/2606.15376v1/fig-005.webp\", \"caption\": \"Figure 4. On request, the ToolSmith builds binding-carrying tools on the fly; Worker reads and writes go only through them, under framework control.\", \"page\": 9, \"index\": 5, \"width\": 506, \"height\": 315}]"
motivation: 经典并发控制机制不适用于LLM智能体的长事务、宽读集和即时写特性，导致并发性能损失。
method: 提出MTPO协议，固定序列化顺序，LLM智能体判断冲突并修复依赖操作，通过工具注册逆操作实现撤销与重排序。
result: "在10个争用负载上，CoAgent在1.4倍加速下保持5%内串行正确性，近串行token成本，2PL和OCC几乎丧失并发增益；在bash系统上在线增长25工具库，任务通过率从45/71升至63/71。"
conclusion: 基于LLM判断和修复的并发控制方法有效，可同时保证正确性和性能，优于传统锁和乐观并发控制。
---

## 摘要
多智能体大语言模型系统——编码智能体、运维智能体、文档智能体——现在通常并行运行多个智能体，针对同一个Git仓库、Kubernetes集群或文档。一旦其中两个智能体改变了共享状态，它们就进入了经典并发控制研究了数十年的领域，但经典机制并不适合LLM智能体。单个智能体的事务跨越数分钟的推理，读集广泛且不透明，而非静态可推断，智能体操作的实时状态既不允许分支也不允许缓冲，因此写入在执行的瞬间即生效。锁会阻塞长时间的推理间隔；OCC的放弃重试会在每次冲突时丢弃数分钟的工作。本文构建了一种经典事务缺乏的并发控制能力：每个智能体内的LLM可以判断一个冲突写入是否使其计划无效，并且可以精确修复依赖该写入的操作。因此控制变成建议性的：运行时通知，智能体修复。我们的协议MTPO（单调轨迹前序）在启动时固定一个序列化顺序，为每次读取提供服务顺序过滤后的值，并在原地推测性地应用写入；单向通知要求受影响的读取者重新判断并修补其计划，而框架通过每个工具预先注册的saga式逆操作机械地撤销和重新排序错位的写入。在静止时，运行按预先决定的顺序是可序列化的。我们将MTPO实现为CoAgent，一种工具调用中间件，其特权工具ToolSmith在线增长声明了占用空间且可撤销的工具。在十个有争用的工作负载上，CoAgent在$1.4	imes$加速比和接近序列化令牌成本下，将正确率保持在序列化的5%以内，而2PL和OCC几乎放弃了所有并发增益；在一个仅bash的目标系统上，它在线增长了一个25工具的库，并以$0.80	imes$的时间和$0.86	imes$的成本将任务通过率从45/71提升到63/71。

## Abstract
Multi-agent LLM systems -- coding agents, devops agents, document agents -- now routinely run several agents in parallel against the same git tree, Kubernetes cluster, or document. As soon as two of them mutate shared state, they enter the regime classical concurrency control has studied for decades, but classical mechanisms fit LLM agents poorly. A single agent transaction spans minutes of inference, read sets are broad and opaque rather than statically inferable, and the live state agents act on admits neither fork nor buffer, so writes take effect the moment they execute. Locks block long inference intervals; OCC abort-and-retry discards minutes of work on every conflict.   This paper builds concurrency control on a capability classical transactions lack: the LLM inside each agent can judge whether a conflicting write invalidates its plan, and can repair exactly the operations that depended on it. Control therefore turns advisory: the runtime informs, the agent repairs. Our protocol, MTPO (Monotonic Trajectory Pre-Order), fixes a serialization order at launch, serves each read the order-filtered value, and applies writes speculatively in place; a one-way notification asks an affected reader to re-judge and patch its plan, while the framework mechanically undoes and reorders misplaced writes through the saga-style inverse each tool registers in advance. At quiescence the run is serializable in the pre-decided order. We realize MTPO as CoAgent, toolcall middleware whose privileged ToolSmith grows footprint-declared, undoable tools online. On ten contended workloads, CoAgent stays within 5\% of serial correctness at a $1.4\times$ speedup and near-serial token cost, where 2PL and OCC surrender nearly all concurrency gains; on a bash-only target system, it grows a 25-tool library online and lifts the task pass rate from 45/71 to 63/71 at $0.80\times$ the time and $0.86\times$ the cost.