---
title: "Self-Questioning Vision-Language Models: Reinforcement Learning for Compositional Visual Reasoning"
title_zh: 自我提问式视觉语言模型：用于组合视觉推理的强化学习
authors: Saraswathy Amjith
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15651v1"
tags: ["query:vl"]
score: 9.0
evidence: 使用强化学习的自问自答视觉语言模型
tldr: "本文提出自提问框架，利用GRPO强化学习训练VLM将视觉问题分解为子问题并逐步回答。在3B参数模型上，无需人工标注分解示例，模型通过奖励信号自行学会分解策略。在A-OKVQA上，自提问方法准确率达52.2%，优于无训练模型的46.8%，为复杂视觉推理提供了高效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15651v1/fig-001.webp\", \"caption\": \"Figure 2. Examples from the two evaluation benchmarks. Top: A-OKVQA, which pairs real-world images with open-ended questions requiring external knowledge. Bottom: CLEVR, which poses compositional reasoning questions over synthetic 3D scenes with geometric objects.\", \"page\": 5, \"index\": 1, \"width\": 350, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-002.webp\", \"caption\": \"Figure 3. Overview of image processing in Qwen2.5-VL. The input image is divided into 14×14 patches, which are encoded by a Vision Transformer (ViT) into a sequence of vision tokens. These vision tokens are interleaved with text tokens in the model’s input sequence, delimited by special <img> tokens.\", \"page\": 5, \"index\": 2, \"width\": 350, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-003.webp\", \"caption\": \"Table 3. GRPO training hyperparameters, matched across all models and datasets.\", \"page\": 9, \"index\": 3, \"width\": 454, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-004.webp\", \"caption\": \"Figure 1. Overview of the self-questioning framework. Given an image and question, the VLM generates sub-questions, answers each by attending to the image, and produces a final answer. The reward function requires both correct format and correct answer; GRPO uses group-normalized advantages to update the policy.\", \"page\": 3, \"index\": 4, \"width\": 350, \"height\": 520}]"
motivation: 现有VLM在组合视觉推理上表现不佳，而人工编写逐步解释的标注成本高且难以扩展。
method: 提出自提问框架，使用Group Relative Policy Optimization (GRPO) 训练VLM生成子问题并回答，奖励信号同时考虑子问题与最终答案的正确性。
result: "在A-OKVQA上，自提问模型准确率52.2%，标准RL为51.6%，均显著高于未训练模型的46.8%。"
conclusion: 教AI自我提问中间子问题是一种有前途的复杂视觉推理策略，尤其当问题难度需要显式逐步分解时。
---

## 摘要
视觉语言模型（VLM）是同时处理图像和文本的AI系统，但它们在处理需要多步骤链式推理的复合视觉推理问题时常常遇到困难，例如识别物体、计数并比较结果。现有方法通过训练模型使用人工编写的逐步解释来改进推理，但创建这些标注成本高昂且难以扩展。我们提出了一种自我提问框架，该框架训练VLM将视觉问题分解为较小的子问题，并在给出最终答案之前逐一回答每个子问题，使用一种称为组相对策略优化（GRPO）的强化学习算法。模型从未见过如何分解问题的示例，它通过奖励信号自主发现这种行为，该信号评估输出是否包含子问题以及最终答案是否正确。我们将此框架应用于一个30亿参数的模型，在合成几何形状场景（CLEVR）和真实世界照片（A-OKVQA）上进行训练。在A-OKVQA上，自我提问和标准强化学习相比未训练模型都显著提高了准确率（分别为52.2%和51.6%，对比46.8%）。我们通过不仅像标准RL那样奖励最终答案，还额外奖励生成中间子问题，引入了第一个自我提问VLM，使其能够发现组合分解策略。这些结果表明，教会AI系统自我提出中间问题是处理复杂视觉推理的一种有前景的策略，尤其是在问题难度需要显式逐步分解的情况下。

## Abstract
Vision-Language Models (VLMs) are AI systems that process both images and text, yet they often struggle with compositional visual reasoning questions that require chaining multiple steps together, such as identifying objects, counting them, and comparing the results. Existing approaches improve this reasoning by training models on human-written step-by-step explanations, but creating these annotations is expensive and difficult to scale. We propose a self-questioning framework that trains a VLM to break visual questions into smaller sub-questions and answer each one before producing a final response, using a reinforcement learning algorithm called Group Relative Policy Optimization (GRPO). The model is never shown examples of how to decompose questions, it discovers this behavior on its own, guided by a reward signal that scores whether the output contains sub-questions and whether the final answer is correct. We apply this framework to a 3-billion-parameter model, training on both synthetic scenes of geometric shapes (CLEVR) and real-world photographs (A-OKVQA). On A-OKVQA, both self-questioning and standard reinforcement learning substantially improve accuracy over the untrained model (52.2% and 51.6% vs. 46.8%). We introduce the first self-questioning VLM by rewarding not only the final answer like standard RL but additionally for generating intermediate sub-questions, enabling it to discover compositional decomposition strategies. These results suggest that teaching AI systems to ask themselves intermediate questions is a promising strategy for complex visual reasoning, particularly when the difficulty of a question warrants explicit step-by-step decomposition.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有视觉语言模型（VLM）在处理组合视觉推理（compositional visual reasoning）时表现不佳。这类问题需要多步骤链式推理，例如先识别物体、再计数、最后比较。VLM通常通过单次前向传播直接输出答案，缺乏显式中间推理步骤。
- **现有方法的局限**：提升组合推理的现有方法依赖于人工编写的逐步解释（如链式思维提示、监督微调或视觉编程）。但这些方法要么需要昂贵的标注数据，要么依赖事先设计好的工具库，难以扩展和泛化。
- **本文意义**：提出一种无需人类演示的自我提问（self-questioning）框架，利用强化学习（RL）让VLM自主学会将视觉问题分解为子问题并逐步回答，从而提升复杂视觉推理能力。这是首次将组相对策略优化（GRPO）应用于多模态视觉推理，并引入对子问题格式的奖励。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：训练VLM在回答最终问题之前，先生成一系列子问题-答案对（sub-question/answer pairs），再基于这些中间信息给出最终答案。模型通过RL从零开始学习这一分解行为，不需要任何人类编写的分解示例。
- **关键技术细节**：
  - **模型**：Qwen2.5-VL-3B-Instruct（30亿参数），结合视觉和文本输入。
  - **算法**：组相对策略优化（GRPO）。GRPO无需单独的critic网络，通过对同一提示（image+question）采样多个完成（G=8），计算组内归一化优势，并使用带KL散度惩罚的目标更新策略。
  - **奖励函数**：同时要求格式合规（至少包含一对子问题-答案）和最终答案正确。两者均满足得1.0分，否则-1.0分。
  - **提示设计**：系统提示要求模型按固定格式输出子问题、子答案和最终答案，但不提供任何示例，以避免模仿特定分解模式。
  - **参数高效微调**：使用LoRA（rank=16, alpha=32），仅更新q_proj和v_proj，更新参数少于1%。
- **公式流程**（文字说明）：
  1. 对每个训练样本（图像I, 问题q），模型根据当前策略采样G个完成序列y_i。
  2. 每个完成序列经奖励函数R(y_i, a*)评分。
  3. 计算组内均值μ_g和标准差σ_g，得到归一化优势Â_i = (R_i - μ_g)/σ_g。
  4. 使用裁剪的代理目标和KL散度惩罚更新策略参数θ，使得高优势完成更可能被生成。

## 3. 实验设计

- **数据集**：
  - **CLEVR**：合成几何形状场景（10,000样本），用于评估组合视觉推理基准。
  - **A-OKVQA**：真实世界照片（10,000样本），需要外部知识和复杂推理。
- **基准（Benchmark）**：使用各自验证集的500样本评估准确率（答案包含匹配）。
- **对比方法**：
  - **Base**：未训练的Qwen2.5-VL-3B-Instruct。
  - **Direct+GRPO**：标准RL训练，仅要求直接答案，无子问题格式。
  - **SQ+GRPO**：自提问框架，要求生成子问题-答案对。
  - 另外做了跨数据集迁移实验（CLEVR训练的模型在A-OKVQA上测试等）和提示消融（用不同提示格式评估同一模型）。

## 4. 资源与算力

- **文中明确说明**：使用了两块48 GB显存的GPU（未指定具体型号，但推测为NVIDIA A6000或类似）。训练时采用LoRA，以避免内存不足。
- **训练步数**：约7,000步（steps），每个批次有效批大小4，每组采样8个完成。
- **未明确提及**：总训练时间、能源消耗、单次训练的具体时长。但称其“紧凑的3B模型使RL训练在学术级硬件上可行”。

## 5. 实验数量与充分性

- **主要实验组**：
  1. 同数据集训练评估（CLEVR和A-OKVQA上分别训练SQ+GRPO和Direct+GRPO）。
  2. 跨数据集迁移实验（CLEVR训练的模型在A-OKVQA上评估，反之亦然）。
  3. 提示消融实验（如用直接提示评估SQ模型，用SQ提示评估Direct模型）。
  4. 定性分析（检查输出示例及失败案例）。
- **实验充分性**：实验设计相对全面，涵盖了同域和跨域泛化、格式影响、奖励曲线分析。但仅使用了两个数据集（一个合成、一个真实），且模型规模限于3B。消融实验很好地区分了RL本身和自提问格式的贡献。但未与其他基线（如CoT提示、监督微调）进行直接数值对比，也未在更大模型（7B、72B）上验证。总体而言，实验数量适中，结论有依据但有限。

## 6. 主要结论与发现

- **RL训练显著提升准确率**：在A-OKVQA上，标准RL（Direct+GRPO）从46.8%提升至51.6%，自提问（SQ+GRPO）达52.2%。RL是主要增益来源。
- **自提问带来边际额外收益**：自提问相比标准RL仅额外+0.6%。但优势体现在跨域迁移：CLEVR训练的SQ模型在A-OKVQA上提升2.6%，而标准RL仅提升0.2%，说明自提问学会了领域通用的分解策略。
- **格式税（Format Tax）**：对简单问题（如CLEVR上的颜色识别），强制生成子问题反而降低准确率（A-OKVQA训练的SQ模型在CLEVR上从97.4%降至81.0%），但使用直接提示后恢复，证明损失来自提示格式而非模型能力损坏。
- **定性发现**：模型100%学会输出格式，但子问题有时仅是原问题复述而非真正分解，表明RL不能保证推理质量。
- **失败案例**：自提问在简单任务（颜色、空间关系、活动识别）上引入误差，但在计数（+50%）和材料识别（+33%）等需要多步推理的任务上效果更佳。

## 7. 优点

1. **无需人工标注思维链**：首次将GRPO应用于多模态VLM，使模型自主学会分解行为，大幅降低数据成本。
2. **奖励设计创新**：同时奖励格式合规和答案正确，将分解本身作为训练目标，促使结构化输出。
3. **跨域迁移验证**：合成场景训练的分解策略能有效迁移到真实世界照片，证明了方法的泛化能力。
4. **消融设计清晰**：区分RL与自提问各自贡献的对比实验（Direct+GRPO vs SQ+GRPO）设计严谨。
5. **参数高效**：使用LoRA在学术级硬件上完成训练，降低了计算门槛。

## 8. 不足与局限

1. **实验覆盖有限**：仅测试了单个模型规模（3B）、两个数据集，未在更大规模（如7B、72B）或更多样化任务（如医学影像、自动驾驶）上验证。
2. **未与最新基线全面对比**：论文未与Multimodal-CoT、ViperGPT等方法的近似设置直接比较准确率，缺乏绝对性能定位。
3. **推理质量难以保证**：RL倾向于学习格式而非真正推理，子问题常为复述，可能产生虚假中间步骤。
4. **格式税问题**：对简单问题强制分解会降低性能，缺乏自适应机制。
5. **完整微调与更复杂任务未探索**：未来工作需考虑更大模型和全参数微调；当前RL可能主要校准了输出分布而不是提升深层推理。
6. **潜在偏差风险**：A-OKVQA的数据分布和外部知识要求可能使模型依赖表面线索，跨域评估仅从CLEVR到A-OKVQA，反向迁移未充分探讨。

（完）
