---
title: "CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs"
title_zh: CheXanatomy：面向胸部X光片的解剖感知视觉语言建模
authors: "Sergios Gatidis, Curtis Langlotz, Christian Bluethgen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08420v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 通过下一个词预测对胸部X光片进行解剖感知的VLM分割
tldr: 该论文提出了CheXanatomy框架，通过自回归的token空间监督将解剖学知识整合到预训练的视觉语言模型中，无需特定任务解码器即可生成解剖分割掩码。利用CT数据合成X光片和投影标签以获取可扩展的标注。在合成和真实X光片上评估，性能与U-Net相当，且对领域偏移具有更好的鲁棒性，解剖预训练提高了样本效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08420v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of CheXanatomy. CT volumes with anatomic labels are projected into synthetic CXRs, and bounding boxes and segmentation masks are encoded into structured tokens. A pretrained vision–language model (based on the Paligemma architecture) is fine-tuned to autoregressively generate anatomical bounding boxes and segmentations via next-token prediction. The trained model approaches convolutional baselines in-distribution and demonstrates improved geometric robustness under domain shift, while supporting adaptation to new localization tasks. Unlike conventional segmentation networks, no task-specific decoder heads are introduced, and supervision is applied entirely in token space.\", \"page\": 3, \"index\": 1, \"width\": 725, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-002.webp\", \"caption\": \"Fig. 2. Segmentation performance on synthetic CT-RATE (top) and combined realworld radiographs from CheXmask and VinDr-RibCXR (bottom). Boxplots show Dice, IoU, Hausdorff distance, and Fourier descriptor distance across model scales and input resolutions. On synthetic data, the best PaliGemma models approach U-Net in overlap metrics and achieve lower boundary and shape errors. Under domain shift, PaliGemma maintains comparable Dice and IoU while consistently reducing boundary errors relative to U-Net. Models are named based on size (3B vs. 10B) and input image size (224× 224 vs. 448× 448). FV = Vision encoder frozen during training.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-003.webp\", \"caption\": \"Fig. 3. Representative examples for out-of-distribution segmentation performance of anatomy-trained VLMs and a specialized U-Net compared to ground truth segmentations. Only VLMs with frozen vision encoders during fine-tuning are shown. Top row: CheXmask example (lungs and heart). Bottom row: VinDr-RibCXR example (bilateral rib structures). Compared to U-Net, anatomy-trained VLMs demonstrate more stable geometric structure and reduced boundary irregularities under domain shift.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-004.webp\", \"caption\": \"Fig. 4. Performance scaling on the RadVLM grounding benchmark comparing anatomy-pretrained and baseline models across training data fractions. Top: Success rate and mean IoU as a function of available training data (0–100%). Anatomypretrained models show clear advantages in the low-data regime (0–3%), with performance converging as more task-specific data becomes available. Bottom: Representative grounding examples for two tasks (“detect pulmonary artery enlargement” and “detect left hemidiaphragm”) across data fractions. Green: ground truth, red: anatomypretrained model (3B-448), blue: baseline model (3B-448). Anatomy pretraining provides improved localization accuracy and stability when supervision is limited.\", \"page\": 8, \"index\": 4, \"width\": 725, \"height\": 487}]"
motivation: 现有视觉语言模型主要优化全局对齐，缺乏显式的细粒度解剖结构编码，限制了在分割等空间精确任务中的应用。
method: 通过自回归的token空间监督，在预训练视觉语言模型中直接生成解剖分割掩码，并利用CT合成X光片和投影标签实现可扩展监督。
result: 在合成和真实X光片上，该方法性能与U-Net相当，且对领域偏移的几何鲁棒性更好，解剖预训练提升了有限标注下的样本效率。
conclusion: 将解剖结构嵌入生成目标可促进空间接地表示，支持解剖感知的医学视觉语言建模。
---

## 摘要
在大规模图像-文本对上预训练的视觉语言模型（VLM）展现出强大的图像级理解能力，但主要针对全局对齐进行优化，并未显式编码细粒度解剖结构，限制了其在空间精确任务（如分割）中的适用性。我们提出CheXanatomy框架，通过自回归令牌空间监督将显式解剖知识集成到预训练VLM中。模型无需添加特定任务解码器头，而是通过下一个令牌预测训练生成解剖分割掩码。为实现可扩展的监督，我们从CT体数据合成逼真的胸部X光片，并前向投影CT分割标签以获得解剖一致性的二维掩码。我们在合成和真实胸部X光片上以U-Net为基线评估该方法，包括模型规模、输入分辨率和视觉编码器微调的消融实验。自回归解剖监督在分布内达到了与专门卷积模型相当的性能，并在域迁移到真实CXR数据时展现出更强的几何鲁棒性。此外，在有限监督下适应新定位任务时，解剖预训练模型表现出更高的样本效率。更大的模型和更高的输入图像分辨率提升了性能，而视觉编码器微调效果有限。这些结果表明，将解剖结构直接嵌入生成目标有利于空间接地表示，并支持解剖感知的医学视觉语言建模。

## Abstract
Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

---

## 论文详细总结（自动生成）

# 论文详细总结：CheXanatomy

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的视觉语言模型（VLM）预训练在大规模图像-文本对上，主要优化全局图像-文本对齐，缺乏对细粒度解剖结构的显式编码。这导致它们在需要精确空间理解的任务（如医学图像分割）中表现不佳，尤其是面对投影几何重叠、解剖结构变异性以及领域偏移（从合成到真实X光片）时，传统的卷积分割模型（如U-Net）依赖密集像素标注且在域外鲁棒性差。
- **整体含义**：该工作旨在将显式的解剖学知识直接注入预训练VLM的自回归生成目标中，使得模型在不增加专用解码器的情况下，能够生成解剖分割结果。这有助于构建空间接地、可泛化的医学视觉语言基础模型，为多任务推理、报告生成等下游任务提供结构化的解剖先验。

## 2. 方法论：核心思想与关键技术细节
- **核心思想**：使用自回归的令牌空间监督（token-space supervision）将解剖分割任务转化为序列预测问题。模型（基于PaliGemma 2架构）通过下一个令牌预测（next-token prediction）来生成边界框和分割掩码，无须引入像素级损失或额外的解码器头。
- **关键技术细节**：
  - **模型架构**：SigLIP-So400m视觉编码器 + 线性投影到语言令牌空间 + Gemma 2大语言模型。分割掩码通过一个VQ-VAE编码为16个离散令牌，模型预测这些令牌后用VQ-VAE解码器重建掩码。
  - **可扩展标注生成**：利用CT-RATE数据集中的非增强CT体数据，通过可微分DRR渲染（Diff-DRR）生成前后位和侧位合成X光片；使用TotalSegmentator获得3D解剖分割，经过前向投影得到与合成X光片对齐的2D掩码。覆盖80个解剖目标（胸廓骨骼、肺、心脏、大血管、上腹部器官等），并采用随机缩放增强模拟解剖大小变化。
  - **参数高效微调**：使用LoRA（低秩适应）进行微调，保持大部分预训练权重固定。实验两种设置：冻结视觉编码器 vs. 微调视觉编码器。
  - **训练损失**：仅使用标准的自回归交叉熵损失（令牌空间），无像素级损失或辅助目标。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - **训练**：CT-RATE训练集的12,984个体积，每个生成前后位和侧位合成X光片共25,968张训练图像。
  - **测试**：
    - 合成域：CT-RATE测试集的683个体积，共1,366张合成X光片。
    - 真实域：CheXmask（200张前后位X光片，心脏和肺掩码）和VinDr-RibCXR（198张前后位X光片，双侧肋骨分割）。
    - 少样本迁移：RadVLM视觉接地数据集（118,360训练样本，22,972测试样本）。
- **基准**：
  - U-Net：二维U-Net（4个编码阶段，特征通道32→64→128→256，瓶颈512，输入448×448）在相同合成训练数据上训练。
  - 无解剖预训练的基线VLM（PaliGemma）。
- **对比方法**：CheXanatomy（不同配置：3B/10B参数、224×224/448×448输入、冻结/微调视觉编码器） vs. U-Net vs. 基线VLM。

## 4. 资源与算力
- 论文明确说明：使用8块H100 GPU进行训练。
- 批量大小：根据模型配置不同，范围为8（10B参数模型，448×448分辨率）到24（3B参数模型，224×224分辨率）。
- 训练时长：未给出具体小时数。仅提及使用LoRA进行参数高效微调，未说明总迭代步数或epoch数。

## 5. 实验数量与充分性
- **实验组数**：主要包含四类实验：
  1. **分布内性能**：在合成CT-RATE测试集上对比U-Net与不同VLM配置（3B/10B, 224/448, frozen/ft）的Dice、IoU、Hausdorff距离、Fourier描述子距离。
  2. **域外泛化**：在真实CheXmask+VinDr-RibCXR数据集上重复上述指标对比。
  3. **消融实验**：模型规模（3B vs. 10B）、输入分辨率（224 vs. 448）、视觉编码器微调策略（冻结 vs. 微调）的消融。
  4. **少样本迁移**：在RadVLM上使用0%、1%、3%、10%、100%训练数据进行微调，对比解剖预训练模型与基线模型的bounding box IoU和成功率。
- **充分性评价**：实验设计较为全面，覆盖了分布内、域外、消融和少样本学习，指标包括重叠度量（Dice, IoU）和边界/形状度量（Hausdorff, FDD），对比了强基线U-Net和未预训练基线，结果通过箱线图可视化。然而，仅在两个真实数据集上评估域外性能（且仅含心脏/肺和肋骨），未验证更多解剖结构在真实数据上的表现。此外，未与其他VLM分割方法（如SAM、CLIP-based）比较，也未提供统计显著性检验。总体而言实验充分但尚有拓展空间。

## 6. 主要结论与发现
- **分布内性能**：最佳PaliGemma配置（10B, 448分辨率）在合成数据上Dice和IoU接近U-Net，而Hausdorff距离和Fourier描述子距离更低（边界和形状更优）。
- **域外泛化**：在真实X光片上，VLM保持了与U-Net相当的Dice/IoU，同时在边界和形状指标上显著优于U-Net，展现出更强的几何稳定性和形状保真度。
- **消融结果**：
  - 更高的输入分辨率（448 vs. 224）一致提升性能。
  - 更大的模型（10B vs. 3B）带来增益。
  - 微调视觉编码器对性能几乎没有影响，表明解剖推理主要发生在多模态令牌空间。
- **少样本迁移**：解剖预训练模型在0–3%极低标注量下显著优于未预训练基线，随标注量增加差距缩小直至收敛，说明解剖预训练提供了强空间先验，提升样本效率。
- **整体结论**：将解剖结构嵌入生成目标能够促进空间接地表示，支持解剖感知的医学视觉语言建模。

## 7. 优点
- **方法创新**：首次将显式解剖监督以自回归令牌预测形式注入预训练VLM，无需修改架构或添加解码器，保持了VLM的通用性。
- **数据高效**：利用CT投影生成合成X光片和标签，避免了昂贵的人工CXR标注，且覆盖80个解剖结构，可无限扩展。
- **参数高效**：使用LoRA微调，计算资源需求适中。
- **鲁棒性验证**：在真实X光片上验证了域外泛化，并展示了边界/形状指标的改善，超越了传统U-Net。
- **少样本学习价值**：低数据场景下的表现提升具有临床实际意义（标注稀缺）。

## 8. 不足与局限
- **实验覆盖不足**：
  - 域外测试仅覆盖心脏/肺（CheXmask）和肋骨（VinDr-RibCXR），未检验更多类型解剖结构（如纵隔、血管、膈肌等）在真实数据上的分割质量。
  - 仅在整体数据集上报告指标，未对不同患者群体或疾病状态（如肺不张、积液）进行亚组分析，可能存在偏差。
  - 未与SAM、MedSAM等专用医学分割模型进行对比，仅与U-Net对比。
  - 未进行统计显著性检验（如置信区间或p值）。
- **合成数据风险**：合成X光片可能无法完全模拟真实X光片的噪声、散射、病人定位等物理效应，模型在真实数据上的鲁棒性可能仍有限。
- **应用限制**：
  - 分割输出精度受限于VQ-VAE表示（16个令牌），可能丢失细粒度细节。
  - 模型对提示词汇敏感（同义词采样），未评估不同提示的影响。
  - 仅基于PaliGemma架构，可推广性需在其他VLM上验证。
- **资源与复现**：训练具体时长未报告，不利于复现成本估计。

## （完）
