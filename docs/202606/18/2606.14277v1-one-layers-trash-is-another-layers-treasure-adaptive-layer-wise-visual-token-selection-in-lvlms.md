---
title: "One Layer's Trash is Another Layer's Treasure: Adaptive Layer-wise Visual Token Selection in LVLMs"
title_zh: 一层的弃物，另一层的珍宝：LVLMs中的自适应逐层视觉Token选择
authors: "Yongru Chen, Kai Zhang, Zeliang Zong, Yuchen Lu, Wenming Tan, Ye Ren, Jilin Hu"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14277v1"
tags: ["query:vl"]
score: 9.0
evidence: LVLM的自适应逐层视觉令牌剪枝
tldr: "大型视觉语言模型（LVLMs）因视觉令牌过长导致计算负担。现有剪枝方法在特定层剪除后令牌不可恢复，造成信息丢失。本文发现不同层关注不同视觉区域，提出自适应层间视觉令牌选择（ALVTS），通过轻量选择器路由重要令牌继续处理，次要令牌跳过层后重新合并，实现层间自适应压缩。基于重要性一致性低秩近似，无需重训练。在LLaVA等模型上，89%压缩率下保留96.7%准确率，取得优越效率-准确率权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14277v1/fig-001.webp\", \"caption\": \"Table 3. Performance Comparison of LLaVA-NeXT-7B. The vanilla number of visual tokens varies by dataset due to dynamic image processing (up to 2880 tokens). “Avg.” refers to the average performance across 8 benchmarks. Best results are shown in bold.\", \"page\": 6, \"index\": 1, \"width\": 1018, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-002.webp\", \"caption\": \"Figure 1. Layer-wise visual token attention patterns of FastV. Colored and grey regions indicate important and unimportant tokens, respectively. Red boxes highlight representative attention regions in each layer.\", \"page\": 2, \"index\": 2, \"width\": 1022, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-003.webp\", \"caption\": \"Figure 4. The distribution of L2 distance between the approximate importance scores and the oracle importance scores.\", \"page\": 7, \"index\": 3, \"width\": 485, \"height\": 737}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-004.webp\", \"caption\": \"Figure 3. Ablation study of dynamic token selection mechanism.\", \"page\": 7, \"index\": 4, \"width\": 480, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of ALVTS. The framework performs adaptive layer-wise visual token selection. At each LLM decoder layer, a lightweight token selector computes importance scores using low-rank approximations of full attention mechanism. Importance tokens participate in the layer’s computation while less important tokens skip the layer. Both streams of tokens are reintegrated before the next layer, enabling adaptive compression across the model.\", \"page\": 3, \"index\": 5, \"width\": 1014, \"height\": 509}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-006.webp\", \"caption\": \"Figure 5. The case comparison between FastV and ALVTS. It presents original images alongside their pruned versions. Grey mask areas indicate discarded tokens. Red bounding boxes highlight key objects referenced in prompts.\", \"page\": 8, \"index\": 6, \"width\": 1024, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2606.14277v1/fig-007.webp\", \"caption\": \"Table 1. Performance comparison of LLaVA-1.5-7B under different token reduction ratios. “Avg.” refers to the average performance across 8 benchmarks. Best results are shown in bold.\", \"page\": 5, \"index\": 7, \"width\": 1022, \"height\": 621}]"
motivation: 现有视觉令牌剪枝方法一旦剪除则后续层无法访问，导致过早信息丢失，而不同层需要不同的最优令牌子集。
method: 提出自适应层间视觉令牌选择（ALVTS），包含轻量令牌选择器，将重要令牌路由到当前层处理，次要令牌跳过层后合并，通过重要性一致性低秩近似模拟全注意力机制。
result: "在LLaVA-1.5、LLaVA-NeXT和Qwen2.5-VL上，89%令牌压缩率下保留96.7%原始准确率。"
conclusion: ALVTS实现了高效且自适应的视觉令牌压缩，无需重训练，显著提升了LVLM推理的效率-准确率权衡。
---

## 摘要
大型视觉语言模型（LVLMs）在多样化的多模态任务中取得了显著成功，但其实际部署仍受到长序列视觉token带来的计算负担的制约。虽然视觉token剪枝已成为一种有前景的解决方案，但现有方法存在根本性局限：一旦token在特定层被剪枝，它们便无法被后续所有层访问，导致过早的信息丢失，从而损害模型性能。通过实验研究，我们观察到不同层展现出不同的视觉区域关注点，表明各层存在不同的最优token子集。受此启发，我们提出自适应逐层视觉Token选择（ALVTS），这是一种新颖的框架，打破了传统的静态token剪枝范式。ALVTS集成了一个轻量级token选择器，用于识别并路由重要token进行进一步处理，同时允许不重要的token跳过该层，从而最小化计算冗余。这两路token在送入后续层之前被无缝地重新整合，促进了整个模型的自适应压缩。基于我们提出的重要性一致性约束低秩近似，所提出的token选择模块紧密模拟了完整注意力机制，有效捕获了其关键模式，且无需重新训练模型。在LLaVA-1.5、LLaVA-NeXT和Qwen2.5-VL上进行的大量实验验证了我们方法的有效性。在89%的token压缩比下，ALVTS保留了原始模型96.7%的准确率，实现了LVLM推理中优越的效率-准确率权衡。

## Abstract
Large Vision-Language Models (LVLMs) have achieved remarkable success across diverse multimodal tasks, yet their practical deployment remains constrained by the computational burden arising from lengthy visual tokens. While visual token pruning has emerged as a promising solution, existing methods suffer from a fundamental limitation: once tokens are pruned at a specific layer, they become inaccessible to all subsequent layers, leading to premature information loss that can compromise model performance. Through empirical studies, we observe that different layers exhibit distinct visual region focus, indicating a varying optimal token subset across layers. Motivated by this insight, we propose Adaptive Layer-wise Visual Token Selection (ALVTS), a novel framework that breaks away from the conventional static token pruning paradigm. ALVTS incorporates a lightweight token selector to identify and route important tokens for further processing, while allowing less important tokens to skip the layer, thus minimizing computational redundancy. These two streams of tokens are seamlessly reintegrated before being fed into subsequent layers, facilitating adaptive compression across the entire model. Grounded in our importance consistency constrained low-rank approximation, the proposed token selection module closely emulates the full attention mechanism, effectively capturing its essential patterns without requiring model retraining. Extensive experiments on LLaVA-1.5, LLaVA-NeXT, and Qwen2.5-VL validate the effectiveness of our method. With an 89% token compression ratio, ALVTS retains 96.7% of the original model's accuracy, achieving a superior efficiency-accuracy trade-off for LVLM inference.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：大型视觉语言模型（LVLMs）在图像描述、视觉问答等任务中表现出色，但视觉token数量远多于文本token（如LLaVA-NeXT最多2880个视觉token），导致计算负担巨大，限制实际部署。
- **现有方法局限**：已有的视觉token剪枝方法（如FastV、PyramidDrop等）在特定层一次性剪除不重要token后，这些token对所有后续层均不可访问，造成不可逆的信息丢失。然而，不同层对视觉区域的关注点不同（例如浅层关注桥梁主体，深层关注桥名牌匾），因此单一剪枝策略会忽略深层所需的关键区域。
- **核心动机**：各层需要的最优token子集不同，需要一种动态、逐层的token选择机制，既能压缩计算量，又能保留各层所需的关键视觉信息。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出自适应逐层视觉Token选择（ALVTS），每个LLM解码层独立地计算视觉token重要性，仅选择重要token参与该层计算，不重要token跳过该层，然后将两路token重新合并送入下一层，实现动态压缩。
- **关键技术细节**：
  - **低秩近似重要性估计**：为避免完整注意力计算的开销，使用低秩分解（SVD）近似查询（Q）和键（K）投影矩阵。将原始投影矩阵分解为 `W_Q ≈ U_Q V_Q` 和 `W_K ≈ U_K V_K`，其中 `R << D`。
  - **注意力分数近似**：用近似Q、K计算所有token间的注意力权重矩阵 `\tilde{A}`，并从中提取视觉-视觉注意力 `\tilde{A}_{V2V}` 和文本-视觉注意力 `\tilde{A}_{T2V}`。
  - **重要性分数计算**：对每个视觉token i，计算其接收的平均视觉-视觉注意力分数 `S_{V2V}(i)` 和平均文本-视觉注意力分数 `S_{T2V}(i)`，最终重要性分数为两者乘积 `S(i) = S_{V2V}(i) · S_{T2V}(i)`。只有同时被视觉和文本上下文关注的token才被认为重要。
  - **动态选择与合并**：基于重要性分数进行top-k选择，保留 `k = ⌊r·N⌋` 个重要token参与当前层计算，其余token跳过。输出后重新按原始顺序合并，保持位置信息。每层独立重复该过程。
  - **优化策略**：先用SVD初始化低秩矩阵，再通过最小化近似分数与真实注意力分数之间的L2损失（式11）进行轻量级微调（仅更新选择器参数，冻结LVLM骨干）。整个优化过程在LLaVA-1.5-7B上只需15分钟，参数量增加不到2%。

## 3. 实验设计
- **数据集/场景**：8个多模态基准：AI2D、POPE、TextVQA、OKVQA、VizWiz、COCO Caption、NoCaps、RealWorldQA，覆盖通用VQA、文本VQA、图像描述、多模态推理等任务。
- **基准模型**：LLaVA-1.5 (7B/13B)、LLaVA-NeXT-7B、Qwen2.5-VL-3B-Instruct。
- **对比方法**：FastV、VTW、PyramidDrop、DART等4种主流视觉token压缩方法。为公平比较，调整各方法超参数使其FLOPs与FastV一致。

## 4. 资源与算力
- 文中明确提及：
  - **优化耗时**：LLaVA-1.5-7B上整个优化过程可在15分钟内完成（使用单GPU）。
  - **推理配置**：效率分析实验在POPE数据集上使用单张4090 GPU进行。
- **未明确说明事项**：未给出完整训练/优化所使用的GPU型号数量、批量大小等具体细节；也未给出大规模试验的总算力消耗。

## 5. 实验数量与充分性
- **实验数量**：
  - **主实验**：在LLaVA-1.5-7B上进行三种压缩比（67%、78%、89%）下的8个基准对比；在LLaVA-1.5-13B上报告平均性能；在LLaVA-NeXT-7B上进行89%压缩比下的对比；在Qwen2.5-VL-3B上进行89%压缩比下的对比。所有对比均包含4种基线方法。
  - **消融实验**：
    - 动态选择机制消融：在LLaVA-1.5-7B上对比ALVTS与静态剪枝版（固定token子集）在POPE、OKVQA、COCO、NoCaps上的性能。
    - 选择器优化有效性：在AI2D数据集上随机采样1000个实例，统计优化前后近似分数与真实分数的L2距离分布。
  - **效率分析**：在POPE上比较总推理时间、延迟、prefill时间、准确率。
  - **可视化**：提供2个case对比ALVTS与FastV的注意力区域。
- **充分性与公平性**：
  - 实验覆盖了不同模型规模（7B、13B、3B）、不同分辨率输入（固定576 tokens、可变2880 tokens）、不同架构（LLaVA系列、Qwen系列），具有较好泛化性。
  - 对比方法均为近年会议论文中的代表性方法，且通过FLOPs对齐保证公平。
  - 消融实验直接验证了核心贡献（动态选择、低秩近似优化），设计合理。
  - 但未对更多极端压缩比（如95%以上）或更多下游任务（如视频理解、视觉定位）进行测试，实验覆盖仍有拓展空间。

## 6. 主要结论与发现
- ALVTS在所有压缩比下均优于所有基线方法，尤其在高压缩比（89%）下仍有96.73%的平均性能保持率（LLaVA-1.5-7B），比次优方法DART高3.46%。
- 动态选择机制比静态剪枝在COCO和NoCaps上分别提升7.76%和4.83%，验证了“不同层需要不同token子集”的核心假设。
- 低秩选择器经优化后模拟完整注意力的L2距离显著降低，证明其能可靠近似真实重要性。
- 在LLaVA-NeXT-7B（高分辨率）上，89%压缩时保持96.25%性能，POPE甚至略超原始模型。
- 效率上，89%压缩下prefill加速1.6×，端到端延迟降低，且准确率远高于同样加速比的FastV。

## 7. 优点
- **思想创新**：首次提出“逐层动态选择”而非“全局静态剪枝”，解决信息不可逆丢失问题，直观并有效。
- **方法设计巧妙**：使用低秩近似避免全注意力计算，并通过轻量微调提升近似精度，无需重训练LVLM，实用性强。
- **实验充分**：覆盖多个模型、多个压缩比、多个基准，消融和可视化支撑充分。
- **性能优异**：在极高压缩比（89%）下仍保持96%+性能，优于现有方法，且加速效果明显。
- **跨模型泛化性强**：在LLaVA系列和Qwen2.5-VL上都取得一致优势。

## 8. 不足与局限
- **优化过程仍需少量数据与微调**：虽然声称“无需重训练”，但选择器仍需要在256个样本上进行梯度更新（约15分钟），并非完全零训练。某些极端场景可能无法获取校准数据。
- **未考虑极端压缩比**：仅测试到89%，对于更极限压缩（如95%+）的性能未知，可能仍有较大下降。
- **下游任务覆盖有限**：仅针对8个VQA/描述类基准，未涉及视频理解、视觉定位、字幕生成等更复杂的多模态任务，泛化结论需谨慎。
- **未讨论不同层选择token的重叠度/多样性**：动态选择可能使每个层重复选择相同token，导致计算冗余未被充分消除，论文未分析这一点。
- **未报告全模型参数量与FLOPs对比**：虽然给出每层参数增量（<2%），但未与基线方法对比总计算量，仅用FLOPs对齐作为公平性手段，缺乏更精细的效率分析。

（完）
