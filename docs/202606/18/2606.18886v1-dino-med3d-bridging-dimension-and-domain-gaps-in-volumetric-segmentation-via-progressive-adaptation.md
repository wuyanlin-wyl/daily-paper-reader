---
title: "DINO-Med3D: Bridging Dimension and Domain Gaps in Volumetric Segmentation via Progressive Adaptation"
title_zh: DINO-Med3D：通过渐进适配弥合三维医学分割中的维度与领域鸿沟
authors: "Haoyu Hu, Xiyao Ma, Shiqi Liu, Linsen Zhang, Xiaoliang Xie, Xiaohu Zhou, Zeng-Guang Hou"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18886v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 将DINOv3适配到3D医学分割
tldr: 针对预训练DINOv3直接用于3D医学分割的维度与域差异，提出DINO-Med3D两阶段渐进框架。第一阶段通过多切片嵌入和分割代理任务弥合间隙，第二阶段添加轻量3D适配器增强体积理解，并设计并行细节恢复流保留边界信息。在五个数据集上显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18886v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison. The best baselines are in gray . Scores exceeding the best and second-best baseline are bolded and underlined respectively.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2606.18886v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative comparison on representative samples. Images are cropped and zoomed for better visualization. Prominent regions are highlighted with boxes.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2606.18886v1/fig-003.webp\", \"caption\": \"Fig. 1. Schematic illustration of DINO-Med3D framework. Stage I aligns the pretrained 2D DINOv3 backbone with volumetric medical data via the Inter-slice Context Embedding (ICE) module and a pseudo-3D proxy task. Stage II freezes the aligned backbone and introduces a dual-stream architecture: a Volumetric Context Encoding Stream leveraging LoRA and lightweight 3D adapters for semantic reasoning, and a parallel Detail-Recovery Stream for capturing high-frequency boundary information. Finally, features are integrated via Adaptive Gated Fusion (AGF) for dense prediction.\", \"page\": 3, \"index\": 3, \"width\": 712, \"height\": 409}]"
motivation: 克服DINOv3从自然图像到3D医学分割的维度与域差异。
method: 提出两阶段渐进框架，包括多切片嵌入、分割代理任务、轻量3D适配器和并行细节恢复流。
result: 在五个公开医学分割数据集上，DINO-Med3D显著优于现有最先进方法。
conclusion: 成功将DINOv3适应到医学域，实现了高性能体积分割。
---

## 摘要
尽管DINOv3在自然图像上展现出了卓越的语义判别能力，但其直接应用于三维医学分割时受到固有的维度与领域差异的阻碍。为解决这些问题，我们提出了DINO-Med3D，一个两阶段的渐进框架，将预训练的DINOv3编码器重新用于三维医学任务。在第一阶段，我们通过引入一个融合伪三维上下文的多切片嵌入模块来弥合维度鸿沟，同时利用分割代理任务将使从自然场景学到的表示适应医学领域。随后，我们通过在冻结的主干网络中添加轻量级三维适配器来强制全局切片间连续性，进一步增强了三维理解。最后，为了弥补嵌入过程中固有的空间信息损失，我们设计了一个并行的细节恢复流以显式保留高频边界线索。在五个公开数据集上的大量实验表明，我们的方法成功地将DINOv3适应到医学领域，并显著优于现有最先进的基线方法。

## Abstract
Although DINOv3 has demonstrated remarkable semantic discrimination in natural imagery, its direct application to volumetric medical segmentation is hindered by inherent dimension and domain disparities. To resolve these issues, we propose DINO-Med3D, a two-stage progressive framework that repurpose the pre-trained DINOv3 encoder for 3D medical tasks. In the first stage, we mitigate the dimension gap by introducing a multi-slice embedding module that incorporates pseudo-3D context, while simultaneously employing a segmentation proxy task to adapt representations learned from natural scenes to the medical domain. Subsequently, we further enhance volumetric understanding by adding lightweight 3D adapters into the frozen backbone to enforce global inter-slice continuity. Finally, to compensate for the spatial information loss inherent in the embedding process, we design a parallel detail recovery stream to explicitly preserve high-frequency boundary cues. Extensive experiments on five public datasets demonstrate that our approach successfully adapts DINOv3 to the medical domain and significantly outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：DINOv3等2D自监督视觉基础模型在自然图像上表现优异，但直接应用于3D医学图像分割时面临两大鸿沟：**维度鸿沟**（2D模型无法处理3D体数据的切片间连续性）和**领域鸿沟**（自然图像与医学图像的纹理、对比度等特征分布差异大）。现有简单微调或投影方法无法有效弥合这些差距。
- **整体动机**：旨在设计一个渐进式两阶段框架，将预训练的2D DINOv3编码器转化为高性能的3D医学分割模型，无需依赖交互式提示或大规模三维预训练数据。
- **背景**：医学图像分割受限于标注数据稀缺；基础模型如SAM及其医学适配需要交互或自动提示；DINOv3等自监督模型虽能学习鲁棒特征，但直接用于3D医学任务性能下降严重（如nnU-Net等专用框架仍占优）。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：两阶段渐进式适配。第一阶段弥合维度与领域鸿沟，第二阶段增强体积推理和细节恢复。
- **关键技术细节**：
  - **Stage I：体积极适应与对齐**
    - **Inter-slice Context Embedding (ICE)**：用深度感知3D卷积处理子体积slab（K×H×W），生成伪3D块嵌入（C×H/16×W/16），同时兼容DINOv3的2D位置编码，并融合切片间上下文。
    - **代理任务（Proxy Task）**：用轻量MLP头预测中心切片的分割图，全微调ICE和backbone，迫使模型从自然场景特征转向医学语义。
  - **Stage II：双流分割网络**
    - **Volumetric Context Encoding Stream**：冻结Stage I对齐的backbone，注入LoRA矩阵进行参数高效微调；对输入体积X ∈ R^{D×H×W}构建伪3D slab序列，经ICE和backbone处理后堆叠恢复体积几何；引入轻量3D Adapter（3D反卷积）将伪3D特征转换为真正3D表示，最后用UperNet解码得到粗糙语义图F_sem。
    - **Detail-Recovery Stream**：设计轻量3D CNN（HRSE，无过度下采样）提取高分辨率空间特征F_det（原始、1/2、1/4尺度），保留高频边界信息。
    - **Adaptive Gated Fusion (AGF)**：生成空间注意力图G = σ(K_{1×1×1}[F_sem; F_det^{C3}])，通过残差方式注入细节：˜F_det^{C3} = F_sem + G ⊙ F_det^{C3}，然后与更低层细节特征逐步聚合产生最终分割图。
- **公式/算法流程**：整体公式M = D(F(F_sem, F_det), F_det) = D(F(E_sem(X), E_det(X)), E_det(X))。

## 3. 实验设计
- **数据集（共5个公开数据集，涵盖CT和MRI）**：
  - CT：AISD（脑梗死病灶，397例）、MSD-Pancreas（胰腺，281例）、MSD-Colon（结肠，126例）。
  - MRI：BraTS 2020（脑肿瘤，368例）、ACDC（心脏，100例）。
  - 预处理：所有图像resize至512×512，随机8:1:1分割；CT应用特定窗宽窗位，MRI裁剪至[0.5, 99.5]百分位并归一化。
- **Benchmark**：与9种方法对比，包括：
  - 传统CNN：nnU-Net
  - Transformer：nnFormer、SwinUNETR
  - Mamba：U-Mamba
  - 自监督预训练：VoCo(Swin-L)
  - DINOv3适配：Dino U-Net（直接使用DINOv3的2D U-Net变体）
  - 本文变体：DINO-Med3D-S/B/L（分别基于DINOv3 small/base/large backbone，参数量47M/112M/334M）。
- **评价指标**：Dice相似系数（DSC）和Hausdorff距离（HD95）。

## 4. 资源与算力
- **文中未明确说明**：论文未提及使用的GPU型号、数量、训练时长等具体算力信息。仅在实现细节中提到训练两阶段各50个epoch，Stage I batch size=24，学习率2e-5；Stage II batch size=1，学习率1e-4，使用LoRA（r=16, α=16, p=0.1），ICE输入切片K=3。

## 5. 实验数量与充分性
- **实验组数**：主实验在5个数据集上与9种基线对比，共45个DSC/HD95指标比较；消融实验包含：
  - ICE输入切片K取值1~5（表2，3个数据集）。
  - 多阶段框架组件消融（表3，3个数据集，包含8个子配置）。
  - 视觉消融（图3，定性展示细节恢复流效果）。
- **充分性与客观性**：
  - 覆盖多模态（CT、MRI）、多器官（脑、胰腺、结肠、心脏）、多种病变（梗死、肿瘤、心脏结构），**充分**。
  - 基线方法覆盖主流架构（CNN、Transformer、Mamba、自监督预训练、DINOv3直接适配），**公平**。
  - 所有基线按默认配置实现，本文方法也给出详细超参数，**可复现**。
  - 消融实验验证了ICE、代理任务、双流设计、AGF各组件必要性，**全面**。

## 6. 论文的主要结论与发现
- DINO-Med3D在所有5个数据集上DSC和HD95均显著优于现有SOTA（如nnU-Net、nnFormer、U-Mamba、Dino U-Net等），特别是纹理丰富/低对比度数据集（MSD-Pancreas、MSD-Colon）提升幅度最大（例如MSD-Colon DSC达62.28%，比nnFormer高11.60%）。
- 存在正向缩放规律：从Small到Large变体性能单调提升。
- 即使Small变体（47M参数）也在多数任务上超越Dino U-Net Large（318M参数），证明了架构设计的效率优势。
- 消融确认：Stage I全微调是后续LoRA适配的前提；ICE最佳切片数为3；AGF细节恢复流能显著改善边界分割质量。

## 7. 优点
- **方法新颖**：首次提出两阶段渐进适配策略，系统解决2D基础模型→3D医学分割的维度与领域鸿沟。
- **设计精巧**：ICE模块兼容2D位置编码同时融入伪3D上下文；双流分离语义抽象与空间细节；AGF实现内容感知的残差融合。
- **性能卓越**：在多个挑战性数据集上大幅超越现有方法，且参数量可控（Small仅47M）。
- **实验全面**：覆盖多模态、多器官、多基线，消融实验严谨，定性定量结合。
- **代码开源**：提供GitHub仓库，利于复现和后续研究。

## 8. 不足与局限
- **计算开销**：Stage I需要全微调整个DINOv3 backbone，计算资源需求较高；未来可探索更高效的参数对齐策略。
- **细节恢复流性能依赖**：在部分数据集上细节恢复流贡献可能有差异（论文提到需进一步研究），可能受纹理复杂度和病变大小影响。
- **未报告明确算力信息**：缺乏GPU型号、训练时长等细节，不利于资源估算。
- **仅针对DINOv3**：方法通用性未在其它2D基础模型（如CLIP、SAM）上验证。
- **数据规模有限**：虽然使用了5个数据集，但每个数据集样本数不算极大（如ACDC仅100例），可能影响统计稳定性。
- **缺乏跨域泛化测试**：未评估在域外（如不同扫描仪、不同序列）上的性能。

（完）
