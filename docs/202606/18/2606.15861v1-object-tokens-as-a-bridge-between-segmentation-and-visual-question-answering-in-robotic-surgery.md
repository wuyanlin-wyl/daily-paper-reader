---
title: Object Tokens as a Bridge Between Segmentation and Visual Question Answering in Robotic Surgery
title_zh: 对象标记作为机器人手术中分割与视觉问答之间的桥梁
authors: "Yiping Li, Ronald de Jong, Romy van Jaarsveld, Franco Badaloni, Gino Kuiper, Jelle Ruurda, Josien Pluim, Marcel Breeuwer"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15861v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用视觉语言模型联合分割和视觉问答在机器人手术中
tldr: 在机器人手术的视觉问答（VQA）中，现有方法依赖粗粒度视觉（如边界框），难以捕捉精细空间结构。本文提出统一框架，将视觉语言模型（VLM）与SAM解码器结合，通过优化对象标记嵌入来联合学习分割和VQA，实现像素级视觉推理。在RAMIE和EndoVis18数据集上，该方法显著优于基线，提升了精细手术场景理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15861v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of the proposed method.\", \"page\": 3, \"index\": 1, \"width\": 686, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-002.webp\", \"caption\": \"Fig. 3: Causal attention visualizations for object token generation and answer prediction. Top: example from the EndoVis18 dataset. Bottom: example from RAMIE dataset. Brighter regions indicate stronger attention weights.\", \"page\": 8, \"index\": 2, \"width\": 713, \"height\": 775}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-003.webp\", \"caption\": \"Fig. 2: Dataset statistics for EndoVis18 (left) and RAMIE (right). The inner ring shows the percentage distribution per question category, while the outer ring displays the train–test split with absolute sample counts.\", \"page\": 5, \"index\": 3, \"width\": 725, \"height\": 266}]"
motivation: 现有手术VQA方法仅依赖粗粒度视觉锚定，无法捕获手术对象的精细空间结构，限制视觉推理能力。
method: 提出框架融合VLM与SAM解码器，利用VLM生成对象标记，同时指导答案预测和投影至SAM产生分割掩码，通过分割和问答目标联合优化。
result: 在私有RAMIE和公开EndoVis18数据集上，所提方法一致优于基线手术VQA方法。
conclusion: 上下文感知的对象标记融入视觉语言模型能提升精细手术场景理解，实现像素级视觉推理。
---

## 摘要
机器人手术中的视觉问答（VQA），即手术VQA，需要对复杂手术场景的高级理解以及视觉感知与语言推理的整合，有潜力支持手术训练和术中决策。最近的视觉-语言模型（VLM）通过参数高效微调展现出有前景的性能；然而，大多数现有方法依赖于粗略的视觉基础定位，通常仅限于边界框，无法捕捉手术对象的细粒度空间结构。在这项工作中，我们提出了一个统一框架，在单个框架中联合执行像素级分割和视觉问答。我们的方法将VLM与基于Segment Anything Model（SAM）的解码器集成，并将场景元素表示为VLM生成的对象标记。这些对象标记指导答案预测，并进一步投影到基于SAM的解码器以生成分割掩码。通过同时优化分割和问答目标的对象标记嵌入，模型学习了空间基础表示，从而增强视觉推理，同时提供显式的像素级基础。我们在私有的RAMIE（机器人辅助微创食管切除术）数据集和公开的EndoVis18数据集上评估了所提方法，该方法在手术VQA上始终优于基线方法。这些结果表明，将上下文感知的对象标记纳入视觉-语言模型可改进细粒度手术场景理解。

## Abstract
Visual Question Answering (VQA) in robotic surgery, referred to as surgical VQA, requires high-level understanding of complex surgical scenes and the integration of visual perception with language reasoning, with the potential to support surgical training and intraoperative decision-making. Recent Vision-Language Models (VLMs) have shown promising performance through parameter-efficient fine-tuning; however, most existing approaches rely on coarse visual grounding, typically limited to bounding boxes, which fails to capture the fine-grained spatial structure of surgical objects. In this work, we propose a unified framework that jointly performs pixel-level segmentation and visual question answering within a single framework. Our approach integrates a VLM with a Segment Anything Model (SAM)-based decoder and represents scene elements as object tokens generated by the VLM. These object tokens guide answer prediction and are further projected to the SAM-based decoder to produce segmentation masks. By optimizing the object token embeddings through both segmentation and question answering objectives, the model learns spatially grounded representations that enhance visual reasoning while providing explicit pixel-level grounding. We evaluate the proposed method on the private RAMIE (Robot-Assisted Minimally Invasive Esophagectomy) dataset and the public EndoVis18 dataset, where it consistently outperforms baseline methods for surgical VQA. These results demonstrate that incorporating context-aware object tokens into vision-language models improves fine-grained surgical scene understanding.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有机器人手术视觉问答（surgical VQA）方法多依赖粗粒度的视觉锚定（如边界框），无法捕捉手术对象的精细空间结构，限制了视觉推理能力。
- **背景**：手术VQA需要高层次理解复杂手术场景并融合视觉与语言推理，对手术训练和术中决策有重要意义。近期视觉-语言模型（VLM）虽通过参数高效微调表现良好，但空间锚定仍局限于边界框，且语言输出多为预定义分类而非开放文本生成。
- **目标**：提出统一框架，联合实现像素级分割与开放文本VQA，通过引入对象标记（object tokens）作为桥梁，增强细粒度场景理解。

## 2. 方法论

### 核心思想
- 将每个分割目标表示为一个离散的**对象标记**（object token），在VLM词汇表中添加特殊`<sam_pad>`标记附加到目标类名后。
- VLM生成的对象标记同时用于：
  - 指导答案预测（作为上下文线索）
  - 投影到SAM解码器生成像素级分割掩码
- 通过联合优化分割损失和VQA交叉熵损失，学习空间有基础的表示。

### 关键技术细节
- **基础VLM**：Qwen2.5-VL-7B，使用LoRA参数高效微调（rank=16, α=32）。
- **连接VLM与SAM**：
  - 将`<sam_pad>`的隐变量通过线性投影层映射到SAM提示嵌入空间，得到`z_m`。
  - 引入一组可学习查询（query）`q`，通过交叉注意力（`z_m`作为key和value）生成投影后的提示嵌入`z_p`。
  - 根据SAM变体（SAM、SAM2、SAM3）采用不同解码方式得到预测掩码。
- **训练策略**：课程学习（curriculum learning）三步：
  1. 纯VQA数据预热
  2. 分割监督训练
  3. 混合纯VQA与分割引导VQA联合训练
- **损失函数**：下一个词预测交叉熵 + 分割Dice损失与二值交叉熵损失。

## 3. 实验设计

### 数据集
- **EndoVis18**：公开手术场景分割数据集（工具与解剖类别），结合扩展VQA数据（5类问题）。使用标准划分：视频2-4,6-7,9-12,14-15训练；视频1,5,16测试。
- **RAMIE**：私有机器人辅助微创食管切除术数据集，含手术阶段标签、分割掩码及衍生的VQA数据（多类问题，如图2饼图分布）。

### Benchmark
- **分割基线**：DINOv3 ViT-L + Encoder-only Mask Transformer (EoMT)。
- **VQA基线**：基础VLM（Qwen2.5-VL-7B）仅用LoRA微调，不含`<sam_pad>`标记。
- 此外对比SOTA模型：LLaMA-3.2-11B、Gemma-3-12B、GLM-4V-9B、EndoChat。

### 对比方法
- 所提方法集成不同SAM变体：VLM+SAM、VLM+SAM2、VLM+SAM3。
- 评估指标：分割用Dice系数和HD95；VQA用准确率（Acc）、mIoU、BLEU-4、CIDEr、Rouge-L、GPT-4评分。

## 4. 资源与算力

- **具体描述**：文中仅提及使用Qwen2.5-VL-7B作为VLM，LoRA rank=16, α=32，学习率2e-4，训练3个epoch，SAM组件学习率1e-5。
- **GPU型号、数量、训练时长**：**未明确说明**。因此无法给出具体算力细节。

## 5. 实验数量与充分性

- **主要实验**：
  - 表1（EndoVis18）：分割+5类VQA指标，对比VLM-only、VLM+SAM/SAM2/SAM3。
  - 表2（RAMIE）：分割+6类VQA指标，同样对比四种设置。
  - 表3：与SOTA模型在EndoVis18上对比VQA性能。
  - 注意力可视化（图3）：展现对象标记与答案标记的因果注意力。
- **充分性分析**：
  - **优点**：覆盖两个不同数据集、多个SAM变体、多类VQA任务、与强基线及SOTA对比，实验设计较为系统。
  - **不足**：缺乏更全面的消融实验（如去掉分割目标、不同投影结构、不同课程学习阶段的影响）；仅报告了最终结果，未分析对象标记数量或选择策略的影响；未在更多公开手术VQA数据集（如Cholec80衍生数据）上验证。
  - **公平性**：训练协议统一（相同VLM、相同LoRA设置），对比公平；但SAM3使用简化损失函数可能未达其最优性能。

## 6. 主要结论与发现

1. 引入对象标记联合优化分割与VQA，能有效提升VQA性能，尤其对需要细粒度空间理解的任务（如手术阶段识别、区域型QA）。
2. SAM2结合VLM表现最佳，分割性能（Dice/HD95）与VQA性能正相关，表明更好分割质量有助于视觉推理。
3. 所提方法在EndoVis18上超越多个SOTA（包括EndoChat），在多个VQA指标上取得最高分。
4. 注意力可视化显示答案标记对语义相关对象标记赋予更高权重，验证了对象标记作为上下文线索的有效性。

## 7. 优点

- **新颖性**：首次在手术领域将像素级分割与开放文本VQA统一到一个框架中，并利用对象标记作为桥梁。
- **架构灵活性**：方法可适配不同VLM骨干和SAM变体，具有良好扩展性。
- **细粒度理解**：从边界框提升到像素级空间基础，增强手术场景理解。
- **可解释性**：通过注意力图可视化，可观察模型如何利用对象标记推理答案。
- **性能优势**：在多个数据集和任务上超越基线及SOTA方法。

## 8. 不足与局限

- **算力与资源**：未报告GPU型号、数量及训练时间，不利于复现和成本评估。
- **消融实验不足**：缺乏对关键组件（如投影层设计、对象标记数量、课程学习策略）的单独消融，无法确定各组件贡献。
- **数据集限制**：
  - 仅使用单帧图像，未利用视频时序信息（文中承认视频数据更丰富但公开数据集有限）。
  - 现有手术VQA数据集推理深度较浅，问题多为事实性，缺乏复杂临床推理。
- **SAM3性能调优**：由于采用统一训练协议，SAM3未发挥最大潜力，结论可能不全面。
- **泛化性**：仅在两个数据集（一个私有）验证，且类分布不平衡（如图2），可能存在过拟合风险。
- **应用限制**：目前为离线单帧处理，实时性未评估；实际术中部署需考虑延迟和可靠性。

（完）
