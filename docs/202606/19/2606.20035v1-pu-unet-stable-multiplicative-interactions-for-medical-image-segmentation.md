---
title: "PU-UNet: Stable Multiplicative Interactions for Medical Image Segmentation"
title_zh: PU-UNet：用于医学图像分割的稳定乘法交互
authors: "Ziyuan Li, Osamah Sufyan, Uwe Jaekel, Babette Dellen"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20035v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学图像分割的创新乘法机制
tldr: 本文提出PU-UNet，通过稳定的乘积单元残差块在U-Net低分辨率阶段引入显式乘法特征交互，解决传统对数-指数乘积单元数值不稳定的问题，在三个医学图像分割数据集上取得了优于残差U-Net的Dice和IoU指标，且参数、计算量基本不变，并显著降低假阳性率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20035v1/fig-001.webp\", \"caption\": \"Table 3. Lesion segmentation performance and image-level false-positive (FP) rate on BUSI under two evaluation settings: including and excluding normal cases.\", \"page\": 7, \"index\": 1, \"width\": 723, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2606.20035v1/fig-002.webp\", \"caption\": \"Table 2. Segmentation performance on ISIC 2018 and Kvasir-SEG. 95% CI denotes the 95% confidence interval. pDice/IoU reports the p-values for the comparisons between PU-UNet and the Residual U-Net baseline on Dice and IoU, respectively. The best result in each column is highlighted in bold.\", \"page\": 7, \"index\": 2, \"width\": 716, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2606.20035v1/fig-003.webp\", \"caption\": \"Table 5. Ablation study of PU-UNet, including the placement of product-unit residual blocks and the effect of stabilization components. “w/” and “w/o” denote with and without, respectively.\", \"page\": 10, \"index\": 3, \"width\": 725, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2606.20035v1/fig-004.webp\", \"caption\": \"Fig. 2. Qualitative segmentation results on three medical image segmentation datasets. Panels (a,b), (c,d), and (e,f) correspond to ISIC 2018, Kvasir-SEG, and BUSI, respectively. In the overlay visualization, green denotes ground truth, red denotes prediction, and yellow denotes their overlap.\", \"page\": 8, \"index\": 4, \"width\": 602, \"height\": 658}, {\"url\": \"assets/figures/arxiv/2606.20035v1/fig-005.webp\", \"caption\": \"Fig. 1. Overview of the proposed PU-UNet, including the network architecture and core product-unit components.\", \"page\": 4, \"index\": 5, \"width\": 725, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2606.20035v1/fig-006.webp\", \"caption\": \"Table 4. Comparison with representative literature on ISIC 2018, Kvasir-SEG, and BUSI. Jaccard@0.65 denotes Jaccard with threshold 0.65.\", \"page\": 9, \"index\": 6, \"width\": 705, \"height\": 690}]"
motivation: 现有密集预测网络主要依赖加法特征变换，隐式建模高阶特征交互，数值不稳定的乘积单元未能有效用于深度网络。
method: 在残差U-Net的低分辨率阶段集成稳定乘积单元残差块，采用平滑正映射和对数域裁剪实现稳定乘法学习。
result: 在ISIC 2018、Kvasir-SEG和BUSI上Dice分别达0.942、0.959和0.925，相比残差U-Net基线一致提升，正常BUSI病例假阳性率从0.077降至零。
conclusion: 稳定的乘积单元残差学习能有效增强U-Net风格分割网络的显式乘法交互能力。
---

## 摘要
许多稠密预测网络依赖加法特征变换，仅隐式地建模高阶特征交互。乘积单元为乘法特征建模提供了显式机制，但其对数-指数公式可能导致数值不稳定性，这限制了它们在深度稠密预测网络中的应用。在这项工作中，我们提出了乘积单元U-Net（PU-UNet），一种残差U-Net，它将稳定的乘积单元残差块集成到丰富的低分辨率阶段中，用于医学图像分割。所提出的公式将平滑正数映射与对数域裁剪相结合，实现了稳定的乘法特征学习，且计算开销可忽略不计。在ISIC 2018、Kvasir-SEG和BUSI数据集上，PU-UNet的Dice分数分别达到0.942、0.959和最高0.925。与匹配的残差U-Net基线相比，PU-UNet在保持参数、FLOPs和推理延迟几乎不变的情况下，持续提高了Dice和IoU，并将正常BUSI病例的图像级假阳性率从0.077降至零。消融研究表明，性能提升与乘积单元交互相关，在低分辨率放置下效果最强，并得益于所提出的稳定化设计。这些结果表明，稳定的乘积单元残差学习可以成为增强U-Net风格分割网络显式乘法交互的有效方法。

## Abstract
Many dense prediction networks rely on additive feature transformations and model higher-order feature interactions only implicitly. Product units provide an explicit mechanism for multiplicative feature modeling, but their logarithmic--exponential formulation can cause numerical instability, which has limited their use in deep dense prediction networks. In this work, we propose Product-Unit U-Net (PU-UNet), a residual U-Net that integrates stable product-unit residual blocks into rich low-resolution stages for medical image segmentation. The proposed formulation combines smooth positivity mapping with log-domain clipping, enabling stable multiplicative feature learning with negligible computational overhead. On ISIC 2018, Kvasir-SEG, and BUSI, PU-UNet achieves Dice scores of 0.942, 0.959, and up to 0.925, respectively. Compared with a matched Residual U-Net baseline, PU-UNet consistently improves Dice and IoU while keeping parameters, FLOPs, and inference latency nearly unchanged, and reduces the image-level false-positive rate on normal BUSI cases from 0.077 to zero. Ablation studies suggest that the gains are associated with product-unit interactions, are strongest under low-resolution placement, and benefit from the proposed stabilization design. These results suggest that stable product-unit residual learning can be an effective way to enhance U-Net-style segmentation networks with explicit multiplicative interactions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有医学图像分割网络（如U-Net及其变体）主要依赖**加法特征变换**（卷积+逐点非线性），对高阶特征交互（如纹理、形状、边界间的非线性依赖）仅能隐式建模。乘积单元（Product Unit）能显式建模乘法交互，但其对数-指数公式在深度网络中容易导致**数值不稳定**（梯度爆炸/消失），限制了其在密集预测任务中的应用。
- **研究动机**：设计一种**稳定**的乘积单元，使其能够被集成到U-Net风格网络中，在几乎不增加计算开销的前提下，显式地引入乘法特征交互，提升分割精度，特别是在低分辨率语义丰富的特征层级中。
- **整体含义**：本文证明了通过适当的稳定化设计（平滑正映射+对数域裁剪），乘积单元可以成为增强U-Net分割网络的有效组件，在多个医学图像数据集上取得一致提升。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 在**残差U-Net**的**低分辨率阶段**（即编码器最后一级、瓶颈层、解码器前两级）选择性地将标准残差块替换为**乘积单元残差块**，引入显式乘法特征建模。
- 提出**稳定的2D乘积单元**，解决传统乘积单元数值不稳定的问题。

### 关键技术细节
1. **稳定乘积单元（Stable 2D Product-Unit）**：
   - 输入特征图 X 经过**平滑正映射**：`X+ = softplus(X) + θ`，其中 θ = softplus(θ_raw) + ε，ε = 10⁻⁷，保证输入对数域严格正且平滑（避免硬截断）。
   - 对数域卷积：`L = Conv(log X+)`。
   - **对数域裁剪**：`L̄ = clip(L, -C, C)`，C=10，确保指数重建在FP16动态范围内（exp(10) ≈ 2.2×10⁴ < FP16最大值 6.55×10⁴）。
   - 指数重建：`ConvPU(X) = exp(L̄)`。
2. **乘积单元残差块**：
   - 保留标准残差块的第一层（Conv→GN→SiLU），将第二层卷积替换为稳定的乘积单元算子，输出经GN后与跳跃连接相加。
3. **网络架构（PU-UNet）**：
   - 编码器-解码器结构，4个阶段，基础通道数32，分辨率从1到1/16。
   - 默认在**最低分辨率（1/16）** 的编码器最后块和瓶颈块，以及**1/8和1/4分辨率**的解码器前两个块中插入乘积单元块；其余保持标准残差块。
   - 使用组归一化（8组）和SiLU激活。

## 3. 实验设计：数据集、基准、对比方法

### 数据集
- **ISIC 2018**：皮肤病变分割，官方训练/验证/测试划分，图像resize到352×352。
- **Kvasir-SEG**：结肠息肉分割，随机80/10/10划分，图像resize到352×352。
- **BUSI**：乳腺超声分割（含正常病例作为空掩膜），分层80/10/10划分，图像resize到256×256。包括“包含正常”和“仅病变”两种评估设置。

### 基准（baseline）
- **残差U-Net**：与PU-UNet具有完全相同的架构、参数量、FLOPs，仅将所有乘积单元块替换为标准残差块。

### 对比方法
- 文献中代表方法：MedSAM+AdSTNet、MAAU、Hybrid TransUNet、DilatedSkinNet、PCA-TransUNet、Dual-branch SSM、μ-Net、ResPVT-B0、UPolySeg、A-DenseUNet、Multi-Task Framework、BGRD-TransUNet、D-TransUNet、DBU-Net等。

### 消融实验
- **放置位置**：
  - 仅瓶颈（Bottleneck only）
  - 低分辨率阶段（PU-UNet）
  - 所有阶段（All stages）
- **稳定化组件**：
  - 使用简单下界截断（如[22]）代替平滑正映射
  - 移除对数域裁剪

### 评估指标
- Dice、IoU（阈值0.5）、ISIC 2018额外报告Jaccard@0.65。对正常BUSI病例报告图像级假阳性率。给出95% bootstrap置信区间和配对置换p值。

## 4. 资源与算力

- **GPU型号**：单个NVIDIA V100 GPU。
- **训练配置**：
  - ISIC 2018 和 Kvasir-SEG：训练500 epochs，batch size 8。
  - BUSI：训练300 epochs，batch size 8。
  - 优化器：AdamW（lr=1e-3，weight decay=1e-4），余弦退火学习率调度，自动混合精度训练。
- **推理延迟**：PU-UNet在352×352输入下约10.2 ms，256×256下约5.6 ms；残差U-Net基本一致。
- **未明确说明**：训练总耗时、多GPU分布式训练情况、显存占用未详述。

## 5. 实验数量与充分性

- **主实验**：3个数据集×2种评估设置（BUSI两种）共4组性能对比。
- **消融实验**：
  - 放置位置：4种配置（无乘积单元、瓶颈、低分辨率、全部）×3个数据集 = 12组。
  - 稳定化组件：3种配置（PU-UNet、简单截断、无裁剪）×3个数据集 = 9组。
- **与文献对比**：每个数据集5-8种代表方法，总计约20组比较。
- **充分性评价**：
  - 实验设计**合理且全面**：包含不同模态（皮肤镜、内镜、超声）、不同难度场景（含正常病例），消融覆盖位置和稳定化关键因素。
  - **统计充分**：报告95%置信区间和p值（2,000 bootstrap，20,000次排列检验），增强结果可靠性。
  - **客观公平**：基线网络高度匹配（仅替换少量块），参数量/FLOPs几乎一致；与其他文献对比时注意了协议不一致（如数据集划分差异）。
  - **略有不足**：未在更多架构（如Transformer-based）或3D数据上验证，也未报告多轮随机种子的平均结果（使用固定种子42）。

## 6. 论文的主要结论与发现

- **性能提升**：PU-UNet在三个数据集上均显著优于残差U-Net基线（Dice提升0.057~0.091，IoU提升0.084~0.116），且参数量/FLOPs/延迟几乎不变。
- **假阳性控制**：在含有正常病例的BUSI上，PU-UNet将图像级假阳性率从0.077降至0，具有重要的临床实用价值。
- **放置位置最优**：仅在低分辨率阶段插入乘积单元块效果最好，瓶颈层单独插入增益较小，全网络替换反而下降，说明乘法交互在语义丰富区域更有用。
- **稳定化设计关键**：平滑正映射和对数域裁剪均不可或缺，移除后性能下降甚至梯度爆炸，尤其在超声数据上最明显。
- **与文献比较**：PU-UNet在大多指标上达到或超过已有方法，特别是在IoU/Jaccard上表现突出。

## 7. 优点

1. **数值稳定性突破**：提出平滑正映射+对数域裁剪，成功克服了乘积单元在深度密集预测网络中的数值不稳定问题，使其首次在实用场景中有效。
2. **开箱即用、零开销**：仅替换少数残差块，参数量和计算量几乎不变，可直接嵌入现有U-Net架构。
3. **临床价值**：显著降低正常病例的假阳性率，对减少不必要的活检或复查具有实际意义。
4. **消融设计严谨**：系统验证了放置位置和稳定化组件的影响，提供了清晰的指导原则（乘法交互适合深层语义特征）。
5. **统计方法规范**：使用bootstrap置信区间和置换检验，结果具有统计显著性。

## 8. 不足与局限

- **架构局限**：仅在2D U-Net风格网络上验证，未在Transformer、Mamba等其他主流架构或3D医学图像上评估。
- **模态限制**：只涉及皮肤、息肉、超声图像，未测试CT、MRI等模态。
- **泛化能力待验证**：仅使用单数据集训练评估，未进行跨域（domain generalization）或跨序列泛化实验。
- **超参数敏感性**：对数域裁剪阈值C=10为经验设定，未进行灵敏度分析；乘积单元块仅替换第二层卷积的结构选择是否最优未深入探讨。
- **实验覆盖**：未报告多个随机种子下的性能波动（仅使用固定种子42），也未对正常病例假阳性率给出置信区间。
- **计算细节缺失**：训练总时间、显存峰值、多卡扩展性等未说明，影响复现性评估。

（完）
