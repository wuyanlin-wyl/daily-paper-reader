---
title: "REVEAL++: Differentiable Phenotypic Grouping for Vision-Language Retinal Modeling of Alzheimer's Disease Risk"
title_zh: REVEAL++：用于阿尔茨海默病风险视觉-语言视网膜模型的可微分表型分组
authors: "Ethan Elio Meidinger, Seowung Leem, Zeyun Zhao, Ruogu Fang"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19522v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 视觉语言视网膜模型预测阿尔茨海默病风险
tldr: 该研究提出REVEAL++，一种可微分的表型分组方法，用于阿尔茨海默病风险的视觉-语言视网膜建模。通过将个体间相似性建模为连续加权函数，代替离散分组，实现了软正例对比学习，在UK Biobank数据上优于现有方法，为多模态神经退行性疾病风险建模提供了新框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19522v1/fig-001.webp\", \"caption\": \"Fig. 1. Architecture of the proposed differentiable phenotypic weighting framework for group-aware contrastive learning. Image and text embeddings are aligned via a similarity-weighted multi-positive contrastive loss, where continuous phenotypic weights replace hard grouping to model the heterogeneous spectrum of Alzheimer’s disease risk\", \"page\": 2, \"index\": 1, \"width\": 725, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2606.19522v1/fig-002.webp\", \"caption\": \"Table 2. Comparison of multi-modal and baseline methods for incident AD prediction. Results are reported as mean ± standard deviation across folds. Bold and Underline represent the best and the second best results.\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 298}]"
motivation: 现有表型分组方法使用离散硬分组，导致组形成与表示学习分离，无法捕捉疾病风险的连续谱特性。
method: 提出可微分加权函数，基于图像和风险描述的嵌入相似性定义连续软正例关系，并通过软目标对比目标联合学习跨模态对齐和表型结构。
result: 在UK Biobank视网膜影像的AD预测任务中，REVEAL++性能优于离散分组对比学习和标准视觉-语言基线。
conclusion: 将表型相似性作为可学习的连续信号，为多模态视网膜和临床数据的大规模神经退行性风险建模提供了原则性且稳健的基础。
---

## 摘要
视网膜为神经退行性疾病提供了一个非侵入性的窗口，捕捉与未来认知衰退风险相关的细微结构模式。诸如REVEAL之类的视觉-语言对齐框架表明，将视网膜眼底图像与结构化的临床风险叙述配对，可以改善阿尔茨海默病（AD）的早期预测。这些方法中的一个关键设计选择是使用表型分组，在对比学习中将具有相似风险特征的个体视为多正样本对。然而，现有方法将表型相似性视为离散结构，依赖于硬分组分配，这强加了严格的监督，并将群体形成与表示学习分离。我们提出了一种对比学习中表型结构的连续形式化方法。不是将样本分配到固定聚类，而是将主体间相似性建模为可微加权函数，该函数源自视网膜图像和风险特征中模态内嵌入相似性。这些权重通过连续聚合算子定义软多正关系，从而实现反映疾病风险谱的梯度监督。我们进一步引入了一个软目标对比目标，以端到端的方式联合学习跨模态对齐和表型结构。在用于AD风险预测的UK Biobank视网膜成像数据上评估，所提出的框架始终优于基于离散组的对比学习和标准视觉-语言基线。通过将表型相似性视为可学习的连续信号，而不是固定的分组规则，我们的方法为从多模态视网膜和临床数据进行人群规模神经退行性风险建模提供了原则性和鲁棒性的基础。

## Abstract
The retina offers a noninvasive window into neurodegenerative disease, capturing subtle structural patterns associated with a risk of future cognitive decline. Vision-language alignment frameworks such as REVEAL have shown that pairing retinal fundus images with structured clinical risk narratives improves early prediction of Alzheimer's disease (AD). A key design choice in these approaches is the use of phenotypic grouping, where individuals with similar risk profiles are treated as multi-positive pairs during contrastive learning. However, existing methods operationalize phenotypic similarity as a discrete construct, relying on hard group assignments that impose rigid supervision and decouple group formation from representation learning. We propose a continuous formulation of phenotypic structure within contrastive learning. Rather than assigning samples to fixed clusters, we model inter-subject similarity as a differentiable weighting function derived from intra-modality embedding similarities in both retinal images and risk profiles. These weights define soft multi-positive relationships through a continuous aggregation operator, enabling graded supervision that reflects the spectrum nature of disease risk. We further introduce a soft-target contrastive objective that jointly learns cross-modal alignment and phenotypic structure in an end-to-end manner. Evaluated on UK Biobank retinal imaging data for incident AD prediction, the proposed framework consistently outperforms discrete group-based contrastive learning and standard vision-language baselines. By treating phenotypic similarity as a learnable, continuous signal rather than a fixed grouping rule, our approach provides a principled and robust foundation for population-scale neurodegenerative risk modeling from multi-modal retinal and clinical data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：阿尔茨海默病（AD）存在漫长的临床前阶段，病理变化（如淀粉样蛋白沉积、血管功能障碍）逐渐累积。视网膜作为中枢神经系统的“窗口”，可捕捉与神经退行相关的结构性变化；而系统性的生活方式和心血管风险因素也提供了互补信息。现有视觉-语言模型（如REVEAL）通过将视网膜图像与个体化临床风险叙述配对，并引入**基于硬分组的多正样本对比学习（GACL）**，改善了AD风险预测。但硬分组使用离散阈值将个体分配到固定类别，假设风险存在清晰边界，这与AD风险的连续谱、异质性特征不符，且分组过程与表示学习相互独立。
- **整体含义**：该工作旨在将表型相似性建模为连续的、可微分的监督信号，从而更真实地刻画疾病风险分布，提升多模态表示学习的质量。

### 2. 论文提出的方法论

- **核心思想**：用**连续加权函数**替代离散分组，将个体间相似性作为软监督信号融入对比学习，实现端到端联合学习跨模态对齐与人群结构。
- **关键技术细节**：
  1. **编码器**：图像编码器 `EI` 使用 RETFound（初始权重并微调），文本编码器 `ET` 使用 GatorTron（冻结）。两分支分别输出嵌入，通过线性投影映射到共享维度 d=1024，并进行 ℓ₂ 归一化。
  2. **模态内相似性**：计算图像嵌入之间的余弦相似度 `Sii(p,q)` 和文本风险叙述嵌入之间的余弦相似度 `Stt(p,q)`。
  3. **可微分加权**：通过带可学习阈值 `τ` 和温度参数 `g` 的 sigmoid 函数将相似性转换为软成员分数：
     - `aF(p,q) = σ((Sii(p,q) - τ_F) / g_F)`
     - `aT(p,q) = σ((Stt(p,q) - τ_T) / g_T)`
  4. **聚合**：通过概率并运算得到最终表型权重：`Wpq = 1 - (1 - aF(p,q))(1 - aT(p,q))`，取值范围 [0,1]。
  5. **软对比损失**：定义跨模态 logit `ℓpq = Sit(p,q)/τ - β`（τ为可学习温度，β为偏置），损失函数为：
     ```
     L_MP = 1/N² Σ_p Σ_q [Wpq log(1+exp(-ℓpq)) + (1-Wpq) log(1+exp(ℓpq))]
     ```
     当 Wpq 接近 1 时作为正样本，接近 0 时作为负样本，中间值提供渐变监督。
- **算法流程**：对每个 minibatch 同时计算图像嵌入和文本嵌入、模态内相似性、可微分加权、软多正对比损失，反向传播同时更新编码器（图像分支微调，文本分支冻结）和加权参数。

### 3. 实验设计

- **数据集**：UK Biobank 基线评估数据，包含彩色眼底照片（CFP）和结构化问卷（人口学、行为、认知、生活方式等）。通过自动质量控制筛选高质量图像，右眼图像水平翻转以统一方向。将结构化数据通过 LLaMA‑3.1 转化为标准化临床叙述。数据划分：训练 30,462 例，验证 3,384 例，测试 5,396 例。
- **Benchmark 任务**：**新发阿尔茨海默病预测**（incident AD prediction），即从基线数据预测未来 AD 诊断。
- **对比方法**：
  - 传统机器学习：SVM（基于结构化变量和 CFP 特征）
  - 视网膜专用基础模型：RETFound（图像单模态 + GatorTron 文本嵌入拼接）
  - 视觉-语言基础模型：BiomedCLIP、PMC-CLIP、RETCLIP、MM‑Retinal
  - REVEAL 框架的两种变体：无 GACL 的 REVEAL、带 GACL 的 REVEAL
  - 本文方法：REVEAL++（可微分表型加权）
- **评估指标**：AUROC、平衡准确率、F1 分数、MCC。所有结果基于 10 次随机种子的均值±标准差。

### 4. 资源与算力

- 文中**未明确说明**使用的 GPU 型号、数量、训练时长。仅提到批量大小 128，优化器 AdamW，超参数通过 Optuna 自动搜索。未提供计算资源信息是本文的一个透明度缺陷。

### 5. 实验数量与充分性

- **实验数量**：主实验对比了 9 种方法（含基线），并重复 10 次，结果统计可靠。但未进行消融研究（如仅使用图像相似性加权、仅使用文本相似性加权、或不同聚合算子的对比）。文中也缺少对不同阈值/温度初始化的鲁棒性分析。
- **充分性评估**：在单一数据集（UK Biobank）上验证，缺少外部数据集或种族多样性验证，泛化性存疑。对比方法覆盖了常用视觉-语言模型和 REVEAL 变体，但未与最新基于其他模态（如 OCT、MRI）的方法比较。总体实验较为充分但不够全面。

### 6. 论文的主要结论与发现

- REVEAL++ 在 AUROC（0.678 ± 0.061）、平衡准确率（0.613 ± 0.048）、F1（0.236 ± 0.079）和 MCC（0.168 ± 0.088）上均显著优于所有对比方法，包括带硬分组的 REVEAL。
- 连续可微分表型加权能够更有效地捕捉 AD 风险的连续和异质性特征，产生更一致的跨模态嵌入空间，从而提升下游预测性能。

### 7. 优点

- **方法创新性强**：将表型相似性建模为可微分连续权重，取代离散硬分组，更符合疾病生理连续谱，且实现端到端学习。
- **监督信号平滑**：软目标对比损失允许梯度随相似度连续变化，避免硬边界带来的信息损失。
- **性能提升明显**：在 UK Biobank 上取得 SOTA，且标准差较小，表明稳定性好。
- **框架通用性**：可扩展至其他慢性疾病风险预测或多模态公共卫生建模。

### 8. 不足与局限

- **计算资源未报告**：无法评估方法的可复现性和资源需求。
- **缺乏消融实验**：未验证各组件（图像相似性、文本相似性、聚合算子、损失形式）的独立贡献。
- **单一数据集验证**：仅在 UK Biobank（白人为主）上测试，跨种族、跨中心泛化性未知。
- **依赖 LLM 生成文本**：使用 LLaMA‑3.1 将结构化数据转为叙述，其质量可能影响下游对齐，且提示工程可能引入偏差。
- **无外部验证**：未在独立前瞻性队列中验证预测能力。
- **可解释性不足**：连续权重虽然更灵活，但与临床实践中的分组决策（如高风险 vs 低风险）之间的映射关系不明确。
- **未讨论失败案例**：例如，对某些特征极端值或缺失值多的个体模型行为如何。

（完）
