---
title: "Mask to Concept: Auto-Promptable SAM3 via Efficient Test-Time Concept Embedding Search for Few-Shot Annotation"
title_zh: 掩码到概念：通过高效的测试时概念嵌入搜索实现自动提示的SAM3用于少样本标注
authors: "Quan Zhou, Shaoqing Zhai, Qiang Hu Jia Chen, Qiang Li, Zhiwei Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26711v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 自动提示SAM3用于医学少样本标注
tldr: 针对医学图像标注中依赖外部模块自动化提示的局限，本文提出Mask to Concept（M2C）框架，无需外部模块或重训练，将SAM3适配为自动可提示的少样本标注工具。通过可学习概念嵌入梯度搜索和混合不确定性估计（HUE）模块，实现高效自动分割并引导人工校正，在医学分割基准上达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26711v1/fig-001.webp\", \"caption\": \"Fig. 3. Annotation efficiency curves on Kvasir-SEG and ISIC-2017 datasets.\", \"page\": 7, \"index\": 1, \"width\": 718, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2606.26711v1/fig-002.webp\", \"caption\": \"Table 1. Quantitative comparison with SOTA FSS methods on Kvasir-SEG and ISIC2017 datasets. We report the mean and standard deviation (µ±σ) for the Dice metric as percentages(%). Methods are divided into training-based (top) and training-free (bottom). The best results are highlighted in bold, and the second-best are underlined.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2606.26711v1/fig-003.webp\", \"caption\": \"Fig. 2. Visualization of 1-shot results on Kvasir-SEG (top) and ISIC-2017 (bottom).\", \"page\": 6, \"index\": 3, \"width\": 725, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.26711v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed human-in-the-loop annotation framework. It consists of two core iterative modules: Mask to Concept (M2C) and Hybrid Uncertainty Estimation (HUE). During each iteration, given a few human-labeled samples from the previous iteration, M2C employs a continuous searching on the dataset-specific concept embedding to automatically segment the remaining unlabeled samples. Following this, HUE estimates the uncertainties of these predictions and applies tailored strategies to the samples based on the uncertainty as they transition into the next iteration.\", \"page\": 4, \"index\": 4, \"width\": 725, \"height\": 281}]"
motivation: 现有方法依赖外部模块自动化几何提示，引入架构开销且可扩展性差；直接使用SAM3文本提示缺乏细粒度临床知识。
method: 利用少量标注图像，在冻结SAM3中初始化并梯度更新可学习概念嵌入以自动搜索视觉概念，同时用HUE模块检测不确定性样本，引导人工校正形成自增强循环。
result: 在医学分割基准上取得了最先进的少样本分割性能和卓越的标注效率。
conclusion: M2C为可扩展的医学图像标注提供了一种实用高效的途径。
---

## 摘要
将基础分割模型从人类提示的工具转变为可自动提示的标注器，对于可扩展的医学数据标注至关重要。当前的方法通常依赖外部特征匹配器或辅助网络来自动化几何提示，但引入了架构开销并限制了性能可扩展性。尽管SAM3通过可重用的文本提示原生支持概念分割，但它在医学影像中的直接使用受到缺乏细粒度临床知识以及人类撰写描述歧义性的阻碍。在这项工作中，我们提出了Mask to Concept（M2C），一种高效的框架，无需外部模块、参数重训练或手动文本工程，即可使SAM3适应医学少样本标注。仅使用少量标注图像，M2C使SAM3能够在其冻结架构内自动搜索可转移的视觉概念：它初始化一个可学习的概念嵌入，使用它来提示分割，并通过最小化概念分割误差的梯度来更新嵌入。我们进一步引入了一个混合不确定性估计（HUE）模块，该模块计算预测熵并将概念预测映射回框提示，从而度量概念-几何提示的不一致性。高不确定性样本被主动标记以供人工纠正，然后纠正后的掩码被反馈给M2C以持续搜索更精确的概念嵌入，形成一个自我增强的标注循环，只需极少专家努力。在医学分割基准上的实验表明，我们的方法达到了最先进的少样本分割性能和出色的标注效率，为可扩展的医学图像标注提供了一条实用且高效的路径。代码位于https://github.com/Huster-Hq/M2C。

## Abstract
Transforming foundation segmentation models from human-prompted tools into auto-promptable annotators is critical for scalable medical data annotation. Current methods commonly depend on external feature matchers or auxiliary networks to automate geometric prompting, but introducing architectural overhead and limiting performance scalability. Although SAM3 natively supports concept segmentation via reusable text prompts, its direct use in medical imaging is hindered by a lack of fine-grained clinical knowledge and the ambiguity of human-written descriptions. In this work, we propose Mask to Concept (M2C), an efficient framework that adapts SAM3 for medical few-shot annotation without external modules, parameter retraining, or manual text engineering. Using only a few labeled images, M2C enables SAM3 to automatically search for transferable visual concepts entirely within its frozen architecture: it initializes a learnable concept embedding, uses it to prompt segmentation, and updates the embedding by gradients of minimizing the concept segmentation error. We further introduce a Hybrid Uncertainty Estimation (HUE) module that calculates the prediction entropy and maps concept predictions back to the box prompts, measuring concept-geometry prompting inconsistency. Highly uncertain samples are flagged actively for human correction, and the corrected masks are then fed back to M2C to continuously search for more precise concept embeddings, forming a self-enhancing annotation loop with minimal expert effort. Experiments on medical segmentation benchmarks show that our method achieves SOTA few-shot segmentation performance and outstanding annotation efficiency, offering a practical and efficient pathway toward scalable medical image labeling. Codes are at https://github.com/Huster-Hq/M2C.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：高质量医学图像分割标注成本高昂，急需自动化工具。现有方法要么依赖预训练（泛化性差），要么引入外部模块实现自动几何提示（架构复杂、性能受限）。SAM3虽原生支持概念分割（文本提示），但医学领域缺乏细粒度临床知识，且人工撰写的文本描述存在歧义。
- **核心问题**：能否在不使用外部模块、不重训练SAM3的前提下，利用其内置概念分割能力，构建跨样本的少样本自动标注器？
- **整体含义**：通过为SAM3注入可学习的视觉概念嵌入，实现从“人工提示”到“自动提示”的转化，为大规模医学数据标注提供高效、可扩展的路径。

## 2. 方法论

- **核心思想**：在冻结SAM3参数的情况下，仅通过梯度优化一个可学习的概念嵌入，使SAM3能够从少量标注掩码中自动提取数据集特定的视觉概念，从而无需人工文本工程即可分割其余未标注数据。
- **关键技术细节**：
  - **Mask to Concept (M2C)**：初始化可学习向量 `E`，将其加到原始文本编码 `TEnc(T)` 上，形成增强的概念嵌入。利用少量标注样本，冻结SAM3，仅优化 `E`，通过最小化分割损失（IoU损失+交叉熵损失）梯度更新：
    > `Et = Et−1 − η ∇E [ (1/|D_label|) Σ Lseg(ypred, y_label) ]`
    - 该过程可随迭代持续进行，逐步适应困难样本。
  - **Hybrid Uncertainty Estimation (HUE)**：用于识别高不确定性样本，引导人工校正。
    - **预测熵 (U_ENT)**：计算模糊像素区域（熵>0.8）的平均香农熵。
    - **概念-几何提示不一致性 (U_CGPI)**：将概念驱动掩码转为边界框，再通过SAM3生成几何驱动掩码，两者IoU的补值作为不一致性。
    - 最终分数 `U_HU = U_ENT + U_CGPI`。
  - **人机协作闭环**：根据HUE排序，将高不确定性样本送入人工修正；修正后的掩码反馈给M2C继续优化概念嵌入，低不确定性样本提前退出循环。

## 3. 实验设计

- **数据集与场景**：
  - **Kvasir-SEG**：内窥镜息肉分割，1000张图像。
  - **ISIC-2017**：皮肤病变分割，仅用官方测试集600张（避免数据泄露）。
- **基准（Benchmark）**：少样本分割（FSS）设置，支持集:查询集 = 1:9，进行1-shot、5-shot、10-shot评估。
- **对比方法**：
  - 基于训练的FSS方法：Multiverseg、Tyche。
  - 免训练方法：ProtoSAM、Matcher、SPFS-SAM、MAUP（均基于SAM-H）。
- **评估指标**：Dice相似系数（DSC）。

## 4. 资源与算力

- 文中明确提到：单张NVIDIA RTX 4090 GPU，使用AdamW优化器，学习率从5×10⁻⁴余弦退火至5×10⁻⁶。未提及训练的epoch数或总时长。

## 5. 实验数量与充分性

- **主要实验**：在两个数据集上，分别针对1-shot、5-shot、10-shot进行定量比较（表1）；定性可视化（图2）。
- **消融实验**：
  - 图4：HUE有效性（随机选择 vs 使用HUE vs 使用HUE+早退）。
  - 表2：两个子不确定度量（U_ENT、U_CGPI）单独与组合对标注效率的影响。
- **标注效率对比**：与Multiverseg、SPFS-SAM比较整体标注质量随标注样本数的变化（图3）。
- **充分性评价**：实验覆盖了多个shot设定、两个不同模态的数据集、充分对比了6种SOTA方法；消融实验验证了HUE各组件效果。数据集规模适中（1000/600），但未在更大规模或多器官数据集上验证，可认为实验较充分但仍有提升空间。

## 6. 论文的主要结论与发现

- M2C在1-shot下，Kvasir-SEG上Dice达76.3%（超第二名4.2%），ISIC-2017上达75.9%（超第二名11.8%）。
- 在5-shot、10-shot下同样保持最优性能，且方差较小，表明方法鲁棒。
- 标注效率显著优于对比方法，能以更少人工标注样本达到较高整体质量。
- HUE的早退策略对减少人工负担至关重要，两个不确定子度量互补。

## 7. 优点

1. **无需外部模块或重训练**：在冻结SAM3骨干上仅优化一个嵌入向量，轻量高效。
2. **自动消除文本歧义**：通过梯度搜索直接学习视觉概念，不再依赖人工撰写模糊文本。
3. **人机协作闭环**：HUE智能筛选困难样本，使专家精力集中在关键处，形成自增强循环。
4. **泛化性好**：在两个不同医学数据集上均超越先前方法，且无需领域特化预训练。
5. **代码开源**：促进可复现研究。

## 8. 不足与局限

1. **数据集规模有限**：仅验证了息肉和皮肤病变两个任务（共约1600张图像），未在大型多器官数据集（如TotalSegmentator、CT-ORG）或3D数据上评估，泛化性有待进一步检验。
2. **依赖SAM3的初始文本提示**：虽然后续优化可不依赖文本，但初始嵌入仍需一个文本编码（如类别名），若类别名称极不准确可能影响起点。
3. **计算资源细节不充分**：未报告优化所需迭代步数或时间，无法直接评估实际部署的成本。
4. **人机交互效率未定量**：仅报告了标注样本数，未统计专家修正每个高不确定性样本所需时间，难以衡量实际节省的人力。
5. **消融实验仅在一个数据集上**：表2和图4的消融仅在Kvasir-SEG上，未在ISIC上重复，可能受数据特性影响。
6. **未与SAM2或最新进展对比**：虽对比了多个SAM-based方法，但SAM3本身为2025年发布，可进一步与后续改进版本比较。

（完）
