---
title: "Zero-Shot Vision-Language Models for Classroom Engagement Recognition: A Benchmark Study of Prompt Sensitivity and Cross-Dataset Generalization"
title_zh: 零样本视觉语言模型用于课堂参与度识别：提示敏感性与跨数据集泛化的基准研究
authors: "Aman Goyal, Kshama Nitin Shah, Kemmannu Vineet Venkatesh Rao"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21861v1"
tags: ["query:vl"]
score: 9.0
evidence: 课堂参与识别的VLM基准测试
tldr: "本研究系统评估了五种主流视觉语言模型（CLIP、BLIP-VQA、GPT-4o、LLaVA-1.5-7B、Qwen2.5VL-7B-Instruct）在零样本条件下对课堂投入度识别的表现，使用DAiSEE和SCB两个教育数据集，并测试三种提示变体。结果表明，模型在学生个体识别上近乎随机（Cohen's kappa≤0.10），存在严重类别坍塌和提示敏感性（准确率波动达32%），但场景级分类表现较好（kappa≈0.60）。GPT-4o的安全过滤器拒绝了98%涉及学生面部的链式思考请求。研究为VLM在教育观察系统中的应用提供了校准基线和关键设计考虑。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21861v1/fig-001.webp\", \"caption\": \"Figure 5. t-SNE projection of CLIP ViT-B/32 image embeddings for all 300 DAiSEE frames, coloured by ground-truth engagement label. The four engagement classes intermix within every cluster, confirming that CLIP organises frames by subject identity rather than engagement state.\", \"page\": 6, \"index\": 1, \"width\": 470, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2606.21861v1/fig-002.webp\", \"caption\": \"Table 2. Full prompt-sensitivity results on DAiSEE. Pred. Dist. shows the fraction of predictions in each class (GT: 1%–28%–35%– 35%).\", \"page\": 4, \"index\": 2, \"width\": 492, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2606.21861v1/fig-003.webp\", \"caption\": \"Figure 3. Fraction of the 300 DAiSEE samples predicted at each engagement level, for every model-prompt combination and the ground truth distribution. Warm colours indicate high concentration. Each model collapses to 1–2 dominant classes, confirming class collapse rather than genuine multi-class discrimination.\", \"page\": 5, \"index\": 3, \"width\": 480, \"height\": 546}]"
motivation: 课堂投入度自动识别对学习分析有重要价值，但现代视觉语言模型在零样本条件下的适用性尚未被充分探索。
method: 使用五个VLM和两个数据集（DAiSEE个体视频、SCB场景图像），通过三种提示变体（最小、基于规则、链式思考）进行零样本分类实验。
result: "模型在个体识别上近乎随机（kappa≤0.10），存在类别坍塌和高达32%的提示敏感度；场景级分类较好（kappa≈0.60），GPT-4o安全过滤器拒绝98%的个体面部推理。"
conclusion: 零样本VLM对个体投入度识别不可靠，但场景级分类可行；提示设计至关重要，部署需考虑安全过滤等实际障碍。
---

## 摘要
自动课堂参与度识别对于可扩展的学习分析具有重要前景，然而现代视觉语言模型（VLM）在零样本条件下对此任务的适用性仍基本未被探索。我们提出了一项系统基准测试，评估了五种广泛使用的VLM：CLIP、BLIP-VQA、GPT-4o、LLaVA-1.5-7B和Qwen2.5VL-7B-Instruct，在两个互补的教育数据集上：DAiSEE（个体学生视频数据集，300个采样测试片段）和学生课堂行为数据集（SCB，1,168个场景级图像）。每个模型使用三种提示变体进行探测，包括最小提示、基于评分标准的提示和思维链设计。我们的实验揭示了零样本VLM在参与度识别中的三个主要失败模式：（1）个体学生上的性能接近随机，Cohen's kappa在DAiSEE上从未超过0.10；（2）严重的类别坍缩，无论视觉内容如何，模型将85-100%的预测分配给单一参与度水平；（3）极端的提示敏感性，仅因提示措辞不同，相同图像上的准确率波动高达32个百分点。值得注意的是，在SCB上的场景级分类更容易处理：当使用基于行为的评分标准提示时，CLIP和GPT-4o的kappa约为0.60。我们还记录了一个实际部署障碍：GPT-4o的安全过滤器拒绝了98%涉及个体学生面孔的思维链请求。我们的发现为在教育观察系统中使用VLM提供了校准基线和关键设计考虑。

## Abstract
Automated classroom engagement recognition holds substantial promise for scalable learning analytics, yet the suitability of modern Vision-Language Models (VLMs) for this task under zero-shot conditions remains largely unexplored. We present a systematic benchmark that evaluates five widely-used VLMs: CLIP, BLIP-VQA, GPT-4o, LLaVA-1.5-7B, and Qwen2.5VL-7B-Instruct across two complementary educational datasets: DAiSEE, an individual-student video dataset (300 sampled test clips), and the Student Classroom Behaviour dataset (SCB, 1,168 scene-level images). Each model is probed with three prompt variants spanning minimal, rubric-anchored, and chain-of-thought designs. Our experiments reveal three primary failure modes of zero-shot VLMs for engagement recognition: (1) near-random performance on individual students, with Cohen's kappa never exceeding 0.10 on DAiSEE; (2) severe class collapse, where models assign 85-100% of predictions to a single engagement level regardless of visual content; and (3) extreme prompt sensitivity, with accuracy swings of up to 32 percentage points on identical images depending solely on prompt phrasing. Remarkably, scene-level classification on SCB is substantially more tractable: CLIP and GPT-4o achieve kappa approximately 0.60 when prompted with behaviorally-grounded rubrics. We also document a practical barrier for deployment: GPT-4o's safety filters reject 98% of chain-of-thought requests involving individual student faces. Our findings provide a calibrated baseline and surface critical design considerations for the use of VLMs in educational observation systems.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：自动课堂参与度识别对于可扩展的学习分析具有重要意义，传统方法依赖人工观察，成本高且难以规模化。现代视觉语言模型（VLM）具备零样本推理能力，可避免特定场景的标注成本，但其在课堂参与度识别任务中的适用性尚未被系统研究。
- **核心问题**：零样本VLM能否准确识别个体学生和整体课堂的参与度水平？提示设计如何影响性能？模型存在哪些系统性的失败模式？
- **整体含义**：论文首次对五种主流VLM在两种互补数据集（个体视频DAiSEE和场景级图像SCB）上进行全面的零样本基准测试，揭示了当前VLM在个体级识别上的根本性局限（近乎随机），但场景级识别具有可行性（kappa≈0.60）。研究为教育AI系统的设计提供了关键指导：应优先采用场景级聚合分析，而非逐学生分析；提示敏感性和安全过滤是实际部署的重要障碍。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：零样本分类——不进行任何微调，直接使用VLM的预训练知识，通过自然语言提示（prompt）对图像进行四类参与度评级（0=未参与，1=勉强参与，2=参与，3=高度参与）。
- **关键技术细节**：
  - **模型选择**：五种VLM覆盖对比学习（CLIP、BLIP-VQA）、闭源多模态（GPT-4o）、开源指令微调（LLaVA-1.5-7B、Qwen2.5-VL-7B-Instruct）。
  - **提示设计**：三种提示变体——
    - **P1（最小提示）**：仅给出四类数字标签，无行为描述。
    - **P2（基于评分标准）**：为每类提供明确的行为锚点（如“低头玩手机”、“举手”等）。
    - **P3（思维链）**：要求模型先描述行为和姿势，再给出评级。
  - **分类机制**：
    - CLIP：计算图像嵌入与每个类别文本模板的余弦相似度，取最高者。
    - BLIP-VQA：以问答形式生成自由文本答案，通过手工映射表转换为类别。
    - GPT-4o、LLaVA、Qwen：直接输出数字评级（P1/P2）或多步骤推理结果（P3）。
  - **评估指标**：准确率（Exact-match 4-class）、F1 Macro、Cohen's κ（二次加权）、MSE（平均平方误差）。对于自一致性，报告三运行的完全一致率与配对一致率。

### 3. 实验设计：使用的数据集、基准、对比方法
- **数据集**：
  - **DAiSEE**（个体学生视频）：9,068个10秒视频片段，112名被试，标注参与度0-3。测试集1,784个片段；论文从中分层抽样300个（保留所有0级和1级样本，随机抽取2/3级各106个），提取中间帧（t=5s）进行静态图像测试。
  - **SCB**（场景级课堂图像）：1,168张验证图像（源于5K原始数据和大学子集），包含每位学生的边界框和六种行为标签（阅读、写字、举手、使用手机、低头、趴桌）。论文将行为映射为参与度得分（0-3），取全班平均后离散化为四级标签。标签分布高度不平衡：54.5%为3级，33.6%为2级，10.1%为1级，1.9%为0级。
- **基准（benchmark）**：论文未提供横向对比的已有零样本VLM结果，而是与文献中的有监督方法进行对比（ResNet+TCN、Huang et al.、CavT），以及对五种VLM的三种提示进行系统内比较。
- **对比方法**：五种模型（CLIP、BLIP-VQA、GPT-4o、LLaVA-1.5-7B、Qwen2.5-VL-7B-Instruct）× 三种提示（P1、P2、P3），共15种配置在DAiSEE和SCB上分别评估。此外，对GPT-4o和LLaVA进行了自一致性实验（温度T=0.7重复3次）。

### 4. 资源与算力
- 文中未明确说明训练所需的GPU型号、数量或训练时长，因为所有实验均为零样本推理，不涉及模型训练。
- 对于推理环境：
  - LLaVA-1.5-7B以4-bit量化在本地运行（内存约4 GB，机器有16 GB RAM）。
  - Qwen2.5-VL-7B-Instruct以bfloat16精度在Hugging Face Transformers框架下运行。
  - GPT-4o通过OpenAI API调用，图像以base64编码低分辨提交。
  - CLIP和BLIP-VQA使用较小模型（ViT-B/32），可在消费级GPU上推理。
- **备注**：未给出具体的GPU型号或推理耗时，但整体计算需求较低（仅推理，无训练）。

### 5. 实验数量与充分性
- **实验数量**：
  - 主实验：两个数据集 × 五个模型 × 三个提示 = 30个配置。每个配置报告准确率、F1 Macro、κ、MSE。
  - 自一致性实验：GPT-4o（P2）和LLaVA（P3）重复3次（T=0.7）。
  - 特征空间分析：对CLIP在DAiSEE上进行t-SNE可视化和文本-图像余弦相似度分析。
- **充分性与公平性**：
  - **充分性**：覆盖了多个模型类型（判别式、生成式、开源、闭源）、多种提示设计（从最小到结构化推理）、两个粒度（个体与场景），实验设计较为全面。
  - **公平性**：提示文本保持统一（CLIP模板略有调整以适应余弦相似度）；BLIP-VQA的映射表为手工构建，可能存在偏好；GPT-4o的P3因安全过滤导致98%拒答，使用fallback到2级，对该配置的公平性有影响。总体而言，实验设计考虑了跨模型直接比较，但未控制模型大小和推理成本差异（如LLaVA是7B量化版，GPT-4o是闭源更大模型）。
  - **局限性**：仅使用单帧图像（DAiSEE），忽略了时间信息；SCB的参与度标签基于行为映射而非直接人工评分，可能偏离教师判断；DAiSEE仅涉及16名被试，样本量较小，可能过拟合于特定人群。

### 6. 论文的主要结论与发现
1. **个体学生识别失败**：所有VLM在DAiSEE上的Cohen's κ不超过0.10，接近随机。最佳配置（LLaVA P3）准确率仅39.0%，但仍低于多数类基线（35.3%），实际仅捕捉到模态类。
2. **三种主要失败模式**：
   - **类别坍缩**：模型将85-100%的预测集中在1-2个类（如BLIP-VQA P3全部预测为2级），忽略其他类。
   - **提示极端敏感性**：同一模型在不同提示下准确率波动可达32个百分点（LLaVA在DAiSEE上从5.3%到39.0%）。
   - **安全过滤阻断**：GPT-4o的P3（要求描述学生面部表情）触发安全过滤器，拒绝98%的个体图像请求，导致fallback到2级，虚增准确率。
3. **场景级识别更可行**：在SCB上，CLIP（P3）和GPT-4o（P2）分别达到κ=0.60和κ=0.58，准确率约67%，达到中等一致性。
4. **特征空间分析揭示根本原因**：CLIP的t-SNE显示图像根据被试身份而非参与度水平聚类；文本-图像余弦相似度范围仅约0.07，且正确类别的模板从未获得最高相似度，表明CLIP无法从视觉特征中区分参与度。
5. **自一致性不足**：GPT-4o在3次运行中仅68%的预测完全一致；LLaVA为73.3%。κ值在不同运行间变化显著（0.008-0.101），单次运行结果不可靠。
6. **开源模型竞争力**：LLaVA-1.5-7B（4-bit量化）在DAiSEE上匹配甚至略优于GPT-4o，在SCB上接近，且无需API成本、数据不外流，适合隐私敏感场景。

### 7. 优点：方法或实验设计上的亮点
- **系统性基准**：首次对多种VLM（涵盖小型对比模型到大型生成模型）在课堂参与度任务上进行零样本统一评估，提供了可复用的基线。
- **提示设计的精细控制**：设计了三种渐进式提示（最小→评分标准→思维链），有效量化了提示敏感性（Δ accuracy、Δ κ），为后续研究提供了评测维度。
- **跨粒度比较**：同时分析个体（近景面部）和场景（课堂全局）两个粒度，揭示出本质差异，给出了“场景优先聚合”的设计建议。
- **深入的特征空间分析**：通过t-SNE和余弦相似度分布，从机制上解释了为何零样本个体识别失败——特征是身份而非参与度主导。
- **揭露实际部署障碍**：记录GPT-4o安全过滤导致98%拒答，以及自一致性差的问题，对工业部署具有警示意义。
- **鼓励开源与隐私保护**：强调LLaVA-1.5在本地运行的竞争力，为数据不出校的方案提供支持。

### 8. 不足与局限
- **实验覆盖局限**：
  - DAiSEE仅使用单帧（第5秒），丢弃了时间动态信息，而视频本身包含10秒动作，单帧可能不足以捕捉参与度（尤其对微妙表情变化）。
  - 仅测试了静态图像输入，未考虑视频VLM（如VideoCLIP）或时间融合方法。
  - SCB的参与度标签是从行为类别映射而来（如“举手”自动映射为3级），并非直接人工评分，可能引入系统性偏差，且与教师的真实参与度判断不一定一致。
  - DAiSEE仅21名被试（测试集），普适性存疑；SCB也是单一环境（教室），未覆盖其他教学场景。
- **模型选择偏差**：仅测试了5个VLM，且CLIP和BLIP-VQA模型较小（ViT-B/32），与GPT-4o等大型模型可比性有限；未测试EVA-CLIP、SigLIP等更新模型。BLIP-VQA的映射表由作者手工构建，可能引入偏好。
- **零样本设置限制**：论文明确限定于零样本，但未探索线性探测或低资源微调是否能缩小差距。事实上，现有有监督方法已在DAiSEE上达到58-62%准确率，而零样本仅39%，说明微调可能是必要的。
- **安全性问题被部分忽略**：GPT-4o的安全过滤被表示为“实际障碍”，但未讨论在其他模型上类似的面部隐私问题；也未提出技术或伦理上的解决方案。
- **自一致性实验仅在T=0.7下进行**，未测试更低温度的影响；GPT-4o的P2在T=0.7下仍有21%的预测不一致，说明即使不使用思维链，稳定性也成问题。
- **缺失统计显著性检验**：论文报告了数值差异（如准确率变化32pp），但未进行统计检验（如配对t检验或McNemar检验）来确认差异是否显著。
- **缺乏代码和提示的完整公开**：虽然补充材料提供了部分提示文本，但未提供完整的实验代码或数据集分割细节，可复现性有待加强。

（完）
