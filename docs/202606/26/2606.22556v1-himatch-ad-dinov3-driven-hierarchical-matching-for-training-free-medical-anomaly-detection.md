---
title: "HiMatch-AD: DINOv3-driven Hierarchical Matching for Training-free Medical Anomaly Detection"
title_zh: "HiMatch-AD: 基于DINOv3驱动的层级匹配实现无需训练的医学异常检测"
authors: "Jiayu Huo, Jingyuan Hong, Meng Zhou, Liyun Chen, Le Zhang"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22556v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用DINOv3的医学异常检测
tldr: 本文提出HiMatch-AD，一种基于DINOv3的无训练医学异常检测方法。通过双分支匹配检索正常参考，跨Transformer阶段生成层次异常图，并利用不确定性加权融合多级响应。在BMAD基准（脑MRI、肝CT、视网膜OCT）上，该方法无需任务训练就超越现有训练型和无训练型方法，展现了层次匹配与不确定性融合的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22556v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed HiMatch-AD framework. Our HiMatch-AD framework contains two steps: K support images are retrieved through dual-branch matching (left). Then the query and support images are sent to the hierarchical training-free anomaly detection pipeline for anomaly map generation (upper right). The details of A-Map Gen module are shown on the bottom right. Here “Enc.” denotes the encoder, where DINOv3 is applied.\", \"page\": 3, \"index\": 1, \"width\": 706, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2606.22556v1/fig-002.webp\", \"caption\": \"Table 1. Comparisons to SOTA models on BMAD benchmark datasets. The best results are in bold and the second best are underlined.\", \"page\": 7, \"index\": 2, \"width\": 727, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2606.22556v1/fig-003.webp\", \"caption\": \"Fig. 2. Quantitative comparison of different ViT backbones for feature extraction (left). Performance and inference speed as the number of support images K increases from 1 to 16 (middle). Visualization of anomaly maps generated by our approach, with green contours indicating anomalous regions (right).\", \"page\": 7, \"index\": 3, \"width\": 710, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2606.22556v1/fig-004.webp\", \"caption\": \"Table 2. Ablation studies of the matching method and the fusion method of our proposed method HiMatch-AD. We keep the number of the support set K to 4 across all ablation experiments.\", \"page\": 8, \"index\": 4, \"width\": 540, \"height\": 287}]"
motivation: 现有训练型方法需要大量正常数据和任务优化，无训练方法依赖简单近邻检索和聚合，缺乏层次语义与多响应可靠性。
method: 使用DINOv3提取特征，双分支匹配（全局CLS+局部patch）检索正常参考，跨多Transformer阶段聚类正常特征生成层次异常图，并通过不确定性加权融合。
result: 在BMAD基准的脑MRI、肝CT和视网膜OCT数据集上，HiMatch-AD性能全面超越现有训练型与DINO基方法。
conclusion: 层次匹配与不确定性感知融合对于可扩展的医学异常检测至关重要。
---

## 摘要
异常检测对于医学图像分析至关重要，其中病理区域通常表现为正常解剖结构的罕见偏离。尽管基于训练的方法已取得良好的性能，但它们需要针对特定任务的优化和大量的正常数据，这限制了跨模态和跨机构的可扩展性。无需训练的方法通过利用预训练的视觉表示提供了更大的灵活性，但现有方法通常依赖于简单的最近邻检索和朴素的聚合策略，这可能无法捕捉层级语义，并忽略多个异常响应的可靠性。在这项工作中，我们提出了HiMatch-AD，一个基于DINOv3驱动的层级匹配框架，用于无需训练的医学异常检测。我们的方法首先通过双分支匹配检索语义相关的正常参考，该匹配联合考虑全局CLS标记相似性和补丁级表示。然后通过比较聚类的正常特征与查询表示，在多个Transformer阶段生成层级异常图。为了稳健地聚合异常响应，我们引入了一种统一的基于不确定性的融合机制，该机制根据每个图的可信度自适应地赋予权重。整个框架无需任何任务特定训练即可运行。在BMAD基准测试上的大量实验，包括脑部MRI、肝脏CT和视网膜OCT数据集，表明HiMatch-AD始终优于基于训练和基于DINO的最先进方法，这突出了多层次匹配和不确定性感知融合在可扩展的医学异常检测中的有效性。

## Abstract
Anomaly detection is essential for medical image analysis, where pathological regions often appear as rare deviations from normal anatomical structures. While training-based methods have achieved promising performance, they require task-specific optimization and extensive normal data, which limits scalability across modalities and institutions. Training-free approaches offer greater flexibility by leveraging pretrained visual representations, yet existing methods typically rely on simple nearest-neighbor retrieval and naive aggregation strategies, which may fail to capture hierarchical semantics and ignore the reliability of multiple anomaly responses. In this work, we propose HiMatch-AD, a DINOv3-driven hierarchical matching framework for training-free medical anomaly detection. Our method first retrieves semantically relevant normal references via dual-branch matching that jointly considers global CLS-token similarity and patch-level representations. Hierarchical anomaly maps are then generated across multiple transformer stages by comparing clustered normal features with query representations. To robustly aggregate anomaly responses, we introduce a unified uncertainty-based fusion mechanism that adaptively weights maps according to their reliability. The entire framework operates without any task-specific training. Extensive experiments on the BMAD benchmark, including brain MRI, liver CT, and retinal OCT datasets, demonstrate that HiMatch-AD consistently outperforms both training-based and DINO-based state-of-the-art methods, which highlights the effectiveness of multi-level matching and uncertainty-aware fusion for scalable medical anomaly detection.

---

## 论文详细总结（自动生成）

# HiMatch-AD: 基于DINOv3驱动的层级匹配实现无需训练的医学异常检测

## 1. 论文的核心问题与整体含义

- **研究动机**：医学图像异常检测中，病理区域通常表现为罕见偏离正常解剖结构。训练型方法（如学生-教师网络、重建框架）需要大量正常数据和任务特定优化，限制了跨模态、跨机构的可扩展性。无训练方法更灵活，但现有方法依赖简单的最近邻检索和朴素聚合策略，无法捕捉层级语义，且忽视多个异常响应的可靠性差异。
- **整体含义**：提出一种**无需任何任务特定训练**的异常检测框架，利用DINOv3强大的自监督视觉表示，通过层级匹配和不确定性感知融合，在多个医学影像数据集上超越现有方法，实现可扩展、高性能的异常检测。

## 2. 论文提出的方法论

### 核心思想
- 使用预训练的DINOv3 ViT-B/16作为特征提取器（冻结参数）。
- 通过**双分支匹配**（全局CLS标记 + 局部补丁）从正常图像池中检索与查询图像最相似的前K张正常图像作为支持集。
- 在DINOv3的4个层级阶段（每3个Transformer块为一个阶段）分别提取查询和支持图像的补丁特征，对每个支持图像使用K-means聚类得到C个质心，计算查询特征与质心的余弦相似度并转化为异常响应，再通过softmax融合多个质心的结果，得到该支持图像在该阶段的异常图。
- 最后通过**统一的不确定性融合**机制：先对多个支持图像的异常图进行实例级融合（基于与平均图的偏差计算不确定性权重，用softmin加权），再对不同阶段的异常图进行阶段级融合（相同公式），得到最终异常图。

### 关键技术细节
- **双分支匹配公式**：
  - CLS分支：查询CLS与正常图像CLS的余弦相似度
  - 补丁分支：对所有补丁token做空间平均池化得到全局补丁描述符，计算余弦相似度
  - 最终相似度 = 两者聚合（原文未明确是平均还是加权，但实验显示两者联合最好）
  - 选取Top-K正常图像作为支持集。
- **异常图生成（A-Map Gen）**：
  - 对每个支持图像在阶段t的特征做K-means，得到C个质心。
  - 计算每个质心与查询特征所有位置的余弦相似度，归一化到[0,1]（1- (sim+1)/2）。
  - softmax融合：对C个响应图按异常值大小加权平均，强调高异常区域。
- **不确定性融合**：
  - 实例级融合：计算所有支持图像异常图的平均图，每个图的偏差平方作为不确定性，softmin归一化得到权重，加权平均。
  - 阶段级融合：同样方法处理4个阶段的异常图。
  - 最终异常图用于像素级和图像级（取最大值）评估。

## 3. 实验设计

### 数据集与Benchmark
- **BMAD benchmark**[3]：包含三个数据集：
  - **脑MRI**（BraTS 2021）：7500正常训练，测试集640正常+3075异常（像素级掩码）
  - **肝脏CT**（BTCV + LiTs）：1542正常训练，测试833正常+660异常
  - **视网膜OCT**（RESC）：4297正常训练，测试1041正常+764异常
- 所有训练集仅含正常图像，用于支持集检索，无异常训练。

### 对比方法
- **训练型方法**：DRAEM, UTRAD, SimpleNet, MKD, RD4AD, STFPM, PaDiM
- **无训练/记忆型方法**：PatchCore, AnomalyDINO, DINO-DPMM
- **评估指标**：I-AUC（图像级）、P-AUC（像素级）、P-PRO（区域重叠）
- **实现细节**：DINOv3 ViT-B/16，K=4（支持集大小），C=2（聚类数），无训练。

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量、训练时长。由于方法无需训练，仅需推理（特征提取、匹配、聚类、融合），算力需求较低。文中提到推理速度随支持集K增大而下降（K=1时10.24 query/s，K=16时0.64 query/s），但未提供具体硬件信息。

## 5. 实验数量与充分性

- **主实验**：在三个数据集上对比了7种训练方法和3种无训练方法，共10个对比方法，报告了I-AUC、P-AUC、P-PRO三个指标。
- **消融实验**：
  - 不同预训练骨干（DINOv3 vs DINOv2 vs CLIP-ViT）
  - 支持集大小K（1,4,8,16）对性能与速度的影响
  - 匹配策略（CLS/补丁/两者联合 vs 随机检索）
  - 融合策略（无/实例级/阶段级/两者联合）
- **可视化**：展示了异常图与ground truth的对齐情况。
- **充分性评估**：实验覆盖了多个模态（MRI、CT、OCT），消融全面验证了各组件贡献，对比方法包含主流方法。但缺少在自然图像或更多医学模态上的验证，且未与最新的基于重建或扩散的方法对比（如DDPM-based AD）。然而，作为一篇训练-free方法，实验已足够充分且公平（固定骨干、同一评估协议）。

## 6. 论文的主要结论与发现

- HiMatch-AD在所有三个数据集上全面超越训练型和无训练型SOTA方法，包括AnomalyDINO和PatchCore。
- 双分支匹配（全局+局部）比单独使用任一分支或随机检索效果更好，表明两者互补。
- 不确定性融合（实例+阶段）显著优于简单平均，表明能有效抑制不一致响应。
- DINOv3骨干优于DINOv2和CLIP-ViT，说明更强的自监督特征对匹配更重要。
- 支持集大小K=4在性能与速度之间取得最佳平衡。
- 整个框架无需训练，具有很好的可扩展性。

## 7. 优点

- **创新性强**：首次将层级匹配（多Transformer阶段）与不确定性融合用于无训练医学异常检测，弥补了现有方法层级语义捕捉和可靠性聚合的不足。
- **方法简洁有效**：无需训练，仅利用冻结的DINOv3特征，推理高效，易于部署。
- **实验扎实**：在3个医学模态、10+对比方法上全面评估，消融实验验证每个模块的必要性。
- **通用性**：可推广到其他预训练视觉模型（如DINOv2、CLIP）和更多医学影像场景。

## 8. 不足与局限

- **缺乏更大的多模态验证**：仅测试了脑MRI、肝CT、视网膜OCT，未涵盖X光、超声、病理等常见模态，可能限制泛化结论。
- **支持集检索依赖正常图像池**：实际应用中需要大规模正常图像库，若正常图像分布偏移（如不同医院设备），检索质量可能下降。未探讨domain shift场景。
- **聚类参数固定**：C=2根据先前工作设定，未在文中分析不同C的影响；K=4基于消融，但可能因数据集而异，缺乏更细的调优。
- **未与其他无训练方法进行更深入比较**：如基于特征重构的方法（如SPADE）或基于高斯分布的方法（如PaDiM变体）。DINO-DPMM是最近方法，但对比数量有限。
- **计算开销**：对每个查询需在支持集上做K-means聚类和逐阶段匹配，虽无需训练，但推理速度随K增大下降明显（K=16仅0.64 qps），可能不满足实时需求。
- **未提供统计学显著性检验**：结果报告为单次数值，缺少多次重复实验的标准差，难以评估稳定性和显著性。
- **局限性总结**：论文未明确讨论方法的局限性，例如对小病灶或扩散性病变的检测能力，以及当查询异常与正常池中图像高度相似时的失效模式。

（完）
