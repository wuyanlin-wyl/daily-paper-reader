---
title: "EnTrust: Modeling Inter-Modal Conflict for Trustworthy Multimodal Medical Image Analysis"
title_zh: "EnTrust: 模态间冲突建模以实现可信的多模态医学图像分析"
authors: "Dwarikanath Mahapatra, Abhijit Das, Behzad Bozorgtabar, Zongyuan Ge, Sudipta Roy, Deepak Nayak, Mauricio Reyes, Imran Razzak"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21384v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 具有冲突建模的多模态医学图像分割
tldr: 多模态医学图像分析中，模态间冲突常被忽略或后处理，导致预测不可靠。EnTrust框架将模态冲突作为不确定性源头，通过解耦模态特征、扩散分割模型和校准不确定性图，实现了高精度和低校准误差，单模型性能超越5倍深度集成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21384v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative comparison on BraTS 2021. EnTrust produces boundarylocalized uncertainty aligned with Fcf ; baselines exhibit progressively diffuse estimates.\", \"page\": 8, \"index\": 1, \"width\": 579, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2606.21384v1/fig-002.webp\", \"caption\": \"Fig. 2. EnTrust architecture. ModEnc extracts per-modality features, which EnFuse disentangles into shared (Fc), unique (Fu,m), and conflict (Fcf ) components. The gated fusion Fen conditions SegDiff for probabilistic mask generation. TrustMap derives pixelwise uncertainty via ensemble entropy H(p), ProbeStep Ŷ K 0 , and CalibHead C(p).\", \"page\": 4, \"index\": 2, \"width\": 579, \"height\": 510}]"
motivation: 现有方法要么确定性融合忽略冲突，要么后验不确定性估计与融合解耦，无法揭示预测不可靠的原因。
method: 提出EnTrust框架，包含EnFuse模块解耦模态特征为共享共识、模态特异和冲突信号，SegDiff扩散分割模型在冲突区域产生假设分歧，TrustMap通过集合熵和冲突引导扰动生成校准的像素级不确定性图。
result: "在四个跨脑、心脏、病变和肿瘤的基准上达到SOTA分割精度，校准误差降低40%，单模型超越5倍深度集成且内存减半。"
conclusion: EnTrust通过显式建模模态冲突实现了可信赖的多模态医学图像分析。
---

## 摘要
多模态医学成像融合了互补的解剖和功能信息，然而在病理异质性区域，不同模态常常产生分歧。当前的分割模型通过两种不充分的方式处理这一问题：确定性融合平均化分歧，或将后验不确定性估计与产生该估计的融合过程解耦。这两种方式都掩盖了临床上关键的问题：为什么这个预测不可靠？我们提出了EnTrust框架，将模态间冲突视为预测不确定性的主要来源。我们的EnFuse模块将多模态特征分解为三个解耦的组成部分：共享的解剖共识（F_c）、模态特定线索（F_{u,m}）以及空间定位的冲突信号（F_{cf}），并通过交叉协方差目标强制实现独立性。这种结构化解耦条件作用于SegDiff（一种基于扩散的生成式分割模型），其采样假设在模态不一致的区域中特异性地发散。随后，TrustMap利用集成熵、冲突引导的扰动探测以及一个学习到的校准头，将这些假设发散转换为经过校准的像素级不确定性，使临床医生不仅了解预测的不确定性位置，还理解其原因。在涵盖脑、心脏、病灶和肿瘤领域的四个基准测试中，EnTrust实现了最先进的分割精度，同时与最强基线相比，校准误差降低了40%。值得注意的是，它使用单个模型，在内存占用大约一半的情况下，超越了5倍深度集成。代码和检查点可从https://github.com/GenMI-Lab/EnTrust.git获取。

## Abstract
Multimodal medical imaging fuses complementary anatomical and functional information, yet modalities frequently disagree in pathologically heterogeneous regions. Current segmentation models handle this in one of two inadequate ways: deterministic fusion that averages away disagreement, or post-hoc uncertainty estimation decoupled from the fusion process that produces it. Both obscure the clinically critical question: why is this prediction unreliable? We present EnTrust, a framework that treats inter-modal conflict as the primary source of predictive uncertainty. Our EnFuse module decomposes multimodal features into three disentangled components: shared anatomical consensus (F_c), modality-specific cues (F_{u,m}), and spatially localized conflict signals (F_{cf}), with independence enforced via a cross-covariance objective. This structured decomposition conditions SegDiff, a diffusion-based generative segmentation model whose sampled hypotheses diverge specifically in regions of modal disagreement. TrustMap then translates this hypothesis divergence into calibrated, pixel-wise uncertainty using ensemble entropy, conflict-guided perturbation probing, and a learned calibration head, enabling clinicians to understand not only where predictions are uncertain, but why. Across four benchmarks spanning brain, cardiac, lesion, and oncology domains, EnTrust achieves state-of-the-art segmentation accuracy while reducing calibration error by 40% compared to the strongest baseline. Notably, it outperforms 5x deep ensembles using a single model at roughly half the memory footprint. Code and checkpoints are available at https://github.com/GenMI-Lab/EnTrust.git.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态医学图像分析中，不同模态（如 MRI 序列、CT、PET）在病理异质性区域常常不一致，现有融合方法要么**确定性融合**（平均化分歧），要么**后验不确定性估计**（与融合过程解耦，反映模型噪声而非数据冲突），都未能直接回答临床关键问题——“为什么这个预测不可靠？”
- **整体含义**：论文提出将**模态间冲突**视为预测不确定性的首要来源，通过显式建模冲突来提高分割准确性和可解释性，使临床医生不仅知道“哪里不可靠”，还知道“为什么不可靠”，从而提升 AI 辅助诊断的信任度。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个端到端框架 EnTrust，包含三个紧密耦合的阶段：
  1. **EnFuse**：将多模态特征解耦为三个可解释成分——共享解剖共识（\(F_c\)）、模态特有线索（\(F_{u,m}\)）和空间定位的冲突信号（\(F_{cf}\)），并通过交叉协方差目标强制独立性。
  2. **SegDiff**：基于扩散模型的分割生成器，条件输入为 EnFuse 输出的结构化特征 \(F_{en}\)（包含冲突信号），使得采样出的多个假设在冲突区域主动发散，而不是均匀随机变化。
  3. **TrustMap**：将假设发散转化为校准的像素级不确定性图，使用集成熵、冲突引导的扰动探测（ProbeStep）和一个学习到的校准头（CalibHead），确保不确定性估计既准确又具有可解释性。
- **关键技术细节**：
  - **ModEnc**：每个模态使用独立的 Swin Transformer 编码器，可替换（如 ResNet-50、ViT-B/16）。
  - **EnFuse**：三个轻量级 ConvNeXt 头分别提取共享、独特和冲突特征；交叉协方差损失 \(L_{en}\) 最小化各成分间相关性。
  - **SegDiff**：标准 DDPM，\(T=1000\) 步，3D U-Net 去噪器，条件通过交叉注意力和 AdaIN 注入。
  - **TrustMap**：
    - 集成熵：对 \(K=5\) 个扩散样本计算平均预测熵 \(H(v)\)。
    - ProbeStep：利用冲突图扰动初始噪声，额外采样以检测边界敏感性（仅推理阶段）。
    - CalibHead：辅助网络预测像素级置信度，通过损失 \(L_{cal}\) 使置信度与正确概率对齐。
  - **联合训练损失**：\(L_{total}=L_{diff}+\lambda_{en}L_{en}+\lambda_{cal}L_{cal}\)，权重通过网格搜索确定（\(\lambda_{en}=0.1,\lambda_{cal}=0.5\)）。

### 3. 实验设计

- **数据集与场景**（四个基准，涵盖脑、心脏、病灶、肿瘤）：
  - BraTS 2021（4 模态 MRI，胶质瘤分割）
  - MS-CMRSeg（bSSFP、LGE，心脏结构分割）
  - ATLAS（CT、FLAIR MRI，脑病变分割）
  - H&N CT-PET（头颈肿瘤分割）
- **Benchmark 与评价指标**：
  - 分割精度：Dice Similarity Coefficient (DSC) ↑、Hausdorff Distance 95% (HD95) ↓
  - 不确定性质量：Expected Calibration Error (ECE) ↓、AUROC-Error ↑、Negative Log-Likelihood (NLL) ↓
- **对比方法**（涵盖主要不确定性范式）：
  - 3D U-Net（确定性融合）
  - Probabilistic U-Net（条件 VAE）
  - Deep Ensemble (5×)（深度集成）
  - DEviS（证据深度学习）
  - MedSegDiff（扩散分割基线，与 EnTrust 共享相同去噪器）

### 4. 资源与算力

- 论文明确说明：在 **4× NVIDIA A100 (64 GB)** GPU 上训练，batch size=2，300 个 epoch。总训练时长：**48 GPU-hours**（单模型），相比 Deep Ensemble (5×) 的 70 GPU-hours 显著降低。推理时峰值内存 **14.8 GB**，约为 Deep Ensemble 的约一半（28.4 GB）。

### 5. 实验数量与充分性

- **实验覆盖**：
  - 4 个多样的数据集，覆盖脑肿瘤、心脏、病变、头颈肿瘤，具有代表性。
  - 与 5 种主流方法对比，包含确定性、贝叶斯、集成、证据、扩散等不同流派。
  - **消融实验**（Table 4）详实：分别去除 EnFuse、去除冲突模块 Dcf、去除 CalibHead、去除 ProbeStep，以及替换编码器（ResNet-50、ViT-B/16），共 7 种变体。
  - 补充材料中还分析了推理样本数 \(K\) 的敏感性（Table S1）和损失权重敏感性（Table 2）。
- **充分性评估**：
  - 实验设计公平：采用固定分割、多次运行取平均（3 个种子）、统计显著性检验（Wilcoxon）。
  - 结果全面展示精度与不确定性质量，且消融实验隔离了每个组件的贡献。
  - 结论：实验充分且客观，覆盖了主要性能维度和关键设计选择。

### 6. 论文的主要结论与发现

- EnTrust 在**所有四个数据集**上取得了 Dice 和 HD95 的最佳平均值，同时**校准误差（ECE）降低约 40%** 相对于最强基线（MedSegDiff）。
- **单模型胜过 5× 深度集成**：性能更高，但参数减少约 3 倍，训练时间减少 17%，推理内存减半。
- **冲突建模提升分割质量**：不仅是不确定性，还直接改善精度（去除 Dcf 导致 DSC 下降 1.9%，ECE 上升 38.3%）。
- **不确定性具有空间可解释性**：TrustMap 得到的不确定性图紧贴病变边界，并与冲突图 \(F_{cf}\) 对齐，而基线方法产生弥散估计。

### 7. 优点

- **方法论创新**：首次显式将模态冲突作为建模目标，通过解耦组件（共识、独特、冲突）实现可解释的不确定性，不依赖于后验噪声假设。
- **实用性强**：
  - 编码器可替换（ResNet-50、ViT-B/16）而不需修改下游模块，便于适配不同部署场景。
  - 单模型计算效率显著优于 5× 集成，适合资源受限环境。
- **实验严谨**：多数据集、多基线、完整消融、统计检验、代码和检查点已公开，可复现。
- **临床相关**：提供了“哪里”和“为什么”的双重信息，辅助临床决策（如根据冲突图决定是否加扫其他序列）。

### 8. 不足与局限

- **临床验证缺失**：论文指出临床效用需在读者研究（reader study）中进一步验证，目前仅展示定量指标。
- **数据模态局限性**：仅测试了 MRI、CT、PET 组合，未评估其他常见模态（如超声、病理图像）或非图像数据（如临床文本）。
- **扩散模型推理开销**：虽然比 5× 集成更优，但单模型推理需要多次采样（K=5），推理时间仍较长（Fig. 4b 显示约 8-10 s/volume，高于 U-Net 等确定性模型），实时场景可能受限。
- **冲突定义简化**：冲突信号仅由特征层面的不一致计算，未显式建模解剖先验或病理生理学意义，可能在某些非标准对齐情况下误判。
- **泛化性验证有限**：所有数据集均为医学影像分割任务，未验证分类或检测任务上的冲突建模效果。

（完）
