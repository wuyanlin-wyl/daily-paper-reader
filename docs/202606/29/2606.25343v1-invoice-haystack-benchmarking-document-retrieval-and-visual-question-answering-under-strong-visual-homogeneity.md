---
title: "Invoice Haystack: Benchmarking Document Retrieval and Visual Question Answering Under Strong Visual Homogeneity"
title_zh: 发票干草堆：在强视觉同质性下对文档检索与视觉问答进行基准测试
authors: "Heethanjan Kanagalingam, Thenukan Pathmanathan, Mokeeshan Vathanakumar, Basim Azam, Sarah Monazam Erfani"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25343v1"
tags: ["query:vl"]
score: 9.0
evidence: 面向文档检索和VQA的VLM基准测试
tldr: "针对视觉高度相似的文档（如同模板发票）检索困难，论文提出Invoice Haystack基准，含1500张发票和200个问题，平均余弦相似度0.73。提出VL-RAG框架，融合文本与视觉嵌入并进行VLM验证过滤，在Invoice Haystack-500上Recall@1达60.0%，超越现有方法13.5个百分点，同时在异构文档集上也取得提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25343v1/fig-001.webp\", \"caption\": \"Fig. 3: (a) Distribution of question types in Invoice Haystack across eight semantic categories. (b) Spatial positioning of answers within the documents.\", \"page\": 7, \"index\": 1, \"width\": 725, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2606.25343v1/fig-002.webp\", \"caption\": \"Table 4: Ablation study for VL-RAG on Document Haystack-1000, InfoHaystack1000, and Invoice Haystack-1500. Results reported as Recall@1 / Recall@3 / Recall@5. Avg. denotes the mean across all three benchmarks.\", \"page\": 13, \"index\": 2, \"width\": 725, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.25343v1/fig-003.webp\", \"caption\": \"Table 3: VQA accuracy (%) comparison across DocHaystack, InfoHaystack, and Proposed Invoice Haystack benchmarks. Zero-shot results use full document context; entries marked “–” reflect context-window limitations. For V-RAG and VL-RAG, the best accuracy across Top-1/3/5 is reported. Best result per column is in bold.\", \"page\": 13, \"index\": 3, \"width\": 725, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2606.25343v1/fig-004.webp\", \"caption\": \"Fig. 4: Proposed VL-RAG architecture. The approach operates via two parallel streams. The Vision Stream (top) employs SigLIP and OpenCLIP to capture layout and structural features, while the Text Stream (bottom) uses OCR followed by BGELarge to encode semantic content. Similarity scores for all three encoders are averaged (SimAvg) to rank the corpus, and the top-m candidates undergo VLM-based binary verification to produce the final top-k output.\", \"page\": 8, \"index\": 4, \"width\": 725, \"height\": 275}]"
motivation: 现有基准低估了视觉同质文档检索的难度，存在嵌入坍缩问题。
method: 提出VL-RAG，混合检索文本与视觉嵌入，再经VLM验证过滤精确定位文档。
result: "在Invoice Haystack-500上Recall@1达60.0%，比SOTA高13.5%；DocHaystack-1000和InfoHaystack-1000上也分别提升至77.1%和84.5%。"
conclusion: 双流融合策略在同类和异构文档检索上均优于纯文本或纯视觉方法。
---

## 摘要
视觉语言模型在单文档视觉问答上已达到接近人类的性能，但在从大量视觉同质文档集合中检索信息时，其有效性显著下降。现有的多文档基准测试聚合了不同类型的文档，在嵌入空间中造成了人为的分离，这未能反映数千条记录共享相同视觉模板的企业文档库。我们将此识别为嵌入坍塌，并引入了Invoice Haystack基准测试，该基准包含1,500张匿名发票图像和200个具有区分性的问答对，专门设计用于在强视觉同质性下对检索进行压力测试。Invoice Haystack的平均成对余弦相似度为0.73，而现有基准测试中分别为0.38（DocHaystack）和0.31（InfoHaystack），构成了一个根本更具挑战性的检索问题。针对所识别的挑战，我们提出了VL-RAG，一种混合检索增强生成框架，该框架联合利用文本和视觉嵌入以利用两种模态的互补优势，随后通过基于VLM的验证过滤器进行精确文档识别。VL-RAG在Invoice Haystack-500上达到了60.0%的Recall@1，比现有最先进方法高出最多13.5个百分点。它还在DocHaystack-1000（77.1%对比75.2%）和InfoHaystack-1000（84.5%对比80.0%）上显著提升了检索性能，将所提出的双流融合确立为在同质和异质文档集合中均一致优越的检索策略。

## Abstract
Vision Language Models have achieved near-human performance on single-document Visual Question Answering, yet their effectiveness degrades significantly when retrieving information from large collections of visually homogeneous documents. Existing multi-document benchmarks aggregate diverse document types, creating artificial separation in embedding space that does not reflect enterprise document repositories where thousands of records share identical visual templates. We identify this as embedding collapse and introduce Invoice Haystack, a benchmark with 1,500 anonymized invoice images paired with 200 discriminative question-answer pairs, specifically designed to stress-test retrieval under strong visual homogeneity. Invoice Haystack exhibits a mean pairwise cosine similarity of 0.73, compared to 0.38 (DocHaystack) and 0.31 (InfoHaystack) in existing benchmarks, posing a fundamentally more challenging retrieval problem. Addressing the identified challenge, we propose VL-RAG, a hybrid retrieval-augmented generation framework that jointly leverages text and visual embeddings to harness the complementary strengths of both modalities, followed by a VLM-based verification filter for precise document identification. VL-RAG achieves 60.0\% Recall@1 on Invoice Haystack-500, outperforming existing state-of-the-art method by up to an absolute 13.5 percentage points. It further improves retrieval considerably on DocHaystack-1000 (77.1\% vs.\ 75.2\%) and InfoHaystack-1000 (84.5\% vs.\ 80.0\%), establishing the proposed dual-stream fusion as a consistently superior retrieval strategy across both homogeneous and heterogeneous document collections.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **现状**：Vision Language Models（VLM）在单文档视觉问答上已接近人类水平，但在面对大量视觉高度相似的文档（如同一模板的多张发票）时，检索效果急剧下降。
- **问题识别**：现有多文档基准测试（如 DocHaystack、InfoHaystack）使用视觉多样化的文档集合，导致嵌入空间中不同文档天然分离，无法反映企业真实场景——例如数千张发票共享相同模板，视觉特征高度一致。作者将这一现象定义为 **嵌入坍塌（embedding collapse）**。
- **研究意义**：为了填补这一评估空白，论文提出了 **Invoice Haystack** 基准，专门用于衡量在**强视觉同质性**下的文档检索与 VQA 性能，并提出了解决方案 **VL-RAG**。

### 2. 论文提出的方法论
- **核心思想**：在视觉同质文档集合中，纯视觉嵌入和纯文本嵌入都难以单独奏效；通过融合两者的互补优势，并引入 VLM 验证过滤，可以实现精准检索。
- **关键技术细节**：
  - **双流架构**：
    - **视觉流**：使用 SigLIP 和 OpenCLIP 编码器提取文档的布局与结构特征，分别计算查询-文档的视觉相似度 Sim_S 和 Sim_O。
    - **文本流**：先用 OCR（DeepSeek OCR）提取文档文本，再用 BGE-Large 密集文本编码器编码，计算文本相似度 Sim_T。
  - **分数融合**：对上述三个相似度取均值得到 `Sim_Avg = (Sim_T + Sim_S + Sim_O)/3`，依此排序并选取 top-m 候选文档。
  - **VLM 验证过滤器**：将 top-m 候选文档与查询输入 Qwen3-VL 进行二元验证（"Can this image provide the answer?"），仅保留回答 "yes" 的文档；若无一通过则返回原始 top-m。
- **算法流程（文字说明）**：给定查询 q → 分别用 OCR + BGE 提取文本嵌入、SigLIP + OpenCLIP 提取视觉嵌入 → 计算三个余弦相似度 → 平均融合排序 → 取 top-m → 对每个候选执行 VLM 验证 → 输出最终 top-k。

### 3. 实验设计
- **数据集 / 场景**：
  - 主要基准：**Invoice Haystack**（1500 张匿名发票，200 个问答对，平均余弦相似度 0.73）。
  - 对比基准：**DocHaystack**（1000 份混合文档，相似度 0.38）和 **InfoHaystack**（1000 份信息图表，相似度 0.31）。每个基准评估了多种规模（500/1000/1500 文档）。
- **对比方法**：
  - 稀疏文本基线：BM25（基于 OCR 文本）。
  - 纯视觉基线：CLIP、SigLIP、OpenCLIP、Jina-CLIP-v2、Nomic-Embed-Vision。
  - 视觉混合基线：V-RAG（最先进的视觉检索增强生成方法，使用 CLIP + SigLIP + OpenCLIP 集成和 VLM 过滤）。
  - **本文方法**：VL-RAG（SigLIP + OpenCLIP + BGE + VLM 过滤）。
- **评估指标**：Recall@1、Recall@3、Recall@5；以及利用 4 种 VLM（Gemini-3-Flash、GPT-5.2、InternVL3-8B、Qwen3-VL-8B）进行的 VQA 准确率。

### 4. 资源与算力
- **明确说明**：所有推理在 **NVIDIA A100 (80GB) GPU** 上执行。未提及具体 GPU 数量、训练时长，因为模型均以冻结的零样本方式使用（不需训练）。仅推理阶段消耗算力。

### 5. 实验数量与充分性
- **实验数量**：包括 9 个检索设置（3 基准 × 3 规模）的 Recall@1/3/5 对比，4 种 VLM 的 VQA 准确率对比（零样本、V-RAG、VL-RAG 三种模式），以及系统的消融实验（10 种编码器组合 + VLM 滤波器开关）和 top-k 深度分析（k=1,3,5）。
- **充分性与公平性**：
  - 所有方法在统一管线中评估（相同 VLM 组件、相同推理配置）。
  - 消融实验覆盖了所有关键组件（单个编码器、无 CLIP 组合、无 VLM 过滤器等），证实了每部分的贡献。
  - 对比方法包括最先进的 V-RAG，并采用相同的 Qwen3-VL 后处理，控制变量。
  - 实验规模充分，结论可靠；但仅进行了零样本推理，未探索微调场景。

### 6. 论文的主要结论与发现
- **Invoice Haystack 更难**：其平均余弦相似度 0.73，远超现有基准（0.31-0.38），纯视觉方法 Recall@1 低至 7%（CLIP）至 20%（SigLIP），V-RAG 也仅 40%。
- **VL-RAG 显著领先**：在 Invoice Haystack-1500 上 Recall@1 达 50.0%，比 V-RAG 高 10 个百分点；在 Doc/InfoHaystack 上也有持续提升（+1.9~4.5 百分点）。
- **双流融合优于单一模态**：文本嵌入（BGE 单独 31.5%）优于所有单视觉编码器；融合后效果最佳。
- **CLIP 引入噪声**：去掉 CLIP 后性能反而提升（No-CLIP 组合的 Recall@1 平均 70.5% vs 包含 CLIP 的 69.4%），表明 CLIP 的自然图像预训练不适用于文档检索。
- **VLM 过滤器关键**：在 Invoice Haystack-1500 上从 35% 提升至 50%（Recall@1），对精度改善显著。

### 7. 优点
- **基准设计的针对性**：Invoice Haystack 填补了视觉同质性评估空白，可复现且难度高（余弦相似度 0.73），能真正区分不同方法的能力。
- **方法简洁有效**：VL-RAG 仅通过简单的平均融合和 VLM 过滤即实现大幅提升，实用性高，易于部署。
- **消融实验深刻**：发现并验证了 CLIP 在文档检索中的反直觉劣势（No-CLIP 更好），为未来工作提供了重要启示。
- **实验全面客观**：覆盖多种规模、多种检索指标和 VLM 后端，并与强基线 V-RAG 进行了公平对比。

### 8. 不足与局限
- **固定融合权重**：文本与视觉相似度采用等权平均，未根据文档同质性动态调整，可能非最优。
- **零样本局限**：所有编码器均为冻结的通用预训练模型，未针对发票或文档域进行微调，可能限制了上限。
- **基准覆盖有限**：仅针对发票，虽具代表性但偏窄；论文自身提到未来需扩展至合同、采购订单等。
- **VLM 过滤器成本**：每个查询需要对 top-m 候选逐一推理 VLM，增加了延迟和计算开销。
- **未分析训练资源**：未提及模型训练所需的算力或时间（因为采用零样本），但对微调场景的估计缺失。
- **潜在偏差风险**：发票数据集仅包含 200 个问答对，样本量相对较小，且来源于特定 OCR/VLM 生成，可能引入标注偏差。

（完）
