---
title: "C2RM-Seg: Causal Counterfactual Reasoning with Structural-Semantic Priors for Weakly Supervised Histopathological Tissue Segmentation"
title_zh: "C²RM-Seg: 基于结构语义先验的因果反事实推理用于弱监督组织病理组织分割"
authors: "Hualong Zhang, Siyang Feng, Zihan Huan, Yi Qian, Zhenbing Liu, Rushi Lan, Xipeng Pan"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25508v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 弱监督组织病理学组织分割
tldr: 针对弱监督组织病理分割中CAM生成噪声伪标签的问题，提出C2RM-Seg两阶段框架：分类阶段通过因果反事实推理模块抑制混淆上下文、生成形态对齐CAM；分割阶段采用双路径结构-语义架构，结合ResNeSt细粒度特征与DINOV3语义先验，并设计不确定性门控边缘损失进一步抑制噪声。在两个公共数据集上达到最优性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25508v1/fig-001.webp\", \"caption\": \"Fig. 3: t-SNE visualization. (a) Baseline features are severely entangled by spurious staining confounders. (b) C2RM eliminates these biases via counterfactual intervention, yielding compact clusters and distinct inter-class separability.\", \"page\": 8, \"index\": 1, \"width\": 640, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-002.webp\", \"caption\": \"Table 2: Ablation study of main modules on LUAD-HistoSeg.\", \"page\": 7, \"index\": 2, \"width\": 702, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-003.webp\", \"caption\": \"Table 1: Quantitative comparison on the BCSS-WSSS and LUAD-HistoSeg datasets. The best and second-best results are highlighted in bold and underlined, respectively. The reported standard deviations are computed over five independent runs.\", \"page\": 6, \"index\": 3, \"width\": 714, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-004.webp\", \"caption\": \"Fig. 1: Overview of C2RM-Seg. (a) Pseudo-Mask Generation: The C2RM rectifies CAMs by performing counterfactual intervention Zcf = Z −E on latent factors Z using a learnable causal matrix A. (b) Segmentation Training: A Dual-Path Structural-Semantic Architecture synergizes ResNeSt [23] features with frozen DINOV3 [24] priors via cross-path gating. The model is optimized using a UGM loss that dynamically re-weights supervision based on prediction uncertainty to mitigate label noise.\", \"page\": 3, \"index\": 4, \"width\": 727, \"height\": 326}]"
motivation: 现有CAM方法关注染色外观而非因果形态，导致虚假定位和结构不一致。
method: 提出C2RM-Seg，包括因果反事实推理模块、双路径结构-语义架构和不确定性门控边缘损失。
result: 在两个公共组织病理学数据集上达到最先进分割性能。
conclusion: 因果反事实推理与结构语义先验能有效提升弱监督组织分割的准确性和一致性。
---

## 摘要
组织病理组织分割对于计算机辅助诊断至关重要，然而弱监督方法常常受到由类激活映射（CAM）生成的嘈杂伪标签的困扰。现有的CAM方法倾向于关注染色驱动的外观线索而非真实的因果组织形态，导致定位虚假且缺乏结构一致性。为解决这一问题，我们提出C²RM-Seg，一个两阶段框架，融合了因果伪标签精炼与结构感知语义增强。在分类阶段，我们引入因果反事实推理模块（C²RM），该模块将特征分解为潜在因子，并通过学习到的因果结构矩阵执行反事实干预，从而抑制混淆上下文，生成符合形态的CAM。在分割阶段，我们设计了一个双路径结构语义架构，将来自ResNeSt的细粒度结构特征与来自冻结的DINOV3基础模型的全局语义先验相结合。一种跨路径门控机制利用局部结构线索自适应地调节语义注入以保持边界保真度。为进一步减轻残差伪标签噪声，我们提出不确定性门控边界（UGM）损失，该损失基于预测不确定性动态平衡边界强制与置信度学习。在两个公开的组织病理组织数据集上进行的大量实验表明，C²RM-Seg实现了最先进的性能。

## Abstract
Histopathological tissue segmentation is essential for computer-aided diagnosis, yet weakly supervised methods often suffer from noisy pseudo-labels generated by Class Activation Mapping (CAM). Existing CAM approaches tend to focus on staining-driven appearance cues rather than true causal tissue morphology, resulting in spurious localization and poor structural consistency. To address this issue, we propose C$^2$RM-Seg, a two-stage framework that integrates causal pseudo-label refinement with structure-aware semantic enhancement. For classification, we introduce a Causal Counterfactual Reasoning Module (C$^2$RM) that decomposes features into latent factors and performs counterfactual intervention via a learned causal structure matrix, suppressing confounding context and producing morphology-aligned CAMs. For segmentation, we design a Dual-Path Structural-Semantic Architecture that combines fine-grained structural features from ResNeSt with global semantic priors from a frozen DINOV3 foundation model. A cross-path gating mechanism adaptively regulates semantic injection using local structural cues to preserve boundary fidelity. To further mitigate residual pseudo-label noise, we propose an Uncertainty-Gated Margin (UGM) loss, which dynamically balances margin enforcement and confidence learning based on prediction uncertainty. Extensive experiments on two public histopathological tissue datasets show that C$^2$RM-Seg achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
弱监督组织病理组织分割中，主流方法依赖于类激活映射（CAM）生成伪标签。然而，现有CAM方法容易受到染色变化、背景纹理等混淆因素的影响，倾向于关注染色驱动的外观线索而非真正的因果组织形态，导致伪标签存在系统性偏差、定位不准确且缺乏结构一致性。这种因果错位削弱了后继分割模型的性能。

### 2. 方法论
**整体框架**：C²RM-Seg采用两阶段流水线：
- **第一阶段（伪标签生成）**：利用因果反事实推理模块（C²RM）纠正CAM。
- **第二阶段（分割训练）**：使用双路径结构-语义架构融合局部形态与全局语义先验，并通过不确定性门控边缘损失（UGM）进行噪声容错优化。

**核心技术细节**：
- **C²RM**：基于结构因果模型（SCM），将编码器特征分解为K个潜在因子（通过通道投影和平均池化），学习一个有向因子图邻接矩阵A（对角掩码后行softmax归一化）。对每个因子，估计来自其他因子的混淆贡献E_i = Σ_{j≠i} A_{i,j} z_j。执行反事实干预：z^{cf}_i ≈ z_i - E_i，即Z^{cf} = Z - AZ。最后通过门控残差修正将Z^{cf}注入空间特征。
- **双路径结构-语义架构**：结构分支使用ResNeSt-200e提取多尺度空间特征F_S；语义分支使用冻结的DINOV3 ViT-B/16编码器+可学习适配器输出全局语义F_V。跨路径门控根据结构特征生成空间门控图G=σ(Ψ(F_S))，语义信息按门控加权注入：F̂ = F_S + G⊙F_V。
- **不确定性门控边缘损失（UGM）**：对每个像素i，定义边缘违背项M_i = [m - (p_{i,y} - p_{i,s})]_+ 和置信度惩罚C_i = -log(p_{i,y}+ϵ)。基于局部不确定性u_i=1-p_{i,y}，使用Sigmoid动态权重α_i = 1/(1+e^{-κ(u_i-0.5)})，损失L = (1/|Ω|) Σ_i (α_i C_i + (1-α_i) M_i)。低不确定性时侧重边界扩展，高不确定性时侧重置信度学习。

### 3. 实验设计
- **数据集**：两个公共组织病理数据集：
  - BCSS-WSSS（乳腺癌）：151张WSI，含TUM、STR、LYM、NEC四类组织，23,422训练patch（图像级标签），8,404验证/测试patch（密集像素标注）。
  - LUAD-HistoSeg（肺腺癌）：54张WSI，含TE、TAS、NEC、LYM四类组织，16,678训练patch，607验证/测试patch。
- **基准方法**：包括全监督SAM/SAM2/MedSAM、弱监督TPRO、HAMIL、MLPS、SIPE、ARML、OEEM、ESFAN等SOTA方法。对比方法均使用统一设置（基础模型用第一阶段的伪掩码提示）。
- **评价指标**：mIoU、bIoU（边界IoU）、HD95（95% Hausdorff距离）。

### 4. 资源与算力
文中未明确给出完整的训练时长和GPU数量，但指出实验在单张NVIDIA RTX 3090上使用PyTorch进行。分类阶段：ResNet38d，K=8，训练20个epoch，batch size 20，PolyOptimizer。分割阶段：ResNeSt-200e（SGD，lr=1e-2）+冻结DINOV3-Base + 适配器（AdamW，lr=5e-3）。总GFLOPs为191.47，FPS为86.08。未报告精确的训练时间。

### 5. 实验数量与充分性
- 做了三组主要实验：
  1. **定量对比**（表1）：在BCSS-WSSS和LUAD-HistoSeg上对比12种方法，报告均值和标准差（五次运行），结果清晰。
  2. **消融研究**（表2）：在LUAD-HistoSeg上分别消融C²RM、双路径架构、UGM损失，展示每个模块独立提升以及组合的增益。
  3. **定性可视化**（图2）：展示各类方法的分割结果对比，以及t-SNE特征可视化（图3）说明C²RM的去混效果。
- 实验覆盖了两个不同癌症类型的数据集，对比了最近数个弱监督方法，消融设计全面，充分检验了各组件的互补性。实验设计较为客观（五次重复、报告标准差）。

### 6. 主要结论
- 识别出CAM伪标签的染色混淆偏差是弱监督组织分割的主要问题，C²RM通过因果反事实干预有效去除混淆，生成形态对齐的伪标签。
- 提出的双路径结构-语义架构和UGM损失进一步提升了分割精度和边界质量。
- 在BCSS-WSSS和LUAD-HistoSeg上均达到SOTA：mIoU分别为72.17%和79.62%，bIoU和HD95也优于现有方法。
- 计算效率可接受（191.47 GFLOPs, 86.08 FPS），具备实际部署潜力。

### 7. 优点
- **因果建模创新**：将CAM生成纳入SCM框架，首次在弱监督组织分割中显式处理染色混淆，方法具有理论深度。
- **基础模型融合策略**：使用冻结的DINOV3提供强语义先验，通过结构门控自适应融合，避免过拟合同时保留泛化能力。
- **噪声鲁棒损失**：UGM损失根据预测不确定性动态调整监督策略，有效抑制伪标签噪声，是轻量且有效的技巧。
- **实验严谨性**：五轮随机重复、报告标准差、覆盖多个SOTA方法、消融分析完整。

### 8. 不足与局限
- **两阶段训练负担**：仍需分别训练分类和分割模型，端到端性不足。
- **冻结基础模型限制**：DINOV3被完全冻结，可能无法充分适应组织病理学领域的特定分布，适配器能力有限。
- **数据集覆盖有限**：仅验证了两个数据集（肺腺癌和乳腺癌），未在更多癌种或更大规模数据集（如TCGA全切片）上评估，泛化性有待验证。
- **超参数依赖**：K=8、m=0.3、κ=5等超参数需手动设置，缺乏敏感性分析。
- **计算资源描述不完整**：未报告确切训练时间，不利于可重复性评估。

（完）
