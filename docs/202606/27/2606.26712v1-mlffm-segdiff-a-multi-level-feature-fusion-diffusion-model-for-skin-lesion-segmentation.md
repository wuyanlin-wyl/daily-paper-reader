---
title: "MLFFM-SegDiff: A Multi-Level Feature Fusion Diffusion Model for Skin Lesion Segmentation"
title_zh: MLFFM-SegDiff：一种用于皮肤病变分割的多层级特征融合扩散模型
authors: "Jingjun Gu, Chaojie Shen, Yifeng Cao, Wei Zhang, Yiliu Li, Aobo Fan"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26712v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于皮肤病变分割的创新多级特征融合扩散模型
tldr: 针对皮肤病变分割中边界模糊、形状多变等挑战，基于扩散模型提出MLFFM-SegDiff，通过双路径编码器、多级特征融合模块和边界敏感损失函数，增强跨层特征交互与细节恢复，在ISIC2018、PH2和HAM10000上超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26712v1/fig-001.webp\", \"caption\": \"Figure 2: Visual comparison of segmentation results on three datasets. The white contours represent model predictions, while the red contours represent ground-truth lesion boundaries.\", \"page\": 17, \"index\": 1, \"width\": 814, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2606.26712v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison on ISIC2018, PH2, and HAM10000. Results are reported as mean ± standard deviation.\", \"page\": 15, \"index\": 2, \"width\": 814, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2606.26712v1/fig-003.webp\", \"caption\": \"Table 3: Ablation study of the proposed modules on three validation sets.\", \"page\": 16, \"index\": 3, \"width\": 814, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2606.26712v1/fig-004.webp\", \"caption\": \"Figure 1: Overall architecture of MLFFM-SegDiff. The noisy mask and dermoscopic image are encoded by a dual-path encoder, where enhanced skip features are fused by MLFFM and direct skip features are preserved for the decoder. The bottom legend summarizes the arrow semantics.\", \"page\": 8, \"index\": 4, \"width\": 810, \"height\": 389}]"
motivation: 现有扩散分割方法存在跨层特征交互有限、边界细节恢复不足的问题。
method: 提出MLFFM-SegDiff，包含双路径U-Net编码器、多级特征融合模块（MLFFM）和边界敏感损失函数。
result: 在ISIC2018、PH2和HAM10000上取得平均Jaccard指数0.8546和Dice系数0.9207。
conclusion: 多级特征融合策略有效提升了皮肤病变分割性能。
---

## 摘要
皮肤病变分割是计算机辅助皮肤病诊断中的关键任务，其准确性直接影响下游分析和疾病分类。然而，由于边界模糊、对比度低、形状变化大以及毛发和阴影等伪影，皮肤镜图像具有挑战性。近年来，扩散模型凭借其渐进去噪和分布建模能力，在医学图像分割中表现出色。然而，现有的基于扩散的方法仍存在跨层级特征交互不足和边界细节恢复不充分的问题。为解决这些问题，我们提出了MLFFM-SegDiff，一种用于皮肤病变分割的多层级特征融合扩散模型。该方法基于扩散框架，引入了双路径U-Net编码器、多层级特征融合模块（MLFFM）和边界敏感损失函数。双路径编码器增强了噪声掩膜特征与皮肤镜图像特征之间的交互。MLFFM通过注意力、尺度对齐和自适应跨层级融合改进了跳跃连接。这些设计使得解码器能够联合利用浅层边界线索和深层语义表示，提高掩膜重建质量。在ISIC2018、PH2和HAM10000上的实验表明，MLFFM-SegDiff在准确率、F1分数、Jaccard指数、召回率和Dice系数上均优于代表性方法，包括DermoSegDiff、U-Net和SwinUNETR。特别是，它获得了平均Jaccard指数0.8546和Dice系数0.9207。这些结果验证了所提出的多层级特征融合策略在提高病变分割性能方面的有效性。代码将在发表后于https://github.com/Qacket/MLFFM-SegDiff.git发布。

## Abstract
Skin lesion segmentation is a key task in computer-aided dermatological diagnosis, where accuracy directly impacts downstream analysis and disease classification. However, dermoscopic images are challenging due to blurred boundaries, low contrast, large shape variations, and artifacts such as hair and shadows. Recently, diffusion models have shown strong performance in medical image segmentation thanks to their progressive denoising and distribution modeling capabilities. Nevertheless, existing diffusion-based methods still suffer from limited cross-level feature interaction and insufficient boundary detail recovery.   To address these issues, we propose MLFFM-SegDiff, a multi-level feature fusion diffusion model for skin lesion segmentation. Built on a diffusion framework, the method introduces a dual-path U-Net encoder, a Multi-Level Feature Fusion Module (MLFFM), and a boundary-sensitive loss function. The dual-path encoder enhances interaction between noisy mask features and dermoscopic image features. MLFFM improves skip connections via attention, scale alignment, and adaptive cross-level fusion. These designs enable the decoder to jointly leverage shallow boundary cues and deep semantic representations, improving mask reconstruction quality.   Experiments on ISIC2018, PH2, and HAM10000 demonstrate that MLFFM-SegDiff outperforms representative methods including DermoSegDiff, U-Net, and SwinUNETR across Accuracy, F1-score, Jaccard index, Recall, and Dice. In particular, it achieves an average Jaccard index of 0.8546 and Dice coefficient of 0.9207. These results validate the effectiveness of the proposed multi-level feature fusion strategy for improving lesion segmentation performance. The code will be released at https://github.com/Qacket/MLFFM-SegDiff.git after publication.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：皮肤病变分割是计算机辅助皮肤病诊断的基础任务，但皮肤镜图像存在边界模糊、对比度低、形状和大小差异大、受毛发和阴影等伪影干扰，导致精确分割极具挑战。
- **现有方法局限**：基于CNN（如U-Net）和Transformer的方法在跨层特征交互和边界细节恢复上仍有不足；扩散模型在分割中展现了潜力（如DermoSegDiff），但仍存在**跨层级特征交互有限**、**边界细节恢复不充分**的问题。
- **本文目标**：提出MLFFM-SegDiff，通过改进扩散模型的去噪网络，增强多级特征融合和边界感知能力，以提升皮肤病变分割的准确性和区域重叠质量。

### 2. 方法论：核心思想与关键技术细节
- **总体框架**：基于扩散分割框架，将皮肤镜图像作为条件，通过逆向扩散过程逐步恢复病变掩膜。
- **关键改进**：
  1. **双路径U-Net编码器**：并行处理噪声掩膜特征和图像引导特征，通过残差块、通道拼接、调制反馈和线性注意力实现交互，输出两类跳连特征：直接跳连（`h_i`）和增强跳连（`b_i`）。
  2. **多层级特征融合模块（MLFFM）**：对各层级增强跳连特征依次进行空间注意力、通道注意力增强、尺度对齐，再通过逐元素乘性融合生成融合跳连特征（`s_i`），使解码器每个阶段同时获取浅层边界细节和深层语义。
  3. **边界敏感损失函数**：在标准去噪MSE损失基础上，加入可配置的边界加权项 `λ_b γ(t) W_Θ(x0)`，其中 `γ(t)=γ0 e^{-μt}` 为时间衰减系数，`W_Θ(x0)` 根据像素到病变轮廓的距离赋予更高权重，引导模型关注模糊边界区域。
- **训练流程**：前向过程按线性噪声调度添加高斯噪声，逆向过程中网络预测噪声 `ϵ̂_θ`；总损失为加权的像素级MSE。

### 3. 实验设计
- **数据集**：三个公开皮肤病变数据集：ISIC2018、PH2、HAM10000。所有图像和掩膜统一resize至128×128。
- **评估协议**：五折交叉验证，取均值报告。
- **对比方法**：DermoSegDiff、Generic U-Net、SwinUNETR、U-Net。
- **评价指标**：Accuracy、F1-score、Jaccard index、Precision、Recall、Specificity、Dice coefficient。
- **消融研究**：分别测试双路径编码器（D-U-Net）、MLFFM、边界损失（Edge Loss）三个组件的独立和组合效果，在三个数据集上记录Dice和敏感度（Sensitivity）。
- **可视化**：给出了代表性分割结果的轮廓对比图（预测白色，真值红色）。

### 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、单卡训练时长等具体算力信息。仅提到实现基于PyTorch，使用Adam优化器与ReduceLROnPlateau调度器，数据集特定的epoch和batch size按数据规模选择。

### 5. 实验数量与充分性
- **数量**：主要实验包括：
  - 三个数据集上五种方法的完整定量对比（表1）；
  - 平均性能汇总（表2）；
  - 消融实验（表3）：三个组件在三个数据集上共9种组合（每个数据集5种配置，共15组结果）；
  - 可视化比较（图2）。
- **充分性评价**：实验设计规范，采用交叉验证，消融分析覆盖了各组件贡献。但**缺少与更多最新扩散分割方法（如MedSegDiff、SegDiff）的直接对比**，且未在大尺寸图像或更大规模（如ISIC2020）上验证。总体而言实验较充分，但泛化性检验不足。

### 6. 主要结论与发现
- MLFFM-SegDiff在**平均Accuracy、F1-score、Jaccard index、Recall、Dice系数**上均优于所有对比方法。
- 在三数据集平均性能上：**Jaccard 0.8546，Dice 0.9207**。
- 消融实验表明双路径编码器、MLFFM和边界损失在大多数设定下具有互补作用，尤其在召回率（Recall）提升显著，说明模型对病变区域的覆盖更完整。
- 在ISIC2018上，Precision和Specificity并非最高，表明模型在扩大覆盖面时可能引入少量误检（背景抑制未达最优）。

### 7. 优点
- **创新性**：首次将多级特征融合模块（MLFFM）系统性地引入扩散分割网络，实现跨尺度边界-语义联合利用。
- **结构设计清晰**：双路径编码增强了噪声掩膜与图像引导特征的交互；MLFFM通过注意力、对齐和乘性融合有效整合多级跳连信息。
- **可配置性**：边界损失作为可选组件，可根据数据集特点启用，提高灵活性。
- **实验规范**：采用五折交叉验证，消融实验证实各模块有效性，结果统计报告均值±标准差，可信度高。

### 8. 不足与局限
- **性能权衡**：在ISIC2018上Recall高但Precision和Specificity略低，表明背景/伪影抑制能力有提升空间。
- **计算开销**：扩散模型推断需多次迭代去噪，比前馈网络（如U-Net）效率低，文中未提供推理时间或浮点运算量对比。
- **对比方法覆盖不全面**：未与MedSegDiff等近年主流扩散分割模型比较，也未与更强Transformer基线（如TransUNet）对比。
- **数据集限制**：仅使用128×128低分辨率输入，未验证高分辨率图像效果；且只针对皮肤病变分割，未在其它医学图像任务（如眼底、细胞）上测试泛化性。
- **消融实验细节**：边界损失对ISIC2018 Dice略有下降，作者解释为更激进的覆盖策略，但未深入分析原因。

（完）
