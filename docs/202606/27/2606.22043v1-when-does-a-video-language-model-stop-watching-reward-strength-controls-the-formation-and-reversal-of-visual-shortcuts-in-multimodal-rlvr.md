---
title: When Does a Video-Language Model Stop Watching? Reward Strength Controls the Formation and Reversal of Visual Shortcuts in Multimodal RLVR
title_zh: 视频语言模型何时停止观看？奖励强度控制多模态RLVR中视觉捷径的形成与逆转
authors: Zekun Xu
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22043v1"
tags: ["query:vl"]
score: 9.0
evidence: 研究视频语言模型在RLVR中视觉捷径的形成与逆转
tldr: 本研究针对多模态强化学习（RLVR）中视觉捷径的形成与逆转机制，通过调节接地惩罚强度λ，发现捷径突现、剂量反应迟滞以及关键干预窗口，揭示了该过程的可控性、时间依赖性和不对称性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22043v1/fig-001.webp\", \"caption\": \"Figure 2: Dose–response and formation–reversal asymmetry. Increasing the grounding penalty λ monotonically lowers the shortcut plateau (right). At the intermediate dose, the trajectory forms and then reverses the shortcut (left), exposing an asymmetry between acquiring and removing it.\", \"page\": 6, \"index\": 1, \"width\": 932, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2606.22043v1/fig-002.webp\", \"caption\": \"Figure 3: A critical intervention window. The same grounding penalty suppresses the shortcut when applied before onset but is much less able to reverse it once the shortcut has consolidated, demonstrating time-dependent intervenability.\", \"page\": 7, \"index\": 2, \"width\": 932, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2606.22043v1/fig-003.webp\", \"caption\": \"Table 1: Trajectory fleet. All runs share a common origin; each varies a single factor. VHS is read on the OOD diagnostic set. “Onset region” denotes the step window over which VHS rises sharply for the λ=0 runs.\", \"page\": 3, \"index\": 3, \"width\": 914, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2606.22043v1/fig-004.webp\", \"caption\": \"Figure 4: A preliminary, layer-localized representation signature. Internal representation statistics of matched checkpoints, contrasting λ=0, 1, 2. The observed dose-dependent tendency is concentrated in the middle layers and near-zero at early and final layers; we report it as an exploratory, directional finding (the mid-layer spread is within bootstrap variability at the current sample size) that motivates a more careful mechanistic study.\", \"page\": 8, \"index\": 4, \"width\": 932, \"height\": 744}, {\"url\": \"assets/figures/arxiv/2606.22043v1/fig-005.webp\", \"caption\": \"Figure 1: Onset is real and seed-robust. On held-out OOD data, two independent seeds exhibit overlapping, sharply rising VHS curves over the same narrow step window, indicating an emergent visual-shortcut transition rather than a memorization artifact.\", \"page\": 4, \"index\": 5, \"width\": 832, \"height\": 499}]"
motivation: 探究结果导向优化中视觉捷径的形成机制、可逆性以及干预时机。
method: 将接地惩罚强度λ作为控制旋钮，在训练时间轴上表征视觉捷径的形成-逆转动态。
result: 视觉捷径具有突现性、单调剂量反应（中等剂量下先形成后逆转）和关键干预窗口（惩罚在形成前应用更有效）。
conclusion: 视觉捷径塌陷不是二元缺陷，而是可控、时间依赖且不对称的过程，需根据时机和强度合理正则化。
---

## 摘要
使用可验证奖励的强化学习（RLVR）正越来越多地应用于大型视觉-语言模型（LVLMs），然而仅基于结果的优化可能导致模型停止关注视频，转而利用语言先验——我们将这种失败称为视觉捷径。虽然这种感知绕行的存在已有文献记载，但它如何形成、能否被撤销以及干预何时仍有效仍悬而未决。我们将接地惩罚的强度λ视为控制旋钮，并沿着训练时间轴刻画视觉捷径的形成-逆转动态。在一个留出的、分布外的诊断集上，我们发现：（i）突然发作——捷径依赖在优化步骤的一个狭窄窗口内突然出现，并且在不同随机种子下均稳健；（ii）单调剂量反应——增加λ逐步抑制捷径，在中等剂量下，轨迹先形成然后逆转捷径，暴露出获得和消除之间的类滞后不对称性；（iii）关键干预窗口——在发作前施加惩罚可阻止捷径形成，而在巩固后施加相同惩罚的效果明显较差。这些结果共同将视觉捷径崩塌重新定义为并非一个二元缺陷，而是一个可控的、时间依赖且不对称的过程，对何时以及以何种强度正则化多模态RLVR具有直接启示。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) is increasingly applied to large vision-language models (LVLMs), yet outcome-only optimization can drive a model to stop attending to the video and instead exploit linguistic priors -- a failure we call a visual shortcut. While the existence of such perception bypass is by now documented, how it forms, whether it can be undone, and when intervention still helps remain open. We treat the strength of a grounding penalty, lambda, as a control knob and characterize the formation-reversal dynamics of visual shortcuts along the training time axis. On a held-out, out-of-distribution diagnostic set, we find: (i) a sharp onset -- shortcut reliance emerges abruptly over a narrow window of optimization steps and is robust across random seeds; (ii) a monotone dose-response -- increasing lambda progressively suppresses the shortcut, and at an intermediate dose the trajectory first forms and then reverses the shortcut, exposing a hysteresis-like asymmetry between acquiring and removing it; and (iii) a critical intervention window -- applying the penalty before onset arrests shortcut formation, whereas the same penalty applied after consolidation is markedly less effective. Together these results recast visual-shortcut collapse not as a binary defect but as a controllable, time-dependent, and asymmetric process, with direct implications for when and how strongly to regularize multimodal RLVR.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在多模态强化学习（RLVR）中，仅优化最终答案奖励（outcome-only optimization）会诱导大型视觉-语言模型（LVLM）忽视视觉输入，转而利用语言先验和表面统计信息来回答——作者称之为**视觉捷径**。这种“感知绕行”（perception bypass）现象虽已被记录，但其**形成过程**（如何随时间涌现）、**可逆性**（能否被撤销）以及**干预时机**是否影响效果，尚缺乏深入刻画。
- **意义**：将视觉捷径重新定义为**可控的、时间依赖的、不对称的过程**，而非简单的二元缺陷。通过将接地惩罚强度λ作为控制变量，揭示出捷径的突现、剂量反应、形成-逆转不对称性以及关键干预窗口，为多模态RLVR的正则化策略提供直接指导（应早期且剂量校准地施加惩罚）。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将接地惩罚强度 λ 作为实验控制旋钮，沿训练时间轴密集读取捷径依赖程度（通过扰动诊断），从而回答形成、逆转和时机问题。
- **关键技术细节**：
  - **奖励函数设计**：
    - 基础奖励：`r_base = (1-w)·a + w·f`，其中 a ∈ {0,1}为答案正确性，f ∈ {0,1}检查答案格式是否正确，w=0.1。
    - 接地惩罚项：`r = r_base - λ·h`，其中 `h = a · 1(y_hat_pert == y_hat_clean)`；h仅在答案正确且对扰动不变时触发（即未使用视频时间内容就答对）。
  - **视觉捷径诊断（VHS）**：在留出的OOD诊断集上，对每个问题在干净视频和**帧顺序随机打乱**的扰动视频上生成答案。VHS = 答案不变的比例，越高表示捷径依赖越强。五选一情况下随机回答的VHS≈0.2。
  - **训练算法**：采用GRPO风格的RLVR（基于DeepSeekMath的GRPO），batch size为512。
  - **轨迹舰队**：多个运行共享同一初始权重，每次仅改变一个因子（见表1）：E1（λ=0，种子A，500步，作为发作基线）、sB（λ=0，种子B，80步，种子稳健性对照）、L1（λ=1，150步，中等剂量）、L2（λ=2，150步，强剂量）、干预运行（从E1的前、中、后检查点恢复，用λ=1训练50步）。
- **算法流程（文字描述）**：
  1. 使用GRPO对LVLM进行post-train，任务为视频问答。
  2. 在每个步骤，基于当前策略对每个训练样本执行干净和扰动视频的两次生成（on-policy）。
  3. 根据公式计算奖励（包含基础项和接地惩罚项）。
  4. 在留出的OOD诊断集上，定期（按检查点）计算VHS、准确率和h率。
  5. 通过改变λ并追踪VHS轨迹，分析捷径突然发作、剂量反应、形成-逆转不对称性及干预窗口。

## 3. 实验设计

- **数据集**：
  - 训练数据：视频问答任务，具体来源文中未详细说明（使用NExT-QA分布，但训练集未明确命名）。
  - 诊断集：从NExT-QA视频QA分布构建的**640个五选一选择题**，与训练集**完全不同**，且为**时间接地问题**（关于动作/事件顺序），用作OOD评估。
  - 表示探针数据：使用MVBench（标准视频理解基准），仅作为输入视频源提取激活，与VHS诊断集分开。
- **基准（Benchmark）**：无外部基准比较。本文主要进行内部对比研究：不同λ值（0, 1, 2）之间的VHS轨迹、不同种子（A vs B）、干预时机（前/中/后）。
- **对比方法**：无其他方法。本文是机制分析性研究，通过控制变量（λ、种子、干预时机）来揭示捷径动态。

## 4. 资源与算力

- **模型**：Qwen3-VL-8B-Instruct（8B参数，36个解码器层，隐藏大小4096）。
- **训练设置**：GRPO RLVR，全局batch size为512，冻结视觉塔（vision tower），视觉输入管道固定。
- **GPU算力**：文中**未明确说明使用的GPU型号、数量和训练总时长**。仅提到batch size 512，可能暗示使用多GPU训练，但具体算力信息缺失。
- **其他**：表示探针提取使用MVBench，固定层集合（每4层取一个），但未提及额外计算资源。

## 5. 实验数量与充分性

- **实验组数**：
  - 主要轨迹：E1（λ=0, 500步）、sB（λ=0, 种子B, 80步）、L1（λ=1, 150步）、L2（λ=2, 150步）。
  - 干预运行：3组（前、中、后），每组50步。
  - 总计至少6组主要运行，加上表示探针的额外分析（但对探针评估的样本量较小）。
- **充分性评估**：
  - **充分**：覆盖了不同的λ值（0,1,2）、两个随机种子、三种干预时机，满足了“一次仅变一个因子”的严格阅读规则，避免了跨因子比较的混淆。
  - **客观与公平**：所有诊断使用贪心解码、相同评估配置；VHS在OOD诊断集上测量，避免了记忆训练样本；种子稳健性验证了现象的可靠性。
  - **不足**：
    - 仅使用一个模型家族（Qwen3-VL-8B），未探索其他规模或架构。
    - 仅使用一种接地惩罚设计（基于时间扰动不变性）。
    - 表示探针分析：样本量较小，结果在bootstrap变异性范围内，仅作为探索性发现报道，缺乏统计显著性。
    - 未进行双向剂量扫描（λ升/降环路），因此“滞后”仅指时间轴上的形成-逆转不对称，而非真正的滞后循环。

## 6. 主要结论与发现

1. **发作是真实的且对种子稳健**：在OOD数据上，VHS在优化步骤的一个窄窗口（约16-24步）内急剧上升，两个独立种子产生几乎相同的发作曲线，表明这是政策的一个发育性转变，而非记忆伪影。
2. **单调剂量反应与形成-逆转不对称性**：
   - 随λ增加（0→1→2），VHS平台值单调下降（0.91→0.67→0.48），且近似线性。
   - 在中等剂量（λ=1）下，VHS先上升至接近未惩罚水平（形成捷径），然后下降（逆转），揭示出获得与消除之间的不对称性（类似滞后）。
3. **关键干预窗口**：在发作前施加λ=1的接地惩罚可以有效阻止捷径形成（VHS降至0.60）；而在巩固后施加相同惩罚效果明显较差（VHS保持0.78-0.88）；发作中干预效果居中（降至0.74-0.78）。说明存在一个时间窗口，在此窗口内惩罚最有效。
4. **内部表示探针**：初步发现剂量效应集中在中间层（语义/推理层），早期和晚期层受影响较小，但该结果在样本量下仍处于bootstrap变异性内，仅作方向性提示。

## 7. 优点

- **新颖的控制变量框架**：将λ作为“剂量”旋钮，结合时间轴密集诊断，揭示了捷径的动态复杂性（突现、剂量反应、不对称性），而不仅仅是终点性质。
- **严格的实验设计**：OOD诊断确保发作是泛化行为而非训练记忆；种子稳健性验证；强制“一次仅变一个因子”避免混淆。
- **实践指导意义**：明确建议正则化应早期且剂量校准地应用，而非作为事后补救。
- **诊断方法可移植**：VHS诊断和接地惩罚设计可直接用于其他模型和任务，促进未来研究。

## 8. 不足与局限

- **模型泛化性有限**：仅使用Qwen3-VL-8B一个模型家族，无法断言发作位置、锐度或近线性剂量响应是否适用于其他规模（如2B、30B+）或架构（如LLaVA、InternVL）。这被认为是未来最重要的工作方向。
- **单一惩罚设计**：仅使用帧顺序随机打乱作为扰动，测量对时间内容的依赖；其他类型视觉捷径（如空间、颜色）可能面临不同动态。
- **表示探针弱**：分析为相关性的、初步的，样本量小，结果在bootstrap变异内，不能因果解释行为抑制。
- **未研究真正的滞后循环**：文中“滞后”仅指时间轴上的形成-逆转不对称，未执行双向剂量扫描（λ升→降），因此不能确认路径依赖性。
- **缺乏消融**：未系统地消融w（格式权重）、batch size等因素对观察结果的影响。
- **算力信息缺失**：未提供GPU型号/数量/总时长，影响可重复性评估。

（完）
