---
title: Cross-modal linkage risk in clinical vision-language models
title_zh: 临床视觉-语言模型中的跨模态链接风险
authors: "Soroosh Tayebi Arasteh, Mahshad Lotfinia, Sven Nebelung, Daniel Truhn"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02276v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 临床视觉语言模型用于胸部X光片和报告配对
tldr: "临床视觉语言模型在配对X光片和报告上学习嵌入，存在图像被重新链接到报告的风险。通过对406,241个样本评估，发现模型专业化程度越高风险越大。提出仅在投影头上应用差分隐私优化（ε=0.34），无需重新训练即降低Recall@1达61.8%，同时基本保持图像分类性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02276v1/fig-001.webp\", \"caption\": \"Fig. 2: Cross-modal re-linkage performance of off-the-shelf vision-language models. Each row corresponds to one evaluation cohort: MIMIC-CXR and CheXpert Plus. a Linkage hierarchy at N = 100 on MIMIC-CXR. Horizontal bars show Recall@1 (solid) and MRR (hatched) per model; vertical lines mark the corresponding chance baselines. b Fold-above-chance Recall@1 across candidate-pool sizes on MIMIC-CXR (log–log scale), computed as observed Recall@1 divided by exact chance Recall@1 at each N . The dashed horizontal line marks 1× chance; grey shading indicates the sub-chance region. c Raw Recall@1 (solid) and MRR (dashed) across all candidate-pool sizes on MIMIC-CXR (log–log scale); grey lines show chance baselines. d–f Corresponding panels for the external CheXpert Plus cohort. Error bars in all panels represent ±1 standard deviation across repeated random samplings of the candidate pool.\", \"page\": 6, \"index\": 1, \"width\": 881, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-002.webp\", \"caption\": \"Table 4: Image-side diagnostic utility of BioViL-T embeddings before and after DP head finetuning. Utility was assessed on the MIMIC-CXR test set using image-only linear probing on the standard 14-label chest radiograph task. Macro AUROC was the primary utility endpoint. Values are reported in percent as mean ± standard deviation followed by the corresponding 95% confidence interval, ∆ denotes DP minus off-theshelf performance in percentage points, and p-values were computed from 1,000 paired bootstrap replicates.\", \"page\": 11, \"index\": 2, \"width\": 775, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-003.webp\", \"caption\": \"Table 2: Cross-modal retrieval performance under pathology-matched hard negatives (setting B) on the MIMIC-CXR and CheXpert Plus test sets. For each query chest radiograph, the true paired report is retrieved from a candidate pool of size N = 10,000, where negative reports are selected to match the query 14-label pathology vector whenever possible and otherwise filled in ascending Hamming distance. Performance is evaluated using Recall@1, Recall@5, Recall@10, and mean reciprocal rank (MRR). Values are reported in percent as mean ± standard deviation (SD) with corresponding 95% confidence intervals (CIs).\", \"page\": 7, \"index\": 3, \"width\": 750, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-004.webp\", \"caption\": \"Fig. 4: Mitigation of cross-modal linkage through head-only finetuning of BioViL-T. Each row in the upper two sections corresponds to one evaluation cohort: MIMIC-CXR and CheXpert Plus. a Recall@1 across candidate-pool sizes on MIMIC-CXR under the random-candidate protocol, comparing off-the-shelf, DP head finetuning, and entropy-regularized head finetuning. The grey dashed line marks chance Recall@1 at each N . b MRR across candidate-pool sizes on MIMIC-CXR under the same three variants. c Percentage change in Recall@1 relative to the off-the-shelf model across candidate-pool sizes on MIMIC-CXR. Negative values indicate reduced linkage; positive values indicate amplified linkage. The horizontal line marks zero change. d–f Corresponding panels for the external CheXpert Plus cohort, using the same heads trained on MIMIC-CXR without retraining. g Fold-above-chance Recall@1 at N = 10,000 on MIMIC-CXR under hard negatives (hatched) and the random-candidate protocol (solid) for each variant; the vertical dashed line marks 1× chance. h Corresponding panel for CheXpert Plus. i Percentage change in Recall@1 relative to off-the-shelf under hard negatives at N = 10,000, for both cohorts (MIMIC-CXR solid, CheXpert Plus hatched). Error bars represent ±1 standard deviation.\", \"page\": 10, \"index\": 4, \"width\": 875, \"height\": 643}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-005.webp\", \"caption\": \"Fig. 3: Cross-modal retrieval under pathology-matched hard negatives at N = 10,000. Each row corresponds to one evaluation cohort: MIMIC-CXR and CheXpert Plus. a Recall@1 before and after replacing random distractors with pathology-matched hard negatives on MIMIC-CXR. Each model is shown as a slope from the random-candidate protocol (open marker) to the hard-negative protocol (filled marker); the dashed horizontal line marks chance Recall@1 at N = 10,000. b Percentage reduction in Recall@1 induced by hard negatives per model on MIMIC-CXR. Bars reaching 100% would indicate that pathology similarity fully accounts for retrieval; the modest observed values show that residual instance-level binding persists beyond label matching alone. c Fold-above-chance Recall@1 under both protocols on MIMIC-CXR. Solid bars show the random-candidate protocol and hatched bars show the hard-negative protocol; the horizontal dashed line marks 1× chance. The strongest medically adapted models remain well above chance even after the hard-negative drop. d–f Corresponding panels for the external CheXpert Plus cohort. Error bars, where shown, represent ±1 standard deviation.\", \"page\": 8, \"index\": 5, \"width\": 881, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-006.webp\", \"caption\": \"Fig. 1: Cross-modal linkage risk in clinical vision-language models (VLMs) and mitigation strategy. This schematic outlines the mechanism of the linkage vulnerability, our systematic evaluation framework, and the targeted privacy-preserving intervention. a When radiographs and reports are kept separate after acquisition, standard de-identification removes explicit metadata from the image but does not prevent the shared embedding space of a clinical VLM from re-linking it to its original, information-rich narrative report. b In the joint multi-modal manifold, images and reports are aligned by angular similarity (∠θ). Aggressive alignment during pretraining preserves instance-level correspondence, allowing a query CXR to retrieve its true paired report from a database. c We assessed the vulnerability across four models with increasing clinical specialization (CLIP, PubMedCLIP, BiomedCLIP, and BioViL-T) and quantified as the candidate-pool size scales to database levels. The signal is further tested under pathology-matched hard negatives to isolate instance-level binding from broad disease similarity. d To reduce linkage while preserving utility, the image and text backbones are frozen, and only the shared alignment layers (projection heads) are finetuned. Differentially private (DP) optimization is used to noise the alignment updates.\", \"page\": 4, \"index\": 6, \"width\": 887, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-007.webp\", \"caption\": \"Fig. 5: Image-side diagnostic utility after DP mitigation. All panels compare off-the-shelf BioViL-T and DPmitigated BioViL-T image embeddings on the MIMIC-CXR 14-label chest radiograph task. DP statistics were computed from 1,000 paired bootstrap replicates. a Asymmetry of the privacy-utility tradeoff. Bars show Recall@1 at N = 10,000 (top) and macro AUROC (bottom) for the DP-mitigated model, normalized to the off-the-shelf value (100%). Blue segments show what remains under DP, grey segments show what was lost; the relative reduction is annotated in each segment. b Paired bootstrap distributions of the DP-minus-off-the-shelf delta for the four macro utility metrics. Violins show the distribution over 1,000 replicates; filled circles mark the median and vertical bars span the 95% bootstrap CI. Metrics whose CI excludes zero are marked with ∗. c Per-label change in AUROC. Bars are color-coded by the direction and statistical significance of the change: green if the 95% CI is strictly positive, red if strictly negative, grey otherwise. d Per-label delta across all four utility metrics. Rows are 14 pathology labels sorted by |∆AUROC|; columns are AUROC, accuracy, sensitivity, and specificity. Cells are colored by difference in percentage points; non-significant cells (95% CI includes zero) are whitened.\", \"page\": 12, \"index\": 7, \"width\": 877, \"height\": 659}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-008.webp\", \"caption\": \"Table 3: Mitigation of cross-modal linkage risk in BioViL-T under head-only finetuning. Retrieval performance is shown after projection-head finetuning with either differentially private (DP) optimization or entropy regularization, while the image and text backbones remain frozen. Random denotes the randomcandidate protocol and Hard-neg denotes the pathology-matched hard-negative protocol. For CheXpert Plus, the BioViL-T projector heads trained on MIMIC-CXR were applied directly without additional mitigation training. The image and text backbones remained frozen throughout. Performance is evaluated using Recall@1, Recall@5, Recall@10, and mean reciprocal rank (MRR). Values are reported in percent as mean ± standard deviation (SD) with corresponding 95% confidence intervals (CIs).\", \"page\": 9, \"index\": 8, \"width\": 841, \"height\": 791}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-009.webp\", \"caption\": \"Table 1: Setting A. Cross-modal retrieval performance of off-the-shelf vision-language models on the MIMIC-CXR and CheXpert Plus test sets under the random-candidate protocol. For each query chest radiograph, the paired radiology report is retrieved from a candidate pool sampled from the held-out test split. Performance is evaluated using Recall@1, Recall@5, Recall@10, and mean reciprocal rank (MRR). Values are reported in percent as mean ± standard deviation. For compact presentation, all four models are shown at N = 100, whereas larger candidate pools focus on the two strongest medically adapted models; exhaustive results for all models and candidate-pool sizes, including 95% confidence intervals, are provided in Supplementary Tables 1 and 2.\", \"page\": 5, \"index\": 9, \"width\": 835, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2606.02276v1/fig-010.webp\", \"caption\": \"Table 5: Characteristics of the datasets utilized in this study. Summary of patient cohorts, AP/PA-filtered radiograph counts, demographics, label sets, and projection distributions for the two chest-radiograph datasets used in this work, namely MIMIC-CXR and CheXpert Plus. Reported values include the number of patients and radiographs after restriction to frontal anteroposterior (AP) and posteroanterior (PA) images, split into training, validation, and test sets, together with age summaries and sex ratios where available. The labels listed correspond to the 14-label abnormality space used for hard-negative construction and image-side utility evaluation. The row “Final held-out retrieval cohort” reports the paired test cohort actually analyzed in the manuscript. N/A = not available from the supplied filtered source file.\", \"page\": 16, \"index\": 10, \"width\": 916, \"height\": 434}]"
motivation: 由于图像与报告的嵌入对应关系可能泄露隐私，需要量化风险并设计高效缓解方法。
method: 将隐私风险形式化为图像到报告的检索任务，评估不同专业化程度的模型，并对齐层的投影头进行差分隐私微调。
result: "在MIMIC-CXR上Recall@1降低61.8%（候选池N=10000），泛化至CheXpert Plus，图像分类AUROC仅从79.63%降至79.43%。"
conclusion: 对少量参数进行目标差分隐私优化能有效减少跨模态重新链接，同时保留临床实用性。
---

## 摘要
基于配对的胸部X光片和放射学报告训练的视觉-语言模型（VLM）学习了一个共享嵌入空间，该空间能够保留实例级别的图像-报告对应关系。这在获取后刻意将X光片和报告分离的场景中构成隐私风险，例如仅共享图像数据或进行访问控制的报告，因为去标识化的图像可能仅通过余弦相似度重新链接到其原始叙述性报告。我们将此形式化为图像到报告的检索，并使用公共配对队列（其中真实配对是设计已知的）作为真实基准来审计风险，而非作为隐私场景。我们在来自MIMIC-CXR（43,793对保留样本）和外部CheXpert Plus（29,296对）的126,804名患者的406,241个配对样本上评估了临床专业化程度递增的VLM，发现重新链接率随专业化程度系统性地上升：最强的VLM在候选池N=100时以15倍随机概率检索到正确报告，在N=10,000时为50倍随机概率，在全数据库规模下远高于随机概率。在去除疾病标签捷径的病理匹配困难负样本下，该信号仍然存在，表明对应关系超越了广泛的诊断类别。为了在不重新训练的情况下减少这种风险，我们冻结了两个编码器，并仅对定义对齐层的投影头应用差分隐私优化（ε=0.34，δ=6x10^-6）。这使MIMIC-CXR上N=10,000时的Recall@1降低了61.8%，并在无需重新训练的情况下迁移到CheXpert Plus，而图像侧的效用基本保持：14个标签的线性探针分类宏AUROC仅从79.63%变为79.43%。对共享对齐层的定向DP微调可以在不实质性损害使这些模型具有临床实用性的图像表示的情况下，大幅减少跨模态重新链接。

## Abstract
Vision-language models (VLMs) trained on paired chest radiographs and radiology reports learn a shared embedding space that can preserve instance-level image-report correspondence. This poses a privacy risk in settings where radiographs and reports are deliberately kept separate after acquisition, such as image-only data sharing or access-controlled reports, because a de-identified image may be re-linked to its original narrative report through cosine similarity alone. We formalized this as image-to-report retrieval and used public paired cohorts, in which the true pairing is known by design, as ground-truth benchmarks to audit the risk rather than as the privacy scenario. Evaluating VLMs of increasing clinical specialization on 406,241 paired examples from 126,804 patients across MIMIC-CXR (43,793 held-out pairs) and external CheXpert Plus (29,296 pairs), we found that re-linkage rose systematically with specialization: the strongest VLM retrieved the correct report at 15 times chance at a candidate pool of N = 100, 50 times chance at N = 10,000, and well above chance at full-database scale. The signal persisted under pathology-matched hard negatives that removed disease-label shortcuts, indicating correspondence beyond broad diagnostic categories. To reduce it without retraining, we froze both encoders and applied differentially private optimization only to the projection heads defining the alignment layer (epsilon = 0.34, delta = 6x10-6). This reduced Recall@1 by 61.8% at N = 10,000 on MIMIC-CXR and transferred to CheXpert Plus without retraining, while image-side utility was largely preserved: macro AUROC for linear-probe classification across 14 labels shifted only from 79.63% to 79.43%. Targeted DP finetuning of the shared alignment layer can substantially reduce cross-modal re-linkage without materially degrading the image representations that make these models clinically useful.

---

## 论文详细总结（自动生成）

# 论文总结：《临床视觉-语言模型中的跨模态链接风险》

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：临床视觉-语言模型（VLM）在配对胸部X光片和放射学报告上训练，学习共享嵌入空间。这一空间可能保留实例级别的图像-报告对应关系，使得去标识化的X光片仅通过余弦相似度就能重新链接到其原始叙述性报告，从而构成隐私泄露风险。
- **背景**：现有的隐私保护研究主要关注成员推断、重建风险或图像单模态的差分隐私，但未直接处理跨模态重新关联问题。在医疗影像实践中，X光片和报告常被有意分离（如仅发布图像、独立访问控制），而VLM的共享嵌入可能破坏这种分离。
- **整体意义**：论文首次系统量化了这一漏洞，并证明它随模型专业化程度而加剧，同时提出无需重新训练两端编码器的轻量级缓解方案，在显著降低链接风险的同时几乎不损伤图像侧的临床诊断效用。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 将跨模态链接风险形式化为“图像→报告”的检索问题：给定查询X光片，在候选报告池中按余弦相似度排序，查看其真实配对报告是否排在前列。
  - 使用公共配对数据集（MIMIC-CXR、CheXpert Plus）作为**真实基准**（ground-truth benchmark）来审计风险（而非作为隐私场景本身），因为配对关系已知，可以精确评估模型的重新链接能力。
  - 缓解方案：冻结图像和文本编码器，仅对投影头（projection heads，即定义对齐层的少量参数）进行差分隐私微调，以弱化图像-报告之间的精确绑定。

- **关键技术细节**：
  - **威胁模型**：假设攻击者拥有去标识的X光片（或图像嵌入），并访问一个包含报告的候选库，试图通过相似度检索找到原始配对报告。
  - **检索协议**：
    - **随机候选协议**：在随机抽取的候选池（N=100、1K、10K、全测试集）中检索。
    - **病理匹配硬负样本协议**：候选负样本被选为与查询图像具有相同14标签病理向量的报告（或按汉明距离最近填充），以排除仅靠疾病标签相似性带来的检索信号。
  - **缓解方法**：
    - **微调对象**：仅更新BioViL-T的图像和文本投影头（197,504个可训练参数），主编码器冻结。
    - **DP-SGD**：每样本梯度裁剪至L2范数上限C=1.5，加高斯噪声，使用Rényi DP会计，达到ε=0.34, δ=6×10⁻⁶。
    - **非隐私对比**：熵正则化微调（无噪声），同样仅更新投影头。
    - **损失函数**：对称对比损失（InfoNCE）+ 温度参数。
    - **训练细节**：缓存冻结特征（cached features）后仅更新投影头，避免全模型DP训练的不稳定。替换批次归一化为组归一化以适应DP。
  - **图像侧效用评估**：用固定图像嵌入训练线性探针（linear probe）进行14类胸部X光异常分类，宏AUROC为主要指标。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - **MIMIC-CXR**：215,187张正面胸片，62,094名患者；测试集43,793对（患者分层随机划分）。
  - **CheXpert Plus**：191,054张正面胸片，64,710名患者；外部测试集29,296对。
  - 仅使用AP/PA正位图像，图像大小224×224，直方图均衡化。
- **Benchmark**：
  - 主要隐私指标：Recall@1、Recall@5、Recall@10、平均倒数排名（MRR）。
  - 主要效用指标：宏AUROC、宏准确率、宏敏感度、宏特异度。
  - 随机基线：每个候选池的精确随机概率（如N=100时Recall@1=1.000%）。
- **对比方法**：
  - 四种VLM：**CLIP**（通用域）、**PubMedCLIP**（生物医学）、**BiomedCLIP**（生物医学/医学）、**BioViL-T**（胸部放射学专用，最强）。
  - 缓解策略：仅BioViL-T上进行投影头DP微调 vs. 熵正则化微调（非隐私对照） vs. 原始模型。
  - 还比较了随机候选与病理匹配硬负样本两种设置。

## 4. 资源与算力

- 论文明确说明：“All experiments reported in this work were performed on a Linux workstation equipped with a single NVIDIA L40S GPU with 48 GB VRAM and Intel Xeon Silver 4310 CPUs.”
- 具体训练时长未报告（但DP微调仅5 epoch，熵正则化500 epoch，数据量约15万训练样本，单卡可完成）。

## 5. 实验数量与充分性

- **实验组数**：
  - 四个模型 × 两种数据集 × 多种候选池大小（4~5种） = 约40组主要检索实验（随机候选）。
  - 硬负样本实验：四个模型 × 两种数据集 × N=10,000。
  - 缓解实验：两种微调策略 × 两种数据集 × 多种候选池大小（含随机和硬负）。
  - 图像侧效用：原始模型 vs DP微调 → 14标签分类（宏AUROC+各标签指标）。
  - 统计：每项实验均使用20次（随机候选）或10次（硬负）重复采样，Bootstrap 1000次计算置信区间和p值。
- **充分性与公平性**：
  - 评估覆盖了从通用到高度专业化的模型，跨度合理。
  - 使用外部数据集（CheXpert Plus）验证泛化性，且缓解头无需重新训练即适用。
  - 消融实验：DP微调 vs 非隐私熵正则化微调，明确证明不是“任意投影头调整”都有效。
  - 统计严谨：Bootstrap置信区间、配对p值、多重指标。
  - 局限性：硬负样本基于14标签（仍较粗糙），图像效用仅用线性探针（未覆盖所有下游任务），缓解仅针对BioViL-T。

## 6. 论文的主要结论与发现

1. **跨模态重新链接真实存在且随专业化增强**：在N=100时，BioViL-T的Recall@1达15.440%（随机为1%），BiomedCLIP为5.829%，CLIP仅为1.203%。CheXpert Plus上趋势一致但绝对水平更低。
2. **信号在全数据库规模和硬负样本下仍显著高于随机**：N=10,000时BioViL-T Recall@1为0.496%（随机0.010%），即50倍随机。硬负样本下仍为0.343%（约34倍随机），说明超越疾病标签的实例级绑定。
3. **投影头DP微调大幅降低链接**：在MIMIC-CXR上，N=10,000时Recall@1从0.496%降至0.190%（降61.8%）；CheXpert Plus外部测试集也有效（0.153%→0.091%）。
4. **图像侧效用基本保持**：宏AUROC从79.63%降至79.43%（仅降0.20pp），准确率几乎不变，敏感度和特异度轻微位移。
5. **非隐私熵正则化反而增强链接**：表明不是任何投影头调整都安全，必须采用隐私扰动。
6. 隐私-效用权衡高度不对称：大幅减少隐私风险的同时，临床诊断性能损失极小。

## 7. 优点：方法或实验设计亮点

- **问题形式化清晰**：将隐私风险明确定义为图像→报告检索，使用公开配对数据集作为审计工具，而非混淆隐私场景。
- **系统全面的评估**：涵盖多种模型、多尺度候选池、两种协议（随机与硬负）、内外部数据集。
- **缓解方案轻量实用**：仅调整投影头（<200K参数），无需重新训练大型编码器，且能跨数据集迁移。
- **公平消融**：引入非隐私熵正则化作为对照，证明DP扰动是降低链接的关键，而非任意优化。
- **统计严谨**：Bootstrap置信区间、配对假设检验、重复采样降低随机性。
- **公开代码与配置**：所有代码开源，可完全复现。

## 8. 不足与局限

- **模态范围窄**：仅限胸部X光片，其他成像模态（CT、MRI等）或不同报告语言/风格的风险未知。
- **威胁模型有限**：假设攻击者只能进行余弦相似度检索，未考虑更复杂的攻击（如对抗性嵌入检索、外部报告语料库）。
- **缓解仅适用于BioViL-T**：最危险的模型，但其他架构可能对投影头微调反应不同。
- **硬负样本基于14标签**：虽能控制大致疾病，但仍不能完全捕捉报告中的复杂叙述细节（如对比、病灶位置、技术参数）。
- **图像效用评估单一**：仅用线性探针分类，未测试零样本、报告生成、细粒度检索等下游任务。
- **隐私保证局限性**：DP保证仅覆盖投影头微调阶段，不覆盖原始预训练或模型的其他使用方式。
- **仅两个公开机构的数据**：外部迁移虽存在，但规模与多样性有限。
- **未进行严格验证集早停选择**：图像效用评估可能略微乐观（论文提及“如果最终提交前未重新运行验证选择，则效用值应视为初步”）。

（完）
