---
title: Attention Consistent Longitudinal Medical Visual Question Answering Guided by Vision Foundation Models
title_zh: 基于视觉基础模型引导的注意力一致性纵向医学视觉问答
authors: "Jialin Wu, Qianru Zhang, Georges El Fakhri, Xiaofeng Liu"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.06534v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于注意力和视觉基础模型的纵向医学视觉问答
tldr: 本文提出一种基于视觉基础模型的注意力一致纵向医学视觉问答方法，通过轻量级仿射配准对齐当前与参考胸部X光片，使用DINO和自适应掩码生成器生成掩码，结合多模态Transformer解码器进行答案生成，并引入掩码重建、Gram风格一致性和KoLeo均匀性损失辅助训练，在Medical-Diff-VQA上取得优异性能，同时提供内在可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06534v1/fig-001.webp\", \"caption\": \"Figure 1. Illustration for our longitudinal medical VQA framework. A. First, perform approximately identical affine pre-registration (with parameter regularization Lreg) on the main and reference images, then extract features via the image encoder. The registered images are fed into DINO and an adaptive mask generator, respectively; both generate temporal masks under shared weights, which are then fused via the union/fuse function and weighted averaging (weights λ and 1 − λ) to produce a change-aware shared mask applied uniformly to both registered images. The masked images are re-encoded, and the resulting two-time-point features are concatenated sequentially with the problem encoding using special separator tokens to form a multimodal prefix. Finally, the generative decoder outputs the answer. B. Mask consistency losses Lmask main and Lmask ref constrain the distance between the masked images’ features and the product of the mask feature and images’ features. C. Language modelling loss Llm directly supervises answer generation. D. Lightweight MLP prediction heads provide auxiliary supervision via Lpred main and Lpred ref respectively enhancing the semantic meaning of the masks. E. Gram-style similarity constraints are applied to both full and masked features (Lgram and Lgram mask) to enforce similar patch structure between the main image and the reference image. F. KoLeo regularization LKoLeo promotes representational separability across samples.\", \"page\": 3, \"index\": 1, \"width\": 1026, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2606.06534v1/fig-002.webp\", \"caption\": \"Figure 2. Examples of questions and their corresponding answers generated by our model and the ground truth. Correct predictions are highlighted in blue, while incorrect predictions are highlighted in red.\", \"page\": 8, \"index\": 2, \"width\": 1020, \"height\": 586}]"
motivation: 针对纵向医学图像问答中需要推理当前与参考时间点图像间的解剖差异，现有方法缺乏有效对齐和可解释性。
method: 采用轻量级仿射配准模块对齐图像对，利用DINO冻结掩码和可训练自适应掩码生成器提取显著性区域，再通过多模态Transformer解码器融合图像与文本特征生成答案，并加入掩码重建、Gram风格一致性和KoLeo均匀性损失辅助学习。
result: 在Medical-Diff-VQA基准上，BLEU、ROUGE-L、CIDEr和METEOR得分均表现优秀，且共享显著性掩码提供了内在可解释性。
conclusion: 基于预对齐的显著性条件生成框架为医学纵向推理提供了原则性方法，同时展示了同时优化监督和无监督目标的训练策略在生物医学视觉基础模型中的应用潜力。
---

## 摘要
纵向医学视觉问答（VQA）需要对当前时间点的图像与参考时间点的图像之间的解剖差异进行推理。我们针对胸部X光片提出了一种注意力引导的编码器-解码器模型。不同于传统的直接对比方法，我们引入了一个轻量级仿射配准模块，通过一个小型配准正则化器将当前图像与参考图像进行共同配准，以减少无关运动。配准后的图像对输入图像编码器，随后经过一个冻结的DINO掩码生成器和一个可训练的自适应掩码生成器，产生应用于原始图像对的掩码。掩码后的图像对再次输入图像编码器，并与文本特征拼接，作为基于多模态Transformer的解码器的输入，以生成最终答案。为了促进学习稳定并明确变化信号，受DINO-v3启发，我们增加了额外的辅助目标，包括掩码重建损失、成对Gram风格一致性损失和KoLeo均匀性损失，这些损失增强了表示的几何结构。在Medical-Diff-VQA基准上，该模型获得了强大的BLEU、ROUGE-L、CIDEr和METEOR分数，同时通过共享显著掩码提供内在可解释性。这些结果支持将显著条件生成与轻度预对齐作为医学VQA中纵向推理的原则性框架。我们的训练策略也展示了一种在生物医学中利用图像基础模型的范式潜力：同时优化监督学习和无监督学习目标。

## Abstract
Longitudinal medical visual question answering (VQA) requires reasoning about anatomical differences between an image of a current time point and an image of a referred time point. We propose an attention-guided encoder-decoder for this task with chest X-rays. Instead of conventional direct contrast, we propose to include a lightweight affine registration module to reduce nuisance motion by co-registering the current image to the reference image with a small registration regularizer. The registered image pair is fed into the image encoder, followed by a frozen DINO-based mask generator and a trainable adaptive mask generator to produce masks applied to the original image pairs. The masked image pairs are again fed into the image encoder and concatenated with text features as the input to a multimodal transformer-based decoder to generate final answers. To facilitate learning stabilization and clarify the change signal, inspired by DINO-v3, we include additional auxiliary objectives, including a mask rebuilding loss, a pairwise Gram-style consistency loss, and a KoLeo uniformity loss, which enhances the geometry of the representation. On the Medical-Diff-VQA benchmark, the model delivers strong BLEU, ROUGE-L, CIDEr, and METEOR scores while offering intrinsic interpretability through the shared saliency mask. These results support saliency-conditioned generation with mild pre-alignment as a principled framework for longitudinal reasoning in medical VQA. Our training strategy also illustrates the potential of a paradigm in utilizing image foundation models in biomedicine: optimizing both supervised and unsupervised learning objectives simultaneously.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
纵向医学视觉问答（Diff-VQA）要求模型对比同一患者两个时间点的医学影像（如胸部X光），推理其中的解剖变化以回答临床问题。现有方法大多聚焦单时间点，或依赖监督微调，未显式鼓励两个时间点的注意力一致性，且缺乏可解释性（仅靠事后显著性分析）。本文旨在解决上述问题，提出一种**注意力一致、可解释的纵向医学VQA框架**，通过引入轻量级配准、共享显著性掩码以及多种辅助损失，同时提升性能与透明度。

#### 2. 论文提出的方法论：核心思想、关键技术细节
**核心思想**：先对当前图像进行微配准以消除姿态噪声，再利用冻结的DINO视觉基础模型与可训练的自适应掩码生成器共同产生一个共享的显著性掩码，施加于两个时间点图像，最后将掩码特征与问题文本拼接送入多模态解码器生成答案。

**技术细节**：
- **微配准模块**：一个轻量级CNN预测2D仿射参数，对当前图像进行可微仿射变换，并用正则项（`L_reg`）约束变换接近恒等，避免抹除真实变化。
- **掩码生成**：利用冻结的RAD-DINO提取两图的注意力图，取最大合成先验`U`；同时从图像编码器特征经MLP生成自适应掩码`F`；通过融合函数（Union/Fuse）和权重`λ`（从1衰减到0.5）融合为共享掩码`M`。
- **编码与解码**：将掩码后的图像重新用同一图像编码器（Swin-Base）编码，与问题文本特征拼接（含特殊token `<img>`, `<qtn>`, `<ans>`），输入GPT-2解码器生成答案。
- **训练损失**（总损失`L_total`）：
  - 语言建模损失`L_lm`（交叉熵）
  - 掩码一致性损失`L_mask_main/ref`：约束掩码后特征等于掩码与原始特征的点积
  - 掩码重建损失`L_pred_main/ref`：微型MLP将掩码特征回归到原始特征
  - Gram风格一致性损失`L_gram`和`L_gram_mask`：保持两图patch间关系相似
  - KoLeo均匀性损失`L_KoLeo`：促进批内样本分离
  - 配准损失`L_reg`
- **两阶段训练**：第一阶段冻结图像编码器训练4轮，第二阶段全模型训练4轮。

#### 3. 实验设计
- **数据集**：Medical-Diff-VQA，源自MIMIC-CXR，共164,223个样本（训练131,556，验证16,278，测试16,389），每个样本包含一对胸部X光片、一个差异相关问题、一个答案。
- **基准方法**：MCCFormers、IDCPCL、EKAID、RegioMix、PLURAL、VED。
- **评估指标**：BLEU-1、METEOR、ROUGE-L、CIDEr（以CIDEr选择最佳模型）。
- **消融实验**：移除“冻结图像编码器”/“DINO启发的无监督目标”/“注意力掩码”，共3组对比。

#### 4. 资源与算力
- 图像编码器预训练在单块NVIDIA A100-SXM4-80G GPU上。
- 全模型训练使用四块NVIDIA A100-SXM4-80G GPU。
- 训练批次大小：训练时10，验证时8。
- 优化器：AdamW，学习率1.5e-4，权重衰减0.05。
- 训练阶段：两阶段各4个epoch（共8个epoch）。
- 数据预处理：图像缩放到384×384，并做亮度/对比度/锐度增广。

#### 5. 实验数量与充分性
- **主要对比实验**：在测试集上报告了BLEU-1、METEOR、ROUGE-L、CIDEr，并与6种SOTA方法比较。
- **消融实验**：3组变体，验证了冻结策略、无监督目标、注意力掩码各自的有效性。
- **充分性评价**：数据集规模中等（~164k样本），对比方法全面且为近年代表性工作，消融实验设计合理。但**缺少跨数据集/多模态/多中心验证**，未在其他疾病或模态上评估泛化能力；仅一组指标实验，未做统计显著性检验（如多次运行取平均）。总体上实验较为充分，但泛化性验证不足。

#### 6. 论文的主要结论与发现
- 提出方法在BLEU-1（0.747）、METEOR（0.700）、ROUGE-L（0.703）上全面超越所有对比方法；CIDEr（2.011）略低于VED（2.119）但仍显著高于其他方法。
- 消融实验表明：两阶段冻结训练、DINO启发的无监督目标、注意力掩码均贡献性能提升，去除任一项均导致分数下降。
- 模型生成的显著性掩码有效定位于变化区域，提供了内在可解释性，无需后处理。
- 提出的联合优化监督与无监督目标的训练策略在生物医学视觉基础模型应用中有潜力。

#### 7. 优点
- **创新性**：首次将显式的共享注意力一致性引入纵向医学VQA，结合冻结DINO先验与自适应掩码，兼顾稳定性与样本适应性。
- **可解释性**：模型直接生成显著性掩码作为推理依据，无需额外后处理，增强医生信任。
- **损失设计全面**：覆盖语义、空间对齐、特征分布、注意力一致等多维度，有效提升表示质量。
- **微配准策略**：轻量级仿射配准减小几何噪声，且正则化防止过度对齐。
- **实验表现**：在关键语义指标METEOR上大幅领先（0.700 vs 第二名0.389），表明模型更擅长捕捉临床含义。

#### 8. 不足与局限
- **计算开销**：引入DINO分支、自适应掩码生成器和多项损失，训练复杂度和计算成本较高。
- **配准能力有限**：仅支持仿射变换，对大位置偏移或显著成像条件差异的图像对齐效果可能不足。
- **掩码噪声**：DINO生成的先验掩码仍含有无关区域噪声，可能影响注意力精度。
- **泛化性未验证**：仅在MIMIC-CXR单一数据集上评估，未测试其他疾病类型、成像模态或多中心数据，存在过拟合风险。
- **CIDEr指标未达最优**：略低于VED，需进一步分析原因。
- **生成答案仍存在错误**：例如遗漏部分发现（如仅输出“edema”而漏掉“pleural effusion”），说明语义完整性不足。
- 缺少对模型鲁棒性的分析（如噪声、遮挡等攻击）。

（完）
