---
title: "ATRIA: Adaptive Traceable ECG Reporting with Iterative Agents"
title_zh: ATRIA：采用迭代代理的自适应可追踪心电图报告
authors: "Donggyun Hong, Kyuhwan Lee, Junmyung Kwon, Yong-Yeon Jo"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24392v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于医学心电图报告的多智能体系统
tldr: 现有ECG报告生成方法存在错误传播和缺乏迭代反馈的问题。ATRIA提出多代理系统，模拟临床迭代工作流，将报告声明与证据绑定，支持医生验证和修改，并整合上下文。系统使用已临床验证的ECG分析模型，作为云服务可立即部署。通过四个交互案例展示了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24392v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of ATRIA. Five agents coordinate over a shared artifact store that every agent reads fromandwrites to, preserving intermediate outputs across the staged workflow.\", \"page\": 2, \"index\": 1, \"width\": 521, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2606.24392v1/fig-002.webp\", \"caption\": \"Figure 1: Screenshot of ATRIA chat interface after an ECG upload: the Orchestrator Agent dispatches the staged workflow, with each agent step streaming in order as artifacts are produced.\", \"page\": 2, \"index\": 2, \"width\": 521, \"height\": 552}]"
motivation: 解决现有ECG报告生成中端到端错误传播和单次通过缺乏迭代反馈的问题。
method: 构建多代理系统，迭代地绑定证据、标记无支持声明、整合上下文，并允许医生交互验证和修改发现。
result: 通过四个交互案例展示了系统功能，并提供在线演示和视频。
conclusion: ATRIA实现了可追溯、可迭代的ECG报告生成，可直接部署使用。
---

## 摘要
现有的心电图报告生成紧密耦合——解读与报告端到端融合，导致错误传播而无法进行阶段级补救——而基于代理的系统虽将任务解耦但仍为单次通过，从不重新审视早期输出。临床心电图报告则是迭代展开的，需要逐步的上下文整合和双向编辑。我们提出ATRIA，一个多代理心电图报告系统，模拟临床医生的迭代工作流程：它将每一条报告声明绑定到其支持证据，标记那些未被证据支持的陈述，在会话中整合额外的上下文，并让临床医生验证和修订个别的发现，而不是接受一个不透明的输出。由于其代理使用已在临床中使用的心电图分析模型，基本的发现在临床上是可信的；作为基于云的Web服务，ATRIA已准备好立即部署。我们通过四个交互案例演示ATRIA，并提供现场演示和视频。

## Abstract
Existing ECG report generation is tightly coupled -- interpretation and reporting fused end-to-end, so errors propagate without stage-level recourse -- while agent-based systems decouple tasks but remain single-pass, never revisiting earlier outputs. Clinical ECG reporting instead unfolds iteratively, requiring progressive context integration and bidirectional editing. We present \textsc{ATRIA}, a multi-agent ECG reporting system that mirrors the clinician's iterative workflow: it binds every report claim to its supporting evidence, flags statements unsupported by that evidence, incorporates additional context mid-session, and lets clinicians verify and revise individual findings rather than accept one opaque output. Because its agents use ECG analysis models already in clinical use, the underlying findings are clinically trustworthy; and as a cloud-based web service, \textsc{ATRIA} is ready for immediate deployment. We demonstrate \textsc{ATRIA} through four interaction cases, with a live demo and video available.

---

## 论文详细总结（自动生成）

# ATRIA：采用迭代代理的自适应可追踪心电图报告 - 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **现有方法的不足**：当前ECG报告生成主要存在两种范式：
  - 紧密耦合的端到端方法：ECG解读与报告生成融合为一个单步流程，错误会直接传播到最终报告，且没有阶段级修正手段。
  - 基于代理的解耦系统：虽将任务拆分为多个专业代理，但依然是单次通过（single-pass），不会重新审视或细化早期输出。
- **临床实际需求**：临床ECG报告是一个迭代过程，需要逐步整合上下文（如实验室值、既往记录），进行双向编辑（如修正某条发现后再验证整体一致性），且每一条声明都需要可追溯的证据支持。
- **核心问题**：现有系统无法支持这种迭代、可审计、证据绑定的工作流程，导致临床医生只能接受或拒绝一个不透明的输出，难以进行精细审核和修正。
- **整体含义**：为了在医疗场景中建立信任，报告生成必须实现阶段级可追溯性（traceability）、上下文渐进整合（progressive context integration）和双向迭代使用（bidirectional iterative use）。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：构建一个多代理系统（ATRIA），模拟临床医生的迭代阅读工作流——每个报告声明都绑定到支持证据，系统自动标记无支持的陈述，允许医生在会话中附加额外上下文，并支持对单个发现进行验证和修改。
- **系统架构**：
  - **协调器代理（Orchestrator Agent）**：负责会话管理、任务路由，不做分析或生成。维护一个共享工件存储（Shared Artifact Store），所有代理可读写，使中间输出在整个会话中保持可访问。
  - **分析代理（Analysis Agent）**：调用已临床使用的ECG模型（MedicalAI的AiTiA套件，包括左心室收缩/舒张功能障碍、主动脉瓣狭窄、急性心肌梗死检测等模型）进行结构化的特征提取和规则总结，输出结构化发现清单。可接收可选的实验室值作为上下文。
  - **报告代理（Report Agent）**：基于分析清单生成分节段的ECG报告（临床印象、特征总结、AI/规则发现、导联观察、建议、注意事项、附件）。每个节段绑定到生成它的上游工件，支持部分重新起草。
  - **文献代理（Literature Agent）**：当报告声明需要外部证据支持时（自动选择弱支持声明或用户指定），检索内部ECG知识库中的相关段落，并作为工件绑定到原始声明。
  - **审核代理（Review Agent）**：将草稿与累积工件对比，检查遗漏、不匹配、无支持或矛盾的声明，返回结构化反馈给报告代理和协调器，供医师审核。
- **关键设计**：
  - 通过聊天界面交互，降低临床采用门槛。
  - 会话保持打开状态，后续请求（如附加实验室值、对比既往记录）只重新执行受影响的代理，而非整个流水线。
  - 支持多个ECG记录绑定到同一会话，进行对比报告。
- **算法流程（文字描述）**：
  1. 用户上传ECG记录（可选上下文如实验室值、既往ECG）。
  2. 协调器代理启动工作流，按顺序调用分析代理 → 报告代理 → 文献代理（可选） → 审核代理。
  3. 每个代理将输出写入共享存储，系统返回分节报告和辅助工件（如ECG可视化、证据）。
  4. 对于后续请求（澄清、附加上下文、对比、修订），协调器仅重新调度受影响的代理。

## 3. 实验设计：数据集、基准、对比方法
- **实验形式**：论文通过四个交互案例（Use Cases）进行演示，每个案例覆盖一个核心需求场景，无定量实验或传统基准测试。
  - **案例1：可追溯性验证**（轨迹性）——初始报告生成后，审核代理标记无上游工件支持的无根基声明（如引用了不在数据集中的指南内容）。
  - **案例2：渐进式上下文整合**——用户附加实验室值（血钾、肌钙蛋白等），分析代理重新调用，报告仅重写受影响节段（临床印象、建议）。
  - **案例3：双向迭代使用**——用户请求对特定声明（如“窦性心动过缓”）检索文献证据，文献代理检索内部知识库并绑定结果。
  - **案例4：对比报告**——用户上传同一患者的第二份ECG记录，系统生成逐节段对比报告。
- **使用的数据**：未提及公开数据集大小或来源。ECG模型是已临床使用的，但论文未报告这些模型的性能指标。
- **基准对比**：无。论文未与任何现有方法（如端到端LLM生成、其他代理系统）进行定量比较。
- **评估指标**：无。仅通过示例展示功能可行性。

## 4. 资源与算力
- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长、推理部署所需的算力资源。仅提到所有语言组件运行在“gpt-5-mini”上（可能为GPT-5 mini模型），且系统以云服务形式部署，但未提供具体硬件规格。
- **结论**：论文未披露任何算力相关信息，因此无法评估资源效率或训练成本。

## 5. 实验数量与充分性
- **实验数量**：仅为4个演示案例，无消融实验、无对比实验、无定量指标。
- **充分性**：极不充分。尽管案例覆盖了三个设计需求（轨迹性、上下文整合、双向迭代），但缺乏：
  - 实际临床数据上的性能评估（如报告准确性、错误率、用户满意度）。
  - 与现有方法的公平对比（如端到端方法、其他代理系统）。
  - 消融研究（如移除审核代理或文献代理后的效果）。
  - 用户研究或临床有效性验证。
- **客观性**：演示案例由论文作者设计，可能倾向于展示系统优势，存在选择偏差风险。

## 6. 主要结论与发现
- ATRIA成功构建了一个支持迭代、可追溯、可交互的ECG报告多代理系统，通过四个案例展示了其核心能力。
- 系统绑定每一条报告声明到证据，标记无支持声明，并能渐进整合上下文（实验室值、既往记录）。
- 通过共享工件存储和部分重新起草机制，后续请求无需重跑整个流水线，提高了效率。
- 系统集成了已临床使用的ECG模型，且作为云服务可立即部署，具有实际应用潜力。
- 论文结论认为ATRIA是可部署、可迭代的ECG报告方案，并计划未来加入更丰富的临床上下文、更强的验证和更广泛的人机交互支持。

## 7. 优点
- **方法创新性**：将多代理系统与ECG报告生成结合，模拟临床迭代工作流，这是现有端到端或单次通过方法未充分探索的。
- **可追溯性设计**：每个报告声明绑定到上游证据，并设置审核代理自动检查不一致，增强了报告的可信性和可审计性。
- **渐进式上下文整合**：支持在会话中附加额外信息（实验室值、对比记录），且仅重写受影响节段，避免了全量重新生成。
- **部署就绪**：使用已临床应用的ECG分析模型（非研究原型），云服务形式可直接部署，实际可行性高。
- **用户界面友好**：采用聊天交互（类似LLM代理），降低了临床医生的学习成本。

## 8. 不足与局限
- **缺乏定量评估**：没有在公开或私有数据集上衡量报告生成质量（如BLEU、ROUGE、临床准确率等），也没有与基线方法对比，无法证实性能优势。
- **无消融实验**：无法判断每个代理的贡献（例如，文献代理在多大程度上改进了报告，审核代理发现了多少错误）。
- **实验规模极小**：仅4个演示案例，且案例内容由作者自定，存在选择偏差；未涉及真实患者数据或临床医生实际使用评估。
- **未报告算力与成本**：没有说明训练/推理所需的GPU资源，也未分析每次报告生成的延迟或API成本（特别是使用了gpt-5-mini）。
- **临床应用风险**：虽然使用了临床ECG模型，但整个报告生成系统（尤其是LLM部分）的可靠性、幻觉、偏见等风险未被评估。审核代理可能无法捕捉所有错误，文献检索质量未量化。
- **可重复性问题**：未提供开源代码或标准数据集，他人难以复现或验证。

（完）
