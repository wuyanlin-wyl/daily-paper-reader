---
title: Surgical Anatomy Recognition with Context Learning using Foundation Representations
title_zh: 利用基础表征进行上下文学习的手术解剖识别
authors: "Ronald L. P. D. de Jong, Tim J. M. Jaspers, Raf A. H. Vervoort, Aron F. H. A. Bakker, Yiping Li, Jip L. Tolenaar, Jelle P. Ruurda, Willem M. Brinkman, Josien P. W. Pluim, Marcel Breeuwer, Daan de Geus, Fons van der Sommen"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22124v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用基础表示进行上下文学习的外科解剖识别，涉及医学图像分割
tldr: 针对微创手术中解剖结构识别因标注数据少和自然场景方法不适用而受限的问题，本文构建了包含120k+帧、14种术式的ATLAS-120k数据集，并提出ATLAS模型，利用基础模型嵌入和轻量时序推理融合手术类型、阶段等上下文信息，实现实时、时序一致的语义分割，为手术场景理解与临床指导系统奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22124v1/fig-001.webp\", \"caption\": \"Fig. 3: ATLAS-120k dataset characteristics.\", \"page\": 6, \"index\": 1, \"width\": 720, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2606.22124v1/fig-002.webp\", \"caption\": \"Fig. 2: Examples of randomly selected frames and annotations, with at least one example from each included procedure and class.\", \"page\": 5, \"index\": 2, \"width\": 725, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2606.22124v1/fig-003.webp\", \"caption\": \"Table 2: Quantitative comparison of foundation models for segmentation. Models are grouped by pretraining domain: natural-image foundation models (top), indomain pretrained medical models (middle), and our proposed ATLAS models (bottom). For DINOv2 and DINOv3, we train a linear layer attached to the ViT.\", \"page\": 8, \"index\": 3, \"width\": 725, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2606.22124v1/fig-004.webp\", \"caption\": \"Fig. 1: Left: the frame-level semantic segmentation model. Middle: the clip-level model with integrated tracking queries. Right: the procedure-level model, incorporating context queries.\", \"page\": 3, \"index\": 4, \"width\": 725, \"height\": 275}]"
motivation: 现有方法缺乏针对手术解剖识别的大规模标注数据与适配模型，导致在微创手术中准确性和时序一致性不足。
method: 提出ATLAS-120k数据集（基于可扩展标注流水线）与ATLAS模型（结合基础模型嵌入和轻量时序推理，融合手术类型、阶段及短时视觉记忆）。
result: 在多种手术视频上实现时序一致且准确的实时语义分割。
conclusion: 该数据集和模型为鲁棒的手术场景理解提供了实用基础，支持临床适用的微创手术引导系统开发。
---

## 摘要
解剖结构的准确识别对于安全有效的微创手术（MIS）至关重要，但由于标注数据有限且方法主要针对自然场景，这一领域在手术计算机视觉中仍未得到充分探索。在这项工作中，我们提出了一个组合数据集和模型框架，以推进MIS中的解剖感知。首先，我们引入了ATLAS-120k，一个大规模片段级语义分割数据集，包含来自100个手术视频的超过12万帧标注图像，涵盖14种手术方式和多种模态，包括腹腔镜和机器人辅助手术。该数据集捕捉了显著的手术变异性，并通过可扩展的标注流程创建，该流程整合了专家手动标注、自动传播、迭代优化和外科医生验证，以确保高质量标注。其次，我们提出了ATLAS（利用基础表征进行上下文学习的解剖识别），这是一个专为手术解剖识别设计的视频语义分割模型。与强调目标跟踪的传统方法不同，ATLAS利用基础模型嵌入与轻量级时间推理相结合，融入上下文线索，如手术类型、手术阶段和短期视觉记忆。这种设计实现了时间一致且准确的预测，同时保持实时可行性。该数据集和模型共同为稳健的手术场景理解奠定了实践基础，并支持开发适用于微创手术的临床指导系统。模型、数据集标注和标注平台公开可见：https://github.com/TimJaspers0801/ATLAS

## Abstract
Accurate recognition of anatomical structures is essential for safe and effective minimally invasive surgery (MIS), yet it remains underexplored in surgical computer vision due to limited annotated data and methods tailored primarily to natural scenes. In this work, we present a combined dataset and model framework to advance anatomy-aware perception in MIS. First, we introduce ATLAS-120k, a large-scale clip-level semantic segmentation dataset comprising over 120,000 annotated frames from 100 surgical videos spanning 14 procedures and multiple modalities, including laparoscopic and robot-assisted surgery. The dataset captures substantial procedural variability and was created using a scalable annotation pipeline that integrates expert manual labeling, automated propagation, iterative refinement, and surgeon verification to ensure high-quality annotations. Second, we propose ATLAS (Anatomy Recognition with Context Learning using Foundation Representations), a video semantic segmentation model specifically designed for surgical anatomy recognition. Unlike conventional approaches that emphasize object tracking, ATLAS leverages foundation-model embeddings together with lightweight temporal reasoning to incorporate contextual cues such as procedure type, surgical phase, and short-term visual memory. This design enables temporally consistent and accurate predictions while maintaining real-time feasibility. Together, the dataset and model establish a practical foundation for robust surgical scene understanding and support the development of clinically applicable guidance systems for minimally invasive surgery. The models, dataset annotations and annotation platform are publicly available at: https://github.com/TimJaspers0801/ATLAS.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：微创手术（MIS）中准确的解剖结构识别对安全和有效操作至关重要。然而，由于缺乏大规模、多样化的标注数据集，以及现有方法主要针对自然场景设计，解剖识别在手术计算机视觉中仍是未充分探索的挑战。
- **关键瓶颈**：现有的解剖数据集（如DSAD、CholecSeg8k、Endoscapes-Seg50）存在程序多样性有限、视频/帧数较少、解剖类别覆盖不全等问题，导致模型泛化能力不足。
- **研究目标**：推进解剖感知在微创手术中的应用，通过构建大规模、多样化数据集和专用模型，实现鲁棒的、实时的解剖语义分割，支持临床引导系统的发展。
- **整体贡献**：同时贡献了**ATLAS-120k数据集**（大规模片段级语义分割，100个视频、14种术式、12万+帧）和**ATLAS模型**（结合基础模型嵌入与轻量级时序/上下文推理的视频语义分割模型）。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：手术解剖识别高度依赖上下文线索（如手术类型、手术阶段）。不同于自然视频中的目标跟踪，手术视频需要程序感知和阶段感知。模型通过引入**上下文查询**（context queries）来捕捉全局过程信息和时序信息，并结合基础模型嵌入实现准确、一致的预测。
- **关键技术细节**：
  - **基础架构**：基于EoMT（图像分割模型）进行扩展，EoMT使用ViT编码器处理可学习的查询（每个查询代表一个对象），预测类别和掩码。
  - **时序推理**：采用类似VidEoMT的查询传播机制，将前一帧的输出查询（ˆQ_t-1）通过线性层与初始查询Q0相加，作为当前帧的输入查询，实现短时记忆和时序一致性。
  - **上下文查询**：
    - **过程查询（Procedure Context Queries）**：一个全局过程查询，通过MLP预测手术过程标签，使用交叉熵损失监督，编码高层的稳定性上下文。
    - **阶段查询（Phase Context Queries）**：多个阶段查询，通过InfoNCE损失拉近同片段内连续帧的嵌入、推开不同片段或远时间帧的嵌入，编码时间状态变化。
  - **输入与联合处理**：每个时间步的输入X_t包括视觉补丁令牌、可学习的初始分割/过程/阶段查询，以及传播的前一帧查询。所有查询由单个Transformer编码器联合处理，注意力全局，使分割查询能同时关注视觉证据和上下文查询。
  - **公式说明**：
    - 过程预测：ˆy^proc_t = MLP(ˆQ^proc_t)，损失L_proc = CE(ˆy^proc_t, y^proc_t)
    - 阶段对比学习：L_phase = -log( exp(z_t·z_t⁺/τ) / Σ_j exp(z_t·z_j/τ) )
    - 查询传播：Q_t = Linear(ˆQ_{t-1}) + Q_0

## 3. 实验设计

- **数据集**：
  - 主数据集：**ATLAS-120k**，包含100个手术视频，14种术式（如胆囊切除术、肾上腺切除术、食管切除术等），共502个片段，121,018帧。同时包含腹腔镜和机器人辅助手术，分辨率480×640至1080×1920，帧率统一为15fps。
  - 数据划分：70个视频训练，10个验证，20个测试。训练限制使用30个语义类别。
- **基准（Benchmark）**：
  - 使用COCO风格平均精度（AP、AP50、AP75）、Dice系数评估检测与分割性能。
  - 使用平均视频一致性指标（mVC12、mVC24）评估时序稳定性。
- **对比方法**：
  - 自然图像基础模型：DINOv2 (ViT-B/L)、DINOv3 (ViT-B/L)、SAM2-UNet、SAM3-UNet
  - 域内预训练医学模型：EndoFM、GastroNet5M、EndoViT、SurgeNet系列（PVTv2、ConvNeXtv2、CaFormer-S18）
  - 自身消融实验：从EoMT基线逐步增加域内预训练、时序传播、上下文查询。
  - 注意：排除了与ATLAS-120k有预训练数据重叠的模型（如SurgeNetXL、LEMON、GSViT）。

## 4. 资源与算力

- 文中明确说明：所有实验在**单张NVIDIA H100 GPU**上完成。
- 训练设置：epoch数为10，batch size为24，每个训练样本为5帧的片段。
- 推理速度：ATLAS ViT-L模型达到**64 FPS**（在H100上基准测试），满足实时要求。
- 未说明训练时长（小时数），仅给出epoch数和batch size。

## 5. 实验数量与充分性

- **实验组数**：主要包括两部分——
  - **SOTA对比**：表中列出了15种模型的定量结果，涵盖自然图像和医疗域基础模型。
  - **消融实验**：包含2个消融表（实际是表3，一个表含两组）：(1) 逐步添加时序传播、上下文查询的效果； (2) 不同预训练方法（DINOv1、DINOv2、DINOv3）的效果。
- **充分性与公平性**：
  - 充分性：实验覆盖了多种主流基础模型，且消融验证了每个组件的贡献。数据集划分按患者级别（避免数据泄露），预训练数据排除ATLAS-120k。评价指标全面（检测、分割、时序一致性）。
  - 可能不足：仅对一个数据集（ATLAS-120k）进行了实验，未在其他已知解剖分割数据集（如DSAD、CholecSeg8k）上跨域验证。训练epoch仅10，可能不充分；但作者解释ATLAS-120k具有很高挑战性（30类长尾分布），绝对mDice适中。
  - 公平性：对比的方法均使用官方或公开实现，来源明确，但缺少统计显著性检验。

## 6. 主要结论与发现

- ATLAS模型在检测、分割和时序一致性上显著优于所有对比方法。ViT-L版本达到AP 0.64，mDice 0.49，mVC24 0.79，推理速度64 FPS。
- **消融结果**：
  - 域内预训练（DINOv3在SurgeNet上预训练）带来大幅提升（AP从0.53到0.57，mDice从0.35到0.43）。
  - 时序查询传播进一步提升AP至0.59，但mDice提升有限（0.36→0.43），说明时序传播主要改善检测识别。
  - 上下文查询（过程+阶段）带来最大增益：AP 0.64，mDice 0.49，mVC大幅提升至0.85/0.79。
  - 预训练方法中，DINOv3优于DINOv2和DINOv1（ATLAS ViT-B: AP 0.60 vs 0.55 vs 0.52）。
- **结论**：时序传播和上下文建模对手术视频解剖理解至关重要，ATLAS-120k的多样性使这种能力得以发挥。

## 7. 优点

- **数据集规模与多样性**：ATLAS-120k是首个涵盖14种术式、腹腔镜和机器人辅助双模式的解剖析语义分割数据集，标注质量通过可扩展管道（自动传播+迭代微调+外科医生验证）保证。
- **模型设计创新**：引入过程查询和阶段查询，将手术背景知识显式编码为可学习的上下文，并利用对比学习促进时序连贯性，同时保持轻量级（仅增加3M参数）。
- **实际可行性**：模型达到实时推理速度（64 FPS），且公开了数据集、模型和标注平台，可复现性强。
- **充分消融**：清晰展示了每个组件（预训练、时序传播、上下文查询）的独立贡献，验证了设计合理性。
- **评价指标全面**：包含检测、分割、时序一致性三方面。

## 8. 不足与局限

- **单一数据集验证**：仅在ATLAS-120k上实验，未在其他公开解剖分割数据集（如DSAD、CholecSeg8k）上测试跨域泛化能力，存在过拟合风险。
- **绝对性能偏低**：mDice仅0.49，尽管作者将原因归为类别长尾分布和挑战性，但这限制了实际临床应用的直接置信度。
- **训练周期偏短**：仅10 epochs，可能未充分收敛；模型较大（ViT-L 318M参数），是否充分学习长尾类别待验证。
- **对比方法不完全**：排除了与ATLAS-120k有预训练重叠的方法（如SurgeNetXL、LEMON），但可能这些模型本身也很强，缺少直接比较。
- **未详细阐述失败案例**：没有定性分析模型在哪些困难类或场景下失败，削弱了理解。
- **时序一致性指标局限**：mVC仅考虑标签一致区域，可能高估模型在背景区域的稳定性；且窗口大小固定（12/24帧），未分析更长时序一致性。
- **未涉及多模态数据**：数据集包含不同分辨率/设备，但模型未显式处理这些差异，可能影响泛化。

（完）
