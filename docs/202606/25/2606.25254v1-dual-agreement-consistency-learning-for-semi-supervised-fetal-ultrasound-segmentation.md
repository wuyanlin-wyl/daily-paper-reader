---
title: Dual Agreement Consistency Learning for Semi-Supervised Fetal Ultrasound Segmentation
title_zh: 双一致性协议学习在半监督胎儿超声分割中的应用
authors: "Fangyijie Wang, Guénolé Silvestre, Ziyang Wang, Kathleen M. Curran"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25254v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用CNN和Transformer的双一致性学习进行半监督胎儿超声分割
tldr: "针对胎儿超声图像像素级标注稀缺问题，提出DACL半监督分割框架，联合轻量级CNN和Transformer，通过双一致一致性损失（像素概率散度+熵引导置信度对齐）和mixup增强，在5%标注数据下Dice提升2.77%，HD95减少14.69mm，显著改善边界精度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25254v1/fig-001.webp\", \"caption\": \"Fig. 2. Visual comparison of latest methods (from 2024) when using 10% labeled data for testing. From left to right, they are four HC18 and four F-Abd samples.\", \"page\": 8, \"index\": 1, \"width\": 725, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2606.25254v1/fig-002.webp\", \"caption\": \"Table 1. The quantitative results on the HC18 dataset. The best results are in bold. The 2nd best results are in underline.\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2606.25254v1/fig-003.webp\", \"caption\": \"Table 2. The quantitative results on the F-Abd dataset. The best results are in bold. The 2nd best results are in underline.\", \"page\": 7, \"index\": 3, \"width\": 717, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2606.25254v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed Dual Agreement Consistency Learning (DACL) framework. A lightweight CNN and a Transformer are jointly trained using supervised learning on labeled data and cross pseudo supervision on unlabeled data. A dualagreement consistency loss enforces pixel-wise distribution alignment and uncertaintyaware agreement between their predictions, while mixup-based interpolation consistency with an EMA teacher further stabilizes training.\", \"page\": 3, \"index\": 4, \"width\": 708, \"height\": 542}]"
motivation: 胎儿超声图像像素级标注稀缺，现有自动分割方法性能受限。
method: 提出DACL框架，联合训练轻量级CNN和Transformer，利用双一致一致性损失（像素级概率散度与熵引导置信度对齐）和mixup一致性策略进行半监督学习。
result: "在5%标注数据下，Dice提升2.77%，HD95减少14.69mm，优于最新半监督方法。"
conclusion: 双一致一致性学习能有效利用未标注数据，实现标注高效的胎儿超声图像分割。
---

## 摘要
母胎超声是监测胎儿发育的主要成像方式，但由于像素级标注的稀缺，准确的自动分割仍然具有挑战性。为了解决这个问题，我们提出了DACL，一种用于稳健胎儿超声图像分割的半监督框架。DACL联合训练一个面向部署的轻量级卷积网络（1.47M参数）和一个基于Transformer的网络，利用标注数据进行监督学习，并通过CPS利用未标注数据。为了增强预测稳定性，我们引入了一种双一致一致性损失，将逐像素的概率散度与熵引导的置信度对齐相结合。与仅在预测层面强制一致的传统CPS方法不同，DACL明确规范了分布对齐和不确定性，从而抑制不可靠的伪标签，并在极端标注稀缺下实现稳定的跨架构伪标签学习。此外，使用mixup的基于插值的一致性策略应用于未标注样本以增强鲁棒性。在5%标注数据下，与最近最强的半监督方法相比，DACL将Dice提高了2.77%，将HD95降低了14.69 mm，在胎儿头部和腹部数据集上显示出边界准确性的显著提升。这些结果证明了基于一致的一致性学习对于标注高效的胎儿超声分割的有效性。我们的代码在GitHub上。

## Abstract
Maternal-fetal US is the primary imaging modality for monitoring fetal development, yet accurate automated segmentation remains challenging due to the scarcity of pixel-level annotations. To address this issue, we propose DACL, a semi-supervised framework for robust fetal US image segmentation. DACL jointly trains a deployment-oriented lightweight convolutional network (1.47\thinsp\mathrm{M} parameters) and a Transformer-based network, leveraging labeled data for supervised learning and unlabeled data via CPS. To enhance prediction stability, we introduce a dual-agreement consistency loss that couples pixel-wise probabilistic divergence with entropy-guided confidence alignment. Unlike conventional CPS methods that enforce agreement only at the prediction level, DACL explicitly regularizes both distributional alignment and uncertainty, thereby suppressing unreliable pseudo-labels and enabling stable cross-architecture pseudo-label learning under extreme annotation scarcity. Furthermore, an interpolation-based consistency strategy using mixup is applied to unlabeled samples to enhance robustness. Under 5% labeled data, DACL improves Dice by up to 2.77% and reduces HD95 by up to 14.69 mm compared with the strongest recent semi-supervised methods, demonstrating significant improvements in boundary accuracy on both fetal head and abdomen datasets. These results demonstrate the effectiveness of agreement-based consistency learning for annotation-efficient fetal US segmentation. Our code is on GitHub.

---

## 论文详细总结（自动生成）

# 论文总结：双一致性协议学习在半监督胎儿超声分割中的应用（Dual Agreement Consistency Learning for Semi-Supervised Fetal Ultrasound Segmentation）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：胎儿超声图像分割在临床中至关重要，但像素级标注极其稀缺且昂贵（需要医学专业知识）。现有深度学习方法依赖大量标注数据，限制了实际应用。
- **研究动机**：利用少量标注数据和大量未标注数据，通过半监督学习提升轻量级模型（适合部署）的分割性能，尤其改善边界精度。
- **整体含义**：提出一种名为 DACL 的半监督框架，通过双一致性协议（分布对齐+不确定性对齐）稳定跨架构伪标签学习，在胎儿头部（HC18）和腹部（F-Abd）数据集上显著优于最新半监督方法，实现了标注高效、边界精确的分割。

## 2. 论文提出的方法论
### 核心思想
- 联合训练一个轻量级 CNN（UNeXt, 1.47M参数）和一个 Transformer（Swin-Unet, 27.15M参数），利用标注数据进行监督学习，利用未标注数据进行交叉伪监督（CPS）。
- 引入**双一致一致性损失（Dual-Agreement Consistency Loss, DAC）**：结合像素级KL散度（概率分布对齐）和熵引导的置信度对齐（抑制不可靠伪标签）。
- 额外使用**基于 mixup 的插值一致性策略**，通过 EMA 教师模型稳定未标注数据的训练。

### 关键技术细节
- **网络架构**：
  - 轻量级 CNN: UNeXt（编码器通道数 {32,64,128,160,256}，仅 1.47M 参数，7.03 GFLOPs）
  - Transformer: Swin-Unet tiny（窗口大小7，参数27.15M，71.17 GFLOPs）
- **交叉伪监督（CPS）**：
  - 对未标注数据，分别生成两个模型的伪标签（one-hot），然后计算交叉熵和Dice损失，相互教导。
- **双一致一致性损失（DAC）**：
  - \( L_{KL} \)：像素级KL散度对齐两个模型的预测分布。
  - \( L_{ent} \)：熵正则项，鼓励两个模型在预测上一致且低不确定性（公式：H(p)+H(q)-H((p+q)/2)）。
- **插值一致性学习（ICT）**：
  - 对未标注数据做 mixup（比例0.5），让 Transformer 学生模型预测与 EMA 教师模型对相应样本 mixup 后的预测一致（MSE损失）。
- **总损失函数**：
  - \( L_{total} = (L_{sup}^1 + L_{sup}^2) + \lambda(L_{cps}^1 + L_{cps}^2) + \tau L_{ict} + \beta L_{dac} \)，超参数 λ=2.0, τ=2.0, β=10.0。
- **推理阶段**：仅使用轻量级 UNeXt 模型，无需 Transformer。

## 3. 实验设计
### 数据集
- **HC18**（胎儿头部）：500训练、50验证、449测试，图像尺寸 800×540，来自荷兰（GE设备）。
- **F-Abd**（胎儿腹部）：1084训练、139验证、922测试，图像尺寸 744×562，来自低收入国家（便携式探头）。
- 全部转为 RGB 格式以适配预训练 Transformer。

### Benchmark
- 采用5%和10%标注数据两种设置（剩余为未标注数据）。
- 对比方法：MT (2017)、DCT (2018)、CPS (2021)、ICT (2022)、CTCT (2022)、PCPCS (2024)、DSTCT (2024)、LMCT (2025) 等8种近期半监督方法。
- 还对比了全监督（100%数据）的 UNeXt 和 Swin-Unet 作为上界参考。

### 公平性设置
- 所有 SSL 框架均使用相同的基础网络：CNN-Transformer 联合训练时用 UNeXt + Swin-Unet；若方法本身只支持同构网络，则用两个 UNeXt。
- 所有方法训练超参数一致（优化器、学习率、批次大小、epoch数等）。

## 4. 资源与算力
- **文中明确说明**：单张 NVIDIA 4090 GPU，Python 3.11.5，PyTorch 2.1.2，CUDA 12.2。
- **训练轮数**：400 epoch，标注批次大小1，未标注批次大小4。
- **未提及**：训练总耗时（小时/天数）、显存占用等细节。

## 5. 实验数量与充分性
- **主要实验**：
  - 两个数据集（HC18和F-Abd），每个数据集两种标注比例（5%和10%），共4组主要对比实验。
  - 每个实验报告4项指标：Dice↑、Jaccard↑、HD95↓、ASD↓，并给出标准差和p值（与最强基线LMCT比较）。
- **消融实验**：在HC18和F-Abd的10%标注设置下，逐步添加Lcps、Lict、Ldac，共5组（Baseline, +Lcps, +Lcps+Lict, +Lcps+Ldac, 完整）。
- **定性可视化**：展示了8个样本（4个HC18, 4个F-Abd）的预测结果对比。
- **充分性评价**：实验较为充分，覆盖两个不同难度数据集、多种标注比例、与所有近年主流方法对比，并进行了消融分析。p值统计表明DACL在多数设置下显著优于对手。唯一不足是未在更多数据集（如其他解剖结构）上验证。

## 6. 论文的主要结论与发现
- DACL在5%标注下显著优于所有对比方法：HC18上Dice提升1.07%（达95.05%）、HD95降低4.47mm；F-Abd上Dice提升4.47%，HD95降低24.91mm。
- 10%标注下，HC18上与最强方法LMCT持平（p=0.974），但Dice和HD95仍有微小优势；F-Abd上Dice略低于某些方法，但边界指标（HD95, ASD）仍最优。
- 轻量级UNeXt在DACL训练后性能大幅超越普通全监督UNeXt，且接近全监督Swin-Unet的上界。
- 消融实验证实每个组件均有贡献：CPS提供基础伪标签，ICT进一步提升精度，DAC对边界质量贡献最大。

## 7. 优点
- **方法创新**：首次在胎儿超声分割中联合使用CNN-Transformer异构网络，并设计双一致性损失（KL+熵对齐），同时解决分布对齐和不确定性，避免错误伪标签。
- **部署友好**：推理仅用轻量级CNN（1.47M参数），适合临床实时应用。
- **边界精度突出**：HD95和ASD指标显著优于对比方法，在超声图像中边界模糊的问题上表现优异。
- **实验严谨**：控制变量（相同基础网络、超参数），提供p值和标准差，消融实验设计合理。

## 8. 不足与局限
- **数据集局限性**：仅两个胎儿结构（头、腹），未在更多解剖结构（如股骨、胸等）或其他超声场景验证。
- **泛化性**：F-Abd数据集来自低端设备，图像噪声大，DACL在Dice上未在所有设置下最佳（10%标注下Dice略低于某些方法），说明方法可能在特定低质数据上仍有改进空间。
- **算力需求**：训练时需要同时运行CNN和Transformer两个大模型（Transformer参数27M），对显存有一定要求，但论文未报告具体训练时长和显存占用。
- **超参数敏感性**：论文未讨论超参数λ, τ, β的调参过程，可能对最优性能有影响。
- **无医学验证**：仅用数值指标，未进行临床有效性评估（如测量误差等）。

（完）
