---
title: "Self-Questioning Vision-Language Models: Reinforcement Learning for Compositional Visual Reasoning"
title_zh: 自我提问的视觉语言模型：用于组合视觉推理的强化学习
authors: Saraswathy Amjith
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15651v1"
tags: ["query:vl"]
score: 9.0
evidence: 用于视觉语言模型的自我提问框架
tldr: "针对VLM在组合视觉推理中难以自动分解多步骤问题，本文提出自提问框架，利用GRPO强化学习训练模型生成子问题并逐步回答，无需人工标注分解示例。在3B参数模型上，自提问方法在A-OKVQA上达到52.2%准确率，优于未训练模型46.8%，表明中间子问题奖励有助于发现组合分解策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15651v1/fig-001.webp\", \"caption\": \"Figure 2. Examples from the two evaluation benchmarks. Top: A-OKVQA, which pairs real-world images with open-ended questions requiring external knowledge. Bottom: CLEVR, which poses compositional reasoning questions over synthetic 3D scenes with geometric objects.\", \"page\": 5, \"index\": 1, \"width\": 350, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-002.webp\", \"caption\": \"Figure 3. Overview of image processing in Qwen2.5-VL. The input image is divided into 14×14 patches, which are encoded by a Vision Transformer (ViT) into a sequence of vision tokens. These vision tokens are interleaved with text tokens in the model’s input sequence, delimited by special <img> tokens.\", \"page\": 5, \"index\": 2, \"width\": 350, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-003.webp\", \"caption\": \"Table 3. GRPO training hyperparameters, matched across all models and datasets.\", \"page\": 9, \"index\": 3, \"width\": 454, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2606.15651v1/fig-004.webp\", \"caption\": \"Figure 1. Overview of the self-questioning framework. Given an image and question, the VLM generates sub-questions, answers each by attending to the image, and produces a final answer. The reward function requires both correct format and correct answer; GRPO uses group-normalized advantages to update the policy.\", \"page\": 3, \"index\": 4, \"width\": 350, \"height\": 520}]"
motivation: 现有VLM在组合视觉推理中需要人工编写步骤解释，成本高且难以扩展。
method: 提出自提问框架，使用GRPO强化学习训练VLM分解视觉问题为子问题并回答，仅通过奖励信号（子问题存在性和最终答案正确性）引导。
result: "在A-OKVQA上，自提问VLM准确率52.2%，标准RL 51.6%，均高于基线46.8%。"
conclusion: 教会AI系统自问中间问题是有前景的复杂视觉推理策略，尤其适合需要明确逐步分解的难题。
---

## 摘要
视觉语言模型（VLM）是同时处理图像和文本的AI系统，但它们在处理需要将多个步骤（如识别物体、计数和比较结果）串联起来的组合视觉推理问题时常常遇到困难。现有方法通过训练模型使用人工编写的逐步解释来改进这种推理，但创建这些注释成本高昂且难以扩展。我们提出了一个自我提问框架，使用称为组相对策略优化（GRPO）的强化学习算法，训练VLM将视觉问题分解为更小的子问题，并在生成最终答案之前逐一回答每个子问题。模型从未被展示如何分解问题的示例，它是在奖励信号的引导下自行发现这一行为的，该信号评估输出是否包含子问题以及最终答案是否正确。我们将此框架应用于一个30亿参数的模型，在合成几何形状场景（CLEVR）和真实世界照片（A-OKVQA）上进行训练。在A-OKVQA上，自我提问和标准强化学习都显著提高了未训练模型的准确性（52.2%和51.6%对比46.8%）。我们引入了第一个自我提问的VLM，不仅像标准RL那样奖励最终答案，还额外奖励生成中间子问题，使其能够发现组合分解策略。这些结果表明，教AI系统向自己提出中间问题是实现复杂视觉推理的一种有前景的策略，特别是当问题的难度需要明确的逐步分解时。

## Abstract
Vision-Language Models (VLMs) are AI systems that process both images and text, yet they often struggle with compositional visual reasoning questions that require chaining multiple steps together, such as identifying objects, counting them, and comparing the results. Existing approaches improve this reasoning by training models on human-written step-by-step explanations, but creating these annotations is expensive and difficult to scale. We propose a self-questioning framework that trains a VLM to break visual questions into smaller sub-questions and answer each one before producing a final response, using a reinforcement learning algorithm called Group Relative Policy Optimization (GRPO). The model is never shown examples of how to decompose questions, it discovers this behavior on its own, guided by a reward signal that scores whether the output contains sub-questions and whether the final answer is correct. We apply this framework to a 3-billion-parameter model, training on both synthetic scenes of geometric shapes (CLEVR) and real-world photographs (A-OKVQA). On A-OKVQA, both self-questioning and standard reinforcement learning substantially improve accuracy over the untrained model (52.2% and 51.6% vs. 46.8%). We introduce the first self-questioning VLM by rewarding not only the final answer like standard RL but additionally for generating intermediate sub-questions, enabling it to discover compositional decomposition strategies. These results suggest that teaching AI systems to ask themselves intermediate questions is a promising strategy for complex visual reasoning, particularly when the difficulty of a question warrants explicit step-by-step decomposition.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：当前视觉语言模型（VLM）在处理组合视觉推理任务（如多步识别、计数、比较）时表现不佳，原因在于它们通常依靠单次前向传播直接输出答案，缺乏显式的中间推理步骤。
- **现有方法局限**：已有的改进方法（如链式思维提示、监督微调、视觉编程）要么依赖人工编写的逐步推理示例（成本高、难扩展），要么需要预定义工具库，泛化性不足。
- **整体目标**：探索是否可以通过强化学习（RL），让VLM在没有任何人工分解示例的情况下，自主学会将复杂视觉问题拆解为子问题并逐步回答，从而提升组合推理能力。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：训练VLM在回答之前，先生成一系列子问题-子答案对，再综合给出最终答案；整个过程只通过奖励信号（奖励子问题格式+最终答案正确性）驱动，模型自行发现分解策略。
- **关键技术**：
  - **自提问格式**：模型输出必须包含 `<sub_q>` 和 `<sub_a>` 标记（实际使用“Sub-question i: ... Answer i: ...”文本格式）。
  - **奖励函数**：若输出同时满足格式要求（至少一个子问题-答案对）且最终答案正确，奖励+1.0；否则-1.0。
  - **训练算法**：采用 **组相对策略优化（GRPO）**，相比PPO无需独立价值网络，节省显存。对每个提示采样G个回复，计算组内标准化优势，并用KL散度惩罚防止灾难性遗忘。
- **公式（文字说明）**：  
  - 组标准化优势：`A_i = (R_i - μ_g) / σ_g`  
  - 策略更新目标：包含截断的重要性采样项和β加权的KL散度惩罚。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **CLEVR**：合成几何场景，组合推理问题（约10,000训练样本）。
  - **A-OKVQA**：真实世界照片，开放域视觉问答，需外部知识（约10,000训练样本）。
- **基准**：采用Qwen2.5-VL-3B-Instruct作为基础模型，评估500个验证样本的准确率。
- **对比方法**：
  - 基础模型（未训练）
  - **Direct+GRPO**：标准RL训练，直接答案无分解，奖励仅依赖最终答案正确性。
  - **SQ+GRPO**：自提问格式训练，奖励包含格式和答案正确性。
  - 交叉评估：CLEVR-trained SQ模型在A-OKVQA测试，反之亦然；以及直接模型使用自提问提示的消融。

## 4. 资源与算力

- **GPU型号**：文中提到“48 GB each”，推测使用单个或多个48G显存的GPU（如NVIDIA A6000或类似）。
- **数量与时长**：未明确说明GPU数量和具体训练时间，仅提及每个模型训练约7,000步，批次大小（有效）为4，采样温度0.7。
- **备注**：由于使用LoRA（r=16, α=32, 目标q_proj, v_proj），参数更新量小于1%，因此训练在学术级硬件上可行。但具体算力细节不够透明。

## 5. 实验数量与充分性

- **主要实验组**：两个数据集（CLEVR, A-OKVQA）下，各训练4个模型（SQ+GRPO, Direct+GRPO 分别在各自数据集，再加上基础模型），共9个评估配置（含交叉评估和提示消融）。
- **消融实验**：
  - 提示消融：SQ模型用直接提示评估。
  - 交叉转移：CLEVR-trained模型在A-OKVQA评估。
  - 领域内/跨领域对比。
- **充分性评估**：实验覆盖了主要假设（RL是否提升、格式是否额外增益、是否转移），但仅评估500样本（可能不够大），且未进行多轮重复或统计学显著性检验。总体设计较公平，但样本量有限，结论需要谨慎。

## 6. 主要结论与发现

- **RL训练是主要提升来源**：在A-OKVQA上，Direct+GRPO提升+4.8%（46.8%→51.6%），SQ+GRPO提升+5.4%（46.8%→52.2%），差值仅0.6%。说明标准RL本身通过校准输出分布带来大部分增益。
- **自提问格式的边际优势**：在A-OKVQA上额外增益有限；但在跨域转移中表现突出（CLEVR-trained SQ在A-OKVQA上+2.6%，而Direct同期仅+0.2%），表明分解策略本身是可迁移的通用技能。
- **格式税（Format Tax）**：在简单任务上（如CLEVR颜色识别），自提问格式引入额外开销导致准确率下降（下降16.4%），但使用直接提示恢复性能，证明模型能力未丢失。
- **定性发现**：模型学会了格式合规（100%），但在简单问题上子问题常是原问题复述，而非有效分解；在需要多步推理的任务（计数、材料识别）上增益明显。

## 7. 优点

- **方法创新**：首次将GRPO应用于VLM，并通过奖励子问题格式促使模型自主发现分解策略，完全不需要人工推理标注。
- **跨域迁移验证**：展示了从合成图像到真实图像的推理策略迁移，证明分解技能是领域通用的。
- **增量消融设计**：通过Direct+GRPO和SQ+GRPO的对比，分离了RL训练效果和格式效果，实验设计严谨。
- **低成本训练**：使用3B参数模型和LoRA微调，在有限硬件上可行，实用性高。

## 8. 不足与局限

- **样本量小**：每组评估仅500样本，可能导致统计波动，缺乏置信区间。
- **模型规模受限**：仅测试3B参数模型，更大模型（7B/72B）或全参数微调可能表现不同，结果泛化性有限。
- **子问题质量未控制**：模型可能生成“伪子问题”（如原问题复述），但未引入奖励或惩罚来区分有效分解与表面格式遵守。
- **格式税问题未解决**：对于简单问题，强制分解反而有害，论文未提出自适应策略。
- **算力细节不充分**：未明确GPU数量、总训练时长，可复现性较弱。
- **评估基准单一**：仅两个数据集，且A-OKVQA部分问题可能不真正需要多步分解，限制了结论范围。

（完）
