---
title: "Beyond Visual Cues: CoT-Enhanced Reasoning for Semi-supervised Medical Image Segmentation"
title_zh: 超越视觉线索：CoT增强推理用于半监督医学图像分割
authors: "Yuming Chen, Yuxin Xie, Tao Zhou, Yi Zhou"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17958v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学图像分割的CoT推理机制
tldr: 针对半监督医学图像分割中视觉依赖导致的视觉-语义不匹配问题，提出CERS框架，集成Chain-of-Thought推理，通过构建语言知识池、语义感知参考选择和多尺度坐标注意力模块，有效融合推理上下文，在边界模糊和语义不一致任务上超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17958v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison in terms of Dice (%) and IoU (%) on MosMedData+, QaTa-COV19 and BRISC 2025 with 50% and 25% labeled ratio. Bold and underlined entries indicate the best and second-best results respectively.\", \"page\": 7, \"index\": 1, \"width\": 725, \"height\": 848}, {\"url\": \"assets/figures/arxiv/2606.17958v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on different retrieval strategies of CERS. Latency refers to the time overhead incurred per training step due to the retrieval computation.\", \"page\": 8, \"index\": 2, \"width\": 579, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2606.17958v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of CERS. A frozen LLM generates CoTs, and labeled CoTs form a knowledge pool for imageCoT hybrid retrieval. Retrieved context is fused with image features via MCAM and used by a retrieval-aware decoder to enable reasoning-guided semi-supervised segmentation beyond visual cues.\", \"page\": 3, \"index\": 3, \"width\": 725, \"height\": 290}]"
motivation: 现有半监督医学图像分割方法依赖视觉模式匹配，在视觉相似但病理不同的临床场景中失效。
method: 提出CERS框架，利用大语言模型生成推理描述构建知识池，通过语义感知参考选择和CoT一致性过滤候选，并用多尺度坐标注意力模块融合推理上下文。
result: 实验表明CERS在解决边界模糊和语义不一致问题上显著优于当前最优方法。
conclusion: CERS通过引入语言推理有效解决了半监督医学图像分割中的视觉-语义不匹配问题。
---

## 摘要
半监督医学图像分割已成为医学图像分析中的主导研究问题，通过利用未标记数据的一致性正则化来缓解标注稀缺。然而，现有方法主要依赖视觉模式匹配，严重依赖像素级相似性。这种视觉中心依赖性在视觉-语义错配的临床场景中常常失效——视觉上相似的病变可能得出截然不同的诊断结论，从而无法捕捉专家使用的底层诊断逻辑。为解决此问题，我们超越视觉线索，提出CERS（CoT增强推理分割），一种集成思维链（CoT）推理以区分病理上不同病例的框架。具体来说，我们构建了一个知识池，其中包含由大语言模型（LLMs）生成的富含语言推理描述。引入一种语义感知的参考选择策略来识别历史证据，先通过形态学筛选候选，再通过CoT一致性进行细化以消除硬负样本。此外，设计了多尺度坐标注意力模块（MCAM）将推理得到的上下文有效融合到解码过程中。大量实验表明，CERS相比最先进的方法具有优越性，尤其在解决边界模糊和语义不一致方面。代码可在https://github.com/cymasuna/CERS获取。

## Abstract
Semi-supervised medical image segmentation has emerged as a dominant research problem in medical image analysis, mitigating annotation scarcity by leveraging consistency regularization on unlabeled data. However, existing approaches operate predominantly via visual pattern matching, relying heavily on pixel-level similarities. This visual-centric dependency often falters in clinical scenarios characterized by the visual-semantic mismatch, where visually similar lesions warrant distinct diagnostic conclusions, thus failing to capture the underlying diagnostic logic used by experts. To address this, we move beyond visual cues and propose CERS (CoT-Enhanced Reasoning Segmentation), a framework that integrates Chain-of-Thought (CoT) reasoning to distinguish pathologically distinct cases. Specifically, we construct a knowledge pool enriched with linguistic reasoning descriptions generated by large language models (LLMs). A semantic-aware reference selection strategy is introduced to identify historical evidence, filtering candidates first by morphology, and then refining them via CoT consistency to eliminate hard negatives. Furthermore, a multi-scale coordinate attention module (MCAM) is designed to effectively fuse this reasoning-derived context into the decoding process. Extensive experiments demonstrate the superiority of CERS against state-of-the-art approaches, particularly in resolving boundary ambiguities and semantic inconsistencies. The code is available at https://github.com/cymasuna/CERS.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义
半监督医学图像分割普遍依赖像素级视觉模式匹配，在临床场景中常面临**视觉-语义不匹配**：即使图像外观高度相似，病理结论可能截然不同。现有方法无法捕捉诊断推理逻辑，导致边界模糊、低对比度、伪影干扰等情况下出现假阳性或漏检。本文提出 **CERS（CoT-Enhanced Reasoning Segmentation）** 框架，将 **Chain-of-Thought (CoT) 语言推理** 融入半监督分割，利用大语言模型生成的推理描述作为语义指导，从而区分视觉相似但病理不同的区域，超越纯视觉线索的局限。

### 2. 方法论
- **核心思想**：构建一个由标注样本 CoT 描述组成的知识池，对每个输入样本执行 **粗到细的混合检索**（先图像相似度筛选，再 CoT 相似度精排），过滤掉视觉相似但诊断不一致的硬负样本；检索到的上下文通过 **多尺度坐标注意力模块 (MCAM)** 融合到解码过程中，并采用 **双解码器一致性** 进行半监督学习。
- **关键技术细节**：
  - **知识池构建**：使用 GPT-5.2 为每个样本生成四维（纹理、边界、上下文、总结）CoT 描述；标注 CoT 经过图像编码器与 CoT 编码器（CXR-BERT）提取嵌入，通过对比学习对齐，存入知识池（含可视化原型与 CoT 原型）。
  - **混合检索策略**：第一步图像相似度检索获取 top-K 形态相似候选；第二步在候选内按 CoT 相似度重排序，保留 top-M 最终证据集。
  - **MCAM 模块**：以当前特征为 query，检索特征为 key/value，通过多头交叉注意力融合；再结合坐标注意力（水平和垂直方向）与多尺度分支，增强空间敏感度和细节捕获。
  - **学习目标**：监督损失（交叉熵+Dice）+ 对比损失（图像与 CoT 对齐）+ 一致性损失（标准解码器与检索感知解码器输出之间）。知识池可视原型通过 EMA 更新。
- **公式/算法流程**（文字说明）：训练分两阶段：第一阶段用标注样本进行有监督热身（式1、2），固定编码器后构建知识池；第二阶段基于热身模型，对每个样本执行上述混合检索，将检索结果输入 DR 解码器，并通过一致性损失（式6）将推理知识传递到标准解码器 DS。总损失如式7。

### 3. 实验设计
- **数据集**：
  - MosMedData+：2729 张胸部 CT（COVID-19 感染）
  - QaTa-COV19：9258 张胸部 X 光（COVID-19 感染）
  - BRISC 2025：4793 张脑 MRI（肿瘤/病灶）
  每种按 8:1:1 划分训练/验证/测试。
- **Benchmark 与对比方法**：与 BCP、DuCiSC、LeFeD、LViT、MT、UCMT 六种 SOTA 半监督方法比较，并包含 U-Net（全监督）基线。统一使用 ConvNeXt 骨干（LViT 和 LeFeD 使用各自原始骨干）。
- **设置**：半监督比例为 50% 和 25% 标注，评估指标 Dice 和 IoU。还做了消融实验（不同检索策略、不同 top-K、不同解码器、MCAM 内部组件）。

### 4. 资源与算力
文中明确说明：**所有实验在单个 NVIDIA RTX 4090 GPU 上运行**。训练超参数：batch size 16，epochs 200，优化器 SGD（初始 lr=1e-2，weight decay=1e-4）。未给出具体训练耗时。

### 5. 实验数量与充分性
- **主要对比实验**：在三个数据集、两种标注比例下与 6 种方法比较，共 6 张表格（表1汇总）。并单独给出全监督比较。
- **消融实验**：
  - 表2：比较无检索、仅图像检索、仅 CoT 检索、两者联合，并报告每步延迟。
  - 图4：不同 top-K 值（3/5/7）在不同标注比例下的性能。
  - 表3：替换 MCAM 为 U-Net 解码器、Swin U-Net 解码器的对比。
  - 内部消融：移除坐标注意力或多尺度分支后的性能下降（文中以数字给出）。
- **定性结果**：图3展示了三个数据集上的示例分割对比。
- **充分性评估**：实验覆盖了主要对比、检索策略、模块设计及超参数敏感性，对比方法合理，消融齐全，结论可信。但缺少与其他近期推理增强方法（如 SAM 系列）的比较，且在 BRISC 数据集 25% 标注下 Dice 略低于 LViT（80.34 vs 80.51），说明并非所有场景都绝对最优。

### 6. 主要结论
- CERS 在多数指标上超越现有 SOTA 方法，尤其是在边界模糊、低对比度、视觉混淆等挑战场景中，能更准确定位病灶并保持边界结构。
- 引入结构化的 CoT 推理描述有效“架起”了视觉外观与诊断逻辑之间的桥梁，解决了纯视觉方法难以处理的视觉-语义不匹配问题。
- 粗到细的混合检索策略优于单一模态检索，MCAM 能高效融合推理上下文，且不显著增加参数量。

### 7. 优点
- **创新性强**：率先将 CoT 推理系统性地引入半监督医学分割，并明确针对视觉-语义不匹配问题。
- **方法设计精巧**：粗到细检索有效过滤硬负样本；双解码器一致性无痕传递推理知识；EMA 更新保证检索稳定性。
- **实验充分**：覆盖多模态数据集（CT、X 光、MR），多种标注比例，并与六种 SOTA 方法对比，消融实验深入。
- **可复现性**：开源代码，实验设置明确。

### 8. 不足与局限
- **依赖 LLM 输出质量**：CoT 生成依赖 GPT-5.2，虽经专家验证准确率 >95%，但仍有错误风险；LLM 产生的描述可能带有系统性偏见。
- **计算开销**：检索步骤增加每步延迟（约 0.1s），且依赖外部 LLM 和检索机制，难以端到端轻量化。
- **实验覆盖有限**：仅针对感染/肿瘤分割，未评估其他医学任务（如器官、血管）；仅在三个公开数据集上验证，缺乏大规模临床验证。
- **方法与设定上的局限**：CoT 编码使用 CXR-BERT，可能不适合非胸部图像（如 BRISC 脑 MRI）；未与近期基于 SAM 或大模型的方法比较；25% 标注下在 BRISC 数据集上并未最优，说明泛化性有提升空间。
- **潜在偏差**：文本标注来自自动生成或模板，可能与真实诊断报告存在差异；知识池仅含标注样本，未标签数据无法写入，可能限制检索多样性。

（完）
