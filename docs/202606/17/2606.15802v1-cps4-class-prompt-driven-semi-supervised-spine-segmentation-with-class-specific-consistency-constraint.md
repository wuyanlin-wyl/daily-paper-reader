---
title: "CPS4: Class Prompt driven Semi-Supervised Spine Segmentation with Class-specific Consistency Constraint"
title_zh: "CPS4: 类提示驱动的半监督脊柱分割与类特定一致性约束"
authors: "Qingtao Pan, Hongzan Sun, Bing Ji, Shuo Li"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15802v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用视觉语言模型进行半监督脊柱分割，利用类别提示
tldr: "本文提出CPS4，首个利用视觉语言模型（VLM）的文本类提示增强半监督脊柱分割伪标签质量的方法。通过两阶段训练：类特定一致性约束的VLM预训练阶段，优化类提示与脊柱单元一致性；类提示驱动的半监督分割阶段，生成多类分割图提升伪标签质量。在公开数据集上仅用5%标注数据达到80.44% Dice，优于现有方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15802v1/fig-001.webp\", \"caption\": \"Table 1. The comparative experiments on MRSpineSeg dataset demonstrate the strong semi-supervised spine segmentation capability of our method. The mean Dice and mIoU values are reported and the best results are highlighted in bold.\", \"page\": 6, \"index\": 1, \"width\": 683, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2606.15802v1/fig-002.webp\", \"caption\": \"Fig. 3. Our method shows superior segmentation visualization results on 5% labeled data, compared to SSL and VLM methods.\", \"page\": 6, \"index\": 2, \"width\": 640, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2606.15802v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of our CPS4. In stage 1, we perform class-specific consistency constrained VLM pretraining to train the vision and text encoder, where token- and pixellevel attention losses are proposed to boost the consistency between class prompts and spine units. Note: In stage 1, we only show the calculation process of {Lumbar Vertebra 1} class, for other classes in the MRI, the calculation process is consistent. In stage 2, we conduct class prompt–driven semi-supervised segmentation to train the segmentation network, where the pseudo label is enhance through class prompt generated segmentation map.\", \"page\": 3, \"index\": 3, \"width\": 725, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2606.15802v1/fig-004.webp\", \"caption\": \"Fig. 1. Our proposed CPS4 generates class prompt guided segmentation map, enhancing the quality of pseudo labels in semi-supervised spine segmentation.\", \"page\": 2, \"index\": 4, \"width\": 725, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.15802v1/fig-005.webp\", \"caption\": \"Fig. 4. (a) Ablation study on various components demonstrates the effectiveness of each component in CPS4. (b) Ablation on different ratio of data labeled shows that CPS4 has better data robustness with a subtle decrease when the labeled training data reduce to 5%.\", \"page\": 7, \"index\": 5, \"width\": 642, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2606.15802v1/fig-006.webp\", \"caption\": \"Fig. 5. Visualization of class-specific attention maps. The first row shows ground-truth annotations, the second row presents attention maps produced by our method, and the third row shows results from MedCLIP. Our method exhibits stronger consistency between class prompts and spine units, with attention highly aligned to each corresponding spine units.\", \"page\": 7, \"index\": 6, \"width\": 645, \"height\": 239}]"
motivation: 现有半监督脊柱分割中伪标签质量受限于缺乏显式约束确保脊柱类提示与区域一致性。
method: 提出CPS4，包括类特定一致性约束的VLM预训练（token和像素级注意力损失）和类提示驱动的半监督分割阶段，将二值图融合为多类分割图。
result: "在公开脊柱分割数据集上仅用5%标注数据达到80.44% Dice，超越流行半监督学习和VLM方法。"
conclusion: 类提示约束有效提升半监督脊柱分割性能，代码将开源。
---

## 摘要
视觉语言模型通过利用文本类提示生成分割图，在半监督脊柱分割中有很大潜力提升伪标签质量，但尚未有人研究。尽管有前景，但缺乏明确约束来确保脊柱类提示与脊柱单元区域之间的一致性，导致多类分割图生成性能不佳。本文提出CPS4，首个使用类提示增强脊柱伪标签质量的文本引导半监督脊柱分割网络。具体通过两个训练阶段实现：(i)类特定一致性约束的VLM预训练阶段：提出token级和像素级注意力损失来优化类提示与脊柱单元之间的一致性，迫使文本类提示在语义空间中与目标脊柱单元紧密耦合。(ii)类提示驱动的半监督脊柱分割阶段：使用预训练的视觉-文本编码器，为未标记脊柱图像导出每个类特定的二值分割图，并将其整合为统一的多类分割图，提高半监督脊柱分割网络生成的脊柱伪标签质量。实验结果显示，CPS4在公共脊柱分割数据集上仅使用5%标记数据，Dice达到80.44%，超越了流行的半监督学习和VLM方法。代码将公开。

## Abstract
Vision Language Model (VLM) has great potential to enhance the quality of pseudo labels in semi-supervised spine segmentation by leveraging textual class prompts to generate segmentation map, but no one has studied it yet. Although promising, it lacks explicit constraints to ensure consistency between spine class prompts and spine unit region, resulting in unsatisfactory performance in multi-class segmentation map generation. In this paper, we propose CPS4, the first text-guided semi-supervised spine segmentation network using class prompts to enhance the quality of spine pseudo labels. Specifically, CPS4 is implemented through two training stages. (i) Class-specific consistency constrained VLM pretraining stage: we propose token- and pixel-level attention loss to optimize the consistency between class prompts and spine units, forcing the textual class prompt to be closely coupled with the target spine unit in the semantic space. (ii) Class Prompt driven semi-supervised spine segmentation stage: using the pretrained vision-text encoder, we derive each class-specific binary segmentation map for the unlabeled spine image and integrate them into an unified multi-class segmentation map, improving the quality of the spine pseudo label generated by the semi-supervised spine segmentation network. Experimental results show that our CPS4 achieves superior spine segmentation performance with Dice of 80.44%, only using 5% labeled data on the public spine segmentation dataset, surpassing popular semi-supervised learning and VLM methods. Our code will be available.

---

## 论文详细总结（自动生成）

### 论文深度分析与总结：CPS4（类提示驱动的半监督脊柱分割）

#### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在医学图像分析中，自动脊柱分割（如椎体和椎间盘的多类分割）高度依赖大量像素级的精细标注，然而手动标注非常耗时且需要专业知识。半监督学习利用少量标注数据和大量未标注数据来缓解标注稀缺问题。但现有的半监督方法，尤其是基于一致性正则化和伪标签的方法，生成的伪标签质量不高，容易引入噪声监督，误导模型学习。
- **研究动机**：视觉语言模型（VLM）通过学习图像与文本的关联，能够利用文本描述理解未标注图像的语义信息，有潜力通过生成类提示引导的分割图来提升伪标签质量。**然而，直接将现有VLM应用于半监督脊柱分割存在挑战**：现有的VLM主要进行图像级别的对齐，缺乏显式约束来确保**类提示（class prompts）与图像中对应的脊柱单元（spine unit）之间的一致性**。这会导致类提示与目标区域在语义空间中的耦合不强，产生类间干扰，从而生成低质量的类提示引导分割图。
- **整体含义**：论文旨在解决如何有效利用文本类提示来提升半监督脊柱分割中伪标签质量这一根本问题，首次将文本类提示引入该领域。

#### 2. 方法论：核心思想、关键技术细节与流程

**核心思想**：提出一个两阶段的半监督分割框架（CPS4），通过**显式约束类提示与脊柱单元的一致性**来生成高质量的伪标签，从而指导半监督分割网络的训练。

**关键技术细节与算法流程**：

- **阶段一：类特定一致性约束的VLM预训练**
    - **目标**：训练视觉编码器和文本编码器，使得每个类别的文本提示（如“Lumbar Vertebra 1”）能够精确地定位并关注到图像中对应的脊柱区域。
    - **核心技术**：提出两种新的注意力损失函数来约束一致性。
        - **Token-级注意力损失**：计算类提示Token与图像所有Patch Token的相似度，生成粗略注意力图。该损失**鼓励注意力图的主要激活区域集中在目标脊柱单元（前景区域）**，抑制对其他无关区域的响应。
        - **像素级注意力损失**：在Token级损失的基础上，引入交叉熵目标，防止VLM将注意力过度集中在目标脊柱单元的某些子区域，**促使注意力图在目标区域内分布更均匀**。
    - **预训练优化目标**：两个损失的均值，即 `1/2 * (L_token + L_pixel)`。

- **阶段二：类提示驱动的半监督脊柱分割**
    - **目标**：利用阶段一预训练好的视觉-文本编码器，提升半监督分割网络（基于Mean Teacher框架）生成的伪标签质量。
    - **流程**：
        1.  **类引导分割图生成**：使用预训练编码器，为未标注图像中**每个类别**计算其文本提示与图像Patch Token的点积，生成**每个类别的二值注意力图**（通过OTSU自动阈值化）。最后将所有类别的二值图堆叠，形成一个统一的多类分割图 `y_text`。
        2.  **伪标签生成**：教师网络 `fθt` 为未标注图像生成伪标签 `y_tu`。
        3.  **联合优化**：学生网络 `fθs` 在以下损失的监督下优化：
            -   **监督损失**：对于标注图像，计算学生网络预测与真值之间的交叉熵损失 `L_sup`。
            -   **半监督损失**：包含两个部分：
                -   `L_text_semi`：学生网络预测 `y_su` 与预先生成的类引导分割图 `y_text` 之间的交叉熵损失。
                -   `L_enhance_semi`：学生网络预测 `y_su` 与**融合后的增强伪标签**（教师网络伪标签 `y_tu` + 类引导分割图 `y_text`）之间的交叉熵损失。

#### 3. 实验设计

- **数据集**：使用**MRSpineSeg数据集**，包含215个受试者的T2加权脊柱MR体数据，共20个类别（19个脊柱结构和背景）。
- **基准与评估指标**：采用**平均Dice系数**和**平均IoU**作为主要评价指标。
- **对比方法**：
    - **无文本提示的方法**：MT、BCP、MC-Net、SS-Net、UCMT等经典半监督学习方法。
    - **有文本提示的VLM方法**：CLIP、MedCLIP、MGCA、EGMA、DuSSS、GraphCL等。
- **实验设置**：在**5%、10%、25%、50%** 四种不同标注比例下进行评估，以验证模型在不同数据稀缺程度下的性能。

#### 4. 资源与算力

- **文中明确提到**：实验环境为Ubuntu 20.04.4 LTS操作系统，配备**1块24GB显存的NVIDIA V100 GPU**。使用PyTorch框架实现。
- **未明确指出**：文中未明确说明预训练和分割网络训练的具体总时长（如使用了多少个GPU小时）。虽然提及了训练轮次（300 epochs），但未给出总耗时。

#### 5. 实验数量与充分性

- **实验数量**：
    - **主对比实验**：在4种标注比例下，与11种方法进行对比，覆盖了广泛的基线。
    - **消融研究**：在5%标注数据设置下，验证了`Token损失`、`Pixel损失`各自及组合的有效性。
    - **数据鲁棒性分析**：通过将CPS4和MT基线在4种标注比例下进行对比，展示了模型对数据量的鲁棒性。
    - **注意力可视化**：对类特定注意力图进行了定性可视化对比（与MedCLIP）。
- **充分性与公平性**：
    - **充分**：实验覆盖了多个主流对比方法、不同标注比例和多种消融组件，验证全面。
    - **客观/公平**：对比方法都来自近年的顶级会议或期刊，且在同一数据集下进行评测，实验设置较为规范。但未进行跨数据集泛化性测试或对噪声标签的鲁棒性分析。

#### 6. 主要结论与发现

1.  **性能卓越**：CPS4在所有标注比例（5%、10%、25%、50%）下，Dice和IoU指标**均超越所有对比方法**（包括无文本提示和有文本提示的方法）。
2.  **低标注下的优势**：在标注数据极度稀缺时（5%和10%），**优势尤为显著**（例如5%标注时比次优方法GraphCL高2.23% Dice和2.42% IoU），证明了方法在弱监督场景下的强大能力。
3.  **类提示一致性是关键**：消融实验和注意力图可视化（与MedCLIP对比）共同证实，所提出的**token和像素级注意力损失能够有效提升类提示与脊柱单元的一致性**，这是性能提升的核心。
4.  **与基线融合有效**：将类引导分割图直接用于半监督训练（`L_text_semi`）以及将其与教师网络伪标签融合（`L_enhance_semi`）两种方式均能带来提升，最终模型显示出更好的数据鲁棒性。

#### 7. 优点

- **方法新颖**：首次将**文本类提示**成功应用于半监督脊柱分割领域，为提升伪标签质量提供了一个全新且有效的思路。
- **技术巧妙**：提出的两个注意力损失（Token级和像素级）**直接针对现有VLM在像素级对齐上的不足**进行设计，原理清晰，且实验证明有效。
- **性能显著**：在多个标注比例下均取得了SOTA结果，尤其在极低标注率下性能提升明显，凸显了实用价值。
- **实验全面**：主实验、消融、可视化、鲁棒性分析设计完整，对比方法广泛，结论可信度高。
- **开源承诺**：承诺开源代码，有利于领域内的复现和进一步发展。

#### 8. 不足与局限

- **方法复杂度**：两阶段训练策略（VLM预训练 + 半监督分割）**增加了训练流程的复杂度和计算成本**，调参可能较为繁琐。
- **数据集局限**：**仅在单一脊柱MRI数据集上进行验证**，泛化性有待在其他医学图像（如CT、其他器官MRI）甚至非医学图像上进一步检验。缺乏对domain shift的鲁棒性测试。
- **标签依赖性**：该方法依赖预训练的VLM和少量标注数据进行第一阶段预训练。**如果标注数据本身存在噪声或错误，第一阶段训练的模型可能会放大这些错误**，从而影响后续分割性能。文中未对标签噪声进行鲁棒性分析。
- **失败案例未分析**：文中未深入分析模型在哪些情况下可能会失败（例如严重图像伪影、解剖结构变异等），对方法的适用场景描述不够全面。

（完）
