---
title: Object Tokens as a Bridge Between Segmentation and Visual Question Answering in Robotic Surgery
title_zh: 对象标记作为机器人手术中分割与视觉问答之间的桥梁
authors: "Yiping Li, Ronald de Jong, Romy van Jaarsveld, Franco Badaloni, Gino Kuiper, Jelle Ruurda, Josien Pluim, Marcel Breeuwer"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15861v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 手术VQA与分割的视觉语言模型
tldr: 针对手术视觉问答中粗粒度视觉感知不足的问题，本文提出统一框架，将视觉语言模型与SAM解码器结合，通过目标令牌引导答案预测并生成分割掩码，同时在分割和问答目标上优化令牌嵌入，在RAMIE和EndoVis18数据集上优于基线方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15861v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of the proposed method.\", \"page\": 3, \"index\": 1, \"width\": 686, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-002.webp\", \"caption\": \"Fig. 3: Causal attention visualizations for object token generation and answer prediction. Top: example from the EndoVis18 dataset. Bottom: example from RAMIE dataset. Brighter regions indicate stronger attention weights.\", \"page\": 8, \"index\": 2, \"width\": 713, \"height\": 775}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-003.webp\", \"caption\": \"Fig. 2: Dataset statistics for EndoVis18 (left) and RAMIE (right). The inner ring shows the percentage distribution per question category, while the outer ring displays the train–test split with absolute sample counts.\", \"page\": 5, \"index\": 3, \"width\": 725, \"height\": 266}]"
motivation: 现有手术VQA方法依赖粗粒度视觉基础（如边界框），无法捕捉手术目标的细粒度空间结构。
method: 集成VLM与SAM解码器，使用VLM生成的目标令牌引导答案预测并投影到SAM解码器产生分割掩码，联合优化分割和问答目标。
result: 在私有RAMIE和公共EndoVis18数据集上，所提方法一致优于基线手术VQA方法。
conclusion: 引入上下文感知的目标令牌到视觉语言模型可提升细粒度手术场景理解。
---

## 摘要
机器人手术中的视觉问答（surgical VQA）需要对复杂手术场景的高层次理解以及视觉感知与语言推理的整合，具有支持手术培训和术中决策的潜力。最近的视觉-语言模型（VLMs）通过参数高效微调展现出有前景的性能；然而，现有方法大多依赖粗略的视觉定位（通常仅限于边界框），无法捕捉手术对象的细粒度空间结构。在这项工作中，我们提出了一个统一框架，在单一框架内联合执行像素级分割和视觉问答。我们的方法将VLM与基于Segment Anything Model（SAM）的解码器集成，并将场景元素表示为VLM生成的对象标记。这些对象标记指导答案预测，并进一步投影到基于SAM的解码器以生成分割掩码。通过分割和问答目标的联合优化，模型学习到空间有根的表示，从而增强视觉推理，同时提供显式的像素级定位。我们在私有的RAMIE（机器人辅助微创食管切除术）数据集和公开的EndoVis18数据集上评估了所提方法，其在手术VQA上一致优于基线方法。这些结果表明，将上下文感知的对象标记纳入视觉-语言模型能够改善细粒度手术场景理解。

## Abstract
Visual Question Answering (VQA) in robotic surgery, referred to as surgical VQA, requires high-level understanding of complex surgical scenes and the integration of visual perception with language reasoning, with the potential to support surgical training and intraoperative decision-making. Recent Vision-Language Models (VLMs) have shown promising performance through parameter-efficient fine-tuning; however, most existing approaches rely on coarse visual grounding, typically limited to bounding boxes, which fails to capture the fine-grained spatial structure of surgical objects. In this work, we propose a unified framework that jointly performs pixel-level segmentation and visual question answering within a single framework. Our approach integrates a VLM with a Segment Anything Model (SAM)-based decoder and represents scene elements as object tokens generated by the VLM. These object tokens guide answer prediction and are further projected to the SAM-based decoder to produce segmentation masks. By optimizing the object token embeddings through both segmentation and question answering objectives, the model learns spatially grounded representations that enhance visual reasoning while providing explicit pixel-level grounding. We evaluate the proposed method on the private RAMIE (Robot-Assisted Minimally Invasive Esophagectomy) dataset and the public EndoVis18 dataset, where it consistently outperforms baseline methods for surgical VQA. These results demonstrate that incorporating context-aware object tokens into vision-language models improves fine-grained surgical scene understanding.

---

## 论文详细总结（自动生成）

### 论文中文详细总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：机器人手术中的视觉问答（surgical VQA）旨在通过自然语言查询理解复杂手术场景，支持手术培训和术中决策。然而，现有方法多依赖粗粒度的视觉定位（如边界框），无法捕捉手术对象的细粒度空间结构（如像素级分割）。同时，当前VQA模型仅输出预定义类别，缺乏开放式的文本生成能力，且对空间推理的利用不足。
- **整体含义**：本文提出一个统一框架，将像素级分割与开放文本生成式VQA结合，通过引入“对象标记”（object tokens）作为桥梁，让VLM在生成答案的同时提供显式的分割掩码，从而提升对手术场景的细粒度理解与空间推理能力。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：在VLM（Qwen2.5-VL-7B）中为每个分割目标添加一个特殊标记 `<sam_pad>`，将其视为普通文本标记进行语言建模。同时，将该标记的潜层特征通过投影层映射到SAM解码器的提示空间，生成分割掩码。通过联合优化语言模型损失与分割损失，使对象标记同时学习语义和空间信息，从而既辅助答案生成，又能按需输出像素级分割。
- **关键技术细节**：
  - **VLM与SAM连接**：使用线性层将VLM的 `<sam_pad>` 特征投影到SAM提示空间（公式1）。然后利用一组可学习的查询向量（query）通过交叉注意力将投影特征映射为最终的提示嵌入（公式2）。最后根据不同SAM变体（SAM、SAM2、SAM3）进行掩码解码（公式3-6）。
  - **训练策略**：采用课程学习（curriculum learning）：阶段一：纯VQA数据预热（仅语言损失）；阶段二：加入分割监督（SAM权重以低学习率训练）；阶段三：混合纯VQA与分割引导VQA数据联合训练。使用LoRA（rank=16, α=32）微调VLM，学习率2e-4，训练3个epoch；SAM组件学习率1e-5。
- **公式说明**：公式1为线性投影；公式2为交叉注意力；公式3-6分别为SAM、SAM2、SAM3的掩码解码过程。

#### 3. 实验设计
- **数据集**：
  - **EndoVis18**：原为语义分割数据集，结合了扩充的VQA数据集（5类问题：单短语、定位、区域、视觉QA、详细描述）。训练-测试划分为视频2-4,6-7,9-12,14-15 训练，1,5,16 测试。
  - **RAMIE**：私有机器人辅助食管切除术数据集，包含手术阶段标注和分割掩码，VQA数据集包含6类问题（定位、结构识别、工具识别、计数、是非、手术阶段识别）。
- **对比方法（baseline）**：
  - 分割基线：DINOv3 ViT-L + EoMT（编码器-仅掩码Transformer）。
  - VQA基线：仅使用VLM（Qwen2.5-VL-7B）微调LoRA，不加 `<sam_pad>` 标记。
  - 本文方法：VLM + SAM / SAM2 / SAM3。
  - 最新手术VQA模型：EndoChat（基于LLaMA-3.2）、Gemma-3、GLM-4V等。
- **评估指标**：VQA使用BLEU-4、CIDEr、Rouge-L、GPT-4评分（用于描述类）；分割使用Dice系数和95百分位Hausdorff距离（HD95）。

#### 4. 资源与算力
- 文中未明确说明使用的GPU型号、数量及训练时长，仅提到采用LoRA微调（rank=16, α=32），学习率2e-4，训练3个epoch，SAM组件学习率1e-5。未提供硬件配置细节。

#### 5. 实验数量与充分性
- **实验组数**：
  - 在EndoVis18上对比了3种SAM变体 + 基线VLM + 分割基线EoMT，共5种设定（表1）。
  - 在RAMIE上同样对比了5种设定（表2）。
  - 与SOTA模型（LLaMA-3.2、Gemma-3、GLM-4V、EndoChat）对比（表3，仅EndoVis18）。
  - 注意力可视化定性分析（图3）。
- **充分性**：实验覆盖了两个数据集（公开与私有），多个SAM版本，与多种SOTA模型对比，并进行了消融（有无对象标记）和课程学习策略。但缺少对组件（如投影层、查询数量）的精细消融，且未报告消融实验的统计显著性。总体而言，实验设计较为充分，对比公平（使用相同的VLM骨干）。

#### 6. 主要结论与发现
- 统一分割与VQA的框架是可行的，且能提升VQA性能（尤其是需要空间理解的类别，如区域问答、手术阶段识别）。
- 更强的分割骨干（SAM2/SAM3）带来更好的VQA表现，表明对象标记的质量与分割能力正相关。
- 在EndoVis18上，本文方法（VLM+SAM3）在多项指标（如GPT-4评分81.20）上优于EndoChat（79.35）等SOTA模型。
- 注意力可视化显示，答案标记对语义相关的对象标记分配更高注意力，说明模型利用了有根的对象表示。

#### 7. 优点
- **创新性**：首次在手术领域统一像素级分割与开放文本VQA，利用对象标记作为桥梁。
- **架构灵活性**：可适配不同VLM骨干和SAM变体，易于扩展。
- **实用价值**：可同时输出答案和分割掩码，提供显式空间解释，有利于手术场景理解的可解释性。
- **课程学习策略**：多阶段训练平衡了语言与分割目标，避免了训练不稳定。

#### 8. 不足与局限
- **缺乏算力信息**：未报告硬件配置和训练时间，影响复现性评估。
- **数据集深度有限**：现有手术VQA数据集推理复杂性较浅（多为简单计数、工具识别），缺乏临床意义丰富的问题，模型在高阶推理上的能力未充分验证。
- **仅处理单帧图像**：未利用视频时序信息，未来需要扩展到视频VQA。
- **缺少消融实验细节**：未对投影层设计、查询数量、损失权重等超参数进行系统消融。
- **SAM3性能不稳定**：在统一训练协议下SAM3未始终优于SAM2，可能因损失函数定制不足，说明框架对不同SAM的适配仍待优化。
- **实验偏差**：RAMIE为私有数据集，结果外部泛化性未知；EndoVis18的VQA标注可能引入一定人工偏差。

（完）
