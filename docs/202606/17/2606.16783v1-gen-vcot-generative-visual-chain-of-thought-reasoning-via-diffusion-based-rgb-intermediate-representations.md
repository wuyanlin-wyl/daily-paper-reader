---
title: "Gen-VCoT: Generative Visual Chain-of-Thought Reasoning via Diffusion-Based RGB Intermediate Representations"
title_zh: Gen-VCoT：基于扩散的RGB中间表示的生成式视觉思维链推理
authors: "Zhiqiang Zhou, Junliang Dai, Xu ling"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16783v1"
tags: ["query:vl"]
score: 9.0
evidence: 使用SAM分割和Qwen2-VL的生成式视觉思维链
tldr: "Gen-VCoT提出利用扩散模型生成的RGB图像作为视觉链式推理的中间表示，通过视觉基础、几何推理和语义推理三个阶段，并采用自适应路由器选择推理深度，在空间和深度推理任务上分别提升25%和50%，但简单事实查询可能下降。文本CoT在CLEVR上优于视觉中间表示（91.2% vs 62.5%），表明最优表示任务相关。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16783v1/fig-001.webp\", \"caption\": \"Figure 4: Ablation study detail: Four pipeline configurations tested on the street scene spatial ordering question. Full pipeline (green) correctly identifies the leftmost building. Removing depth (yellow) still works but is less reliable. Removing segmentation (orange) produces incorrect answers. Baseline (red) hallucinates. The depth map provides the critical distance cue for spatial ordering.\", \"page\": 6, \"index\": 1, \"width\": 519, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2606.16783v1/fig-002.webp\", \"caption\": \"Table 6: Inference efficiency breakdown. The optimized pipeline loads each model once for batch processing.\", \"page\": 7, \"index\": 2, \"width\": 425, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2606.16783v1/fig-003.webp\", \"caption\": \"Figure 2: Three evaluation scenes with their segmentation maps and depth maps. (a) Indoor scene with furniture and plants. (b) Urban street scene with buildings and vehicles. (c) Park scene with trees and recreational objects.\", \"page\": 4, \"index\": 3, \"width\": 519, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2606.16783v1/fig-004.webp\", \"caption\": \"Figure 3: Qualitative comparison between Gen-VCoT and baseline across four representative questions. Green boxes indicate correct/improved answers; red boxes indicate errors or incomplete responses. Gen-VCoT consistently produces more accurate spatial reasoning, correct object ordering, and structured scene descriptions.\", \"page\": 5, \"index\": 4, \"width\": 519, \"height\": 301}]"
motivation: 现有多模态大语言模型依赖基于文本的思维链，缺乏可解释的视觉中间表示。
method: 提出Gen-VCoT框架，使用专家视觉模型生成RGB图像作为推理中间体，包括视觉基础（SAM分割）、几何推理（Marigold深度图）和语义推理（Qwen2-VL集成），并设计自适应路由器。
result: "Gen-VCoT在空间和深度推理任务上分别提升25%和50%，但在简单事实查询上可能下降。"
conclusion: Gen-VCoT建立了可解释多模态推理的新范式，但最优表示依赖于任务类型。
---

## 摘要
多模态大语言模型在视觉推理方面表现出色，但依赖于基于文本的思维链，缺乏可解释的视觉中间表示。现有方法使用不透明标记或外部工具，缺乏关键属性。我们提出Gen-VCoT，一种使用专家视觉模型生成RGB图像作为推理中间表示的框架。它包含三个阶段：视觉定位（SAM分割）、几何推理（Marigold深度图）和语义推理（Qwen2-VL集成）。自适应路由器选择推理深度。评估显示Gen-VCoT在空间问题（提升25%）和深度问题（提升50%）上有所改进，但可能损害简单事实查询。在CLEVR上，文本思维链优于视觉中间表示（91.2% vs 62.5%），表明任务依赖的最优表示。Gen-VCoT为可解释的多模态推理建立了新范式。

## Abstract
Multimodal large language models (MLLMs) excel at visual reasoning but rely on text-based chain-of-thought (CoT), lacking interpretable visual intermediates. Existing methods use opaque tokens or external tools, missing key properties. We propose Gen-VCoT, a framework using expert vision models to generate RGB images as reasoning intermediates. It has three stages: visual grounding (SAM segmentation), geometric reasoning (Marigold depth maps), and semantic reasoning (Qwen2-VL integration). An adaptive router selects reasoning depth. Evaluations show Gen-VCoT improves spatial (25% better) and depth (50% better) questions, but may hurt simple factual queries. Text CoT outperforms visual intermediates on CLEVR (91.2% vs 62.5%), showing task-dependent optimal representations. Gen-VCoT establishes a new paradigm for interpretable multimodal reasoning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有多模态大语言模型（MLLMs）依赖基于文本的思维链（CoT）进行视觉推理，但文本是间接的介质，无法显式、可解释地处理空间和几何信息。已有方法（如COVT使用不透明连续视觉token、Visual Sketchpad依赖外部工具）均无法同时满足**可解释性**、**端到端可训练性**和**密集像素级视觉表示**。
- **背景**：受“生成即理解”范式（Vision Banana等）启发，作者认为生成分割图、深度图的过程本身蕴含了对场景的视觉理解。
- **整体含义**：提出**Gen-VCoT**框架，通过专家模型生成RGB图像作为中间推理产物，建立了一种全新的、可解释的多模态推理范式，但同时也揭示范式高度依赖任务类型——视觉中间表示在空间推理上有效，在简单事实查询上可能适得其反。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将视觉推理分解为三个可解释的阶段，每个阶段输出RGB图像（而非隐藏向量或稀疏图形），最后用MLLM整合所有视觉证据回答。
- **关键技术细节**：
  - **Stage 1：视觉基础（Where）**  
    使用**SAM ViT-Large**，以8×8网格点作为提示，生成实例分割图。对每个提示点选取最高IoU的掩膜，过滤面积<100像素的掩膜，然后为每个实例分配不同颜色。
    - 公式：\( M_{seg} = G_{seg}(I, P_{seg}(Q)) \)
  - **Stage 2：几何推理（How）**  
    使用**Marigold LCM**（扩散模型）估计单目深度，5次集成平均，将深度归一化后映射到彩虹色图（红近紫远）。
    - 公式：\( M_{depth} = G_{depth}(I, P_{depth}(Q, M_{seg})) \)
  - **Stage 3：语义推理（What）**  
    将原始图像、分割图、深度图作为三个输入，送入**Qwen2-VL-7B-Instruct**，附加提示要求根据视觉证据简洁回答。
    - 公式：\( A = F(I, M_{seg}, M_{depth}, Q) \)
- **自适应推理路由器**：用BERT-base分类器（110M参数）根据问题文本选择三条路径之一（仅分割、分割+深度、全部），训练目标为交叉熵 + 效率惩罚项（\(\lambda \cdot step\_count\)）。
- **算法流程**（Algorithm 1）：先由路由器选择路径，条件调用相应阶段，最后用Qwen2-VL推理答案。

### 3. 实验设计：数据集 / 场景、基准、对比方法

- **数据集 / 场景**：
  - **主要场景**：三个合成的简单场景（室内、街道、公园），每个场景设计8个问题，覆盖6个类别，共19个有效问题（论文称24场景-问题对，最终用19个）。
  - **CLEVR风格场景**：生成10个合成场景，每个含2–4个彩色3D物体，共80个问题（存在、计数、颜色、形状、空间5类），有确定性真值。
- **基准**：直接使用Qwen2-VL回答（无中间产物）作为基线。
- **对比方法**：
  - 不同管道配置：完整管道（Gen-VCoT）、无深度、无分割、基线。
  - 额外对比：文本思维链（Text CoT）——提供结构化文本描述（颜色、形状、大小等）代替视觉中间产物。
- **评估指标**：答案准确率（精确匹配或包含匹配）、推理延迟。

### 4. 资源与算力

- **硬件**：单块NVIDIA RTX 3090（24GB VRAM）。
- **模型**：模型依次加载（SAM、Marigold、Qwen2-VL），每个阶段显存占用：SAM 1.3GB，Marigold 3GB，Qwen2-VL 16.6GB。
- **处理时间**（表6）：  
  - 一次性加载成本：SAM加载1.1s+推理1.1s，Marigold加载2.1s+推理2.5s，Qwen2-VL加载11.9s。  
  - 每问题推理：完整管道0.8s，基线0.3s。  
  - 批量处理19个问题时，每问题摊销约1.8s（计算见附录C）。  
- **训练**：路由器训练时间<1分钟（100条数据，3 epoch）。MLLM和专家模型均未微调。

### 5. 实验数量与充分性

- **实验组数**：
  - 主要场景：19个问题 × 4种管道配置（表1、表5），共76次评估。
  - CLEVR场景：80个问题 × 2种配置（表2），另加文本CoT对比（表4）共80×3=240次。
  - 消融实验（表5）：3场景×8问题×4配置 = 96次。
  - 总计约数百次推理，但样本量偏小（任务类型有限）。
- **充分性与公平性**：
  - **充分性**：覆盖了不同管道变体、不同问题类型、合成场景与CLEVR，并对比了文本CoT，设计较全面。但**缺乏真实世界基准**（如GQA、CLEVR官方数据集）的评估，论文虽提到“正在开展”，但现有实验仅基于自建小规模合成数据，外部有效性受限。
  - **公平性**：对比基线为相同MLLM（Qwen2-VL）直接推理，控制变量合理。文本CoT采用结构化描述，保证了信息量相当。
  - **局限**：合成场景简单（几何形状+少量物体），与真实场景复杂度差距大；每个问题只测试一次，未报告方差或置信区间。

### 6. 论文的主要结论与发现

1. **Gen-VCoT在空间和深度推理上显著提升**：空间准确率+25%，深度感知+50%，总准确率78.9% vs 基线68.4%（+10.5%）。
2. **视觉中间表示对简单事实查询有害**：在CLEVR上基线85.0% vs Gen-VCoT 62.5%，下降22.5%。SAM的网格分割可能引入噪声，深度图在合成场景中梯度无意义。
3. **文本CoT优于视觉中间表示（CLEVR）**：文本CoT达到91.2%，比基线高6.2%，比Gen-VCoT高28.7%。说明简单事实查询更适合结构化文本而非视觉中间产物。
4. **最优表示任务依赖**：复杂空间/深度问题需视觉中间，简单事实问题需文本或不加中间。
5. **自适应路由器的必要性**：上述发现为路由器设计提供了强烈动机，路由器应能选择视觉中间（空间推理）或文本描述（事实查询）或两者结合。
6. **分割与深度互补**：消融证实完整管道优于任何部分配置，分割帮助物体识别与计数，深度帮助空间排序。

### 7. 优点：方法或实验设计上的亮点

- **创新性**：首次系统使用专家模型生成的RGB图像作为视觉推理中间表示，兼顾可解释性、端到端性、密集像素级表示，弥补了现有方法的空白。
- **模块化设计**：三阶段清晰可分离，便于独立调试和替换底层模型。
- **自适应路由器**：动态选择推理链长度，平衡效率与准确性，具有实用价值。
- **对比全面**：不仅对比基线，还引入文本CoT、消融不同组件，揭示了不同任务对中间表示的敏感性，结论更有深度。
- **效率分析详细**：提供了延迟分解和批量优化策略，展示了实际可行性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅用自建合成场景（几何形状和平涂颜色），未在真实世界基准（GQA、CLEVR官方、MIRA等）上验证，泛化性存疑。论文承认“评估仍在进行”。
  - 每个场景问题数量极少（19个），统计显著性无法保证。
  - 未测试不同MLLM（如GPT-4V、LLaVA）作为推理模型，结论可能依赖于Qwen2-VL。
- **方法局限**：
  - 固定使用SAM和Marigold，未针对任务微调，专家模型的错误会传播到最终答案。
  - 三阶段顺序处理带来约8秒的预处理开销（虽可批量摊销），实时应用受限。
  - 网格提示策略可能分割出无关掩膜，引入噪声，尤其在简单场景。
  - 路由器训练数据仅100对，由作者人工标注，可能不够客观或覆盖所有类型。
- **偏差风险**：
  - 合成场景中深度图使用彩虹色映射，可能引入与人类直觉不一致的编码（如红色近、紫色远并非普适）。
  - 实验场景有意选择有利于视觉中间的问题（如空间、深度），可能夸大提升；而CLEVR问题偏向事实查询，导致负效果，两者对比强化了“任务依赖”结论，但未系统量化平衡点。
- **应用限制**：
  - 需要同时加载多个大模型（SAM、Marigold、Qwen2-VL），显存和算力要求高（24GB起步）。
  - 文本CoT在简单任务上显著更优，表明对许多实用场景（如VQA）添加视觉中间可能得不偿失，需先路由判断。
- **其他**：未公开代码和数据集，结果难以复现。

（完）
