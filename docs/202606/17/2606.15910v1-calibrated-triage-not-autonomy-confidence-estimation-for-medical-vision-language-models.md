---
title: "Calibrated Triage, Not Autonomy: Confidence Estimation for Medical Vision-Language Models"
title_zh: 校准分流，而非自主：医学视觉-语言模型的置信度估计
authors: "Reza Khanmohammadi, Kundan Thind, Mohammad M. Ghassemi"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15910v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学视觉语言模型置信度估计
tldr: "该研究评估了医疗视觉语言模型（LVLMs）在回答医学图像问题时依赖语言先验而非图像的风险，提出用置信度估计实现校准分流而非自主决策。比较了7种置信度估计器在5个LVLMs和3个数据集上的表现，发现标准指标区分度低，最佳探针在错误率1-4%内，而弱基线为41-45%。结论是模型能力设上限，置信度层决定可达上限，适合校准分流而非自主。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15910v1/fig-001.webp\", \"caption\": \"Fig. 1. Confidence estimation across the clinical landscape, pooled over the five LVLMs. Rows are clinical categories (GMAI-MMBench by department, SLAKE by anatomical region, PATH-VQA by question type), sorted by base VQA accuracy, with a per-dataset summary row; GMAI-MMBench uses the shared question intersection common to all methods, SLAKE and PATH-VQA full coverage. Cells report AUROC, ECE, and safe yield at a 20 percent error tolerance (Y@20) on a green ramp, greener is better (for ECE, lower). All values are scaled to 0 to 100. Base accuracy and AUROC ramp together top to bottom, the capability-to-automatability gradient, and the trained probes read greener than the inference baselines in the lower-accuracy categories. Y@20 is a category’s own operating point in a category row but the single global threshold in a summary row, and the gap between the two, high in strong categories yet near zero pooled, is itself the finding: one global operating point suppresses automation that category-specific thresholds recover. Categories below 150 pooled cases are hatched, since per-category ECE is unstable on small strata.\", \"page\": 4, \"index\": 1, \"width\": 1048, \"height\": 764}, {\"url\": \"assets/figures/arxiv/2606.15910v1/fig-002.webp\", \"caption\": \"TABLE I SELECTIVE PREDICTION (TRIAGE). AURC IS THE AREA UNDER THE RISK-COVERAGE CURVE (LOWER IS BETTER); YIELD@τ IS THE LARGEST AUTOMATABLE FRACTION HOLDING ERROR AMONG AUTOMATED CASES AT OR BELOW τ (AT LEAST 30 AUTOMATED CASES); ERRTOP10% IS THE ERROR RATE IN THE TOP CONFIDENCE DECILE. POOLED OVER (LVLM, QUESTION) PAIRS ACROSS THE FIVE LVLMS, SEED-AVERAGED FOR TRAINED PROBES. ORACLE IS A PERFECT-RANKING PREDICTOR; BEST PER COLUMN (EXCLUDING ORACLE) IN BOLD.\", \"page\": 5, \"index\": 2, \"width\": 529, \"height\": 529}]"
motivation: 医疗图像问答中，模型可能仅依赖语言先验而忽略图像，生成看似可信但错误的答案，需要可靠的置信度来指导系统何时放弃回答。
method: 将任务重述为有界选择性预测，在5个开源LVLMs和3个医学VQA数据集上，用仅训练于自然图像的7种置信度估计器进行评估，无需适应性调整。
result: "标准度量（如判别能力）无法区分方法好坏；弱基线在41-45%的错误上自信预测，而最佳探针仅1-4%；无估计器在跨域或跨模型上表现一致；校准得分在20%误差容忍下可自动化约三分之一的放射学病例，但病理学几乎为零。"
conclusion: 当前可行角色是校准分流而非自主：自动处理校准置信度标记安全的案例，其余转给临床医生。
---

## 摘要
视觉-语言模型可以流畅且自信地回答关于医学图像的问题，但几乎不使用图像，而是依赖语言先验。在医学领域，这是最关键的失败，因为答案看起来可信实则不然，唯一的保护措施是足够可靠的置信度分数，以告知系统何时应放弃回答。我们提出的不是准确性问题，而是部署问题：一个模型能安全独立处理多少影像工作，以及哪种置信度信号能使这成为可能。我们在五个开放权重的LVLM和三个涵盖广泛临床影像、放射学和病理学的医学视觉问答数据集上评估了七种置信度估计器，每个探测器仅用自然图像训练且未经适应直接应用。重新定义为有界选择性预测（仅在置信度超过阈值时自动处理病例，其余延迟），比较结果令人警醒。标准指标指导性差：判别能力几乎无法区分各方法，而廉价自我报告的弱校准可通过域外温度缩放轻易去除，却不改变可部署的产出。区分可用估计器的是临床医生所依据的高置信区域：最弱的基线在41%至45%的错误上自信地犯错，而最佳探测器仅为1%至4%，且没有任何估计器在所有领域或模型上可靠最优。安全交接由两个层面决定：基础模型能力设定了上限，因此校准良好的分数在20%误差容限下能恢复约三分之一的放射学病例，但病理学几乎无法恢复；置信度层则决定了该上限有多少可达。如今可行的角色是校准分流，而非自主：自动化那些校准分数标记为安全的病例，其余转交给临床医生。我们发布所有输出、正确性判断和置信度分数，并附代码。

## Abstract
A vision-language model can answer a question about a medical image fluently and confidently while barely using the image, leaning instead on language priors. In medicine this is the failure that matters most, because the answer looks trustworthy and is not, and the only protection is a confidence score reliable enough to tell the system when to abstain. We ask a deployment question rather than an accuracy one: how much imaging work a model can safely handle alone, and which confidence signal makes that possible. We evaluate seven confidence estimators across five open-weight LVLMs and three medical visual-question-answering datasets spanning broad clinical imaging, radiology, and pathology, with every probe trained only on natural images and applied without adaptation. Recast as bounded selective prediction (automate a case only when confidence clears a threshold, defer the rest), the comparison is cautionary. The standard metrics are poor guides: discrimination barely separates the methods, and the weak calibration of a cheap self-report is cheaply removed by off-domain temperature scaling without changing deployable yield. What distinguishes a usable estimator is the high-confidence region a clinician acts on: the weakest baselines are confidently wrong on 41 to 45 percent of their errors against 1 to 4 percent for the best probe, and no estimator is reliably best across domains or models. Safe handoff is governed at two levels: base-model competence sets a ceiling, so a well-calibrated score recovers roughly a third of radiology cases at a 20 percent error tolerance but almost none of pathology; the confidence layer then decides how much of that ceiling is reachable. The usable role today is calibrated triage, not autonomy: automate the cases a calibrated score marks safe, route the rest to a clinician. We release all outputs, correctness judgments, and confidence scores, with code.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：医学视觉-语言模型（LVLMs）可能看似流畅自信地回答关于医学图像的问题，但实际上依赖于语言先验而几乎不使用视觉输入，导致“看似可信实则错误”的答案，这对临床决策构成严重风险。
- **关键挑战**：现有置信度估计器主要针对通用领域的排序和平均校准，缺乏对医学成像中“安全分流”目标的针对性设计。临床需要的是：能明确区分哪些病例可以自动化（误差可控）、哪些必须转给医生，而不仅仅是全局排序或平均校准。
- **研究意义**：提出用“校准分流”替代“自主决策”——即仅在置信度可靠时自动处理，其余延迟给临床医生。这是首次系统性地将置信度估计置于医学成像的部署场景下，评估其在跨域（自然图像训练→医学图像测试）条件下的表现。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将置信度估计重新定义为有界选择性预测（Bounded Selective Prediction）。给定一个误差容忍阈值τ（如10%或20%），仅当置信度分数超过某个界限时自动化该病例，其余延迟。要求置信度不仅排序好，还必须校准（即数值本身对应真实准确性），以便固定阈值能映射到受控的错误率。
- **置信度估计器（7种）**：
    - **基于提示的自报告**：P(True)、Self-Probing、Prompt Ensemble。直接让模型用语言或令牌概率表达信心，不依赖模型内部。
    - **内部状态探针**：SAPLMA（读最后隐状态）、InternalInspector（多层特征融合）、CCPS（基于扰动表示稳定性）。
    - **感知-对齐探针**：BICR（Blind-Image Contrastive Ranking），唯一考虑了“是否使用图像”的接地感知方法，通过对真实图像和空白图像表示的对比训练来惩罚模型在不看图像时仍保持高置信。
- **流程**：
    1. 所有可训练探针仅在通用领域GQA（20k训练，5k验证）上训练和验证，然后在三个医学VQA数据集上零适配测试。
    2. 对每个LVLM的每个问题，得到答案和置信度分数，用gpt-5-mini（单一LLM）判断答案是否正确（语义等价）。
    3. 评估指标包括：校准误差（ECE）、判别能力（AUROC）、选择性预测指标（AURC、安全产出Y@τ、高置信十分位错误率）。
- **关键细节**：为了消除不同数据集使用不同评分规则导致的混淆，所有回答正确性统一由同一个gpt-5-mini判定；可训练探针采用5个种子取平均。

### 3. 实验设计：使用了哪些数据集 / 场景，benchmark是什么，对比了哪些方法

- **数据集（3个医学VQA）**：
    - **GMAI-MMBench**：涵盖放射学、病理学、皮肤科、眼科等40+临床任务，疾病诊断为主，约21,748个测试样本。
    - **SLAKE**：放射学与解剖学基准（CT、MRI、X光），约2,094个样本，含解剖区域元数据。
    - **PATH-VQA**：病理学（显微镜/组织病理），约6,111个样本，问题类型多样（是/否、位置、识别等）。
- **模型（5个开源LVLM）**：Qwen3-VL-8B、LLaVA-NeXT-13B、InternVL3.5-14B、DeepSeek-VL2、Gemma-3-27B。参数规模4.5B~27B，三种视觉编码器谱系。所有模型采用贪婪解码，最多64新令牌。
- **对比方法**：上述7种置信度估计器（3种基于提示 + 4种内部探针）。另外在不同分析中引入Oracle（完美排序器）作为上界。
- **核心基准**：将每个（LVLM, 问题）对汇聚后评估。还按临床科室（GMAI内19个科室）、解剖区域（SLAKE内7个区域）、问题类型（PATH-VQA内9种）进行分层分析。

### 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长。** 仅提及可训练探针在GQA上训练，使用了5个种子（seed: 23, 42, 137, 2024, 3407）。评估模型推理和探针训练的计算资源细节未报告。
- 声明：由于论文未提供，此处指出这一点。

### 5. 实验数量与充分性

- **实验数量**：非常充足。7种估计器 × 5个LVLM × 3个数据集 = 105个主要实验条件。此外，在GMAI内按19个科室、SLAKE内7个区域、PATH-VQA内9种问题作了分层分析，并报告了跨模型和跨域汇总以及每模型每领域的单独结果。还进行了显著性检验（DeLong检验、Friedman检验、Wilcoxon检验等）。
- **充分性与公平性**：
    - 充分：覆盖多个域、多种方法、多种模型，结果稳定且有意义。
    - 公平：所有可训练探针均仅在自然图像GQA上训练，未使用任何医学数据，确保是“零适配”转移测试。回答正确性采用统一LLM评判，避免不同数据集评分规则差异。
    - 潜在不足：正确性标签仅依赖单一LLM（gpt-5-mini），未独立验证其在医学语义等价上的准确率；但作者指出该协议在先前工作中已建立。另外，探针未进行医学域内微调（这被列为未来方向），这可能低估了其上限，但论文目的就是评估零迁移的实用性。

### 6. 论文的主要结论与发现

1. **标准指标（如AUROC）无法区分可靠估计器**：全局判别能力差距很小（例如GMAI上P(True) AUROC 61.9 vs BICR 62.9），且全局校准ECE可通过简单的域外温度缩放修复，但修复后安全产出不变——因此校准不足以说明可用性。
2. **真正区分在于高置信尾部的错误率**：最差的基线（如P(True)）在41%~45%的错误上赋予0.8以上置信度，而最佳探针（BICR）仅1%~4%。临床医生依赖的高置信区域的安全错误率才是关键。
3. **无单一估计器在所有域或所有模型上最优**：BICR和InternalInspector在放射学中领先，但病理学中Prompt Ensemble AUROC更好；跨15个(LVLM+dataset)单元，InternalInspector最多获胜（5/15），但没有任何方法获胜超5个。
4. **安全自动化受两级限制**：
    - 基础模型能力设定上限（如SLAKE准确率60.9%的顶线，最高安全产出可达76%）；能力越低，上限越低。
    - 置信度层决定能够达到该上限的比例：在放射学（SLAKE）上BICR实现了34.6%的20%误差容限产出；在临床宽域（GMAI）仅4.5%；在病理学（PATH-VQA）几乎0%。
5. **接地感知探针（BICR）独特优势**：当模型不使用图像（如随机图像替代后答案不变）时，BICR自信度下降，而其他方法不变。这是临床最需要标记的故障模式。
6. **模型聚合会误导部署决策**：跨模型池化得出的排名可能不适用于特定模型，部署时必须按具体模型和临床类别评估。

### 7. 优点

- **创新部署视角**：将置信度估计从普通排序任务转换为有界选择性预测，直接回答“能自动多少工作”和“在多少误差容忍下”的临床实际问题。
- **系统全面的跨域评估**：覆盖放射学、临床宽域、病理学三大不同分布偏移程度，并包含5种不同规模/架构的LVLM，结果具有较高外部有效性。
- **统一公平的评估协议**：所有可训练探针零迁移测试（只训练于自然图像），统一LLM评判正确性，确保跨方法和跨数据集可比性。
- **细致的分层分析**：不仅给出全局指标，还提供按科室、区域、问题类型、成像模态的分层结果，揭示了聚合数值掩盖的公平性和可靠性差异（如BICR在常见模态上校准好，稀见模态差）。
- **聚焦高置信尾部安全**：揭示了标准指标不足，强调临床医生真正关注的极低错误率区域。
- **开源所有数据和代码**：促进可重复性和后续研究。

### 8. 不足与局限

- **正确性标签依赖单一LLM**：gpt-5-mini可能对医学语义等价判断有偏差，尤其是细微的临床表现差异。作者承认需要专家对标签子集进行审核。
- **探针仅自然图像训练**：未进行医学域内微调，这可能低估了探针在医学上的潜力（也为未来研究留出空间）。当前结论对“完全零迁移”场景有效，但未必反映在域内适应后的表现。
- **安全阈值（10%-20%误差）是假定**：实际临床中误差容忍度取决于具体任务风险（如漏诊癌症 vs. 误报）和机构政策，文中仅作为演示。
- **数据集静态VQA**：所有测试数据为静态图像和问题，不包含用户交互、多轮对话或实时临床工作流，可能高估或低估实际部署性能。
- **模型和探针的隐私限制**：仅评估开源模型，但许多医院可能使用闭源API，其内部状态对探针方法不可用。论文也指出开放权重是使用内部探针的必要条件。
- **统计显著性跨方法较弱**：虽然池化后DeLong检验显示BICR优于其他，但每模型/每域的Friedman检验仅弱显著（p=0.029），且配对Wilcoxon无显著差异，表明排名敏感于聚合方式。
- **计算资源未报告**：缺少GPU型号和训练时长，不利于复现或估算成本。

（完）
