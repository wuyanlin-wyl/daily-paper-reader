---
title: Unified Multimodal Model for Brain MRI Imputation and Understanding
title_zh: 用于脑部MRI插补与理解的统一多模态模型
authors: "Zhiyun Song, Che Liu, Tian Xia, Avinash Kori, Wenjia Bai"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16484v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于脑MRI的统一多模态模型
tldr: 针对脑MRI多模态数据缺失和高质量训练数据稀缺的问题，提出统一多模态模型UniBrain。采用联合模态补全与理解的统一训练策略，构建交错描述增强的数据流进行自回归训练，引入自对齐机制学习细粒度解剖特征，并提出动态隐藏状态机制缓解长上下文推理中的曝光偏差。在多疾病脑MRI数据集上，UniBrain在不同程度模态缺失下均实现了高性能的脑图像补全、理解和疾病诊断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16484v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative results for diagnosis and report generation with missing MRIs. Und. means the understanding-only counterparts of the model.\", \"page\": 7, \"index\": 1, \"width\": 719, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2606.16484v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative results for unified brain MRI analysis using UniBrain.\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2606.16484v1/fig-003.webp\", \"caption\": \"Table 2. Quantitative evaluation of missing modality imputation. * means we ensemble results sampled with 5 random seeds.\", \"page\": 8, \"index\": 3, \"width\": 714, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2606.16484v1/fig-004.webp\", \"caption\": \"Fig. 1. Overall framework of UniBrain. (a) We construct a sequential, interleaved data flow for brain MRI analysis during training. (b) The framework incorporates a unified multimodal model for image generation and understanding. Dynamic hidden states via training-time KV-cache are designed to mitigate exposure bias for long-context multimodal reasoning. A self-alignment loss conditions the generation expert on dense embeddings extracted from the ViT for image reconstruction.\", \"page\": 3, \"index\": 4, \"width\": 725, \"height\": 446}]"
motivation: 现有医学多模态大模型面临高质量训练数据稀缺和临床应用中的模态缺失挑战。
method: 提出UniBrain，通过统一训练策略联合进行脑MRI模态补全与理解，采用交错描述数据流自回归训练、自对齐机制和动态隐藏状态机制。
result: 在多疾病脑MRI数据集上，UniBrain在不同模态缺失程度下均实现了高性能的图像补全、理解和疾病诊断。
conclusion: UniBrain有效解决了脑MRI分析中的模态缺失问题，为医学多模态大模型提供了一种可行方案。
---

## 摘要
多模态大语言模型（MLLMs）在医学领域具有巨大潜力，因为它们继承了LLM的知识，并允许在自然语言中集成、分析和解释多种数据模态。然而，医学MLLMs领域面临重大挑战，特别是高质量训练数据的稀缺以及真实临床环境中频繁出现的数据缺失。在此，我们提出了一种新颖的统一多模态模型UniBrain，用于脑部磁共振图像（MRI）分析。为了解决可能缺失的脑部MRI模态，我们采用统一的训练策略，联合执行成像模态插补和脑图像理解。在训练过程中，构建交错且描述丰富的数据流，以自回归方式训练模型，从而实现对生成的多模态数据进行医学推理。引入自对齐策略，利用密集图像嵌入学习细粒度解剖特征，而无需详细的图像描述。此外，我们提出了一种动态隐藏状态机制，以缓解长上下文多模态推理过程中的暴露偏差。在多疾病脑部MRI数据集上的大量实验表明，UniBrain在不同程度模态不完整的情况下，在脑图像插补、理解和疾病诊断方面均实现了高性能。

## Abstract
Multimodal large language models (MLLMs) hold great potential for medicine, as they inherit knowledge from LLM and allow multiple data modalities to be integrated, analysed and interpreted in natural language. However, the field of medical MLLMs is constrained by non-trivial challenges, notably the scarcity of high-quality training data and the frequent occurrence of missing data in the real-world clinical setting. Here, we propose a novel unified multimodal model, UniBrain, for brain magnetic resonance image (MRI) analysis. To address potential missing brain MRI modalities, we employ a unified training strategy to perform joint imaging modality imputation and brain image understanding. During training, an interleaved and description-enriched data flow is constructed to train the model in an autoregressive manner, enabling medical reasoning with generated multimodal data. A self-alignment strategy is introduced to leverage dense image embeddings to learn fine-grained anatomical features without requiring detailed image captions. Furthermore, we propose a dynamic hidden state mechanism to alleviate the exposure bias during long-context multimodal inference. Extensive experiments on multi-disease brain MRI dataset demonstrate that UniBrain achieves high performance for brain image imputation, understanding, and disease diagnosis under various extents of modality incompleteness.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：脑部MRI分析在临床中高度依赖多模态数据（如T1、T2、FLAIR等），但实际场景中经常出现某些模态缺失的情况，影响诊断完整性。同时，医学多模态大语言模型（MLLMs）面临高质量训练数据稀缺、通用模型缺乏医学专业知识等挑战。
- **整体含义**：本文提出UniBrain，一个统一的生成-理解模型，旨在同时实现缺失模态的插补（image imputation）与脑部疾病的诊断理解，使模型能够在数据不完整的情况下仍能生成高质量图像并做出可靠诊断，提升临床适用性。

### 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：将缺失模态插补和图像理解统一为一个自回归序列建模过程，模型按顺序执行“思考（包含描述性文本）→生成缺失模态图像→基于累积上下文进行最终诊断”，使生成与理解相互促进。
- **关键技术细节**：
  - **交错描述增强的数据流**：在训练时构造 `输入图像 → 思考文本（含模态转换说明）→ 当前模态发现 → 生成图像 → ... → 最终印象` 的序列，使模型学习逐步推理。
  - **自对齐策略（Self-Alignment, SA）**：利用ViT编码器提取的密集视觉嵌入作为条件，让生成专家（VAE解码器）重建原始图像，无需详细放射学标注，损失函数为：
    \[
    \mathcal{L}_{\text{SA}} = \mathbb{E}_{t,\epsilon} \left[ \| V_\theta(e_i^t, t, w_i, p_{\text{rec}}) - (\epsilon - e_i) \|_2^2 \right]
    \]
  - **动态隐藏状态机制（Dynamic Hidden State, DHS）**：在训练时利用KV缓存进行自回归展开，用自生成的视觉预测状态（而非真实状态）填充后续上下文，使模型学会容忍自身生成瑕疵，缓解长上下文推理中的暴露偏差。
  - **整体损失函数**：融合自对齐损失（\(\mathcal{L}_{\text{SA}}\)）、语言建模损失（\(\mathcal{L}_{\text{NTP}}\)，语言部分为下一个token预测）、流匹配生成损失（\(\mathcal{L}_{\text{flow}}\)，视觉部分预测速度场），统一优化插补与理解。

### 3. 实验设计

- **数据集**：RadGenome-Brain MRI 公开数据集，共3408个脑部MRI扫描，包含6种模态（T1, T2, FLAIR, DWI, ADC, T1ce）和5种疾病（胶质瘤、脑膜瘤、转移瘤、卒中、白质高信号）。划分为训练集2382、验证集338、测试集688。
- **Benchmark 设置**：在多种模态缺失场景下（仅T1w、T1w+T2w、T1w+T2w+T2f、完整数据）评估诊断准确率、报告生成质量。
- **对比方法**：
  - 隐式表示学习：ShaSpec、SimMLM
  - 显式模态插补 + MLLM：ResViT + Lingshu/UniBrain、M2DN + UniBrain
  - 其他MLLM：BAGEL、UniMedVL、Lingshu
- **评估指标**：诊断采用Top-1准确率（Acc-1）；报告生成采用ROUGE-1、RaTEScore；图像生成采用PSNR、SSIM，以及生成图像用于下游诊断的Top-1（诊断可用性）。

### 4. 资源与算力

- 论文明确说明：使用 **8张 Nvidia A100 GPU** 进行实验。
- 训练步骤：自对齐预训练2000步 → 无KV缓存训练2000步 → 动态隐藏状态（DHS）训练200步。
- 文中未给出总训练时长，但指出推理需≥32GB GPU显存，说明计算开销较大。

### 5. 实验数量与充分性

- **实验数量**：主要包含三组定量实验：
  1. 不同模态缺失下的诊断与报告生成性能对比（表1）。
  2. 缺失模态插补的保真度与诊断可用性对比（表2）。
  3. 消融实验（表3）：去掉SA、DHS等组件，验证各模块贡献。
- **充分性与公平性**：
  - 对比方法覆盖了隐式、显式、MLLM三类主流范式，且在同一数据集上评估，公平性较好。
  - 消融实验逐步验证统一建模、SA、DHS的效果，设计合理。
  - 仅使用单个公开数据集，缺乏外部验证或多中心数据，样本量中等（688测试集），泛化性有待进一步验证。

### 6. 主要结论与发现

- UniBrain在仅提供T1w模态的困难场景下达到74.47%诊断准确率，在完整数据下提升至82.06%，显著优于其他MLLM（如BAGEL仅11.35%~28.37%）。
- 在图像生成方面，UniBrain的PSNR/SSIM与专门生成模型相当，但生成的图像用于诊断时准确率更高（通过5次抽样集成可超越专有生成模型）。
- 动态隐藏状态机制有效缓解了长上下文暴露偏差，提升最终诊断性能；自对齐策略改善了生成质量但对诊断略有干扰，整体平衡后达到最优。

### 7. 优点

1. **统一框架**：首次在一个自回归模型中同时实现缺失模态插补与疾病诊断，生成结果可直接用于后续推理，避免两阶段串联导致的特征不匹配。
2. **数据高效**：自对齐策略利用图像自身作为监督，极大减少了对精细放射学标注的依赖。
3. **鲁棒性提升**：动态隐藏状态机制强制模型在训练时接触自生成状态，增强了实际推理时的容错能力。
4. **性能全面**：无论是在诊断精度还是生成质量上，均超越或持平现有方法，特别是在模态严重缺失时优势显著。

### 8. 不足与局限

- **计算资源需求高**：推理至少需要32GB GPU显存，限制了低资源场景部署。
- **2D切片处理**：仅基于2D切片进行建模，未利用3D空间信息，丢失了立体解剖结构。
- **数据集单一**：仅在RadGenome数据集上验证，样本量有限（688测试），疾病类型和模态类别不够全面，可能存在偏差。
- **文本增强局限**：原始报告为3D描述，需人工投影为2D切片描述，可能引入误差。
- **消融实验中SA轻微干扰诊断**：自对齐专注于像素级重建，可能引入与诊断无关的细节，需进一步调优。
- **鲁棒性测试不足**：未对噪声、分辨率变化等临床常见变异进行评估。

（完）
