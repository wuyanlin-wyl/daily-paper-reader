---
title: "XSSR: Cross-Domain Self-Supervised Representative Selection for Efficient Annotation in Medical Image Segmentation"
title_zh: XSSR：跨域自监督代表性选择用于医学图像分割中的高效标注
authors: "Byunghyun Ko, Aleksei Anisimov, Kobe Ke, Suhas Bharthepude, Jeongkyu Lee"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04301v1"
tags: ["query:vlmmed"]
score: 8.0
evidence: 用于医学分割的跨域自监督选择
tldr: "99.3%），超过随机选择和CoreSet基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04301v1/fig-001.webp\", \"caption\": \"Fig. 4. Prostate MRI segmentation. Rows and color coding as in Fig. 2; XSSR uses 57 slices (5%).\", \"page\": 10, \"index\": 1, \"width\": 616, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2606.04301v1/fig-002.webp\", \"caption\": \"Fig. 3. Fundus optic disc segmentation. Rows and color coding as in Fig. 2; XSSR uses 18 samples (5%).\", \"page\": 10, \"index\": 2, \"width\": 616, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2606.04301v1/fig-003.webp\", \"caption\": \"Fig. 1. Pipeline of the proposed XSSR approach.\", \"page\": 4, \"index\": 3, \"width\": 725, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2606.04301v1/fig-004.webp\", \"caption\": \"Fig. 2. Chest X-ray lung segmentation. Top row: ground truth (blue). Middle row: XSSR (22 samples, 5%). Bottom row: baseline (100%). Predictions use TP/FP/FN color coding: green = correct, red = over-segmentation, yellow = under-segmentation. Samples sorted by XSSR Dice from worst (left) to best (right).\", \"page\": 9, \"index\": 4, \"width\": 616, \"height\": 258}]"
motivation: 减少跨域医学图像分割中目标域的人工标注成本。
method: 利用无监督掩码自编码器构建共享嵌入空间，通过贪心算法结合密度、新颖性和多样性选择代表性样本，并自动校准新颖性-多样性平衡参数。
result: "在5%标注预算下，Chest X-ray用22个样本达99.3%全数据性能，Prostate MRI超过随机2.5 Dice点，全部数据集超越CoreSet 0.4~1.2 Dice点。"
conclusion: XSSR通过自监督代表性选择有效降低了跨域场景下的标注需求，且多样性是关键组件。
---

## 摘要
获取带标签的医学图像数据是资源密集型的，而在跨域场景中，当源域和目标域在成像设备、人群或临床地点上存在差异时，这一挑战更加严峻。本研究提出了XSSR（跨域自监督代表性选择），这是一个旨在最小化目标域标注工作量的框架，同时保持稳健的分割性能。XSSR包含三个阶段：首先，在无标签源数据上训练掩码自编码器（MAE），以建立共享嵌入空间，无需目标标签；其次，一种贪心选择算法基于复合密度、新颖性和多样性标准对无标签目标样本进行评分；第三，仅在选择后的子集上训练U-Net分割模型。新颖性-多样性权衡参数alpha通过最小化嵌入空间覆盖自动校准，无需手动调整。我们在三个公开基准数据集上评估了XSSR：胸部X光、RIGA+视网膜眼底成像和多中心前列腺MRI，每个数据集均采用固定的5%标注预算。XSSR在胸部X光上仅使用22个标注样本就达到了全数据性能的99.3%，在前列腺MRI上比随机选择高出2.5个Dice点，并且在所有数据集上持续超越CoreSet基线0.4至1.2个Dice点。消融研究表明多样性是最具影响力的评分组成部分，而逐站点分析表明性能与扫描仪与源域的相似性相关。

## Abstract
Acquiring labeled medical image data is resource-intensive and a challenge further exacerbated in cross-domain scenarios where source and target datasets differ in imaging equipment, population, or clinical site. This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection), a framework designed to minimize annotation effort in the target domain while maintaining robust segmentation performance. XSSR comprises three stages: first, a Masked Autoencoder (MAE) is trained on unlabeled source data to establish a shared embedding space without requiring target labels; second, a greedy selection algorithm scores unlabeled target samples based on a composite density, novelty, and diversity criterion; and third, a U-Net segmentation model is trained exclusively on the selected subset. The novelty-diversity trade-off parameter, alpha, is automatically calibrated by minimizing embedding-space coverage, eliminating manual tuning. We evaluate XSSR on three public benchmarks: Chest X-ray, RIGA+ retinal fundus imaging, and multi-site Prostate MRI, each under a fixed 5% annotation budget. XSSR achieves 99.3% of full-data performance on Chest X-ray using only 22 labeled samples, surpasses random selection by up to 2.5 Dice points on Prostate MRI, and consistently outperforms the CoreSet baseline by 0.4 to 1.2 Dice points across all datasets. Ablation studies indicate that diversity is the most influential scoring component, and per-site analysis shows that performance correlates with scanner similarity to the source domain.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在跨域医学图像分割中，源域与目标域之间存在成像设备、人群、临床场景等的差异，传统模型性能下降；且标注医学图像成本高昂、耗时费力。现有域适应方法复杂、依赖大规模数据，未解决标注效率问题；主动学习方法多针对单域，不适用于跨域场景下的代表性样本选择。
- **研究动机**：提出一种能同时处理域漂移和降低标注成本的高效框架，在仅使用目标域极少标注样本的情况下保持高分割性能。
- **整体含义**：通过自监督学习获得可迁移嵌入，结合密度、新颖性、多样性三维度评分自动选择最具信息量的目标样本进行标注，从而大幅减少人工标注量。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：XSSR（Cross-Domain Self-Supervised Representative Selection）是一个三阶段框架：
  1. **自监督表征学习**：在无标签源数据上训练卷积掩码自编码器（MAE），学习共享嵌入空间，无需目标标签。
  2. **贪心样本选择**：基于密度、新颖性、多样性复合评分函数，贪心地从未标注目标域中选择子集。
  3. **分割模型训练**：仅对所选子集训练U-Net分割模型。
- **关键技术细节**：
  - **MAE编码器**：四层步长2的卷积块输出空间特征图，经全局平均池化得到d维向量。
  - **重建损失**：L1损失计算输入与重建图像的差异。
  - **评分函数**：Score(x_i) = λ·Density(x_i) + (1−λ)[α·Novelty(x_i) + (1−α)·Diversity(x_i)]，其中λ控制密度权重，α平衡新颖性和多样性，所有项经z-score归一化。
    - **密度**：负的平均k近邻距离，鼓励选择处于目标域密集区域的样本。
    - **新颖性**：到最近源域嵌入的距离，衡量与源域的差异。
    - **多样性**：到当前已选样本最近嵌入的距离，促进覆盖不同区域。
  - **自动校准α**：通过最小化所选子集对目标域的覆盖度（平均最近邻距离）来自动确定α值，无需手动调整。
- **算法流程**（文字说明）：
  - 输入：目标域嵌入Zt、源域嵌入Zs、预算k。
  - 初始化空集合S。
  - 迭代k次：对每个未选样本计算综合得分，选最高分样本加入S。
  - 返回选中的索引集S。

## 3. 实验设计：数据集、benchmark、对比方法
- **数据集**（三个公开基准）：
  - **Chest X-ray**：源域Montgomery County（美国），目标域Shenzhen Hospital（中国），共453张标注图像，灰度域漂移。
  - **RIGA+视网膜眼底成像**：源域BinRushed和Magrabia，目标域MESSIDOR，共362张标注图像，RGB跨临床站点。
  - **Prostate MRI**：六站点多域基准，源域RUNMC（西门子3T表面线圈），目标域五个不同站点（I2CVB、BMC、UCL、HK、BIDMC），共1159张2D切片，涉及多制造商、场强、线圈类型差异。
- **Benchmark**：所有实验固定5%标注预算，即k = ⌊0.05 × N⌋，具体：胸部X光22张，眼底18张，前列腺MRI 57张。
- **对比方法**：
  - **上界**：使用100%目标域标注训练（Full Data）。
  - **随机选择**：随机抽取k个样本。
  - **CoreSet**：基于贪心最远点采样的多样性选择方法。
- **评估指标**：Dice相似系数（DSC），模型输出二值化阈值0.5。
- **训练配置**：U-Net，100 epoch，batch size 8，学习率1e-3，AdamW优化器，输入分辨率128×128。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、训练时长等具体硬件资源信息。仅提到使用U-Net训练100个epoch，但未提及计算平台或时间成本。

## 5. 实验数量与充分性
- **实验数量**：
  - **方法对比**：三个数据集各对比三种方法（XSSR、CoreSet、随机），给出Dice值。
  - **消融实验**：在三个数据集上分别移除密度、新颖性、多样性组件（共9组实验），分析各组件贡献。
  - **自动校准α分析**：虽然没有单独列出，但通过消融“without novelty”项间接验证了自动校准的效果（前列腺MRI上无新颖性项性能略高，说明自动α赋予新颖性低权重正确）。
  - **标注效率消融**：在三个数据集上分别测试5%、10%、20%、50%标注预算下的性能（共12组）。
  - **按站点分析**：在前列腺MRI上给出五个目标站点各自的性能。
- **充分性与公平性**：
  - 覆盖了多个模态（X光、眼底、MRI）、不同域漂移程度，实验设计较全面。
  - 对比方法均为经典基线（Random、CoreSet），且统一训练参数，公平性较高。
  - 但未对比其他域适应方法（如CycleGAN、DANN等）或更先进的主动学习方法（如Learning Loss），对比范围有限。
  - 消融实验说明了各组件贡献，但缺乏对λ和α全范围的敏感性分析。

## 6. 论文的主要结论与发现
- XSSR在5%标注预算下：
  - 胸部X光：22个样本达0.945 Dice，保留全数据性能的99.3%。
  - 眼底：18个样本达0.940 Dice，相比随机提升1.3 Dice点。
  - 前列腺MRI：57个切片达0.760 Dice，相比随机提升2.5 Dice点。
- 在所有数据集上XSSR均优于CoreSet（0.4~1.2 Dice点）。
- **消融结论**：
  - 多样性是最重要的组件，缺失时前列腺MRI性能从0.732骤降至0.350。
  - 新颖性在眼底（系统性外观差异）中贡献最大。
  - 密度提供一致但仍较小的增益。
- **按站点分析**：性能随扫描仪与源域相似度降低而下降，BIDMC（GE 3T内腔线圈）保留率最低（84.5%）。
- **标注效率**：低预算下（5%）XSSR优势最明显，预算增加后随机采样逐渐追赶，在前列腺MRI上10%以上随机略优于XSSR（因随机自然覆盖多站点比例）。

## 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - 首次将自监督表征学习与跨域样本选择结合，无需目标域标签即可获得可迁移嵌入。
  - 三位一体评分函数（密度+新颖性+多样性）综合考虑代表性、域差异和覆盖度，避免CoreSet仅靠多样性可能选到离群点的问题。
  - 自动校准α机制，消除手动调参，适应不同域漂移特性。
- **实验设计亮点**：
  - 覆盖三种成像模态（X光、眼底、MRI），域漂移类型多样（灰度/RGB、场强/制造商/线圈），验证泛化性。
  - 消融实验清晰揭示各组件在不同场景下的相对重要性。
  - 标注效率曲线（5%~50%）展示了XSSR在极低标注预算下的显著优势。

## 8. 不足与局限
- **实验覆盖不足**：
  - 未与更多先进的主动学习方法（如学习损失、变分主动学习）或域适应方法（如CycleGAN、DANN）对比。
  - 仅验证了一个预算水平（5%），虽然给出了不同预算的探索，但非主要对比指标。
  - 未在更大规模/更多模态的数据集（如CT、超声）上验证。
- **偏差风险**：
  - 前列腺MRI实验中，随机采样在10%以上反而优于XSSR，说明XSSR的嵌入驱动选择在混合多站点分布时可能偏向某些站点，缺乏显式的站点平衡。
  - 自动校准α基于覆盖度最小化，可能在某些分布下不稳健。
- **应用限制**：
  - 假设源域有足够无标签数据训练MAE，若源域很小则自监督表征质量下降。
  - 依赖预定义预算k，未考虑自适应停止条件。
  - 未讨论模型在完全未见域上的泛化能力。

（完）
