---
title: SwInception -- Local Attention Meets Convolutions
title_zh: SwInception——局部注意力与卷积的结合
authors: "David Hagerman, Roman Naeem, Jakob Lindqvist, Carl Lindström, Fredrik Kahl, Lennart Svensson"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29954v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: Swin Transformer结合Inception用于医学体积分割
tldr: 本文提出SwInception架构，通过在Swin Transformer的Feed-Forward层引入Inception多分支卷积，增强局部归纳偏置和跨尺度特征推理，并改进解码器以减少参数。在11个医学数据集上超越先前SOTA，展示了稀疏视觉Transformer在分割任务中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29954v1/fig-001.webp\", \"caption\": \"Table 5: Visual comparison using examples from MSD. A. Liver cancer. B. Colon cancer. C. Pancreatic cancer. Green denotes organ segmentation, and yellow denotes cancer segmentation for all examples.\", \"page\": 11, \"index\": 1, \"width\": 673, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2605.29954v1/fig-002.webp\", \"caption\": \"Table 3: Ablation study over encoder, decoder, and patch-merging strategies on MSD Prostate.\", \"page\": 10, \"index\": 2, \"width\": 688, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.29954v1/fig-003.webp\", \"caption\": \"Fig. 1: An overview of the SwInception architecture for volumetric segmentation.\", \"page\": 4, \"index\": 3, \"width\": 639, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2605.29954v1/fig-004.webp\", \"caption\": \"Table 1: Cross-validation performance on MSD from SwInception, SwinUNETR, nnUNet, and DiNTS.\", \"page\": 9, \"index\": 4, \"width\": 616, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.29954v1/fig-005.webp\", \"caption\": \"Fig. 2: (a) A SwInception block with layer normalization (LN), windowed multihead self-attention (W-MHSA), and the Inception feed-forward block. (b) The Inception feed-forward block. Here C denotes the number of channels.\", \"page\": 5, \"index\": 5, \"width\": 588, \"height\": 428}]"
motivation: Swin Transformer在小型数据集上易过拟合，需增强其归纳偏置。
method: 在Swin的FFN中插入Inception块，实现多尺度局部卷积，并优化解码器。
result: 在11个医学数据集上性能提升，尤其在Medical Segmentation Decathlon等基准中超越先前SOTA。
conclusion: 强化Swin的归纳偏置可提升稀疏视觉Transformer在医学和自然图像分割中的能力。
---

## 摘要
稀疏视觉变换器作为医学体积分割的高效编码器越来越受欢迎，其中Swin成为主要选择。Swin使用局部注意力来降低复杂度，在许多任务上表现优异，但容易在小型数据集上过拟合。为了缓解这一弱点，我们提出了一种新颖的架构，通过在馈送层中引入Inception块进一步增强Swin的归纳偏置。这些多分支卷积的引入使得能够在变换器块内对局部多尺度特征进行更直接的推理。我们还修改了解码器层，以使用更少的参数捕捉更精细的细节。通过广泛的实验，我们在十一个不同的医学数据集上展示了性能提升。我们特别展示了在医学分割十项全能（Medical Segmentation Decathlon）和颅穹窿外（Beyond the Cranial Vault）等基准挑战上，相比之前最先进的骨干网络的进步。通过证明Swin中现有的归纳偏置可以进一步改进，我们的工作为提高稀疏视觉变换器在医学和自然图像分割任务中的能力提供了一条有前景的途径。代码和预训练权重可在https://github.com/Eiphodos/SwInception获取。

## Abstract
Sparse vision transformers have gained popularity as efficient encoders for medical volumetric segmentation, with Swin emerging as a prominent choice. Swin uses local attention to reduce complexity and yields excellent performance for many tasks but still tends to overfit on small datasets. To mitigate this weakness, we propose a novel architecture that further enhances Swin's inductive bias by introducing Inception blocks in the feed-forward layers. The introduction of these multi-branch convolutions enables more direct reasoning over local, multi-scale features within the transformer block. We have also modified the decoder layers in order to capture finer details using fewer parameters. We demonstrate a performance improvement on eleven different medical datasets through extensive experimentation. We specifically showcase advancements over the previous state-of-the-art backbones on benchmark challenges like the Medical Segmentation Decathlon and Beyond the Cranial Vault. By showing that the existing inductive bias in Swin can be further improved, our work presents a promising avenue for enhancing the capabilities of sparse vision transformers for both medical and natural image segmentation tasks. Code and pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：Swin Transformer 在医学体积分割中表现出色，其局部注意力机制降低了计算复杂度并引入了归纳偏置，但在小数据集上仍容易过拟合。现有的稀疏视觉变换器（如 Swin）的归纳偏置主要局限于注意力窗口，尤其在医学分割中窗口尺寸受 GPU 内存限制而较小，导致捕捉局部特征的能力不足。
- **整体含义**：本文通过增强 Swin 的局部归纳偏置来提升其在小数据集上的泛化能力，进而推动稀疏视觉变换器在医学及自然图像分割任务中的应用。

### 2. 论文提出的方法论
- **核心思想**：在 Swin Transformer 的 **馈送层（Feed-Forward Layer）** 中引入 **Inception 多分支卷积**，替代原来的 MLP。利用卷积的强局部归纳偏置、多尺度感受野和更好的位置编码，同时保留 Swin 的稀疏注意力优势。
- **关键技术细节**：
  - **SwInception 块**：由 Layer Normalization → Window Multi-Head Self-Attention → 残差连接 → Layer Normalization → Inception FF（馈送层）→ 残差连接组成。
  - **Inception 馈送层**：包含四个并行的分支：
    - 1×1×1 卷积（单卷积块）
    - 3×3×3 卷积（先 1×1×1 瓶颈降维，再 3×3×3）
    - 5×5×5 卷积（等效于两个 3×3×3，同样先瓶颈降维）
    - 平均池化（3×3×3）后接 1×1×1 卷积
    - 所有分支输出拼接后经线性层降回原始通道数。
  - **改进的解码器**：基于 SwinUNETR 解码器，但特征提取改为在 **patch-merging 之前**，从而获得更高分辨率特征，减少一次上采样，并降低解码器参数量。
  - **Patch Merging**：采用重叠卷积（overlapping convolutional layer）替代原始线性合并，增强后续阶段表示能力。
- **公式流程**（文字说明）：
  - 输入 X → LN → SW-MHSA → 残差得到 Z
  - Z → LN → 四个 Inception 分支并行处理 → 拼接 → Linear → 残差得到 Y

### 3. 实验设计
- **数据集**：共 11 个医学数据集。
  - **Medical Segmentation Decathlon (MSD)**：包含 10 个任务（6 个 CT，4 个 MRI），如脑肿瘤、心脏、肝脏、海马体、前列腺、肺、胰腺、肝血管、脾脏、结肠等。
  - **Beyond the Cranial Vault (BTCV)**：腹部多器官分割，30 个 CT 体积。
- **Benchmark**：与 MSD 挑战中排名前三的模型（SwinUNETR、nnUNet、DiNTS）以及 Universal Model 对比。
- **对比方法**：SwinUNETR, nnUNet, DiNTS, Universal Model（分别基于 SwinUNETR 和 SwInception 骨干）。
- **评估指标**：平均 Dice 系数（Mean Dice），采用 5 折交叉验证。

### 4. 资源与算力
- **明确说明**：所有模型使用 **4 块 A100 GPU** 在单个节点上训练，采用混合精度（mixed precision）。预训练使用与 SwinUNETR 相同的数据集和方法。
- **训练细节**：未具体说明每折训练时长，但提及使用 SwinUNETR 的超参数，预训练权重非严格加载（前 25 轮冻结部分层）。

### 5. 实验数量与充分性
- **实验数量**：
  - 主实验：MSD 全部 10 个任务 + BTCV（共 11 个数据集）的 5 折交叉验证。
  - 消融实验（4 组）：
    - 编码器-解码器-合并策略组合（表 3，在 Prostate 上）
    - Inception vs. 深度可分离卷积（表 4，在 Liver、Pancreas、Colon 上）
    - 小数据集性能比较（表 6，在 Hippocampus 和 Colon 上取 10%、25%、50% 子集）
    - SwInception 作为 UniversalModel 骨干（表 7，MSD CT 任务 + BTCV）
  - 可视化展示（表 5）。
- **充分性与公平性**：
  - 对比方法均使用各自官方代码、权重和最优超参数。
  - 所有实验采用 5 折交叉验证，避免单次划分偏差。
  - 消融实验严格控制参数数量（通过调整 MLP ratio 使参数接近）。
  - 但未对测试集直接评测（因挑战关闭且后处理未知），仅用交叉验证，这虽公平但可能遗漏与真实排行榜的对比。

### 6. 论文的主要结论与发现
- **主要结论**：在 Swin 的馈送层中引入 Inception 卷积（即 SwInception 架构）能显著增强局部归纳偏置，在 11 个医学数据集上均超越 SwinUNETR、nnUNet 和 DiNTS 等先前 SOTA，尤其在小型数据集上相对提升更大（如结肠 10% 子集 Dice 从 15.18% 增至 20.42%）。
- **其他发现**：
  - 改进的解码器在减少参数的同时提升性能。
  - SwInception 作为 UniversalModel 骨干也带来稳定增益（如 Lung 任务 Dice 从 57.07% 提升至 60.06%）。

### 7. 优点
- **方法创新**：首个将 Inception 多分支卷积直接集成到 Transformer 馈送层的工作，兼顾多尺度特征和强归纳偏置。
- **实验全面**：覆盖多个解剖部位、模态（CT/MRI）和任务（器官分割、肿瘤检测），消融实验设计合理（控制参数、分析不同组件贡献）。
- **性能显著**：在 11 个数据集上持续超越 SOTA，且在小样本场景下优势突出。
- **开源友好**：提供代码和预训练权重，可复现。

### 8. 不足与局限
- **实验覆盖**：
  - 未在自然图像分割任务上验证（论文声称适用于自然图像但无实验）。
  - 仅使用交叉验证，未在 MSD 和 BTCV 官方测试集上直接评测（因挑战关闭及后处理未知），可能低估与实际排行榜的差距。
- **偏差风险**：
  - 所有实验仅在医学影像领域进行，泛化性未证明。
  - 超参数调优可能偏向 SwInception*（优化版），与基线 SwinUNETR 的公平性需注意。
- **应用限制**：
  - 引入 Inception 块增加了参数（65M vs 63M），但基本持平；不过在实际部署中可能增加推理计算量。
  - 预训练策略需依赖 SwinUNETR 的设置，初始化部分权重冻结 25 轮，可能不是最优。
  - 对于极大数据集（如 >1000 例），未测试 SwInception 是否仍有优势。

（完）
