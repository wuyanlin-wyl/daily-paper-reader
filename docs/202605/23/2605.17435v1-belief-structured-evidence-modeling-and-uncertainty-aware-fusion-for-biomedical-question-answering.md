---
title: "BELIEF: Structured Evidence Modeling and Uncertainty-Aware Fusion for Biomedical Question Answering"
title_zh: BELIEF：面向生物医学问答的结构化证据建模与不确定性感知融合
authors: "Chang Zong, Hao Ning, Siliang Tang, Jie Huang, Jian Wan"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17435v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 结构化证据建模和不确定性融合用于生物医学问答
tldr: 生物医学问答需从文献中决策，但文献质量不均。BELIEF将检索文献转为结构化证据对象，记录临床属性、来源质量、相关性、支持强度和候选假设，并通过符号路径（Dempster-Shafer理论）和神经路径（LLM）进行不确定性感知融合，经可靠性感知仲裁模块输出。在PubMedQA、MedQA和MedMCQA上，BELIEF在30个设置中25个取得最佳，提升了证据利用和决策可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17435v1/fig-001.webp\", \"caption\": \"Fig. 1. Traditional RAG treats retrieved evidence as flat context, whereas BELIEF structures evidence and exposes reliability and uncertainty signals.\", \"page\": 1, \"index\": 1, \"width\": 520, \"height\": 499}, {\"url\": \"assets/figures/arxiv/2605.17435v1/fig-002.webp\", \"caption\": \"Fig. 4. Divergent Acc of arbitration on cases where the D-S and LLM paths produce different predictions.\", \"page\": 10, \"index\": 2, \"width\": 512, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.17435v1/fig-003.webp\", \"caption\": \"TABLE VI UNCERTAINTY-STRATIFIED ACCURACY OF BELIEF AND NAIVE RAG.\", \"page\": 10, \"index\": 3, \"width\": 504, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2605.17435v1/fig-004.webp\", \"caption\": \"TABLE III RESULTS OF GENERAL LLMS UNDER DIFFERENT QA PARADIGMS. BEST RESULTS ARE SHOWN IN BOLD, SECOND-BEST RESULTS ARE UNDERLINED.\", \"page\": 8, \"index\": 4, \"width\": 1032, \"height\": 679}, {\"url\": \"assets/figures/arxiv/2605.17435v1/fig-005.webp\", \"caption\": \"Fig. 6. Illustration of complementary reasoning behaviors and reliability-aware arbitration in BELIEF. The upper panel shows a reasoning–answer mismatch in the neural LLM path, which is corrected through evidence-grounded symbolic reasoning. The lower panel presents a symbolic evidence insufficiency scenario, where high uncertainty in the D-S branch causes the arbitration mechanism to defer to the neural LLM path.\", \"page\": 12, \"index\": 5, \"width\": 1058, \"height\": 523}]"
motivation: 现有检索增强LLM将文献视为扁平文本，隐含证据可靠性和不确定性，导致决策不透明。
method: BELIEF将文献转换为结构化证据对象，通过D-S理论的符号路径和LLM的神经路径进行不确定性感知融合，并用仲裁模块调和两路径输出。
result: 在三个数据集和五个LLM骨干上，BELIEF在30个设置中25个取得最佳，与生物医学域模型相比也具有竞争力。
conclusion: BELIEF通过显式建模证据结构和不确定性，提升了检索证据利用率和决策可靠性。
---

## 摘要
生物医学问答通常需要从检索到的文献中做出决策，而这些文献的相关性、质量以及对候选答案的支持度参差不齐。大多数检索增强的大语言模型方法将这些文献作为纯文本输入模型，使得证据的可靠性和剩余不确定性在很大程度上隐含不显。我们提出BELIEF，一种用于封闭集生物医学问答的结构化证据建模与不确定性感知融合框架。BELIEF不将检索到的文档视为无差别的上下文，而是将其转换为证据对象，记录临床属性、来源质量、问题相关性、支持强度以及相关的候选假设。这些证据对象为两条互补的推理路径提供了共享基础。符号路径在有限答案空间上基于Dempster-Shafer理论构建可靠性加权的基本概率分配，并执行不确定性感知的符号证据融合以估计信念和剩余不确定性。神经路径则利用相同的结构化证据进行基于LLM的语义推理，同时一个可靠性感知的仲裁模块根据信念强度、不确定性、证据可靠性和语义一致性来协调符号与神经输出。在PubMedQA、MedQA和MedMCQA上使用五个通用LLM骨干进行的实验表明，BELIEF在30个骨干-数据集-度量设置中的25个上取得了最佳结果。与生物医学领域模型的比较表明，BELIEF在MedQA和MedMCQA上具有竞争力，而专业化的生物医学预训练在PubMedQA上仍然具有优势。消融、互补性、不确定性分层和成本分析进一步表明，BELIEF通过显式化证据结构、路径分歧和决策不确定性，提高了检索证据的利用率。

## Abstract
Biomedical question answering often requires decisions from retrieved literature whose relevance, quality, and support for candidate answers are uneven. Most retrieval-augmented large language model (LLM) methods feed this literature to the model as flat text, leaving evidence reliability and remaining uncertainty largely implicit. We propose BELIEF, a structured evidence modeling and uncertainty-aware fusion framework for closed-set biomedical question answering. Rather than treating retrieved documents as undifferentiated context, BELIEF converts them into evidence objects that record clinical attributes, source quality, question relevance, support strength, and the associated candidate hypothesis. These evidence objects provide a shared basis for two complementary reasoning paths. The symbolic path constructs reliability-weighted basic probability assignments based on Dempster--Shafer (D-S) theory over a finite answer space and performs uncertainty-aware symbolic evidence fusion to estimate belief and residual uncertainty. The neural path uses the same structured evidence for LLM-based semantic inference, while a reliability-aware arbitration module reconciles the symbolic and neural outputs according to belief strength, uncertainty, evidence reliability, and semantic consistency. Experiments on PubMedQA, MedQA, and MedMCQA with five general-purpose LLM backbones show that BELIEF obtains the best result in 25 of 30 backbone--dataset--metric settings. Comparisons with biomedical-domain models indicate that BELIEF is competitive on MedQA and MedMCQA, while specialized biomedical pretraining remains advantageous on PubMedQA. Ablation, complementarity, uncertainty-stratified, and cost analyses further show that BELIEF improves retrieved-evidence utilization by making evidence structure, path disagreement, and decision uncertainty explicit.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：生物医学问答（QA）常依赖检索到的文献证据做出决策，但文献在相关性、质量、支持强度上参差不齐。现有检索增强大语言模型（RAG）方法将检索文献作为扁平文本输入，依赖隐式神经聚合，无法显式建模证据可靠性、剩余不确定性和多证据对候选假设的支持，导致预测不稳定或过度自信。
- **整体含义**：本文提出将生物医学QA重新定义为“不确定性下的结构化证据到决策问题”，要求将非结构化文献转化为可比较的结构化证据对象，量化可靠性和不确定性，并显式暴露决策信号，以支持更可靠的决策。

## 2. 论文提出的方法论

### 核心思想
BELIEF（Biomedical Evidence Modeling with Uncertainty-Aware Evidence Fusion）将检索文献转换为结构化证据对象，通过两条互补推理路径（符号路径基于Dempster–Shafer理论进行可靠性加权融合，神经路径使用LLM进行语义推理）处理，最后由可靠性感知仲裁模块整合输出。

### 关键技术细节
1. **结构化证据建模**：
   - 问题分析：提取PICOS元素（Population, Intervention, Comparison, Outcome, Study design）或核心概念；定义有限答案空间（Frame of Discernment）Θ = {h₁, …, hL}。
   - 证据检索：构造关键字查询Q(q) = Compose(Kc, Ka)，从PubMed检索top-k文档（默认k=5）。
   - 证据对象：每个文档̃eᵢ = (PICOSᵢ, xᵢ) ，包含临床属性、文本片段。
   - 假设条件标注aᵢ = (ηᵢ, rᵢ, sᵢ, hᵢ)：来源质量、相关性、支持强度、主要支持假设（通过约束提示获得，不暴露真实答案）。
2. **符号D-S路径**：
   - 基于标注计算可靠性得分Wᵢ = Rᵢ·r′ᵢ（Rᵢ来源质量得分，r′ᵢ相关性权重）和支持强度Dᵢ。
   - 基本概率分配（BPA）：mᵢ({hᵢ}) = Wᵢ·Dᵢ，mᵢ(Θ) = 1 - Wᵢ·Dᵢ（仅分配质量给主要假设和无知集）。
   - 使用Dempster组合规则迭代融合所有BPA：m* = ⊕ᵢ mᵢ。
   - 计算信念函数Bel({hj})，并根据阈值τ（默认0.5）决定是否输出最终符号预测zDS或“不确定”。
3. **神经LLM路径**：
   - 以相同结构化证据为输入，LLM产生预测zLLM和推理轨迹pLLM。
4. **可靠性感知仲裁**：
   - 输入包Iarb包含符号预测、信念向量b、残余不确定性m*(Θ)、总体证据可靠性W̄、神经预测及理由。
   - 三阶段决策：①符号信号评估（信念、不确定性、可靠性）；②神经一致性检查（LLM输出是否与证据语义一致）；③可靠性感知整合（根据证据可靠性选择或协调输出）。
   - 输出最终答案y*、可靠性状态g、整合理由ρ。

### 公式/算法流程（文字说明）
- 每个证据的BPA：m({h}) = W·D，m(Θ)=1-W·D
- 迭代融合：对两个BPA，组合后非空子集C的质量为交叉部分乘积之和除以归一化因子Z。
- 最终决策需要符号路径信念大于阈值，否则神经路径主导，仲裁模块综合判断。

## 3. 实验设计

### 数据集
- **PubMedQA**：基于文献的yes/no/maybe任务（500个测试实例）
- **MedQA**：专业医学考试（500个实例）
- **MedMCQA**：大规模多选题（1000个实例）
（所有测试实例从官方划分中随机固定种子采样）

### 基准方法
- **通用LLM骨干**：Qwen2.5-7B, Qwen3-8B, Qwen3-32B, GPT-4o-mini, GPT-OSS-20B
- **对比范式**：
  - Direct Generation（仅内部知识）
  - Naive RAG（检索-阅读）
  - RAT（检索融入思维链）
  - Self-RAG（自反思）
  - CRAG（纠正性检索）
- **生物医学特化模型**（额外对比）：BioMistral-7B, Self-BioRAG-7B, UltraMedical-8B, Huatuo-GPT-o1-8B, Llama-3-Meditron-8B, MEDITRON-70B

### 评价指标
- **主要**：Accuracy (ACC)
- **辅助**：Macro-F1

### 实验设置
- 所有检索方法统一使用PubMed检索，top-k=5
- 零样本设置，4096 token上下文窗口，低温度解码
- BELIEF的符号路径固定阈值τ=0.5，未进行任务特定调优

## 4. 资源与算力

论文未明确说明训练所需的GPU型号、数量及训练时长。文中仅在实践成本分析中提及使用GPT-4o-mini作为骨干进行推理时间测量（平均每问题64.31秒，P95延迟77.98秒，相对token消耗为Naive RAG的5.21倍），但未涉及模型训练或微调。因此**算力资源未被详细披露**。

## 5. 实验数量与充分性

- **主实验**：5个骨干 × 3个数据集 × 2个指标 = 30个设置（表III），BELIEF在25个最佳。
- **消融实验**（图3）：5个变体（w/o结构化证据、w/o标注、w/o D-S融合、w/o LLM、w/o仲裁），在多个骨干上验证。
- **互补性分析**（表V）：量化双路径的预测一致性和仲裁对分歧样本的解决率。
- **不确定性分层分析**（表VI）：按残余不确定性分组（低/中/高）对比BELIEF和Naive RAG。
- **超参数敏感性**（图5）：分析检索深度k和阈值τ对性能的影响。
- **成本分析**（表VII）：比较不同方法的推理时间和token消耗。
- **案例研究**（图6）：展示互补推理和仲裁行为。
- **公平性**：所有方法在同一采样集、同一检索池、相同推理环境下评估；标注和仲裁过程未暴露真实答案；使用固定prompt模板。

**结论**：实验数量充足，覆盖多维度分析，对比方法和骨干多样，公平性有保障，充分支持结论。

## 6. 论文的主要结论与发现

1. BELIEF在30个骨干-数据集-度量设置中25个取得最佳，所有15个骨干-数据集比较中均优于Naive RAG。
2. 在小/中型骨干上增益更明显，表明结构化证据建模弥补了模型隐式聚合能力的不足。
3. 消融实验证明各组件（结构化证据、标注、D-S融合、LLM、仲裁）均对最终性能有贡献。
4. 符号与神经路径在约50%的样本上产生相同预测，在分歧样本上仲裁正确率显著高于简单选择某一路径。
5. 不确定性分层显示：在高不确定性组中BELIEF相比Naive RAG的增益更大（如PubMedQA增益达13.5个百分点），表明不确定性建模有助于困难场景。
6. 检索深度k=5时效果最优；阈值τ在0.5附近稳定。
7. 与生物医学特化模型相比，BELIEF在MedQA和MedMCQA上具有竞争力，但PubMedQA上特化预训练模型仍占优，提示两者互补。

## 7. 优点

- **显式结构化证据**：将非结构化文献转化为包含临床属性、可靠性、支持强度的结构化对象，增强了可解释性和可控性。
- **不确定性感知融合**：通过D-S理论显式量化信念和残余不确定性，避免过度自信。
- **双路径互补**：符号路径提供证据可审计的信念，神经路径利用隐式知识，仲裁机制灵活协调。
- **可靠性仲裁**不仅依赖置信度，还考虑证据可靠性、语义一致性和不确定性，提高了鲁棒性。
- **实验全面**：覆盖多种骨干、数据集、对比方法，进行了消融、互补性、不确定性分层、敏感性等分析。
- **模块化设计**：标注策略固定，可复用；无需重新训练即可适配不同LLM。

## 8. 不足与局限

- **仅限于封闭集问答**：要求有限答案空间（Θ），无法直接用于开放生成式QA。扩展需要动态假设构建。
- **BPA简化**：只将质量分配给主要假设和无知集，未考虑多假设支持或证据间冲突的精细建模（如质量分配给子集）。
- **依赖检索和标注质量**：若检索漏掉相关证据或标注出错，不确定性融合难以弥补。标注由LLM通过固定prompt完成，可能存在错误。
- **额外推理开销**：结构化处理、双路径推理和仲裁导致更高的延迟和token消耗（约Naive RAG的5.21倍），不适合延迟敏感的对话场景。
- **未明确训练成本**：论文未提及模型训练或微调所需资源，限制了可复现性评估。
- **部分设置下Macro-F1未达最优**：表明仲裁机制主要提升整体准确率，但类平衡性能有时不如某些基线。
- **生物医学领域的PubMedQA上，特化模型仍优于BELIEF**，提示在专业知识密集的任务上，纯结构化证据可能不如领域预训练有效。

（完）
