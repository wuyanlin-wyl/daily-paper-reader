---
title: Vision Harnessing Agent for Open Ad-hoc Segmentation
title_zh: 面向开放即席分割的视觉利用智能体
authors: "Zilin Wang, Stella X. Yu"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19410v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用VLM智能体的开放即席分割视觉利用智能体
tldr: 针对开放临时概念的分割难题，提出VASA，一种无需训练的视觉引导分割代理，结合VLM代理与分割基础模型，通过持久工作掩码进行推理、构建和验证。构建PARS基准，在PARS和RefCOCOm上超越基线，验证了代理视觉构建的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19410v1/fig-001.webp\", \"caption\": \"Figure 4: PARS provides detailed long-form descriptions for open ad-hoc concepts via a semiautomated pipeline. Given a small set of annotated examples for each concept, we prompt a VLM to disambiguate the target concept and what to include or exclude, as if producing segmentation guidelines for human annotators. Manual verification is then applied for quality control.\", \"page\": 6, \"index\": 1, \"width\": 819, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-002.webp\", \"caption\": \"Figure 10: Additional results on RefCOCOm [64]. We compare with baselines on diverse examples.\", \"page\": 21, \"index\": 2, \"width\": 814, \"height\": 1279}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-003.webp\", \"caption\": \"Figure 1: Open ad-hoc segmentation requires constructing visual concepts on the fly, not merely retrieving common ones. We compare LISA [9], Seg-Zero [10], SAM3 Agent [1], and VASA (our proposed vision harnessing agent), on three prompts for the same image. Row 1 asks for a known visual whole, cat; all methods succeed. Row 2 asks for an ad-hoc collection, the cat’s right paw and the stick she is reaching for; baselines retrieve only one part of the requested composition, while VASA captures both. Row 3 asks for an exclusion-defined concept, the cat’s head without the ears and eyes; baselines confuse it with cat head or cat, while VASA constructs the requested mask. The comparison illustrates the central challenge of open ad-hoc segmentation: The required grounding may not exist as a single learned concept and must be built from parts, relations, and exclusions.\", \"page\": 2, \"index\": 3, \"width\": 814, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-004.webp\", \"caption\": \"Table 1: VASA outperforms all baselines on both ad-hoc and common concepts in PARS, approaching fully supervised closed-world methods. We reproduce all baselines and report gIoU, cIoU, and the proposed xIoU metric for cross-concept confusion. VASA substantially improves upon its underlying segmentation tool, SAM3, and consistently outperforms prior agentic methods, particularly SAM3 Agent. ∗ VLPart is trained on PartImageNet, from which PARS is derived, and is excluded from direct comparison; nevertheless, VASA achieves comparable performance without task-specific training. † Semantic-SAM uses a ground-truth-derived point prompt for inference.\", \"page\": 7, \"index\": 4, \"width\": 818, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-005.webp\", \"caption\": \"Figure 11: Additional results on RefCOCOm [64]. We compare with baselines on diverse examples.\", \"page\": 22, \"index\": 5, \"width\": 814, \"height\": 1285}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-006.webp\", \"caption\": \"Figure 2: VASA reasons, constructs, and validates an open ad-hoc segmentation solution, while SAM3 Agent searches over text prompts. We compare the rolled-out prediction process of SAM3 Agent and VASA on the query the cat’s head, without the ears and eyes. Top: SAM3 Agent handles the task by revising text queries to SAM3, trying prompts such as cat muzzle, cat nose, and cat head. Each trial is a fresh retrieval attempt: Intermediate masks are neither preserved nor used to build the final answer. Bottom: VASA instead plans a visual strategy, maintains a persistent working mask, and updates it through executable operations such as segment, accept, and remove. Reading the two processes side by side shows the key difference: SAM3 Agent searches over prompts, whereas VASA incrementally constructs a visual solution, allowing VASA to succeed on this open ad-hoc concept.\", \"page\": 3, \"index\": 6, \"width\": 816, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-007.webp\", \"caption\": \"Table 2: VASA achieves state-of-the-art performance on the multi-granularity referring segmentation benchmark RefCOCOm. We compare against referring segmentation methods, reasoning segmentation methods, and agentic segmentation frameworks under both part-only and part-and-object settings, focusing on gIoU. VASA consistently outperforms prior methods across all subsets and settings, with especially strong gains over the closest competitor, SAM3 Agent. The gains are particularly notable in the partonly setting, where targets are more finegrained and ambiguous, requiring careful reasoning and verification. We reproduce SAM3 and all agentic methods, while the remaining results are reported by [62, 65].\", \"page\": 8, \"index\": 7, \"width\": 460, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-008.webp\", \"caption\": \"Figure 6: Additional reasoning steps correlate with improved segmentation quality. The x-axis shows the number of more reasoning steps used by VASA than SAM3 Agent, while the y-axis shows the corresponding gIoU improvement. Darker colors indicate bins containing more instances. Positive correlations suggest that additional long-horizon reasoning and refinement are often beneficial for challenging open ad-hoc concepts. VASA updates a persistent working mask after each segmentation step, naturally resulting in more reasoning steps than SAM3 Agent.\", \"page\": 9, \"index\": 8, \"width\": 820, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2605.19410v1/fig-009.webp\", \"caption\": \"Figure 7: Qualitative comparisons between SAM3 Agent and VASA on PARS (left) and RefCOCOm (right). SAM3 Agent often oversegments semantically related regions or confuses nearby concepts, while VASA precisely follows the text query through visual reasoning and iterative mask refinement. This enables VASA to isolate fine-grained target regions in both open ad-hoc and referring segmentation. PARS prompts are shortened for clarity, while RefCOCOm prompts are original.\", \"page\": 9, \"index\": 9, \"width\": 814, \"height\": 231}]"
motivation: 现有分割方法难以处理开放临时概念，因为缺乏现成视觉映射，需通过部分、关系、排除和集合等证据构建。
method: 提出VASA，利用视觉语言模型（VLM）代理、分割基础模型和视觉引导工作流，通过持久工作掩码进行规划、调用工具、检查结果、编辑掩码和错误恢复。
result: "在PARS基准上，VASA超越开放词汇、推理和代理基线，比SAM3 Agent提升14-25%；在RefCOCOm上提升5-9%，比其他代理基线提升达20%。"
conclusion: 验证了代理视觉构建对开放临时分割的有效性，并指出AI代理应在基础模型之外编程任务知识、VLM行为、视觉例程、工作记忆和失败感知工作流。
---

## 摘要
当概念已知时，分割变得容易，只需从文本中检索学习到的视觉基础。对于开放即席概念，分割仍然困难，因为基础可能不存在于单一学习掩码中，而通常需要通过部分、关系、排除和集合从图像证据中构建。我们提出了一种视觉引导的即席分割智能体（VASA），这是首个用于开放即席分割的视觉利用智能体。VASA无需训练，将VLM智能体、分割基础模型和视觉引导工作流相结合。VASA不是仅修改文本提示，而是使用持久的工作掩码来推理、构建和验证解决方案。它规划视觉操作，调用分割工具，检查结果，编辑掩码，并从错误中恢复。我们构建了PARS，这是一个新的基准，通过长形式定义查询将PartImageNet中的部分级别标签转化为开放即席概念。在PARS上，VASA优于开放词汇、基于推理和智能体基线，比SAM3 Agent高出14-25%。在RefCOCOm（一个标准的多粒度指代分割基准）上，VASA比SAM3 Agent提高5-9%，比其他智能体基线提高高达20%。这些结果验证了智能体视觉构建在开放即席分割中的有效性。我们的工作为AI智能体超越将基础模型作为工具封装指明了道路：通过任务知识、VLM行为、视觉例程、工作记忆和故障感知工作流来编程它们。

## Abstract
Segmentation has become easy when the concept is known, requiring retrieval of a learned visual grounding from text. It remains hard for open ad-hoc concepts, where the grounding may not exist as one learned mask and must often be constructed from image evidence through parts, relations, exclusions, and collections. We propose a Vision-guided Ad-hoc Segmentation Agent (VASA), the first vision harnessing agent for open ad-hoc segmentation. VASA is training-free and couples a VLM agent, a segmentation foundation model, and a visually grounded workflow. Rather than revising text prompts alone, VASA uses a persistent working mask to reason, construct, and validate a solution. It plans visual operations, invokes segmentation tools, inspects results, edits the mask, and recovers from errors. We construct PARS, a new benchmark that turns part-level labels in PartImageNet into open ad-hoc concepts through long-form definition queries. On PARS, VASA outperforms open-vocabulary, reasoning-based, and agentic baselines, surpassing SAM3 Agent by 14-25%. On RefCOCOm, a standard multi-granularity referring segmentation benchmark, VASA improves over SAM3 Agent by 5-9% and over other agentic baselines by up to 20%. These results validate agentic visual construction for open ad-hoc segmentation. Our work points to a path for AI agents beyond wrapping foundation models as tools: Programming them with task knowledge, VLM behavior, visual routines, working memory, and failure-aware workflows.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有分割方法在处理已知概念（如“猫”“车”）时表现良好，因为这类概念在训练数据中有丰富的视觉映射。但对于**开放即席（open ad-hoc）概念**——即用户临时定义的、可能涉及部分、关系、排除、集合等复杂语义的任意概念——难以直接通过文本检索得到现成的掩码，而需要从图像证据中动态构建。
- **研究背景**：当前主流方法（如LISA、Seg-Zero、SAM3 Agent）要么通过语言推理改进文本提示，要么将分割模型作为工具由VLM调用，但都存在“语言主导、视觉构建不持久”的问题：每次尝试都是独立检索，中间结果不保留，缺乏对视觉构建过程的积累和验证。
- **整体含义**：开放即席分割要求模型具备对视觉概念的构造能力，而非仅仅检索已习得的概念。论文指出这不仅仅是分割任务，更是AI智能体如何利用视觉工作记忆、操作和故障恢复能力来完成复杂视觉构建的范式探索。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：提出 **VASA（Vision-guided Ad-hoc Segmentation Agent）**，一种无需训练的视觉利用智能体，将分割问题转化为**基于持久工作掩码的迭代视觉构建过程**。通过耦合VLM智能体、分割基础模型（SAM3）和一套“视觉 harness 工作流”，实现对开放即席概念的逐步分解、组合、排除和细化。
- **关键技术细节**：
  1. **状态管理**：维护持久状态，包括输入图像、用户查询、当前策略、工作掩码（初始为空）、动作历史等。工作掩码是核心状态，记录已构建的视觉结果，可跨轮次更新。
  2. **工具调用与掩码编辑**：使用 `segment_phrase` 调用 SAM3 获取候选掩码，`examine_each_mask` 让VLM检查视觉叠加结果，`update_working_mask` 根据VLM选择的候选和操作（ADD/REMOVE/REPLACE）执行像素级布尔更新。
  3. **长视距规划**：VLM首先选择整体策略（如“直接检索”“过分割再移除”“欠分割再添加”“粗到细细化”），然后逐步执行，并根据中间结果调整策略，受预算和交互历史约束。
  4. **视觉审查与约束**：每个步骤后，VLM检查工作掩码是否满足查询中的包含/排除/结构/关系约束，检测遗漏、过分割、概念混淆等问题。
  5. **错误恢复与停止**：对进度停滞、格式错误等故障进行重试或局部重置，并在验证通过、进度停滞或预算耗尽时停止。
- **算法流程**（文字说明）：  
  输入图像 I 和查询 q，初始化空的工作掩码 M0 和空历史 h0。VLM智能体 A 选择初始策略 π0。循环 t=1…T：  
  (a) 基于 (I, q, M_{t-1}, h_{t-1}, π_{t-1}) 生成文本提示 p_t；  
  (b) 调用 `segment_phrase(I, p_t)` 获得候选掩码集合 C_t；  
  (c) 用 `examine_each_mask(C_t, I, q, M_{t-1})` 让VLM检查；  
  (d) 选择操作 o_t ∈ {ADD, REMOVE, REPLACE} 和子集 Ĉ_t ⊆ C_t；  
  (e) 执行 `update_working_mask(M_{t-1}, o_t, Ĉ_t)` 更新 M_t；  
  (f) 更新历史 h_t 并验证进度；  
  (g) 若验证通过、进度停滞或预算耗尽则终止，返回最终 M_t。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **PARS（PartImageNet Ad-hoc Referring Segmentation）**：新构建的基准，从 PartImageNet 中选取“body”类（因不同物体定义不同，具有即席性），将原始类别标签替换为详细的长文本描述（指定包含、排除、结构等），共2021张图像，其中937个开放即席样本、1084个常见概念样本。
  - **RefCOCOm**：标准多粒度指代分割基准，包含部分级和物体级标注，使用验证集、testA、testB（分别有12690+3811、9666+1975、4119+1810个样本）。
- **对比方法**（按级别分组）：
  - *开放词汇分割*：OVSeg、Semantic-SAM、SAM3（标注了训练集来源）。
  - *VLM作为视觉推理器*：LISA、LISA++、VisionReasoner、Seg-Zero、RESAnything。
  - *上下文智能体分割*：CoReS、SegAgent-SC、SegAgent-SAM、SAM3 Agent。
  - *封闭世界监督方法*（仅参考，不可直接比较）：VLPart（在PartImageNet上训练）。
- **评价指标**：gIoU（广义交并比）、cIoU（累积IoU）、**xIoU**（交叉概念混淆率，衡量预测掩码误包含其他标注概念区域的比例，越低越好）。

## 4. 资源与算力

- 论文未明确给出训练时长或总GPU天数（因VASA无需训练，仅在推理阶段使用VLM和SAM3）。
- 部署细节：VLM使用Qwen3-VL 32B Thinking（PARS中使用8B Instruct变体以降低成本），本地部署于 **8×A40 GPU服务器**；SAM3和智能体框架运行于 **单A40 GPU服务器**。推理时最大交互轮数为20轮，单个样本通常耗时 **不到2分钟**，但未提供精确的推理时间统计或总测试样本数下的累计算力。

## 5. 实验数量与充分性

- **实验组数**：主要对比实验在PARS（总表1）和RefCOCOm（总表2）上各有一张主表，覆盖多种方法、多个子集。另外包含以下分析：
  - 短文本 vs 长文本查询的效果对比（表3）。
  - 额外推理步数与性能改善的相关性分析（图6）。
  - 定性结果图（图7、附录C多个示例）。
  - 消融实验？文中未明确列出独立的消融实验（如移除状态管理、去除视觉审查等），但对比了不同基线之间的差距，间接体现了组件贡献。
- **充分性评价**：
  - **充分**：主表覆盖了主流方法类型，指标全面（gIoU、cIoU、xIoU），数据集包含即席和常见场景，并在标准基准上验证。
  - **客观公平**：性能数据均来自复现或引用已发表结果，VASA与基线使用相同的基础分割模型（SAM3），对比公平。
  - **局限**：缺少对VASA内部组件的消融分析（如去掉持久掩码、去掉视觉审查等），无法量化各组件贡献；在PARS上仅测试了“body”类，概念多样性有限。

## 6. 论文的主要结论与发现

- VASA在开放即席分割任务上显著优于所有基线，在PARS的即席概念上gIoU比SAM3 Agent高13.5%，cIoU高17.8%，xIoU降低25.3%。
- 在RefCOCOm上，VASA一致优于SAM3 Agent（4.8%–8.8% gIoU），尤其在部分级细粒度目标上优势明显。
- 长文本详细查询对VASA有益（gIoU提升5.9%），但对SAM3 Agent有害（gIoU下降22.1%），说明VASA能利用详细描述作为逐步构建的指导，而基于文本检索的智能体会被混淆。
- 额外推理步数与性能改善正相关，表明长视距的迭代视觉构建能有效解决复杂概念。
- 开放即席分割的瓶颈不在于分割基础模型，而在于如何利用它。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次提出“视觉 harness 工程”用于分割，通过持久工作掩码将视觉构建过程显式化、可审查、可迭代，区别于仅基于文本提示的智能体。
- **无需训练**：VASA直接使用预训练VLM和SAM3，无需微调，便于迁移和扩展。
- **设计完整性**：集成了规划、状态管理、工具调用、约束检查、视觉审查、错误恢复等组件，形成完整的工作流闭环。
- **基准构建**：PARS为开放即席分割提供了第一个带长文本详细描述的基准，并通过半自动化+人工验证流程保证质量。
- **评价指标**：引入xIoU用于检测跨概念混淆，能更精细地衡量分割是否正确排除了无关区域，弥补了IoU对细微错误的敏感度不足。

## 8. 不足与局限

- **推理延迟**：迭代长视距推理导致推理时间较长（通常约2分钟/样本），不适合实时应用。
- **依赖基础模型能力**：性能受制于底层VLM（如Qwen3-VL）和分割模型（SAM3）的能力，若SAM3无法正确分割基本部件，或VLM误判掩码覆盖，迭代也难完全恢复。
- **基准范围有限**：PARS仅基于PartImageNet的“body”类，概念覆盖不够广；未包含更多涉及关系、功能、动态场景的即席概念。
- **消融分析缺失**：文中未系统消融各组件（如去掉持久掩码、去掉视觉审查、去掉自动纠错等）的贡献，难以定量评估各模块的效果。
- **评估指标单一**：只用了IoU类指标，未采用人工评估或用户研究，无法全面反映最终构建掩码的语义正确性（尤其在排除关系上的细微错误）。

（完）
