---
title: Confidence-Aware Teacher-Student Distillation for 3D Medical Segmentation
title_zh: 用于3D医学分割的置信度感知师生蒸馏
authors: "Georgios Triantafyllou, Dimitris K. Iakovidis"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23815v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于师生蒸馏的标注高效3D医学图像分割
tldr: 针对3D医学分割依赖密集体素标注的问题，本文提出置信度感知的师生蒸馏框架，仅需每个容积单张2D切片上的少量点提示，由基础模型教师生成全容积伪标签及空间置信度，3D学生网络在置信度感知损失下训练。在心脏MRI上显著优于半监督方法，大幅降低标注成本并提升边界精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.23815v1/fig-001.webp\", \"caption\": \"Table 1: Effect of the slice selection strategy and number of positive point prompts on MedSAM2 pseudo-annotation quality, averaged across MSD Heart and LA.\", \"page\": 10, \"index\": 1, \"width\": 725, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2609.23815v1/fig-002.webp\", \"caption\": \"Fig. 2: Qualitative comparison of 3D reconstructions on representative held-out volumes from (a) MSD Heart and (b) LA. GT denotes the ground truth, and FS denotes the fully supervised 3D nnU-Net.\", \"page\": 13, \"index\": 2, \"width\": 732, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2609.23815v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of the proposed framework\", \"page\": 4, \"index\": 3, \"width\": 725, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2609.23815v1/fig-004.webp\", \"caption\": \"Table 4: Quantitative comparison with state-of-the-art semi-supervised and promptbased methods on MSD Heart and LA. A fully supervised 3D nnU-Net is included as an upper bound. Bold indicates the best performance among the non-fully supervised methods.\", \"page\": 12, \"index\": 4, \"width\": 725, \"height\": 421}]"
motivation: 医学3D分割模型依赖大量密集标注体数据，跨任务与模态扩展受限，因此需从极端稀疏标注中预测完整3D解剖结构。
method: 提出标注高效的师生框架：以基础模型为离线教师，用单张2D切片点提示生成全容积伪标注及空间置信度，再以置信度感知优化训练任务特定的3D学生网络。
result: "在3D心脏MRI数据集上，该方法较最先进半监督方法分割性能最高提升43.6%，边界精度较教师提升14.7%，并恢复至全监督上限差距的34.1%。"
conclusion: 该框架仅需每容积少量正点提示即可实现高精度3D医学分割，显著降低人工标注负担并缓解伪标签噪声传播。
---

## 摘要
医学图像分割模型通常依赖大量密集标注的体数据，这限制了其跨任务和成像模态的可扩展性。这项工作旨在解决从极端标注稀疏条件下预测完整3D解剖结构的挑战。我们提出了一种标注高效的师生框架，用于自动3D医学分割，仅需每个体数据中单个2D切片上的一组点提示作为输入。一个基础模型作为离线教师，在训练学生网络之前，利用所选切片中提供的点提示生成全容积伪标注及其对应的空间置信度分数。为缓解含噪伪标注的错误传播，我们采用置信度感知优化策略训练任务特定的3D学生网络。通过利用教师预计算的置信度分数，该策略在损失计算中显式排除伪标注中空间不确定的区域，同时强调置信度更高的区域。在3D心脏MRI数据集上评估，我们的框架优于最先进的半监督方法，分割性能提升最高达43.6%。此外，它将人工标注负担大幅降低至每个体数据仅需少量正点提示，同时相比教师将表面边界精度提高最高14.7%，并成功将与全监督上限之间的性能差距缩小了最多34.1%。

## Abstract
Medical image segmentation models typically rely on large amounts of densely annotated volumetric data, limiting their scalability across tasks and imaging modalities. This work addresses the challenge of predicting entire 3D anatomical structures from extreme annotation sparsity. An annotation-efficient student-teacher framework is proposed for automatic 3D medical segmentation that requires only a set of point prompts on a single 2D slice per volume, as input. A foundation model serves as an offline teacher, utilizing the provided point prompts from the selected slice to full-volume pseudo-annotations alongside their corresponding spatial confidence scores prior to student training. To mitigate the error propagation of noisy pseudo-annotations, a task-specific 3D student network is trained using a confidence-aware optimization strategy. By leveraging the teacher's pre-computed confidence scores, this strategy explicitly excludes statically uncertain regions of the pseudo-annotations from the loss calculation, while simultaneously emphasizing regions with higher confidence. Evaluated on 3D cardiac MRI datasets, our framework outperforms state-of-the-art semi-supervised methods, improving segmentation performance by up to 43.6%. Furthermore, it drastically reduces the manual annotation burden to just a few positive point prompts per volume, while improving surface boundary precision by up to 14.7% over the teacher and successfully recovering up to 34.1% of the performance gap toward the fully supervised upper bound.

---

## 论文详细总结（自动生成）

# 论文结构化总结：Confidence-Aware Teacher-Student Distillation for 3D Medical Segmentation

## 1. 核心问题与整体含义
- **研究动机**：3D 医学图像分割通常依赖大量密集体素级标注，标注成本高，难以跨器官、扫描仪和成像模态扩展。
- **核心挑战**：在**极端标注稀疏**条件下，仅用极少人工提示预测完整 3D 解剖结构。
- **现有方法问题**：
  - 半监督分割方法常依赖训练中动态生成的伪标签，易产生确认偏差，尤其在边界和低对比度区域。
  - 基于 SAM / MedSAM2 等基础模型的方法虽具零样本能力，但通常需要持续交互，且推理计算和内存开销大。
  - SemiSAM+ 等在线协作框架仍需在训练中反复查询大模型，训练成本高。
- **论文目标**：提出一种标注高效的师生蒸馏框架，把基础模型的零样本知识离线蒸馏到轻量 3D 学生网络中，部署时完全丢弃教师，实现无需交互提示的自动 3D 分割。

## 2. 方法论
- **核心思想**：教师离线生成全容积伪标签和置信度，学生只在可靠区域上学习，从而抑制伪标签噪声传播。
- **三阶段流程**：
  1. **初始化阶段**：输入每个 volume 的单张 2D 切片及少量正点提示，冻结的基础模型 MedSAM2 作为教师，生成全容积前景概率图、硬伪标签 \(\hat{y}_i\) 和逐体素置信度 \(c_i(v)\)。
  2. **学生训练阶段**：任务特定 3D 学生网络 \(S_\theta\) 使用置信度感知伪监督损失训练。
  3. **推理阶段**：仅部署学生网络，丢弃教师模型，无需点提示。
- **关键技术细节**：
  - 对每个体素设置置信度阈值 \(\tau=0.5\)：低于阈值的体素标为“ignore”，不参与损失；高于阈值的体素构成有效掩码 \(m_i(v)\in\{0,1\}\)。
  - 对有效体素使用连续置信度权重：
    \[
    w_i(v)=w_{\min}+(1-w_{\min})\cdot c_i(v)^\gamma
    \]
    其中 \(w_{\min}=0.7\)，\(\gamma=1.5\)。
  - 训练损失为带掩码的交叉熵和 Dice 损失：
    \[
    L_{\text{pseudo}}=\lambda_{ce}\cdot CE(w_i,S_\theta(X_i),\hat{y}_i;m_i
