---
title: "AdaVSkip: Adaptive Visual Token Skipping Across Layers For Efficient MLLMs Inference"
title_zh: AdaVSkip：面向高效 MLLMs 推理的跨层自适应视觉 token 跳过
authors: "Yuyao Sun, Tao Deng, Shuang Li, Deqing Wang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15131v1"
tags: ["query:vl"]
score: 6.0
evidence: 多模态大模型推理中视觉token自适应跳过
tldr: 针对MLLMs视觉token计算量大，现有压缩或固定跳层未考虑冗余随输入和模块变化，提出AdaVSkip，为每层配置轻量路由器自适应决定视觉token是否经过自注意力和MLP，并用两阶段训练学习路径；在三个骨干上大幅降算力并保持性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15131v1/fig-001.webp\", \"caption\": \"Figure 1: Fixed layer-wise skipping vs. adaptive module-wise visual token skipping.\", \"page\": 2, \"index\": 1, \"width\": 397, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison of layer skipping and visual token compression on LLaVA-1.5-7B. AdaVSkip-S1 denotes the Stage-I-only variant, while AdaVSkip denotes the full method.\", \"page\": 7, \"index\": 2, \"width\": 827, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-003.webp\", \"caption\": \"Table 3: Performance analysis of layer skipping and visual token pruning on Qwen3-VL-8B-Instruct.\", \"page\": 8, \"index\": 3, \"width\": 827, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-004.webp\", \"caption\": \"Table 2: Performance analysis of layer skipping and visual token compression on LLaVA-NeXT-7B.\", \"page\": 8, \"index\": 4, \"width\": 826, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-005.webp\", \"caption\": \"Figure 2: Layer-wise visual-token necessity varies across benchmarks and samples. (a) and (b) report necessity scores for self-attention and MLP, respectively, on representative benchmarks. (c) shows a t-SNE visualization of LLaVA-1.5 SFT samples, each represented by a 64-dimensional vector formed by concatenating its self-attention and MLP necessity scores across all layers.\", \"page\": 4, \"index\": 5, \"width\": 818, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-006.webp\", \"caption\": \"Figure 4: AdaVSkip routing visualization. Red boxes denote preserved visual-token computation.\", \"page\": 9, \"index\": 6, \"width\": 830, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2609.15131v1/fig-007.webp\", \"caption\": \"Figure 3: Overview of AdaVSkip’s progressive router training framework.\", \"page\": 5, \"index\": 7, \"width\": 832, \"height\": 302}]"
motivation: 现有高效MLLM推理方法多利用水平冗余压缩视觉token，或利用垂直冗余早退/固定跳层，但冗余程度与分布随输入变化且自注意力与MLP模块间不同。
method: AdaVSkip为每层设置两个轻量路由器独立决定视觉token跳过自注意力或MLP，形成输入特定计算路径，并采用冻结主干、仅训练路由器的两阶段框架：先监督学习模块必要性目标，再用强化学习结合答案正确性与跳层一致性奖励优化。
result: "在三个MLLM骨干上保持强任务性能并显著减少计算；LLaVA-NeXT-7B上FLOPs降低53.2%且平均性能不降，与视觉token压缩结合可降低91.2%并保留97.2%平均性能。"
conclusion: 表明按层、按模块自适应跳过视觉token能有效利用垂直冗余，与水平压缩互补，为高效MLLM推理提供可行方案。
---

## 摘要
多模态大语言模型（MLLMs）需要在所有 Transformer 层中处理大量视觉 token，因而需要大量计算。大多数高效 MLLM 推理方法通过压缩视觉 token 来利用水平冗余。除 token 缩减之外，近期研究通过早退或固定层跳过利用垂直冗余。然而，我们发现这种冗余的程度与分布会因输入而异，并且在自注意力与 MLP 模块之间也存在差异。受这些观察启发，我们提出 AdaVSkip，为每一层配备两个轻量级路由器，分别独立决定视觉 token 是经过还是跳过自注意力与 MLP 模块。这些决策共同定义了一条输入特定的视觉计算路径，但其离散且不可微的性质使学习有效路径具有挑战性。为应对这一挑战，我们提出一种渐进式两阶段训练框架，仅更新路由器，同时保持骨干网络冻结。阶段 I 通过监督训练建立初始路由策略，其输入特定的目标由模块级必要性分数导出。为了进一步使路由策略与任务性能对齐，阶段 II 使用强化学习，以生成答案的直接反馈来优化路由决策。它结合了答案正确性奖励与跳过一致性奖励，后者抑制过度保留视觉 token 计算。在三个 MLLM 骨干网络上，AdaVSkip 以显著更少的计算量保持强劲的任务性能。在 LLaVA-NeXT-7B 上，AdaVSkip 将 FLOPs 降低 53.2%，同时保持原始模型的平均性能。将其与视觉 token 压缩结合后，该降低幅度提升至 91.2%，同时平均保留原始性能的 97.2%。

## Abstract
Multimodal large language models (MLLMs) require substantial computation to process numerous visual tokens across all transformer layers. Most methods for efficient MLLM inference exploit horizontal redundancy by compressing visual tokens. Beyond token reduction, recent studies exploit vertical redundancy through early exit or fixed-layer skipping. However, we find that the extent and distribution of this redundancy vary across inputs and differ between self-attention and MLP modules. Motivated by these observations, we propose AdaVSkip, which equips each layer with two lightweight routers that independently determine whether visual tokens pass through by or skip the self-attention and MLP modules. These decisions collectively define an input-specific visual-computation path, but their discrete and non-differentiable nature makes learning effective paths challenging. To address this challenge, we develop a progressive two-stage training framework that updates only the routers while keeping the backbone frozen. Stage I establishes an initial routing policy through supervised training with input-specific targets derived from module-wise necessity scores. To further align the routing policy with task performance, Stage II uses reinforcement learning to optimize routing decisions with direct feedback from generated answers. It combines an answer correctness reward with a skip-consistency reward that discourages excessive retention of visual-token computation. Across three MLLM backbones, AdaVSkip maintains strong task performance with substantially less computation. On LLaVA-NeXT-7B, AdaVSkip reduces FLOPs by 53.2\% while preserving the original model's average performance. Combining it with visual token compression increases this reduction to 91.2\%, while retaining 97.2\% of the original performance on average.