---
title: "Lumen: Parameter-Efficient Alignment of Pretrained Vision and Language Encoders for Zero-Shot Computational Pathology"
title_zh: Lumen：用于零样本计算病理学的预训练视觉与语言编码器参数高效对齐
authors: "Kiarash Tajbakhsh, Abdelrahman Faqieh, Michael Jopiti, Javier Garcia-Baroja, Philipp Zens, Branislav Zagrapan, Yuri Tolkach, Martin D. Berger, Aurel Perren, Bastian Dislich, Inti Zlobec, Amjad Khan"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17868v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 参数高效对齐的病理视觉语言模型
tldr: "Lumen 探索无需微调单模态编码器，仅通过参数高效对齐构建病理视觉语言模型。它用 rank-4 适配器和投影头连接冻结的 Virchow2 与 BioMedBERT，在 QUILT-1M 上仅训练 0.40% 参数。在零样本 patch 和淋巴结转移检测中取得领先或竞争性能，证明冻结基础模型对齐可高效迁移。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.17868v1/fig-001.webp\", \"caption\": \"Figure 4 Zero-shot cross-modal retrieval on the ARCH dataset. (a) Recall@5 in both directions, image-to-text (dark shade) and text-to-image (light shade). (b) Cosine-similarity distributions for true image–caption pairs and randomly paired captions. Values at right are the AUROC for separating matched from random pairs.\", \"page\": 7, \"index\": 1, \"width\": 760, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2609.17868v1/fig-002.webp\", \"caption\": \"Table 1 Patch benchmark datasets, with the size of the evaluated partition and the canonical class names used to build every model’s prompts.\", \"page\": 10, \"index\": 2, \"width\": 735, \"height\": 707}, {\"url\": \"assets/figures/arxiv/2609.17868v1/fig-003.webp\", \"caption\": \"Figure 1 Graphical abstract. (a) Model schematic and its training. Lumen is composed of two independently pretrained frozen encoders, Virchow2 and BioMedBERT, each adapted with rank-4 low-rank adapters and a projection head into a shared 512-dimensional space. The towers are aligned by a symmetric InfoNCE contrastive objective on paired image–caption data from QUILT-1M. Only the adapters and projection heads are trained (0.40% of the model), while the backbones remain frozen. (b) Zero-shot prompt-based classification illustrated on a patch example from PatchCamelyon. A query patch is embedded by the vision tower. Each class name is expanded across a bank of 22 templates, embedded by the text tower, L2-normalized, averaged into a single class vector and L2-normalized again. The predicted label is the class whose vector is nearest the image embedding by cosine similarity in the shared space. (c) Patch-classification benchmark. Zero-shot performance of Lumen across nine public patch-level datasets against six comparison vision–language models. Bars report chance-corrected balanced accuracy (bcc). For each fine-grained dataset, Lumen’s zero-shot score (dark shade) is shown alongside a supervised linear-probe upper bound computed on the same frozen features (light shade). Asterisks mark datasets on which Lumen is the best-performing VLM. (d) Cross-modal retrieval. Top, schematic of retrieval in the shared space: a query image embedding is ranked against candidate caption embeddings, with the matched caption recovered within the top five (highlighted). Middle, bidirectional retrieval, reported as Recall@5 for the image→text and text→image directions. Bottom, distribution of cosine similarities for matched versus randomly paired image–caption pairs. Their separation corresponds to a matched-versus-random AUROC of 0.957. (e) Slide-level lymph-node metastasis detection. Left, number of whole-slide images per organ for the internal and external cohorts used for evaluation. Right, whole-slide images are tiled at 2 µm and passed to Lumen along with the class captions (positive and negative for lymph node metastasis). The cosine similarity between each tile embedding and the two class vectors is computed and converted at the model’s learned logit scale to a two-class softmax probability of metastasis. The per-slide score is the maximum tile probability, and a slide is labelled positive when this score exceeds a single threshold τ , fixed by maximizing Youden’s index on 215 internal calibration cases and then locked for every cohort (internal AUROC 0.964, external 0.955).\", \"page\": 3, \"index\": 3, \"width\": 1054, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2609.17868v1/fig-004.webp\", \"caption\": \"Figure 2 Zero-shot patch classification across nine public benchmarks. (a) Chance-corrected balanced accuracy on each of the six datasets whose classes are distinct tissues or entities, with a 95% bootstrap interval on every bar. (b) The three fine-grained datasets whose classes are instead adjacent grades or subtypes of one entity, on the same scale as (a). Notably, every model falls close to chance on these. Hatched bars fall below chance, meaning the model selects the wrong class more often than a label-blind predictor. (c) The mean over all nine datasets, one bar per model. The whisker is the range of that mean across the nine leave-one-dataset-out subsets, so it shows how far the ranking depends on any single dataset. All models met the same prompt protocol, one canonical class name across 22 templates, giving 22 prompts per class for every dataset.\", \"page\": 4, \"index\": 4, \"width\": 759, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2609.17868v1/fig-005.webp\", \"caption\": \"Figure 3 Prompt-based lymph-node metastasis detection under one locked decision rule. (a) Discrimination by AUROC. (b) Balanced accuracy at the locked operating point. In both panels, external performance is in darker shades and internal held-out performance is in lighter shades. Bars rise from the chance level (0.5), marked by the dashed line. (c) Lumen’s per-window probability of metastasis on one external slide from the UKK lung cohort, with MetAssist 2.0’s tumour outline in black. The box marks the region enlarged in (d) and (e), shown as H&E alone in (d) and with prediction overlay in (e).\", \"page\": 5, \"index\": 5, \"width\": 787, \"height\": 633}]"
motivation: 病理视觉语言模型通常依赖大规模预训练或微调，作者试图验证能否通过参数高效对齐冻结的单模态基础模型来构建，而不改变其预训练表征。
method: "Lumen 在 QUILT-1M 上以 rank-4 适配器和投影头对齐冻结的 Virchow2 与 BioMedBERT，仅训练总参数量的 0.40%。"
result: 在九个零样本 patch 基准上平均机会校正平衡准确率 0.546（最强基线 0.461）；淋巴结转移检测 AUROC 内部 0.964、外部 0.955，校准阈值平衡准确率 0.909/0.915；跨模态检索排第三，逊于 CONCH 和 PathGen-L/14。
conclusion: 对齐冻结单模态基础模型可在 patch 与 slide 级别实现强且可迁移性能，仅需少量参数，全量微调未带来一致增益（检索除外）。
---

## 摘要
病理学视觉-语言模型通常通过在配对的图像-文本数据上预训练或微调大型编码器来构建。我们提出，是否可以通过对冻结的单模态基础模型进行参数高效对齐来构建病理学视觉-语言模型，而不改动其预训练表示。在此，我们提出 Lumen，它使用秩为4的适配器和投影头对齐冻结的 Virchow2 与 BioMedBERT 主干，在公开 QUILT-1M 语料上仅训练总参数的 0.40%。在九个公开零样本图像块基准上，Lumen 取得了最高的平均机会校正平衡准确率，0.546 对最强基线的 0.461（配对差异 0.086，95% CI 0.042–0.136）。在淋巴结转移检测中，Lumen 在 4,214 张留出的内部切片上达到 0.964 的 AUROC（95% CI 0.956–0.971），并在来自九个外部队列和六个器官的 2,368 张切片上达到 0.955（95% CI 0.942–0.966）。在内部校准的阈值下，它优于所有视觉-语言基线，内部平衡准确率为 0.909（95% CI 0.896–0.923），外部为 0.915（95% CI 0.902–0.929）。Lumen 在各项评估中表现具有竞争力，但跨模态检索除外，其在该任务中排名第三，落后于 CONCH 和 PathGen-L/14。完全微调两个编码器并未给 Lumen 带来相对于低秩适应的一致收益，尽管它改善了检索性能。因此，对齐冻结的单模态基础模型可在图像块和切片层面产生强大且可迁移的性能，同时仅训练一小部分参数。

## Abstract
Pathology vision-language models are commonly built by pretraining or fine-tuning large encoders on paired image-caption data. We asked whether a pathology vision-language model can instead be assembled by parameter-efficient alignment of frozen unimodal foundation models, leaving their pretrained representations untouched. Here we present Lumen, which aligns frozen Virchow2 and BioMedBERT backbones using rank-4 adapters and projection heads, training only 0.40% of the total parameters on the public QUILT-1M corpus. Across nine public zero-shot patch benchmarks, Lumen achieved the highest mean chance-corrected balanced accuracy, 0.546 versus 0.461 for the strongest baseline (paired difference 0.086, 95% CI 0.042-0.136). On lymph-node metastasis detection, Lumen reached an AUROC of 0.964 (95% CI 0.956-0.971) on 4,214 held-out internal slides and 0.955 (95% CI 0.942-0.966) on 2,368 slides across nine external cohorts and six organs. At the internally calibrated threshold, it outperformed all vision-language baselines, with a balanced accuracy of 0.909 (95% CI 0.896-0.923) internally and 0.915 (95% CI 0.902-0.929) externally. Lumen performed competitively across the evaluations, with the exception of cross-modal retrieval, where it ranked third behind CONCH and PathGen-L/14. Fully fine-tuning both encoders gave Lumen no consistent benefit over low-rank adaptation, although it improved retrieval. Aligning frozen unimodal foundation models therefore yields strong and transferable performance at patch and slide level while training only a small fraction of the parameters.

---

## 论文详细总结（自动生成）

# Lumen 论文总结

> 说明：以下总结基于所提供的论文元数据、摘要及图表说明，未额外引入原文之外的信息。

## 1. 核心问题与整体含义

- **研究动机**：病理学视觉-语言模型（VLM）通常依赖在配对图像-文本数据上大规模预训练或微调大型编码器，计算与参数成本高。
- **核心问题**：能否不改变预训练单模态基础模型的表示，仅通过参数高效对齐，组装出具有竞争力的病理学 VLM？
- **整体含义**：作者提出 **Lumen**，验证“冻结单模态基础模型 + 参数高效对齐”的可行性，目标是在图像块（patch）和切片（slide）级别获得强且可迁移的零样本性能，同时只训练极少量参数。
- **背景定位**：病理 VLM 常用于零样本分类、跨模态检索和切片级诊断；Lumen 试图在保留 Virchow2 与 BioMedBERT 预训练表征的前提下完成跨模态对齐。

## 2. 方法论：核心思想与关键技术细节

- **核心思想**：
  - 使用两个独立预训练且**冻结**的单模态编码器：视觉塔 **Virchow2**，文本塔 **BioMedBERT**。
  - 不微调主干，只训练轻量适配器与投影头，将两个模态映射到共享嵌入空间。
  - 在公开 **QUILT-1M** 图像-描述配对语料上做对比学习对齐。

- **关键技术细节**：
  - 每个编码器接入 **rank-4 低秩适配器**和投影头。
  - 共享空间维度为 **512 维**。
  - 仅训练适配器和投影头，占总参数量的 **0.40%**。
  - 使用**对称 InfoNCE 对比目标**对齐图像塔与文本塔。

- **零样本 patch 分类流程**：
  - 查询 patch 经视觉塔编码为图像嵌入。
  - 每个类别名通过 **22 个模板**扩展为提示。
  - 所有提示经文本塔编码、L2 归一化，再平均为单个类向量，并再次 L2 归一化。
  - 在共享空间中，以余弦相似度最近的类向量作为预测标签。

- **切片级淋巴结转移检测流程**：
  - 全切片图像按 **2 µm** 切块。
  - 每个 tile 与正/负类描述计算余弦相似度。
  - 通过模型学习到的 logit scale 转换为二分类 softmax 概率。
  - 每张切片的得分为最大 tile 概率；超过阈值 τ 即判为阳性。
  - 阈值 τ 在 **215 个内部校准病例**上通过最大化 Youden’s index 确定，随后锁定用于所有队列。

- **跨模态检索流程**：
  - 在共享空间中，用查询图像嵌入对候选描述嵌入排序，或反向排序。
  - 报告双向 **Recall@5**，并计算匹配图像-描述对与随机配对的可分性 AUROC。

## 3. 实验设计：数据集、场景与对比方法

- **Patch 级零样本分类**：
  - 使用 **9 个公开 patch 级数据集**。
  - 指标为 **机会校正平衡准确率（chance-corrected balanced accuracy, bcc）**。
  - 其中 6 个数据集的类别为不同组织或实体；3 个为细粒度数据集，类别为同一实体的相邻分级或亚型。
  - 对比 **6 个视觉-语言模型基线**。
  - 同时给出在同一冻结特征上的**监督线性探针上界**。
  - 所有模型使用相同提示协议：每个数据集一个规范类名，扩展到 22 个模板，即每类 22 个提示。

- **切片级淋巴结转移检测**：
  - 内部队列：**4,214 张留出内部切片**。
  - 外部队列：**2,368 张切片**，来自 **9 个外部队列、6 个器官**。
  - 指标包括 AUROC 和锁定阈值下的平衡准确率。
  - 与所有视觉-语言基线比较。

- **跨模态检索**：
  - 在 **ARCH 数据集**上评估。
  - 报告图像到文本、文本到图像双向 **Recall@5**。
  - 比较模型包括 **CONCH**、**PathGen-L/14** 等。

- **全量微调对照**：
  - 将 Lumen 与完全微调两个编码器的设置进行比较。
  - 观察低秩适配与全量微调在不同任务上的收益差异。

## 4. 资源与算力

- 所提供的论文文本中**未明确说明**使用的 GPU 型号、数量、训练时长、显存或能耗等算力信息。
- 可确认的资源相关信息仅有：
  - 训练数据为公开 **QUILT-1M** 语料。
  - 仅训练总参数量的 **0.40%**。
  - 主干编码器 Virchow2 与 BioMedBERT 保持冻结。
- 因此，无法根据现有内容判断其实际计算成本或训练效率优势的具体幅度。

## 5. 实验数量与充分性

- **大致实验组数**：
  - 9 个 patch 级零样本基准，覆盖粗粒度与细粒度分类。
  - 与 6 个 VLM 基线比较，并加入线性探针上界。
  - 1 个切片级淋巴结转移检测任务，包含内部队列与 9 个外部队列、6 个器官。
  - 1 个跨模态检索任务，在 ARCH 数据集上做双向 Recall@5。
  - 1 组全量微调 vs 低秩适配的对照。
  - 提示协议固定为每类 22 个模板，并统一用于所有模型。

- **充分性与客观性**：
  - 覆盖 patch 分类、切片诊断、跨模态检索三类任务，且包含多外部队列验证，实验面较广。
  - 使用 bootstrap 95% CI、配对差异、留一数据集范围等统计方式，结果报告较严谨。
  - 所有模型采用同一提示协议，有助于公平比较。
  - 切片检测阈值在内部校准后锁定，再用于外部队列，减少外部调参带来的偏置。
  - 但细粒度 patch 数据集上所有模型均接近随机，说明该维度区分能力有限。
  - 未提及多随机种子、超参数敏感性、adapter rank、投影维度、模板数量等消融实验，实验充分性仍有一定缺口。

## 6. 主要结论与发现

- **Patch 级零样本分类**：
  - Lumen 在 9 个公开 patch 基准上取得最高平均机会校正平衡准确率：**0.546**，最强基线为 **0.461**。
  - 配对差异为 **0.086**，95% CI 为 **0.042–0.136**。

- **淋巴结转移检测**：
  - 内部留出切片 AUROC 为 **0.964**（95% CI 0.956–0.971）。
  - 外部九队列、六器官 AUROC 为 **0.955**（95% CI 0.942–0.966）。
  - 在内部校准阈值下，平衡准确率内部为 **0.909**（95% CI 0.896–0.923），外部为 **0.915**（95% CI 0.902–0.929）。
  - 优于所有视觉-语言基线。

- **跨模态检索**：
  - Lumen 排名第三，落后于 **CONCH** 和 **PathGen-L/14**。
  - 匹配与随机图像-描述对分离 AUROC 为 **0.957**。

- **全量微调对照**：
  - 完全微调两个编码器没有给 Lumen 带来相对于低秩适配的一致收益。
  - 但全量微调改善了检索性能。

- **总体结论**：
  - 对齐冻结的单模态基础模型可以在 patch 和 slide 级别产生强且可迁移的性能。
  - 仅需训练一小部分参数，即可构建有竞争力的病理 VLM。

## 7. 优点

- **参数高效**：仅训练 0.40% 参数，冻结两个预训练主干，显著降低可训练参数规模。
- **保留预训练表征**：不破坏 Virchow2 与 BioMedBERT 的单模态知识，通过适配器完成跨模态对齐。
- **任务覆盖较广**：同时评估零样本 patch 分类、切片级淋巴结转移检测和跨模态检索。
- **外部验证较强**：淋巴结转移检测覆盖 9 个外部队列、6 个器官，外部 AUROC 与内部接近。
- **统计报告严谨**：使用 95% CI、配对差异、bootstrap 区间和留一数据集范围。
- **提示协议统一**：所有模型使用相同的 22 模板提示协议，减少提示工程差异带来的不公平。
- **阈值锁定设计**：切片检测阈值在内部校准后固定，外部队列直接应用，增强可迁移性评估的可信度。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、
