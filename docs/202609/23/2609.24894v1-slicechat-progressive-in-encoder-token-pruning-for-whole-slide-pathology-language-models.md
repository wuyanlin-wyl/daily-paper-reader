---
title: "SLICEChat: Progressive In-Encoder Token Pruning for Whole-Slide Pathology Language Models"
title_zh: SLICEChat：面向全切片病理语言模型的编码器内渐进式令牌剪枝
authors: "Ali Kerem Bozkurt, Baris Cem Bakay, Ibrahim Kulac, Cigdem Gunduz-Demir, Erkut Erdem, Aykut Erdem"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24894v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 面向病理分析的多模态大模型
tldr: 针对全切片病理图像（WSI）千亿像素导致幻灯片级多模态大模型可扩展性差、多模态注意力昂贵的问题，提出 SLICEChat，在混合 Mamba-Transformer 切片编码器中逐步进行语言监督、区域感知的 token 剪枝，生成紧凑切片表示。在 SlideBench VQA 等基准上取得更优准确率与高效内存/延迟，实现高效多模态推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24894v1/fig-001.webp\", \"caption\": \"Figure 4. Overview of the slide encoder training stages. Blue arrows denote Stage 1 pretraining, while red arrows indicate Stage 2. In Stage 1, randomly masked tiles are processed by the hybrid encoder without pruning, and a lightweight MAE decoder reconstructs the masked tokens. In Stage 2, masking and the decoder are removed and the pruner is trained using CLIP-based text-slide alignment objectives.\", \"page\": 6, \"index\": 1, \"width\": 1031, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2609.24894v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of SLICEChat. Patch embeddings of the WSI are created by encoding the tiles with the CONCH v1.5 patch encoder. These patch embeddings are then transformed and pruned in the hybrid state-space slide encoder. The pruner uses learned queries to score and progressively select informative slide tokens. We use a LLaVA-style multimodal fusion to pass the pruned slide tokens to the MLLM.\", \"page\": 3, \"index\": 2, \"width\": 987, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2609.24894v1/fig-003.webp\", \"caption\": \"Table B.1. Reported SlideBench accuracies of slide-level MLLMs whose model weights are not publicly available. Results are taken directly from the original publications and may not be strictly comparable due to differences in evaluation protocols.\", \"page\": 13, \"index\": 3, \"width\": 478, \"height\": 355}]"
motivation: 现有方法通常仅在切片编码后压缩数千 patch token，导致多模态注意力计算昂贵，难以扩展到千亿像素 WSI。
method: SLICEChat 在混合 Mamba-Transformer 编码器内分阶段进行语言监督、区域感知的 token 剪枝，按受控保留率移除低效用区域，并在多模态融合前生成紧凑切片表示。
result: "在 SlideBench VQA 上 TCGA 准确率达 79.84%、BCNB 达 59.09%，优于先前切片级病理 MLLM，并在 WSI-Bench 总体指标最高，同时内存使用和推理延迟具有竞争力。"
conclusion: 结果表明该方法能在千亿像素 WSI 上实现准确且计算高效的多模态推理。
---

## 摘要
全切片病理图像（WSIs）包含十亿像素级的视觉内容，这给切片级多模态大语言模型（MLLMs）带来了重大的可扩展性挑战。现有方法处理数千个图像块 token，并且通常仅在切片编码后进行压缩，导致多模态注意力计算代价高昂。我们提出 SLICEChat，一种切片级 MLLM，其在混合 Mamba--Transformer 切片编码器中集成渐进式 token 剪枝。Mamba 层实现高效的长程传播，而 Transformer 层在序列逐步缩短时保持全局交互。在阶段之间，语言监督、区域感知的剪枝在受控保留率调度下移除空间连贯的低效用区域，从而在多模态融合前生成紧凑的切片表示。在 SlideBench VQA 上，SLICEChat 在 TCGA 上达到 79.84% 的准确率，在 BCNB 队列上达到 59.09%，优于先前的切片级病理 MLLM，并在 WSI-Bench 上取得最高的总体指标。在评估的模型中，它还提供有竞争力的内存占用和推理延迟。这些结果证明了在十亿像素级 WSI 上进行准确且计算高效的多模态推理。

## Abstract
Whole-slide pathology images (WSIs) contain gigapixel-scale visual content, creating a major scalability challenge for slide-level multimodal large language models (MLLMs). Existing approaches process thousands of patch tokens and typically apply compression only after slide encoding, leaving multimodal attention computationally expensive. We introduce SLICEChat, a slide-level MLLM that integrates progressive token pruning within a hybrid Mamba--Transformer slide encoder. Mamba layers enable efficient long-range propagation, while Transformer layers preserve global interactions as the sequence is progressively shortened. Between stages, language-supervised, region-aware pruning removes spatially coherent low-utility regions under a controlled keep-rate schedule, producing compact slide representations before multimodal fusion. On SlideBench VQA, SLICEChat achieves 79.84% accuracy on TCGA and 59.09% on BCNB cohorts, outperforming prior slide-level pathology MLLMs, and achieves the highest overall WSI-Bench metrics. It also provides competitive memory usage and the inference latency among the evaluated models. These results demonstrate accurate and computationally efficient multimodal reasoning over gigapixel WSIs.

---

## 论文详细总结（自动生成）

# SLICEChat 论文中文总结

> 说明：以下总结仅依据提供的 PDF 提取文本、元数据与图注信息；正文中未出现的训练细节、消融实验和算力配置无法确认。

## 1. 核心问题与整体含义

- **研究背景**：全切片病理图像（WSI）具有十亿像素级视觉内容，通常被切分为数千个图像块 token。切片级多模态大语言模型（MLLM）需要处理这些超长视觉 token 序列，导致多模态注意力计算代价极高，难以扩展到真实 WSI 场景。
- **核心问题**：现有方法大多在切片编码完成后再压缩 token，压缩位置偏后，前端编码和后续多模态融合仍然昂贵。
- **整体含义**：论文提出 SLICEChat，将 token 剪枝前移到切片编码器内部，通过渐进式压缩生成紧凑切片表示，从而在保持病理问答准确率的同时降低内存与推理延迟，目标是在十亿像素级 WSI 上实现准确且计算高效的多模态推理。

## 2. 方法论

### 2.1 核心思想

- 在混合 **Mamba–Transformer** 切片编码器中集成渐进式 token 剪枝。
- **Mamba 层**负责高效长程传播，适应数千 patch token 的长序列。
- **Transformer 层**在序列逐步缩短时维持全局交互能力。
- 在编码阶段之间执行 **语言监督、区域感知** 的剪枝，按受控保留率调度移除空间连贯的低效用区域，从而在多模态融合前得到紧凑的切片表示。

### 2.2 关键技术流程

- **Patch 编码**：WSI 的图像块由 **CONCH v1.5** patch encoder 编码为 patch embeddings。
- **混合切片编码器**：patch embeddings 输入混合状态空间切片编码器，在其中进行变换与剪枝。
- **渐进式剪枝器**：剪枝器使用可学习查询（learned queries）对 token 进行打分，并逐步选择信息量高的 slide tokens；低效用且空间连贯的区域被移除。
- **多模态融合**：剪枝后的紧凑 slide tokens 通过 **LLaVA 风格**的多模态融合送入 MLLM。
- **两阶段训练**：
  - **Stage 1**：随机掩码 tile，经混合编码器处理，不进行剪枝；轻量 MAE 解码器重建被掩码 token。
  - **Stage 2**：移除掩码和解码器，使用基于 CLIP 的文本–切片对齐目标训练剪枝器。
- 提供的材料未给出具体打分公式、损失函数表达式或保留率调度细节。

## 3. 实验设计

- **主要基准**：
  - **SlideBench VQA**：在 TCGA 队列上达到 **79.84%** 准确率，在 BCNB 队列上达到 **59.09%**。
  - **WSI-Bench**：取得最高总体指标。
- **效率评估**：比较内存使用和推理延迟，报告具有竞争力的表现。
- **对比对象**：与先前的切片级病理 MLLM 进行比较，声称优于这些方法。
- **补充对比**：附录 Table B.1 报告了模型权重未公开的切片级 MLLM 在 SlideBench 上的准确率，结果直接取自原论文，并注明由于评估协议差异，可能不严格可比。
- **场景**：病理全切片图像上的幻灯片级视觉问答与多模态推理。

## 4. 资源与算力

- 提供的摘要、元数据和图注中 **未提及 GPU 型号、GPU 数量、训练时长、参数量、能耗或具体训练资源**。
- 因此无法从现有材料总结该工作的算力开销，也无法评估其训练成本与复现门槛。

## 5. 实验数量与充分性

- 从现有材料看，实验至少覆盖：
  - SlideBench VQA 的 TCGA 与 BCNB 两个队列；
  - WSI-Bench 总体指标；
  - 内存占用与推理延迟；
  - 附录中未公开权重模型的 SlideBench 结果对照。
- **充分性判断**：材料中未展示消融实验、剪枝保留率分析、Mamba/Transformer 组件贡献分析、不同 patch encoder 或剪枝策略对比，因此无法判断方法各模块的必要性。
- **公平性风险**：
  - 与未公开权重模型的比较被作者明确标注“可能不严格可比”。
  - 与公开基线方法的训练数据、参数规模、评估协议是否完全一致，提供材料未说明。
  - 因此，现有信息不足以全面评估实验的客观性与公平性。

## 6. 主要结论与发现

- SLICEChat 在 SlideBench VQA 上超过先前切片级病理 MLLM：TCGA 79.84%，BCNB 59.09%。
- 在 WSI-Bench 上取得最高总体指标。
- 在评估模型中提供有竞争力的内存占用和推理延迟。
- 结果表明：在编码器内进行语言监督、区域感知的渐进式 token 剪枝，可以在十亿像素级 WSI 上实现准确且计算高效的多模态推理。

## 7. 优点

- **剪枝位置前移**：不同于编码后压缩，SLICEChat 在切片编码器内部剪枝，可减少后续多模态注意力的计算负担。
- **混合架构设计合理**：Mamba 适合长程高效传播，Transformer 保留全局交互，兼顾效率与表达能力。
- **剪枝具有区域感知和语言监督**：按空间连贯区域移除低效用 token，比独立 token 剪枝更符合病理图像结构。
- **两阶段训练策略清晰**：Stage 1 用 MAE 重建预训练，Stage 2 用 CLIP 文本–切片对齐训练剪枝器，逐步引入剪枝。
- **多模态融合采用成熟范式**：LLaVA 风格融合便于接入现有 MLLM 生态。
- **效率指标与准确率同时报告**：不仅关注 VQA 准确率，也评估内存和延迟，贴近真实部署需求。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、训练时长等，难以评估训练成本和可复现性。
- **实验覆盖有限**：主要基于 SlideBench VQA、WSI-Bench 和 TCGA/BCNB 队列，缺少更多中心、更多病种、更多临床任务的验证。
- **消融不足**：提供材料中未见对剪枝策略、保留率、Mamba/Transformer 比例、语言监督目标等关键设计的系统消融。
- **比较公平性存疑**：部分对比模型权重未公开，评估协议可能不同，作者也提示不可严格比较。
- **剪枝风险未充分讨论**：低效用区域可能包含稀有但诊断关键的病灶，剪枝是否会造成临床信息损失尚不明确。
- **依赖上游模型**：使用 CONCH v1.5、CLIP 等组件，可能继承其偏差或领域限制。
- **临床应用限制**：未讨论可解释性、安全性、隐私、监管和临床部署流程。
- **预印本性质**：作为 arXiv 预印本，尚需同行评审验证。

（完）
