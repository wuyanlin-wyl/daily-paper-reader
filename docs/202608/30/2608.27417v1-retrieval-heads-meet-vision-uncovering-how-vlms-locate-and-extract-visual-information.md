---
title: "Retrieval Heads Meet Vision: Uncovering How VLMs Locate and Extract Visual Information"
title_zh: 检索头遇见视觉：揭示VLM如何定位和提取视觉信息
authors: "Chanho Park, Daehyeon Choi, Jihyun Lee, Minhyuk Sung"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27417v1"
tags: ["query:vl"]
score: 9.0
evidence: 揭示VLM中注意头实现视觉定位与检索的内在机制
tldr: "视觉语言模型（VLM）能根据文本定位图像区域并提取视觉信息，但其内部机制尚不明确。本研究受大语言模型中检索头的启发，提出视觉检索头（VRHs）概念，即一小部分（约1.7-2.6%）因果负责将文本描述与图像区域关联的注意力头。通过统一评分方法，发现从输出预测标记对真实参照区域求和最可靠。在11个VLM和5个基准上，仅掩蔽顶部20个VRH即可使定位精度下降高达80个百分点，而随机头影响甚微。VRH不仅复制了文本检索头的因果-稀疏-通用三元组，还展现出跨任务泛化、功能特性和架构共享等新特性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.27417v1/fig-001.webp\", \"caption\": \"Figure 4: Universality of VRHs across models and benchmarks. We report visual grounding accuracy after masking the top-k VRHs across four VLMs and three benchmark families: RefCOCO/+/g [5, 10], RefSpatialBench [11], and Toloka [12]. The x-axis denotes the number of masked heads (k). Masking only a few VRHs consistently causes large accuracy drops, while masking random heads has little effect. This broad causal effect shows that VRHs are not architecture- or dataset-specific, but instead capture a shared attention-head mechanism for visual grounding.\", \"page\": 7, \"index\": 1, \"width\": 799, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2608.27417v1/fig-002.webp\", \"caption\": \"Figure 6: VRHs are universal across VLMs. Visual grounding accuracy on RefCOCO [5] for all eleven evaluated VLMs, before masking, after masking 20 random heads, and after masking the top-20 VRHs. Masking the VRHs causes a large accuracy drop in every model, whereas masking the same number of random heads has little effect. Exact values, evaluation splits and per-model protocols are given in Sec. J.\", \"page\": 7, \"index\": 2, \"width\": 825, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2608.27417v1/fig-003.webp\", \"caption\": \"Table 3: Generalization across visual reference tasks. Masking the top-20 VRHs detected on RefCOCO [5] consistently degrades performance on seven VQA benchmarks [13–19] spanning attributes, spatial reasoning, counting, vision-centric understanding, and visual math, while randomhead masking has little effect.\", \"page\": 8, \"index\": 3, \"width\": 825, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2608.27417v1/fig-004.webp\", \"caption\": \"Figure 7: VRH detection consistency across same-backbone VLMs. Each block is a pair of VLMs that share a language backbone but differ in vision encoder, projector and instruction tuning: Qwen2.5-VL-7B [6] with InternVL3-8B [8], which share a Qwen2.5-7B backbone; Qwen3-VL8B [7] with InternVL3.5-8B [9], which share a Qwen3-8B backbone; and Magma-8B [27] with LLaVA-NeXT-Llama3-8B [28], which share a Llama-3 backbone. Top row: Layer-head score maps, where each cell denotes the VRH score Θ̄l,h at layer l and head h; high-scoring heads appear at similar layer-head locations across same-backbone VLMs. Bottom row: Cross-model masking results, where the top-k VRHs identified in one model are masked at the same layer-head indices in the other model. Own VRHs are the heads detected in the panel’s own model and transferred VRHs those detected in its partner. The x-axis denotes the number of masked heads (k).\", \"page\": 9, \"index\": 4, \"width\": 826, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2608.27417v1/fig-005.webp\", \"caption\": \"Figure 2: Causal validation of head-scoring variants on RefCOCO [5] across four VLMs [6–9]. We mask the top-20 heads detected by each variant and report visual grounding accuracy; lower accuracy indicates that the detected heads are more causally important. Ours denotes our default variant (Qout s ,Φsum,Ωmean). The Qs, Φ, and Ω ablations each change one component of this default design at a time, while Random masks 20 randomly selected heads. The dashed line shows the unmasked model (k = 0), and ∆ denotes the accuracy gap relative to Ours.\", \"page\": 5, \"index\": 5, \"width\": 829, \"height\": 229}]"
motivation: 理解VLM如何内部实现从文本到图像区域的定位与视觉信息提取机制，并探索是否与LLM中的检索头类似存在专门的视觉检索结构。
method: 将现有头部评分方法统一到由查询标记、键聚合和跨样本聚合组成的设计空间中，系统地识别出视觉检索头（VRHs），并验证其因果性。
result: 仅掩蔽前20个VRH即可使定位精度下降高达80个百分点，而随机头影响甚微；VRH在属性、空间、计数和视觉数学任务上保持因果性，且跨共享LLM骨干的VLM可迁移。
conclusion: VLM中存在与文本检索头类似的视觉检索头机制，且具有跨任务泛化、功能特性和架构共享性，为理解VLM的视觉-语言对齐提供了新视角。
---

## 摘要
视觉-语言模型（VLM）能够定位文本提示所指的图像区域，并将相应的视觉证据路由到输出，然而这种行为背后的内部机制尚不清楚。受大型语言模型中检索头的启发，我们探究VLM是否包含类似的视觉检索机制。我们通过引入视觉检索头（VRHs）给出肯定答案，这是一小部分注意力头（约占1.7%-2.6%），在因果上负责将文本描述接地到图像区域。为了找到它们，我们在查询令牌、键聚合和跨样本聚合的统一设计空间下重新构造现有的头部评分方法。然后我们证明，使用对真实参照区域求和的输出预测令牌的注意力评分，最能可靠地识别因果头。在十一个VLM和五个指代表达基准上，仅遮蔽前20个VRH可将定位准确率降低多达80个百分点，而遮蔽相同数量的随机头则几乎没有影响。除了复制为文本检索头建立的因果-稀疏-普遍三元组外，VRH还表现出一些先前未报告的特性：它们跨视觉参考任务泛化，尽管是通过边界框预测发现的，但在属性、空间、计数和视觉数学基准上仍然保持因果性；它们在功能上是特定的，在破坏定位的同时保留输出格式；它们在架构上是共享的，在共享LLM骨干但视觉编码器、投影仪和指令调优不同的VLM之间因果转移。

## Abstract
Vision-language models (VLMs) can locate an image region referred to by a text prompt and route the corresponding visual evidence to the output, yet the internal mechanism behind this behavior is not understood. Inspired by retrieval heads in large language models, we ask whether VLMs contain an analogous mechanism for visual retrieval. We answer affirmatively by introducing Visual Retrieval Heads (VRHs), a small subset of attention heads (about 1.7-2.6%) that are causally responsible for grounding text descriptions to image regions. To find them, we recast existing head-scoring methods under a unified design space over query tokens, key aggregation, and cross-sample aggregation. We then show that scoring attention from output prediction tokens with a sum over the ground-truth referent region most reliably identifies causal heads. Across eleven VLMs and five referring-expression benchmarks, masking only the top 20 VRHs reduces grounding accuracy by up to 80 percentage points, while masking the same number of random heads has little effect. Beyond replicating the causal-sparse-universal triad established for text retrieval heads, VRHs exhibit several properties not previously reported: they generalize across visual reference tasks, remaining causal on attribute, spatial, counting, and visual-math benchmarks despite being discovered through bounding-box prediction; they are functionally specific, preserving output format while corrupting localization; and they are architecturally shared, transferring causally across VLMs that share an LLM backbone but differ in vision encoder, projector, and instruction tuning.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言模型（VLM）具备将文本描述与图像区域关联并提取相应视觉信息的能力，但其内部机制长期处于“黑箱”状态。本研究试图回答：**VLM 是否包含与大型语言模型（LLM）中“检索头”（Retrieval Heads）相类似的、专门负责视觉信息定位与提取的注意力头结构？**

- **研究背景**：先前研究已在纯文本 LLM 中发现“检索头”——即一小部分因果重要性极高的注意力头，负责从上下文中检索事实性信息。本文作者受此启发，将这一视角延伸到多模态领域，系统性地探究 VLM 是否也存在类似的、专门负责“视觉检索”的结构化机制。

- **整体含义**：若视觉检索头存在且可识别，将为理解 VLM 的视觉-语言对齐机制提供全新视角，也为模型的可解释性研究、安全审计和精细化的模型编辑提供切入点。

## 2. 论文提出的方法论

- **核心概念**：论文提出 **视觉检索头（Visual Retrieval Heads, VRHs）** 这一概念，指代那些在因果层面负责将文本描述“接地”（ground）到图像区域的注意力头。这类头占总注意力头数量的 **1.7%–2.6%**，属于极度稀疏的子集。

- **关键技术细节**：
  - **统一设计空间**：作者将现有的各类头部评分方法拆解为三个正交的设计维度，并将其统一在同一个框架下：
    - **查询令牌选择（Query Token）**：选择用哪一层/哪个位置的注意力查询向量作为评分基准，例如输出预测令牌（output prediction token）或中间层令牌。
    - **键聚合方式（Key Aggregation）**：在计算注意力得分时，如何对图像区域的键（key）向量进行聚合，例如求和（sum）、平均（mean）或取最大值。
    - **跨样本聚合方式（Cross-sample Aggregation）**：在多个样本之间如何汇总单个注意力头的得分，例如取均值（mean）或众数等。
  - **最优识别方案**：通过系统性的因果验证，作者发现**使用“输出预测令牌的注意力”对“真实参照区域（ground-truth referent region）的键求和”**，即以 \(\mathcal{Q}_{\text{out}}^{s}, \Phi_{\text{sum}}, \Omega_{\text{mean}}\) 为组合的评分方式，最能稳定地识别因果头部。
  - **因果验证算法**：识别出候选头部后，采用“掩蔽”（masking）操作——即将指定注意力头的输出置零——来检测模型性能的变化。若掩蔽某个头后定位精度显著下降，则证明该头具有因果重要性。

- **方法本质**：整个方法流程可概括为：**统一评分 → 筛选 top-k 头部 → 因果掩蔽验证**，以此完成对视觉检索头的定位与功能确认。

## 3. 实验设计

- **数据集 / Benchmark**：
  - **指代表达理解（Referring Expression）基准**：RefCOCO、RefCOCO+、RefCOCOg、RefSpatialBench、Toloka，共 5 个标准基准。
  - **视觉问答（VQA）泛化基准**：涵盖属性理解、空间推理、计数、视觉中心理解和视觉数学等 7 个 VQA benchmark，用于验证 VRH 的跨任务泛化性。
  - **模型覆盖**：共 11 个 VLM，包括具备不同视觉编码器、投影器和指令微调策略的主流模型（如 Qwen2.5-VL、Qwen3-VL、InternVL3、InternVL3.5、LLaVA-NeXT、Magma 等）。

- **对比方法**：
  - **随机头部掩蔽**：作为基线，证明 VRH 的因果效应不是任意头部都具备的。
  - **不同评分变体（Ablation）**：在统一设计空间中，逐一替换查询令牌、键聚合方式和跨样本聚合方式，共构造多种头部评分变体进行对比。
  - **跨模型迁移**：在共享 LLM 骨干的模型之间，将某一模型识别出的 VRH 直接迁移到另一模型中掩蔽，验证架构共享性。

- **实验任务**：视觉定位（visual grounding）为主要评测任务，辅以 VQA 泛化评测作为迁移性检验。

## 4. 资源与算力

- **论文未明确报告其使用的 GPU 型号、数量或训练时长**。所有实验均基于已开源的预训练 VLM 进行推理和注意力头分析，不涉及从零开始的模型训练。可以推测评估所需的算力相对有限，但作者未提供详细的硬件配置信息，这一点属于论文中未说明的部分。

## 5. 实验数量与充分性

- **实验数量**：实验规模较为充分，主要包括：
  - **跨模型验证**：在 11 个 VLM 上的系统性验证。
  - **跨基准验证**：在 5 个指代表达基准上的精度降级测试。
  - **消融实验**：针对评分方法三个维度（查询令牌、键聚合、跨样本聚合）的逐一替换对比。
  - **跨任务泛化**：在 7 个 VQA benchmark 上的因果性迁移验证。
  - **架构共享实验**：对 3 组共享 LLM 骨干的 VLM 进行交叉掩蔽测试。

- **充分性与客观性评价**：
  - **优点**：模型覆盖面广（11 个模型），基准类型多样（指代定位 + VQA 泛化），对比基线丰富（随机掩蔽 + 多种评分变体），能够有力地支撑“VRH 具有因果性、稀疏性、普遍性和架构共享性”的核心论点。
  - **潜在不足**：论文未报告重复实验的方差或置信区间，也缺少对模型规模（如 13B 及以上）的覆盖。整体实验设计在基准覆盖上已较充分，但随机种子、评估重复次数等细节未给出，可能影响结果的精确可复现性。

## 6. 论文的主要结论与发现

- **视觉检索头的存在性**：VLM 中确实存在少数（约 1.7%–2.6%）因果负责视觉信息检索的注意力头，即 VRH。
- **掩蔽效应显著**：仅掩蔽 top-20 个 VRH，即可使视觉定位精度下降高达 **80 个百分点**；同数量随机头几乎没有影响。
- **复现“因果-稀疏-普遍”三元组**：VRH 复现了文本检索头已有的三个核心特质——因果性（掩蔽后性能大幅下降）、稀疏性（仅占极少数头部）、普遍性（在多个模型和基准上均有效）。
- **新发现的特性**：
  - **跨任务泛化**：VRH 虽通过边界框预测任务发现，但在属性、空间、计数、视觉数学等 VQA 任务上依然保持因果性。
  - **功能特异性**：破坏 VRH 主要损害定位能力，但保留了模型的输出格式能力，说明其功能高度专门化。
  - **架构共享性**：共享 LLM 骨干但视觉编码器、投影器和指令调优各不相同的 VLM，其 VRH 可跨模型因果迁移，表明 VRH 的位置可能由语言骨干决定。

## 7. 优点

- **统一理论框架**：将多种已有的头部评分方法统一到一个三维设计空间并系统比较，方法论上有较强的归纳性和普适性，避免了各自为政的评分方式所带来的混乱。
- **因果验证为本**：所有头部识别结果均通过掩蔽因果实验验证，避免了纯相关性分析的陷阱，结论更加可靠。
- **覆盖面广、结论稳健**：在 11 个模型、多个基准和多种任务上的一致结果，使“VRH 存在”这一结论具有很强的说服力。
- **发现新颖且可操作**：VRH 的架构共享性暗示了模型编辑的可能性——在无需重新训练的情况下，可以根据 VRH 的位置对模型进行跨模型迁移的可解释调整，这对多模态模型的可解释性研究有较大价值。
- **稀疏性带来实践价值**：只需关注约 2% 的注意力头即可控制模型的视觉定位能力，为模型审计提供极小的干预面。

## 8. 不足与局限

- **算力信息缺失**：论文未报告硬件配置与大致运行成本，对于希望复现的研究者而言缺少参考。
- **模型规模覆盖有限**：实验模型集中在 7B–8B 量级（如 Qwen2.5-VL-7B、InternVL3-8B 等），未覆盖更大规模的 VLM（如 13B、30B 或 70B 级别），结论在大模型上的适用性尚待验证。
- **下游任务覆盖仍有空间**：虽然验证了跨向 VQA 的泛化，但未覆盖图文检索、视觉对话、多轮交互等更复杂的多模态任务。
- **功能特异性的解读可能有替代解释**：掩蔽 VRH 后“输出格式保留但定位受损”，也可能与其他因素（如解码策略、采样机制）相关，作者未完全排除此类替代解释。
- **跨模型迁移的边界未彻底探明**：论文验证了共享 LLM 骨干的模型间 VRH 的可迁移性，但未进一步测试骨干不同的模型之间是否也存在部分共享机制。
- **评估方差与显著性检验缺失**：缺少多次重复实验的统计显著性检验和误差范围报告，在一定程度上削弱了量化结论的严谨性。

（完）
