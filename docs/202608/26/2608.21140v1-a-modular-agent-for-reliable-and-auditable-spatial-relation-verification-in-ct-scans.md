---
title: A Modular Agent for Reliable and Auditable Spatial Relation Verification in CT Scans
title_zh: 一种用于CT扫描中可靠且可审计的空间关系验证的模块化智能体
authors: "Simon Vincent Abel, Heiko Hillenhagen, Michael Götz, Timo Ropinski, Ayhan Can Erdur, Daniel Santak Wolf"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21140v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 模块化医学影像智能体用于CT空间关系验证，面向医学视觉语言系统
tldr: "针对现有视觉语言模型在医学图像空间推理中的不可靠问题，提出模块化医学图像智能体，将空间关系验证分解为语言解析、YOLO定位和几何验证三阶段。在MIRP基准上达到94.1%准确率，比端到端Qwen2-VL高42.5个百分点，保持可解释和可审计中间结果。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21140v1/fig-001.webp\", \"caption\": \"Fig. 1. Hybrid task-oriented agent for medical image reasoning. To ensure reliable and auditable answers, our agent delegates specific visual reasoning tasks to specialized components. A central VLM-based controller receives a CT slice and a user prompt, extracts the core query, and routes it to an appropriate task-specific pathway. The pathway processes the image and returns a structured, verifiable result. Box colors indicate module type: VLM/language (blue), YOLO perception (green), deterministic logic (orange), and input/output or future extension (gray). Blue arrows mark the evaluated spatial-verification flow; dashed elements mark future extensions. In the evaluated pathway, the binary answer is computed from YOLO detections and deterministic geometry rather than directly predicted by the VLM.\", \"page\": 4, \"index\": 1, \"width\": 625, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2608.21140v1/fig-002.webp\", \"caption\": \"Table 2. Stage-wise failure attribution for incorrect or invalid hybrid-agent outputs of the best-performing hybrid-agent configuration (Qwen2-VL + hybrid agent). Percentages are computed over the 55 incorrect or invalid cases. Failures were assigned to the earliest identifiable failing stage. In LLM-fallback cases where no separate pre-mapping raw query was stored, incorrect detector-aligned queries were conservatively attributed to parsing/query extraction.\", \"page\": 8, \"index\": 2, \"width\": 525, \"height\": 323}]"
motivation: 现代视觉语言模型在医学图像空间关系推理中表现不佳，给诊断准确性带来风险。
method: 将自然语言查询转换为结构化元组，用YOLO定位器官，并通过确定性几何规则验证空间关系。
result: "在MIRP基准上达到94.1%准确率和94.2% F1，比直接提示Qwen2-VL高42.5个百分点。"
conclusion: 显式模块化空间验证可作为构建可靠、可审计的医学影像报告智能体的有效模块。
---

## 摘要
可靠的空间理解是未来旨在支持放射学报告生成和结构化图像理解的医学视觉-语言系统的重要先决条件。尽管现代视觉-语言模型（VLMs）在许多医学影像任务上表现出有希望的性能，但近期证据表明，它们在受控空间推理方面仍然薄弱，并且常常无法可靠地将空间关系基于图像证据进行 grounding。鉴于放射学推理依赖于理解解剖结构和发现的相对位置，这种空间弱点对诊断准确性构成风险。我们提出了一种用于轴向CT切片中二元空间关系验证的模块化医学影像智能体。该系统不是直接端到端地预测空间答案，而是将任务分解为明确的阶段：语言解析、解剖定位和确定性几何验证。自然语言查询被转换为结构化关系元组，查询的器官通过基于YOLO的检测器进行定位，最终的空间决策则使用确定性几何规则从对象中心计算得出。我们在留出的MIRP空间问答基准上评估了该方法，并将其与代表性的端到端VLM基线进行比较。性能最佳的混合配置达到了94.1%的准确率和94.2%的F1分数，在准确率上比直接使用Qwen2-VL提示高出42.5个百分点，同时保留了可解释的中间表示和可审计的推理阶段。结果表明，显式的模块化空间验证可以成为未来面向报告生成的医学影像智能体的一个有前景的构建模块。

## Abstract
Reliable spatial understanding is an important prerequisite for future medical vision-language systems that aim to support radiological report generation and structured image understanding. While modern vision-language models (VLMs) show promising performance on many medical imaging tasks, recent evidence suggests they remain weak in controlled spatial reasoning and often fail to reliably ground spatial relations in image evidence. Given that radiological reasoning hinges on understanding the relative positions of anatomical structures and findings, this spatial weakness poses risks to diagnostic accuracy. We present a modular medical imaging agent for binary spatial relation verification in axial CT slices. Instead of directly predicting spatial answers end-to-end, the system decomposes the task into explicit stages: language parsing, anatomical localization, and deterministic geometric verification. Natural-language queries are converted into structured relation tuples, queried organs are localized with a YOLO-based detector, and the final spatial decision is computed from object centers using deterministic geometric rules. We evaluate the approach on the held-out MIRP spatial QA benchmark and compare it against representative end-to-end VLM baselines. The best-performing hybrid configuration reaches 94.1% accuracy and 94.2% F1, outperforming direct Qwen2-VL prompting by 42.5 percentage points in accuracy, while preserving interpretable intermediate representations and auditable reasoning stages. The results suggest that explicit modular spatial verification can serve as a promising building block for future report-oriented medical imaging agents.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义（研究动机与背景）

- **核心问题**：现代视觉-语言模型（VLMs）在医学图像空间关系推理上表现极差，在受控测试中接近随机水平（约50%准确率），无法可靠地将空间语言（如"左/右"、"上/下"）与图像证据进行 grounding。
- **临床意义**：放射学报告的核心内容之一就是解剖结构的相对位置描述，空间定位错误可能造成严重后果（如椎体手术定位错误、肿瘤与邻近结构关系判断失误）。
- **整体含义**：论文主张放弃端到端的黑盒预测范式，转而提出一种模块化、工具调用的医学影像智能体架构，将空间推理分解为可审计的显式阶段，为未来面向报告生成的医学影像智能体提供一个可靠的基础构建模块。

## 方法论：核心思想、关键技术细节与流程

**核心思想**：让语言模型负责它擅长的语言理解和任务路由，将视觉定位交给专用检测器，把空间关系判定交给确定性几何规则——没有任何神经网络直接预测最终的真值答案。

**系统架构（四阶段流程）**：

1. **元组提取（Tuple extraction）**：将自然语言问题转换为结构化关系元组，如 "Is the liver left of the spleen?" → `(liver, left of, spleen)`。使用轻量级解析器，并以 VLM 作为回退机制处理复杂表述。
2. **本体匹配（Ontology matching）**：将提取的实体映射到检测器的标准类别。若映射失败，则立即返回无效结果并记录审计条目，避免支持不支持的查询。
3. **解剖定位（Localization）**：调用基于 YOLO 的检测器定位CT切片中的目标结构，每个类别仅传递最高置信度的边界框。
4. **确定性几何验证（Deterministic verification）**：根据检测对象质心的水平或垂直排序，显式计算空间关系的真值，直接从工具提供的坐标推导，避免神经网络的预测误差。

**审计机制**：系统记录完整的中间状态——原始提示、提取的查询、本体匹配结果、选中的检测框、二元结果及审计信息。任何错误案例均可追溯到最早失败的模块，实现精细的失败归因。

## 实验设计：数据集、基准与对比方法

- **基准**：MIRP RQ1 空间问答基准（MICCAI 2025 发布），包含轴向CT切片配对的解剖结构间二元空间关系问题。
- **数据集划分**：将 MIRP RQ1 划分为训练集和测试集，留出的测试集包含 **938个图像-问题对**。
- **检测器训练**：YOLO 检测器在训练集（由分割导出的边界框）基础上，补充 AMOS 和 BTCV 数据集图像；严格执行患者级隔离（测试集患者不出现在检测器训练中）；最终覆盖 **61个解剖类别**，训练实例数 **421,023个**。
- **对比方法**：
  - 直接端到端 VLM：MedGemma 4b（医学领域模型）、Qwen2-VL 7b（通用模型），温度设为0，确定性解码。
  - 混合智能体：同样的两个 VLM 作为控制器接入混合智能体框架。
  - 先前工作基线（来自 MIRP 排行榜，未在本工作中重测）：GPT-4o、Gemma 3 12b、Pixtral 12b、MedGemma。
- **统一评测协议**：所有系统接收完全相同的提示文本，仅内部推理架构不同；严格评测将无效/失败/非二元输出计为错误。
- **评估指标**：准确率（Accuracy）、F1 分数、无效输出率。

## 资源与算力

- **论文未明确说明**：文中未提供 GPU 型号、数量、训练时长或推理成本等具体算力信息。
- **可推断的规模信息**：检测器训练数据规模较大（421,023个实例、61个类别、多数据集补充），但未报告实际训练耗时；使用的 VLM（MedGemma 4b、Qwen2-VL 7b）均为中等规模开放权重模型，推断成本相对可控。
- **代码可用性**：代码已开源（GitHub），但未附带算力配置说明。

## 实验数量与充分性

- **实验数量概览**：
  - 主实验：4 个本工作配置（2 个直接 VLM + 2 个混合智能体）× 1 个基准（MIRP RQ1 留出测试集），加上 4 个先前工作基线用于参考。
  - 失败归因分析：对最佳配置（Qwen2-VL + hybrid）的 55 个错误案例进行逐阶段归因。
  - 尚未包含不同数据集上的跨域验证或多任务验证。
- **公平性评估**：
  - **优点**：统一提示协议、患者级隔离、确定性解码（温度=0），保证了直接对比的公平性。
  - **注意点**：与先前工作的排行榜基线并非严格配对比较（不同测试数据），论文自己也承认了这一点。
- **充分性判断**：对于验证"模块化空间验证优于端到端 VLM"这一核心主张，实验设计是充分且聚焦的；但作为会议短文，实验广度有限——仅覆盖单一的二元空间关系验证任务，缺乏 3D 推理、距离关系、包含/重叠关系或多结构一致性等更复杂场景的验证。

## 主要结论与发现

- **性能大幅提升**：最佳混合配置（Qwen2-VL + hybrid agent）达到 **94.1% 准确率** 和 **94.2% F1**，较直接 Qwen2-VL 提示（51.6% 准确率）提升 **42.5 个百分点**。
- **直接 VLM 接近随机水平**：所有直接 VLM 基线（包括先前工作的 GPT-4o、Gemma 3、Pixtral 等）准确率均在 50% 左右，验证了医学图像空间推理对端到端 VLM 的挑战性。
- **失败归因高度集中**：最佳混合配置剩余 55 个错误中，**50.9%（28例）** 来自 YOLO 定位不精确、**27.3%（15例）** 来自解析/查询提取错误、**21.8%（12例）** 来自检测缺失；没有错误归因于问题提取、路由、本体匹配、几何歧义或格式化/运行时问题。
- **系统性能受限于工具质量**：检测器的定位精度和召回率是当前系统的主要瓶颈，而非语言理解或几何推理。

## 优点

- **范式转变**：从黑盒预测转向显式、可审计的模块化推理，每个中间阶段都是可检查的。
- **确定性验证**：最终空间决策由确定性几何规则计算，而非神经网络预测，从根本上消除了空间关系上的幻觉问题。
- **可解释性与失败归因**：阶段式分析使错误可追溯到具体模块，指导后续改进方向。
- **统一接口下的公平对比**：混合智能体与直接 VLM 使用相同外部提示，严格隔离了内部推理架构差异这一变量。
- **患者级隔离训练协议**：确保检测器评估的泛化性能可信。
- **工程可扩展性**：架构设计支持未来扩展更多工具（如存在性验证、测量推理、报告生成），当前空间验证是概念验证。
- **代码开源**：有利于可复现性和后续研究。

## 不足与局限

- **空间推理范围有限**：仅评估轴向 CT 切片中的成对 2D 空间关系，未涉及完整 3D 体积推理、距离敏感关系、包含关系、重叠关系或多结构一致性验证。
- **任务覆盖单一**：仅验证了二元空间关系这一受控子问题，尚无端到端报告生成或更全面临床推理任务的验证。
- **检测器决定性能上限**：YOLO 定位误差和漏检是错误的主要来源，说明系统天花板受制于基础感知工具质量；提升检测精度是当前最关键的改进方向。
- **解析器对 VLM 回退的依赖**：采用 LLM 回退解析复杂自然语言时，存在解析错误风险；论文对回退场景下的错误归因采用了保守策略，可能低估了语言理解的实际错误贡献。
- **可比性限制**：与先前工作基线的比较并非在同一测试数据上进行，严格意义上不是配对比较。
- **未报告的算力成本**：缺乏硬件配置和推理/训练成本的数据，不利于评估实际临床部署的经济可行性。
- **临床应用距离**：作为概念验证，尚未经过临床环境验证，也未集成到完整的工作流中。

（完）
