---
title: "WoundFormer: Multi-Scale Spatial Feature Fusion for Multi-Class Wound Tissue Segmentation"
title_zh: WoundFormer：面向多类伤口组织分割的多尺度空间特征融合
authors: "Muhammad Ashad Kabir, Rabin Dulal"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.19868v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于Transformer的伤口分割架构
tldr: "针对多类伤口组织分割中类内差异大、标注数据少等挑战，提出WoundFormer框架，通过将SegFormer解码器替换为空间保持的多尺度聚合头，维持特征拓扑并增强卷积融合，从而强化层次空间特征交互。在WoundTissueSeg数据集上，六类组织分割平均Dice达81.9%，较基线提升最高4.3个百分点，特别是在少数类上改进一致，为伤口量化评估提供更可靠支持。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19868v1/fig-001.webp\", \"caption\": \"Table 2. Abalation analysis. A. Conv. means additional convolutional layers.\", \"page\": 7, \"index\": 1, \"width\": 664, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2605.19868v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative comparison of segmentation results on representative samples from the WoundTissueSeg dataset. From left to right: input, ground truth, nnU-Net, FPN+VGG16, SegFormer-B5, DFUTissueSegNet, and WoundFormer. Color legend indicates tissue classes.\", \"page\": 7, \"index\": 2, \"width\": 728, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2605.19868v1/fig-003.webp\", \"caption\": \"Table 1. Quantitative comparison on the WoundTissueSeg dataset. DSC (%) is reported per class and averaged. GFLOPs (GLPs), model parameters (Param, in millions), and inference time (seconds per image, meanstd) are provided. WoundFormer results are shown with and without data augmentation.\", \"page\": 6, \"index\": 3, \"width\": 752, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2605.19868v1/fig-004.webp\", \"caption\": \"Table 3. Quantitative comparison on the DFUTissue dataset.\", \"page\": 8, \"index\": 4, \"width\": 721, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2605.19868v1/fig-005.webp\", \"caption\": \"Fig. 1. The proposed WoundFormer architecture consists of two main components: a hierarchical Transformer encoder, which extracts coarse and fine features, and a convolutional decoder, which fuses these multi-level features to predict the semantic segmentation mask.\", \"page\": 3, \"index\": 5, \"width\": 725, \"height\": 310}]"
motivation: 现有伤口分割方法多限于二值分割，无法有效处理组织异质性强、类间相似度高的多类组织分割任务。
method: 提出WoundFormer，用空间保持的多尺度聚合头替换SegFormer解码器，以维持特征拓扑并引入卷积融合强化上下文交互。
result: "在WoundTissueSeg基准上达到81.9% Dice，超越CNN和transformer基线最多4.3个点，且少数类提升稳定。"
conclusion: 显式建模层次空间交互能有效增强transformer对异质伤口组织的表示能力，提升多类分割精度，助力临床伤口评估。
---

## 摘要
慢性伤口如糖尿病足溃疡和压力性损伤需要准确的组织级评估以指导治疗规划和监测愈合进展。虽然深度学习方法已推动自动化伤口分析的发展，但由于高类内变异和有限的标注数据，大多数现有方法集中于二元分割，未能充分建模异质的组织构成。因此，多类伤口组织分割仍然是一个具有挑战性且临床相关的问题。我们提出 WoundFormer，一种基于 Transformer 的框架，用于增强多类伤口组织分割的层次化空间特征融合。具体而言，我们用空间保留的多尺度聚合头替换标准的 SegFormer 解码器，该头在跨尺度集成期间保持特征拓扑，并通过卷积融合加强上下文交互。这种设计改进了边界定位和对视觉相似组织类别的区分，同时保持了 Transformer 的效率。我们在 WoundTissueSeg 数据集（147 张图像，六种组织类别）和另一个基准（DFUTissue 数据集）上评估了 WoundFormer。所提方法在 WoundTissueSeg 基准上实现了 81.9% 的整体 Dice 分数，优于强大的 CNN 和基于 Transformer 的基线高达 4.3 个 Dice 点，并在少数组织类别上取得了一致改进。这些结果表明，显式建模层次化空间交互增强了 Transformer 对异质伤口组织分割的表示，并支持更可靠的定量伤口评估。

## Abstract
Chronic wounds such as diabetic foot ulcers and pressure injuries require accurate tissue-level assessment to guide treatment planning and monitor healing progression. While deep learning methods have advanced automated wound analysis, most existing approaches focus on binary segmentation and inadequately model heterogeneous tissue composition due to high intra-class variability and limited annotated data. Multi-class wound tissue segmentation, therefore, remains a challenging and clinically relevant problem. We propose WoundFormer, a transformer-based framework that enhances hierarchical spatial feature fusion for multi-class wound tissue segmentation. Specifically, we replace the standard SegFormer decoder with a spatially-preserving multi-scale aggregation head that maintains feature topology during cross-scale integration and strengthens contextual interactions through convolutional fusion. This design improves boundary localization and discrimination between visually similar tissue categories while preserving transformer efficiency. We evaluate WoundFormer on the WoundTissueSeg dataset (147 images, six tissue classes) and a second benchmark (DFUTissue dataset). The proposed method achieves an overall Dice score of 81.9%, outperforming strong CNN- and transformer-based baselines by up to 4.3 Dice points on the WoundTissueSeg benchmark, with consistent improvements across minority tissue classes. These results indicate that explicit modeling of hierarchical spatial interactions enhances transformer representations for heterogeneous wound tissue segmentation and supports more reliable quantitative wound assessment.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究背景

- **研究问题**：慢性伤口（如糖尿病足溃疡、压力性损伤）的临床管理往往需要对创面内多种组织类型进行像素级分类（即**多类伤口组织分割**），以便客观评估愈合进程、指导清创和治疗决策。
- **现有挑战**：
  - 多数已有深度学习方法仅聚焦于**二值分割**（区分创面与正常皮肤），无法刻画创面内异质组织的构成。
  - 多类组织分割面临**类内差异大、类间视觉相似、类别严重不平衡**以及**标注数据稀缺**等难点。
  - 主流的 Transformer 分割框架（如 SegFormer）虽具备强大的全局上下文建模能力，但其**全 MLP 解码器在跨尺度融合时易丢失空间结构**，不利于精细边界定位和相似组织的辨别。
- **整体意图**：提出 **WoundFormer**，通过重新设计解码器，增强层次化多尺度空间特征交互，提升多类伤口组织分割的精度与鲁棒性。

## 2. 方法论

### 2.1 编码器

- 直接采用 **SegFormer 的 MiT-B5** 层级式 Transformer 编码器，输出 4 个不同分辨率的特征图 \(F_i \in \mathbb{R}^{H_i\times W_i\times C_i}\)，其中 \(i=1,2,3,4\)，分辨率从高到低（1/4 到 1/32）。

### 2.2 空间保持的多尺度解码器（核心创新）

- **通道对齐**：每个编码器特征图经 \(1\times 1\) 卷积、批归一化和 ReLU 激活，统一投影到通道数 \(C=128\)：
  \[
  \hat{F}_i = \sigma\left(\mathrm{BN}(\mathrm{Conv}_{1\times 1}(F_i))\right)
  \]
- **粗到细渐进式融合**：
  - 从最低分辨率开始，令 \(X = \hat{F}_4\)。
  - 对 \(i=3,2,1\) 依次执行：
    1. 将当前特征 \(X\) 双线性上采样至与 \(\hat{F}_i\) 相同分辨率。
    2. 将 \(\hat{F}_i\) 与上采样后的 \(X\) 沿通道拼接。
    3. 通过 \(1\times 1\) 卷积、批归一化和 ReLU 得到更新后的 \(X\)。
  该过程保持了特征图的空间拓扑，使高分辨率细节逐步注入语义丰富的低分辨率表示。
- **空间细化**：融合完成后，使用 \(3\times 3\) 卷积进一步建模局部空间依赖：
  \[
  X_{\text{fused}} = \mathrm{Conv}_{3\times 3}(X)
  \]
- **预测层**：通过 \(1\times 1\) 卷积生成分割 logits，最终输出分辨率为输入 1/4 的类别预测图。

### 2.3 设计动机

- 保留特征图**空间结构**，避免 MLP 解码器因 Token 扁平化而丢失局部上下文。
- 结合**层级式 Transformer 的全局语义**与**卷积的局部空间细化**，在不显著增加计算量的前提下，改善边界描绘和易混淆类别的判别。

## 3. 实验设计

### 3.1 数据集与基准

- **WoundTissueSeg 数据集**：
  - 147 张图像，6 类组织：Granulation、Slough、Maceration、Necrotic tissue、Bone、Tendon。
  - 官方划分：训练 118/验证 14/测试 15。
- **DFUTissue 数据集**（额外独立基准）：
  - 110 张图像，固定分辨率 256×256，4 类：Granulation、Callus、Fibrin、Background。
  - 划分：训练 78/验证 16/测试 16。
- 两类数据集**未进行交叉验证**，严格使用原始划分以保证公平。

### 3.2 对比方法

包含多种经典及前沿架构：

- **通用分割模型**：DETR、Mask R-CNN、HarDNet、SegNet、TransNeXt、Deeplabv3、SegFormer‑B0 到 B5。
- **医学专用分割模型**：nnU‑Net、Swin UNETR、FuSegNet、FPN+VGG16、DFUTissueSegNet。

### 3.3 统一实验协议

- 所有模型均使用 PyTorch 实现，加载官方预训练权重。
- 统一配置：输入大小 224×224，Adam 优化器（lr=\(1\times10^{-4}\)），batch size=8，交叉熵损失，ReduceLROnPlateau（factor 0.1，patience 5），早停 patience=15。
- 无数据增强的主实验确保公平对比；增强实验采用 DFUTissueSegNet 相同的概率化几何/光度/噪声变换。

## 4. 资源与算力

- 论文**未明确报告** GPU 型号、数量或训练总时长。
- 仅提供了模型的 **GFLOPs、参数量**及**推理时间**（秒/图），表明 WoundFormer 与 SegFormer‑B5 的算力消耗相当（约 183.6 GFLOPs，84.9M 参数）。
- 作者未提及训练过程的时间开销或硬件配置，属于信息缺失。

## 5. 实验数量与充分性

- **对比实验**：在 WoundTissueSeg 上比较了近 20 种基线，覆盖 CNN、Transformer、专用医学模型。
- **多数据集验证**：除主数据集外，额外在 DFUTissue 上独立评估，并给出有无数据增强的结果。
- **消融实验**：深入分析解码器组件（批归一化、激活函数、卷积核大小、1×1 与 3×3 的堆叠、损失函数选择）对 Dice 的影响，从 51.11% 逐步提升至 85.50%。
- **统计检验**：采用配对 Wilcoxon 符号秩检验评估显著性，增加了结论的可信度。
- 实验设计**较为全面**，统一协议保证了公平性，但数据集规模偏小，且未进行跨验证或多中心外部验证。

## 6. 主要结论与发现

- WoundFormer 在 WoundTissueSeg 上**无增强**即获得 **81.9% 的平均 Dice**，相比最强基线（SegFormer‑B5 的 77.6%）提升 **4.3 个百分点**，尤其在 Bone、Tendon 等少数类上改善显著。
- 在 DFUTissue 基准上，无增强达 **78.34%**，有增强达 **85.40%**，与专用模型 DFUTissueSegNet 性能可比，验证了方法的泛化能力。
- 消融实验确认**空间保持的 1×1 与 3×3 卷积组合**和**批归一化+ReLU** 是性能提升的关键。
- 结论：**显式建模层次化空间交互**能有效增强 Transformer 在异质伤口组织分割中的表示能力，为客观、可重复的伤口量化评估提供支撑。

## 7. 优点

- **方法创新性**：用卷积和空间保留策略替代传统的轻量 MLP 解码器，在保持 Transformer 效率的同时显著提升边界和细粒度判别能力。
- **计算效率**：相比 SegFormer‑B5，仅增加极少量参数和计算量，却带来明显精度增益。
- **一致且全面**：不仅在主数据集上取得领先，还在独立数据集上维持竞争力，且对各类别（包括少数类）均有稳定改进。
- **实验严谨**：公平的统一训练协议、统计显著性检验、详细的消融分析，支撑了主要主张。

## 8. 不足与局限

- **数据集规模与多样性有限**：WoundTissueSeg 仅 147 张图像，DFUTissue 仅 110 张，且未包含多中心、多设备、多种族的数据，泛化能力仍需更大规模验证。
- **计算资源信息缺失**：未说明 GPU 型号、训练时长，不利于复现成本评估。
- **编码器固定**：仅尝试了 MiT‑B5，未探讨与其他层级式 Transformer（如 Swin、ConvNeXt 等）结合的潜力。
- **损失函数单一**：主要使用交叉熵，虽尝试过 Focal+Dice 但未深入调优，可能未充分挖掘复合损失的潜力。
- **类间相似性挑战**：虽然在部分类别上表现提升，但 Maceration 与 Slough 等高度相似类的 Dice 仍存在波动，未深入分析错误模式。
- **临床应用链路未打通**：尚未整合到自动化临床报告系统或进行临床验证。

（完）
