---
title: A Human-in-the-Loop Framework for Efficient Prompt Selection in Microscopy Vision-Language Models
title_zh: 一种面向显微镜视觉语言模型中高效提示选择的人机协作框架
authors: "Abhiram Kandiyana, Ankur Mali, Lawrence O. Hall, Peter R. Mouton, Dmitry Goldgof"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20495v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于显微镜图像分类的视觉语言模型
tldr: "针对显微镜图像分类中专家标注成本高的问题，提出一种人机协同框架，利用视觉语言模型草拟候选示例并让专家验证编辑，通过目标驱动的主动学习策略选择优先标注的图像，以最少注释达到目标性能。实验表明，该方法平均仅需20个标注图像即可达到100%测试准确率，显著减少标注成本。"
source: arxiv
selection_source: fresh_fetch
motivation: 减少显微镜图像分类中耗时且昂贵的专家标注，同时保持模型性能。
method: 将提示集构建视为目标驱动的主动学习问题，优先选择专家验证的图像，结合VLM草拟与专家编辑。
result: "在严格低资源条件下，平均仅需20个标注图像即可达到100%测试准确率，远优于随机选择。"
conclusion: 该人机协同框架有效利用生成式AI，专家参与验证和优化输出，大幅降低注释成本，实现高效提示选择。
---

## 摘要
用于显微镜图像分类的深度学习流水线通常需要昂贵、劳动密集且耗时的人工专家标注，以生成高质量的训练真实标签。最近的研究表明，通过对视觉语言模型进行提示微调，可以通过构建一个由专家验证的图像-标题示例组成的小型提示集来减少人工标注，该提示集在推理时作为少样本上下文被重复用于对所有剩余图像进行分类。为了进一步减少工作量，视觉语言模型可以为候选示例起草标题，然后专家进行验证并轻微编辑，而不是从头开始编写文本。然而，两个实际问题仍未解决：（1）哪些未标记图像应优先进行验证，以及（2）需要多少已验证的示例才能达到性能目标。在这项工作中，我们通过将提示集构建制定为一个目标驱动的主动学习问题来解决这些问题，该问题优先考虑哪些图像需要标注。我们在严格低资源约束下使用小型未标记池研究了三种互补的筛选标准。实验表明，我们的方法使用比随机选择少得多的专家验证图像就能达到目标性能，平均仅需20张标注图像即可实现100%的测试准确率。更广泛地说，我们的人机协作框架展示了在生物医学图像分析中以人为中心的生成式人工智能应用，其中专家仍然积极参与验证和优化模型输出，同时显著降低标注成本。代码和数据将公开提供。

## Abstract
Deep-learning pipelines for microscopy image classification often require expensive, labor- and time-intensive expert annotation to produce high-quality ground truth for training. Recent work has shown that prompt tuning of vision-language models (VLMs) can reduce manual annotation by constructing a small prompt set of expert-verified image-caption exemplars that is reused as few-shot context to classify all remaining images at inference time. To further reduce effort, the VLM can draft captions for candidate exemplars, which experts then verify and lightly edit instead of writing text de novo. However, two practical questions remain unaddressed: (1) which unlabeled images should be prioritized for verification, and (2) how many verified exemplars are needed to reach a performance target. In this work, we address these questions by formulating prompt-set construction as a target-driven active learning problem that prioritizes which images to annotate. We study three complementary selection criteria under strict low-resource constraints with small unlabeled pools. Experiments show that our methods reach the target performance with substantially fewer expert-verified images than random selection, achieving 100% test accuracy with as few as 20 annotated images on average. More broadly, our human-in-the-loop framework demonstrates a human-centered use of generative AI in biomedical image analysis, where experts remain actively involved in verifying and refining model output while significantly reducing annotation cost. Code and data will be publicly available.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：显微镜图像分类在神经科学研究中至关重要（如评估细胞损伤、脑衰老、神经炎症等），但传统深度学习需要大量专家标注，成本高昂且耗时。近期视觉语言模型（VLM）通过提示微调（prompt tuning）可以在推理时利用少量专家验证的图像–标题示例（prompt set）进行分类，减少标注工作。VLM 还能自动为候选图像起草标题，专家只需验证和轻量编辑。
- **核心问题**：尽管已有工作（如 APT-USF）利用 VLM 草稿+专家验证的流程，但仍有两个实际问题未解决：  
  1. 应该优先选择哪些未标注图像给专家验证？  
  2. 需要多少已验证示例才能达到目标性能？
- **研究动机**：设计一种人机协作框架，通过主动学习（Active Learning）策略自动选择最有价值的未标注图像进行专家验证，以最小化标注成本，同时满足性能目标。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将提示集构建视为**目标驱动的主动学习问题**。从一个很小的种子提示集（10张图像）开始，迭代地从未标注池中选择一个“主动集”（active set），由 VLM 基于当前上下文草拟标题，专家验证并修正后加入提示集。循环直到验证集准确率达到预设目标（80%）或达到轮次/预算上限。
- **关键技术：三种获取函数（acquisition function）**  
  1. **不确定性引导获取（APT-U）**  
     - 使用随机解码（stochastic decoding，T=5次）估计每个未标注图像的预测不确定性（熵），选择熵最高的 m 个样本。
  2. **复杂度感知不确定性获取（APT-CA）**  
     - 在不确定性基础上加入对标题复杂性和冗余性的惩罚。  
     - 复杂性由标题长度（token数）和与已有标题的语义相似度（余弦相似度）加权组成。  
     - 通过拉格朗日松弛将约束优化转化为最大化 `u(x) - λc * κ(x)`，选择得分最高的样本。
  3. **密度树边界获取（APT-DTB）**  
     - 基于 VLM 图像编码器（如 BioMedCLIP）提取的嵌入空间，构建 kNN 图，通过“最陡上坡”算法识别密度聚类和边界区域。  
     - 计算边界分数 b(i)（邻居中属于不同聚类根的比例），选择位于聚类间“山谷”区域的样本。  
     - 通过限制微小簇的贡献避免集体异常值（collective outliers）干扰。
- **迭代流程**：每一轮主动集大小 m=10，从未标注池的候选子集（|C|=100）中选取。专家验证后更新提示集，然后在验证集上评估是否满足停止条件。

## 3. 实验设计：数据集、基准和对比方法

- **数据集**：Lurcher 数据集——2D显微镜图像，来自21只小鼠小脑组织切片（11只Lurcher突变，10只野生型），分类任务是区分突变与正常。所有图像在10倍放大、甲酚紫染色下拍摄。
- **评估协议**：四折留一法（4-fold leave-one-out）。每折：2只动物（每类1只）作为测试集，2只作为验证集，17只用于训练（从中10张初始种子提示集+剩余作为未标注池）。
- **基准方法**：APT-USF（随机获取，固定提示集大小36，无主动选择）。
- **对比方法**：三种变体——APT-U（不确定性）、APT-CA（复杂度感知）、APT-DTB（密度树边界）。
- **评价指标**：图像级平均分类准确率、动物级准确率（多数投票）、所需专家标注图像数量（标注效率）。

## 4. 资源与算力

- 论文未明确提及使用的 GPU 型号、数量或训练时长。仅说明使用 GPT-4o（通过 OpenAI API）作为 VLM 模型，因此计算资源主要由 API 调用构成。主动学习和提示集构建过程不涉及模型微调，所有计算均在前向推理和文本嵌入（Sentence-Transformer）上进行，对算力需求相对较低。实验在 CPU/轻度 GPU 环境下可复现。

## 5. 实验数量与充分性

- **实验数量**：在 Lurcher 数据集上进行了 4 折交叉验证，每折运行所有方法（基线+三种变体），总计 4×4=16 组实验。此外进行了消融实验（表3），使用三种不同的嵌入骨干（CLIP、BioMedCLIP、MedSigLIP）在3折上评估 APT-DTB，共 3×3=9 组。
- **充分性评价**：
  - 实验覆盖了对比基线、多种主动学习策略、不同嵌入模型，但仅在一个数据集上进行，缺乏对其他显微镜数据集或不同模态的泛化验证。
  - 4折交叉验证样本量有限（每折仅2只动物作为测试集），统计稳定性可能不足；论文报告了均值±标准差，但未进行显著性检验。
  - 超参数（如 λc=0.5, α=0.01, β=0.1 等）通过小幅验证集调整，但未在独立测试集上做严格调优分析。
  - 整体而言，实验设计较为客观公平（固定种子、统一验证目标、共享 VLM 模型），但数据集单一限制了结论的普遍性。

## 6. 论文的主要结论与发现

- 三种主动获取函数均优于随机选择（APT-USF），在达到 80% 验证准确率目标时，所需专家标注图像数量大幅减少（平均 20~25 张 vs. 36 张）。
- 在测试集上，所有变体均达到 100% 动物级准确率；图像级平均准确率以 APT-CA 最高（88.6%），比基线提升约 8 个百分点。
- 不确定性引导获取（APT-U）在标注效率上最优（平均 20 张图像即达标）。
- 密度树边界获取（APT-DTB）对嵌入骨干敏感：BioMedCLIP 优于 CLIP 和 MedSigLIP，表明领域适配的嵌入空间更有助于识别决策边界样本。
- 整体结论：所提人机协作框架可**显著降低专家标注成本**，同时保持甚至提升分类性能，体现了“人在回路中”的生成式 AI 在生物医学图像分析中的有效应用。

## 7. 优点

- **方法设计创新**：将提示集构建问题形式化为目标驱动的主动学习，提出了三种互补的获取函数，覆盖不确定性、复杂性、结构多样性，具有理论依据和实用价值。
- **人机协同明确**：专家仅需验证和轻量编辑 VLM 草稿，而非从头写标题，极大降低认知负荷和时间成本。
- **标注效率高**：在严格低资源条件下（候选池仅100张），仅需 20 张左右标注图像即达到 100% 测试准确率，对实际应用意义重大。
- **可解释性强**：VLM 生成的标题包含链式推理（chain-of-thought），提供分类理由，增强模型透明度和专家信任。
- **实验设计严谨**：采用留一法交叉验证、固定种子和验证目标、统一 VLM 模型，确保比较公平。

## 8. 不足与局限

- **单数据集验证**：仅在 Lurcher 小鼠脑切片数据集上测试，缺乏对其他显微镜模态（如病理组织、细胞培养）、其他动物模型或人类样本的泛化评估。
- **样本量小**：数据集仅21只动物，4折交叉验证中测试集仅2只动物，统计波动较大（标准差高，如表2中基线标准差14.7%）。
- **超参数敏感**：方法涉及多个超参数（λc, α, β, k, kρ, kt, kb 等），当前通过小范围验证集选择，未进行系统敏感性分析，可能在不同数据集上需要重新调整。
- **计算假设**：使用 GPT-4o 商用 API，存在成本波动和依赖，且未评估开源 VLM（如 LLaVA）的表现，限制了可复现性和通用性。
- **未处理多类问题**：当前任务为二分类，未验证在更多类别或更细粒度分类上的效果。
- **未报告耗时**：论文未量化专家验证每张图像所需时间，以及整个流程的实际时间成本，无法直接与传统标注方法对比。

（完）
