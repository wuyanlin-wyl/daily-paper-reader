---
title: "EMA: Approximate Nearest Neighbor Search with General Attribute Filtering and Dynamic Updates"
title_zh: "EMA: 支持通用属性过滤和动态更新的近似最近邻搜索"
authors: "Mocheng Li, Baotong Lu, James Cheng, Chenhao Ma"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00734v1"
tags: ["query:vlmmed"]
score: 8.0
evidence: 用于RAG和智能体记忆的过滤近似最近邻搜索
tldr: 针对现有过滤近似最近邻搜索方法局限于范围或标签过滤、难以处理多谓词和动态更新的问题，本文提出EMA算法。它在图边上附加紧凑的Markers，提供零假阴性的保守引导，并结合边界恢复机制进行联合搜索。实验表明，EMA在混合属性和多种选择率下比最先进方法快1.68-12.25倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00734v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of FANN algorithms.\", \"page\": 2, \"index\": 1, \"width\": 1064, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-002.webp\", \"caption\": \"Figure 4: QPS for label+range multi-predicate query at 10%-100% selectivity with 95% recall@10.\", \"page\": 10, \"index\": 2, \"width\": 1048, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-003.webp\", \"caption\": \"Figure 5: QPS for label+range multi-predicate query at 1%-10% selectivity with 95% recall@10.\", \"page\": 10, \"index\": 3, \"width\": 1048, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-004.webp\", \"caption\": \"Figure 6: QPS for composed multi-predicate queries at 1%–10% selectivity with 95% recall@10.\", \"page\": 10, \"index\": 4, \"width\": 1048, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-005.webp\", \"caption\": \"Figure 10: QPS in OCQ at 95% recall@10.\", \"page\": 13, \"index\": 5, \"width\": 775, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-006.webp\", \"caption\": \"Figure 7: QPS for range query at 95% recall@10. Figure 8: QPS for label query at 95% recall@10.\", \"page\": 12, \"index\": 6, \"width\": 1050, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2606.00734v1/fig-007.webp\", \"caption\": \"Figure 2: EMA workflow.\", \"page\": 5, \"index\": 7, \"width\": 1052, \"height\": 364}]"
motivation: 现有FANN方法在处理多谓词过滤和动态更新时效率低、开销大。
method: EMA通过在图的边上附加紧凑的Markers来提供保守的谓词和几何引导，并在查询时结合边界恢复进行联合搜索。
result: 在多种工作负载下，EMA比最先进方法快1.68-12.25倍。
conclusion: EMA有效支持通用属性过滤和动态更新的近似最近邻搜索，显著提升查询效率。
---

## 摘要
过滤近似最近邻（FANN）搜索是一项关键且新兴的任务，用于增强向量数据库的查询能力，支持推荐系统、检索增强生成（RAG）和智能体记忆等应用。然而，现有大多数方法仅限于范围或标签过滤，通常会导致不可接受的索引构建时间和内存开销。谓词无关的方法进一步难以有效处理广泛的谓词选择性。在本文中，我们提出了EMA，一种过滤ANN算法，支持对混合数值和分类属性的多谓词查询以及高效的动态更新。EMA引入了标记（Markers）作为附加在图边上的紧凑摘要，在标记层面提供保守的谓词感知和几何感知指导，且零假阴性。在查询处理过程中，EMA执行标记增强的联合搜索，并带有有界边恢复机制，从而在保持图可导航性的同时实现高效过滤。大量实验表明，在多样化工作负载下，EMA相比最先进的通用过滤ANN方法实现了1.68倍到12.25倍的加速。

## Abstract
Filtering Approximate Nearest Neighbor (FANN) search is a critical and emerging task for strengthening the query capability of vector databases, supporting applications such as recommendation systems, retrieval-augmented generation (RAG), and agent memory. However, most existing methods are limited to range or label filtering, often incurring unacceptable index construction time and memory overhead. Predicate-agnostic approaches further struggle to handle a wide range of predicate selectivities effectively. In this paper, we propose EMA, a filtering ANN algorithm that supports multi-predicate queries over mixed numerical and categorical attributes, and efficient dynamic updates. EMA introduces Markers as compact summaries attached to graph edges, providing conservative predicate- and geometric-aware guidance with zero false negatives at the Marker level. During query processing, EMA performs Marker-augmented joint search with a bounded edge recovery mechanism, enabling efficient filtering while preserving graph navigability. Extensive experiments demonstrate that EMA achieves 1.68x--12.25x speedup over state-of-the-art general filtering ANN methods across diverse workloads.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有过滤近似最近邻搜索（FANN）方法大多局限于单一类型谓词（如仅范围过滤或仅标签过滤），难以高效支持混合数值和分类属性的多谓词组合查询；同时在低选择率（selectivity）和离簇查询（OCQ）场景下，图导航能力严重退化；此外大多数方法缺乏动态更新支持，需要频繁重建索引。
- **整体含义**：本文旨在提出一种统一的、选择性鲁棒、支持通用谓词过滤和动态更新的图索引方案，以提升向量数据库在推荐系统、RAG、智能体记忆等实际应用中的查询效率。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：通过在图的边上附加紧凑的**标记（Marker）**——一种几何感知的谓词相关摘要，实现谓词感知的图导航，同时保证标记层面零假阴性；并在查询时结合**有界边恢复机制**，缓解低选择率下的连通性问题。
- **关键技术细节**：
  - **Codebook**：对每个属性（数值或分类）定义离散化映射，将属性值映射到固定长度位数组的桶索引，用于构建Marker。
  - **Marker构建与传播**：在HNSW图的底层使用RNG风格剪枝时，若候选节点被已有邻居支配，则将该候选节点的属性Marker通过按位OR合并到支配边的Marker中，从而保留被剪枝路径的谓词信息。
  - **多样性感知剪枝**：在保留节点时，使用计数滤波器（CT）鼓励保留属性值不同的邻居，增强图的过滤连通性。
  - **联合搜索**：上层按标准ANN搜索定位入口，底层使用Marker预先检查边的谓词兼容性（数值谓词要求至少一位匹配，分类谓词要求全覆盖），不兼容的边跳过；当兼容边数量低于阈值时，恢复最近的不兼容边以保证最小出度。
  - **动态更新**：插入自然支持；删除采用惰性标记+批量修补（patch）；属性修改局部更新Marker；向量与属性联合修改则删除再插入+修补。
- **公式/算法**：文中给出MEncode编码算法（算法2）、Marker增强剪枝（算法3）、Codebook生成（算法1）的伪代码；Marker检查条件（式1）为MMatch的合取。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集**：四个公开数据集——SIFT (128维, 10M), RedCaps (512维, 4M), YouTube-RGB (1024维, 1M), Wiki (1024维, 15.4M)。每个数据集均赋予合成或真实数值/分类属性，支持多谓词查询。
- **场景**：包括(1)高选择率（10%–100%）和低选择率（1%–10%）下的标签+范围双谓词查询；(2)复杂布尔组合谓词（AND/OR）；(3)纯范围查询对比iRangeGraph；(4)纯标签查询；以及**离簇查询（OCQ）**——向量相似性与谓词属性空间解耦。
- **基准方法**：Milvus, VBase, ACORN, NaviX, Filtered DiskANN, iRangeGraph（共6种）。所有方法在相同硬件上运行，指标为QPS（每秒查询数）和召回率@10（固定95%）。
- **超参数**：M=40, ef_c=300; EMA额外参数s=256, M_div=16, d_min=16。索引构建单线程/32线程，查询单线程（除Milvus内部多线程）。

## 4. 资源与算力

- **实验平台**：Ubuntu 24.04, Intel Xeon Platinum 8358 CPU (64物理核心), 2 TB RAM, HDD存储。**未使用GPU**。
- **计算开销**：论文报告了索引构建时间和索引大小（表5），以及查询QPS。没有提及训练时长（因不需要训练）。

## 5. 实验数量与充分性

- **实验组数**：覆盖4个数据集、多种选择率（1%–100%）、多种谓词类型（range, label, label+range, range+range, 复合布尔）、OCQ场景、动态更新（插入/删除/属性修改/向量+属性修改）。还包含消融研究（如Marker效果、边恢复效果、假阳性率分析等，在附录中提及，但正文摘要未列出具体图表，论文第5.5节有额外研究）。
- **充分性与公平性**：对比方法均使用官方或推荐参数，且对ACORN/NaviX应用了相同的惰性谓词评估策略以确保公平；报告了95%召回下的QPS，若某些基线无法达到则注明其实际召回（如Filtered DiskANN仅报告80%召回）。实验场景覆盖了论文声称的难点（低选择率、OCQ、动态），比较全面。不过缺少在更大规模（如1B）数据集上的验证。

## 6. 论文的主要结论与发现

- EMA在所有场景下均取得最高QPS，加速比1.68×–12.25×。在低选择率（1%–10%）下依然稳定高效，优于所有基线。
- 在OCQ场景下，ACORN和NaviX无法达到95%召回（分别低于60%和约90%），VBase也无法达到，而EMA稳定满足召回要求。
- 动态更新实验中，修补机制（patch）在删除50%时QPS仅下降约30%，而全重建后恢复；属性修改和联合修改场景下EMA保持稳健。
- Marker假阳性率理论上有界（定理4.5、4.6），实际实验中受控且有助于导航。

## 7. 优点

- **方法创新**：提出Marker这一轻量级边摘要，实现零假阴性、几何感知的谓词信息传播，避免传统RNG剪枝带来的过滤连接性丧失。
- **鲁棒性强**：多样性剪枝和边恢复机制使EMA在极低选择率和OCQ场景下仍能保持图导航性，填补了现有方法空白。
- **动态支持**：提供完整的动态操作（插入/删除/修改）且通过局部修补避免频繁全重建，实用性强。
- **理论支撑**：给出了复杂度分析（保持HNSW渐近复杂度）和Marker假阳性率界，为参数选择提供指导。
- **实验全面**：覆盖多种谓词类型、选择率范围、特殊场景（OCQ）及动态更新，对比方法多且公平。

## 8. 不足与局限

- **索引存储开销较大**：由于每条边需存储Marker，表5显示EMA索引大小约为NaviX的1.6倍（SIFT10M: 32 GB vs 7.9 GB），在更大规模数据集上可能成为瓶颈。
- **实验未在极大规模（如1B）上验证**：论文数据集最大15.4M，未测试十亿级规模，实际扩展性有待进一步评估。
- **参数敏感性**：Marker长度s、多样性阈值M_div、边恢复阈值d_min等需要调参，文中仅使用固定值，未展示敏感度分析。
- **动态更新代价**：批量修补仍有一定时间开销（对SIFT5M每调用约45s），且修改操作频率高时性能下降较明显（图9d），未与在线重建方案对比。
- **理论分析有限**：假阳性率推导假设谓词满足独立同分布，实际属性可能存在相关性，可能高估或低估；复杂度分析假设M和m为常数，但Marker大小为s*m，s=256时实际常数较大。
- **适用性限制**：当前方法基于HNSW两层结构，若底层图结构不同（如DiskANN的Vamana），需调整；未讨论分布式或多GPU场景。

（完）
