---
title: "One Modality to Forget Them All: Enhancing Cross-Modal Unlearning in Vision-Language Models"
title_zh: 一模态以忘之：增强视觉语言模型中的跨模态遗忘
authors: "Sudharshan Balaji, Yili Ren, Guangjing Wang, Yimin Chen, Ning Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16442v1"
tags: ["query:vl"]
score: 9.0
evidence: 视觉语言模型遗忘研究
tldr: 机器遗忘用于移除大语言模型的有害知识，但现代视觉-语言模型(VLM)处理文本和视觉输入，引发跨模态遗忘迁移的安全问题。本文首次系统性双向研究三类VLM架构，发现遗忘跨模态迁移但不对称不完整，且易受文字攻击恢复。为此提出CrossInf策略，聚焦对跨模态泛化影响最大的transformer块，减少迁移差距一半以上，将攻击成功率降至近零。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16442v1/fig-001.webp\", \"caption\": \"Figure 4: Three VLM fusion designs studied in this work:MLP\", \"page\": 6, \"index\": 1, \"width\": 494, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-002.webp\", \"caption\": \"Figure 14: Borderline sensitivity analysis. The three bars at\", \"page\": 15, \"index\": 2, \"width\": 492, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-003.webp\", \"caption\": \"Figure 11: Ablation profiles across four interventions in Exp 2, visual→text. Each axis shows a normalized metric: same-\", \"page\": 10, \"index\": 3, \"width\": 506, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-004.webp\", \"caption\": \"Figure 12: Interpretability analysis. Top: LoRA magnitude per layer (Exp 0 red, Exp 1 blue; 𝑟=0.55–0.71). Middle/bottom: CKA\", \"page\": 12, \"index\": 4, \"width\": 801, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-005.webp\", \"caption\": \"Figure 7: Cross-modal transfer results. Top: Exp 0 (text→visual) transfers effectively. Bottom: Exp 1 (visual→text) shows large\", \"page\": 9, \"index\": 5, \"width\": 831, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-006.webp\", \"caption\": \"Figure 2: Overview of the CrossInf experimental framework. Each of the three VLM architectures is evaluated in its baseline\", \"page\": 5, \"index\": 6, \"width\": 1002, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2607.16442v1/fig-007.webp\", \"caption\": \"Table 8: Experiment 0 (text-to-visual transfer). Unlearning on\", \"page\": 16, \"index\": 7, \"width\": 491, \"height\": 675}]"
motivation: VLM中一个模态的遗忘是否迁移到另一模态？存在不对称和浅层遗忘问题。
method: 首次系统性双向研究三类VLM架构，提出影响指导的CrossInf策略聚焦关键transformer块。
result: CrossInf减少跨模态迁移差距一半以上，将文字攻击成功率降至接近零，保持模型效用。
conclusion: 跨模态遗忘存在不对称性和浅层鲁棒性问题，通过聚焦关键组件可有效缓解。
---

## 摘要
机器遗忘被广泛用于从大型语言模型中移除有害知识。然而，现代视觉语言模型（VLM）同时处理文本和视觉输入，引发了一个基本的安全问题：一种模态中的遗忘是否会转移到另一种模态？我们首次对三种VLM架构：LLaVA-1.5（MLP投影）、InstructBLIP（Q-Former）和IDEFICS（门控交叉注意力）进行了系统、双向的跨模态遗忘转移研究。我们发现遗忘会跨模态转移，但转移是不对称且不完全的。在某些情况下，文本遗忘强烈转移到视觉。然而，这种鲁棒性在操控文本视觉呈现的排版攻击下无法保持。在此类攻击下，先前遗忘的知识可以轻易恢复，表明遗忘是浅层的。为了解决转移差距和浅层鲁棒性问题，我们提出了\textsc{CrossInf}，一种基于影响力的缓解策略。受不同模型组件对跨模态转移贡献不均的观察启发，\textsc{CrossInf}将遗忘聚焦于对跨模态泛化影响最大的Transformer块。在具有强融合的架构中，它将转移差距减少一半以上，同时保持模型效用。它还在排版攻击下提高了鲁棒性，将攻击成功率降至接近零。我们进一步进行了三位标注者（κ=0.77）的人工评估以验证我们的发现。最后，我们使用中心核对齐（CKA）分析浅层遗忘，为观察到的转移行为和鲁棒性限制提供了见解。

## Abstract
Machine unlearning is widely used to remove hazardous knowledge from large language models. Modern Vision-Language Models (VLMs), however, process both text and visual inputs, raising a fundamental security question: does unlearning in one modality transfer to the other? We present the first systematic, bidirectional study of cross-modal unlearning transfer across three VLM architectures: LLaVA-1.5 (MLP projection), InstructBLIP (Q-Former), and IDEFICS (gated cross-attention). We find that unlearning transfers across modalities, but the transfer is asymmetric and incomplete. In some cases, text unlearning strongly transfers to vision. However, this robustness is not preserved under typographic attacks that manipulate the visual presentation of text. Under such attacks, previously unlearned knowledge can be readily recovered, indicating shallow unlearning.   To address the transfer gap and shallow robustness, we propose \textsc{CrossInf}, an influence-guided mitigation strategy. Motivated by the observation that different model components contribute unequally to cross-modal transfer, \textsc{CrossInf} focuses unlearning on transformer blocks that most influence cross-modal generalization. It reduces the transfer gap by more than half in architectures with strong fusion, while preserving model utility. It also improves robustness under typographic attacks, reducing the attack success rate to near zero. We further conduct human evaluation with three annotators ($κ{=}0.77$) to validate our findings. Finally, we analyze shallow unlearning using Centered Kernel Alignment (CKA), providing insights into the observed transfer behavior and robustness limitations.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **研究动机**：视觉语言模型（VLM）同时处理文本和视觉输入，现有的安全干预（如机器遗忘）通常仅针对单模态（如文本），导致视觉通道可能成为安全漏洞。核心问题是：**单模态中的遗忘是否会自动迁移到另一模态？** 如果迁移不完全，攻击者可以通过切换模态绕过安全限制。
- **整体含义**：首次系统性、双向研究跨模态遗忘迁移，揭示迁移的不对称性、架构依赖性以及“浅层遗忘”现象（易被排版攻击恢复），并提出针对性缓解策略。

### 论文提出的方法论

- **核心思想**：不同模型组件对跨模态转移的贡献不同，应仅对最关键的Transformer块施加遗忘更新，冻结其余部分，从而增强迁移效果。
- **关键技术细节**：
  1. **Vanilla遗忘基线**：采用Yao等人的三损失函数：梯度上升（驱动模型远离有害输出）+ 拒绝响应交叉熵损失 + 对正常数据的KL散度保留效用。
  2. **CrossInf策略**：
     - **影响力估计**：基于DataInf（适用于LoRA微调模型的闭合形式Hessian近似）计算每个块的影响力。
     - **块级粒度**：将每个（层，LoRA适配模块）对定义为一个块，而非逐参数或逐层，兼顾效率与精度。
     - **跨模态影响力评分**：计算同一模态遗忘集 \(D_f\) 与少量跨模态探针集 \(D_{\text{cross}}\) 之间的影响矩阵，聚合绝对值得到每个块的影响力分数 \(S(b)\)。
     - **Top-\(k\) 选择**：只对得分最高的 \(k\%\) 个块执行遗忘更新，其余冻结。超参数 \(k\) 经实验调优（如LLaVA取50，InstructBLIP/IDEFICS取30）。
  3. **流程**：先计算一次影响力分数（耗时约75-108秒），再在选定块上运行标准遗忘训练。

- **公式/算法说明**：文中给出了块级影响力矩阵的计算公式（式4）及聚合评分式（5），但未列出完整伪代码。核心计算为：\( \text{IF}_b = -G_{\text{cross}}^{(b)} (C^{(b)})^{-1} (G_f^{(b)})^\top \)，其中 \(C^{(b)}\) 为基于 \(D_f\) 的费雪信息矩阵正则化近似。

### 实验设计

- **数据集与场景**：
  - 有害数据：PKU-SafeRLHF（文本）、JailbreakV-28K（图像）。
  - 效用保持：TruthfulQA（文本）、VQA-v2（图像）。
  - 对抗评估：FigStep排版攻击（500张预定义图像）以及自建的90张多类别排版攻击图像、72个概念对齐的文本探针。
- **基准与对比方法**：
  - 对比对象：Vanilla遗忘（全部参数更新） vs. CrossInf（仅更新top-\(k\)块）。
  - 消融实验：四种干预点（仅视觉编码器、仅融合层、仅LLM、融合+LLM），三种架构（LLaVA-1.5-7B、InstructBLIP-7B、IDEFICS-9B）。
- **评估指标**：目标字符串ASR（TS）、LlamaGuard-4 ASR（LG-4）、VQA准确率、TruthfulQA准确率，以及三位标注者的人工评估（Fleiss'κ=0.77）。
- **实验数量**：4个主要实验（Exp0-3）+ 架构×方向×干预点的消融实验（12组）+ CrossInf的 \(k\) 值扫描（每架构4种配置）+ 人工评估。总计超过30组独立实验。

### 资源与算力

- **硬件**：2颗Intel Xeon Platinum 8592V CPU（共128核）、4张NVIDIA RTX PRO 6000 Blackwell Max-Q GPU（每张96GB VRAM）。每次运行使用单张GPU。
- **训练设置**：所有模型以4-bit NF4量化加载，采用QLoRA（rank=32, α=16, dropout=0.05）。训练1000次迭代，batch size=2，AdamW优化器（lr=3e-4）。
- **额外开销**：CrossInf的影响力分数计算一次约75-108秒，占训练总时间的3.8%-8.7%（文中有明确数值）。

### 实验数量与充分性

- **充分性评价**：实验覆盖双向迁移、三种代表性融合架构、四种干预点、两种ASR指标+人工验证，且进行了超参数敏感性扫描（\(k\)值），设计较为系统。
- **客观性**：人工评估采用盲法，三位标注者独立标注，κ=0.77表明高度一致。同时分析了度量间的偏差（如TS召回高但假阳性多，LG-4精确高但漏报多）。
- **不足之处**：仅与Vanilla遗忘对比，未与其他先进的参数选择方法（如SalUn、WAGLE）比较；自定义探针集规模较小（72个文本探针）；排版攻击测试仅90张图像，分类覆盖虽多但数量有限；未测试更大规模模型（如13B/70B）或不同融合设计（如Qwen-VL的早期融合）。

### 主要结论与发现

1. **迁移不对称**：文本→视觉迁移强（ASR降至<0.5%）；视觉→文本迁移弱且架构依赖（LLaVA残差13.9%，IDEFICS 9.7%，InstructBLIP仅2.8%）。
2. **架构决定转移**：融合机制是关键中介，但仅需在LLM上施加遗忘即可实现跨模态转移；融合块本身若单独更新则转移失效。
3. **浅层遗忘**：Vanilla遗忘后的VLM在排版攻击下可恢复58%-69%的有害行为，表明未真正消除知识，仅改变输出倾向。
4. **CrossInf有效**：将视觉→文本转移差距从13.9%降至1.4%（LLaVA），排版攻击ASR降至0%（LG-4指标）；效用保持良好。
5. **CKA分析**：LLaVA中文本→视觉的强转移发生在表征几乎不变的情况下（CKA=0.95），说明转移可能是边界效应而非表征重构。

### 优点

- **首创性**：首次系统性双向研究跨模态遗忘转移，填补了该领域空白。
- **高效性**：提出块级影响函数，在保持可计算性的同时提升了针对性。
- **实用价值**：CrossInf无需额外多模态遗忘数据，仅用小量跨模态探针计算一次分数，即可大幅提升安全性。
- **深入分析**：通过LoRA幅度和CKA提供可解释性，揭示了转移的机制。
- **人工验证**：使用三位标注者验证度量可靠性，并量化了自动指标的偏差。

### 不足与局限

- **模型范围有限**：仅7-9B参数规模，且融合设计未覆盖所有主流（如Qwen-VL早期融合、Gemini原生多模态）。
- **遗忘方法单一**：仅使用梯度上升+QLoRA，未比较表示重定向（如WMDP）、偏好优化等最新方法。
- **评估规模**：自定义文本探针仅72项，排版攻击图像90张，统计功效有限；未在更大型数据集上验证。
- **自动度量偏差**：LlamaGuard在遗忘后输出上假阴性率高达49.2%（表5），可能高估安全性；目标字符串召回对非标准拒绝形式漏检。
- **未讨论成本替代方案**：虽然声称为避免昂贵多模态遗忘数据，但未量化增加跨模态探针集（50条）的实际收集成本，也未与联合多模态遗忘对比。

（完）
