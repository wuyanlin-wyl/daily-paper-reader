---
title: Localization-Infused Vision-Language Semantic Fusion for Text-Guided Medical Image Segmentation
title_zh: 融合定位信息的视觉-语言语义融合用于文本引导的医学图像分割
authors: "Songyue Han, Mingye Zou, Shuchang Ye, Lei Bi, Mingyuan Meng"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16327v1"
tags: ["query:vlmmed"]
score: 10.0
evidence: 文本引导的医学图像分割与视觉语言融合
tldr: "现有文本引导医学图像分割方法未能显式利用临床报告中的目标位置信息，且融合策略单一。本文提出LoG框架，通过联合多尺度目标定位任务，实现三级定位引导的语义融合：定位引导特征融合、定位门控注意力融合和定位约束损失融合。在三个基准数据集上，LoG分别取得91.59%、80.71%和94.59%的Dice分数，显著优于现有方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16327v1/fig-001.webp\", \"caption\": \"Fig. 5. Qualitative comparison with representative medical image segmentation methods on Kvasir-SEG (rows 1–2), MosMedData+ (rows 3–4), and QaTaCOV19 (rows 5–6). The DSC score is shown below each prediction. Red, green, and blue contours denote the ground truth, predictions of LoG, and predictions of the compared methods, respectively.\", \"page\": 11, \"index\": 1, \"width\": 1056, \"height\": 737}, {\"url\": \"assets/figures/arxiv/2607.16327v1/fig-002.webp\", \"caption\": \"Fig. 6. Visualization of the input image, ground truth (GT), localization map, LGAF gating mask, and segmentation prediction from left to right. Green and blue contours denote the ground truth and segmentation prediction, respectively.\", \"page\": 12, \"index\": 2, \"width\": 871, \"height\": 1065}, {\"url\": \"assets/figures/arxiv/2607.16327v1/fig-003.webp\", \"caption\": \"TABLE I QUANTITATIVE COMPARISON BETWEEN LOG AND EXISTING METHODS FOR MEDICAL IMAGE SEGMENTATION\", \"page\": 9, \"index\": 3, \"width\": 732, \"height\": 565}]"
motivation: 现有文本引导分割方法未显式捕获目标位置信息且缺乏多级融合策略。
method: 提出LoG框架，通过三级定位融合（特征、注意力、损失）显式注入位置语义。
result: "在QaTa-COV19、MosMedData+、Kvasir-SEG上Dice分别达91.59%、80.71%、94.59%，优于SOTA。"
conclusion: LoG通过显式定位指导有效提升了文本引导医学图像分割性能。
---

## 摘要
医学图像分割对于现代计算机辅助医学至关重要。近年来，文本引导的分割通过将临床医生撰写的文本报告作为图像分割的语义指导，展现出潜力。这些报告描述了目标的外观、位置和邻近解剖结构，为定位和描绘提供了明确指导。现有的文本引导分割方法通常通过预训练文本编码器隐式提取文本语义，然后通过直接的图像-文本特征融合来整合视觉-语言语义。然而，这些方法未能显式捕获文本报告中嵌入的目标导向信息，特别是目标位置，也未探索超越基础特征融合的多层次信息融合策略，限制了关键文本语义的提取与整合。在本研究中，我们提出了LoG，一种融合定位信息的视觉-语言融合框架，用于文本引导的医学图像分割。通过联合执行多尺度目标定位任务，LoG显式捕获目标导向的视觉-语言语义，并实现三个层次的定位融合语义融合：(i) 定位引导的特征融合，直接将位置相关语义注入视觉特征；(ii) 定位门控注意力融合，重新引导多尺度定位预测以强化关键区域；(iii) 定位约束的损失融合，基于与目标定位的空间一致性监督分割。在三个基准数据集（包括三种医学成像模态及其配对的文本报告）上的大量实验表明，LoG在QaTa-COV19、MosMedData+和Kvasir-SEG上分别取得了91.59%、80.71%和94.59%的Dice分数，持续优于最先进的医学图像分割方法。

## Abstract
Medical image segmentation is essential for modern computer-aided medicine. Recently, text-guided segmentation has shown promise by incorporating clinician-formulated textual reports as semantic guidance for image segmentation. These reports describe target appearance, location, and neighboring anatomy, providing explicit guidance for localization and delineation. Existing text-guided segmentation methods typically extract textual semantics implicitly through a pretrained text encoder and then integrate vision-language semantics via straightforward image-text feature fusion. However, these methods do not explicitly capture target-oriented information embedded in textual reports, particularly target location, and do not explore multi-level information fusion strategies beyond basic feature-level fusion, limiting the extraction and integration of critical textual semantics. In this study, we propose LoG, a localization-infused vision-language fusion framework for text-guided medical image segmentation. By jointly performing multi-scale target localization tasks, LoG explicitly captures target-oriented vision-language semantics and enables three-level localization-infused semantic fusion: (i) localization-guided feature fusion that directly infuses location-relevant semantics into visual features, (ii) localization-gated attention fusion that redirects multi-scale localization predictions to reinforce critical regions, and (iii) localization-constrained loss fusion that supervises segmentation based on spatial consistency with target localization. Extensive experiments on three benchmark datasets, involving three medical imaging modalities with paired textual reports, demonstrate that LoG achieves Dice scores of 91.59%, 80.71%, and 94.59% on QaTa-COV19, MosMedData+, and Kvasir-SEG, respectively, consistently outperforming state-of-the-art medical image segmentation methods.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有文本引导的医学图像分割方法存在两个主要局限：
  - 隐式提取文本语义，未显式捕获临床报告中关于目标位置的关键信息，导致定位线索被稀释；
  - 融合策略单一，多局限于特征级融合（如交叉注意力），缺乏多级融合（如门控调制、损失约束）的综合利用。
- **背景**：临床报告包含外观、位置、邻近解剖等明确描述，能为分割提供强先验；但现有方法无法有效将位置信息转化为像素级指导。
- **整体含义**：本文提出一种目标导向的多级融合范式，通过显式定位任务提取视觉-语言语义，并注入三个层次（特征、注意力、损失）来提升分割精度。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将文本引导分割重新定义为“定位导向的语义提取与融合”问题，通过联合多尺度定位预测，同时获得隐式特征空间和显式预测空间的语义，并实施三级融合。
- **框架结构**（LoG）：
  1. **视觉编码器**：ConvNeXt-Tiny提取四个尺度特征（1/4~1/32），另加两个CNN-MLP混合层保留高分辨细节。
  2. **文本编码器**：BiomedBERT提取词级文本特征。
  3. **定位导向语义提取**：在四个尺度上，文本特征与视觉特征通过双向交叉注意力交互，生成：
     - 特征空间语义 Z_s（隐含位置信息的融合特征）
     - 预测空间语义 ˆL_s（patch级定位图，表示目标出现概率）
     - 同时用图像到文本交叉注意力逐步更新文本特征，实现粗到细交互。
  4. **三级融合**：
     - **LGFF（定位引导特征融合）**：将Z_s通过交叉注意力注入原始视觉特征，得到定位引导特征 ˜F_s^X。
     - **LGAF（定位门控注意力融合）**：多尺度定位图 ˆL_s 经卷积、上采样、融合后生成空间门控掩码 M_s，逐元素调制 ˜F_s^X 得到 ¯F_s^X。
     - **LCLF（定位约束损失融合）**：总损失包含分割损失（Dice+Focal+CE）、定位损失（BCE）、定位-分割一致性损失（约束分割前景落在定位区域内）。
  5. **分割解码器**：基于定位门控特征金字塔及高分辨视觉特征，通过6级解码产生最终分割掩码及4个中间预测。
- **数学表示**：总损失 L_total = L_seg(Y, ˆY) + λ_seg Σ L_seg(Y_s, ˆY_s) + λ_loc Σ L_loc(L_s, ˆL_s) + λ_con Σ L_con(ˆY_s, ˜L_s)。

## 3. 实验设计

- **数据集**：三个公开基准，覆盖三种成像模态：
  - QaTa-COV19（X光，COVID-19感染区域，9258张）
  - MosMedData+（CT，肺部感染，2729张）
  - Kvasir-SEG（内镜，息肉，1000张）
  - 所有数据集均配有配对文本报告（由Li et al.标注）。
- **评估指标**：Dice Similarity Coefficient (DSC) 和 mean Intersection over Union (mIoU)。
- **对比方法**：分为三类：
  - **图像only**：U-Net、U-Net++、Attention U-Net、Swin-UNet、nnU-Net。
  - **视觉-语言预训练**：CLIP、GLoRIA、BiomedCLIP、MedSAM。
  - **文本引导**：LViT、Ariadne、MAdaptor、FMISeg、EviVLM、RecLMIS、TeViA、ViTexNet、PCSA-Seg。
- **数据划分**：各数据集按官方或文献标准划分训练/验证/测试，如QaTa-COV19（80%训练，20%验证），MosMedData+（2183/273/273），Kvasir-SEG（8:1:1）。

## 4. 资源与算力

- **硬件**：单块NVIDIA RTX 4090 GPU（24GB显存）。
- **训练配置**：Adam优化器，batch size 32，训练100 epochs。
- **未明确说明**：论文未给出总训练时长、有无多卡并行或具体推理速度等细节。

## 5. 实验数量与充分性

- **主要对比**：在三个数据集上报告了DSC和mIoU，与14种方法进行公平比较（部分引用官方结果，部分自行复现）。
- **消融实验**（Table II-V）：
  - 组件逐步添加（Baseline→+LGFF→+LGAF→+LCLF）。
  - LGFF内部消融（去掉更新文本特征、去掉特征融合、去掉定位预测）。
  - LGAF内部消融（单尺度定位、无定位信息、去掉LGAF）。
  - LCLF内部消融（逐尺度移除一致性损失）。
- **敏感性分析**（Table VI）：调整定位patch大小P_s，测试不同粒度影响。
- **文本截断分析**（Table VII）：逐步缩短文本输入长度（16/12/8/4 tokens，以及无文本）。
- **可视化**（Fig.5-6）：定性对比分割结果、定位图、门控掩码。
- **客观性**：实验设计较为全面，数据集多样（三种模态），对比方法覆盖主流基线，消融验证了每个组件的贡献；但缺乏在不同随机种子或多次重复下的统计显著性检验。

## 6. 主要结论与发现

- LoG在三个数据集上均取得最佳DSC和mIoU（如QaTa-COV19 91.59%，MosMedData+ 80.71%，Kvasir-SEG 94.59%）。
- 文本引导方法整体优于图像only和预训练方法，证明文本信息的有效性。
- 三级融合中，LGFF贡献最大（+2.17%平均DSC），LGAF(+0.95%)和LCLF(+0.69%)进一步改进。
- 定位预测是整个框架的核心基础（移除后性能下降3.44%）。
- 文本截断至8 token时性能开始下降，无文本时下降约3%，表明文本信息对定位融合至关重要。
- 定位patch大小对性能不敏感，默认设置平衡了精度与效率。

## 7. 优点

- **方法论亮点**：
  - 首次将显式多尺度定位任务引入文本引导分割，实现定位导向的语义提取。
  - 创新性的三级融合策略（特征、门控、损失），超越了传统单一特征级融合。
  - 定位-分割一致性损失新颖且有效，与定位单独监督协同防止平凡解。
- **实验设计亮点**：
  - 在三个不同模态的基准上验证，结果具有良好泛化性。
  - 消融实验细致，清晰证明每个组件的贡献。
  - 敏感性分析和文本截断分析增强了结论的鲁棒性。

## 8. 不足与局限

- **依赖配对文本**：推理时必须提供完整临床报告；部分场景下文本可能缺失或与图像不一致，影响性能。
- **报告结构化假设**：当前实验基于结构化报告（三句固定模板）；真实临床报告更多样化、可能含噪声，鲁棒性待验证。
- **仅限2D**：实验局限于2D图像（X光、CT切片、内镜图像），未扩展到3D体积分割（如完整CT序列）。
- **领域覆盖有限**：仅三个数据集（感染、息肉），未涉及更多解剖结构（如器官、肿瘤）或模态（MRI、超声）。
- **计算效率**：未报告推理速度或参数量，多尺度定位预测与多次交叉注意力可能带来额外开销。
- **统计检验**：未报告多次重复实验的均值和方差，无法评估结果稳定性。

（完）
