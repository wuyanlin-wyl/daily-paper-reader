---
title: "BUS: Brain-Inspired Unsupervised Self-Reflection for Advanced Multimodal Reasoning"
title_zh: BUS：受脑启发的无监督自我反思用于高级多模态推理
authors: "Jiacheng Yang, Tongying Xiao, Yunkai Dang, Cong Wang, Yuekun Yang, Qi Fan, Wenbin Li, Feng Miao, Yang Gao"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07361v1"
tags: ["query:vl"]
score: 9.0
evidence: 脑启发VLM自我反思
tldr: 当前视觉-语言模型处理复杂视觉任务时推理能力不足，现有监督方法依赖大量标注数据且缺乏显式反思行为。受人类大脑反向预测能力启发，本文提出无监督自反思框架BUS，通过让模型进行反向预测并提供无标签学习信号来增强推理。BUS兼容SFT和RL，在8个基准上显著提升性能，验证了反向预测对推理的关键作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07361v1/fig-001.webp\", \"caption\": \"Figure 1: Backward prediction process in question-answering scenarios. To answer the given question, the brain predicts which events are likely to precede an image that possibly contains a cat.\", \"page\": 1, \"index\": 1, \"width\": 466, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2607.07361v1/fig-002.webp\", \"caption\": \"Table 2: Comparison with competitive methods on popular general visual benchmarks. Bold indicates the best results.\", \"page\": 6, \"index\": 2, \"width\": 1052, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2607.07361v1/fig-003.webp\", \"caption\": \"Figure 5: Visualization results of Qwen3-VL-8B and our BUS-8B on a constructed backward-prediction question.\", \"page\": 7, \"index\": 3, \"width\": 1046, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2607.07361v1/fig-004.webp\", \"caption\": \"Figure 2: VLMs perform backward prediction. (a) Task environment. States are represented by images, and darker arrows denote higher state-to-state transition probabilities. The experiment begins with a learning phase in which all models are presented with state transitions. (b) Decision phase. Rewards are placed in three states to dissociate backward prediction and forward prediction. (c) Evidence of backward prediction. Among these models, at least 65% of the choices are consistent with backward prediction.\", \"page\": 3, \"index\": 4, \"width\": 1036, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2607.07361v1/fig-005.webp\", \"caption\": \"Figure 3: BUS Framework. BUS achieves effective label-free training through self-verification of reflective reasoning. In Stage I, we generate multiple reasoning–answer pairs through repeated sampling. In Stage II, we guide the model to perform brain-inspired backward prediction and fine-tune it on unlabeled data, removing the need for annotated datasets.\", \"page\": 4, \"index\": 5, \"width\": 1046, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2607.07361v1/fig-006.webp\", \"caption\": \"Table 1: Comparison with competitive methods on popular high-resolution visual benchmarks. Bold indicates the best results.\", \"page\": 5, \"index\": 6, \"width\": 1052, \"height\": 609}]"
motivation: 现有监督方法依赖大量标注数据且测试时缺乏显式反思行为，受神经科学中大脑反向预测能力启发，旨在实现无监督的自反思推理增强。
method: 提出脑启发无监督自反思框架BUS，通过让视觉-语言模型进行反向预测（从未来状态推测当前状态），并在无标签数据上提供显式学习信号，无需人工标注即可训练反思能力。
result: 在8个复杂视觉任务基准上，BUS显著优于基座模型，且兼容监督微调和强化学习等流行方法，仅使用无标签数据即取得明显提升。
conclusion: 实验验证了反向预测能力对视觉-语言模型推理的关键作用，BUS为无监督增强多模态推理提供了有效且无需标注的解决方案。
---

## 摘要
当前视觉语言模型（VLM）在处理需要一致且细粒度推理的复杂视觉任务时常常遇到困难。近期方法旨在训练模型以促进自我反思推理，即审查和改进生成的推理。然而，这些方法需要大量标注数据，且在测试时缺乏明确的反思行为。本研究旨在通过神经科学的启发弥合这一差距。人脑表现出高效的反向预测，即预测当前哪些状态可能先于给定的未来状态。在这项工作中，我们首先验证了主流VLM可以像人脑一样进行反向预测。然后，我们提出了受脑启发的无监督自我反思（BUS），一种无需标签的训练框架，以增强具有挑战性的图像分析中的反思推理能力。BUS使VLM能够进行反向预测，并在无真实标签的数据上提供明确的学习信号。通过这种方式，BUS消除了对标注数据的依赖，同时提高了推理性能。值得注意的是，BUS与流行的微调方法（如监督微调（SFT）和强化学习（RL））兼容。最后，在8个基准上的广泛实验证明了BUS在各种复杂视觉任务上的有效性。它在仅使用无标签训练数据的情况下，相较于基础模型取得了显著的改进。我们的实验发现验证了反向预测能力对VLM推理至关重要。

## Abstract
Current Vision-Language Models (VLMs) often struggle to handle complex visual tasks that require consistent and fine-grained reasoning. Recent methods aim to train models to facilitate self-reflective reasoning, i.e., reviewing and improving the generated reasoning. However, they require large volumes of annotated data and lack explicit reflective behavior during test time. This work aims to bridge this gap through inspiration from neuroscience. The human brain exhibits efficient backward prediction, i.e., predicting which current states are likely to precede a given future state. In this work, we first verify that mainstream VLMs can perform backward prediction, similar to the human brain. Then, we propose Brain-inspired Unsupervised Self-reflection (BUS), a label-free training framework to enhance reflective reasoning capability in challenging image analysis. BUS enables VLMs to perform backward prediction and provide explicit learning signals on data without ground-truth labels. In this way, BUS eliminates reliance on annotated data while improving reasoning performance. Notably, BUS is compatible with popular fine-tuning methods, such as Supervised Fine-Tuning (SFT) and Reinforcement Learning (RL). Finally, extensive experiments on 8 benchmarks demonstrate the effectiveness of BUS across a wide range of complex visual tasks. It achieves notable improvements over the base model while using only unlabeled training data. Our experimental findings validate that backward prediction capability is critical for VLM reasoning.

---

## 论文详细总结（自动生成）

# 论文详细总结：BUS: Brain-Inspired Unsupervised Self-Reflection for Advanced Multimodal Reasoning

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有视觉-语言模型（VLM）在复杂视觉任务（如高分辨率图像问答、细粒度推理）中，常产生不一致、不可靠的推理路径，导致性能下降。  
- **现有方法局限**：  
  - **监督自我反思方法**（如 MIRROR、V-Reflection）需要大规模人工标注的反思数据集，成本高昂。  
  - **测试时缺乏显式反思行为**：模型在测试时仍以“前向预测”模式直接生成答案，没有明显的自我反思过程。  
- **神经科学启发**：人脑不仅进行前向预测（从当前状态预测未来），还进行**反向预测**（从未来状态推测当前状态），这在决策中起到关键的自反思作用。  
- **本文目标**：验证 VLM 是否具备反向预测能力，并利用该能力实现**无监督的自我反思训练**，消除对标注数据的依赖，同时提升推理性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过让 VLM 进行反向预测（即根据已给出的答案，预测哪些推理路径可能导致该答案），从而在无标签数据上产生显式学习信号，实现自我反思训练。  
- **框架：Brain-inspired Unsupervised Self-reflection (BUS)**  
  - **阶段 I：重复采样**  
    - 对每个输入 \( x_{I\&T} \)（图像+问题），通过前向预测采样 \( n \) 个推理-答案对 \( \{(y_i, a_i)\}_{i=1}^n \)。  
    - 将相同答案归为同一类别 \( \{c_j\} \)。  
  - **阶段 II：无标签训练**  
    - 对每个答案类别 \( c_j \)，构造新输入：  
      > 原问题：`[x_{I\&T}]`，模型答案为：`[c_j]`，以下哪个推理能导致该答案？选项：`[y_1, ..., y_n]`  
    - 引导模型进行反向预测：输出选择的推理路径子集 \( a' \)。  
    - **学习信号**：真实答案 \( a'_g \) 即为先前采样的、属于该类别的推理路径集合（无需外部标签）。通过比较 \( a' \) 与 \( a'_g \) 计算损失或奖励。  
- **兼容的微调方法**：  
  - **BUS-SFT**：直接最大化正确推理路径的对数似然。  
  - **BUS-GRPO（RL）**：采用 Group Relative Policy Optimization，奖励函数 \( R(a', a'_g) \) 基于选择正确推理路径的比例（部分正确给予部分奖励）。  
- **理论分析**：  
  - 反向预测概率可分解为 \( p_\theta(y|c_j, x) \propto p_\theta(c_j|y, x) p_\theta(y|x) \)，促使模型学习推理与答案间的逻辑一致性。  
  - 优化目标等价于最小化模型预测分布与采样分布之间的 KL 散度，从而提升反向预测的准确性。

## 3. 实验设计

- **数据集与基准（Benchmark）**：  
  - **细粒度视觉推理**：  
    - MME-RealWorld-Lite（ID，平均分辨率 2076×1434）  
    - HR-Bench-4K / 8K（OOD）  
    - V* Bench（OOD，平均分辨率 2246×1583）  
  - **通用推理**：  
    - MathVerse、MathVista、WeMath（数学推理）  
    - MMStar（通用推理）  
- **对比方法**：  
  - **开源通用模型**：InternVL3-8B、Qwen2.5-VL-7B、Qwen3-VL-8B / 4B  
  - **自我反思方法**：MIRROR、V-Reflection  
  - **监督方法**：PixelReasoner、DeepEyes、DeepEyesV2、Thyme、TreeVGR、TikArt、SIEVE  
  - **私有模型**：GPT-4o、GPT-5、GPT-5-nano、Gemini-2.5-pro、Gemini-2.5-flash  
  - **基座模型**：主要基于 Qwen3-VL-8B，同时测试了 Qwen2.5-VL-7B 和 Qwen3-VL-32B。

## 4. 资源与算力

- 论文提到使用 **Transformer Reinforcement Learning (TRL)** 框架进行分布式训练。  
- **未明确说明**具体的 GPU 型号、数量及训练时长。  
- 采样参数 \( n = 8 \)，训练数据量未公开（但强调使用无标签数据）。

## 5. 实验数量与充分性

- **实验数量**：  
  - 在 **8 个不同基准**（4 个细粒度 + 4 个通用）上进行了主要对比。  
  - 包含 **消融实验**：  
    - 训练数据影响（仅用错误答案 vs 混合正误答案）  
    - 采样数量 \( n \) 的敏感性（2, 4, 8）  
    - 模型规模扩展（8B → 32B）  
  - 预测一致性分析：比较基座模型、BUS-SFT、BUS-GRPO 在反向预测任务上的准确率。  
  - 可视化案例展示。  
- **充分性评价**：  
  - **优点**：覆盖了 ID 和 OOD 场景，模型规模多样，对比方法丰富（包括开源、监督、私有模型），消融实验较全面。  
  - **不足**：未进行更多跨模态任务（如视频、音频）的扩展；理论分析仅提供初步解释，深度有限；部分私有模型（如 GPT-5-nano）仅在部分基准上比较，未在所有基准上统一对比。

## 6. 主要结论与发现

- **核心发现**：VLM 确实具备类似人脑的反向预测能力（实验中至少 65% 的决策与反向预测一致），且该能力可通过 BUS 训练显著增强。  
- **性能提升**：  
  - MME-RealWorld-Lite：+5.8%（BUS-GRPO）  
  - HR-Bench-4K：+7.7%  
  - HR-Bench-8K：+8.0%  
  - V* Bench：+6.3%  
  - 通用推理基准：平均提升 4.7%（Qwen3-VL-8B 基础上）。  
- **无监督有效性**：训练仅使用未标注数据，却能达到甚至超越部分监督方法（如 TikArt、SIEVE）。  
- **可扩展性**：BUS 能有效提升更大模型（32B）的性能，验证了其规模适应性。  
- **消融结论**：即使只使用错误答案进行训练，BUS 仍能改善推理，说明其学习的是逻辑一致性而非单纯记忆答案；正确答案提供更强的学习信号。

## 7. 优点

- **无监督训练**：完全无需人工标注，大幅降低数据成本。  
- **显式反思**：通过反向预测在测试时实现显式的自我反思行为，区别于传统“前向”模式。  
- **兼容性**：可与 SFT 和 RL 等主流微调方法无缝结合。  
- **理论基础**：从贝叶斯角度解释了反向预测如何促进推理-答案一致性。  
- **实验全面**：涵盖多个维度（ID/OOD、不同规模、消融、对比），结果可靠。  
- **性能卓越**：在多个高分辨率视觉任务上取得显著提升，部分超越强监督方法。

## 8. 不足与局限

- **计算开销**：需要采样多个推理-答案对（\( n=8 \)），增加了训练和推理的计算成本。  
- **依赖采样多样性**：若推理路径多样性不足，可能影响反向预测的学习信号质量。  
- **模型规模限制**：仅验证了 7B/8B/32B 级别，更大模型（如 70B+）未测试。  
- **比较不完整**：部分私有模型（如 GPT-5-nano）未在所有基准上参与对比，结论泛化性受限。  
- **理论深度有限**：优化目标推导基于采样分布，未深入分析分布偏移或收敛性。  
- **任务领域局限**：实验集中在视觉问答和数学推理，未涉及更广泛的多模态任务（如视频理解、文档分析）。  
- **潜在偏差风险**：训练数据来自同一模型自身采样，可能存在自我强化偏差，影响反思的客观性。

（完）
