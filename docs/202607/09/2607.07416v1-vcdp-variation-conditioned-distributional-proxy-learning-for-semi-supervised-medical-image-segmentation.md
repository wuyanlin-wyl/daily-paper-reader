---
title: "VCDP: Variation-Conditioned Distributional Proxy Learning for Semi-Supervised Medical Image Segmentation"
title_zh: VCDP：基于变化条件的分布代理学习用于半监督医学图像分割
authors: "Zimu Zhang, Yiheng Zhong, Zhuoru Zhang, Yingzhen Hu, Yanan He, Fanliang Meng, Xiaofeng Liu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07416v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用代理学习的半监督3D医学图像分割
tldr: VCDP是一种用于半监督3D医学图像分割的可插拔训练正则化模块，通过为每个类别学习一个高斯分布和多个变异原型，并计算变异条件兼容性分数来融合全局与局部信息，引导体素嵌入对齐。该方法无需额外推理成本，在多器官分割基准上显著提升性能，尤其对小器官和高变异区域。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07416v1/fig-001.webp\", \"caption\": \"TABLE II: Per-class quantitative comparison with state-of-the-art semi-supervised segmentation methods on the Synapse multiorgan CT dataset. Light purple rows indicate methods equipped with VCDP. Dice scores are reported for 13 foreground organs. Mean Dice denotes the average Dice coefficient over all foreground classes, and HD95 denotes the 95th percentile Hausdorff distance. Best organ-wise results are highlighted in red.\", \"page\": 6, \"index\": 1, \"width\": 1064, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2607.07416v1/fig-002.webp\", \"caption\": \"Fig. 1: Motivation of the proposed VCDP framework. (a) Cotraining methods mainly enforce prediction-level consistency on unlabeled images. (b) Single-prototype methods compress each class into one deterministic representative. (c) Distribution proxy methods model class-level feature distributions but do not explicitly capture intra-class variation. (d) Our method couples distributional proxies with intra-class variation prototypes to better represent fine-grained anatomical patterns. (e) Organ-wise rankings in the Synapse dataset are shown in terms of both foreground organ size and segmentation Dice.\", \"page\": 2, \"index\": 2, \"width\": 515, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2607.07416v1/fig-003.webp\", \"caption\": \"TABLE III: Per-class quantitative comparison on the AMOS multi-organ CT dataset. Light purple rows indicate methods equipped with VCDP. Dice scores are reported for 15 foreground organs. Mean Dice denotes the average Dice coefficient over all foreground classes, and HD95 denotes the 95th percentile Hausdorff distance. Best organ-wise results are highlighted in red.\", \"page\": 7, \"index\": 3, \"width\": 1064, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2607.07416v1/fig-004.webp\", \"caption\": \"Fig. 2: Overview of the proposed VCDP framework for semi-supervised medical image segmentation.\", \"page\": 3, \"index\": 4, \"width\": 1072, \"height\": 620}, {\"url\": \"assets/figures/arxiv/2607.07416v1/fig-005.webp\", \"caption\": \"TABLE I: Quantitative comparison with state-of-the-art semi-supervised segmentation methods on Synapse (20% labeled) and AMOS (5% labeled). Light purple rows indicate methods equipped with VCDP. Gray values indicate performance changes relative to the corresponding baseline. Bold values denote better results within each baseline–Ours pair. Large gains (≥ 5) are highlighted in red. DSC: Dice coefficient; NSD: normalized surface Dice; HD95: 95th percentile Hausdorff distance.\", \"page\": 5, \"index\": 5, \"width\": 902, \"height\": 407}]"
motivation: 现有半监督方法缺乏对解剖复杂结构（特别是小器官和边界区域）的特征空间组织，无法处理较大类内变异。
method: 提出VCDP，为每个类别学习一个高斯分布和多个变异原型，通过变异条件兼容性分数融合分布相似性和软变异聚合，引导体素嵌入对齐全局和局部解剖特征。
result: 在多器官分割基准上，VCDP改进了大多数评估基线，尤其在小器官、模糊边界和高变异器官上表现优异。
conclusion: VCDP是一种有效的可插拔正则化模块，能够在无推理成本下提升半监督3D医学图像分割性能，特别是处理复杂解剖结构。
---

## 摘要
半监督三维医学图像分割通过利用未标注的体数据减少了对密集体素级标注的需求。尽管现有方法如一致性正则化、伪标签和协同训练提高了预测层面的鲁棒性，但它们通常无法为解剖结构复杂的区域（尤其是小器官和类内变异大的模糊边界区域）提供足够的特征空间组织。为解决这一问题，我们提出了基于变化条件的分布代理学习（VCDP），一种即插即用的、仅用于训练的正则化模块，用于半监督三维医学图像分割。VCDP 为每个类别表示一个可学习的高斯分布以共享类别语义，以及多个变化原型以捕捉细粒度的类内模式。进一步制定了统一的变化条件兼容性分数，融合了分布相似性和软变化聚合，引导体素嵌入与全局器官身份和局部解剖变异对齐。VCDP 在训练期间附加到解码器特征上，推理时移除，不引入额外推理成本。在多器官分割基准上的实验表明，VCDP 改进了大多数评估基线，特别是在小、模糊和高变异器官上的性能。我们的匿名代码发布在 https://anonymous.4open.science/r/VCDP_code-41ED。

## Abstract
Semi-supervised 3D medical image segmentation reduces the need for dense voxel-level annotations by exploiting unlabeled volumes. Although existing methods such as consistency regularization, pseudo-labeling, and co-training improve prediction-level robustness, they often provide insufficient feature-space organization for anatomically complex structures, especially small organs and ambiguous boundary regions with large intra-class variations. To address this issue, we propose Variation-Conditioned Distributional Proxy Learning (VCDP), a plug-and-play training-only regularization module for semi-supervised 3D medical image segmentation. VCDP represents each class with a learnable Gaussian distribution for shared class semantics and multiple variation prototypes for fine-grained intra-class patterns. A unified variation-conditioned compatibility score is further formulated to fuse distributional similarity and soft variation aggregation, guiding voxel embeddings to align with both global organ identity and local anatomical variations. VCDP is attached to decoder features during training and removed during inference, introducing no additional inference cost. Experiments on multi-organ segmentation benchmarks show that VCDP improves most evaluated baselines, particularly for small, ambiguous, and highly variable organs. Our anonymous code is released at https://anonymous.4open.science/r/VCDP_code-41ED.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，现基于您提供的论文全文，对《VCDP: Variation-Conditioned Distributional Proxy Learning for Semi-Supervised Medical Image Segmentation》进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：半监督三维医学图像分割旨在通过利用大量未标注数据，减少对昂贵且耗时的人工标注的依赖。现有主流方法（如一致性正则化、伪标签、协同训练）主要关注提升**预测层面（输出空间）的鲁棒性**。
- **核心问题**：这些方法忽视了**特征空间的组织**，导致对解剖结构复杂的器官（尤其是小器官、边界模糊区域及**类内变异大**的结构）分割效果不佳。例如，同一器官（如胰腺、食道）在不同患者或扫描条件下，其形状、大小、纹理和边界清晰度存在显著差异，传统的单一原型或代理学习难以表征这种多样性。
- **整体含义**：论文指出，要解决该问题，需要超越预测层面的一致性，深入建模特征空间，显式地捕捉和表征每个解剖类别的**全局语义**与**局部细粒度变异**，从而提升模型对复杂、变异性器官的分割能力。

### 2. 论文提出的方法论：核心思想与关键技术细节

- **核心思想**：提出 **VCDP（Variation-Conditioned Distributional Proxy Learning）**，一种即插即用、仅用于训练的正则化模块。它为每个解剖类别学习一个联合表征，该表征由一个**高斯分布代理**（捕捉共享类别语义和不确定性）和多个**变异原型**（捕捉细粒度的类内结构子模式）组成，并通过一个统一的可学习分数引导体素嵌入对齐。
- **关键技术细节**：
    1.  **变化条件类表征**：对每个类别 `c`，维护一个高斯代理均值 `μc` 和尺度 `σc`，以及 `K` 个可学习的变异原型 `{v_c,1, ..., v_c,K}`。
    2.  **分布兼容性**：通过从高斯分布 `N(μc, diag(σ²_c))` 中采样 `S` 个代表点 `u_c,s`，计算体素嵌入 `z_i` 与这组采样点的平均余弦相似度作为分布相似度 `s_dist(z_i, c)`，以建模类别层面的不确定性。
    3.  **变异兼容性**：采用 **Log-Sum-Exp** 函数，计算体素嵌入与类别 `c` 的 `K` 个变异原型的相似度，并使用温度系数 `τ` 控制软聚合的锐度，得到变异相似度 `s_var(z_i, c)`。这允许每个体素自适应地匹配最相关的类内模式。
    4.  **融合兼容性分数**：将上述两个分数加权融合，得到最终的统一兼容性分数 `g(z_i, c)`，用于衡量体素嵌入与一个类别的全局语义和局部变异的匹配程度。
    5.  **密集嵌入正则化**：基于该分数计算每个体素的软分配 `q_i(c)`。设计**对齐损失 `L_align`** 和**判别损失 `L_dis`**，以鼓励体素嵌入向高兼容性类别靠近，并增强类别间的可区分性。在此过程中，高斯均值 `μc` 的梯度被停止，防止未标注数据的噪声直接干扰类别语义中心，仅更新变异原型和体素嵌入。
    6.  **标注代理校准**：使用可用的标注数据，计算每个类别标注体素的平均特征（作为锚点），通过**校准损失 `L_cal`** 稳定高斯代理均值 `μc` 的训练，防止代理漂移。
    7.  **架构特点**：VCDP 是一个**训练时附件模块**，连接到解码器特征之后，通过投影头获取嵌入。**推理时完全移除**，不增加任何计算成本。

### 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集**：
    - **Synapse**：腹部CT数据集，包含13个前景器官（包括小器官和高变异器官如胰腺、食道等）。
    - **AMOS**：大规模腹部多器官CT数据集，包含15个前景器官。为增加挑战性，论文采用了更严格的标注比例设置。
- **实验场景**：两种极低标注比例下的半监督分割设置。
    - **Synapse**：使用**20%** 的训练数据作为有标签数据。
    - **AMOS**：使用**5%** 的训练数据作为有标签数据。
- **Benchmark & 评估指标**：使用 **Dice 系数（DSC）、归一化表面Dice（NSD）和 95% 豪斯多夫距离（HD95）** 进行评估。DSC/NSD 越高、HD95 越低，表示性能越好。
- **对比方法**：与**7种**不同类型的先进半监督分割框架进行对比，包括：
    - 基于协同训练的：**CPS**
    - 基于数据增强与重建的：**MagicNet**
    - 基于去偏和协同训练的：**DHC**
    - 基于通用半监督框架的：**GenSSL**
    - 基于平滑性和类分离的：**SS-Net**
    - 基于自适应阈值：**Adsh**
    - 基于状态空间模型的：**DCMamba**

### 4. 资源与算力

- 论文明确指出所有实验均在 **NVIDIA A40 GPU** 上运行，**批处理大小为4**。
- **未提及**具体的训练时长（如小时或轮次），也未给出总计算量估算。

### 5. 实验数量与充分性

- **实验数量**：较为充分。主要包括：
    1.  **主对比实验**：在 Synapse (20%) 和 AMOS (5%) 两个场景下，将 VCDP 集成到7个基线方法中，并报告了平均指标（DSC, NSD, HD95）。
    2.  **细粒度分析**：在 Synapse 和 AMOS 数据集上报告了**每个类别**的 Dice 分数，用于分析 VCDP 对特定困难器官（如胰腺、食道）的影响。
    3.  **定性分析**：提供了 MagicNet 和有 VCDP 加持后的视觉分割结果对比，展示了对胰腺这类小器官的改善。
    4.  **消融实验**：在 Synapse 上设计了消融实验，分别验证了高斯代理（G）、变异原型（V）以及两者组合的效果。
- **充分性与公平性**：实验设计与分析是**充分且客观公平**的。
    - **对比公平**：VCDP 作为一个插件，直接添加到现有基线方法之上，**保持了基线的所有训练目标和解码器架构不变**，仅添加了额外的辅助损失。这确保了性能提升归因于提出的方法。
    - **评估全面**：涵盖了多种主流基线和多种评估指标。
    - **分析深入**：消融实验和逐类分析有力地证明了各模块的有效性，特别是对困难器官的提升。

### 6. 论文的主要结论与发现

1.  **VCDP 显著且广泛地提升了不同半监督分割框架的性能**，尤其是在 Synapse (20% 标签) 和 AMOS (5% 标签) 这两种极具挑战性的场景下。
2.  **VCDP 的性能增益主要集中在小器官、边界模糊和类内变异大的器官上**，如胰腺、食道、小/左肾上腺等（见逐类分析表）。这验证了其通过显式建模变化来改善复杂解剖结构的有效性。
3.  **高斯代理和变异原型是互补的**。消融实验表明，两者结合（VCDP 完整版本）比单独使用其中任何一个都能带来更大的性能提升。
4.  VCDP 作为一个**即插即用且无推理成本**的模块，具有很高的实用价值。

### 7. 优点

1.  **创新性**：提出了一种新颖的特征空间正则化方法，联合建模了类别的分布不确定性（高斯代理）和细粒度类内变异（变异原型），直接针对了半监督医学分割中的关键痛点。
2.  **实用性**：即插即用、训练时附件、推理时移除，不增加任何推理成本和模型复杂度，易于集成到现有各种半监督框架中。
3.  **有效性**：在多个数据集和多种基线方法上取得了显著且一致的性能提升，特别是在处理困难器官方面，验证了其卓越的有效性。
4.  **实验设计严谨**：保持基线不变进行对比，消除了变量干扰；通过细致的消融实验和逐类分析，有力地支撑了论文的论点。

### 8. 不足与局限

1.  **跨模态与任务泛化性未验证**：实验仅局限于腹部 CT 数据集（Synapse, AMOS），未在 MRI 或其他模态（如超声、病理图像）上进行验证。此外，仅测试了多器官分割任务，其在其他分割任务（如病变分割、脑部分割）上的表现未知。
2.  **主要关注了性能提升，未评估对训练效率的影响**：论文未报告 VCDP 模块带来的额外训练时间或内存开销。虽然推理时无成本，但训练时增加复杂度（如采样过程、嵌入正则化损失计算），这部分代价未被量化。
3.  **部分超参数设置缺乏深入分析**：例如，变异原型数量 `K` 设定为5，温度系数 `τ` 等均设为固定值，未提供关于这些超参数敏感性的详细分析或搜索实验。
4.  **未提及统计显著性检验**：尽管性能提升数值显著，但未报告多次重复实验的标准差或进行统计显著性检验（如 T 检验），以证明改进是随机的还是统计显著的。
5.  **对特定基线提升不稳定**：在 AMOS 5% 标注的设置下，对于 DHC 和 DCMamba 这两种基线，Dice 系数的提升不明显甚至略有下降。这表明 VCDP 对不同的基础模型和后处理方式可能有不同的适应性，其鲁棒性在极端情况下有待进一步研究。

（完）
