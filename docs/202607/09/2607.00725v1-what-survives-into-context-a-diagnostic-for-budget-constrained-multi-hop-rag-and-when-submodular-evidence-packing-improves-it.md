---
title: "What Survives Into Context: A Diagnostic for Budget-Constrained Multi-Hop RAG and When Submodular Evidence Packing Improves It"
title_zh: 什么存留于上下文中：预算受限的多跳RAG的诊断指标以及次模证据打包何时能改进它
authors: Ananto Nayan Bala
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00725v1"
tags: ["query:vlmmed"]
score: 6.0
evidence: 预算受限上下文中的检索增强生成诊断
tldr: 本文针对预算受限的多跳RAG，指出文档召回率不是最优指标，提出answer-in-context诊断指标，能更好预测答案质量。同时将上下文构建建模为预算单调子模最大化，设计打包器联合优化相关性、查询覆盖率、代表性和多样性，在HotpotQA上以更少token取得更高F1。实验揭示其优势条件：多跳互补结构、检索命中证据、适中预算、弱阅读器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00725v1/fig-001.webp\", \"caption\": \"Figure 4: Reader-scale ladder (HotpotQA, budget 160, seeds {42, 13} pooled). The packer’s edge over the focused heuristic (blue) is positive at 3B, null at 7B, and significantly negative at 14B (∗p<0.05); the 7B fp16-vs4-bit control (hollow diamond) overlaps the fp16 point, so the trend is scale, not quantization. The edge over naive packing (red) stays significantly positive at every rung.\", \"page\": 7, \"index\": 1, \"width\": 458, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2607.00725v1/fig-002.webp\", \"caption\": \"Figure 3: HotpotQA budget sweep (seed 42; B=160 is the three-seed result). The submod−focused gap is an inverted-U, significant only at B≈160 (∆F1 +0.035, p=0.04): too tight and nothing complementary fits, too loose and the heuristic catches up. Against naive packing (band) submod wins at every budget. Per-budget F1 in Table 7.\", \"page\": 7, \"index\": 2, \"width\": 423, \"height\": 333}]"
motivation: 固定阅读器上下文预算下，标准检索指标文档召回率不能准确预测答案质量，需要更好的诊断和优化方法。
method: 提出answer-in-context诊断指标，并基于预算单调子模最大化构建上下文打包器，联合优化相关性、查询覆盖率、代表性和多样性。
result: 在HotpotQA上，answer-in-context比召回率更预测答案F1（r=0.39-0.55 vs. 0.31），打包器在160 token预算下比强基线F1提升+5.1，但优势在更强调读器下消失。
conclusion: answer-in-context是预算受限RAG的有效诊断工具，子模打包在特定条件下（多跳、检索好、预算适中、阅读器弱）显著提升性能。
---

## 摘要
在固定阅读器上下文预算下，检索增强生成（RAG）面临一个选择问题：检索到的证据中只有一部分可以展示给阅读器。我们认为文档召回——标准检索指标——在这种情境下是错误的最优化目标，并做出两项贡献。首先，作为一般性贡献，我们引入了“答案在上下文中”（answer-in-context）这一诊断指标，用于衡量黄金答案是否以连续片段的形式存在于打包后的阅读器上下文中（而非检索集合中）。它比召回率更好地预测答案F1（相关系数0.39-0.55 vs. 约0.31），将答案质量分开约五倍（在HotpotQA上0.60 vs. 0.12），并且携带了超出检索的信息：在召回率之上增加了0.17的Delta R平方，即使在所有黄金证据都被检索到的问题中，也显示出4.6倍的精确匹配（EM）差距。我们还通过干预实验证实了这一点：在2WikiMultiHopQA上，一种提高覆盖率但不提高“答案在上下文中”的打包改变并未带来准确率提升。其次，作为条件性贡献，我们将阅读器上下文构建形式化为预算单调次模最大化问题，并构建了一个打包器，联合优化相关性、查询覆盖率、代表性和多样性。在HotpotQA上，使用160个token的预算和3B阅读器，它击败了强大的聚焦启发式方法、MMR和朴素打包——在相同或更低的token成本下，F1最多提高5.1，跨三个随机种子。关键的是，我们诚实地界定了这一胜利的范围：它需要（i）多跳互补结构，（ii）检索出的证据，（iii）有约束但非极端的预算，以及（iv）阅读器足够弱，以至于证据密度（而非阅读能力）成为瓶颈。通过量化控制的阅读器规模阶梯（3B、7B、14B），我们看到相对于启发式方法的优势在7B时被吸收，并在14B时显著逆转，而诊断指标用一个单一变量解释了每个边界。

## Abstract
Retrieval-augmented generation (RAG) under a fixed reader-context budget forces a selection problem: of the evidence retrieved, only a fraction can be shown to the reader. We argue that document recall -- the standard retrieval metric -- is the wrong quantity to optimize in this regime, and we make two contributions. First, as a general contribution, we introduce answer-in-context, a diagnostic that measures whether a gold answer survives as a contiguous span in the packed reader context (not the retrieved set). It predicts answer F1 better than recall (r=0.39-0.55 vs. about 0.31), separates answer quality roughly five-fold (0.60 vs. 0.12 on HotpotQA), and carries information beyond retrieval: it adds Delta R squared=0.17 over recall and shows a 4.6x EM gap even among questions where all gold was retrieved. We also confirm it interventionally: on 2WikiMultiHopQA a packing change that raises coverage but not answer-in-context yields no accuracy gain. Second, as a conditional contribution, we cast reader-context construction as budgeted monotone submodular maximization and build a packer that jointly optimizes relevance, query coverage, representativeness, and diversity. On HotpotQA with a 160-token budget and a 3B reader it beats a strong focused heuristic, MMR, and naive packing -- by up to +5.1 F1 at equal-or-lower token cost, across three seeds. Crucially, we map the scope of this win honestly: it requires the conjunction of (i) multi-hop complementary structure, (ii) retrieval that surfaces the evidence, (iii) a binding but not extreme budget, and (iv) a reader weak enough that evidence density, not reading capacity, is the bottleneck. A quantization-controlled reader-scale ladder (3B to 7B to 14B) shows the edge over the heuristic is absorbed by 7B and significantly reverses by 14B, while the diagnostic explains every boundary with a single variable.