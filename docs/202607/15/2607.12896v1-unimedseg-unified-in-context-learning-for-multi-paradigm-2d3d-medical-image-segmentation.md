---
title: "UniMedSeg: Unified In-Context Learning for Multi-Paradigm 2D/3D Medical Image Segmentation"
title_zh: UniMedSeg：面向多范式2D/3D医学图像分割的统一上下文学习
authors: "Yunzhou Li, Jiesi Hu, Yanwu Yang, Hanyang Peng, Chenfei Ye, Jianfeng Cao, Yixuan Yuan, Ting Ma"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12896v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于Transformer和上下文学习的医学图像分割
tldr: 现有医学图像分割基础模型因提示范式和空间维度不同而碎片化，2D/3D图像及视觉、交互、语言引导分割均独立建模。本文提出UniMedSeg，基于Transformer的统一框架，将视觉示例、几何交互、语言指令和2D/3D图像映射到共享序列空间，通过解耦分裂注意力降低长序列复杂度，在27个数据集上训练，无需微调即可处理多种分割任务，达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12896v1/fig-001.webp\", \"caption\": \"TABLE III SEGMENTATION COMPARISON ACROSS ALL 16 UNSEEN TASKS IN TERMS OF DICE SCORES (%) UNDER THE 2D ICL SETTING. \\\"(R)\\\" AND \\\"(L)\\\" REFER TO RIGHT AND LEFT, RESPECTIVELY. \\\"(B)\\\" INDICATES BILATERAL EVALUATION TASKS. THE CONTEXT SET FOR ICL MODELS ARE SIZE 16.\", \"page\": 6, \"index\": 1, \"width\": 1064, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-002.webp\", \"caption\": \"TABLE II SEGMENTATION COMPARISON ACROSS ALL 16 UNSEEN TASKS IN TERMS OF DICE SCORES (%). \\\"(R)\\\" AND \\\"(L)\\\" REFER TO RIGHT AND LEFT, RESPECTIVELY. \\\"(B)\\\" INDICATES BILATERAL EVALUATION TASKS. THE CONTEXT SET FOR ICL MODELS ARE SIZE 4. 2D MODELS MARKED WITH AN * UTILIZE THE ORTHOGONAL VIEW FUSION METHOD.\", \"page\": 6, \"index\": 2, \"width\": 1064, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-003.webp\", \"caption\": \"TABLE IV INTERACTIVE SEGMENTATION COMPARISON ACROSS 12 UNSEEN TASKS IN TERMS OF DICE SCORES (%). THE TABLE EVALUATES BOTH POINT-BASED AND BOUNDING BOX-BASED INTERACTIVE PROMPTS.\", \"page\": 7, \"index\": 3, \"width\": 1064, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-004.webp\", \"caption\": \"Fig. 3. Qualitative results of ICL models on 3D segmentation tasks. For each 3D segmentation target, results are shown from two views. By utilizing green for correct overlaps (TP), red for false positive over-segmentations (FP), and blue for false negative misses (FN), this comparison plot visually highlights the overwhelming advantage of our model (on the far right), demonstrating the largest accurate green area with minimal red and blue errors compared to other baseline models.\", \"page\": 7, \"index\": 4, \"width\": 1014, \"height\": 480}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-005.webp\", \"caption\": \"Fig. 1. Overview of the UniMedSeg architecture. The model unifies diverse ICL contexts (including visual examples, interactive prompts, and language instructions) within a Transformer-Centric architecture. Specifically, when the context involves interactive prompts such as points or bounding boxes, the context image is the target image itself. Multimodal inputs are transformed into a unified one-dimensional token sequence and augmented with learnable Type Embeddings. Distinct RoPE strategies are applied. The global concatenate sequence then undergoes deep feature interaction via SplitEvaBlocks using a Decoupled Split Attention mechanism. Finally, target tokens are sliced and routed to dimension-specific decoders.\", \"page\": 3, \"index\": 5, \"width\": 968, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-006.webp\", \"caption\": \"TABLE V LANGUAGE-GUIDED 3D SEGMENTATION COMPARISON ON BRAIN STRUCTURES IN TERMS OF DICE SCORES (%). T: TEXT, I: ICL, T+I: TEXT+ICL.\", \"page\": 8, \"index\": 6, \"width\": 516, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2607.12896v1/fig-007.webp\", \"caption\": \"Fig. 2. Illustration of the Decoupled Split Attention mechanism.The target path attends globally, while the context paths are restructured into the batch dimension for parallel, isolated computation. k: Number of reference examples. Qlang: Language queries. Klang: Language keys.\", \"page\": 4, \"index\": 7, \"width\": 448, \"height\": 406}]"
motivation: 现有方法按提示范式（视觉、交互、语言）和维度（2D/3D）分离建模，无法联合学习异构标注，限制了跨范式知识迁移。
method: UniMedSeg采用Transformer架构，将多种输入统一为序列，通过解耦分裂注意力实现线性复杂度，并统一上下文学习。
result: 在27个公开数据集训练和测试，无需任务特定微调，在视觉上下文、交互和语言引导分割上均达到SOTA。
conclusion: UniMedSeg通过统一上下文学习接口有效融合异构医学监督，在多种分割范式和维度上展现强泛化能力。
---

## 摘要
医学图像分割基础模型应能泛化到各种临床场景，然而现有的通用方法仍因提示范式和空间维度而碎片化。视觉上下文学习、交互式分割和语言引导分割通常由特定范式模型处理，同时2D和3D图像也分别建模。这种隔离阻碍了异构标注和数据被单个可扩展模型联合吸收，并限制了跨范式的知识迁移。为了解决这一瓶颈，我们提出了UniMedSeg，一个以Transformer为中心的通用分割框架，将视觉示例、几何交互、语言指令以及2D/3D图像映射到共享序列空间，使得异构医学监督能够通过统一的上下文界面联合学习，无需提示或维度特定分支。为了克服视觉上下文带来的长序列内存瓶颈，我们引入了解耦分裂注意力，将注意力复杂度降低到线性，同时保持硬件友好的计算和聚焦的上下文-目标交互。通过在来自27个公共数据集精心整理的大规模语料上进行广泛训练和评估，UniMedSeg在视觉上下文、交互式和语言引导分割上均达到了最先进性能，无需任务特定的微调，在多种未见任务上展现出强大的泛化能力。代码和模型权重公开发布在 https://github.com/Lii1228/UniMedSeg

## Abstract
Medical image segmentation foundation models are expected to generalize across diverse clinical scenarios, yet existing universal methods remain fragmented by prompt paradigms and spatial dimensions. Visual in-context learning, interactive segmentation, and language-guided segmentation are typically handled by paradigm-specific models, while 2D and 3D images are also modeled separately. Such isolation prevents heterogeneous annotations and data from being jointly absorbed by a single scalable model and limits cross-paradigm knowledge transfer. To address this bottleneck, we propose UniMedSeg, a Transformer-centric universal segmentation framework that maps visual examples, geometric interactions, language instructions, and 2D/3D images into a shared sequence space, enabling heterogeneous medical supervision to be jointly learned through a unified in-context interface without prompt- or dimension-specific branches. To overcome the long-sequence memory bottleneck caused by visual contexts, we introduce Decoupled Split Attention, which reduces attention complexity to linear while preserving hardware-friendly computation and focused context-target interaction. Extensively trained and evaluated on a large corpus curated from 27 public datasets, UniMedSeg achieves state-of-the-art performance across visual in-context, interactive, and language-guided segmentation without task-specific fine-tuning, demonstrating strong generalization on diverse held-out tasks. The code and model weights are publicly available at https://github.com/Lii1228/UniMedSeg

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有医学图像分割基础模型按提示范式（视觉上下文学习、交互式分割、语言引导分割）和空间维度（2D/3D）分别建模，形成碎片化状态。这种隔离导致异构标注和数据无法被单个可扩展模型联合吸收，限制了跨范式知识迁移和模型的规模化潜力。
- **背景**：通用基础模型（如SAM）在自然图像上取得成功，但在医学领域面临标注异构（不同模态、维度、提示格式）的挑战。现有方法要么依赖范式特定的分支结构，要么将2D/3D分离建模，无法实现真正的统一。
- **目标**：提出一个单一框架，能够统一处理视觉示例、几何交互、语言指令以及2D/3D图像，实现异构监督的联合学习和跨范式/跨维度泛化。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将所有输入（图像、掩码、几何提示、语言指令）转换为统一的 token 序列，通过共享的 Transformer 骨干进行上下文学习，抛弃范式或维度特定的分支。
- **技术细节**：
  - **输入编码**：
    - 图像（目标/上下文）：通过2D或3D卷积（非重叠）进行 patch embedding，生成 token 序列。
    - 几何提示（点、框、涂鸦、套索、密集切片）：栅格化为与图像同空间尺寸的掩码，作为上下文图像-标签对的一部分。
    - 语言指令：通过 BioMedBERT 和 MLP 适配器编码为 token 序列。
    - 引入可学习的**类型嵌入**（Type Embedding）区分上下文图像、标签和目标图像；语言 token 不添加类型嵌入。
  - **位置编码**：使用独立的2D/3D 旋转位置编码（RoPE）应用于每个视觉图（上下文和目标），保证空间对应；语言 token 跳过 RoPE 以保留语义。
  - **解耦分裂注意力（Decoupled Split Attention）**：
    - 目标路径：查询（Q_tgt）与全局所有键值（K_global, V_global）进行注意力计算。
    - 上下文路径：每个上下文对（图像+标签）的查询仅与自身键值和目标键值交互，不与其他上下文交互。
    - 实现方法：通过**批处理重组**（Batch Restructuring）将上下文维度折叠进 batch 维度，利用 Flash Attention 等硬件加速核，复杂度从 O(N^2) 降至 O(N)。
  - **预测**：从 Transformer 输出中切片目标 token，经轻量级2D/3D解码器（转置卷积）重建为分割掩码。

## 3. 实验设计
- **数据与场景**：
  - 训练/验证：21个公开数据集（11,397个3D扫描），涵盖T1/T2/FLAIR/MRI/PET/CT等模态，以及脑、腹、前列腺、心脏等区域；另加20,000个合成3D体积。
  - 测试：6个未见数据集（1,361个3D扫描），包括：
    - 跨中心域偏移（脑、腹、前列腺来自不同扫描仪）
    - 未见解剖结构（鼻腔、鼻窦）
    - 跨物种（小鼠肺、胰腺）
  - 实验范式：视觉上下文学习（ICL，3D/2D）、交互式分割（点、框提示）、语言引导分割（脑结构）。
- **对比方法**：
  - ICL：SegGPT、Neuralizer、UniverSeg、Tyche（含2D和正交视图融合版本）、Neuroverse3D、Medverse。
  - 交互：SAM-Med3D、Medical SAM 2、nnInteractive、WS-ICL、MedSAM（2D逐切片）。
  - 语言：VoxTell、SegVol、BiomedParseV2、Text3DSAM、CAT。
- **评估指标**：Dice 系数，报告8次随机上下文的平均值。

## 4. 资源与算力
- **硬件**：NVIDIA V100和A800 GPU（文中未明确具体数量、型号分布和训练时长）。
- **模型规模**：
  - UniMedSeg (S)：12层，6头，嵌入维度432，总参数量27.88M（Transformer部分27M）。
  - UniMedSeg (L)：20层，12头，嵌入维度864，总参数量181.74M。
- **训练细节**：优化器 Adam，视觉模块学习率0.001，文本适配器学习率100×lr；每次训练实例按四种条件模式（仅视觉、仅交互、仅语言、混合）等概率采样；3D与2D采样概率0.65/0.35。

## 5. 实验数量与充分性
- **实验数量**：
  - ICL：在16个未见任务上分别进行3D（4个上下文）和2D（16个上下文）评估。
  - 交互：12个未见任务，分为点提示和框提示两类。
  - 语言：6个脑结构任务。
  - 消融实验：包括注意力机制（全注意力 vs. 解耦分裂注意力）、空间RoPE、类型嵌入、文本适配器、骨干（EVA vs. 标准ViT）。
  - 效率分析：不同上下文大小下的显存和推理时间。
  - 上下文大小影响（2D/3D均测试1~16/32个上下文）。
  - 提示类型适应性（雷达图：框、点、套索、涂鸦、密集切片）。
- **充分性与公平性**：
  - 对比方法覆盖了当前主流通用医学分割模型，且大部分在无微调条件下比较（除个别语言模型需微调后进行公平比较）。
  - 不同范式独立评估，消融实验验证了各组件必要性。
  - 使用了跨中心、跨器官、跨物种的零样本测试，泛化评估充分。
  - 但是未报告训练时间/GPU数量，也未见在大规模多任务联合微调场景下的对比（如医学分割挑战赛）。

## 6. 主要结论与发现
- UniMedSeg 在三种范式（ICL、交互、语言引导）和两种维度（2D/3D）上均达到 SOTA，无需任务特定微调。
- 解耦分裂注意力有效降低长序列复杂度并消除上下文间串扰，比全注意力提升性能且节省显存。
- 统一序列空间促进了跨范式协同（如文本+ICL优于单独任一模式）。
- 在跨中心域偏移、未见解剖结构、跨物种目标上表现出强泛化能力，验证了统一上下文学习的有效性。
- 上下文数量增加持续提升性能，3D模型在小样本下快速饱和，2D模型需更多上下文补偿缺失的切片信息。

## 7. 优点
- **方法创新**：突破性地将多范式输入统一为序列空间，无需分支设计；解耦分裂注意力巧妙结合线性复杂度和硬件友好计算。
- **全面统一**：同时支持2D/3D、视觉/交互/语言输入，且可任意组合（混合条件）。
- **实验扎实**：跨27个数据集的大规模训练，广泛的零样本测试，消融实验覆盖关键组件，结果清晰。
- **代码开源**：提供模型权重和完整代码，可复现性强。
- **泛化能力**：在跨中心、跨物种等困难场景下仍保持高Dice，实用价值高。

## 8. 不足与局限
- **计算资源不透明**：未明确训练所需GPU数量、时长，难以评估大规模训练成本。
- **语言引导局限**：仅对脑数据集使用语言指令，其他模态（如腹、胸）未评估文本驱动的效果。
- **2D ICL 差距**：在2D ICL中，UniMedSeg 虽然优于2D基线，但提升幅度不如3D显著，且需要较多上下文（16个）才能达到较好性能，可能受限于2D切片间信息缺失。
- **交互提示的瓶颈**：在某些复杂结构（如鼻咽、海马）的点提示下Dice仍较低（20-50%），提示稀疏性限制仍存在。
- **合成数据风险**：使用20000个合成3D体积训练，可能引入合成数据分布偏移，对真实临床泛化有一定影响。
- **与其他模型公平性**：某些基线（如SegVol、CAT）在语言任务上进行了微调，而UniMedSeg无微调，但Dice优势明显，然而若这些基线也进行多范式联合训练（如本文的混合条件），结果可能不同；缺少这种消融。
- **未见极大规模场景**：未在医学分割挑战赛（如BraTS多个子任务）的完整榜单上对比，零样本性能评估仍有提升空间。

（完）
