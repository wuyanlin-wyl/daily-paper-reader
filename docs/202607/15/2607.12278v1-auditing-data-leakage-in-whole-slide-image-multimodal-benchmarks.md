---
title: Auditing Data Leakage in Whole-Slide Image Multimodal Benchmarks
title_zh: 审计全切片图像多模态基准中的数据泄露
authors: "Wenhao Zhang, Zhongliang Zhou, John Kang, Sheng Li"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12278v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 审计医学视觉语言模型基准中的数据泄露
tldr: "最近病理视觉语言模型在WSI VQA基准测试中表现惊人，但审计发现存在严重数据泄漏：患者级（同一病例的切片跨训练/测试集）和机构级（不同病例共享染色批处理/扫描仪签名）。通过追踪TCGA标识符，发现病例重叠率达92.3%~100%，机构重叠几乎完全。这些泄漏可从基础模型特征线性解码，导致评估无法区分真实推理与检索记忆。本文提出无污染评估建议。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12278v1/fig-001.webp\", \"caption\": \"Figure 3: Excess slide-level cosine similarity over each encoder’s own cross-TSS baseline, on the shared set of 6,756 slides. For both encoders, similarity increases monotonically with hierarchical proximity (cross-TSS < intra-TSS < intra-patient), and the increase is markedly steeper for TITAN, the natively slide-level encoder. Absolute baseline similarities are 0.664 (CONCH-v1) and 0.233 (TITAN).\", \"page\": 6, \"index\": 1, \"width\": 686, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2607.12278v1/fig-002.webp\", \"caption\": \"Figure 1: Two-level data leakage in pathology vision-language models. (a) Hierarchical organization of TCGA wholeslide images. (b) Three partitioning regimes considered in this work. (c) The evaluation-time shortcut induced by either form of leakage.\", \"page\": 2, \"index\": 2, \"width\": 976, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2607.12278v1/fig-003.webp\", \"caption\": \"Figure 4: Linear probing on frozen slide embeddings for two encoders, on the shared 6,756-slide population. (a) TSS detection macro AUROC. (b) Patient ReID top-1 accuracy as a multiple of the chance baseline (absolute accuracy in parentheses). Both confounders are recoverable far above chance for both encoders, and the natively slide-level encoder (TITAN) yields the stronger leakage signal on both tasks.\", \"page\": 7, \"index\": 3, \"width\": 976, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2607.12278v1/fig-004.webp\", \"caption\": \"Figure 2: Two-panel train-test overlap matrix across pathology VLM training corpora (rows) and four TCGA-derived evaluation benchmarks (columns). (a) Case-level overlap ρcase (%). (b) TSS-level overlap ρTSS (%). TCGA-derived benchmarks are heavily contaminated at both levels. TSS-level overlap is uniformly higher than case-level overlap, revealing institutional contamination even where case-level deduplication appears successful. Non-TCGA benchmarks (BCNB, CPTAC, HistAI) trivially maintain 0% at both levels and are omitted.\", \"page\": 5, \"index\": 4, \"width\": 976, \"height\": 379}]"
motivation: 揭露当前WSI VQA基准测试因数据泄漏导致性能虚高，无法反映模型真正的多模态推理能力。
method: 通过追踪TCGA等公共资源中的切片、病例和机构标识符，量化患者级和机构级数据泄漏，并从特征空间验证泄漏的可解码性。
result: "发现TCGA衍生基准中病例重叠率92.3%~100%，机构重叠几乎完全，且泄漏与模型性能正相关。"
conclusion: 现有WSI VQA评估不可靠，必须通过改进基准构建、披露来源和自动化审计来确保无污染评测。
---

## 摘要
近期针对计算病理学的视觉语言模型在基于全切片图像的视觉问答基准上报告了惊人的零样本性能。我们审计了这些声明，发现它们在两个层级上受到数据泄露的根本性损害：患者级泄露，即同一病例的切片同时出现在训练集和测试集中；以及机构级泄露，即不同病例通过共同的组织源站点共享染色批次和扫描仪特征。通过追踪主要公共资源中的规范切片、病例和组织源站点标识符，我们记录了TCGA衍生基准中病例级训练测试重叠率达92.3%~100%，同时组织源站点重叠几乎完全。我们进一步证明，这两种泄露级别都可以从基础模型特征空间线性解码，它们在已发布检查点上导致泄露病例与审计清洁病例之间可测量的准确率差距，并且在多个已发布的WSI VLM上，最高报告准确率集中在污染最严重的基准上。因此，当前的WSI VQA评估无法区分真正的多模态推理与基于记忆的机构及患者特定伪影的最近邻检索。最后，我们概述了关于无污染评估的具体建议。通过解决基准构建、来源披露和自动化重叠审计，我们旨在引导未来研究朝着可验证的进展声明发展。

## Abstract
Recent vision-language models (VLMs) for computational pathology report striking zero-shot performance on whole-slide image (WSI) visual question answering (VQA) benchmarks. We audit these claims and find them fundamentally compromised by data leakage at two hierarchical levels: patient-level leakage, where slides from the same case appear in both training and test folds, and institutional-level leakage, where different cases nonetheless share staining-batch and scanner signatures through a common Tissue Source Site (TSS). By tracing canonical slide, case, and TSS identifiers across major public resources, we document case level train test overlaps of 92.3~100% on TCGA-derived benchmarks, together with near-complete TSS overlap. We further demonstrate that both leakage levels are linearly decodable from foundation-model feature space, that they induce a measurable accuracy gap between leaked and audit-clean cases on a published checkpoint, and that across multiple published WSI VLMs, peak reported accuracies concentrate on the most heavily contaminated benchmarks. Therefore, the current WSI VQA evaluation cannot distinguish genuine multimodal reasoning from nearest-neighbor retrieval over memorized institutional and patient-specific artifacts. Finally, we outline concrete recommendations for contamination-free evaluation. By addressing benchmark construction, provenance disclosure, and automated overlap auditing, we aim to guide future research toward verifiable claims of progress.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前针对全切片图像（WSI）的视觉语言模型（VLM）在多个公开基准（如WSI-VQA、SlideBench等）上报告了惊人的零样本性能，但作者怀疑这些性能并非来自真正的多模态推理，而可能源于**数据泄漏**。
- **研究动机**：病理VLM的训练数据和评估基准大多源自同一公共仓库——癌症基因组图谱（TCGA）。由于缺乏系统的训练-测试重叠审计，模型可能通过记忆患者级或机构级的特定伪影（如染色批次、扫描仪特征）来取得高分，而非学习到病理学通用知识。这种泄漏会严重高估模型的实际泛化能力，阻碍可信的临床转化。
- **整体含义**：本文首次系统性审计了整个WSI VLM生态系统中的训练-测试污染，证实了严重的双层泄漏（患者级和机构级），并指出当前基准无法区分真正的推理与记忆检索，呼吁改进评估实践。

### 2. 论文提出的方法论

- **核心思想**：将数据泄漏形式化定义为两个层级：
  - **患者级泄漏**：测试集切片与训练集切片来自同一TCGA病例（case）。
  - **机构级泄漏**：测试集切片与训练集切片虽来自不同病例，但属于同一组织源站点（TSS），共享实验室级染色/扫描伪影。
- **关键技术细节**：
  - 利用TCGA规范的条形码结构（TSS-病例-切片）提取唯一标识符，计算病例重叠率 \(\rho_{\text{case}} = |C_{\text{test}} \cap C_{\text{train}}| / |C_{\text{test}}|\) 和TSS重叠率 \(\rho_{\text{TSS}} = |T_{\text{test}} \cap T_{\text{train}}| / |T_{\text{test}}|\)。
  - 特征空间验证：对两种代表性的冻结编码器（CONCH-v1（patch级）和TITAN（原生幻灯片级））进行成对余弦相似度分析（intra-patient、intra-TSS、cross-TSS），并训练线性探针（逻辑回归）检测患者身份和TSS身份。
  - 内部基准分层：利用同一测试集内被污染与未污染的病例，控制任务难度，计算准确率差距。
- **公式/算法流程（文字说明）**：
  1. 收集所有训练和评估数据集的TCGA条形码，提取病例ID和TSS ID。
  2. 对每一对训练-测试集，求病例和TSS的交集大小，并按测试集总数归一化得到 \(\rho_{\text{case}}\) 和 \(\rho_{\text{TSS}}\)。
  3. 选取共享的6756张切片，提取CONCH-v1和TITAN的幻灯片级特征。
  4. 按三种配对类型计算余弦相似度，并归一化至各编码器自身cross-TSS基线。
  5. 对于线性探测，保留至少含两张切片的类别，70/30分层分割，训练逻辑回归并评估AUROC（TSS检测）和top-1准确率（患者再识别）。
  6. 在WSI-LLaVA检查点上，根据泄漏标签将SlideBench-VQA-TCGA测试集分为泄漏子集和干净子集，计算并比较各癌症类型和问题类别的准确率。

### 3. 实验设计

- **数据集/场景**：
  - **训练语料**：包含8个指令微调语料（如WSI-Bench-train、WSI-VQA-train、SlideInstruction、PathGen-1.6M、SlideInstruction+等）。
  - **评估基准**：4个TCGA衍生基准（SlideBench-VQA-TCGA、SlideBench-Cap-TCGA、WSI-VQA-test、WSI-Bench-test）以及非TCGA基准（BCNB、CPTAC、HistAI等）。
  - **特征验证**：共享的6,756张TCGA切片，覆盖CONCH-v1和TITAN两个编码器。
  - **内部准确率差距**：使用已发布的WSI-LLaVA检查点在SlideBench-VQA-TCGA（1,292张切片）上进行分层分析。
- **Benchmark**：所审计的基准均为公开的WSI VQA或caption基准，其中SlideBench-VQA-TCGA、WSI-VQA-test等被重点分析。
- **对比方法**：跨论文元分析对比了多个已发表WSI VLM（Quilt-LLaVA、WSI-VQA、SlideChat、PathGen-LLaVA、WSI-LLaVA、MLLM-HWSI等）在相同基准上的报告精度，并结合泄漏率进行比较。

### 4. 资源与算力

- **文中未明确说明使用了多少GPU型号、数量或训练时长**。本文主要进行审计分析，依赖公开的模型检查点和特征，未进行新的大规模训练。所有实验（特征提取、线性探测、模型推理）均可在单GPU或CPU上完成（具体配置未提及），因此没有算力细节可总结。

### 5. 实验数量与充分性

- **主要实验组**：
  1. **交叉数据集重叠审计**（图2）：8个训练语料×4个TCGA衍生基准，计算ρ_case和ρ_TSS。
  2. **特征空间相似性分析**（图3）：两个编码器×三种配对类型。
  3. **线性探测**（图4）：两个编码器×两种任务（TSS检测、患者ReID）。
  4. **跨论文元分析**（表1）：6个模型×2个基准，列出精度与泄漏率。
  5. **内部基准分层**（表2）：单一模型在4个子集（按癌症类型和问题类别）上的泄漏vs干净准确率差距。
- **充分性评价**：
  - 实验覆盖了从元数据层到特征层再到模型输出层的完整证据链，设计合理。
  - 消融实验（如两种编码器、不同配对类型、不同子集）增强了结论稳健性。
  - 客观性：所有数据来源（TCGA标识符、公开检查点、报告数字）均为第三方可验证，审计流程可复现。
  - 公平性：在内部分层分析中控制任务难度，在同一基准内比较，避免了跨基准难度差异的混淆。

### 6. 论文的主要结论与发现

- **主要结论**：当前WSI VLM生态系统中的零样本评估因数据泄漏而完全不可靠。TCGA衍生基准存在92.3%~100%的病例级重叠，且机构级（TSS）重叠几乎完全。
- **关键发现**：
  - 泄漏信号线性可解码：从两个不同编码器的特征空间均可高准确率恢复患者身份（TITAN达到20.3% top-1准确率，是随机基线的65倍）和TSS身份（AUROC>0.95）。
  - 内部基准中，泄漏病例比干净病例高3~8个百分点，且该差距在不同癌症类型和问题类别中一致。
  - 跨论文分析显示，报告的最高精度集中出现在污染最严重的基准上。
  - 原生幻灯片级编码器（TITAN）并未减轻泄漏，反而放大了患者特异性信号。

### 7. 优点

- **①问题意义重大**：首个系统性审计WSI VLM数据泄漏的工作，揭示了社区可能忽略的根本性评估缺陷，对病理AI领域的可信度有重要警示。
- **②方法全面**：从元数据匹配、特征相似度、线性探测、元分析到内部分层，构建了完整的证据链，论证严密。
- **③可复现性**：基于公开标识符和检查点，审计流程可自动化，代码和数据链接（arXiv提交）便于后续研究者验证。
- **④解决措施具体**：提出了三项可操作的改进方向（基准构建、来源披露、自动化审计），具有实践指导价值。
- **⑤实验客观**：控制了任务难度、编码器类型、数据集范围，结论稳健。

### 8. 不足与局限

- **①上游泄漏未量化**：审计局限于指令微调阶段，视觉编码器预训练阶段的数据泄漏因预训练语料通常不公开而无法评估。线性探针虽可作为黑盒信号，但无法定量。
- **②内部分层样本量有限**：干净的病例子集较小（如Microscopy Qs仅14个），导致准确率差距（+3~8 pp）在统计上虽方向一致，但数量级估计不够精确。
- **③依赖元数据完整性**：通过TCGA标识符匹配只能检测保留原始条码的泄漏；如果条码被剥离、重新导出或重命名，则会漏检，因此本文报告的是真实泄漏的下界。
- **④未覆盖所有基准和模型**：非TCGA基准（如BCNB、CPTAC）虽显示0%重叠，但尚未验证是否存在通过其他途径的泄漏（如共享Web来源）。另外，审计仅涵盖8个训练语料和4个TCGA衍生基准，可能不完整。
- **⑤缺乏对泄漏缓解方案的全面测试**：论文提出了建议，但未实施一个完全去污染的基准并验证模型表现，因此建议的有效性有待后续证明。
- **⑥计算资源未说明**：缺乏对实验硬件和时间的透明披露，虽不影响结论，但不利于复现细节。

（完）
