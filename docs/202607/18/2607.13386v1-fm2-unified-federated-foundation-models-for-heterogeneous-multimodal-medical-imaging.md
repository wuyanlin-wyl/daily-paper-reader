---
title: "FM$^2$: Unified Federated Foundation Models for Heterogeneous Multimodal Medical Imaging"
title_zh: FM$^2$：面向异质多模态医学影像的统一联邦基础模型
authors: "Shengchao Chen, Ting Shu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13386v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 面向异构医学影像的联邦多模态基础模型
tldr: 针对异构多模态医学影像联邦学习中的模态异质性挑战，提出FM$^2$框架，采用从头训练的核心骨干保持医学域保真度，结合双专家混合模块（类级与域级）和异构模态对齐正则化器，并通过字幕增强学习实现跨模态表示迁移。在分类、字幕学习和医学VQA任务上均优于现有联邦基线，并具有可证明的收敛性和泛化保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13386v1/fig-001.webp\", \"caption\": \"Figure 1: FM2 (single-client view). Observer extracts representative samples for training dual MoE modules. Domain-wise MoE is globally aggregated; Class-wise MoE remains local. Text inputs (diagnostic captions for CEL; questions for VQA) are processed by a local text encoder and fused with MoE-enhanced visual features, enabling all downstream tasks within a single protocol.\", \"page\": 3, \"index\": 1, \"width\": 962, \"height\": 517}, {\"url\": \"assets/figures/arxiv/2607.13386v1/fig-002.webp\", \"caption\": \"Figure 4: Convergence and feature analysis. (a) Classification convergence onMIMH-5-Domain (𝜑=0.1, 𝑟=100%); shaded bands show±1 std over 5 runs. (b) VQA convergence on Task 1 (F-CL, Non-IID, 50 rounds). (c) t-SNE on MIMH-SA without HMA: features cluster by modality. (d) With HMA (FM2): features cluster by semantic class across modalities.\", \"page\": 8, \"index\": 2, \"width\": 473, \"height\": 499}, {\"url\": \"assets/figures/arxiv/2607.13386v1/fig-003.webp\", \"caption\": \"Figure 3: Ablation and analysis across all three tasks. (a) Component ablation. (b) Hyperparameter sensitivity across different (𝛾𝑐 , 𝛾𝑑 , 𝜆) configurations. (c) Impact of caption quality on CEL. (d) Effect of partial caption availability on CEL.\", \"page\": 8, \"index\": 3, \"width\": 968, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2607.13386v1/fig-004.webp\", \"caption\": \"Figure 2:MIMHwith four configs:MIMH-5/4/3-Domain (overlapping modalities) and MIMH-SA (non-overlapping).\", \"page\": 5, \"index\": 4, \"width\": 455, \"height\": 363}]"
motivation: 现有联邦基础模型无法处理客户端间模态重叠或不重叠的异构场景，且自然图像预训练模型医学域迁移差，缺乏统一框架。
method: 提出FM$^2$，包含双混合专家模块（类级和域级）、异构模态对齐正则化器和字幕增强学习，支持从头训练核心骨干并可选集成生物医学预训练编码器。
result: 在MIMH基准和真实医学VQA数据集上，分类、字幕学习和VQA任务均显著优于最先进联邦基线，并展现出强跨模态泛化能力。
conclusion: FM$^2$有效解决了异构多模态医学影像联邦学习中的模态异质性问题，为构建统一联邦基础模型提供了可行方案。
---

## 摘要
构建医学影像基础模型需要汇集跨机构数据，但隐私法规禁止集中聚合。现有联邦基础模型要么对自然图像模型进行微调（医学领域迁移效果差），要么在单一模态内从头训练，缺乏统一各类任务的灵活性。我们识别出一个未被充分探索的挑战——影像模态异质性，其中客户端在两种结构模式下运行：重叠模式（共享模态但标签分布异质）和非重叠模式（每个客户端完全不同的模态）。我们提出FM$^2$，一个统一框架，从头训练核心骨干网络以保留医学领域保真度，同时可选地结合生物医学预训练编码器进行视觉-语言对齐。FM$^2$为每个客户端配备双混合专家模块（类别级MoE用于个性化类别知识，领域级MoE用于共享跨模态表示），并配以一个异质模态对齐（HMA）正则化器，显式对齐模态特定的专家参数，提供可证明的$O(1/\sqrt{T})$收敛性和泛化保证。FM$^2$进一步融合了字幕增强学习（CEL），其中本地保留的GPT-4o生成字幕作为文本语义桥梁，实现跨非重叠模态客户端的表示迁移，并展示了扩展到联邦医学VQA的能力。在我们MIMH基准（分类和CEL）和真实世界医学VQA数据集上的实验证实了在三个任务上均优于最先进的联邦基线，并展现了强大的跨模态泛化能力。

## Abstract
Building foundation models for medical imaging requires pooling data across institutions, yet privacy regulations prohibit centralized aggregation. Existing Federated Foundation Models either fine-tune natural-image models with poor medical-domain transfer, or train from scratch within a single modality, lacking the flexibility to unify tasks. We identify an under-explored challenge, Imaging Modality Heterogeneity, where clients operate under two structural regimes: Overlapped (shared modalities with heterogeneous label distributions) and Non-overlapped (fully disjoint modalities per client). We propose FM$^2$, a unified framework that trains the core backbone from scratch to preserve medical domain fidelity while optionally incorporating biomedical pretrained encoders for vision-language alignment. FM$^2$ equips each client with dual Mixture-of-Experts modules (a Class-wise MoE for personalized category knowledge and a Domain-wise MoE for shared cross-modality representations), coupled with a Heterogeneous Modality Alignment (HMA) regularizer that explicitly aligns modality-specific expert parameters, admitting provable $O(1/\sqrt{T})$ convergence and generalization guarantees. FM$^2$ further incorporates Caption-Enhanced Learning (CEL), where locally retained GPT-4o-generated captions serve as a textual semantic bridge enabling representation transfer across clients with disjoint modalities, and demonstrates extensibility to Federated Medical VQA. Experiments on our MIMH benchmark (classification and CEL) and real-world medical VQA datasets confirm consistent superiority over state-of-the-art federated baselines and strong out-of-modality generalization across all three tasks.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：构建医学影像基础模型需要聚合多机构数据，但隐私法规（如GDPR、HIPAA）禁止集中化。联邦学习（FL）提供了解决方案，但现有联邦基础模型面临两个主要限制：
  - **微调自然图像模型**：如ViT、CLIP、SAM，在医学域迁移效果差（医学影像的细微病变、噪声和解剖上下文需要领域原生知识）。
  - **单模态从头训练**：缺乏统一处理多模态任务的灵活性。
- **新挑战**：**影像模态异质性**（Imaging Modality Heterogeneity），客户端处于两种结构模式：
  - **重叠模式**（Overlapped）：共享模态但标签分布异质（非IID）。
  - **非重叠模式**（Non-overlapped）：每个客户端持有完全不同的模态（如CT、MRI、眼底相机等）。
- **研究动机**：现有个性化FL方法仅针对标签偏移设计，无法处理特征分布、噪声特性和语义粒度根本不同的模态异质性。亟需一个统一框架，同时支持分类、字幕学习和医学视觉问答（VQA）等任务，并处理两种模态结构。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**FM²**（Federated Medical Multimodal foundation model），一个从零训练、统一的多任务联邦框架，通过双混合专家（MoE）模块解耦个性化类别知识和共享跨模态表示，并利用字幕作为语义桥梁实现非重叠模态间的知识迁移。

- **关键技术细节**：
  1. **观察者（Observer）与Top-K采样**：
     - 每个客户端先本地训练视觉编码器 `F`，将其骨干 `F_b` 设为冻结的观察者 `V`。
     - 对每类图像，计算`Sigmoid(V(X_z))`作为代表性分数，选择Top-K个样本组成采样数据集 `D_s`。
     - 目的：克服本地标签漂移，只让最有信息的样本进入第二阶段MoE训练。

  2. **双MoE模块**：
     - **类别级MoE（CMoE）**：每个类别分配一个专家，学习类别特定的特征变换，保持个性化知识（不上传服务器）。
     - **域级MoE（DMoE）**：每个模态分配一个专家，学习跨客户端共享的域表示（全局聚合）。  
     - 最终预测：`y = γ_d·M_d(V(X)) + γ_c·M_c(V(X))`，其中γ_d+γ_c=1平衡领域共识和个性化。

  3. **异质模态对齐（HMA）正则化**：
     - 目标函数：`min Σ (n_i/n)[L_i(θ_i; D_i, D_{i,s}) + λ||M_i^d - M̂^d||^2]`，其中`M̂^d`为FedAvg聚合后的全局域级MoE。
     - 作用：防止本地域专家偏离全局共识，同时允许模态特定适应。只约束DMoE，保留CMoE完全个性化。

  4. **收敛性与泛化理论**：
     - **定理1**：在平滑性、无偏梯度、模态异质性有界假设下，FM²以O(1/√T)收敛，对齐漂移项以O(1/T)消失。
     - **命题2**：泛化误差分解为域级专家最优风险、类级专家最优风险、JS散度项和观察者近似误差，HMA和CEL分别减少JS散度和近似误差。

  5. **多模态扩展**：
     - **字幕增强学习（CEL）**：使用GPT-4o生成诊断字幕（本地保留，不泄露），通过InfoNCE对比损失对齐本地视觉特征与字幕嵌入，在非重叠模态客户端间建立共享语义桥梁，提升表示质量。
     - **联邦医学VQA**：在双MoE视觉特征上融合问题编码，通过轻量答案头进行问答，DMoE处理模态特定问题，CMoE提供类别感知推理。

#### 3. 实验设计：数据集、基准、对比方法

- **数据集与基准**：
  - **MIMH基准**（Medical Imaging Modality Heterogeneity）：从MedMNIST Plus选取5个模态（眼底、血细胞显微镜、肾脏皮质显微镜、结肠病理、皮肤镜），构建四种配置：
    - MIMH-5-Domain (10 clients, 5 modalities, 37 classes)
    - MIMH-4-Domain (10 clients, 4 modalities, 22 classes)
    - MIMH-3-Domain (10 clients, 3 modalities, 29 classes)
    - MIMH-SA (5 clients, each with unique modality, zero overlap)
    - 控制非IID严重度：Dirichlet分布 (φ ∈ {0.1, 0.3, 100})
  - **跨模态泛化**：3个保留模态数据集（PneumoniaMNIST, OCTMNIST, OrganAMNIST），仅用5%数据微调。
  - **医学VQA**：SLAKE、VQA-RAD、VQA-Med 2019/2020/2021，构造三个任务（SLAKE IID/非IID，完全模态异质8客户端，混合模态5数据集）。

- **对比方法**：
  - FL基线：FedAvg, FedProx, PerFedAvg, FedBN, FedProto, FedRep, FedPer, FedDyn, FedAdam, FedAvgM, FedYogi。
  - 联邦VLM方法：FedDAT, PromptFL, F³OCUS。
  - 不同微调策略：全微调、适配器、LayerNorm、LoRA、Bias。

#### 4. 资源与算力

- **文中明确说明**：所有MIMH实验在**NVIDIA A5500**上进行（4.1节末尾）。但未提及具体GPU数量、训练总时长或显存消耗。VQA实验未指定GPU型号。
- **通信成本分析**（表7）：FM²-Tiny每轮通信718K参数（57.4 MB），每轮耗时12.5秒；FM²-Large 1035K参数（82.8 MB），每轮18.3秒。相比FedAvg (520K, 41.6 MB, 8.2秒)，FM²以适度通信开销换取显著精度提升。

#### 5. 实验数量与充分性

- **实验数量**：非常充分，涵盖三大任务（分类、CEL、VQA），每个任务有多种子设置。
  - 分类（表1）：3种域数×3种φ×3种联合率×6基线+3种FM²规模 = 324个条件×5次运行。
  - 跨模态泛化（表2）：MIMH-SA + 3个保留数据集。
  - CEL（表3）：10种图像方法 + 7种CEL方法。
  - VQA（表4-6）：三个任务、多种微调策略、多个基线。
  - 消融与分析（图3-4）：组件消融、超参数敏感性、字幕质量与覆盖率、通信成本、收敛性、t-SNE特征可视化。
- **公平性**：所有方法使用相同骨干（ResNet-4或LLaVA-1.5），FM²的通信轮数和总训练预算与基线匹配（100轮，1个local epoch），VQA实验中所有模型采用相同的LoRA配置。结果报告5次运行均值和标准差。
- **充分性**：实验覆盖了重叠和非重叠模态、IID/非IID标签、不同客户端参与率、各种微调策略，并提供了理论分析支撑，论证充分。

#### 6. 论文的主要结论与发现

- FM²在所有三个任务（分类、CEL、VQA）上**一致优于最先进联邦基线**，平均精度增益显著（如MIMH-5-Domain硬设置下比FedRep高12-16个百分点）。
- **跨模态泛化能力突出**：在非重叠模态（MIMH-SA）上FM²大幅超越局部训练和所有基线；在三个未见模态上微调5%数据后，FM²-Large达到91%以上平均精度。
- **字幕增强学习（CEL）** 为所有方法带来提升，但FM²+CEL组合增益最大（MIMH-SA提升4.56个百分点），验证了语言作为语义桥的有效性。
- **收敛更快**：FM²在40轮内达到80%准确率（基线100轮未达到）；VQA收敛也最快且方差更小。
- **t-SNE可视化**：无HMA时特征按模态聚类；有HMA时特征按语义类别聚类，验证了对齐正则化成功合并跨模态表示。

#### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **双MoE解耦**：首次在联邦医学影像中同时处理类别异质和模态异质，个性化与共享知识互不干扰。
  - **HMA正则化**：仅约束域级MoE，避免全局漂移，且提供收敛率和泛化界（O(1/√T)）。
  - **CEL设计**：利用本地GPT-4o生成字幕作为共享语义模态，避免直接特征聚合，保护隐私同时提升非重叠模态表示。
  - **统一框架**：同一骨干可支持分类、CEL、VQA，无需任务特定重工程。
- **实验亮点**：
  - 构建了**MIMH基准**，系统模拟医学联邦中模态异质性的真实场景（重叠/非重叠、不同非IID程度）。
  - 对字幕质量和覆盖率进行消融，证明LLM生成字幕接近专家水平，即使在20%覆盖率下也有显著提升。
  - 通信成本分析展示了FM²在实际部署中的可行性。

#### 8. 不足与局限

- **计算资源受限**：所有实验仅在单一NVIDIA A5500上进行，未报告GPU数量或大规模分布式训练场景下的性能（如>50客户端），可能无法反映超大规模联邦部署中的通信瓶颈和同步开销。
- **局限性假设**：
  - DMoE的专家索引需预先通过全局模态词汇表分配，假设每个客户端已知自身模态；文中虽提到可通过在线特征聚类扩展到未知模态，但未实验验证。
  - 整体架构依赖一个共享的观察者（Observer）骨干，其容量可能成为瓶颈（近似误差ϵ_V）。虽然CEL部分降低了该误差，但未系统分析骨干大小对性能的影响。
- **隐私顾虑**：虽然CEL中字幕保留本地不泄露，但使用了GPT-4o API（第三方）生成字幕，可能涉及数据离开本地（即使通过API处理），隐私风险需谨慎评估。作者提到是“本地保留”但实际生成过程依赖外部模型。
- **医学VQA实验规模有限**：VQA任务数据量较小（SLAKE约700+图像，VQA-RAD约300+），且对比的联邦VLM方法（FedDAT, PromptFL, F³OCUS）均在同一主干上重实现，但缺乏与更大规模VQA数据集（如MIMIC-CXR）的对比。
- **未涉及多模态输入缺失**：论文假设所有客户端视觉模态是完整的，未考虑部分客户端某模态缺失的情况。
- **理论假设的间隙**：定理假设损失函数是L-平滑且梯度噪声独立，实际医学影像数据可能违反这些假设（如高噪声、长尾分布），理论边界可能过于乐观。

（完）
