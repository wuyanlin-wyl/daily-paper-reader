---
title: Bi-Level Collaborative Learning for Few-Shot Scribble-Supervised Medical Image Segmentation
title_zh: 双级别协作学习用于少样本涂鸦监督医学图像分割
authors: "Xiang-Xiang Su, Yufan Ye, Yihang Zheng, Min Gan, Guang-Yong Chen"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25432v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 少样本涂鸦监督医学图像分割
tldr: 本文针对医学图像分割中涂鸦标注样本稀少且监督稀疏的双重挑战，提出了一种双层协作学习框架。上层可学习超像素模型提供区域结构先验，通过超像素区域伪标签传播和空间先验引导滤波生成可靠伪标签指导下层分割；下层分割模型学习到的解剖语义反馈给上层，驱动其学习与分割任务更一致的区域结构表示。上下层双向交互和协作学习在ACDC和Prostate数据集上显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25432v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative comparison of Dice and HD95 with baseline methods on the ACDC test set under the setting of five scribble-annotated training cases. The best and second-best results are highlighted in red and blue, respectively.\", \"page\": 7, \"index\": 1, \"width\": 1066, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2607.25432v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison of our method with state-of-the-art methods on the ACDC and Prostate test sets under the setting of five scribble-annotated training cases.\", \"page\": 7, \"index\": 2, \"width\": 1056, \"height\": 492}, {\"url\": \"assets/figures/arxiv/2607.25432v1/fig-003.webp\", \"caption\": \"Figure 2: An overview of the proposed BiSCL framework.\", \"page\": 3, \"index\": 3, \"width\": 1041, \"height\": 325}]"
motivation: 现有涂鸦监督方法在标注样本稀缺时面临结构恢复和边界描绘困难。
method: 提出双层协作学习框架，上层超像素模型提供先验并生成伪标签，下层分割模型反馈语义信息，通过双向交互优化。
result: 在ACDC和Prostate数据集上的少样本涂鸦监督设置下显著超越现有方法。
conclusion: 双层协作学习有效缓解了稀疏监督和样本稀缺问题，提升了分割性能。
---

## 摘要
涂鸦标注为医学图像分割提供了一种替代昂贵像素级标注的高效方式，然而在真实临床场景中，涂鸦标注样本往往仍然有限，这带来了稀疏监督和标注样本稀缺的双重挑战。这些复合约束严重剥夺了模型进行完整区域恢复和精确边界描绘所需的结构证据。为了突破这一瓶颈，我们提出了一种用于少样本涂鸦监督医学图像分割的双级别协作学习框架。具体而言，引入上层的可学习超像素模型为下层分割提供区域结构先验，同时执行基于超像素的区域级伪标签传播和空间先验引导的过滤策略，为分割学习生成可靠的密集伪标签。同时，下层分割模型在当前超像素指导下学到的解剖语义反馈回上层，进一步驱动其学习更符合分割任务的区域结构表示。通过上下层之间的双向交互和协作学习，所提出的框架在少样本涂鸦监督设置下，在ACDC和Prostate数据集上显著优于现有的最先进涂鸦监督方法。

## Abstract
Scribble annotations offer an efficient alternative to costly pixel-wise labeling for medical image segmentation, yet in real clinical scenarios, scribble-annotated samples are often still limited, imposing the dual challenges of sparse supervision and annotated sample scarcity. These compounded constraints severely deprive models of the structural evidence needed for complete region recovery and precise boundary delineation. To break this bottleneck, we propose a bi-level collaborative learning framework for few-shot scribble-supervised medical image segmentation. Specifically, an upper-level learnable superpixel model is introduced to provide region-structural priors for lower-level segmentation, while superpixel-based region-wise pseudo-label propagation and a spatial-prior-guided filtering strategy are performed to generate reliable dense pseudo-labels for segmentation learning. Meanwhile, the anatomical semantics learned by the lower-level segmentation model under the guidance of the current superpixels are fed back to the upper level, further driving it to learn region-structural representations better aligned with the segmentation task. Through bidirectional interaction and collaborative learning between the upper and lower levels, the proposed framework significantly outperforms existing state-of-the-art scribble-supervised methods on the ACDC and Prostate datasets under the few-shot scribble-supervised setting.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究背景**：医学图像分割需要像素级标注，成本高昂。涂鸦标注仅需少量笔画，是一种高效的弱监督形式。然而，在真实临床场景中，涂鸦标注样本往往仍然有限，导致模型同时面临**稀疏监督**（只有少量像素有标签）和**标注样本稀缺**（仅几个病例有涂鸦）的双重挑战。
- **现有局限**：现有涂鸦监督方法通常假设有大量涂鸦样本可用，以学习解剖先验来补偿稀疏性。当标注样本极少时，模型无法形成稳定的解剖表征，容易出现前景-背景混淆、区域恢复不完整、边界描绘不准确等问题。
- **本文目标**：突破该瓶颈，提出一种**双级别协作学习框架（BiSCL）**，通过可学习的超像素模型提供结构先验，并与分割模型双向交互，在少样本涂鸦设定下实现可靠的分割。

## 2. 方法论

**核心思想**：将超像素作为**可学习的区域级别结构先验**，上层超像素网络为下层分割提供伪标签扩散的基础，下层分割模型学到的解剖语义反馈回上层，使超像素逐步对齐分割目标。上下层通过双向协作共同优化。

**关键技术细节**：

- **上层：可学习超像素网络（SSN）**：使用差分超像素采样网络，输出像素-超像素软关联矩阵。初始化采用**中心密集、边缘稀疏**的非均匀种子策略，使更多超像素集中在潜在目标区域。
- **下层：分割网络（U-Net）**：采用Mean Teacher半监督框架，利用教师模型生成高置信伪标签，然后通过超像素进行区域级伪标签传播：每个超像素内，若存在高置信前景种子，则整个超像素被赋予该种子类别的多数标签。
- **空间先导自适应过滤**：为防止错误扩散，对每个超像素计算**扩张比**（伪标签像素数/种子像素数），并根据超像素质心到图像中心的距离设定可容忍的最大扩张比。若超像素扩张比超过阈值，则丢弃其扩散结果。该机制抑制了远离中心的背景误扩散。
- **双向语义对齐**：下层通过组合的交叉熵和Dice损失进行分割学习（Lseg = LpCE + λ(t)Lpseudo）；上层通过**低层特征重建损失**（保持边界）和**语义重建损失**（重构教师伪标签和涂鸦标签）来优化超像素，使其结构分区更符合分割语义。
- **双层优化训练策略**：
  1. 分别预训练分割模型（Mean Teacher）和超像素模型（无语义反馈）5,000步。
  2. 正式协作训练：先固定上层参数，优化下层参数 N_l=50步；然后固定下层参数，优化上层参数 N_u=5步；交替迭代至总步数20,000。
  3. 上层更新时忽略隐式梯度项（近似优化），以降低计算复杂度。

## 3. 实验设计

**数据集**：
- **ACDC**：心脏MRI，分割目标为左心室（LV）、心肌（MYO）和右心室（RV）。使用官方涂鸦标注，70/15/15划分。**少样本设定**：仅取前5个训练病例的涂鸦标注作为有标签数据，其余65个训练病例视为无标签。
- **Prostate**：前列腺T2-MRI，分割目标为中央腺（CG）和外周带（PZ）。使用已有涂鸦标注，55训练/10验证/15测试。**少样本设定**：仅保留前5个训练病例的涂鸦标签。

**基准方法（共12种）**：
- 基础：PCE（部分交叉熵）
- 数据增强：Cutout、CutMix、CycleMix
- 半监督：CPS、MT
- 涂鸦监督：DMPLS、SC-Net、ScribbleVC、ScribbleVS、ScribFormer、QMaxViT-Unet+
- 所有含伪标签的基线均评估“仅涂鸦”和“半监督”两种设置，取较优结果。

**评价指标**：Dice相似系数（↑）、95% Hausdorff距离HD95（mm，↓）。

## 4. 资源与算力

- **GPU**：单张 NVIDIA RTX A6000。
- **框架**：PyTorch。
- **训练迭代**：总步数20,000，其中预训练5,000步。
- **批量大小**：12张切片（6有标签+6无标签）。
- **优化器**：AdamW，初始学习率1e-3，余弦退火。
- 论文**未提及**总训练时间或GPU小时数，仅说明单卡训练。

## 5. 实验数量与充分性

- **主实验**：在ACDC和Prostate两个数据集上对比12种方法，报告平均Dice和HD95及标准差。每个数据集均进行5次实验？论文未明确重复次数，但给出了均值±标准差，表明有多次运行。
- **消融实验**：在ACDC上进行了4组对比，验证了每个组件（超像素传播、空间先导过滤、双层训练）的贡献。
- **超参数敏感性分析**：对空间先导过滤的两个参数（σ、γ_center）各取3个值进行测试，显示一定鲁棒性。
- **定性可视化**：图3展示多方法分割结果对比；图4展示超像素在协作训练前后的变化；图5展示消融变体视觉效果。
- **公平性**：对基线中涉及伪标签的方法同时评估了“仅涂鸦”和“半监督”设置并取最好结果；所有方法均采用相同的少样本训练病例。实验比较客观、充分。

## 6. 主要结论与发现

- 在ACDC上，BiSCL取得**Mean Dice 0.828**，比第二名QMaxViT-Unet+（0.776）高出5.2个百分点；**Mean HD95 5.57 mm**，远优于对手（21.01 mm）。
- 在Prostate上，**Mean Dice 0.534** 比最佳基线ScribbleVC（0.478）提高5.6个百分点；**Mean HD95 16.09 mm** 大幅下降（ScribbleVC为32.19 mm）。
- 消融实验表明：
  - 单纯加入超像素传播反而降低性能（由于噪声放大），
  - 加入空间先导过滤后性能显著提升（Dice从0.311→0.774），
  - 再加入双层协作训练达到最优（0.828）。
- 超像素经过语义反馈后，分区更对齐解剖结构，证明了双向交互的有效性。

## 7. 优点

- **问题定位精准**：直面涂鸦监督在少样本场景下失效的痛点，提出针对性的双层协作框架。
- **方法创新性强**：将超像素从静态先验变为可学习的、与分割任务动态对齐的结构先验；双向反馈机制新颖且有效。
- **设计细腻**：空间先导自适应过滤策略巧妙利用了目标位于中心的先验，抑制错误扩散；非均匀超像素初始化也考虑了领域知识。
- **实验充分**：涵盖两个不同模态数据集、多种基线、消融和参数分析，定量定性证据一致，验证了方法鲁棒性。
- **计算效率合理**：单卡A6000即可训练，未依赖大规模算力。

## 8. 不足与局限

- **领域覆盖有限**：仅测试了心脏和前列腺MRI，未在CT或其他解剖部位（如肝脏、肿瘤）上验证，推广性未知。
- **样本量小**：少样本设定下仅5个涂鸦案例，但训练集仍有大量无标签数据（65或50例），并非严格意义上的“零标签”场景。更极端（如1-shot）下的表现未探索。
- **超参数敏感**：空间先导过滤的γ_center和σ需要手动设定，且论文仅分析了两个参数，其他超参数（如N_l, N_u, λ权重）的敏感性未讨论。
- **双层优化近似**：忽略隐式梯度项可能带来偏差，论文虽引用相关工作表示在实践中有效，但未分析该近似对最终性能的影响程度。
- **模型复杂度**：同时维护分割网络和超像素网络，且需要预训练和交替更新，训练流程相对复杂，复现难度增加。
- **未报告代码开源**：论文未提及代码是否公开，可复现性存疑。

（完）
