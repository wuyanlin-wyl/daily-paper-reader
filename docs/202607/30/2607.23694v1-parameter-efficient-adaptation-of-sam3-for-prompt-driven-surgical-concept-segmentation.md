---
title: Parameter-Efficient Adaptation of SAM3 for Prompt-Driven Surgical Concept Segmentation
title_zh: SAM3的参数高效适配用于提示驱动的手术概念分割
authors: "Changjing Liu, Yiming Huang, Beilei Cui, Liangjing Shao, Long Bai, Yanheng Li, Haoxuan Che, Hongliang Ren"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23694v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: SAM3的参数高效适配用于外科手术分割
tldr: "针对手术分割中SAM3因域差距导致精度下降及全参数微调计算成本高的问题，本文提出参数高效的LoRA适配方法，在提示编码器、检测器和跟踪器中注入低秩适配器，冻结视觉骨干，仅优化0.98%参数，支持单GPU训练。实验表明该方法在手术概念分割上优于零样本SAM3和主流基线，分割结果可直接用于下游手术场景重建与物理仿真。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23694v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative comparison of our method and other baselines on representative frames from CholecSeg8k and EndoVis18.\", \"page\": 7, \"index\": 1, \"width\": 725, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-002.webp\", \"caption\": \"Fig. 2. Efficient adaptation of SAM3 for surgical segmentation via LoRA. The backbone remains frozen; only 0.98% of parameters (low-rank adapters on the prompt encoder, detector, and tracker) are trainable.\", \"page\": 3, \"index\": 2, \"width\": 725, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-003.webp\", \"caption\": \"Fig. 4. Per-class segmentation results on CholecSeg8k, EndoVis18 and CaDISv2 test splits. Left: mean IoU (%); Right: mean Dice (%). Axes cover a curated subset of tissue and instrument categories to streamline visual interpretation.\", \"page\": 8, \"index\": 3, \"width\": 652, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-004.webp\", \"caption\": \"Fig. 5. From segmentation to downstream applications. Left: masks predicted by our method supply region-level tissue labels to assign unique physical material parameters in surgical simulation. Right: Applying the same force on tissues with different stiffness (e.g. soft gallbladder vs. stiff liver) produces visibly different deformation.\", \"page\": 9, \"index\": 4, \"width\": 725, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-005.webp\", \"caption\": \"Table 1. Quantative results on the CholecSeg8K [11] dataset. We compare\", \"page\": 5, \"index\": 5, \"width\": 732, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-006.webp\", \"caption\": \"Table 2. Quantative results on the EndoVis18 [1] dataset. We highlighted\", \"page\": 5, \"index\": 6, \"width\": 724, \"height\": 325}]"
motivation: 解决SAM3在手术数据上的域差距以及现有全参数微调方法计算效率低的问题。
method: "在SAM3的提示编码器、检测器和跟踪器中注入低秩适配器，冻结视觉骨干，仅优化0.98%参数。"
result: 在手术概念分割任务上一致优于零样本SAM3和主流基线，且分割结果可直接部署到下游手术场景重建与仿真。
conclusion: 提出的参数高效LoRA适配方法有效提升了手术概念分割性能，同时大幅降低了计算资源需求。
---

## 摘要
高效的手术分割能够赋能临床诊断、术中监测以及下游的机器人重建和模拟流程。尽管像Segment Anything Model 3 (SAM3)这样的提示驱动基础模型在自然图像上取得了强大的分割性能，但手术数据与其预训练数据之间存在领域差距，导致分割精度下降。此外，现有的医学SAM方法需要全参数微调，导致计算消耗大、效率低。为了解决这些限制，本工作提出了一种参数高效的SAM3低秩适配（LoRA）方法用于手术概念分割。我们向提示编码器、检测器和跟踪器注入低秩适配器，同时完全冻结视觉主干，仅优化总模型参数的0.98%，并支持在单个消费级GPU上进行训练。综合实验表明，我们的方法持续优于零样本SAM3和其他主流基线，生成的分割结果可以直接部署以支持下游机器人手术场景重建和物理仿真流程。

## Abstract
Efficient surgical segmentation empowers clinical diagnosis, intraoperative monitoring, and downstream robotic pipelines for reconstruction and simulation. Although prompt-driven foundation models like Segment Anything Model 3 (SAM3) achieve strong segmentation performance on natural images, surgical data exhibits domain gaps against its pre-training data, resulting in degraded segmentation accuracy. Furthermore, existing medical SAM methods require full-parameter fine-tuning, incurring heavy computational consumption and low efficiency. To address these limitations, this work proposes a parameter-efficient Low-Rank Adaptation (LoRA) adaptation of SAM3 for surgical concept segmentation. We inject low-rank adapters into the prompt encoder, detector and tracker while fully freezing the vision backbone, which only optimizes 0.98% of the total model parameters and supports training on a single consumer GPU. Comprehensive experiments demonstrate that our method consistently outperforms zero-shot SAM3 and other mainstream baselines, and the generated segmentation results can be directly deployed to support downstream robotic surgical scene reconstruction and physical simulation pipelines.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：手术图像概念分割对于临床诊断、术中监测和下游机器人手术（如场景重建、物理仿真）至关重要。尽管 SAM3 等提示驱动基础模型在自然图像上表现出色，但手术数据与预训练数据存在显著领域差距，直接零样本推理精度不足。同时，现有医学 SAM 方法（如 MedSAM、Medical SAM3）需要全参数微调，消耗高达 80 GB GPU 内存，难以在临床常用消费级 GPU 上部署。
- **整体含义**：本文旨在提出一种参数高效的 SAM3 适配方法，通过极少的可训练参数（0.98%）和较低的内存开销（9 GB），实现对手术概念分割的高效迁移，并验证其在下游重建与仿真中的应用价值。

## 2. 方法论

- **核心思想**：利用低秩适配（LoRA）对 SAM3 进行参数高效微调，仅更新提示编码器、检测器和跟踪器中的低秩矩阵，完全冻结视觉骨干（Vision Backbone）。
- **关键技术细节**：
  - **LoRA 注入**：对于每个冻结的线性层 \(W_0 \in \mathbb{R}^{d \times k}\)，添加低秩分解 \(W = W_0 + BA\)，其中 \(B \in \mathbb{R}^{d \times r}\)，\(A \in \mathbb{R}^{r \times k}\)，秩 \(r \ll \min(d, k)\)，仅优化 \(A\) 和 \(B\)。
  - **适配组件**：提示编码器（交叉模态对齐层）、DETR 解码器（所有注意力投影 q/k/v/out_proj）和跟踪器（跟踪注意力层），视觉编码器保持冻结。
  - **统一输入表示**：将不同数据集的原始类别标签映射到统一概念词汇表 \(V\)，支持跨数据集共享权重，避免每数据集单独微调。
  - **训练配置**：LoRA 超参数 rank=16，alpha=32，dropout=0.1；使用 bf16 混合精度，batch size=1，梯度累积 8；优化器 AdamW（lr=1e-4，余弦退火，200 warmup steps），权重衰减 0.01，梯度裁剪 1.0，训练 10 轮，选取验证集最高 mIoU 的 checkpoint。

## 3. 实验设计

- **数据集**：
  - **CholecSeg8k**（腹腔镜胆囊切除术）：4 个保留视频作为测试集，包含 Liver、Grasper、L-hook Electrocautery 等类别。
  - **EndoVis18**（肾切除术）：2 个测试序列，包含 instrument-wrist、kidney-parenchyma 等。
  - **CaDISv2**（白内障手术）：3 个测试视频，不含详细类别列表，但用于整体评估。
- **基准方法**：CLIP、SurgVLP、CAT-Seg、SAM3（零样本）、Medical SAM3（2D 版和 3D 版，均全参数微调）。
- **评估指标**：平均交并比（mIoU）和 Dice 系数，按类别计算后平均。
- **实验充分性**：
  - 在三个涵盖不同手术类型、不同组织/器械类别的数据集上评估。
  - 每数据集包含多个测试序列/视频，并报告每个类别的 mIoU（见表 1、表 2 和图 4）。
  - 提供定性对比（图 3）和下游任务展示（图 5）。
  - **不足**：缺少消融实验（如不同 LoRA 秩/位置、不同冻结策略、有无统一输入表示等），未评估推理速度或模型参数量对比。

## 4. 资源与算力

- **GPU 型号**：单张 NVIDIA RTX 3090（消费级 GPU）。
- **训练内存峰值**：9 GB（相比全参数微调的约 80 GB 降低 80%+）。
- **训练时长**：未明确说明总训练时间，但提到训练 10 epochs，采用梯度累积 8，每次迭代处理 8 张图像（由于 batch size=1 且梯度累积 8）。
- **可训练参数量**：8.32 M，占 SAM3 总参数（849 M）的 0.98%。

## 5. 实验数量与充分性

- **实验数量**：主要包含 3 个数据集上的定量对比（表 1、表 2、图 4 中的总体与分类别结果），以及定性对比（图 3）。无额外的消融实验或敏感性分析。
- **充分性评估**：
  - **优点**：覆盖三种不同手术场景（胆囊、肾、白内障），对比方法包括零样本、全参数微调的基线，且报告了每个类别的细粒度指标，相对公平。
  - **不足**：缺乏以下验证：
    - 未比较不同 LoRA 配置（如 rank 4/8/32）的影响。
    - 未分析视觉骨干是否真的不需微调（例如微调骨干的收益或风险）。
    - 未评估推理速度或内存占用（仅报告训练内存）。
    - 未讨论文本提示的不同措辞对结果的影响。
  - **结论**：实验设计基本合理，但可进一步通过消融实验增强说服力。

## 6. 主要结论与发现

- 提出的 LoRA-adapted SAM3 在所有三个手术数据集上均一致优于零样本 SAM3 和全参数微调的 Medical SAM3（2D/3D），尤其对细小手术器械（如 Grasper、L-hook Electrocautery）提升显著。
- 仅训练 0.98% 参数即可取得领先性能，训练内存仅 9 GB，适合资源受限的临床环境。
- 分割结果可直接用于高斯泼溅场景重建和基于 MPM 的物理仿真，生成符合不同组织刚度（如软胆囊 vs. 硬肝脏）的形变效果，表明分割质量足以支持下游任务。

## 7. 优点

- **参数效率极高**：仅 0.98% 参数可训练，是已发表方法中最轻量的手术分割适配方案之一。
- **低内存可部署**：峰值 9 GB，可在单张 RTX 3090 上完成训练，显著降低硬件门槛。
- **统一输入表示**：跨数据集共享权重，避免每数据集独立微调，降低工程开销。
- **方法简洁有效**：直接适配预训练基础模型，无需修改骨干或设计复杂结构，具备良好可复现性。
- **下游应用验证**：将分割结果接入重建与仿真管线，展示实际临床机器人场景的可用性。

## 8. 不足与局限

- **缺少消融实验**：未分析不同 LoRA 超参数（秩、适配位置、是否冻结骨干）的影响，也未比较不同 PEFT 方法（如 Adapter、Prefix Tuning）。
- **仅评估图像级分割**：当前方法基于单帧，未考虑视频时序信息（尽管 SAM3 本身支持视频跟踪，但本文仅用图像帧训练和评估）。
- **评估指标单一**：仅报告 mIoU/Dice，未包含 F1、Hausdorff Distance 等边缘精度指标，也未报告推理速度或 FLOPs。
- **数据集覆盖有限**：仅三个手术数据集，且类别数不多（如 CaDISv2 仅作整体评估）。未在更广泛医学图像（如 CT、MRI）上验证。
- **潜在偏差风险**：测试集划分仅固定几个视频，可能不足以代表数据集整体分布；未进行交叉验证。
- **下游应用展示有限**：重建与仿真仅定性示例，未定量评估分割误差对仿真结果的影响。
- **未探讨少样本或零样本泛化**：实验设置中所有类别都有训练样本，未验证模型泛化到未见类别或仅有少量标注的情况。

（完）
