---
title: "TextGaze: Prompting Gaze Target Estimation with Textual Scene Cues"
title_zh: TextGaze：利用文本场景线索提示注视目标估计
authors: "Junhui She, Fei Wang, Kun Li, Yiqi Nie, Yuxin Liu, Zhangling Duan, Xun Yang"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10130v1"
tags: ["query:vl"]
score: 9.0
evidence: 利用大型视觉语言模型进行注视估计
tldr: 注视目标估计存在多分支方法高标注负担与简化设计忽视注视意图的矛盾。TextGaze提出统一跨模态架构，利用大型视觉语言模型(LVLM)作为可扩展语义引导，通过冻结编码器提取视觉特征，LVLM获取文本线索，设计transformer融合模块与层级文本监督，联合预测注视热图和内外状态。在四个数据集上表现竞争力，跨数据集泛化良好，无需额外微调，凸显LVLM作为辅助指导的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10130v1/fig-001.webp\", \"caption\": \"Figure 1: Pipelines for gaze target estimation. (a) Multi-branch methods Chen et al. [2021] suffer from slow convergence due to increased structural complexity. (b) Prompt-tuning method Ryan et al. [2025] encounter semantic defocus as they struggle to isolate taskrelevant saliency via positional prompts. By contrast, our method (c) leverages accessible scene-level text to achieve precise semantic focus on the gaze target.\", \"page\": 1, \"index\": 1, \"width\": 508, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative results on the GazeFollow, VideoAttention-Target (fine-tuned), and ChildPlay/GOO-Real (without fine-tuning) datasets. The input image on the left and the predicted heatmaps on the right. Our method shows consistent accuracy on different scenes.\", \"page\": 6, \"index\": 2, \"width\": 1052, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-003.webp\", \"caption\": \"Figure 5: Qualitative comparison on the GazeFollow dataset. Compared to GazeLLE, our method demonstrates its superiority in three representative scenarios.\", \"page\": 7, \"index\": 3, \"width\": 508, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the proposed TextGaze. The diagram illustrates the end-to-end pipeline, including visual feature extraction via frozen DINOv2, text feature processing with pre-trained BERT, cross-modal fusion through a 3-layer fusion transformer, and final prediction heads for gaze heatmap and in/out status.\", \"page\": 4, \"index\": 4, \"width\": 1052, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2607.10130v1/fig-005.webp\", \"caption\": \"Table 1: Gaze target estimation results on GazeFollow and VideoAttentionTarget. Inputs: I (image), D (depth), E (eyes), P (pose), O (objects), and T (text). The best results are in bold and the second-best are underlined. ∗Results reproduced by us from the official code releases.\", \"page\": 5, \"index\": 5, \"width\": 1044, \"height\": 485}]"
motivation: 现有方法要么多分支需额外监督、阻碍迁移，要么简化设计忽略真实注视意图，导致预测注意力与实际目标错位。
method: TextGaze采用冻结视觉编码器提取特征，LVLM生成注视对齐文本线索，通过transformer融合模块和层级文本监督实现热图与状态联合预测。
result: 在四个主流数据集上取得竞争性结果，且无需额外微调即可跨数据集泛化。
conclusion: 提供了一种简化替代范式，并展示了LVLM作为可访问辅助指导在注视估计中的潜力。
---

## 摘要
注视目标估计旨在推断一个人视线在场景中的位置。在主流设计逻辑中，多分支方法需要额外的监督和标注，而流线型设计则优先考虑低层视觉显著性而非真实的注视意图。前者导致标注负担高并阻碍领域迁移，后者则造成预测注意力与实际注视目标之间的错位。为了解决这个问题，我们提出TextGaze，一种统一的跨模态架构，利用大型视觉-语言模型（LVLM）作为可扩展的语义指导来平衡这两种设计范式。该模型从冻结编码器中提取视觉特征，并利用LVLM获取与注视对齐的文本线索。我们设计了一个基于Transformer的融合模块，具有层级文本监督以保持任务语义。轻量级解码头能够联合预测注视热图和帧内/帧外状态。我们在四个主流数据集上评估了我们的方法，结果显示在关键指标上具有竞争力的性能，并且无需额外微调即可实现稳健的跨数据集泛化。总的来说，我们为传统设计提供了一种流线型替代方案，并突出了LVLM作为注视估计可访问辅助指导的潜力。

## Abstract
Gaze target estimation aims to infer the position of a person's gaze within a scene. Within mainstream design logic, multi-branch methods require extra supervision and annotations, while streamlined designs prioritize low-level visual saliency over true gaze intent. The former leads to a high annotation burden and hinders domain transfer, whereas the latter causes misalignment between predicted attention and actual gaze targets. To address this issue, we propose TextGaze, a unified cross-modal architecture that leverages a Large Vision-Language Model (LVLM) as scalable semantic guidance to balance the two design paradigms. The model extracts visual features from a frozen encoder and utilizes an LVLM to obtain gaze-aligned textual cues. We design a transformer-based fusion module with hierarchical text supervision to preserve task semantics. Lightweight decoding heads enable the joint prediction of gaze heatmaps and in-/out-of-frame status. We evaluate our method on four mainstream datasets, and the results show competitive performance across key metrics with robust cross-dataset generalisation without extra fine-tuning. Overall, we provide a streamlined alternative to traditional designs and highlight the potential of LVLMs as accessible auxiliary guidance for gaze estimation.

---

## 论文详细总结（自动生成）

# TextGaze: 利用文本场景线索提示注视目标估计 — 详细分析

## 1. 论文的核心问题与整体含义

- **研究动机**：注视目标估计（gaze target estimation）的核心挑战在于现有方法存在两种对立范式。多分支方法（multi-branch）依赖深度、姿态、眼睛等额外监督信号，导致标注成本高、收敛慢、域迁移困难；而流线型方法（如 prompt-tuning）虽然简化结构，但过度依赖低层视觉显著性（如空间邻近性、视觉突兀性），忽略真正的注视意图，造成 **语义失焦**（semantic defocus）——预测的热图与人的实际注视目标错位。
- **整体含义**：本文认为注视本质上是一种意图级推理，而非单纯的几何回归。作者提出利用大型视觉-语言模型（LVLM）作为可扩展、易获取的语义辅助，在不引入额外标注和多分支复杂度的前提下，为模型注入高层语义先验，从而平衡两种设计范式的矛盾。

## 2. 论文提出的方法论

### 核心思想
构建一个统一的跨模态架构 **TextGaze**，它由三个核心模块组成：
1. **LVLM 文本线索模块**：利用冻结的 LVLM（Qwen2.5-VL-7B）结合精心设计的提示模板（描述目标人物外观、动作、注视方向，但不命名注视对象以避免幻觉），生成结构化文本描述，再通过冻结的 BERT 编码为文本嵌入序列。
2. **特征标记序列处理模块**：视觉流使用冻结的 DINOv2 提取特征图，通过可学习的头部标记（head token embedding）对头部区域进行增强，并加入 2D 正弦位置编码形成视觉标记序列；文本流使用可学习位置编码增强。
3. **跨模态融合与预测模块**：将视觉与文本标记拼接后输入三层的融合 Transformer。为了保持任务相关文本语义在深层融合中的一致性，设计 **层级文本特征一致性监督**（每层对文本子序列进行线性投影，与原始 BERT 输出计算 MSE 损失）。最后，视觉标记段经卷积解码器输出注视热图，可选的 [CLS] 标记经 MLP 输出帧内/帧外概率。

### 关键技术细节
- **提示设计**：统一系统提示 + 用户提示，指定头部边界框，要求用单数人称描述外观、动作和相对于自身的注视方向（不提及具体物体）。
- **冻结骨干**：DINOv2（ViT-B/L） + BERT-base-uncased 均冻结，仅训练融合模块与解码头。
- **层级文本监督**：对每一层融合后提取文本标记，投影到 BERT 隐藏维度，计算 MSE 损失并平均作为辅助损失项（权重 λ₂=0.1）。
- **总损失**：\( \mathcal{L}_{\text{all}} = \mathcal{L}_{\text{heat}} + \lambda_1 \mathcal{L}_{\text{in/out}} + \lambda_2 \mathcal{L}_{\text{text}} \)，其中热图损失为像素级二元交叉熵（高斯目标 σ=3），λ₁=1。

## 3. 实验设计

### 数据集与场景
- **微调（fine-tune）**：GazeFollow（117,727 训练图像）、VideoAttentionTarget（58,507 训练帧）
- **跨数据集测试（无需微调）**：ChildPlay（儿童行为）、GOO-Real（零售场景）
- **评估协议**：AUC（热图 ROC 曲线）、像素级 L2 距离（Avg L2 / Min L2）、帧内/帧外平均精度 AP

### 对比方法
- **多分支方法**：Chong et al.、Chen et al.、Bao et al.、Gupta et al.、Miao et al.、Liu et al.、Tafasca et al. 等（使用深度/姿态/眼部等额外输入）
- **流线型方法**：Ryan et al. (GazeLLE) 基于 DINOv2 ViT-B/L，作者复现其官方代码进行公平对比

### 实验结果概述
- **GazeFollow（表 1）**：TextGaze（ViT-L）获得最低 Avg L2（0.099），AUC 达到 0.956，与 SOTA 持平或略优。
- **VideoAttentionTarget（表 1）**：在 AUC、L2、AP in/out 三项指标上全面超越所有对比方法（包含 GazeLLE）。
- **GOO-Real（表 2）**：跨数据集表现最佳，AUC 0.918，L2 0.159。
- **ChildPlay（表 3）**：AUC 和 L2 略低于 GazeLLE（ViT-L），但 AP 达到 0.996，为最高。

## 4. 资源与算力

论文 **未明确说明** 使用的 GPU 型号、数量或训练时长。仅提及优化器为 AdamW，初始学习率 1e-4，权重衰减 1e-2，输入图像尺寸 448×448，热图尺寸 64×64。这给复现带来一定不确定性，但通过公开代码可推测其计算需求应与 GazeLLE 相近（单卡/多卡 8GB+ 显存可能足够）。

## 5. 实验数量与充分性

- **主实验**：两个基准数据集（GazeFollow、VideoAttentionTarget）上对比了 15+ 方法。
- **跨数据集泛化实验**：两个未见基准（ChildPlay、GOO-Real）上测试，验证泛化能力。
- **消融实验**：
  - 融合策略：加法融合、交叉注意力、Transformer 融合（最优）
  - 关键组件：文本监督、视觉残差连接、视觉 2D 位置编码（三者同时启用最优）
- **定性分析**：展示三个代表性场景（多人、遮挡、框外误判）下相比 GazeLLE 的优势。

**充分性与公平性**：
- 消融实验覆盖了设计决策的核心点，组件逐一验证。
- 与 SOTA（GazeLLE）使用同一骨干（DINOv2 ViT-B/L）并复现代码，保证了公平对比。
- 跨数据集测试未做任何微调或提示修改，凸显鲁棒性。
- 缺点：未进行统计显著性测试；仅对比了数值指标，缺少运行时间/效率对比。

## 6. 论文的主要结论与发现

1. **LVLM 文本线索有效弥补语义缺失**：在多人、遮挡、外观模糊等场景中，文本描述提供的高层语义先验能引导模型关注真正注视目标，缓解语义失焦。
2. **层级文本监督防止语义漂移**：在深层融合中保持文本特征的稳定，避免任务相关语义被视觉特征稀释。
3. **统一框架兼顾简洁与性能**：无需深度/姿态等多分支额外标注，仅用图像+文本即可达到或超越 SOTA，且跨数据集泛化能力强（尤其 AP 指标优势明显）。
4. **LVLM 作为可访问的辅助工具**：展示了无需额外训练即可利用预训练 LVLM 为零样本注视估计提供指导的潜力。

## 7. 优点

- **创新性**：首次将 LVLM 生成的文本场景线索系统性地引入注视目标估计，将任务从几何回归提升为语义驱动的意图识别。
- **架构简洁**：冻结双骨干 + 轻量融合网络，避免多分支复杂度和额外标注成本，易于部署和迁移。
- **鲁棒性强**：跨数据集测试表现优异，尤其帧内/帧外分类（AP）指标提升显著，显示对场景变化的适应力。
- **消融充分**：组件逐一验证，揭示了融合策略、文本监督、位置编码的各自贡献，设计合理。
- **可复现**：提供代码仓库和详细提示设计，有利于后续研究。

## 8. 不足与局限

- **计算资源未披露**：缺少 GPU 型号、数量、训练时长等信息，复现成本不明确。
- **LVLM 依赖与偏差风险**：
  - 文本质量高度依赖 LVLM 的零样本能力，若 LVLM 产生幻觉或描述不准确（尤其框外目标），可能引入错误引导。
  - 提示模板固定，对极端场景（如不同文化、视角变化）的适应能力未验证。
- **实验覆盖不完整**：
  - 未在更多领域（如工业检测、人机交互）测试泛化。
  - 未进行实时性分析（LVLM 推理延迟较大）。
  - 跨数据集测试中 ChildPlay 的 AUC/L2 并非最优，说明部分场景下仍有提升空间。
- **细节缺失**：文本监督权重 λ₂ 设为 0.1，但未给出调参依据或灵敏度分析。
- **潜在偏差风险**：LVLM 训练数据可能包含带有性别/种族偏见的 gaze 模式，但论文未讨论公平性问题。

（完）
