---
title: "VocaDet: Sample-Driven Open-Vocabulary Object Detection and Segmentation via Visual Tokenization and Vector Database Retrieval"
title_zh: "VocaDet: 基于视觉令牌化和向量数据库检索的样本驱动开放词汇目标检测与分割"
authors: ZhiXin Sun
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08541v1"
tags: ["query:vlmmed"]
score: 7.0
evidence: 基于VLM的开放词汇分割
tldr: VocaDet提出一种基于样本驱动的开放词汇目标检测与分割框架，无需模型重训练，通过视觉标记化和向量数据库检索实现任意目标识别。它利用DINOv3提取特征，经凝聚聚类生成多粒度视觉token，并存储为可扩展物体记忆。推理时匹配查询图像token进行定位分割，辅以背景过滤减少冗余。在UA-DETRAC上验证了高效性和可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08541v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed VocaDet framework. Given an input image, DINOv3 is first used to extract dense visual features, which are discretized into visual words through agglomerative clustering. A background filtering layer removes irrelevant visual words, while the remaining foreground visual words are retrieved against the vector database for object matching. The matched visual clusters are then converted into bounding boxes, followed by Non-Maximum Suppression (NMS) to obtain the final detection results.\", \"page\": 3, \"index\": 1, \"width\": 826, \"height\": 244}]"
motivation: 现有开放词汇检测依赖文本提示、有限视觉示例或昂贵特征匹配，难以扩展到持续增长的目标库。
method: 将连续视觉特征离散化为多粒度视觉token，通过位置去偏和空间拓扑信息存入向量数据库，推理时检索匹配实现检测分割。
result: 在UA-DETRAC数据集上无需常规检测器训练即实现有效的开放词汇检测，且支持随样本累积持续扩展识别能力。
conclusion: VocaDet通过样本驱动和向量数据库检索，实现了无需重训练的可扩展开放词汇目标检测与分割，验证了方法的有效性和实用性。
---

## 摘要
开放词汇目标检测与分割旨在识别超出预定义类别的任意物体。尽管最近的视觉语言和基于参考的方法显著推动了该领域的发展，但它们通常依赖文本提示、有限的视觉示例或昂贵的特征匹配过程，难以扩展到大型且持续扩展的物体库。在这项工作中，我们提出了VocaDet，一种样本驱动的开放词汇目标检测与分割框架，它无需模型重新训练，直接从用户提供的正负样本集合中学习物体概念。关键思想是将连续的视觉表示转化为离散的视觉词汇，并通过可扩展的向量数据库进行高效的基于检索的识别。具体而言，我们采用DINOv3作为视觉特征提取器，并应用具有自适应聚类敏感性的凝聚聚类生成多粒度视觉令牌。这些视觉令牌与去偏位置表示和空间拓扑信息一起，作为可扩展的物体记忆存储在向量数据库中。在推理时，查询图像被转换为视觉令牌，并与存储的物体记忆进行高效匹配以实现目标定位和分割。此外，引入背景过滤机制以移除频繁出现的背景模式，减少在实际固定摄像机场景中的冗余检索操作。在UA-DETRAC数据集上的实验表明，VocaDet在无需传统检测器训练的情况下实现了有效的开放词汇检测性能，同时随着更多正负样本的积累，支持持续扩展的识别能力。

## Abstract
Open-vocabulary object detection and segmentation aim to recognize arbitrary objects beyond predefined categories. Although recent vision-language and reference-based approaches have significantly advanced this field, they often rely on text prompts, limited visual examples, or expensive feature matching procedures, making them difficult to scale to large and continuously expanding object repositories. In this work, we propose VocaDet, a sample-driven open-vocabulary object detection and segmentation framework that learns object concepts directly from user-provided positive and negative sample collections without model retraining. The key idea is to transform continuous visual representations into discrete visual vocabularies and perform efficient retrieval-based recognition through a scalable vector database. Specifically, we employ DINOv3 as the visual feature extractor and apply agglomerative clustering with adaptive clustering sensitivity to generate multi-granularity visual tokens. These visual tokens, together with position-debiased representations and spatial topology information, are stored as expandable object memories in a vector database. During inference, query images are converted into visual tokens and efficiently matched against the stored object memories for object localization and segmentation. Furthermore, a background filtering mechanism is introduced to remove frequently occurring background patterns and reduce redundant retrieval operations in practical fixed-camera scenarios. Experiments on the UA-DETRAC dataset demonstrate that VocaDet achieves effective open-vocabulary detection performance without conventional detector training, while supporting continuously expandable recognition capability as additional positive and negative samples are accumulated.