---
title: "Multimodal Routing for Interpretable, Robust, and Auditable Clinical Prediction"
title_zh: 面向可解释、鲁棒且可审计的临床预测的多模态路由
authors: "Nikkie Hooman, Zhongjie Wu, Eric C. Larson, Mehak Gupta"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09982v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于临床预测的多模态路由，结合胸部X光和临床笔记
tldr: 针对电子健康记录多模态数据预测中深度融合缺乏可解释性的问题，提出显式多模态路由框架，构建离散单模态、定向双模态和三模态路径，并引入推理时路径掩码以审计和评估鲁棒性。在MIMIC-IV三模态数据上进行的多标签表型预测和ICU死亡率预测实验，揭示了临床条件组间模态依赖的系统差异，提供了透明、可审计且实用的多模态临床预测方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09982v1/fig-001.webp\", \"caption\": \"Table 3: We report the per-phenotype performance (F1) on the test dataset using our proposed model. We also group each phenotype into disease categories and report the mean performance across each group.\", \"page\": 7, \"index\": 1, \"width\": 506, \"height\": 805}, {\"url\": \"assets/figures/arxiv/2607.09982v1/fig-002.webp\", \"caption\": \"Figure 4: The figure illustrates local interpretability for three patients, showing how routing weights are distributed across the three modalities for different phenotype prediction groups.\", \"page\": 10, \"index\": 2, \"width\": 1043, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2607.09982v1/fig-003.webp\", \"caption\": \"Figure 1: Architecture of the proposed multimodal routing framework.\", \"page\": 3, \"index\": 3, \"width\": 1021, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2607.09982v1/fig-004.webp\", \"caption\": \"Figure 2: Routing activation and effective weight across all phenotypes. The top heatmap shows the average primary activation (𝛼𝑟 ) across routing primaries. The bottom heatmaps show the effective routing weights for each phenotype. Three heatmaps at the top and bottom correspond to the FULL setting and missing-modality settings (Missing-I and Missing-N).\", \"page\": 8, \"index\": 4, \"width\": 842, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2607.09982v1/fig-005.webp\", \"caption\": \"Figure 5: Routing coefficient heatmaps across all phenotypes. Three heatmaps correspond to the FULL setting and missingmodality settings (Missing-I and Missing-N).\", \"page\": 12, \"index\": 5, \"width\": 1034, \"height\": 733}]"
motivation: 现有深度多模态融合方法掩盖了各模态贡献，限制了可解释性，需要可解释、鲁棒且可审计的多模态临床预测框架。
method: 构建显式多模态路由，包括单模态、定向双模态和三模态路径，并引入推理时路径掩码模拟缺失模态，重新加权剩余路径以审计推理过程。
result: 在MIMIC-IV三模态数据上的多标签表型预测和二元ICU死亡率预测中，框架揭示了不同临床条件组对模态依赖的系统差异。
conclusion: 该框架提供了透明、可审计且鲁棒的多模态临床预测方法，有助于理解不同数据源如何驱动模型决策。
---

## 摘要
电子健康记录（EHR）数据本质上具有多模态特性，利用多种模态可以提升预测性能。然而，现有方法大多依赖深度融合，这掩盖了单个模态对预测的贡献，并限制了多模态推理的可解释性。我们提出了一种面向临床预测的显式多模态路由框架，能够在三种EHR模态上实现可解释、鲁棒且可审计的推理：结构化纵向变量（L）、临床笔记（N）和胸部X光片（I）。我们的模型构建了离散的单模态、有向双模态和三模态路径，以捕获个体模态信号及非对称的跨模态交互。为了审计多模态推理并评估鲁棒性，我们引入了推理时路径掩蔽，它在不重新训练的情况下模拟缺失模态并重新加权剩余路径。我们分析了这些场景下性能和路由权重的变化，以理解模型决策。我们在MIMIC-IV的三模态住院记录上对多标签表型预测（K=25）和二元ICU死亡率预测进行了评估，揭示了不同临床条件组间模态依赖的系统性差异。总体而言，我们的框架为多模态临床预测提供了一种透明、可审计且实用的方法，提供了可解释性、鲁棒性，并深入揭示了不同数据源如何驱动模型决策。

## Abstract
Electronic health record (EHR) data are inherently multimodal, and leveraging multiple modalities can improve predictive performance. However, most existing approaches rely on deep fusion, which obscures how individual modalities contribute to predictions and limits the interpretability of multimodal reasoning. We propose an explicit multimodal routing framework for clinical prediction that enables interpretable, robust, and auditable reasoning across three EHR modalities: structured longitudinal variables (L), clinical notes (N), and chest X-rays (I). Our model constructs discrete unimodal, directional bimodal, and trimodal routes to capture both individual modality signals and asymmetric cross-modal interactions. To audit multimodal reasoning and assess robustness, we introduce inference-time route masking, which simulates missing modalities and reweights the remaining routes without retraining. We analyze changes in performance and routing weights under these scenarios to understand model decision-making. We evaluate our framework on multi-label phenotype prediction (K = 25) and binary ICU mortality prediction using trimodal patient stays from MIMIC-IV, revealing systematic differences in modality reliance across clinical condition groups. Overall, our framework offers a transparent, auditable, and practical approach to multimodal clinical prediction, providing interpretability, robustness, and insights into how different data sources drive model decisions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：电子健康记录（EHR）数据天然是多模态的，包含结构化纵向变量（L）、临床笔记（N）和医学影像（如胸部X光片I）。利用多模态信息可提升临床预测性能。
- **核心问题**：现有多模态融合方法（如深度融合、后期融合）将多种模态混合为单一潜在表示，掩盖了各模态的个体贡献和跨模态交互，可解释性差，难以满足临床对透明度和审计性的要求。
- **整体含义**：需要一种既能保持高性能，又能显式暴露证据路径的框架，使模型决策可解释、鲁棒且可审计，从而增强临床信任。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：构建显式的**多模态路由框架**，将信息划分为离散的**单模态路径（R1）**、**有向双模态路径（R2）** 和**三模态路径（R3）**，共10条路径。每条路径使用可学习的**路由激活系数（α）** 和**路由系数（R）** 来量化其表达强度和任务特定重要性。
- **关键技术细节**：
  - **模态编码器**：结构化数据用BEHRT Transformer，临床笔记用BioClinicalBERT，胸部X光用ResNet-34，分别得到模态级嵌入。
  - **路径构建**：
    - 单模态路径：L、N、I各一个。
    - 双模态路径：通过**定向交叉注意力**实现，例如L←N表示用N的token作为key/value更新L的token。共6条（L←N, N←L, L←I, I←L, N←I, I←N）。
    - 三模态路径：将三组双向双模态嵌入（LN、LI、NI）组合投影得到。
  - **路由激活与系数**：
    - 每条路径输出一个向量˜e_r和一个标量激活α_p,r（sigmoid后∈(0,1)）。
    - 路由系数R_p,r,c跨路径归一化（和为1），表示路径r对患者p和标签c的贡献。
    - 最终决策表示：d_p,c = Σ_r R_p,r,c · α_p,r · ˜e_r。
  - **推理时路径掩蔽**：测试时移除包含缺失模态的所有路径，并重新归一化剩余路径系数，无需重新训练。用于模拟缺失模态场景并审计模型推理。
- **公式/算法流程**（文字说明）：
  1. 输入三模态数据，通过各自编码器得到池化嵌入。
  2. 将嵌入线性投影到公共维度d=256。
  3. 对每个有序模态对执行交叉注意力，生成6个双模态路径嵌入。
  4. 组合双模态嵌入得到1个三模态路径嵌入。
  5. 每条路径通过线性层产生内容向量和激活值。
  6. 对每个患者-标签对，计算路由系数并加权聚合路径，最后经softmax/sigmoid输出预测。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：MIMIC-IV EHR数据集，包含三种模态的ICU住院记录。
  - **ICU死亡率预测**：使用前48小时数据，N=6,677。
  - **多标签表型预测**：25种临床表型，使用全住院数据，N=11,230。排除出院小结以防泄漏。
  - 数据划分：80%训练，20%测试，从训练中取5%验证。
- **基准方法**：与多种多模态融合基线对比：
  - **联合融合（平均模态嵌入）**
  - **晚期融合**
  - **TriMF**：构建双模态路径并合并为单一多模态嵌入。
  - **MoE-Health**：基于门控的专家混合路由。
  - 所有基线使用相同的模态编码器、训练数据优化设置，仅融合机制不同。
- **评价指标**：AUROC和F1（多表型取宏平均）。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提到使用PyTorch实现，并使用SMU O’Donnell数据科学与研究计算研究所的计算资源。需要指出这一点。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：与4种基线对比（表1）。
  - 消融实验：3种消融设置（去掉交叉注意力、去掉路由激活、去掉路由系数），以及完整模型（表2）。
  - 缺失模态评估：6种模态组合（L、N、I、L+N、L+I、全模态），见表4。
  - 图2/3：路由激活和有效权重的全局可解释性分析（完整及缺失模态）。
  - 图4：3个患者的局部可解释性示例。
  - 表3：25个表型的详细F1及分组均值。
  - 路由系数热力图（附录图5/6）。
- **充分性与公平性**：
  - 实验覆盖了不同任务、不同模态缺失场景、不同层级（全局/局部/分组）的评估。
  - 消融实验隔离了各组件的贡献，验证了路由激活、系数和交叉注意力的必要性。
  - 基线方法采用了代表性融合范式，并在公平条件下比较（相同编码器、数据、优化）。
  - 不足：未在更多数据集（如其他EHR系统）上验证；未与基于图或超图的融合方法（如MINGLE）直接比较，因输入表示不同；未展示统计显著性检验；部分低患病率表型F1较低，可能受类不平衡影响。

## 6. 论文的主要结论与发现
- **预测性能**：所提路由框架在死亡率和表型预测上均显著优于所有基线（AUROC提高3.9%~4.7%，F1提高4.7%~6.4%）。
- **可解释性**：
  - 全局：三模态路径权重最高；双模态中N←L（笔记受结构化数据影响）最活跃；不对称性符合临床直觉（笔记受益于结构化证据，影像受益于生理背景）。
  - 局部：展示了三个患者的路由权重分布，与临床描述一致。
  - 分组：不同疾病组依赖不同模态（呼吸组依赖影像，代谢组依赖结构化数据）。
- **鲁棒性与审计**：
  - 缺失模态时性能下降符合临床预期（如呼吸组缺失影像下降最大）。
  - 通过路由权重重新分布可识别依赖窄信号或潜在虚假相关的表型（如脑血管疾病对笔记过度依赖，缺失笔记时性能崩溃，提示可能存在捷径）。
- **关键发现**：显式路由机制提供了透明证据路径，便于审计模型是否使用鲁棒的多模态证据。

## 7. 优点
- **方法论创新**：首次为临床多模态预测设计显式有向路由框架，捕获非对称交互，优于对称或深度融合。
- **动态可解释性**：通过路由激活（患者级）和路由系数（标签级）双重机制，支持局部、全局和分组解释。
- **推理时审计**：缺失模态分析无需重训练，可直接观察模型重分配能力，识别脆弱的决策逻辑。
- **性能提升**：在两项任务上均超过现有方法，同时保持可解释性。
- **开源实现**：代码已公开，可复现。

## 8. 不足与局限
- **算力资源未报告**：未提供GPU型号、训练时间等，不利于复现和效率评估。
- **实验覆盖**：仅在MIMIC-IV一个数据集上评估，缺乏跨机构或跨疾病泛化验证。仅包含三种模态，未扩展到更多模态（如遗传、药物）。
- **双模态路径数量**：随模态数二次增长（O(M²)），扩展到更多模态时计算开销大（作者已讨论）。
- **低患病率表型性能**：部分表型F1较低（如呼吸道疾病<0.3），类不平衡问题未充分解决。
- **鲁棒性分析不足**：缺失模态实验仅测试单模态缺失，未测试同时缺失两个模态或部分缺失（如部分笔记缺失）。未与专门处理缺失模态的方法（如FuseMoE、Flex-MoE）比较（因训练数据全模态）。
- **计算效率**：需要为每个双模态方向执行交叉注意力，可能增加推理时间。
- **统计检验缺失**：未报告性能差异的显著性（如t检验或置信区间重叠）。
- **临床接受度**：虽然提供了可解释性，但仍需实际临床用户验证解释的实用性和信任度。

（完）
