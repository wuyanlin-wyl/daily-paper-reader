---
title: Medical Image Segmentation based on Deep Active Contour and Mean Curvature Loss Function
title_zh: 基于深度主动轮廓和平均曲率损失函数的医学图像分割
authors: "Xiao-qiang Zhai, Zhi-feng Pang, Peng Zheng, Ze-wen Li, Yan-zhe Hou"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12586v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于深度活动轮廓和平均曲率损失的医学图像分割
tldr: 针对医学图像分割中深度学习缺乏几何先验的问题，本文提出基于深度主动轮廓和平均曲率损失函数（DACMC），通过卷积核近似平均曲率引入几何约束，在肝脏和脾脏数据集上取得了新最优性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12586v1/fig-001.webp\", \"caption\": \"Fig. 4. Segmentation results on the spleen MRI dataset. From left to right: original image, Ground Truth, CE, DC, AC, ACE, and DACMC.\", \"page\": 11, \"index\": 1, \"width\": 864, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2607.12586v1/fig-002.webp\", \"caption\": \"Fig. 3. Segmentation results on the liver CT dataset. From left to right: original image, Ground Truth, CE, DC, AC, ACE, and DACMC.\", \"page\": 11, \"index\": 2, \"width\": 864, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2607.12586v1/fig-003.webp\", \"caption\": \"Fig. 2. Consider the iterative case of segmentation boundaries for network prediction. The region term is minimized only when the segmentation boundary lies on the object boundary.\", \"page\": 8, \"index\": 3, \"width\": 902, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2607.12586v1/fig-004.webp\", \"caption\": \"TABLE 1 Quantitative Analysis Results (U-Net), Mean (Variance).\", \"page\": 10, \"index\": 4, \"width\": 910, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2607.12586v1/fig-005.webp\", \"caption\": \"Fig. 1. The general overview of the proposed DACMC loss, integrating curvature loss terms and region terms as geometric natural constraints for image segmentation.\", \"page\": 7, \"index\": 5, \"width\": 848, \"height\": 435}]"
motivation: 现有深度学习方法在医学图像分割中缺乏对分割区域的几何表征，需要引入更有效的几何约束。
method: 提出DACMC损失函数，利用卷积核近似平均曲率作为几何自然约束，集成到主动轮廓模型中。
result: 在肝脏和脾脏数据集上验证，达到新的最先进分割性能。
conclusion: 引入平均曲率曲率损失有效提升医学图像分割的几何表征能力，且计算高效。
---

## 摘要
医学图像分割是临床分析和应用领域中的一项关键任务。尽管深度学习技术近年来在多种场景中发挥了重要作用，但基于单个像素级别的训练导致了缺乏几何先验信息。学者们提出将Chan-Vese模型集成到损失函数中进行训练，这样可以考虑到分割过程中区域内部和外部的区域和长度，从而提高医学图像分割的性能。然而，这些方法仍然缺乏对分割区域的有效表征。为了克服这个问题，我们引入平均曲率作为几何自然约束，并提出了一种深度主动轮廓和平均曲率（DACMC）损失函数，其中使用卷积核来近似平均曲率以节省计算成本。我们在肝脏和脾脏数据集上验证了我们的方法性能。我们提出的方法在多个分割数据集上展示了新的最先进性能。

## Abstract
Medical image segmentation is a crucial task in the field of clinical analysis and applications. Though deep learning techniques recently play a crucial role in several scenarios, the training at the individual pixel level leads to a lack of geometric prior information. Scholars proposed to integrate the Chan-Vese model into the loss function for training which can take into account the region and length of the region inside and outside the segmentation process and then improve the performance in medical image segmentation. However, these methods still lack an effective characterization of the segmented region. To overcome this problem, we introduce the mean curvature as a geometric natural constraint and propose a Deep Active Contour and Mean Curvature (DACMC) loss function where the convolution kernel is used to approximate the mean curvature to save computational cost. We have validated the performance of our method on the liver and spleen dataset. Our proposed method demonstrates new state-of-the-art performance on several segmentation datasets.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：深度学习方法在医学图像分割中逐像素训练，缺乏几何先验信息，导致分割结果不够平滑、连通性差，尤其对复杂解剖结构表征不足。现有将 Chan-Vese 主动轮廓模型集成到损失函数中的方法（如 AC、ACE）虽然引入了区域和长度约束，但仍缺少对分割区域局部几何属性（如平均曲率）的有效刻画。
- **研究动机**：利用平均曲率作为几何自然约束，提升分割结果的几何合理性（边界平滑、内部连通），同时保持计算效率。
- **整体含义**：本文提出一种新的损失函数 DACMC，将平均曲率项与 Mumford-Shah 区域项结合，在 U-Net 和 CE-Net 上验证，显著提升了肝脏 CT 和脾脏 MRI 分割性能，达到新 SOTA。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将平均曲率约束引入主动轮廓损失函数，通过卷积核快速近似曲率，避免高阶导数带来的计算开销。
- **关键技术细节**：
  - **DACMC 损失函数定义**（公式 16）：
    \[
    L_{ACC} = \sum_{i=1}^m \sum_{j=1}^n \left[ u_{i,j}(c_1 - v_{i,j})^2 + (1 - u_{i,j})(c_2 - v_{i,j})^2 \right] + \lambda \mathcal{C}
    \]
    其中 \(u\) 为预测分割图，\(v\) 为输入图像灰度，\(c_1, c_2\) 为前景/背景平均灰度（训练中固定为 1 和 0）。
  - **曲率项 \(\mathcal{C}\)**（公式 17）：
    \[
    \mathcal{C} = \sum_{i=1}^m \sum_{j=1}^n \left| \frac{u^*_{i,j} * v_{i,j}}{v_{i,j} * + \epsilon} \right|
    \]
    其中 \(*\) 表示卷积操作，使用固定卷积核（公式 19）近似平均曲率：
    \[
    \text{Kernel} = \begin{bmatrix}
    -1/16 & 5/16 & -1/16 \\
    5/16 & -1 & 5/16 \\
    -1/16 & 5/16 & -1/16
    \end{bmatrix}
    \]
  - **网络架构**：采用 U-Net 和 CE-Net 作为骨干网络，训练时通过反向传播优化上述损失函数。
  - **训练流程**：像素级监督，前景/背景均值 \(c_1, c_2\) 固定为常数（0 和 1），曲率项促进预测边界平滑和内部连通。

#### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：
  - **肝脏 CT**：Kaggle 公共数据集，100 训练 + 15 验证 + 16 测试。
  - **脾脏 MRI**：CHAOS 挑战赛数据集，120 训练 + 20 验证 + 25 测试；图像统一缩放到固定尺寸。
- **Benchmark 指标**：Dice 相似系数 (DSC)、95% Hausdorff 距离 (HD95)、Jaccard 相似度 (JS)、平均表面距离 (ASD)。
- **对比方法**：CE（交叉熵）、DC（Dice 损失）、AC（主动轮廓损失）、ACE（主动轮廓+弹性损失），以及本文的 DACMC。
- **网络架构**：分别基于 U-Net 和 CE-Net 进行对比。

#### 4. 资源与算力
- **硬件**：Intel Xeon 5218R CPU + Nvidia RTX5000 GPU。
- **训练配置**：batch size=4，epochs=300，Adam 优化器，学习率 0.0001。
- **未说明**：模型训练总时长、是否进行多轮调整等未明确。文中仅提到一次参数调优得到合适区间。

#### 5. 实验数量与充分性
- **实验组数**：
  - 两个数据集（肝脏 CT、脾脏 MRI）× 两种网络（U-Net 和 CE-Net）× 五种损失函数 = 20 组主要对比（但表格仅列出 U-Net 的结果，CE-Net 的结果只在文字中提及，未给出完整表格）。
  - 消融实验：从 CE 基线逐步添加 Dice、AC、ACE 直到完整 DACMC，验证每个组件贡献。
- **充分性评价**：
  - **优点**：评价指标全面（4个），数据集来源公开，对比方法覆盖主流损失函数，消融实验设计合理。
  - **不足**：仅包含两个数据集（肝脏和脾脏），且均为腹部器官；网络架构仅 U-Net 和 CE-Net，未对比其他更强网络（如 nnUNet、Transformer 类）；未给出 CE-Net 的完整数值表格，公平性打折扣；未与其他基于曲率的损失（如直接使用 Euler's Elastica）进行更详尽比较。

#### 6. 论文的主要结论与发现
- DACMC 损失函数在 DSC、HD95、JS、ASD 上均优于 CE、DC、AC、ACE，尤其在 DSC 上提升明显（肝脏：0.9426 vs 次优 0.9370；脾脏：0.8443 vs 次优 0.8358）。
- 平均曲率约束有效保证了分割结果的内部连通性和边界平滑度（如图 3、图 4 可视化结果）。
- 消融实验表明，每个组件（区域项、曲率项）都对最终性能有正向贡献，曲率项尤其在小目标或尺度变化大的数据集上表现鲁棒。

#### 7. 优点
- **方法创新**：首次将平均曲率作为几何约束直接嵌入损失函数，弥补了现有主动轮廓损失缺乏局部几何表征的不足。
- **计算高效**：采用固定卷积核近似平均曲率，避免了高阶导数的显式计算，易于 GPU 并行加速。
- **性能优异**：在肝脏和脾脏数据集上均达到 SOTA，且泛化到不同网络架构。
- **消融验证充分**：逐步增加组件，清晰展示了每个模块的贡献。

#### 8. 不足与局限
- **数据集覆盖有限**：仅评估了肝脏和脾脏两个器官，缺乏多器官、多模态（如心脏、脑部、CT/MR 混合）的验证。
- **网络多样性不足**：仅采用 U-Net 和 CE-Net，未与现代先进架构（如 nnUNet、SwinUNet、Mamba 系列）对比。
- **数值报告不完整**：CE-Net 的结果仅在文字中提及，未以表格形式呈现，影响复现和公平比较。
- **参数调优未深入**：仅说明存在一个合适区间，未提供参数敏感性分析。
- **计算资源描述模糊**：未给出训练时间、内存占用等关键效率指标。
- **缺乏与其他曲率方法的直接对比**：如直接使用 Euler's Elastica 的 ACE 已被包含，但更专门的曲率正则化方法（如基于水平集的曲率项）未被比较。
- **潜在偏差风险**：训练集规模较小（肝脏100张，脾脏120张），可能无法充分代表真实临床分布。

（完）
