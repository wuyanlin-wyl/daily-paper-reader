---
title: "ShapKO: Shapley-Adaptive Modality Knockout for Robust Multimodal Learning"
title_zh: "ShapKO: 基于夏普利自适应的模态剔除用于鲁棒多模态学习"
authors: "Nusrat Binta Nizam, Fengbei Liu, Sunwoo Kwak, Minh Nguyen, Ruining Deng, Mert R. Sabuncu"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09884v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 动态训练提升鲁棒多模态医学学习
tldr: 针对多模态医疗模型中输入缺失和模态主导问题，提出ShapKO动态训练策略，通过Shapley值自适应更新模态掩码概率，抑制主导模态，促进互补表示，无需修改架构，在三个数据集上显著提升缺失模态下的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09884v1/fig-001.webp\", \"caption\": \"Fig. 3. ShapKO rates over training (warm up epoch, K =20). Mean (solid line) and standard deviation (shaded band) of the learned adaptive knockout rate for each modality across 15 cross-validation folds, plotted at the KO update epochs. Lower KO indicates the modality is retained more often, while higher KO indicates more frequent knockout.\", \"page\": 8, \"index\": 1, \"width\": 652, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2607.09884v1/fig-002.webp\", \"caption\": \"Table 2. AUC (%, mean±std over 5 folds) for binary classification under different modality combinations. Best method per row is bolded.\", \"page\": 6, \"index\": 2, \"width\": 733, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2607.09884v1/fig-003.webp\", \"caption\": \"Fig. 2. C-index performance across modality subsets under different strategies (across 15 cross-validation folds). Grouped bar plots compare Baseline, OGM-GE, Fixed KO, and ShapKO for single-modality + full-modality, two-modality, and three-modality input subsets. Vertical gray ranges indicate the variability (± standard deviation) around each estimate. D = Demographics, G = Genomics, P = Pathology, R = Radiology. Combinations denote modality fusion, e.g., P+R = Pathology + Radiology, and P+R+D+G = all four modalities.\", \"page\": 6, \"index\": 3, \"width\": 711, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2607.09884v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of ShapKO (architecture-agnostic). ShapKO trains with sampled modality masks by replacing knocked-out modalities (or embeddings) with fixed placeholders before fusion. Every K epochs, it evaluates validation utility over modality subsets, estimates Shapley-based modality importance, and updates permodality knockout probabilities with a stable drop-strong-more rule. The procedure is architecture-agnostic and applies across tasks.\", \"page\": 3, \"index\": 4, \"width\": 675, \"height\": 262}]"
motivation: 多模态模型在缺失输入时性能下降，且训练中模态主导导致鲁棒性差。
method: ShapKO基于验证效用通过Shapley值动态调整各模态的掩码概率，频繁抑制主导模态。
result: 在三个数据集（多任务临床分类、生存预测、癌症检测）上，缺失模态下性能一致提升。
conclusion: ShapKO自适应掩码策略有效提升多模态模型对缺失输入的鲁棒性，且掩码行为可解释。
---

## 摘要
多模态医学模型在输入缺失时常常会退化，这在真实临床工作流程中是常见情况。另外，即使所有模态都存在，训练过程中也会观察到模态主导现象，即优化过度依赖预测性高的模态而忽略互补来源，导致在部分可用性下鲁棒性差。虽然训练时剔除模态可以提高缺失模态的鲁棒性，但现有方法使用静态掩码率，无法适应训练过程中模态效用的变化。我们提出ShapKO（基于夏普利自适应的模态剔除），一种动态训练策略，基于验证效用学习特定模态的剔除概率。ShapKO定期评估模态子集的性能，通过夏普利值估计模态重要性，并更新掩码概率以更频繁地抑制主导模态。这种自适应过程促进了互补表示，同时不需要修改架构。我们在三个数据集上评估ShapKO，涵盖多任务临床分类、生存预测和癌症检测。ShapKO在模态缺失下持续改进性能，并产生可解释的所学掩码行为轨迹。代码可在：https://github.com/sumona00/ShapKO 获取。

## Abstract
Multimodal medical models often degrade when inputs are missing, a common scenario in real-world clinical workflows. Separately, even when all modalities are present, modality dominance is observed during training, where optimization over-relies on a highly predictive modality and undertrains complementary sources, resulting in poor robustness under partial availability. While training-time modality knockout improves missing-modality robustness, existing approaches use static masking rates that cannot adapt to evolving modality utility during training. We introduce ShapKO (Shapley-Adaptive Modality Knockout), a dynamic training strategy that learns modality-specific knockout probabilities based on validation utility. ShapKO periodically evaluates performance across modality subsets, estimates modality importance via Shapley values, and updates masking probabilities to suppress dominant modalities more frequently. This adaptive process promotes complementary representations, while requiring no architectural modifications. We evaluate ShapKO on three datasets covering multitask clinical classification, survival prediction, and cancer detection. ShapKO consistently improves performance under modality absence and yields interpretable trajectories of learned masking behavior. Code is available at: https://github.com/sumona00/ShapKO

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态医学模型在实际临床中常面临输入缺失（如成像、生理信号、实验室检查等），导致性能显著下降。此外，即使在所有模态都可用时，训练中会出现“模态主导”现象——优化过度依赖预测性最高的模态，而弱化了互补模态的学习，使得模型在部分模态缺失时鲁棒性更差。
- **研究背景**：现有训练时模态剔除（modality knockout）方法采用静态掩码率，无法根据训练过程中模态效用的动态变化进行调整，因而无法有效缓解模态主导问题。作者希望提出一种自适应、架构无关的策略，在训练中动态调整各模态的剔除概率，以提升模型在任意模态可用性下的鲁棒性。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：ShapKO 基于验证集上模态子集的效用（utility）估计每个模态的夏普利值（Shapley value），并据此更新各模态的剔除概率，实现“抑制强势模态”（drop-strong-more）的自适应策略。该方法无需修改模型架构。
- **关键技术细节**：
  - **模态效用评估**：将模态视为合作博弈中的玩家，对于任意子集 \( S \subseteq O \)（O为当前可用的模态集），定义效用 \( v(S) \) 为模型在验证集上仅使用S中模态时的任务指标（如AUC、C-index），并统一为“越大越好”方向。
  - **夏普利值计算**：按合作博弈公式计算每个模态的夏普利值 \( \phi_m \)，表示该模态在所有可能子集中的平均边际贡献，反映其重要性和与其他模态的交互效应。
  - **剔除概率更新**：
    1. 将夏普利值映射到非负权重 \( w_m \)，并归一化使其和为1。
    2. 计算均匀权重 \( \bar{w}=1/|O| \)。
    3. 更新每个模态的剔除概率 \( r_m = \text{clip}( r \cdot (w_m/\bar{w})^\delta, r_{\min}, r_{\max}) \)，其中 \( r \) 为基础剔除率，\( \delta \) 控制敏感度，\( r_{\min}, r_{\max} \) 限制边界。当 \( w_m > \bar{w} \) 时，剔除概率高于基础值，实现“抑制强势模态”。
  - **训练流程**：交替进行两个阶段：
    - **阶段1**：使用当前剔除概率对每个批次随机生成掩码（保证至少保留一个模态），替换被剔除模态的嵌入为固定占位符（placeholder），计算损失并更新模型参数。
    - **阶段2**：每K个epoch后，固定模型参数，在验证集上评估所有模态子集的效用，计算夏普利值并更新剔除概率。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集与场景**：
  - **前列腺癌检测（Prostate MRI）**：使用T2w、ADC、DWI三种MRI模态进行二分类（检测临床显著性前列腺癌），AUC为评估指标。
  - **生存预测（MMD benchmark）**：整合病理、放射、基因组、人口统计学四种模态，用于脑癌生存预测，C-index为评估指标。
  - **多任务临床分类（FlexCare，基于MIMIC-IV/CXR/NOTE）**：包括EHR、胸部X光、临床笔记三种模态，覆盖住院死亡率、失代偿、表型、住院时长、再入院、诊断六个任务，平均多任务AUC为评估指标。
- **基准与对比方法**：
  - **Baseline**：标准多模态训练（无剔除）。
  - **OGM-GE**：一种基于梯度的模态平衡方法。
  - **Fixed KO**：使用固定静态剔除率的训练。
  - **ShapKO**：提出的方法。
- **公平性考虑**：所有方法采用相同的基础架构（同一模态编码器、融合器），超参数通过交叉验证调整，对比结果基于多个交叉验证折叠（5折或15折）的均值与标准差。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量或训练时长。仅提到了训练epoch数（如100、300）和学习率等超参数。因此无法从本文获取具体的算力资源信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 三个数据集/场景，覆盖不同任务类型（分类、生存分析、多标签分类）。
  - 每个数据集对比了多种模态子集（单模态、双模态、三模态/全模态）下的性能，例如前列腺癌检测包含7种子集组合，生存预测包含所有1/2/3/4模态组合，多任务分类包含7种子集组合。
  - 多任务分类中报告了6个任务的AUC，生存预测中报告了分组柱状图。
  - 展示了ShapKO剔除概率在训练过程中的轨迹（图3），给出可解释性分析。
- **充分性与客观性**：
  - 实验覆盖了多种缺失模态场景，对比了多个强基线，且结果保留统计变异性（标准差/波动带）。
  - 消融方面：由于方法本身已包含动态机制，论文没有额外进行如不同δ、不同基础剔除率的系统消融，但提到了超参数通过交叉验证设定。
  - 总体实验设计较为全面，但缺少对近似Shapley估计效率的评估（如抽样法），也未测试更大规模数据集（如更多模态数）。
  - **结论客观**：ShapKO在大多数模态子集上达到最优或持平，且在部分场景改进显著，但未在所有子集上全面超越（如前列腺T2w单模态略逊于Fixed KO）。

## 6. 论文的主要结论与发现

- ShapKO通过**自适应的基于夏普利值的剔除概率更新**，显著提升了模型在不同缺失模态下的鲁棒性，且不降低全模态下的性能。
- 相比静态剔除率和梯度平衡方法，ShapKO能够更有效地抑制主导模态，促进互补模态的学习。
- ShapKO的剔除概率轨迹可作为训练过程中模态效用演变的诊断工具（可解释性）。
- 在三个医学多模态基准上一致改进，验证了方法的通用性和有效性。

## 7. 优点：方法与实验设计上的亮点

- **方法论亮点**：
  - 首次将夏普利值引入训练过程用于动态调整模态剔除概率，理论依据充分（合作博弈解释模态贡献）。
  - 架构无关，适用于任意多模态融合模型，易于迁移。
  - 更新规则简单稳定，通过“抑制强势模态”直接针对模态主导问题。
  - 剔除概率轨迹提供了可解释性诊断。
- **实验设计亮点**：
  - 覆盖多类型任务（分类、生存、多标签），验证泛化性。
  - 对比多个强基线，包括近期梯度平衡方法。
  - 报告所有可能的模态子集性能，全面评估缺失场景。
  - 多次交叉验证（5或15折）并报告均值和标准差，结果稳健。

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅测试了3种模态（前列腺）和4种模态（生存）的场景，未涉及更多模态（如5个以上）的情况。
  - 缺少对近似夏普利值估计（如蒙特卡罗抽样）的效率与精度评估，实际中枚举所有子集可能代价较高。
  - 没有进行大规模消融实验（如不同基础剔除率r、不同敏感度δ的影响）。
- **偏差与风险**：
  - 剔除概率更新依赖于验证集效用，若验证集分布与真实部署有差异，可能引入偏差。
  - 占位符（placeholder）的设计（零向量、-1向量、学习向量）对结果有影响，论文未深入探讨其敏感性。
  - 未考虑最差情况或临床优先的缺失模式，优化目标是平均子集效用。
  - 算力资源未报告，不利于评估实际应用成本。
- **应用限制**：
  - 需要定期评估所有模态子集，对于模态数量多且验证集大的场景，时间开销可能成为瓶颈。
  - 超参数（δ、rmin、rmax）需针对不同数据集进行调优，通用性依赖调参。

（完）
