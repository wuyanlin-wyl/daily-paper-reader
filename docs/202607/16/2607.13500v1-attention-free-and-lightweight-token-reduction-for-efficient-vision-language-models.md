---
title: Attention-Free and Lightweight Token Reduction for Efficient Vision-Language Models
title_zh: 高效视觉-语言模型的免注意力轻量级令牌缩减
authors: "Xuanyi Hao, Zuoyuan Zhang, Zhibo Wang, Xiaoyi Pang, Jiahui Hu, Jiacheng Du, Shuguo Zhuo"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13500v1"
tags: ["query:vl"]
score: 9.0
evidence: 用于视觉语言模型的免注意力令牌缩减
tldr: 针对视觉语言模型计算开销大的问题，本文提出了一种无注意力、轻量级的令牌缩减框架。该框架通过基于信息熵的准则保留重要令牌，并利用变换一致性信号实现多样化选择，从而生成紧凑的视觉表示。实验表明，该方法在激进压缩下仍能保持竞争力，实现了良好的精度-效率权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13500v1/fig-001.webp\", \"caption\": \"TABLE I: Performance on LLaVA-v1.5. Values outside parentheses denote the original accuracy (%) on TextVQA, ScienceQA, and GQA, or the original total score on MME. Values in parentheses denote the corresponding performance relative to the vanilla model, whose performance is normalized to 100%. The average is computed over the normalized results.\", \"page\": 6, \"index\": 1, \"width\": 1076, \"height\": 663}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative visualization of retained visual tokens in the ablation study.\", \"page\": 10, \"index\": 2, \"width\": 531, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-003.webp\", \"caption\": \"Fig. 5: Sensitivity of our method to the balancing coefficient λ under different visual-token budgets.\", \"page\": 10, \"index\": 3, \"width\": 502, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-004.webp\", \"caption\": \"TABLE IV: OOM-free generation boundary on Jetson AGX Orin under a 16GB available-memory budget.\", \"page\": 9, \"index\": 4, \"width\": 524, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-005.webp\", \"caption\": \"Fig. 3: Ablation study results.\", \"page\": 9, \"index\": 5, \"width\": 498, \"height\": 312}]"
motivation: 现有令牌缩减方法依赖注意力图或密集型比较，不兼容加速框架且计算开销大，限制了实际部署。
method: 提出即插即用框架：用熵估计令牌重要性，用变换一致性信号排序令牌并通过跨步选择确保多样性。
result: 在多个视觉语言模型基准上，与现有方法相比，在保持相近准确率的同时显著降低了计算成本。
conclusion: 该框架作为轻量级模块，能够有效加速视觉语言模型推理，适用于资源受限的边缘设备。
---

## 摘要
视觉-语言模型在多模态理解中取得了强劲性能，但由于处理大量视觉令牌带来的巨大计算开销，在资源受限的边缘设备上部署仍具有挑战性。令牌缩减是加速视觉-语言模型推理的一个有前景的方向，但现有方法要么依赖与现代加速框架不兼容的注意力图，要么依赖计算密集型的成对相似性比较，这损害了可扩展性并抵消了其在实际部署中的实用优势。本文提出了一种免注意力的轻量级令牌缩减框架，作为视觉-语言模型的即插即用模块，该框架通过保留重要且多样化的令牌来生成紧凑的视觉表示。首先，为实现免注意力重要性估计，我们从信息论视角出发，利用一种基于熵的新准则量化令牌信息，保留那些特征表示更具表现力且退化程度更低的令牌。其次，为以轻量方式确保视觉覆盖的多样性，我们引入了一种变换诱导的一致性信号，相似令牌产生相似信号，从而通过排序使相似令牌彼此靠近，并利用步长选择生成多样化的令牌集合。跨多个视觉-语言模型基准的大量实验表明，我们的框架实现了良好的精度-效率权衡，在激进压缩下仍能保持有竞争力的性能。

## Abstract
Vision-Language Models (VLMs) have achieved strong performance in multimodal understanding, yet remain challenging to deploy on resource-constrained edge devices due to the substantial computational overhead of processing numerous visual tokens. Token reduction is a promising direction for accelerating VLMs inference, but existing approaches either rely on attention maps that are incompatible with modern acceleration frameworks or depend on computationally intensive pairwise similarity comparisons, which undermine scalability and negate their practical benefits in deployment. In this paper, we propose an attention-free and lightweight token reduction framework as a plug-and-play module for VLMs, which preserves both important and diverse tokens to produce a compact visual representation. First, to enable attention-free importance estimation, we adopt an information-theoretic perspective and quantify token information using a novel entropy-based criterion, retaining those with more expressive and less degenerate feature representations. Second, to ensure diverse visual coverage in a lightweight manner, we introduce a transformation-induced consistency signal where similar tokens yield similar signals, such that sorting by this signal places similar tokens close to each other and enables stride-based selection to produce a diverse token set. Extensive experiments across multiple VLMs benchmarks demonstrate that our framework achieves a favorable accuracy-efficiency trade-off, maintaining competitive performance under aggressive compression.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：视觉-语言模型（VLM）在资源受限的边缘设备上部署困难，主要原因是视觉编码器产生大量视觉令牌（数百甚至数千个），导致LLM自注意力计算量呈二次增长，造成巨大计算开销。
- **现有方法局限**：
  - 基于注意力图的令牌缩减方法（如SparseVLMs、PyramidDrop、MustDrop）依赖显式注意力计算，与现代加速框架（如FlashAttention）不兼容，无法获得系统级加速收益。
  - 基于令牌相似性的方法（如SAINT、DyMU）需要大量的成对比较（通常为二次复杂度），引入额外计算负担，削弱了令牌缩减的实际效率优势。
- **研究目标**：提出一种免注意力、轻量级、即插即用的视觉令牌缩减框架，同时确保高有效性、高效率和广泛兼容性。

## 2. 论文提出的方法论
### 核心思想
- 通过联合建模令牌**重要性**和**多样性**，一次性选择视觉令牌，生成紧凑且富有表现力的令牌子集，不依赖注意力图或成对比较。

### 关键技术细节
- **第一阶段：重要性感知令牌选择**（Stage I）
  - **方法**：利用二阶Rényi熵衡量每个令牌特征激活的分散程度。将token的绝对值归一化视为离散分布，计算熵值。
  - **公式**：
    - 归一化：\( p_{i,j} = \frac{|x_{i,j}|}{\sum_{k=1}^D |x_{i,k}|} \)
    - 二阶Rényi熵：\( H_2(x_i) = -\log \sum_{j=1}^D p_{i,j}^2 \)
  - **解释**：熵值越高，表示令牌激活越均匀，包含更丰富、更少退化的语义信息。保留熵最高的Top-K1个令牌作为重要性集 \( X_{\text{imp}} \)。

- **第二阶段：多样性感知令牌选择**（Stage II）
  - **方法**：利用视觉编码器最后一层MLP的变换一致性信号。计算每个令牌在MLP变换前后的余弦相似度。
  - **公式**：\( s_i = \cos(x'_i, x''_i) \)，其中 \( x''_i = f(x'_i) = W_2 \sigma(W_1 x'_i) \)。
  - **解释**：相似语义的令牌会得到相似的\( s_i \)值，因此按\( s_i \)排序后，步长采样可确保覆盖不同语义区域，避免冗余。从剩余令牌中选择K2个作为多样性集 \( X_{\text{div}} \)。

- **整体流程**（Algorithm 1）：
  1. 根据预算K和平衡因子λ，计算K1 = λK, K2 = K - K1。
  2. 计算所有令牌的Rényi熵，取Top-K1作为重要性集。
  3. 从剩余令牌中计算MLP变换余弦相似度，排序后等步长选取K2个作为多样性集。
  4. 合并两个集合得到最终缩减令牌集 \( X^* = X_{\text{imp}} \cup X_{\text{div}} \)。
- **关键特性**：无训练、无需注意力图、无成对比较，复杂度为O(ND + N log N)。

## 3. 实验设计
- **使用的数据集与场景**：
  - LLaVA-v1.5-7B上的四个基准：MME（总得分）、TextVQA（准确率）、ScienceQA（准确率）、GQA（准确率）。
  - LLaVA-NeXT（高分辨率）上的GQA。
  - Qwen2.5-VL-7B上的TextVQA。
  - 边缘测试：Jetson AGX Orin上评估OOM边界（可生成的最大令牌长度）。
  - 消融实验与超参数敏感性（λ变化）。

- **对照方法（基线）**：
  - Inner-LM：SparseVLMs (ICML 25)、PyramidDrop (CVPR 25)、MustDrop (arXiv)
  - Before-LM：VisionZip (CVPR 25)、VisPruner (ICCV 25)
  - 上界：完整令牌（Vanilla，100%性能）

- **评估指标**：
  - 性能保留率（归一化到Vanilla的百分比）
  - 计算效率：FLOPs、CUDA时间（毫秒）
  - 兼容性：FlashAttention-2 vs Eager attention下的延迟与峰值内存
  - 边缘部署：不同令牌预算下无OOM的最大生成长度

## 4. 资源与算力
- **文中明确信息**：
  - 效率分析在单张NVIDIA RTX 5880 GPU上进行。
  - 边缘测试平台为NVIDIA Jetson AGX Orin（16GB内存预算）。
- **未明确信息**：
  - 训练时长：方法无需训练，因此未涉及训练资源。
  - 多卡并行/分布式训练相关资源：未提及。
  - 端到端推理总时间：仅报告了压缩模块本身的额外开销（FLOPs、CUDA时间）和LLM prefilling延迟，未报告完整VLM推理总时长。

## 5. 实验数量与充分性
- **实验数量**：约10组以上主要实验。
  - 主表（Table I）：LLaVA-v1.5在3个压缩率 × 4个数据集 × 6个方法。
  - 其他架构：LLaVA-NeXT（Fig.2）、Qwen2.5-VL（Sec V-B）。
  - 效率对比（Table II）：3个Before-LM方法的复杂度、FLOPs、CUDA时间。
  - 兼容性（Table III）：FlashAttention-2下的延迟和内存。
  - 边缘测试（Table IV）：3个压缩率 × 4个方法（SparseVLMs无法运行）。
  - 消融（Fig.3）：重要性、多样性组件移除对比。
  - 超参数λ敏感性（Fig.5）：在3种预算下扫描λ从0到1。
  - 可视化（Fig.4）：令牌选择定性示例。
- **充分性评价**：
  - **充分**：覆盖了主流VLM架构、多种压缩率、多个基准、效率和兼容性、边缘部署实际场景、消融与超参数分析。
  - **公平性**：基线均为最新代表性方法，对比在相同令牌预算和相同推理阶段（Before-LM）下进行；实验设置一致。
  - **不足之处**：仅测试了7B规模模型，未扩展到更大模型（如13B、34B）；缺乏与其他更近期方法（如Token Merging类）的比较；未报告方差或多次运行统计。

## 6. 论文的主要结论与发现
- **性能保持性**：在LLaVA-v1.5上，保留192令牌时达到98.86%平均性能；保留128令牌时97.95%；保留64令牌时94.52%，均优于所有基线。
- **效率优势**：ALTR的压缩模块仅需4.13M FLOPs和0.579 ms CUDA时间，远低于VisionZip（106.91M FLOPs, 0.884 ms）和VisPruner（2880.35M FLOPs, 7.151 ms）。
- **兼容性**：与FlashAttention-2完全兼容，在Qwen2.5-VL上使用FlashAttention-2可将视觉编码延迟从179.6 ms降至35.6 ms，prefill延迟从214.2 ms降至71.5 ms。
- **边缘部署可行性**：在Jetson AGX Orin上，ALTR使最大无OOM生成长度从768扩展到1536（128/64令牌预算），且压缩模块开销仅约70 ms（远低于对比方法）。
- **消融验证**：重要性和多样性组件均不可或缺，联合使用性能最优。
- **超参数鲁棒性**：λ在1/4到3/4范围内性能变化很小，无需精细调参。

## 7. 优点
- **方法创新性**：首次结合二阶Rényi熵（重要性）和MLP变换一致性信号（多样性）进行令牌选择，无需注意力图和成对比较，设计精巧。
- **即插即用**：无需训练或微调，可作为前后处理模块插入任意VLM的视觉编码器与LLM之间。
- **高效轻量**：总体复杂度O(ND + N log N)，实际开销仅约0.6 ms（GPU），适合边缘设备。
- **全面兼容**：不依赖注意力矩阵，与FlashAttention等加速框架无缝集成。
- **实验扎实**：涵盖多个架构、多组压缩率、效率与兼容性对比、边缘硬件实测、消融与敏感性分析，实验设计全面且公平。
- **实际部署验证**：在真实边缘设备上验证了内存节省和生成能力扩展，具实践价值。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅评估7B规模模型，未在更大模型（如13B、34B）上验证泛化性。
  - 未测试视频VLM或多图像场景，令牌冗余问题更严重。
  - 缺少与其他主流方法（如Token Merging、基于聚类的KV缓存压缩）的直接对比。
- **超参数依赖**：虽然鲁棒，但λ仍需手动设定，文中未提供自适应选择策略。
- **无理论保证**：重要性/多样性准则基于启发式，缺乏信息论或优化理论上的严格保证。
- **潜在偏差风险**：
  - 实验仅使用单次运行结果（未报告方差），可能受随机性影响（尽管方法确定性强）。
  - 边缘测试仅在一个平台（Jetson AGX Orin）及固定内存预算下进行，未覆盖其他边缘硬件。
- **应用限制**：方法假设视觉编码器提供MLP中间表示（常见于ViT），若编码器无此类结构（如纯CNN），则多样性信号无法直接获取。
- **未探索与投影器的交互**：令牌缩减在投影器之前进行，但投影器本身也可能引入冗余，文中未讨论端到端优化。

（完）
