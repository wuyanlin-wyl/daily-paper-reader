---
title: "FabriVLA: A Lightweight Vision-Language-Action Model for Precise Multi-Task Manipulation"
title_zh: "FabriVLA: 一种轻量级视觉-语言-动作模型，用于精确的多任务操作"
authors: "Shiyuan Yang, Borong Zhang, Jizheng Zhang, Zhijia Tao, Junfei Guo, Donglai Ran, Xu Bian, Qingbiao Li"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08575v1"
tags: ["query:vl"]
score: 9.0
evidence: 轻量级VLA模型使用InternVL3.5骨干网络
tldr: "本文提出FabriVLA，一种轻量级视觉-语言-动作模型，通过结合InternVL3.5骨干与流匹配动作头及门控自注意力，实现多任务精确操作。在Meta-World MT50基准上达到90.0%平均成功率，证明1B参数规模模型可媲美大模型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08575v1/fig-001.webp\", \"caption\": \"Figure 4: Meta-World MT50 success rate versus training step. Colored lines are the four difficulty tiers; the solid black and dashed gray lines are the tier-average and overall episodelevel success rates. The 93k checkpoint corresponds to the selected result in Table 1.\", \"page\": 6, \"index\": 1, \"width\": 469, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2607.08575v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of FabriVLA. RGB observations and the task instruction are encoded by an InternVL3.5-1B vision-language backbone (14 retained layers) into fused context tokens. A shallow VLM layer (layer 6) is additionally extracted and fused with the deep features via a learned linear projection to enrich spatial detail. A flow-matching action head composed of a stack of L=8 gated cross-attention blocks denoises an action chunk conditioned on these tokens, the robot state, and the flow timestep. The entire model is trained jointly from a pretrained VLM and randomly initialized action head.\", \"page\": 2, \"index\": 2, \"width\": 968, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2607.08575v1/fig-003.webp\", \"caption\": \"Figure 3: Design space of optional modules in the FabriVLA action head. All three variants share a common base head—action encoder → L=8 cross-attention blocks → LayerNorm → sequence-pooled MLP head → velocity—and each adds a single zero-initialized gated residual module, so at initialization (gate = 0) every variant reduces exactly to the base head; they differ only in where temporal or per-step information is injected. (a) Gated self-attention mixes the 50 action tokens inside every block before cross-attention (the learnable gate g of Section 2). (b) A token residual head adds a per-step linear map R1024→R24 in parallel with the pooled MLP head. (c) A depthwise temporal convolution (k=3) smooths adjacent steps along the horizon after the block stack. The released FabriVLA enables variant (a) only.\", \"page\": 3, \"index\": 3, \"width\": 964, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2607.08575v1/fig-004.webp\", \"caption\": \"Table 1: Comparison on Meta-World MT50 against recent VLA models. “Params” is the parameter count and “Robo-Pre.” indicates robotic data pretraining or the reported pretraining recipe. Bold and underline denote the best and second best per column; for “Params” they denote the smallest and second smallest. Easy/Medium/Hard/Very Hard are per tier success rates and Tier Avg. is their mean. Baseline numbers are extracted from the corresponding papers.\", \"page\": 5, \"index\": 4, \"width\": 918, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2607.08575v1/fig-005.webp\", \"caption\": \"Table 2: MT50 diagnostic results by task grouping. Manipulation demand buckets form a mutually exclusive partition of all 50 tasks; precision oriented subsets are non-exclusive diagnostics. Scores are means of per-task success rates with 10 episodes per task.\", \"page\": 5, \"index\": 5, \"width\": 968, \"height\": 275}]"
motivation: 减少对数十亿参数VLA骨干的依赖，构建高效紧凑的VLA模型。
method: 采用InternVL3.5视觉-语言骨干和带门控自注意力的流匹配动作头，通过浅层VLM层融合和单阶段联合优化训练。
result: "在Meta-World MT50的50个任务上平均成功率90.0%。"
conclusion: 紧凑的1B参数VLA模型也能取得与大规模模型相当的性能。
---

## 摘要
我们提出了FabriVLA，一种用于精确多任务操作的轻量级视觉-语言-动作模型。FabriVLA结合了InternVL3.5视觉-语言骨干网络和一个流匹配动作头，该动作头具有跨动作令牌的门控自注意力机制和浅层VLM融合层，以丰富空间上下文。该模型通过单阶段联合优化进行训练，包含预训练的VLM和随机初始化的动作头。在涵盖50个不同操作任务的Meta-World MT50基准上，FabriVLA实现了90.0%的层级平均成功率，表明基于1B规模VLM构建的紧凑型VLA可以在不依赖数十亿参数VLA骨干网络的情况下实现强大的性能。

## Abstract
We present FabriVLA, a lightweight Vision-Language-Action model for Precise Multi-Task Manipulation. FabriVLA combines an InternVL3.5 vision-language backbone with a flow-matching action head featuring gated self-attention across action tokens and shallow VLM layer fusion for enriched spatial context. The model is trained via single stage joint optimization from a pretrained VLM and randomly initialized action head. On the Meta-World MT50 benchmark spanning 50 diverse manipulation tasks, FabriVLA achieves a tier-average success rate of 90.0%, demonstrating that a compact VLA built on a 1B scale VLM can achieve strong performance without relying on multi billion parameter VLA backbones.

---

## 论文详细总结（自动生成）

# FabriVLA 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机与背景）
- **研究动机**：当前 Vision-Language-Action (VLA) 模型（如 RT-2、π0）通常依赖数十亿参数的大规模骨干网络，虽然性能出色，但计算成本和推理延迟较高，难以满足实时机器人控制的需求。因此，亟需设计**轻量级 VLA 架构**，在保持高性能的同时降低资源消耗。
- **研究目标**：提出 FabriVLA，一种仅约 **0.89B 参数**的紧凑型 VLA 模型，在 Meta-World MT50 多任务操作基准上达到与大型模型相当甚至更优的成绩，证明小规模 VLA 的可行性。

## 2. 论文提出的方法论
### 核心思想
- 采用 **InternVL3.5-1B** 作为视觉-语言骨干，结合**流匹配**生成动作序列，通过**门控自注意力**建模动作令牌间的时序依赖，并引入**浅层 VLM 层融合**增强空间细节。

### 关键技术细节
- **骨干网络**：InternVL3.5-1B，保留 14 层 transformer，输入图像 448×448，任务指令文本与视觉令牌拼接。
- **状态与动作编码**：
  - 机器人状态 `s ∈ R²⁴` 通过两层 MLP 编码为状态令牌 `zs ∈ R¹⁰²⁴`。
  - 噪声动作 `xt ∈ R⁵⁰×²⁴` 通过共享 MLP 编码为动作令牌序列，并添加位置编码。
- **动作头模块**（8 层 FabriVLA TransformerBlock）：
  1. **门控自注意力（Gated Self-Attention）**：动作令牌先经过自注意力，带可学习门控 `g`（初始化为 0），随训练逐渐打开，学习跨时间步依赖。
  2. **交叉注意力到上下文**：动作令牌与 VLM 上下文令牌交互。
  3. **前馈网络与时间条件**：加入流匹配时间步 `t` 的正弦嵌入。
  4. **浅层 VLM 层融合**：将第 6 层（浅层，含空间细节）与第 14 层（深层，语义）特征拼接，通过可学习线性投影融合（初始化为 `[I|0]`，兼容 deep-only 基线）。
- **流匹配训练**：
  - 插值：`xt = (1-t)ε + t a`，`t ~ Beta(2,2)`。
  - 损失：`L(θ) = E[||vθ(xt, t|·) - (a-ε)||²]`。
  - 推理：从均匀噪声开始，Euler 积分 50 步得到动作序列。

## 3. 实验设计
- **数据集**：使用公开的 Evo-1 Meta-World 演示数据集，包含 **50 个任务**，每任务 **50 条轨迹**，共 **2500 条轨迹**。
- **Benchmark**：**Meta-World MT50**，包含 50 个不同操作任务，分为 easy / medium / hard / very hard 四个难度层级。
- **评估指标**：层级平均成功率（tier-average success rate，四个层级成功率的均值）和总体回合成功率（overall episode-level success rate，所有 500 个回合的总成功率）。每任务评估 10 个回合，环境 seed=4042，最大步数 400。
- **对比方法**：TinyVLA (1.3B)、π0 (3.5B)、SmolVLA (2.3B)、RoboTron-Mani (4B)、Evo-1 (0.8B)、Evo-Depth (0.9B)、LA4VLA (1B)。对比方法结果取自原论文。

## 4. 资源与算力
- **GPU 配置**：5 块 **NVIDIA RTX PRO 6000**。
- **训练超参数**：
  - 总步数 S = 100,000，最终使用 93,000 步的 checkpoint。
  - 批大小 B = 40。
  - 学习率 η = 2.0×10⁻⁵，warmup 1,000 步，余弦退火。
  - 梯度裁剪范数 c = 1.0，权重衰减 λ = 10⁻⁴。
  - 优化器：AdamW，混合精度 BF16，使用 DeepSpeed ZeRO-2 并启用 FP32 主权重（避免 BF16 量化噪声导致 VLM 参数更新不足）。
- **训练时长**：文中未明确给出具体时间。

## 5. 实验数量与充分性
- **主要实验**：在 MT50 全部 50 个任务上的完整评估（表 1）。
- **消融实验**：
  - **浅层 VLM 层融合消融**（图 5）：对比 deep-only 基线（仅第 14 层）与 layer-6 融合，测试是否提升精度。
  - **动作头模块消融**（图 6）：在 VLM 冻结、50k 步、无 DeepSpeed FP32 条件下，依次添加门控自注意力（SA）+ 令牌残差头（TR）+ 时序卷积（TC），比较 tier-average 和 overall 成功率。
  - **训练动态**（图 4）：绘制不同 checkpoint（20k~100k）的成功率曲线。
  - **任务需求分组诊断**（表 2）：按操作需求（如平面滑动、关节接触、插入等）和精度导向子集（如紧插入、精确放置）分析性能。
- **充分性评价**：实验较为充分，涵盖了与多个基线的对比、核心组件的消融、训练动态、细粒度任务分析。但**仅在一个基准（Meta-World MT50）上评估，且均基于仿真环境**，未涉及真实机器人实验。数据集仅含 2500 条轨迹，规模较小。对比方法的结果来自原论文，超参数可能未完全对齐，公平性有一定保证但非完全复现。

## 6. 论文的主要结论与发现
- FabriVLA 在 MT50 上达到 **90.0% tier-average 成功率** 和 **92.0% overall episode-level 成功率**，**优于所有对比方法**（包括更大规模的 π0、SmolVLA 等）。
- **门控自注意力是决定性组件**：消融表明其带来最大增益（tier-average 从 48.5% 升至 57.7%）。
- **浅层 VLM 层融合**显著提升所有难度层级，tier-average 从 82.9% 提升至 90.0%。
- 紧凑的 1B 参数模型无需多模态预训练或大量机器人数据即可达到强性能，挑战了“越大越好”的假设。

## 7. 优点
- **轻量高效**：仅 0.89B 参数，推理延迟低，适合实时控制。
- **设计精巧**：
  - 门控自注意力初始化为零，不影响初始训练，逐步学习时序依赖，优化路径平滑。
  - 浅层融合使用 `[I|0]` 初始化，保证与 deep-only 基线兼容，训练后自然提升空间感知。
  - 单阶段联合训练简化流程。
- **实验扎实**：在 50 任务上系统评估，提供按难度和操作需求的细粒度分析，消融对比清晰。
- **开源友好**：代码和模型未明确声明开源，但基于公开数据集和预训练模型，可复现性较好。

## 8. 不足与局限
- **仅仿真验证**：所有实验均在 Meta-World 仿真环境中进行，未在真实机器人上测试，实际部署效果未知。
- **数据量较小**：仅使用 2500 条轨迹（每任务 50 条），数据多样性有限，可能不足以泛化到更复杂场景。
- **未分析不同骨干规模**：仅使用 InternVL3.5-1B，未对比同骨干更大/更小版本的效果，难以分离骨干贡献。
- **训练后期退化**：100k 步 checkpoint 性能低于 93k 步，说明模型可能存在过拟合或优化不稳定，未使用早停。
- **消融设置不一致**：动作头模块消融在 VLM 冻结且无 DeepSpeed 条件下进行，结果绝对值远低于完整模型，相对结论有一定可信度但需谨慎。
- **缺乏跨任务泛化测试**：未评估在未见过的任务或新环境下的零样本/少样本能力。

（完）
