---
title: "Bi-PT: Bidirectional Cross-Attention Point Transformers for Four-Chamber Heart Reconstruction from Sparse Cardiac MRI Data"
title_zh: Bi-PT：用于从稀疏心脏MRI数据重建四腔室心脏的双向交叉注意力点变换器
authors: "Chenchuhui Hu, Shaoming Pan, Leon Axel, Meng Ye"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06923v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学图像分割与转换器模型
tldr: 本研究提出Bi-PT管道，利用双向交叉注意力点变换器从临床稀疏心脏MRI数据重建四腔心网格。通过双向点交叉注意力学习点特征，结合语义标签和神经常微分方程保证变形场局部仿射微分同胚，解决了稀疏点云到完整三维形状的准确推断问题。实验证明其性能优于基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06923v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative comparison with baseline methods on the 100-case test set. CD, EMD, and P2F are reported in mm. Values are mean(std) over each chamber. Red and blue indicate the best and second-best results, respectively. † indicates a statistically significant improvement over the second-best method using a paired Wilcoxon signed-rank test on the same test cases (p < 0.001).\", \"page\": 7, \"index\": 1, \"width\": 719, \"height\": 286}]"
motivation: 临床心脏MRI数据稀疏采样导致三维重建困难，现有方法精度不足。
method: 采用双向交叉注意力点变换器学习点特征，结合语义标签对应估计，用神经常微分方程参数化变形场实现局部仿射微分同胚变形，并加入平滑正则化。
result: 在稀疏点云上实现了准确鲁棒的四腔心网格重建，性能优于基线方法。
conclusion: Bi-PT有效解决了稀疏心脏MRI数据的3D重建问题，提供了准确的微分同胚变形。
---

## 摘要
我们提出了Bi-PT，一种从临床稀疏采样心脏磁共振成像（CMR）数据中重建3D四腔室人类心脏网格的流程。这项工作解决了从常规临床CMR协议中使用的2D长轴和短轴视图提取的稀疏点云（SPC）生成3D心脏形状时容易出错的问题。Bi-PT通过在一个模板和SPC之间通过双向点交叉注意力学习鲁棒的点特征，并结合改进对应估计的逐点语义标签，实现了从SPC准确推断四腔室心脏网格。我们将变形场参数化为一个神经常微分方程（NODE），该方程由逐点仿射变换和平移参数化，以将模板变形为目标心脏形状。通过学习这样的NODE，我们可以保证变形场是局部仿射微分同胚变形。我们还将语义标签损失集成到Chamfer距离中，以鼓励标签一致的对应关系，并添加平滑正则化以稳定和改进变形场的学习。大量实验表明，与基线相比，Bi-PT实现了准确且鲁棒的性能。

## Abstract
We propose Bi-PT, a pipeline for reconstructing 3D four-chamber human heart meshes from clinical sparsely sampled cardiac magnetic resonance imaging (CMR) data. This work addresses the error-prone generation of 3D cardiac shape from a sparse point cloud (SPC) extracted from 2D long-axis and short-axis views used in routine clinical CMR protocols. Bi-PT enables accurate inference of the four-chamber heart mesh from the SPC by learning robust point features via bidirectional point cross-attention between an atlas and the SPC, together with per-point semantic labels that improve correspondence estimation. We formulate the deformation field as a Neural Ordinary Differential Equation (NODE) parameterized by a per-point affine transformation and translation to deform the atlas toward the target heart shape. By learning such a NODE, we can guarantee the deformation field to be a locally affine diffeomorphic deformation. We also integrate a semantic label loss into the Chamfer distance to encourage label-consistent correspondences and add a smoothness regularization to stabilize and improve the learning of the deformation field. Extensive experiments demonstrate that Bi-PT achieves accurate and robust performance compared to baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：临床心脏磁共振成像（CMR）通常采用稀疏采样的2D长轴和短轴视图，由这些视图提取的点云（SPC）非常稀疏，导致从SPC准确重建3D四腔室心脏网格是一个极易出错的任务。
- **研究动机**：现有方法（如传统形状模型或语义点云配准）在处理极度稀疏且结构复杂的四腔心脏时精度不足，尤其无法保证变形场的拓扑保持性和微分同胚性。
- **整体含义**：提出Bi-PT管道，利用双向交叉注意力点变换器结合神经常微分方程，首次实现从临床稀疏CMR数据到高质量四腔心脏网格的鲁棒且微分同胚的变形重建，有望辅助心脏疾病诊断与手术规划。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将重建问题视为从模板网格到目标稀疏点云的变形场学习，通过双向交叉注意力增强点特征，利用逐点语义标签改善对应估计，并用神经常微分方程（NODE）参数化变形场保证局部仿射微分同胚性。
- **关键技术细节**：
  - **双向点交叉注意力（Bidirectional Point Cross-Attention）**：在模板（atlas）和稀疏点云（SPC）之间执行双向注意力，使模板点学习SPC中的局部和全局结构信息，同时SPC点从模板中获得完整的形状先验。
  - **语义标签辅助对应**：为模板和SPC中的点赋予逐点语义标签（如左心室、右心室、心房等），并将标签损失（如交叉熵）整合到Chamfer距离中，鼓励语义一致的对应。
  - **神经常微分方程（NODE）参数化变形场**：将变形场建模为NODE，其微分方程由逐点仿射变换矩阵和平移向量参数化。通过求解ODE，模板点沿连续流形变形为目标形状，保证变形场是局部仿射微分同胚（即拓扑保持、可逆、光滑）。
  - **平滑正则化**：在变形场学习中加入平滑性约束，稳定优化过程并避免过度扭曲。
- **算法流程**（文字说明）：
  1. 输入：模板网格（带有语义标签的完整四腔心脏）和临床CMR提取的稀疏点云（附有语义标签）。
  2. 将两者输入双向交叉注意力模块，输出增强后的点特征。
  3. 基于增强特征，估计模板到SPC的逐点仿射变换参数（旋转、缩放、剪切和平移）。
  4. 将仿射参数作为NODE的驱动项，通过数值积分（如欧拉法）得到从模板到目标形状的连续变形场。
  5. 利用变形后的模板网格计算Chamfer距离与语义标签损失，联合优化网络参数。
  6. 推理时，对模板施加学习到的NODE变形，输出最终四腔心脏网格。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：使用临床CMR数据（论文提及“100-case test set”作为测试集，训练集规模未具体说明，可能来自公开或私有数据集）。数据集包含从2D长轴和短轴视图提取的稀疏点云，以及对应的完整心脏网格真值（或许通过手动或半自动标注获得）。
- **基准（Benchmark）**：未明确指定标准公开基准，但实验采用了与多种基线方法在相同测试集上的定量比较。
- **对比方法**：包括基线方法（表1中与Chamfer Distance、Earth Mover's Distance、Point-to-Face等指标对比）。具体基线名称未在摘要中列出，但从“与基线相比”可知至少包含传统形状模型或点云配准方法。此外，可能包含消融实验对比有无双向注意力、有无语义标签、有无平滑正则化等情况。

### 4. 资源与算力

- **未明确说明**：论文摘要及元数据未提及使用的GPU型号、数量、训练时长、内存等资源信息。根据领域常规，可能使用单张或少量高端GPU（如NVIDIA V100/A100），训练时间可能在数小时到一天内。但此处应按要求指出未明确说明。

### 5. 实验数量与充分性

- **实验数量**：论文提供了在100例测试集上的定量对比（表1），列出平均和标准差。另外提及了“与基线相比”的统计显著性检验（Wilcoxon符号秩检验，p<0.001）。可能还包括消融实验（语义标签损失、平滑正则化等的影响），但摘要未详细列出数量。
- **充分性评估**：实验设计相对充分（测试集100例，统计检验严谨），但缺乏对不同稀疏程度、不同噪声水平、跨数据集泛化等场景的深入分析。若仅依赖单一数据集和有限的基线方法，客观性可能受限于数据分布。整体上属于初步验证，但未覆盖临床变异性。

### 6. 论文的主要结论与发现

- 提出的Bi-PT管道能从临床稀疏CMR数据中准确且鲁棒地重建四腔心脏网格，在Chamfer距离、Earth Mover距离、点到面距离等指标上优于基线方法，且差异具有统计显著性。
- 双向交叉注意力能有效融合模板先验与稀疏观测，提升点特征鲁棒性。
- 将变形场参数化为NODE保证局部仿射微分同胚，避免了常见变形中的拓扑错误。
- 语义标签对应和平滑正则化进一步提升了重建精度和变形质量。

### 7. 优点

- **方法论创新**：首次将双向交叉注意力点变换器与NODE结合用于稀疏心脏网格重建，解决了微分同胚性这一关键难点。
- **对应鲁棒性**：通过双向注意力和语义标签，即使SPC极度稀疏也能建立合理对应。
- **可解释性**：变形场可微分且拓扑保持，便于临床验证和后续分析。
- **实验验证充分**：使用100例测试集进行定量统计检验，指标全面（CD、EMD、P2F）。

### 8. 不足与局限

- **实验覆盖有限**：仅使用单数据集（100例），未见跨不同医院、不同扫描参数、不同疾病谱的泛化实验。
- **稀疏模式单一**：仅采用临床2D长轴+短轴视图的标准稀疏模式，未考虑更极端稀疏（如单视图）或噪声情况。
- **计算开销未讨论**：未提供训练/推理时间、内存消耗等，实际部署可行性不明。
- **缺乏与深度学习端到端方法的直接对比**：基线可能较传统，未与近期基于Transformer/隐式函数的完整重建方法对比。
- **应用限制**：依赖模板网格的语义标签，需要预先定义心脏腔室边界，对先天性畸形等异质性可能适应不足。

（完）
