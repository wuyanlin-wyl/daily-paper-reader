---
title: Screening Is Effective for Visual Recognition
title_zh: 筛选对视觉识别是有效的
authors: "Shunya Shimomura, Kazuhiro Hotta"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13983v1"
tags: ["query:vlmmed"]
score: 6.0
evidence: ViT筛选机制以排除无关块
tldr: 针对Vision Transformer自注意力难以显式排除背景或冗余patch的问题，借鉴语言模型中的Screening机制，提出VisionScreen模型，在二维空间基于查询-键相似度独立评估每个patch的相关性，并通过阈值排除低相关patch，使每个patch只聚合内容与空间相关patch。图像分类实验表明该方法优于传统ViT，为视觉识别提供了一种替代softmax注意力进行相对特征聚合的新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13983v1/fig-001.webp\", \"caption\": \"Figure 4. Visualization of interactions from a specific patch (marked with ×) to all patches.\", \"page\": 6, \"index\": 1, \"width\": 471, \"height\": 633}, {\"url\": \"assets/figures/arxiv/2607.13983v1/fig-002.webp\", \"caption\": \"Figure 6. Learned screening window values across layers and heads. The horizontal axis denotes the layer index, and the vertical axis denotes the screening window size. Only a small number of heads have very large windows, while many heads preserve local or mid-range spatial aggregation even in deeper layers.\", \"page\": 6, \"index\": 2, \"width\": 496, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2607.13983v1/fig-003.webp\", \"caption\": \"Figure 1. Overview of VisionScreen.\", \"page\": 2, \"index\": 3, \"width\": 496, \"height\": 620}, {\"url\": \"assets/figures/arxiv/2607.13983v1/fig-004.webp\", \"caption\": \"Figure 2. Validation loss curves of ViT-Tiny/16 and VisionScreen on ImageNet-1k and CIFAR-100.\", \"page\": 4, \"index\": 4, \"width\": 496, \"height\": 667}, {\"url\": \"assets/figures/arxiv/2607.13983v1/fig-005.webp\", \"caption\": \"Figure 3. Visualization of CLS-to-patch interactions. For ViT, we visualize softmax attention weights. For VisionScreen, we visualize relevance scores before value aggregation.\", \"page\": 5, \"index\": 5, \"width\": 471, \"height\": 632}]"
motivation: 自注意力机制对所有patch分配权重，无法显式排除不相关的背景或冗余patch，导致特征聚合引入不必要的噪声。
method: 将Screening机制扩展到视觉领域，在二维网格上基于查询-键相似度独立评估每个patch的相关性，并通过阈值排除低相关patch，实现选择性聚合。
result: 在图像分类基准上，VisionScreen的性能优于传统的Vision Transformer。
conclusion: Screening机制能有效用于视觉识别，为基于softmax注意力的相对特征聚合提供了替代方案。
---

## 摘要
视觉Transformer（ViT）已被广泛用作对图像块间全局依赖关系进行建模的强大框架。然而，其核心组件自注意力机制为所有块分配了经softmax归一化的相对权重，这使得难以独立评估块之间的相关性。在视觉识别中，图像通常包含许多背景或冗余块，而自注意力无法明确拒绝此类不相关块，这可能会在特征聚合中引入不必要的信息。为解决这一局限，语言建模领域提出了筛选机制，其中每个标记的相关性基于查询-键相似性独立评估，并通过阈值化显式排除低相关性标记。在本工作中，我们提出了VisionScreen，一种将筛选机制扩展到视觉识别的新型视觉模型。VisionScreen将图像块视为排列在二维网格上的标记，并将基于查询-键相似性的绝对相关性估计扩展到二维空间域。这使得每个块能够选择性地仅聚合内容上和空间上相关的块，而无需依赖块间的竞争。在图像分类基准上的实验表明，所提方法优于传统ViT。这些结果表明筛选对视觉识别有效，为基于softmax注意力的相对特征聚合提供了一种替代方案。

## Abstract
Vision Transformer (ViT) has been widely used as a powerful framework for modeling global dependencies among image patches. However, its core component, self-attention assigns softmax-normalized relative weights to all patches, making it difficult to evaluate the relevance between patches independently. In visual recognition, images often contain many background or redundant patches, yet self-attention cannot explicitly reject such irrelevant patches, which may introduce unnecessary information into feature aggregation. To address this limitation, Screening has been proposed in the field of language modeling, where the relevance of each token is independently evaluated based on query-key similarity and low-relevance tokens are explicitly excluded through thresholding. In this work, we propose VisionScreen, a new vision model that extends Screening mechanism to visual recognition. VisionScreen treats image patches as tokens arranged on a two-dimensional grid and extends absolute relevance estimation based on query-key similarity to the two-dimensional spatial domain. This allows each patch to selectively aggregate only content-wise and spatially relevant patches without relying on competition among patches. Experiments on image classification benchmarks demonstrate that the proposed method outperforms conventional ViT. These results suggest that Screening can be effective for visual recognition, offering an alternative to relative feature aggregation based on softmax attention.