---
title: "CRISP: Constrained Refinement via Iterative Squeezing Process for Robust Medical Image Segmentation under Domain Shift"
title_zh: CRISP：通过迭代挤压过程实现域偏移下鲁棒医学图像分割的约束精炼
authors: "Yizhou Fang, Pujin Cheng, Yixiang Liu, Xiaoying Tang, Longxi Zhou"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15231v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 域偏移下的鲁棒医学图像分割
tldr: "分布偏移是医学AI临床转化的关键瓶颈。现有域适应方法受限于模拟偏移或伪监督，难以应对开放真实世界的无限偏移。本文提出CRISP框架，基于“正区域秩稳定性”假设，通过扰动不变性推导鲁棒空间先验（高精度核心与高召回支持），递归细化分割。在心脏MRI和CT肺血管多中心、人口统计和模态偏移下，CRISP显著超越SOTA，HD95降低最高达38.9%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15231v1/fig-001.webp\", \"caption\": \"Table 2: Quantitative evaluation across different clinical scenarios. (S): Source domain, (T): Target domain. Dice score (%) ↑ and HD95 (pixel) ↓. Bold = best result. ⋆ = Methods that cannot access the target domain. # = Methods that need the target domain to update parameters (need more information, need retraining). Remarkably, our source-only (⋆) method even outperforms the deployment-time adaptation methods (#), despite using no target-domain data. Source-domain (S) columns are reported to verify source-performance preservation; the target-adaptive methods (#) adapt only on the target split, so their source scores coincide with the source model.\", \"page\": 6, \"index\": 1, \"width\": 1044, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2607.15231v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative segmentation results of different methods. The top and bottom rows present the segmentation performance under modality shift (Non-contrast) and demographic shift (COVID Cohort), respectively.\", \"page\": 6, \"index\": 2, \"width\": 1044, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2607.15231v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of CRISP, a rank-based segmentation framework rather than probability-driven prediction: predict → estimate rank-stability → get HP and HR → predict again with HP and HR → estimate rank-stability again → better HP and HR → · · · → HP and HR converge to final segmentation. (a) Three representative clinical shifts: demographic, modality, and protocol variations. (b) Empirical Rank Stability: the relative rank of foreground regions remains stable despite probability drifts across domains. (c) Latent Feature Perturbation: injecting Gaussian noise into bottleneck features to simulate domain shift, yielding stochastic rank maps. Across perturbations, consistently top-graded voxels form stable high-probability (MHP ) core, while persistently non-background voxels form the stable non-background region (MHR). (d) Recursive Self-Evolution: guided by these hints, successive model generations progressively squeeze the ambiguity region between MHP and MHR, progressively bringing the two boundaries closer until convergence forms the final segmentation.\", \"page\": 3, \"index\": 3, \"width\": 1054, \"height\": 600}]"
motivation: 现有域适应方法因依赖模拟偏移或伪监督，无法应对开放未知的真实世界分布偏移。
method: CRISP框架利用正区域秩稳定性假设，通过特征扰动推导扰动不变的高精度核心与高召回支持，递归细化分割。
result: "在不同医学影像域偏移任务中，CRISP的HD95降低最高达0.14、1.90、8.39像素，相对提升7.0%、13.1%、38.9%。"
conclusion: CRISP是一种无需目标域数据或测试时更新的插件式细化框架，显著提升了医学图像分割在域偏移下的鲁棒性。
---

## 摘要
医学影像中的分布偏移仍然是医学AI临床转化的核心瓶颈。未能解决该问题会导致在未见环境中性能严重下降，并加剧健康不平等。现有的域适应方法本质上受限于通过模拟偏移或伪监督穷举预定义可能性。这些策略在面对开放且不可预测的真实世界时难以奏效，因为分布偏移实际上是无限的。为应对这一挑战，我们采用“正区域的秩稳定性”作为分布偏移下的工作假设，并据此为仅源域分割推导鲁棒的空间提示。在该假设的指导下，我们提出CRISP，一个模型无关的框架，与部署时适应不同，它不需要测试时参数更新，也不需要目标域数据——一个无目标、即插即用的精炼框架，使用冻结权重进行分割。CRISP并非直接利用排序输出掩膜，而是利用分布偏移下概率排序的稳定性来推导鲁棒的先验空间信息。通过潜在特征扰动，扰动不变的高等级区域定义了一个高精度（HP）核心，而在至少一次扰动下仍可能为前景的体素则定义了一个高召回率（HR）支持；这些双先验随后在扰动下递归精炼。我们设计了一个迭代训练框架，逐步将HP和HR挤压至最终分割。在多中心心脏MRI和基于CT的肺血管分割上的广泛评估表明，CRISP具有卓越的鲁棒性，在多中心、人口统计和模态偏移下，分别显著优于最先进方法，HD95降低高达0.14像素（提升7.0%）、1.90像素（提升13.1%）和8.39像素（提升38.9%）。

## Abstract
Distribution shift in medical imaging remains a central bottleneck for the clinical translation of medical AI. Failure to address it can lead to severe performance degradation in unseen environments and exacerbate health inequities. Existing methods for domain adaptation are inherently limited by exhausting predefined possibilities through simulated shifts or pseudo-supervision. Such strategies struggle in the open-ended and unpredictable real world, where distribution shifts are effectively infinite. To address this challenge, we adopt the "Rank Stability of Positive Regions" as a working assumption under distribution shift, and use it to derive robust spatial hints for source-only segmentation. Guided by this assumption, we propose CRISP, a model-agnostic framework that, unlike deployment-time adaptation, requires no test-time parameter updates and no target-domain data--a target-free, plug-in refinement framework that segments with frozen weights. Rather than using ranking to directly output masks, CRISP exploits the stability of probability rankings under distribution shift to derive robust spatial priors. Via latent feature perturbation, perturbation-invariant high-grade regions define a high-precision (HP) core, while voxels that remain potentially foreground under at least one perturbation define a high-recall (HR) support; these dual priors are then recursively refined under perturbation. We then design an iterative training framework that progressively squeezes HP and HR toward the final segmentation. Extensive evaluations on multi-center cardiac MRI and CT-based lung vessel segmentation demonstrate CRISP's superior robustness, significantly outperforming state-of-the-art methods with striking HD95 reductions of up to 0.14 (7.0% improvement), 1.90 (13.1% improvement), and 8.39 (38.9% improvement) pixels across multi-center, demographic, and modality shifts, respectively.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）
- **核心问题**：医学影像分割模型因训练域与部署域之间的分布偏移（multi-center, demographic, modality shift）而性能严重下降，阻碍临床转化。
- **现有方法局限**：训练时间泛化（模拟偏移）和部署时间适应（更新参数）均受限于“穷举预定义可能性”，无法应对真实世界中无限且不可预测的偏移。
- **本文贡献**：提出CRISP框架，依赖 **“正区域秩稳定性”** 假设（即前景区域的相对排序在分布偏移下保持稳定），无需目标域数据或测试时参数更新，实现源仅（source-only）鲁棒分割。

### 论文提出的方法论
- **核心思想**：利用秩稳定性而非绝对概率来抵御偏移。通过特征扰动模拟域变化，识别扰动下保持稳定的体素作为高精度（HP）核心，以及在所有扰动下可能为前景的体素作为高召回（HR）支持；然后递归缩小HP与HR之间的不确定性区域，直至收敛到最终分割。
- **关键技术细节**：
  1. **潜在特征扰动**：在编码器瓶颈处注入高斯噪声（\(\xi\sim\mathcal{N}(0,\sigma^2 I)\)，\(N\)次采样），得到多个概率图\(P^{(n)}\)，转换为log-odds后量化成\(L\)个等级（\(G_c^{(n)}\)）。
  2. **双先验生成**：
     - 高精度核心 \(M_{HP,c} = \bigcap_{n} \{G_c^{(n)} = L-1\}\)（所有扰动下均为最高等级）
     - 高召回支持 \(M_{HR,c} = \bigcup_{n} \{G_c^{(n)} > 0\}\)（至少一次扰动下非背景）
     - 不确定性区域 \(\Omega_u = M_{HR} \setminus M_{HP}\)
  3. **递归自我进化框架**：
     - 将\(M_{HP}, M_{HR}\)作为额外通道与图像拼接，输入分割网络。
     - 从平凡提示（\(M_{HP}=0, M_{HR}=全图\)）开始，经多代训练（每一代由上一代模型生成更紧的HP/HR），最终模型可脱离提示直接递归推理。
     - 每代训练使用不同深度提示的平衡混合（depth 0,1,...,k-1）。
  4. **不确定性挤压损失**：
     \[
     \mathcal{L}_{\text{squeeze}} = \frac{1}{|\Omega_u|+\epsilon} \sum_{n=1}^{N} \left\| (P_k - P_k^{(n)}) \odot \Omega_u \right\|^2
     \]
     仅在不确定性区域强制扰动一致性，加速边界收敛。
  5. **总损失**：\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{dice}} + \alpha_d \mathcal{L}_{\text{squeeze}}\)，其中\(\alpha_d\)对depth=0样本为零。

### 实验设计
- **数据集与场景**：
  1. **Multi-center shift**：M&Ms心脏MRI（四个厂商A/B/C/D，源域A，目标域B/C/D，分为LV/MYO/RV三结构）。
  2. **Modality shift**：CT肺血管（增强CT→非增强CT）。
  3. **Demographic shift**：CT肺血管（正常→COVID）。
- **基准方法**：
  - 源仅方法：FedDG, DDG-Med（域泛化）
  - 目标感知方法（需目标域参数更新）：IPLC（SFDA）, TEGDA（TTA）
  - 参考：Baseline（源模型直接测试），A+T Joint（源域+单个目标域联合训练）
- **评价指标**：Dice (%), HD95 (pixel)
- **实现细节**：分割网络使用DeepLabv3+（MobileNetV2骨干），单张TITAN RTX GPU，M&Ms训练60k迭代（batch size 10），CT数据集batch size 35。CRISP超参数：\(N=10, \sigma=0.35, L=5, \alpha=0.5\)。

### 资源与算力
- **明确说明**：所有实验单张TITAN RTX GPU（24GB显存）。
- **未明确说明**：总训练时长（小时/天）、各方法具体耗时未报告。CRIPS推理约为6倍单次前向传播（迭代约3步，每步两次前向）。
- **可复现性**：使用固定随机种子控制所有随机源。

### 实验数量与充分性
- **实验组数**：
  - 三组独立数据集实验（表1: M&Ms三个目标域×3结构，表2: 两个真实临床场景）
  - 消融实验（表3: 三种场景下基线、仅RSE、全CRISP）
  - 收敛性分析（图3: 代际Dice/HD95变化）
  - 最差case分析（Dice分布、失败率）
  - 定性对比（图2）
- **充分性评价**：覆盖三种主要偏移类型，对比了最先进源仅和目标感知方法，消融验证了各组件贡献。主结果由单次训练产生（固定种子可复现），A+T参考和IPLC使用5折交叉验证。实验量充分，比较公平（源仅 vs 源仅，目标感知需更多信息）。但未在不同骨干网络（如Swin UNet）或更多数据集上验证泛化性。

### 论文的主要结论与发现
- CRISP在所有场景下均显著优于源仅Baseline，且**在不访问目标域的情况下匹配甚至超过多数目标感知方法**。
- 在最具挑战的非增强CT（modality shift）上，Dice从33.26提升至59.46，HD95从29.97降至13.19（降幅8.39像素，38.9%）。
- 消除长尾失败：Baseline中36.4% volume的Dice<0.30，CRISP降至0%；失败率（Dice<0.50）从97.4%降至6.5%。
- 收敛迅速（约3代），且Unsqueeze Loss显著加速边界挤压。
- 源域性能不降反升（Normal: +2.59 Dice, Contrast: +3.76 Dice）。

### 优点
- **方法论创新**：基于秩稳定性假设，区别于传统概率/伪标签范式，提供全新鲁棒分割视角。
- **实用性强**：无需目标域数据、无需测试时更新、模型无关（可即插即用于任何分割网络）。
- **鲁棒性强**：在多种极端偏移下均稳定提升，尤其擅长处理灾难性失败。
- **实验设计全面**：覆盖三种典型偏移，对比方法全面（含源仅和目标感知），消融、收敛、最坏情况分析详实。

### 不足与局限
- **计算开销**：推理需约6倍前向，对实时性要求高的场景可能受限。
- **网络通用性**：仅在DeepLabv3+上验证，未在其他主流架构（如UNet、Transformer）上测试。
- **扰动模拟**：仅使用高斯噪声，可能未涵盖所有真实偏移模式（如解剖变异、标注噪声）。
- **训练复杂度**：世代训练需多次完整训练（每代稳定后生成新提示），总训练成本较高。
- **可扩展性**：仅在两个特定任务（心脏MRI、肺血管CT）上评估，大规模多器官或多种成像模态待验证。
- **伦理/公平性**：虽提及可减少健康不平等，但未在不同种族/地理人群上直接验证。

（完）
