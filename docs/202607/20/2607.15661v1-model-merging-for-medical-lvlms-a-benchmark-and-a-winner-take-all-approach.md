---
title: "Model Merging for Medical LVLMs: A Benchmark and a Winner-Take-All Approach"
title_zh: 医学大型视觉-语言模型的模型合并：基准测试与赢者通吃方法
authors: "Lichao Mou, Shilan Zhang, Chunlei Li, Bingcong Yan, Jingliang Hu, Yilei Shi, Shengwu Xiong, Xiao Xiang Zhu, Lei Li, Yaxiong Chen"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15661v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学LVLM合并的基准和方法
tldr: 大型视觉语言模型通过LoRA微调可适应多种医学成像任务，但部署多个专家模型计算开销大。本文首次系统研究医学LVLM模型合并，提出基准MergeMedBench（8种模态、16个LoRA模型），并设计无超参的Winner-Take-All方法，仅保留各专家中最主导的参数，避免平均或对齐策略的信息稀释。实验表明，该方法简单且一致优于现有合并方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15661v1/fig-001.webp\", \"caption\": \"Table 1. Comparison of model merging methods based on Qwen-VL. We report accuracy (%) across eight imaging modalities (CT, MRI, X-ray, ultrasound, dermoscopy, fundus photography, OCT, and microscopy), along with average accuracy and runtime.\", \"page\": 6, \"index\": 1, \"width\": 718, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2607.15661v1/fig-002.webp\", \"caption\": \"Table 2. Comparison of model merging methods based on InternVL.\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2607.15661v1/fig-003.webp\", \"caption\": \"Fig. 1. Modality (left) and task (right) distributions of the evaluation set.\", \"page\": 3, \"index\": 3, \"width\": 711, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2607.15661v1/fig-004.webp\", \"caption\": \"Fig. 3. Examples of our merged model on multiple modalities and tasks.\", \"page\": 9, \"index\": 4, \"width\": 721, \"height\": 258}]"
motivation: 部署多个医学专家LVLM模型计算和运营开销大，模型合并是无需重训的解决方案，但此前在医学领域缺乏系统性研究。
method: 提出Winner-Take-All方法，通过保留各LoRA专家中最主导的参数、丢弃较弱参数，避免信息稀释，且无需超参数。
result: 在包含8种成像模态和16个LoRA模型的MergeMedBench上，Winner-Take-All一致优于现有的平均、对齐等合并策略。
conclusion: Winner-Take-All方法为医学LoRA模型合并提供了新视角和强基线，简单有效且无需调参。
---

## 摘要
大型视觉-语言模型（LVLMs）可以通过参数高效微调方法（如低秩适应LoRA）适应专门的医学成像任务，从而产生一个日益增长的专家模型生态系统，这些模型针对特定的成像模态和临床场景。然而，在实践中部署多个专家LVLMs会带来大量的计算和操作开销。模型合并提供了一种有前景的解决方案，通过将多个专家合并为一个单一模型而无需重新训练，但在医学领域尚未得到充分探索。在这项工作中，我们首次对医学LVLMs的模型合并进行了系统性研究。我们引入了MergeMedBench，一个涵盖八种成像模态和多种临床任务类型的综合基准测试，包含基于两种主流架构构建的16个LoRA微调模型。我们对现有合并方法进行了广泛评估，并进一步提出了一种简单且无需超参数的方法——赢者通吃，该方法仅保留专家模型中最主要的参数。通过保留控制模型行为的关键参数并丢弃较弱参数，我们的方法避免了基于平均或对齐策略中固有的信息稀释。尽管简单，赢者通吃始终优于现有方法，为LoRA合并提供了新的视角，并为未来研究提供了强大的实践基线。

## Abstract
Large vision-language models (LVLMs) can be adapted to specialized medical imaging tasks via parameter-efficient fine-tuning approaches such as low-rank adaptation (LoRA), leading to a growing ecosystem of expert models tailored to specific imaging modalities and clinical scenarios. However, deploying multiple expert LVLMs in practice incurs substantial computational and operational overhead. Model merging provides a promising solution by consolidating multiple experts into a single model without retraining, yet it remains largely unexplored in the medical domain. In this work, we present the first systematic study of model merging for medical LVLMs. We introduce MergeMedBench, a comprehensive benchmark spanning eight imaging modalities and diverse clinical task types, comprising 16 LoRA fine-tuned models built upon two mainstream architectures. We conduct an extensive evaluation of existing merging methods and further propose winner-take-all, a simple and hyperparameter-free approach that retains only the most dominant parameters across expert models. By preserving the critical parameters that govern model behavior and discarding weaker ones, our method avoids the information dilution inherent in averaging- or alignment-based strategies. Despite its simplicity, winner-take-all consistently outperforms existing approaches, offering both a new perspective on LoRA merging and a strong practical baseline for future research.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：大型视觉-语言模型（LVLMs）通过低秩适应（LoRA）等参数高效微调方法，可以适应专门的医学成像任务，从而形成一个多样化的“专家模型”生态系统。然而，在实际应用中同时部署多个专家模型会带来巨大的计算和运营开销。因此，一个核心问题是如何将多个专家模型高效地合并成一个单一模型，使其在所有任务上表现良好且无需重新训练。
- **背景**：模型合并是自然语言处理和计算机视觉领域的一个新兴方向，已有多种方法（如任务向量平均、TIES-Merging、DARE等），但它们在医学LVLMs中的系统研究尚属空白。医学影像具有多模态、任务复杂、数据分布差异大等特点，现有通用合并方法的有效性未知。
- **核心问题**：能否设计一种简单、有效且无需超参数调优的LoRA合并方法，在医学LVLMs场景中优于现有方法？

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：观察到LoRA参数中，只有一小部分“主导参数”对模型行为起决定性作用，修改这些参数会导致性能急剧下降；而非主导参数的影响很小。因此，合并时应该保留每个参数位置上最“主导”的专家参数，而不是通过平均或对齐来稀释它们。
- **关键技术细节**：
  - **问题形式化**：给定预训练模型参数θ0，对N个下游任务分别用LoRA进行微调，得到N组低秩矩阵{(Aⁿ, Bⁿ)}。合并目标是得到单一的Δθ<sup>merged</sup>。
  - **赢者通吃（Winner-Take-All）方法**：
    1. **定义评分函数**：用于衡量每个参数位置上的“主导程度”。论文采用归一化幅度：
       \[
       S(\Phi^{(n)}_{l,M,i,j}) = \frac{\Phi^{(n)}_{l,M}[i,j] - E[\Phi^{(n)}]}{\sqrt{Var[\Phi^{(n)}] + \epsilon}}
       \]
       其中Φ∈{A, B}，E和Var分别表示该低秩矩阵所有元素的均值和方差，ε为小常数。
    2. **赢家选择**：对于每个低秩矩阵的每个位置，选择评分最高的专家：
       \[
       n^* = \arg\max_n S(\Phi^{(n)}_{l,M,i,j})
       \]
    3. **直接赋值**：合并后的参数直接取该赢家的原始值，不做任何缩放或变换：\(\Phi^{\text{merged}}[i,j] = \Phi^{(n^*)}[i,j]\)。
    4. **最终合并**：对所有层的所有低秩矩阵重复上述操作，得到B<sup>merged</sup>和A<sup>merged</sup>，然后计算ΔW<sup>merged</sup> = B<sup>merged</sup>A<sup>merged</sup>，加到预训练模型上。
  - **特点**：无超参数、无需梯度、计算开销极低（仅需归一化和比较）。

### 3. 实验设计
- **数据集与场景**：使用OmniMedVQA数据集的开放子集，包含88,995个图像-问题-答案三元组，涵盖8种成像模态：CT、MRI、X-ray、超声、皮肤镜、眼底摄影、OCT、显微镜。任务类型包括：模态识别、解剖识别、临床诊断、病变分级、生物学属性分析。固定训练/测试划分：71,333训练，17,662评估。
- **基准（MergeMedBench）**：在两种主流LVLM架构（Qwen3-VL-2B-Instruct和InternVL2-1B）上，为每个模态分别微调一个LoRA专家模型，共16个专家模型。所有专家作为合并的输入。
- **对比方法**：
  - 简单基线：Task Arithmetic (TA)
  - 基于修剪/符号冲突解决：TIES-Merging、DARE-TIES
  - 基于子空间：Isotropic Merging (Iso-C, Iso-CTS)、STF、KnOTS及其变体、Core Space Merging及其变体
  - 基于方向鲁棒性：RobustMerge
  - 所有方法均为无梯度方法，保证公平对比。
- **评价指标**：归一化准确率（每个模态合并后准确率除以该模态单独微调模型准确率），以及平均准确率和合并时间。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量、训练时长、合并时间等硬件资源细节。仅报告了合并方法的运行时间（秒级），例如Winner-Take-All在Qwen-VL上仅需0.21秒，但未提供微调过程的算力消耗。因此，对于训练和合并所需的算力，文中信息不完整。

### 5. 实验数量与充分性
- **主要实验**：在两组架构（Qwen-VL和InternVL）上分别对8种模态进行合并评估，报告了每个模态的准确率、平均准确率和时间。对比了10余种现有方法及其变体。
- **消融实验**：
  - 赢家选择策略（k=1到8）：证明k=1最优，支持“保留最主导参数”的假设。
  - 赢者通吃 vs. 简单平均：WTA 90.88% vs. 平均72.18%，验证了方法优势。
  - 将WTA与子空间方法结合：Core+WTA和KnOTS+WTA提升显著，表明方法可推广到子空间。
  - 归一化方式比较（无归一化、min-max、RMS、本文方法）：证明归一化的重要性，本文方法最优。
- **充分性**：实验覆盖了8个模态、2个架构、多种对比方法，并进行了必要的消融和组合分析，设计较为充分。但未进行跨模型大小或跨数据集的外部验证，也未提供统计显著性检验。

### 6. 论文的主要结论与发现
- 首次系统研究医学LVLMs的模型合并问题，并发布基准MergeMedBench。
- 提出的赢者通吃（Winner-Take-All）方法在Qwen-VL上达到90.88%平均准确率，在InternVL上达到84.64%，均优于所有现有方法，且计算时间极短。
- 揭示主导参数的重要性：保留主导参数比分平均或对齐更有效。
- 子空间方法结合WTA可显著提升性能，表明该方法具有通用性。

### 7. 优点
- **方法简单高效**：无需超参数、无需梯度、计算开销极小（0.2秒级），易于部署。
- **视角新颖**：从“参数主导性”出发，颠覆了平均或对齐的合并范式，提供了新思路。
- **实验设计全面**：涵盖8个模态、2个架构、10余种对比方法，消融实验丰富，验证了核心假设。
- **开源贡献**：提供了MergeMedBench基准和代码，便于后续研究。

### 8. 不足与局限
- **评分函数依赖幅度**：虽然归一化改进，但幅度不一定完全反映参数对任务的重要性，未来可探索基于梯度或Fisher信息的评分。
- **未考虑任务冲突处理**：WTA在参数位置直接选择赢家，但对于严重冲突的任务（如某些参数在不同任务中都很重要但方向相反），可能导致单一任务性能下降。论文仅在InternVL上的超声模态（62.15%）明显低于其他方法，显示潜在局限。
- **实验覆盖有限**：仅使用两个中等规模模型（2B和1B），未验证在更大模型（如7B、13B）上的表现；仅用OmniMedVQA数据集，未在其他医学VQA数据集上验证泛化性。
- **缺乏统计显著性分析**：未报告置信区间或显著性检验，无法判断性能差异是否显著。
- **算力信息缺失**：未说明训练专家模型所需的GPU型号、数量和训练时长，降低了复现的便利性。

（完）
