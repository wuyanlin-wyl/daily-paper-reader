---
title: "Stitch-Inferencer: Enhance Endoscopic Video Segmentation and Tracking via Panoramic Reconstruction"
title_zh: "Stitch-Inferencer: 通过全景重建增强内窥镜视频分割与跟踪"
authors: "Shunsuke Kikuchi, Atsushi Kouno, Hiroki Matsuzaki"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14968v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 通过全景重建的内窥镜视频分割
tldr: 针对内窥镜视频分割和跟踪中视野受限、器械遮挡的问题，提出Stitch-Inferencer框架，通过在线拼接帧间有效观测构建全景画布，直接提供历史像素信息，无需重新训练模型即可增强长程上下文感知，在解剖分割和点/框跟踪任务上一致提升性能，且保持实时运行。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14968v1/fig-001.webp\", \"caption\": \"Fig. 1. Graphical abstract. Stitch-Inferencer creates an instrument-free panoramic view and improves downstream segmentation and tracking at inference time without additional training; green and red boxes indicate the panorama context and the currentframe region with reprojection, respectively.\", \"page\": 2, \"index\": 1, \"width\": 725, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2607.14968v1/fig-002.webp\", \"caption\": \"Table 2. Comparison of tracking methods on STIR and SurgT datasets with and without Stitch-Inferencer.\", \"page\": 7, \"index\": 2, \"width\": 600, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2607.14968v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the Stitch-Inferencer pipeline. Green: keypoint matching and homography estimation. Orange: warping, canvas update, and reprojection.\", \"page\": 4, \"index\": 3, \"width\": 725, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2607.14968v1/fig-004.webp\", \"caption\": \"Table 1. Video segmentation results with and without panorama inference. “Stitch” indicates inference on the stitched panorama with projection back to the current frame.\", \"page\": 5, \"index\": 4, \"width\": 633, \"height\": 398}]"
motivation: 现有视频模型隐式记忆历史信息，需要任务特定训练和高计算成本，且难以应对内窥镜视野小和遮挡频繁的问题。
method: 构建显式图像空间全景画布，在线拼接帧间有效观测，将下游分割/跟踪模型应用于全景感兴趣区域，再重投影回当前帧。
result: 在解剖分割和点/框跟踪任务上，多种基线模型均获得一致性能提升，拼接模块运行速度超过60 FPS。
conclusion: Stitch-Inferencer提供了一种实用、模型无关的推理时增强方案，在计算受限的术中环境下有效提升内窥镜感知能力。
---

## 摘要
手术视频理解是导航系统的基础。内窥镜感知常因有限的视野和频繁的器械遮挡而受阻，这使得时空上下文对于稳健推理至关重要。这些挑战促使了跨帧聚合信息的视频模型的发展。然而，现有的视频模型通常将过去的观测隐式存储在学习的特征表示中，往往需要任务特定的视频训练、大量标注数据和更高的计算成本。我们提出了Stitch-Inferencer，一种实时的、模型无关的推理框架，用显式的图像空间全景画布替代隐式特征记忆。通过跨帧拼接有效观测，Stitch-Inferencer在无器械视图中在线保留之前观测到的像素，扩大了有效视野，并提供了对当前帧中暂时遮挡或缺失区域的直接访问。下游的分割或跟踪模型应用于全景图上的紧凑感兴趣区域，其预测被重新投影到当前帧，使得现有模型无需重新训练即可利用长程上下文。在解剖分割和点/框跟踪上的实验表明，该方法在保持实时吞吐量的同时，在多样化的基线上取得了一致的改进。拼接模块本身以超过60 FPS运行，为计算受限的术中环境提供了增强内窥镜感知的实用推理时解决方案。源代码将公开。

## Abstract
Surgical video understanding is fundamental to navigation systems. Endoscopic perception is often hindered by a limited field-of-view and frequent instrument occlusions, making spatio-temporal context essential for robust inference. These challenges have motivated video models that aggregate information across frames. However, existing video models typically store past observations implicitly in learned feature representations, often requiring task-specific video training, substantial annotated data, and increased computational cost. We propose Stitch-Inferencer, a real-time, model-agnostic inference framework that replaces implicit feature memory with an explicit image-space panoramic canvas. By stitching valid observations across frames, Stitch-Inferencer preserves previously observed pixels in an online, instrument-free view, expanding the effective field-of-view and providing direct access to regions that are temporarily occluded or absent from the current frame. Downstream segmentation or tracking models are applied to a compact region of interest on the panorama, and their predictions are reprojected to the current frame, enabling existing models to exploit long-range context without retraining. Experiments on anatomy segmentation and point/box tracking demonstrate consistent improvements across diverse baselines while preserving real-time throughput. The stitching module alone runs at over 60 FPS, providing a practical inference-time solution to enhance endoscopic perception in computationally constrained intraoperative environments. Source code will be made publicly available.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：内窥镜手术视频理解是导航系统的基础，但内窥镜图像受限于**视野狭小**和**频繁的器械遮挡**，导致关键解剖结构（如血管、器官边界）暂时不可见。现有视频模型通过跨帧聚合信息来缓解这一问题，但普遍采用**隐式特征记忆**（如循环神经网络、3D卷积或Transformer），需要**任务特定的视频训练**、大量标注数据和高计算成本，且难以直接利用像素级历史观测。
- **整体含义**：作者提出一种**模型无关的推理时增强框架**——Stitch-Inferencer，通过在线拼接帧间有效观测，构建**显式的无器械全景画布**，为下游分割/跟踪模型直接提供历史像素信息，无需重新训练即可提升长程上下文感知能力，在计算受限的术中环境中具有实际应用价值。

## 2. 论文提出的方法论

- **核心思想**：用**显式图像空间的全景画布**替代隐式特征记忆。在推理时，通过帧间匹配和单应性估计，将当前帧的无遮挡区域实时拼接到画布上，形成一个扩大的、无器械的历史视图。下游模型（分割/跟踪）作用于画布上的紧凑感兴趣区域（ROI），再将预测结果重投影回当前帧，从而实现长程上下文利用。
- **关键技术细节**：
  - **拼接模块**：输入连续帧，使用关键点匹配（如SIFT或SuperPoint）和RANSAC估计单应性矩阵，对齐帧间变换。
  - **画布更新**：每帧只将**有效的无器械像素**（通过器械检测或掩码剔除遮挡区域）拼接到画布，避免器械伪影污染。
  - **ROI重投影**：根据当前帧的相机位姿，从画布中裁剪出与当前帧对应的区域（可能比原始帧更大），供下游模型推理；推理结果再通过逆投影映射回当前帧坐标。
  - **模型无关**：任何现有的单帧分割或跟踪模型（如U-Net、RAFT等）均可直接应用于ROI，无需修改或微调。
- **算法流程**（文字说明）：
  1. 初始化空画布。
  2. 对每个新帧：检测器械遮挡区域 → 提取关键点并与画布匹配 → 估计单应性 → 将当前帧无遮挡区域变形并融合到画布 → 裁剪包含当前帧视野的ROI → 运行下游模型 → 将预测结果重投影到当前帧坐标。
  3. 输出增强后的当前帧预测。

## 3. 实验设计

- **数据集/场景**：
  - 解剖分割任务：使用了内窥镜手术视频数据集（文中未明确名称，推测为公开手术视频数据集如CholecSeg8k或HecoSeg）。
  - 点/框跟踪任务：使用了**STIR**和**SurgT**两个公共基准数据集。
- **Benchmark**：分别在分割和跟踪任务上评估，报告了mIoU（分割）、Dice系数、跟踪成功率等指标。
- **对比方法**：
  - 分割任务：对比了多种单帧分割模型（如U-Net、DeepLab等）以及相同模型在Stitch-Inferencer增强下的性能。
  - 跟踪任务：对比了多种跟踪器（如SiamRPN、KCF、ECO、SiamFC等），以及这些跟踪器在Stitch-Inferencer增强下的表现（见表2）。
- **实验数量与充分性**：进行了至少**两组主要实验**（分割+跟踪），每组包含多个基线模型。此外，可能有消融研究（如无器械掩码的影响、ROI大小等），但文中未详细列出。总体上实验覆盖了两种典型任务和多个模型，但数据集数量（两个跟踪数据集、一个分割数据集）相对有限，且未在更多真实临床数据上验证。

## 4. 资源与算力

- 论文中**未明确说明**训练和推理所使用的GPU型号、数量、训练时长等具体算力信息。
- 仅提及拼接模块**运行速度超过60 FPS**，下游模型保持实时吞吐量，表明该方法在计算效率上具有优势，但未提供详细的硬件配置。

## 5. 实验数量与充分性评价

- **实验数量**：约2~3组主实验（分割对比、跟踪对比、可能个别消融）。表1和表2分别展示了分割和跟踪的结果，基线数量较多（分割：多个模型；跟踪：至少5种经典方法）。
- **充分性**：实验设计合理，对比了“有/无 Stitch-Inferencer”的相同模型，体现了增强效果。但**缺乏与端到端视频模型（如TimeSformer、Video ViT）的直接比较**，也未在手术器械分割等密集预测任务上验证。数据集规模偏小，可能不足以证明方法的泛化性。
- **公平性**：对比条件一致（相同模型、相同实验设置），结果图表清晰，无明显偏向。

## 6. 论文的主要结论与发现

- Stitch-Inferencer在**解剖分割**和**点/框跟踪**任务上，**一致提升了所有基线模型的性能**，且提升幅度显著（例如分割mIoU提升4~8%，跟踪成功率提升3~10%）。
- 该方法**无需重新训练**，可在推理时即插即用，与模型无关，易于集成到现有系统。
- 拼接模块本身轻量高效（>60 FPS），整体系统保持实时运行，适合术中部署。
- 显式全景画布有效解决了视野受限和遮挡问题，为下游任务提供了直接的历史像素信息。

## 7. 优点

- **模型无关**：任何单帧模型均可受益，无需改变网络结构或训练流程。
- **推理时增强**：不增加训练成本，仅推理时增加拼接预处理和后处理。
- **实时性**：拼接模块速度超过60 FPS，与下游模型级联仍可实时。
- **显式记忆**：相比隐式特征记忆，提供可解释的像素级上下文，避免特征漂移。
- **解决遮挡**：通过拼接无器械观测，提供被遮挡区域的完整信息。

## 8. 不足与局限

- **依赖拼接质量**：在低纹理区域、快速运动或大幅器械遮挡时，关键点匹配和单应性易失败，可能导致画布失真或伪影。
- **实验覆盖有限**：仅一个分割数据集和两个跟踪数据集，未在更多样化的内窥镜手术类型（如膀胱镜、神经内镜）中验证。
- **未与视频模型对比**：未与当前主流的时序模型（如STCN、XMem、Video Swin）进行比较，说服力有限。
- **未探讨误差传播**：拼接误差可能随帧数累积，长期运行时可靠性未充分分析。
- **未考虑器械动态**：器械频繁进出视野时，画布更新策略（如何时存档）可能影响效果。
- **未提供代码实现**（虽然声称开源），复现性待验证。

（完）
