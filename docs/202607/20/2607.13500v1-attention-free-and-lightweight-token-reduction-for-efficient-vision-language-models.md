---
title: Attention-Free and Lightweight Token Reduction for Efficient Vision-Language Models
title_zh: 面向高效视觉-语言模型的无注意力轻量级令牌缩减
authors: "Xuanyi Hao, Zuoyuan Zhang, Zhibo Wang, Xiaoyi Pang, Jiahui Hu, Jiacheng Du, Shuguo Zhuo"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13500v1"
tags: ["query:vl"]
score: 9.0
evidence: 面向视觉语言模型的无注意力令牌减少
tldr: 针对视觉语言模型在边缘设备上因大量视觉token导致计算开销大的问题，本文提出一种注意力无关的轻量级token减少框架。通过信息熵评估token重要性保留表达性强的token，利用变换一致性信号实现轻量级多样性选择，在保持竞争性能的同时显著加速推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13500v1/fig-001.webp\", \"caption\": \"TABLE I: Performance on LLaVA-v1.5. Values outside parentheses denote the original accuracy (%) on TextVQA, ScienceQA, and GQA, or the original total score on MME. Values in parentheses denote the corresponding performance relative to the vanilla model, whose performance is normalized to 100%. The average is computed over the normalized results.\", \"page\": 6, \"index\": 1, \"width\": 1076, \"height\": 663}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative visualization of retained visual tokens in the ablation study.\", \"page\": 10, \"index\": 2, \"width\": 531, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-003.webp\", \"caption\": \"Fig. 5: Sensitivity of our method to the balancing coefficient λ under different visual-token budgets.\", \"page\": 10, \"index\": 3, \"width\": 502, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-004.webp\", \"caption\": \"TABLE IV: OOM-free generation boundary on Jetson AGX Orin under a 16GB available-memory budget.\", \"page\": 9, \"index\": 4, \"width\": 524, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-005.webp\", \"caption\": \"Fig. 3: Ablation study results.\", \"page\": 9, \"index\": 5, \"width\": 498, \"height\": 312}]"
motivation: 现有token减少方法依赖注意力图或密集成对比较，与加速框架不兼容且计算开销大，需一种轻量级、即插即用的注意力无关方案。
method: 采用熵基准则衡量token信息量保留重要token，并通过变换一致性信号排序后步长采样确保多样性，实现无注意力机制的轻量级token减少。
result: 在多个视觉语言模型基准上，该方法在激进压缩下仍能维持竞争性能，实现了良好的准确率与效率权衡。
conclusion: 所提框架作为即插即用模块，兼顾重要性与多样性，有效加速VLM推理且不依赖注意力机制。
---

## 摘要
视觉-语言模型（VLM）在多模态理解中取得了强劲性能，但由于处理大量视觉令牌带来的巨大计算开销，在资源受限的边缘设备上部署仍具挑战。令牌缩减是加速VLM推理的一个有前景的方向，但现有方法要么依赖与现代加速框架不兼容的注意力图，要么依赖计算密集型的成对相似性比较，这削弱了可扩展性并抵消了其在实际部署中的优势。本文提出了一种无注意力且轻量级的令牌缩减框架，作为VLM的即插即用模块，该框架保留重要且多样化的令牌以生成紧凑的视觉表示。首先，为了实现无注意力的重要性估计，我们从信息论视角出发，采用一种新颖的基于熵的标准量化令牌信息，保留那些具有更具表达性且更少退化特征表示的令牌。其次，为了以轻量级方式确保视觉覆盖的多样性，我们引入了一种变换诱导的一致性信号，使得相似令牌产生相似信号，从而通过排序使相似令牌彼此靠近，并实现基于步长的选择以生成多样化的令牌集。在多个VLM基准上的大量实验表明，我们的框架实现了良好的精度-效率权衡，在激进压缩下仍保持有竞争力的性能。

## Abstract
Vision-Language Models (VLMs) have achieved strong performance in multimodal understanding, yet remain challenging to deploy on resource-constrained edge devices due to the substantial computational overhead of processing numerous visual tokens. Token reduction is a promising direction for accelerating VLMs inference, but existing approaches either rely on attention maps that are incompatible with modern acceleration frameworks or depend on computationally intensive pairwise similarity comparisons, which undermine scalability and negate their practical benefits in deployment. In this paper, we propose an attention-free and lightweight token reduction framework as a plug-and-play module for VLMs, which preserves both important and diverse tokens to produce a compact visual representation. First, to enable attention-free importance estimation, we adopt an information-theoretic perspective and quantify token information using a novel entropy-based criterion, retaining those with more expressive and less degenerate feature representations. Second, to ensure diverse visual coverage in a lightweight manner, we introduce a transformation-induced consistency signal where similar tokens yield similar signals, such that sorting by this signal places similar tokens close to each other and enables stride-based selection to produce a diverse token set. Extensive experiments across multiple VLMs benchmarks demonstrate that our framework achieves a favorable accuracy-efficiency trade-off, maintaining competitive performance under aggressive compression.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：视觉-语言模型（VLM）处理大量视觉 token（数百至数千）导致巨大的计算开销，尤其在资源受限的边缘设备上部署困难。现有 token 缩减方法要么依赖注意力图（不兼容 FlashAttention 等加速框架），要么需要昂贵的成对 token 相似性比较（复杂度高），限制了实际效率提升。
- **目标**：设计一种**无注意力（attention-free）、轻量级（lightweight）、即插即用**的视觉 token 缩减框架，在保持多模态推理性能的同时显著加速推理，并兼容现代系统级加速技术。

### 2. 论文提出的方法论

#### 核心思想
- **一次性 token 选择**，同时考虑**重要性**和**多样性**，生成紧凑且富有表现力的视觉 token 子集。
- **不依赖注意力图或成对比较**，仅利用视觉编码器自身输出的特征统计量和 MLP 变换行为。

#### 关键技术细节

1. **Stage I：基于二阶 Rényi 熵的重要性感知选择**
   - 将每个 token 的特征向量沿通道维度归一化为概率分布 \( p_{i,j} = |x_{i,j}| / \sum_k |x_{i,k}| \)。
   - 计算二阶 Rényi 熵 \( H_2(x_i) = -\log \sum_j p_{i,j}^2 \)。
   - 熵值越高，表示特征分布更均匀，token 包含更丰富、更少退化的信息。选择熵最高的 \( K_1 \) 个 token 作为重要性子集 \( X_{\text{imp}} \)。

2. **Stage II：基于变换一致性信号的多样性感知选择**
   - 对剩余 token，利用视觉编码器最后一层 MLP 的输入 \( x'_i \) 和输出 \( x''_i \)，计算余弦相似度 \( s_i = \cos(x'_i, x''_i) \)。
   - 相似 token 会产生相似的 \( s_i \) 值，因此按 \( s_i \) 排序后，通过**步长采样**（stride-based sampling）从有序列表中均匀选取 \( K_2 \) 个 token，确保输入空间的广泛覆盖，形成多样性子集 \( X_{\text{div}} \)。

3. **最终输出**：合并 \( X_{\text{imp}} \) 和 \( X_{\text{div}} \)，总 token 数为 \( K = K_1 + K_2 \)，由超参数 \( \lambda \) 控制比例 \( K_1 = \lambda K \)。

- **算法流程**（文字描述）：
  1. 从视觉编码器获取 N 个 token。
  2. 计算每个 token 的 Rényi 熵，选 top-K1 作为 X_imp。
  3. 对剩余 token，从视觉编码器最后一层 MLP 获取变换前后的表示，计算余弦相似度 s_i。
  4. 按 s_i 升序排序，计算步长 t = |X_rem| / K2，每隔 t 个取一个 token，直至凑齐 K2 个。
  5. 返回 X_imp ∪ X_div。

- **时间复杂度**：\( O(ND + N\log N) \)，远低于成对比较方法（\( O(N^2D) \) 或 \( O(N^3D) \)）。

### 3. 实验设计

#### 数据集与基准
- **LLaVA-v1.5-7B**：MME（总分）、TextVQA（准确率）、ScienceQA（准确率）、GQA（准确率）。
- **LLaVA-NeXT**（高分辨率多子图，2880 token）：GQA 数据集。
- **Qwen2.5-VL-7B**：TextVQA 数据集。

#### 对比方法
- **Inner-LM**：SparseVLMs（ICML 2025）、PyramidDrop（CVPR 2025）、MustDrop（arXiv）。
- **Before-LM**：VisionZip（CVPR 2025）、VisPruner（ICCV 2025）。
- 所有方法在相同 token 预算下比较（保留 192/128/64 token，对应原始 576 token 的 33.3%/22.2%/11.1%）。

#### 其他实验
- **效率分析**：FLOPs 和 CUDA 时间（单张 NVIDIA RTX 5880）。
- **兼容性测试**：Qwen2.5-VL 启用 FlashAttention-2 前后的推理延迟和峰值内存。
- **边缘部署测试**：NVIDIA Jetson AGX Orin（16GB 内存），测量无 OOM 的最大生成长度。
- **消融实验**：去除重要性模块或多样性模块的性能影响。
- **超参数 λ 敏感性**：λ 在 0~1 范围变化下的性能曲线。

### 4. 资源与算力

- **效率分析**：使用**单张 NVIDIA RTX 5880** 测量 FLOPs 和 CUDA 时间，但未明确提及模型训练所需算力（该方法为 training-free，无需训练）。
- **边缘测试**：基于 **NVIDIA Jetson AGX Orin**（16GB 可用内存）。
- **未提及**：模型微调或基线方法的训练资源（因为所有方法均为即插即用或已有预训练模型）。

### 5. 实验数量与充分性

- **充分性评估**：
  - 覆盖**三种主流 VLM 架构**（LLaVA-v1.5、LLaVA-NeXT、Qwen2.5-VL）。
  - **三种 token 保留预算**（192/128/64），全面评估不同压缩程度。
  - **四个基准数据集**（MME、TextVQA、ScienceQA、GQA），涵盖感知、OCR、科学推理、视觉问答等能力。
  - 与**五个代表性基线**对比，包括 Inner-LM 和 Before-LM 两类方法。
  - 进行了**消融实验**（重要性 vs 多样性贡献）、**超参数 λ 分析**、**效率比较**、**兼容性测试**、**边缘部署 OOM 边界测试**。
- **公平性**：所有 Before-LM 方法在相同 token 预算下评估；超参数 λ 统一为相同值；报告归一化性能（以全 token 为 100%）。实验设计较为客观。

### 6. 论文的主要结论与发现

1. **ALTR 在多种压缩率下均保持高准确率**：
   - LLaVA-v1.5 保留 192 token 时平均保留 98.86% 性能，128 token 时 97.95%，64 token 时 94.52%，均优于所有对比方法。
   - 在 LLaVA-NeXT 和 Qwen2.5-VL 上同样取得最佳或接近最佳结果。
2. **效率优势显著**：
   - FLOPs 仅 4.13M（VisionZip 106.91M，VisPruner 2880.35M），CUDA 时间 0.579 ms。
3. **完全兼容 FlashAttention-2**，使视觉编码器加速约 5 倍，推理预填充阶段加速约 3 倍。
4. **在边缘设备上有效扩展 OOM-free生成长度**（从 768 token 提升至 1536 token），且缩减延迟最低（约 70 ms）。
5. **消融验证**：重要性和多样性模块互为补充，联合使用达到最佳性能。

### 7. 优点

- **方法创新**：首次将二阶 Rényi 熵用于无注意力 token 重要性评估，利用 MLP 变换一致性作为轻量级多样性代理。
- **轻量高效**：时间复杂度 \( O(ND + N\log N) \)，完全避免成对比较和注意力图计算，适合边缘部署。
- **兼容性强**：不依赖任何注意力操作，可直接与 FlashAttention 等现代加速框架协同工作。
- **即插即用**：不需要训练或微调，直接作用于预训练 VLM 的视觉编码器输出。
- **实验全面**：覆盖多种架构、数据集、压缩率，并包含效率、兼容性、边缘部署等多种评估视角。

### 8. 不足与局限

- **静态预算**：token 保留数量 K 和平衡参数 λ 需预先设定，未提供自适应选择机制。
- **依赖 MLP 层**：多样性信号依赖于预训练视觉编码器最后一层 MLP 的结构和权重，若视觉编码器无 MLP 或设计不同，可能需调整。
- **未考虑文字 token 交互**：token 缩减完全在视觉侧进行，未利用文本指令信息指导选择（与某些 Inner-LM 方法不同，但这也是其兼容性优势的来源）。
- **实验范围**：仅评估了 7B 规模模型，未在更大规模 VLM（如 13B/70B）上验证。同时未涉及视频或多图场景。
- **消融实验仅在一组配置下进行**：128 token 下，未展现不同压缩率下的消融趋势。
- **缺乏对“性能下降”的统计分析**：报告的是平均值，未提供方差或最差情况分析。

（完）
