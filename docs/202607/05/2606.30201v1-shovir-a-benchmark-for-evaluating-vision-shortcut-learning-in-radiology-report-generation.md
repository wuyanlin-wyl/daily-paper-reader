---
title: "SHOVIR: A Benchmark for Evaluating Vision Shortcut Learning in Radiology Report Generation"
title_zh: SHOVIR：评估放射学报告生成中视觉捷径学习的基准
authors: "Filippo Ruffini, Marco Salmé, Rosa Sicilia, Valerio Guarrasi, Paolo Soda"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30201v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于评估放射学报告生成中视觉捷径学习的VLMs基准
tldr: 针对放射学报告生成中视觉语言模型可能利用虚假相关性而非真实病理证据的问题，提出SHOVIR基准，通过遮挡实验测试模型的空间基础能力。评估八个模型发现，高报告质量不一定意味着好的视觉基础，揭露了当前评估协议的盲点。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30201v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of the SHOVIR benchmark and evaluation protocol.\", \"page\": 3, \"index\": 1, \"width\": 861, \"height\": 532}, {\"url\": \"assets/figures/arxiv/2606.30201v1/fig-002.webp\", \"caption\": \"Table 1. Image-level evaluation on MIMIC-CXR and PadChest-GR. Baseline: performance on clean images. Full Noise Image (blue): performance when the entire image is replaced with noise. ∆FN (gray): absolute drop relative to baseline. Per metric, the best baseline is in bold; the highest and lowest ∆ are in bold and underlined, respectively.\", \"page\": 6, \"index\": 2, \"width\": 929, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2606.30201v1/fig-003.webp\", \"caption\": \"Figure 2. Disease-level perturbation analysis on MIMIC-CXR (top row) and PadChest-GR (bottom row). Each curve reports the µ-F1 (weighted by class distribution) as a function of the perturbation ratio p for three experimental conditions: RO (left panel), OCO (center panel), and DOCO (right panel).\", \"page\": 7, \"index\": 3, \"width\": 709, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2606.30201v1/fig-004.webp\", \"caption\": \"Figure 3. Per-pathology F1 breakdown (a) and per-model delta scores (b) on MIMIC-CXR and PadChest-GR.\", \"page\": 8, \"index\": 4, \"width\": 1034, \"height\": 404}]"
motivation: 现有评估指标无法检测模型是否真正依赖病理视觉证据，允许模型通过视觉捷径获得高分。
method: 扩展两个胸部X光数据集，设计图像级和疾病级遮挡实验，对比干净图像与局部扰动下的预测，分离直接和上下文捷径。
result: 不同架构和数据集上的捷径行为差异显著；报告质量高的模型在空间基础能力上不一定排名靠前。
conclusion: 临床流畅生成可能伴随浅层视觉证据依赖，需引入区域感知评估协议。
---

## 摘要
当前放射学报告生成中视觉-语言模型的评估协议依赖于报告级指标，这些指标衡量词汇重叠或总体临床正确性。然而，此类指标并不检验单个诊断陈述是否源于图像中可见的实际病理证据。这使得模型能够通过利用学习到的先验或虚假相关性来获得具有竞争力的分数，我们将这种失败模式称为“视觉捷径”。我们引入了SHOVIR，这是一个评估放射学报告生成中视觉捷径行为的基准。SHOVIR扩展了两个带有空间标注的胸部X射线数据集MIMIC-CXR和PadChest-GR，提供了每个边界框的CheXpert标签，并定义了图像级和疾病级遮挡实验，将干净图像上的基线性能与局部、区域特定的扰动进行对比。通过比较这些条件下的预测，可以在疾病类别层面分离出两种失败模式：直接捷径（当视觉证据被移除后，某个发现仍然存在）和上下文捷径（当共病的病理被遮挡后，尽管目标区域保持完整，但检测能力下降）。通过对八种最先进的视觉-语言模型进行基准测试，我们发现捷径行为在不同的架构和数据集之间存在显著差异。达到最高基线报告质量的模型在空间接地方面并不一定排名最高，这表明临床流畅的生成可以与对视觉证据的浅层依赖共存。这些发现揭示了当前放射学报告生成评估中的盲点，并推动了区域感知的评估协议。

## Abstract
Current evaluation protocols for Vision-Language Models (VLMs) in Radiology Report Generation (RRG) rely on report-level metrics that measure lexical overlap or aggregate clinical correctness. However, such metrics do not test whether individual diagnostic statements stem from the actual pathological evidence visible in the image. This allows models to achieve competitive scores by exploiting learned priors or spurious correlations, a failure mode we refer to as vision shortcut. We introduce SHOVIR, a benchmark for evaluating vision shortcut behavior in RRG. SHOVIR extends two spatially annotated chest X-ray datasets, MIMIC-CXR and PadChest-GR, with per-box CheXpert labels, and defines image-level and disease-level occlusion experiments that contrast baseline performance on clean images against localized, region-specific perturbations. Comparing predictions across these conditions isolates two failure modes at the disease-class level: direct shortcuts, where a finding persists after its visual evidence is removed, and contextual shortcuts, where detection degrades once co-occurring pathologies are occluded despite the target region remaining intact. Benchmarking eight state-of-the-art VLMs, we find that shortcut behavior varies substantially across architectures and datasets. Models achieving the highest baseline report quality do not necessarily rank highest in spatial grounding, revealing that clinically fluent generation can coexist with shallow reliance on visual evidence. These findings expose a blind spot in current RRG evaluation and motivate region-aware assessment protocols.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前放射学报告生成（RRG）的评估指标（如BLEU、ROUGE、F1-CheXbert、GREEN等）均基于报告级别的词汇重叠或临床标签匹配，无法检验模型是否真正从图像中的病理区域提取诊断证据。模型可能利用“视觉捷径”——即依赖训练数据中的虚假相关性（如疾病共现模式、报告模板、强度分布等）而非真实视觉特征——来获得高基准分数。
- **整体含义**：这种评估盲点会导致临床部署风险：一个生成流畅报告但实际“看”不到病灶的模型，可能在分布外数据上失效。论文旨在系统量化这一现象，并推动区域感知的评估协议。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过**基于空间标注的遮挡实验**，隔离模型在不同条件下的诊断性能变化，从而区分“真正依赖视觉证据”与“依赖捷径”。
- **关键技术细节**：
    - **数据集扩展**：将两个胸部X光数据集（MIMIC-CXR + ChestImaGenome、PadChest-GR）的原始边界框转化为统一的CheXpert标签空间。通过规则映射+放射科医师验证+CheXbert一致性过滤，得到每个图片的病理-区域关联。
    - **四种实验条件**：
        - **图像级**：原始图像（Baseline）vs 全图替换为匹配强度分布的高斯噪声（Full Noise Image）。定义ΔFN = F1(Baseline) - F1(Full Noise)，衡量整体视觉依赖。
        - **疾病级**：针对每个目标疾病类c：
            - **随机遮挡（RO）**：在不覆盖目标区域或共病区域的位置插入噪声，作为控制条件。
            - **目标类遮挡（OCO）**：遮挡所有包含c的边界框Bc。
            - **不同类遮挡（DOCO）**：遮挡所有不包含c但与其他病理重叠的盒子B̄c（且与Bc无空间重叠）。
            - 使用混合强度 p ∈ {0%,20%,40%,60%,80%,100%} 线性插值噪声与原始像素，固定随机种子。
    - **捷径指标**：
        - ∆OCO = μ-F1(RO@100%) − μ-F1(OCO@100%)：越大表示模型越依赖目标区域视觉证据（没有捷径）。
        - ∆DOCO = μ-F1(RO@100%) − μ-F1(DOCO@100%)：越大表示模型依赖共病上下文（上下文捷径）。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：
    - **MIMIC-CXR**：经ChestImaGenome扩展后获得2,160张测试图像，包含13个可定位CheXpert类（有边界框）。
    - **PadChest-GR**：经映射和过滤后获得1,390张图像，包含8个类别，共1,997个标注区域。
- **基准（Benchmark）**：SHOVIR提供的标准评估协议，包括上述遮挡实验和计算∆指标的代码。
- **对比方法**：8种代表性VLM，涵盖不同架构和训练范式：
    - CheXagent-2, CXRMate, Libra-v1, LLaVA-Rad, MAIRA-2, MedGemma, RaDialog, NV-Reason-CXR。
    - 所有模型只接受**单张正面胸片**作为输入，使用作者提供的默认推理配置（提示、预处理、解码策略），以保证公平。

### 4. 资源与算力
- **文中未明确说明**具体使用的GPU型号、数量或训练时长。仅在补充材料中提及推理的计算成本（表S6），例如每个模型在处理测试集时的大致时间，但未涉及训练资源。作者仅采用预训练模型的推理过程，不重新训练。

### 5. 实验数量与充分性
- **实验组数**：
    - 图像级实验：2个数据集 × 8个模型 = 16组基线+16组全噪声，报告F1₁₄、F1₅、F1-RG、GREEN、BLEU-1/4、ROUGE-L等。
    - 疾病级实验：每个数据集上，对每个疾病类c，进行RO/OCO/DOCO三组，并测量p从0到100%的6个点（p=0为基线，p=100%为完全遮挡）。共16条μ-F1曲线（见图2）。
    - 捷径分析：每个模型计算∆OCO和∆DOCO，按疾病类别加权。
    - 每病理F1分解（图3a）：平均所有模型在每个疾病上的表现。
- **充分性与公平性**：
    - 数据集经过严格的标注验证（放射科医师人工审核一致性，CheXbert自动校验），消除标签噪声。
    - 控制条件RO用于排除一般噪声干扰。
    - 保持输入统一、推理配置默认，避免人为偏向。
    - 实验覆盖了两种不同语言/标注风格的数据集，增加了泛化性检验。
    - 消融方面：还做了遮挡类型消融（补充§B）和提示敏感性分析（补充§C.3），验证方法稳健性。
    - 总体充分，客观，公平。

### 6. 论文的主要结论与发现
- **核心发现**：所有八个VLM均表现出不同程度的视觉捷径。**报告质量最高的模型（如MAIRA-2、CXRMate）并不一定拥有最强的空间接地性**，反而在∆OCO上排名靠后。
- **具体结论**：
    - CheXagent-2在两个数据集上均展示最高的∆OCO和最低的∆DOCO，即依赖直接视觉证据、很少依赖上下文。
    - LLaVA-Rad的∆FN最小，即在全噪声下仍能维持较高F1，表明高度依赖统计先验而非像素内容。
    - 上下文捷径显著：如Cardiomegaly在MIMIC-CXR上因DOCO下降程度接近OCO，表明模型靠共病位置预测。
    - 捷径行为不是疾病固有的，而是数据集共现结构和报告风格的产物。
- **临床启示**：仅凭报告级高分不能保证模型真正“看见”病灶；需引入区域感知评估。

### 7. 优点：方法或实验设计上的亮点
- **创新性**：首次系统地诊断RRG中的视觉捷径，并提出了可操作的量化框架（直接vs上下文）。
- **设计精巧**：通过RO控制、p渐变、固定种子等设计，隔离了区域遮挡带来的特异性影响，非简单模糊。
- **标注严谨**：对两个不同来源的数据集进行统一映射和多重验证，保证了基准的可靠性。
- **可复现性**：提供了元数据生成代码和标准化评估脚本（将开源）。
- **广度与深度**：覆盖8个模型、多个疾病类、两个独立数据集，并给出每类病理的细粒度分析。

### 8. 不足与局限
- **局限性**：
    - **噪声替代**：当前使用加性高斯噪声模拟遮挡，可能引入人眼不可见的伪影或改变低层次统计特征，未必等价于真实病理缺失。
    - **标注覆盖有限**：PadChest-GR只有部分图像有多类边界框，导致DOCO实验子集偏小；MIMIC-CXR也仅利用了约一半的测试集。
    - **模型选择**：仅评估8个模型，且均为开箱即用推理，不包含训练阶段的分析（如数据增强对捷径的影响）。
    - **输入限制**：故意省略了MAIRA-2等模型设计的额外临床输入（如病史、前后对比），可能低估了某些模型的设计优势。
    - **因果性弱**：遮挡仅能揭示相关性，无法断言模型究竟如何利用特征（需反事实生成进一步验证）。
    - **单模态**：仅针对胸部X光，不直接推广到其他解剖部位或成像类型。
    - **计算消耗**：未提供所有模型在统一硬件下的时间消耗对比，难以评估实际部署可行性。

（完）
