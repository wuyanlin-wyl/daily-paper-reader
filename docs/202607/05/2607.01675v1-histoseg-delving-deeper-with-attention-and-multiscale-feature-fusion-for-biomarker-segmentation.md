---
title: "HistoSeg++: Delving deeper with attention and multiscale feature fusion for biomarker segmentation"
title_zh: HistoSeg++：利用注意力机制和多尺度特征融合深入研究生物标志物分割
authors: "Saad Wazir, Rao Faizan, Daeyoung Kim"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01675v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 使用注意力和多尺度融合的生物标志物分割
tldr: 针对生物标志物分割中多尺度信息捕获和上采样不足的问题，本文提出一种新的Nested-UNet架构，通过内外注意力单元、通道特征重新校准（SE模块）和边缘感知损失，提升分割性能。在三个公开数据集上的实验表明，其泛化能力优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01675v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative results of the proposed work and comparison of existing methods. The best results are highlighted in bold, while the second best results are underlined.\", \"page\": 2, \"index\": 1, \"width\": 506, \"height\": 767}, {\"url\": \"assets/figures/arxiv/2607.01675v1/fig-002.webp\", \"caption\": \"Figure 1: An overview of the proposed architecture (a) Outer-UNet (b) SE Block. (c) I-UNet Block with k = 4 (d) Bridge Block.\", \"page\": 3, \"index\": 2, \"width\": 1056, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2607.01675v1/fig-003.webp\", \"caption\": \"Figure 2: Qualitative Results Comparison\", \"page\": 5, \"index\": 3, \"width\": 529, \"height\": 394}]"
motivation: 现有方法在多尺度信息捕获和上采样方面存在不足，导致泛化能力差。
method: 提出Nested-UNet架构，引入内外注意力单元、SE模块进行通道特征重新校准，并采用边缘感知损失强调边界精度。
result: 在三个公开数据集上测试，分割性能优于现有Nested-UNet方法。
conclusion: 所提方法通过多尺度特征融合和注意力机制，有效提升了生物标志物分割的泛化能力。
---

## 摘要
在生物信息学或生物医学应用中，医学图像中生物标志物的分割通常被视为医学图像分析的第一步。尽管已有进展，现有方法仍难以在多尺度上捕获信息，并在不同数据集上有效执行上采样。这些缺陷往往导致泛化能力欠佳。最近，属于Nested-UNet系列架构在捕获多尺度上下文信息并有效上采样方面表现出色。在这项工作中，我们提出了一种新颖的Nested-UNet架构，能够有效捕获多尺度上下文信息。它包括内部和外部注意力单元，以增强上采样过程中的聚焦能力，同时利用挤压-激励模块进行通道特征重新校准，从而提升分割性能。此外，该架构集成了边缘感知损失，通过赋予边缘区域更高的重要性来强调边界精度。我们在三个公开可用的基准数据集上进行了广泛测试。我们的方法展示了优于现有Nested-UNet方法的泛化性能。代码：https://github.com/saadwazir/histosegplusplus

## Abstract
Segmentation of biomarkers in medical images is frequently viewed as a first step towards medical image analysis in any bioinformatics or biomedical application. Despite progress, existing methods still struggle to capture information at multiple scales and to perform upsampling effectively across different datasets. These shortcomings often result in suboptimal generalization capabilities. Recently, architectures belonging to the Nested-UNet family excel in capturing multiscale contextual information and upsample them effectively. In this work, We propose a novel Nested-UNet architecture that effectively captures multi-scale contextual information. It includes inner and outer attention units to enhance focus during upsampling, along with channel-wise feature recalibration using squeeze-and-excitation modules, leading to improved segmentation performance. Additionally, the architecture integrates an edge-aware loss to emphasize boundary accuracy by assigning greater importance to edge regions. Tested extensively on three publicly available benchmark datasets. Our method demonstrates a generalization performance superior to existing Nested-UNet methods. Code: https://github.com/saadwazir/histosegplusplus

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在医学图像生物标志物分割任务中，现有方法（包括UNet及其变体、Transformer模型）难以同时有效捕获多尺度上下文信息，并在不同数据集间实现高效的上采样，导致泛化能力不足。
- **背景**：生物标志物分割是生物医学图像分析的关键第一步。Nested-UNet系列（如UNet++、U2-Net）在捕获多尺度信息上表现出色，但仍有改进空间；Transformer模型虽能建模长距离依赖，但往往丢失局部细节，且依赖大规模数据。
- **研究动机**：设计一种能够深度整合多尺度特征、利用注意力机制强化上采样过程、并注重边界精度的Nested-UNet架构，以提升分割性能与泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 采用**分层嵌套的编码器-解码器结构（Outer-UNet）**，内部嵌入多个**Inner-UNet模块**，实现从局部细节到全局上下文的多尺度特征融合。
- 引入**内外注意力单元**：在每次上采样时通过注意力门控（Attention Gate）聚焦重要特征，过滤噪声。
- 使用**挤压-激励模块（SE Block）** 进行通道特征重新校准，增强有用通道的贡献。
- 引入**边缘感知损失（Edge-aware Loss）**，通过Sobel算子提取真实边缘，对边界区域的分类错误赋予更高权重，提升分割边界精度。

### 关键技术细节
- **Inner-UNet (I-UNet) Blocks**：每个I-UNet内部包含自己的编码器-解码器、注意力门控、跳跃连接和残差连接，参数k控制模块深度（例如I-UNet 4、5、6、7）。
- **Bridge Block**：在深层使用空洞卷积（dilated convolution）保持空间分辨率的同时提取高语义特征。
- **ASPP (Atrous Spatial Pyramid Pooling)**：修改为深度可分离卷积，在保持特征图尺寸的同时扩大感受野。
- **Outer-UNet**：编码器路径依次堆叠I-UNet 7、6、5、4，每个后接卷积块、SE块和最大池化；解码器路径通过上采样、注意力门控、跳跃连接恢复分辨率；最终将所有解码阶段输出拼接，经1×1卷积和sigmoid生成二值掩膜。
- **损失函数**（公式1）：  
  \(L(y, \hat{y}) = \frac{1}{N} \sum_{i=1}^N (1 + em_i \times (w-1)) \times BCE(y_i, \hat{y}_i)\)  
  其中 \(em\) 为通过Sobel梯度计算的边缘掩膜（梯度幅值>0.1视为边缘），\(w\) 为边缘权重（文中未给出具体值）。

## 3. 实验设计
### 数据集
1. **MoNuSeg**（多器官细胞核分割）：H&E染色组织病理图像，共44张（1000×1000像素），30张训练/14张测试。
2. **DSB 2018**（细胞分割）：来自不同生物和成像条件，图像尺寸128~512像素，仅使用Stage1训练集（公开ground truth），按9:10划分训练和评估。
3. **Electron Microscopy (EM)**（线粒体分割）：CA1海马区电镜图像，165张训练/165张测试，768×1024像素。

### 预处理与增强
- 生成256×256重叠块，步长128。
- **离线增强**：随机亮度/对比度、网格扭曲、图像转置、弹性变换（共生成8820、13710、9852个子样本）。
- **在线增强**：旋转90°、平移、剪切、缩放、水平/垂直翻转。

### 对比方法
- **CNN基准**：UNet, nnU-Net, HoVer-Net（生物标志物专用）。
- **Nested-UNet族**：UNet++, UNet 3+, U2-Net。
- **Transformer**：Swin-UNet, TransUNet。
- 所有对比方法使用官方公开代码和推荐设置复现。

### 评估指标
- 像素级：IoU, Dice, Precision, Recall
- 表面距离：95% Hausdorff距离 (HD95)
- 每张预测图单独计算后取平均。

### 实验设置
- 优化器：Adam，学习率0.0001，批大小8，核初始化：He Uniform。
- 训练200个epoch，从零开始。
- 使用验证集选择最佳检查点进行评估。
- 边缘感知损失中参数w未明确说明。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等具体计算资源信息。仅描述了批大小8和200个epoch的训练配置，但未报告实际硬件环境（如V100/A100等）。这在实验复现方面存在一定模糊性。

## 5. 实验数量与充分性
### 实验数量
- 在**三个不同模态的数据集**（组织病理、荧光显微镜、电镜）上进行了主要对比实验（表1），每个数据集对比了8种现有方法 + 本文方法。
- 在MoNuSeg上进行了**消融实验**（表2），逐步添加in_A, out_A, ASPP, SE共5组配置，验证每个组件贡献。
- 提供了**定性结果对比图**（图2），展示不同方法的分割效果。

### 充分性与公平性
- **充分**：覆盖了多种主流方法（CNN、嵌套UNet、Transformer），评估指标全面（像素级+距离度量）。
- **客观**：所有对比方法使用官方代码和推荐设置复现，并采用相同的数据处理流程（分块、增强等）。
- **潜在局限**：① 仅在三个公开数据集上测试，未涵盖更多疾病/器官类型（如肺部、肝脏等）；② 对Transformer方法（Swin-UNet、TransUNet）的性能差距可能因数据量不足导致，但论文未深入讨论数据规模对Transformer的影响；③ 消融实验仅在一个数据集上进行，泛化验证稍弱。

## 6. 论文的主要结论与发现
- 提出的**HistoSeg++**在**MoNuSeg、DSB、EM**三个数据集上均取得最优或次优的IoU、Dice、Recall和HD95指标，显著优于现有Nested-UNet方法。
- **在细胞核分割（MoNuSeg）**：IoU 71.44%、Dice 83.30%、Recall 92.33%、HD95 2.44，超越UNet++等基线。
- **在细胞分割（DSB）**：Precision 95.84%为最高，IoU 85.50%、Dice 91.81%。
- **在线粒体分割（EM）**：Recall 94.50%最高，Dice 92.74%，HD95仅次于UNet++。
- 消融实验证明：内外注意力、ASPP、SE模块依次提升性能，且HD95持续下降，说明边界准确性改善。
- Transformer方法在较小数据集上表现不如Nested-UNet变体，但具有较高的Precision（如TransUNet在DSB上Precision 93.37%）。

## 7. 优点
- **方法创新**：将内外注意力与SE通道校准融入Nested-UNet框架，形成有效的多尺度信息融合机制；边缘感知损失针对生物标志物边界模糊的问题进行优化。
- **泛化能力**：在三种不同成像模态（组织病理、荧光、电镜）和不同尺度对象（细胞核、细胞、线粒体）上均表现优异，证明架构通用性强。
- **实验设计**：对比方法广泛且采用官方复现，确保基准的可靠性；消融实验清晰展示每个模块贡献。
- **开源代码**：提供GitHub仓库，利于复现和后续研究。
- **清晰的可视化**：图2定性结果显示，本文方法产生的掩膜更完整、过分割更少。

## 8. 不足与局限
- **计算资源未报告**：缺乏GPU类型、训练时间、模型参数量和推理速度等关键信息，难以评估实际部署效率。
- **数据覆盖有限**：仅使用三个公开数据集，未在更大规模或更多类别（如多类分割）上验证，可能限制临床适用性。
- **Transformer对比不充分**：实验中Swin-UNet和TransUNet在部分指标上表现较弱，可能由于数据集规模小（Transformer需要大数据预训练），但论文未讨论或使用预训练权重的可能性。
- **边缘权重参数未说明**：边缘感知损失中的权重w未给出具体值（可能是超参数），影响复现精确性。
- **消融实验仅单数据集**：仅在MoNuSeg上验证组件效果，未在其他两个数据集上测试，组件泛化性证据不足。
- **未与最新SOTA（如基于SAM的微调方法）对比**：截止2026年，已有基于Segment Anything Model (SAM) 的医学图像分割方法，论文未提及这一方向，使对比基准稍显陈旧。

（完）
