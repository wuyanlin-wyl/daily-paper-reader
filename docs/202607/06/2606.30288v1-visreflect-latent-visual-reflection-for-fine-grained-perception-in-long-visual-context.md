---
title: "VisReflect: Latent Visual Reflection for Fine-Grained Perception in Long Visual Context"
title_zh: VisReflect：长视觉上下文中细粒度感知的潜在视觉反射
authors: "Xiaoqian Shen, Mohamed Elhoseiny"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30288v1"
tags: ["query:vl"]
score: 9.0
evidence: 通过潜在视觉反射改进LVLM的细粒度感知
tldr: "针对LVLMs在长视觉上下文中细粒度感知困难的问题，本文提出VisReflect框架，通过潜在空间生成连续视觉反射来选择性强调相关区域或帧，无需中间解码或额外前向传播。在图像和视频基准上分别提升4.1%和1.8%，推理时间减少约44%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30288v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of VisReflect. Given visual tokens extracted from a high-resolution image or a long-form video together with a textual query, a LVLM generates a sequence of visual reflection tokens between the special tokens <BOR> and <EOR>. These tokens are trained to approximate the latent visual representations of the region of interest (for images) or frames of interest (for videos) that are relevant to the question. Instead of explicitly cropping images or re-encoding selected frames, the model recalls questionrelevant visual features directly in the latent embedding space using its last hidden states. The reflected visual features then guide the model’s attention toward informative parts of the visual context, enabling accurate fine-grained perception in high-resolution images and long-form videos while requiring only a single forward pass.\", \"page\": 6, \"index\": 1, \"width\": 578, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2606.30288v1/fig-002.webp\", \"caption\": \"Fig. 4: Attention visualization of visual reflection tokens (<VR>). It illustrates the spatial focus of the generated visual reflection tokens on the input image.\", \"page\": 15, \"index\": 2, \"width\": 578, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2606.30288v1/fig-003.webp\", \"caption\": \"Fig. 1: Existing zooming-based methods localize relevant regions or frames by predicting coordinates in discrete token space and performing repeated forward passes. In contrast, VisReflect generates latent visual reflection tokens in continuous visual space, enabling the model to internally recall question-relevant visual features within a single forward pass.\", \"page\": 2, \"index\": 3, \"width\": 577, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2606.30288v1/fig-004.webp\", \"caption\": \"Fig. 3: Attention map visualization comparing our VisReflect with the baseline Qwen2.5-VL.\", \"page\": 23, \"index\": 4, \"width\": 723, \"height\": 258}]"
motivation: LVLMs在长视觉上下文中存在视觉注意力汇聚现象，导致细粒度感知困难。
method: 提出VisReflect框架，在潜在空间生成连续视觉反射，选择性强调相关视觉区域或帧以引导注意力。
result: "在图像和视频基准上分别提升4.1%和1.8%，推理时间减少约44%。"
conclusion: VisReflect通过潜在视觉反射有效且高效地改进了长视觉上下文中的细粒度感知。
---

## 摘要
大型视觉语言模型（LVLMs）在视觉-语言任务上取得了显著成功，但在高分辨率图像和长上下文视频的细粒度感知方面仍面临挑战。随着视觉token数量的增加，视觉注意力沉没现象愈发严重，导致无关token吸收了过多的注意力权重。近期方法尝试通过显式预测边界框或时间跨度并重新编码裁剪后的视觉区域来缓解这一问题。然而，这些方法依赖于离散token空间中不可靠的数值定位，并因额外的前向传播而带来显著的计算开销。本文提出**VisReflect**，一种简单而有效的框架，通过潜在视觉反射提升长视觉上下文中的细粒度感知。模型不将中间预测解码为离散token，而是在潜在空间中生成代表问题相关视觉特征的连续视觉反射。这些反射选择性突出显著区域或帧，在单次前向传播中引导注意力聚焦于相关视觉token。我们在具有挑战性的高分辨率图像基准（包括BLINK、V*和HRBench-4K/8K）以及视频理解基准（如MVBench、VideoMME和MLVU）上进行了全面评估。我们的方法在强基线基础上持续改进，在图像基准上提升4.1%，在视频基准上提升1.8%。与基于缩放的方法相比，我们的模型在视频理解任务中实现了可比的性能，同时推理时间减少了约44%。

## Abstract
Large Vision Language Models (LVLMs) have achieved remarkable success on vision-language tasks, yet fine-grained perception over high-resolution images and long-context videos remains challenging. As the number of visual tokens increases, the visual attention sink phenomenon becomes increasingly severe, causing irrelevant tokens to absorb a disproportionate amount of attention mass. Recent approaches attempt to mitigate this issue by explicitly predicting bounding boxes or temporal spans and re-encoding the cropped visual regions. Such methods depend on unreliable numeric localization in the discrete token space and incur significant computational overhead due to additional forward passes. In this work, we propose **VisReflect**, a simple yet effective framework that improves fine-grained perception in long visual contexts through latent visual reflection. Instead of decoding intermediate predictions into discrete tokens, the model generates continuous visual reflection that represents question-relevant visual features in the latent space. These reflections selectively emphasize salient regions or frames, guiding attention towards relevant visual tokens within a single forward pass. We conduct comprehensive evaluations on challenging high-resolution image benchmarks, including BLINK, V*, and HRBench-4K/8K, as well as video understanding benchmarks such as MVBench, VideoMME, and MLVU. Our method consistently improves over strong baselines, achieving gains of 4.1% on image benchmarks and 1.8% on video benchmarks. Compared with zooming-based methods, our model achieves comparable performance while reducing inference time by roughly 44% on video understanding.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

大型视觉语言模型（LVLMs）在处理高分辨率图像和长上下文视频时，随着视觉 token 数量激增，出现了 **“视觉注意力沉没”（visual attention sink）** 现象——无关的视觉 token 吸收了过多注意力权重，导致模型对细粒度区域（如小物体、特定帧）的感知能力严重下降。现有的解决方法（如“缩放”方法）需要先显式预测边界框或时间跨度，再裁剪并重新编码相关区域，这依赖于离散 token 空间中不可靠的数值定位，并且由于多次前向传播带来了巨大的计算开销。因此，本文旨在**在不增加推理计算量的前提下，提升 LVLM 在长视觉上下文中的细粒度感知能力**。

## 2. 论文提出的方法论

### 核心思想
提出 **VisReflect** 框架：在潜在空间中生成连续的“视觉反射”（visual reflection） token，这些 token 代表与问题相关的视觉特征（如图像中的区域或视频中的帧），从而在单次前向传播中引导模型的注意力聚焦于有用的视觉 token，避免中间解码和多次前向传播。

### 关键技术细节
- 使用特殊 token `<BOR>`（Begin of Reflection）和 `<EOR>`（End of Reflection）包裹生成的视觉反射 token 序列。
- 训练目标：让这些视觉反射 token 的隐藏状态逼近目标视觉区域或帧的潜在表示（latent visual representations），而不需要显式裁剪图像或重新编码帧。
- 在推理时，模型仅需一次前向传播：先生成视觉反射 token，然后利用这些 token 的最终隐藏状态来指导后续注意力，突出显示与问题最相关的视觉内容。

### 算法流程（文字说明）
1. 输入：高分辨率图像或长视频的视觉 token 序列 + 文本查询。
2. 模型生成 `<BOR>` ～ `<EOR>` 之间的视觉反射 token。
3. 这些 token 在潜在空间“回想”与问题相关的视觉特征。
4. 在下游生成阶段，这些反射特征引导跨注意力层关注信息丰富的原始视觉 token。
5. 最终输出答案，整个过程仅需一次模型前向传播。

## 3. 实验设计

### 使用的数据集与 benchmark
- **图像细粒度感知基准**：BLINK、V*、HRBench-4K/8K（高分辨率）。
- **视频理解基准**：MVBench、VideoMME、MLVU（长视频）。
- 对比方法：包括基线模型（如 Qwen2.5-VL）以及基于缩放的方法（需预测坐标/时间跨度并重新编码）。

### 对比方法
- 与多种强基线对比（论文中未列出具体方法名称，但提及了“zooming-based methods”）。
- 在图像和视频任务上分别与基线以及更复杂的缩放方法进行比较。

## 4. 资源与算力

论文元数据及摘要中**未明确说明**训练使用的 GPU 型号、数量以及训练时长。仅提到推理时间减少了约 44%（相比于缩放方法），但未提供绝对计算成本数据。**缺乏算力细节是本文的一个不足**。

## 5. 实验数量与充分性

- **图像基准**：4个数据集（BLINK、V*、HRBench-4K、HRBench-8K），平均提升 4.1%。
- **视频基准**：3个数据集（MVBench、VideoMME、MLVU），平均提升 1.8%。
- **消融实验**：论文中提到了 attention 可视化（见图4）以及视觉反射 token 的空间聚焦分析，但未明确列出消融实验的数量（如不同设计选择的对比）。
- **公平性**：在同一基线（Qwen2.5-VL）上对比，单次前向传播 vs 多轮前向传播，设置公平。但未测试不同模型家族（如 LLaVA、InternVL 等），泛化性需进一步验证。
- **充分性评价**：实验覆盖了图像和视频两类长上下文场景，但消融实验细节不足（如视觉反射长度、训练策略的影响），未对比不同视觉编码器或不同解码策略。

## 6. 论文的主要结论与发现

1. VisReflect 在图像基准上平均提升 4.1%，在视频基准上平均提升 1.8%，证明其有效改善了细粒度感知。
2. 相比需要多次前向传播的缩放方法，VisReflect 在视频理解任务上推理时间减少约 44%，同时性能相当甚至更优。
3. 视觉反射 token 能够在潜在空间自动聚焦于问题相关的区域/帧（通过注意力图可视化验证），避免了显式坐标预测的不可靠性。
4. 该方法支持单次前向传播，计算效率高，易于集成到现有 LVLM 架构中。

## 7. 优点

- **创新性**：首次提出在潜在空间生成连续视觉反射，绕过离散预测和重新编码，思路简洁且高效。
- **效率优先**：单次前向传播，推理速度大幅提升（~44%），适合实际部署。
- **通用性**：同时适用于图像和视频两种长上下文任务，无明显领域依赖。
- **可视化验证**：通过注意力图直观展示了视觉反射 token 确实聚焦于相关区域，增强可解释性。
- **强基线改进**：在 Qwen2.5-VL 等强模型上取得一致提升，说明方法具有泛化潜力。

## 8. 不足与局限

- **算力资源未披露**：未说明训练所需的 GPU 型号、数量、时长，难以评估可复现性和商业成本。
- **消融实验不够充分**：没有分析视觉反射 token 长度、训练损失形式、是否必须使用 `<BOR>/<EOR>` 等设计选择的影响。
- **仅基于单一基线**：仅在 Qwen2.5-VL 上进行实验，未在更多主流 LVLM（如 LLaVA-NeXT、InternVL2、GPT-4V）上验证，泛化性存疑。
- **视频任务提升较小（1.8%）** ：虽然图像任务提升显著，但视频上改进有限，可能与视频帧间冗余有关。
- **对比方法不全面**：未与更近期的注意力压缩方法（如 token merging、visual prompter）对比，仅对比了缩放类方法。
- **应用限制**：需要训练视觉反射 token 的生成能力，对于从未见过该机制的模型需微调，且对原始模型架构有侵入性（需添加特殊 token）。

（完）
