---
title: Set-Inclusive Uncertainty Modeling for Robust Brain Tumor Segmentation
title_zh: 面向鲁棒脑肿瘤分割的集合包含不确定性建模
authors: "Seunghun Baek, Jihwan Park, Jaeyoon Sim, Hoseok Lee, Seungjoo Lee, Won Hwa Kim"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30374v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 脑肿瘤分割，多模态MRI不确定性
tldr: 针对多模态MRI缺失导致的信息损失和不确定性，本文提出集合包含不确定性建模框架，将特征表示为高斯分布，通过对齐部分与全模态均值并利用差异缩放方差来度量不确定性，并引入层次排序约束，在BraTS数据集上取得优于基线的分割性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30374v1/fig-001.webp\", \"caption\": \"Fig. 2. Qualitative results on BraTS 2020. Our model produces improved segmentations over RFNet [8] and DC-Seg [14] across various configurations. (Region: ET (blue) ⊂ TC (blue, red) ⊂ WT (blue, red, green), Sample: “HG_BraTS20_Training_357”)\", \"page\": 7, \"index\": 1, \"width\": 699, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2606.30374v1/fig-002.webp\", \"caption\": \"Table 1. Test performance measured by the Dice similarity coefficient (DSC, %) on the BraTS 2020 dataset. For each modality, • and ◦ denote its presence and absence, respectively. The best and second-best results are highlighted in bold and underline.\", \"page\": 6, \"index\": 2, \"width\": 716, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.30374v1/fig-003.webp\", \"caption\": \"Fig. 1. Overall framework. (a) For each sample xi, three modality sets Ssub i ⊂ Ssup i ⊂ Sfull i are constructed and then processed independently. Subset configurations are modeled probabilistically as rki ∼N (µk i , (σ k i ) 2), whereas the full configuration serves as a deterministic anchor as µfull i . (b) LUA aligns each subset mean µk i with the full-modality anchor while enabling σk i to reflect their discrepancy. LUO further enforces an ordering constraint such that smaller subsets maintain higher uncertainty than their supersets.\", \"page\": 3, \"index\": 3, \"width\": 686, \"height\": 281}]"
motivation: 多模态MRI在临床中常缺失，导致信息损失和不确定性，现有确定性表示缺乏可靠性。
method: 提出概率表示框架，将特征建模为高斯分布，通过部分模态均值对齐全模态均值并利用差异缩放方差，并引入集合包含策略施加排序约束。
result: 在BraTS 2018和2020上的缺失模态实验中，该方法性能优于基线。
conclusion: 本文方法有效建模了缺失模态的不确定性，提升了脑肿瘤分割的鲁棒性。
---

## 摘要
多模态MRI对于准确的脑肿瘤分割至关重要。然而，在实践中，推理时获取所有模态往往具有挑战性，这会导致因不可避免的信息丢失而产生的固有不确定性。如果不对此不确定性进行建模，现有方法会将不完整的证据编码为看似合理但缺乏可靠性的确定性表示。针对这一问题，我们提出了一种概率表示框架，将表示建模为高斯分布，其均值捕捉任务信息，方差度量因缺失证据带来的不确定性。为使方差反映信息不足，我们将每个部分配置的均值向全模态对应值进行正则化，同时根据对齐均值之间的差异对方差进行缩放。我们进一步引入了一种集合包含策略，该策略利用模态子集的层次结构，并强制执行排序约束以维持其一致的不确定性关系。在BraTS 2018和2020上的大量实验表明，我们的方法在多种缺失模态场景下均优于基线方法。代码和模型检查点可在https://github.com/atlas-sky/SIUM获取。

## Abstract
Multimodal MRI is essential for accurate brain tumor segmentation. However, acquiring all modalities at inference is often challenging in practice, which causes intrinsic uncertainty due to unavoidable information loss. Without modeling this uncertainty, existing methods encode incomplete evidence into deterministic representations that appear plausible but lack reliability. In this regime, we propose a probabilistic representation framework that models representations as Gaussian distributions, where their mean captures task information and their variance measures uncertainty from missing evidence. To make variance reflect information deficiency, we regularize the mean from each partial configuration toward its full-modality counterpart, while scaling the variance with the discrepancy between their aligned means. We further introduce a set-inclusive strategy that exploits the hierarchical structure of modality subsets and enforces an ordering constraint to maintain their consistent uncertainty relationships. Extensive experiments on BraTS 2018 and 2020 demonstrate that our approach offers superior performance over baselines across diverse missing-modality scenarios. Code and model checkpoint are available at https://github.com/atlas-sky/SIUM.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，以下是对给定论文的结构化、深入、客观的中文总结。

---

### 论文中文总结

#### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：脑肿瘤分割在临床诊断和治疗规划中至关重要，通常依赖于四种标准MRI模态（T1、T1c、T2、FLAIR）的互补信息。然而，在实际临床场景中，由于成本、患者负担或扫描时间限制，采集所有模态往往不切实际，导致模态缺失。
*   **核心问题**：当模态缺失时，会造成不可逆的信息损失和固有的不确定性。现有的大多数方法（如RFNet、mmFormer等）将缺失模态下的特征编码为**确定性表示**，这会导致模型在缺乏足够证据的情况下做出看似合理但不可靠的预测，即模型可能会“幻觉”出虚假特征。
*   **整体含义**：为解决上述问题，本文提出了一种**概率表示框架**，旨在显式地建模因模态缺失带来的不确定性，从而提升模型在缺失模态场景下的鲁棒性和预测可靠性。核心思想是用高斯分布来替代确定性向量表示特征，方差作为不确定性指标。

#### 2. 论文提出的方法论

*   **核心思想**：将每个模态配置下的任务特征表示建模为一个**高斯分布**。分布的**均值 (μ)** 负责编码任务相关的确定性信息，而分布的**方差 (σ²)** 则用于量化因证据不足（模态缺失）带来的**不确定性**。
*   **关键技术细节**：
    *   **集合包含模态掩码**：在训练时，为每个样本构造三个具有包含关系的模态集合：全模态集合 \( S_{full} \)，以及两个部分模态集合 \( S_{sub} \subset S_{sup} \subset S_{full} \)。全模态作为概率分布中的“锚点”，其均值 \( \mu^{full} \) 被视为无不确定性的确定性表示。
    *   **不确定性感知对齐损失**：为了引导方差 \( \sigma \) 反映信息缺失的程度，该损失将每个部分模态集合的均值 \( \mu^{k} \) 向全模态锚点 \( \mu^{full} \) 对齐，同时利用对齐偏差 \( ||\mu^{k} - \mu^{full}|| \) 来缩放对应的方差 \( \sigma^k \)。偏差越大，方差越大。这是通过高斯负对数似然函数实现的。
    *   **排序约束损失**：为了确保不确定性关系的层次一致性，该损失强制施加一个排序约束，即子集 \( S_{sub} \) 的不确定性必须大于或等于其超集 \( S_{sup} \) 的不确定性（即 \( \sigma^{sub}^2 \geq \sigma^{sup}^2 \)）。
*   **核心公式**：
    *   **重参数化采样**：\( r^k = \mu^k + \epsilon \odot \sigma^k \)，其中 \( \epsilon \sim \mathcal{N}(0, I) \)。用于实现可微分的梯度反向传播。
    *   **总体损失函数**：\( \mathcal{L} = \mathcal{L}_{Task} + \alpha\mathcal{L}_{UA} + \beta\mathcal{L}_{UO} \)，其中 \( \mathcal{L}_{Task} \) 是任务损失（如Dice损失），\( \mathcal{L}_{UA} \) 是不确定性感知对齐损失，\( \mathcal{L}_{UO} \) 是排序约束损失，α和β是平衡超参数。
*   **理论分析**：论文从理论上分析了概率表示的梯度行为，证明较大的方差 \( \sigma \) 会引入更高的梯度方差，从而在梯度更新中起到正则化作用，抑制模型对缺失证据的过度依赖。

#### 3. 实验设计

*   **数据集与场景**：
    *   使用了两个公开的脑肿瘤分割基准数据集：**BraTS 2018** (285名受试者) 和 **BraTS 2020** (369名受试者)。
    *   评估场景涵盖了从缺失单一模态到缺失多个模态的所有 15 种可能的模态缺失组合，全面测试模型的鲁棒性。
*   **评价指标**：主要指标是**Dice相似系数**，分别评估三个肿瘤子区域：全肿瘤 (WT)、肿瘤核心 (TC) 和增强肿瘤 (ET)。
*   **对比方法**：
    *   与四种最新的（SOTA）缺失模态分割方法进行对比：**RFNet, mmFormer, M³AE, DC-Seg**。
    *   复现结果时，遵循各基线方法的官方代码和超参数，以确保公平性。

#### 4. 资源与算力

*   **明确说明**：论文中**未明确说明**所使用的GPU型号、数量及具体训练时长。
*   **细节**：仅提及使用Adam优化器，学习率2×10⁻⁴，batch size为2，训练了800个epoch。这些参数主要作为可复现性参考，而非算力消耗的直接证据。

#### 5. 实验数量与充分性

*   **实验数量**：
    *   **主实验**：在BraTS 2020 (15种配置)和BraTS 2018 (平均性能)上进行了全面的定量比较，展示了在几乎所有配置下的最佳或次佳性能。
    *   **消融实验**：对核心组件（集合包含、概率表示、对齐损失、排序损失）进行了消融研究，验证了每个模块的贡献。
    *   **定性分析**：提供了分割结果的可视化对比。
    *   **行为分析**：通过可视化不确定性分布（空间分布、与性能的逆相关关系）和梯度方差分析，从多个角度验证了模型的设计合理性和有效性。
*   **充分性与公平性**：
    *   实验设计比较充分，覆盖了主要的评估场景和方法。
    *   实验对比相对客观公平，使用了一致的评估协议，并复现了基线结果。
    *   不过，论文未提供在更大规模、更多样化数据集上的验证，这是实验覆盖上的一个潜在局限。

#### 6. 论文的主要结论与发现

*   **主要结论**：本文提出的概率表示框架（SIUM）通过对模态缺失导致的不确定性进行显式建模，显著优于现有的确定性方法。在BraTS 2018和2020数据集上，SIUM在几乎所有模态缺失场景下均取得了最高的平均Dice系数。
*   **关键发现**：
    *   **不确定性量化有效**：模型学习到的方差能够有效反映证据的不足，例如在缺失关键模态时，模型在肿瘤区域，尤其是增强肿瘤区域，表现出更高的不确定性。
    *   **不确定性决定可靠性**：不确定性水平与预测性能呈清晰的**反比关系**。不确定性高的配置性能差，不确定性低的配置性能好，这表明模型确实在“有把握的地方才下结论”。
    *   **排序约束成立**：子集的不确定性确实高于超集，证实了集合包含策略的有效性。

#### 7. 优点

*   **方法创新性强**：提出将缺失模态带来的固有不确定性用概率表示（高斯分布）来建模，不同于以往的确定性表示方法，概念新颖且逻辑自洽。
*   **理论分析扎实**：作者提供了严谨的数学推导（泰勒展开），证明了不确定性对梯度的影响机制，为模型的成功提供了理论支撑。
*   **实验验证全面**：在两大基准数据集上进行了详尽的实验，包括15种模态配置、定性和定量分析、消融研究和鲁棒性分析，支撑了方法的有效性。
*   **性能提升显著**：性能提升明显，特别是在更难分割的肿瘤核心（TC）和增强肿瘤（ET）区域，以及最困难的单模态场景下，优势更为突出。
*   **结果可解释性强**：通过可视化不确定性图，使得模型的决策过程更透明、更可解释，让研究者能直观理解模型对证据缺失的响应。

#### 8. 不足与局限

*   **计算资源未公开**：论文未提及具体的硬件资源和训练时间，不利于其他团队精确复现和对比计算效率。
*   **未在大型多中心数据上验证**：实验仅在 BraTS 2018/2020 上进行，虽然数据标准，但未在规模更大、数据异质性更强的多中心临床数据集上验证，其泛化能力有待进一步确认。
*   **超参数敏感性未分析**：损失函数中的超参数α和β对模型性能影响较大，但论文未进行详细的敏感性分析，其选取的最优值可能在其他数据上不是最优的。
*   **应用场景局限**：模型主要针对缺失模态问题，其设计目标是为“输入模态变少”的场景设计的，对于其他类型的不确定性（如标注噪声、图像质量差）的鲁棒性未做探讨。
*   **代码依赖性强**：方法的实现基于特定的基线模型（RFNet）的骨干网络，其通用性和迁移到其他骨干网络或任务（如多器官分割）的能力有待验证。

（完）
