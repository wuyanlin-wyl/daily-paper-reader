---
title: "ScAle: Attention Head Scaling as a Minimal Adapter for Spatial Reasoning in Vision Language Models"
title_zh: "ScAle: 作为视觉语言模型空间推理最小适配器的注意力头缩放"
authors: "Rahul Chowdhury, Timothy A Rupprecht, Xuan Shen, Pu Zhao, Yanzhi Wang"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29579v1"
tags: ["query:vl"]
score: 9.0
evidence: 提出ScAle，一种用于视觉语言模型空间推理的极简适配器
tldr: "空间推理对视觉语言模型（VLM）是个挑战，通常需要微调大量参数。本文发现调整选定层的激活值可显著影响性能，提出ScAle方法：仅学习少量标量系数来调制注意力与MLP激活，无需修改预训练权重。在空间推理和VQA任务上，只用1K参数即获得最高134.1%的相对精度提升，接近标准PEFT方法，同时保持非空间VQA能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29579v1/fig-001.webp\", \"caption\": \"Table 3: Test accuracy on WhatsUp-VLM and POPE benchmarks.\", \"page\": 11, \"index\": 1, \"width\": 442, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-002.webp\", \"caption\": \"Table 2: SpatialEval evaluation with different models under 20% training data.\", \"page\": 10, \"index\": 2, \"width\": 704, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-003.webp\", \"caption\": \"Fig. 3: Learned scaling factors for (a) LLaVA-7B and (b) Qwen2.5-VL-3B.\", \"page\": 14, \"index\": 3, \"width\": 652, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-004.webp\", \"caption\": \"Fig. 1: Examples of SpatialEval: (a) Spatial-Grid, (b) Maze-Nav, and (c) Spatial-Map.\", \"page\": 4, \"index\": 4, \"width\": 652, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-005.webp\", \"caption\": \"Table 4: Cross-task evaluation with Qwen2.5-VL-3B.\", \"page\": 12, \"index\": 5, \"width\": 658, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-006.webp\", \"caption\": \"Table 1: SpatialEval evaluation with LLaVA-7B under different training data size.\", \"page\": 9, \"index\": 6, \"width\": 706, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2606.29579v1/fig-007.webp\", \"caption\": \"Fig. 2: Accuracy heatmaps for multiplying one scalar to the last-token MLP activations in one single layer. Different scalars ranging from −10 to +10 are applied to different layers to obtain the heatmap. We evaluate LLaVA-Next on three SpatialEval tasks.\", \"page\": 5, \"index\": 7, \"width\": 652, \"height\": 221}]"
motivation: 空间推理提升通常需要大量参数微调，而研究发现仅重缩放激活值即可影响性能，启发设计超轻量适配方法。
method: 提出ScAle，为冻结骨干网络学习少量标量系数，分别缩放最后token的注意力与MLP激活，作为最小适配器。
result: "在SpatialEval和两个VQA数据集上，仅用1K可训练参数取得最高134.1%的相对精度增益，接近LoRA性能且不损非空间VQA精度。"
conclusion: 有界激活重加权提供了一种架构无关、超参数高效的VLM适配替代方案，无需大量可训练参数。
---

## 摘要
空间推理对许多视觉语言模型（VLM）来说仍是一个持续挑战，改进它通常需要微调大量额外参数。我们的初步分析显示，在不修改预训练权重的情况下，调整选定Transformer层中的激活值可以显著影响下游性能。受此观察启发，我们提出了ScAle，一种超轻量级的适配方法，它学习一小部分标量系数来调制全冻结主干中的最后token注意力和MLP激活值。我们在合成空间推理基准SpatialEval以及多个模型系列的真实世界VQA数据集（COCOQA和VGQA）上评估了我们的方法。我们的方法ScAle仅使用1K可训练参数即可实现高达134.1%的相对准确率提升，而无需像LoRA等标准PEFT方法那样需要数百万可训练参数。尽管极其紧凑，我们的方法恢复了标准PEFT性能的相当大一部分，同时保持了强大的非空间VQA准确率。这些结果表明，有界的激活重加权为适配预训练VLM提供了一种简单、架构无关且高度参数高效的替代方案。

## Abstract
Spatial reasoning remains a persistent challenge for many vision language models (VLMs), and improving it typically requires fine-tuning with substantial additional parameters. Our preliminary analysis reveals that rescaling activations in selected transformer layers-without modifying pretrained weights-can significantly influence downstream performance. Motivated by this observation, we propose ScAle, an ultra-lightweight adaptation method that learns a small set of scalar coefficients to modulate last-token attention and MLP activations in a fully frozen backbone. We evaluate our method on the synthetic spatial reasoning benchmark SpatialEval and on real-world VQA datasets (COCOQA and VGQA) across multiple model families. Our method, ScAle, achieves up to 134.1% relative accuracy gains using only 1K trainable parameters without requiring millions of trainable parameters as in standard PEFT methods such as LoRA. Despite its extreme compactness, our approach recovers a substantial fraction of standard PEFT performance while preserving strong non-spatial VQA accuracy. These results demonstrate that bounded activation reweighting provides a simple, architecture-agnostic, and highly parameter-efficient alternative for adapting pretrained VLMs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：空间推理是视觉语言模型（VLM）的关键能力，但当前主流VLM（如LLaVA-Next、Qwen2.5-VL）在空间推理基准SpatialEval上表现较差（准确率仅20%~30%）。传统改进方法需要大量参数微调（如LoRA需数百万参数），成本高、部署困难。
- **核心观察**：论文通过初步实验发现，仅对单个Transformer层的最后一个token的MLP激活值进行手动缩放（无需修改权重），即可显著改变空间推理准确率（准确率从20%~30%提升至70%以上）。这表明激活值的幅度调控可影响模型内部表征，因此作者提出一种极简的、可学习的激活缩放方案。

## 2. 方法论
### 核心思想
- 在完全冻结的VLM骨干上，学习一组**有界的标量系数**，用于缩放**最后一个token**的注意力头输出和/或MLP输出。通过这种轻量级重加权，引导模型激活对空间推理有利的模式，而不引入新的表征方向。

### 关键技术细节
1. **位置选择**：仅缩放最后一个token的激活，而非所有token，以避免干扰上游上下文编码。
2. **缩放模块**：
   - **注意力头缩放**：每层每个注意力头学习一个标量 \( u^{\text{attn}}_{l,h} \)，缩放该头最后一个token的输出：\(\tilde{z}_{l,h} = \text{eff}(u^{\text{attn}}_{l,h}) \cdot z_{l,h}\)。
   - **MLP缩放**：每层MLP学习一个标量 \( u^{\text{MLP}}_{l} \)，缩放最后一个token的MLP输出：\(\tilde{Y}_l = \text{eff}(u^{\text{MLP}}_{l}) \cdot Y_l\)。
3. **有界函数**：\(\text{eff}(u) = 1 + s_{\max} \tanh(u)\)，其中 \( s_{\max}=10 \)，将缩放因子限制在 \( (1-s_{\max}, 1+s_{\max}) \) 范围内，保证数值稳定性。
4. **训练目标**：最小化交叉熵损失加上L2正则化，仅更新标量参数 \(\phi\)，冻结所有预训练权重。公式：\(\mathcal{L}(\phi) = \text{CE}(\ell_\phi(I,q), c^*) + \lambda_{\text{attn}} \sum \|u^{\text{attn}}\|_2^2 + \lambda_{\text{MLP}} \sum \|u^{\text{MLP}}\|_2^2\)。

### 参数效率
- 参数量极小：例如LLaVA-7B（32层，32头）仅需 \(32 \times 32 = 1024\) 个注意力头标量，加上32个MLP标量共1056个可训练参数。相比LoRA-all（约2.65M）少2500倍以上。

## 3. 实验设计
### 数据集与Benchmark
- **空间推理基准**：SpatialEval（包含三个子任务：Spatial-Grid、Maze-Nav、Spatial-Map），以及WhatsUp-VLM（包含VGQA、COCOQA两个真实世界空间VQA数据集）。
- **幻觉鲁棒性**：POPE基准（评估对象幻觉）。
- **语言理解**：GLUE中的SST-2、MRPC（用于跨任务迁移测试）。

### 对比方法
- **Frozen**：原始冻结模型。
- **(IA)³**：学习激活维度级缩放向量（参数量约600K~1M）。
- **LoRA-all**：标准LoRA（rank=1）应用于所有线性层（参数量2.6M~4M）。
- **LoRA-last**：仅最后一层LoRA（参数量约50K~100K）。
- **ScAle**：本文方法，分注意力缩放(attn)、MLP缩放(mlp)、两者结合(both)。

### 训练设置
- 数据划分：从每任务1500样本中随机取5%、10%、20%作为训练集，其余测试。固定分割确保公平。
- 超参数：学习率1e-3，λ=1e-4，AdamW优化器，micro-batch=1，训练5个epoch。

## 4. 资源与算力
- **论文未明确说明**所使用的GPU型号、数量及训练时长。仅提及使用PyTorch及现有开源模型，训练超轻量参数（≤1K），因此训练成本极低，远低于LoRA等需要反向传播更新大矩阵的方法。推测单卡（如A100）即可在数分钟内完成训练。

## 5. 实验数量与充分性
- **实验组数**：相当充分。
  - 主表：Table 1（LLaVA-7B上不同数据量、不同缩放方式的对比）。
  - Table 2：跨四个模型（LLaVA-7B/13B, Qwen2.5-VL-3B/7B）的对比。
  - Table 3：真实世界VQA（VGQA、COCOQA）及POPE幻觉测试。
  - Table 4：跨任务迁移实验（训练于某任务，测试于其他任务）。
  - Table 5：ScAle与LoRA混合设计（LoRA-QKV + ScAle）。
  - Fig. 3：可视化学习到的缩放因子分布。
- **充分性评价**：实验设计较为全面，覆盖了不同模型族、不同数据规模、不同缩放位置、跨任务迁移、与主流PEFT对比、混合设计以及消融。结果客观，报告了多次重复（虽未明确说明重复次数，但推测基于固定分割单次运行，但鉴于数据划分固定且参数量极小，结果稳定性应较高）。不足之处在于缺少对模型参数量更大（如70B）的验证，且仅限于空间推理相关评测，通用能力仅通过GLUE两个子任务简单测试。

## 6. 主要结论与发现
1. **超轻量激活缩放有效**：仅用约1K可训练参数（比LoRA少2500倍），即可在SpatialEval上实现最高134.1%的相对准确率提升（例如Spatial-Grid从29.3%提升至68.6%）。
2. **注意力缩放优于MLP缩放**：注意力头的缩放效果显著好于MLP缩放，且结合两者无额外收益，表明空间推理瓶颈主要来自注意力层的不校准。
3. **跨模型通用**：在LLaVA-7B/13B、Qwen2.5-VL-3B/7B上均一致有效。
4. **真实世界VQA与幻觉改善**：在VQA和POPE上也取得提升，表明该方法不止提升合成空间推理，还能改善视觉接地和减少幻觉。
5. **跨任务迁移性**：训练于合成空间任务，可直接提升真实空间VQA和部分GLUE任务性能，而不损害通用语言理解（甚至有所改善）。
6. **与LoRA互补**：ScAle与LoRA组合（仅对QKV应用LoRA）可超越完整LoRA-all的性能，同时参数量减少超过60%。

## 7. 优点
- **极致的参数效率**：仅需几百到一千多个参数，训练和存储成本极低，适合边缘部署和多任务适配。
- **架构无关**：方法简单，只需在现有VLM推理过程中插入一个缩放层，无需修改模型结构。
- **可解释性**：学习到的缩放因子可通过热图可视化，揭示哪些头/层对空间推理更重要。
- **性能-效率平衡好**：虽然精度略低于LoRA-all，但参数量减少超过三个数量级，且可通过与LoRA混合来弥补差距。
- **实验全面**：覆盖合成与真实场景、多个尺寸模型、多种训练数据规模以及跨任务迁移。

## 8. 不足与局限
- **未说明算力消耗**：论文未报告训练所需的GPU型号、数量及时间，虽然推断成本极低，但缺乏量化参考。
- **模型规模有限**：仅在3B~13B模型上实验，未在更大模型（如34B、70B或更大）上验证，可能在大模型上缩放效果减弱。
- **通用语言能力评估不足**：仅用GLUE两个子任务（SST-2、MRPC）测试跨任务迁移，缺乏对更广泛NLP任务（如数学、代码、知识等）的影响分析。
- **空间推理基准单一**：主要依赖SpatialEval（合成场景），真实世界空间推理仅通过WhatsUp-VLM（两个数据集），未在其他更复杂的空间VQA（如VSR、CLEVR等）上测试。
- **训练数据量小**：最多仅使用20%（300样本）的训练数据，虽然验证了小样本有效性，但未探索更大训练数据是否带来进一步增益或饱和。
- **可能的风险**：激活缩放作为额外的可学习组件，若在不同任务间切换时忘记重置缩放因子，可能导致灾难性遗忘，论文未讨论多任务连续学习场景。

（完）
