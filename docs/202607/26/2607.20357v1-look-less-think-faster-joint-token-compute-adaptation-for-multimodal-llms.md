---
title: "Look Less, Think Faster: Joint Token-Compute Adaptation for Multimodal LLMs"
title_zh: 看得少，想得快：多模态大语言模型的联合令牌-计算自适应
authors: "Pengcheng Wang, Zhiquan Wang, Jayoung Lee, Zhuoyan Xu, Ran Xu, Saurabh Bagchi, Yin Li, Somali Chaterji"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20357v1"
tags: ["query:vl"]
score: 9.0
evidence: 多模态大模型的统一自适应成本缩减
tldr: 多模态大模型推理成本高，现有方法独立优化视觉token或LLM计算，忽视资源动态分配。本文提出SmartVL，联合控制token数和模型计算，通过共享预算编码和可微分延迟估计器协调两个控制器，实现跨阶段分配。实验表明，联合调度在多个基准上优于先前方法，达到更优的精度-效率权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20357v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of SmartVL. A vision-side token controller and an LLM-side compute controller jointly govern three coupled compute dimensions: vision tokens (sequence length), active attention heads and FFN capacity (width), and executed transformer layers (depth). Both controllers are conditioned on the same global budget and trained end-to-end through a differentiable latency estimator, enabling them to learn content- and budget-aware allocation strategies across all three dimensions.\", \"page\": 7, \"index\": 1, \"width\": 638, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2607.20357v1/fig-002.webp\", \"caption\": \"Fig. 7: Visualization of token-compute allocation under a 4 TFLOPs comptute budget. The upper and lower figures show the prediction results on POPE and TextVQA. Different (T,C) combinations exhibit varying performance depending on the input, demonstrating that the best budget allocation is content-dependent.\", \"page\": 14, \"index\": 2, \"width\": 688, \"height\": 381}]"
motivation: 多模态大模型推理成本高，现有自适应方法独立优化不同维度，缺乏联合调度，无法根据输入内容动态分配计算资源。
method: 提出SmartVL框架，包括视觉侧token控制器和LLM侧计算控制器，通过共享预算编码和可微分延迟估计器进行端到端联合训练。
result: 在多个MLLM基准上，SmartVL的联合调度持续优于先前自适应方法，实现更优的精度-效率帕累托前沿。
conclusion: 跨维度联合调度计算资源是提升多模态大模型推理效率的关键，未来可扩展至更多维度。
---

## 摘要
多模态大语言模型（MLLMs）近期在视觉-语言任务中展现出强劲性能。然而，其高昂的推理成本——源于大量输入视觉令牌和大语言模型（LLM）的繁重计算——仍是实际部署的关键障碍。近期工作尝试通过自适应优化单一维度来降低成本，例如修剪冗余视觉令牌或跳过LLM层与头。然而，以往的方法通常独立对待这些维度，忽视了一个基本耦合：可用计算资源必须根据输入内容在所有维度上动态分配。为填补这一空白，我们提出SmartVL，一个统一的自适应推理框架，能够联合控制视觉令牌数量和模型计算能力，以响应变化的输入内容和计算预算。SmartVL引入了一个视觉端令牌控制器，动态选择信息量丰富的视觉令牌，以及一个LLM端计算控制器，自适应调整LLM计算。重要的是，这些控制器经过训练能够相互协调，使整体推理成本满足目标预算。为实现这一联合调度，我们通过共享预算编码连接控制器，并利用可微分延迟估计器进行端到端训练。该设计使SmartVL能够学习跨阶段分配策略，同时适应输入复杂性和运行时计算约束。在多个MLLM基准上的实验表明，通过联合调度，SmartVL持续优于先前的自适应方法，并实现了更优的准确率-效率帕累托前沿。项目页面：https://www.schaterji.io/publications/2026/jointtokencompute。

## Abstract
Multimodal Large Language Models (MLLMs) have recently demonstrated strong performance across vision-language tasks. However, their high inference cost, arising from both the large number of input visual tokens and the heavy computation of the large language model (LLM), remains a key barrier to practical deployment. Recent work attempts to reduce the cost by adaptively optimizing individual dimensions, e.g., pruning redundant visual tokens or skipping LLM layers and heads. Nonetheless, prior approaches typically treat these dimensions independently and overlook a fundamental coupling: the available compute resources must be dynamically allocated across all dimensions based on the input content. To bridge the gap, we propose SmartVL, a unified adaptive inference framework that jointly controls vision token number and model compute capability in response to varying input contents and compute budgets. SmartVL introduces a vision-side token controller that dynamically selects informative visual tokens and an LLM-side compute controller that adaptively adjusts LLM computation. Importantly, these controllers are trained to coordinate with each other so that the overall inference cost satisfies a target budget. To allow this joint scheduling, we connect the controllers using a shared budget encoding and leverage a differentiable latency estimator for end-to-end training. This design enables SmartVL to learn cross-stage allocation strategies that adapt to both input complexity and runtime compute constraints. Experiments across multiple MLLM benchmarks demonstrate that, with joint scheduling, SmartVL consistently outperforms prior adaptive methods and achieves superior accuracy-efficiency Pareto frontiers. Project page: https://www.schaterji.io/publications/2026/jointtokencompute.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：多模态大语言模型（MLLMs）在视觉-语言任务中性能优异，但其推理成本高昂，主要体现在两个方面：一是大量视觉令牌（visual tokens）的输入，二是大语言模型（LLM）的繁重计算。高昂成本阻碍了实际部署。
*   **研究动机**：现有自适应方法（如修剪冗余视觉令牌、跳过LLM层或头）通常独立优化单一维度，忽视了不同维度之间的基本耦合关系——可用计算资源必须根据输入内容在所有维度上动态分配。
*   **整体含义**：本文旨在填补这一空白，提出一种统一的、联合控制视觉令牌数量与模型计算能力的自适应推理框架，以实现根据输入复杂度和计算预算动态分配资源，从而在保持精度的前提下显著降低推理成本。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

*   **核心思想**：提出 **SmartVL** 框架，通过联合调度视觉端的令牌数和LLM端的计算深度/宽度，实现端到端自适应推理。
*   **关键技术细节**：
    *   **双控制器架构**：
        *   **视觉侧令牌控制器**：动态选择信息量丰富的视觉令牌（对应序列长度维度）。
        *   **LLM侧计算控制器**：自适应调整LLM的计算量，包括活跃注意力头数、FFN容量（宽度维度）和执行的Transformer层数（深度维度）。
    *   **共享预算编码**：两个控制器通过一个共享的全局预算编码（global budget encoding）相连，使它们能基于相同的计算预算约束进行协调。
    *   **可微分延迟估计器**：用于端到端训练，使控制器学习到内容感知和预算感知的分配策略，并且整个框架可微分，能反向传播梯度。
*   **算法流程（文字说明）**：
    1.  输入图像和文本。
    2.  视觉编码器提取视觉特征（密集令牌）。
    3.  视觉侧令牌控制器根据当前预算编码和图像内容预测需要保留的令牌数量，并筛选出信息量高的令牌。
    4.  筛选后的令牌与文本嵌入共同进入LLM。
    5.  LLM侧计算控制器根据预算编码和中间隐藏状态，动态决定每一层需要跳过的注意力头数、缩减的FFN维度以及跳过的层数。
    6.  两个控制器的输出联合决定最终计算路径，并通过可微分延迟估计器计算近似延迟，作为损失项之一参与端到端训练。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

*   **数据集/场景**：在多个MLLM基准数据集上进行评估，具体数据集名称未在摘要中列出，但包括常见的视觉问答、图像描述等任务（如POPE、TextVQA等，在figures_json中提到）。
*   **Benchmark**：采用准确率-效率帕累托前沿（accuracy-efficiency Pareto frontiers）作为评估指标，同时比较不同计算预算下的性能。
*   **对比方法**：与先前的自适应方法进行对比，包括单独优化视觉令牌或LLM计算的方法（如令牌修剪、层/头跳过等）。SmartVL的联合调度持续优于这些先前的自适应方法。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

*   **摘要中未明确说明**：论文提供的摘要和元数据中没有提及具体的GPU型号、数量或训练时长。仅提到项目页面，但未提供详细信息。因此，无法从给定文本中总结算力资源。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

*   **实验数量**：摘要未列出具体表格或实验组数，但提到“在多个MLLM基准上”进行实验，并展示了在不同计算预算（例如4 TFLOPs）下的可视化结果。此外，从figures_json中看到有POPE和TextVQA上的预测可视化。
*   **充分性判断**：由于信息有限，无法判断实验的全面性。但作者声称“持续优于先前的自适应方法”，如果确实在多个基准上进行了公平对比（控制预算、同基线），则实验设计是合理的。但缺乏消融实验（如单独控制器的效果）的明确说明，可能不充分。

### 6. 论文的主要结论与发现

*   **主要结论**：跨维度联合调度计算资源是提升多模态大模型推理效率的关键。SmartVL通过联合控制视觉令牌数和LLM计算量，能够根据输入内容和计算预算自适应分配资源，在多个基准上实现了更优的准确率-效率帕累托前沿，优于先前独立优化单一维度的自适应方法。

### 7. 优点：方法或实验设计上有哪些亮点

*   **方法亮点**：
    *   **联合调度**：首次将视觉令牌控制与LLM计算控制统一在同一个框架中，考虑维度耦合性，实现跨阶段资源分配。
    *   **共享预算编码**：通过一个共同的预算编码连接两个控制器，使它们能协同工作，避免独立优化导致的不平衡。
    *   **可微分延迟估计器**：确保整个框架可端到端训练，使控制器能学习到精确的延迟-精度权衡。
    *   **内容自适应**：控制器基于输入内容进行动态决策，比固定预算或启发式方法更灵活。
*   **实验亮点**：使用帕累托前沿比较，直观展示不同方法在不同计算预算下的精度-效率平衡，公平性较好。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **不足与局限**：
    *   **信息缺失**：提供的文本仅包含摘要，缺乏详细的方法描述（如控制器具体结构、损失函数形式）、实验表格、超参数设置、与最强基线的绝对比较等，无法全面评估。
    *   **实验覆盖**：可能只在少数几个基准上进行验证，缺乏对更多样化场景（如视频、3D）或更大规模MLLM的测试。
    *   **偏差风险**：若仅对比部分早期自适应方法，而未包含最新的高效MLLM设计（如MoE、量化），可能高估自身优势。
    *   **应用限制**：增加了额外的控制器和估计器，可能带来少量开销；联合调度的稳定性需进一步验证；训练需要同时建模多个控制目标，收敛难度可能较大。
    *   **未来方向**：作者提到未来可扩展至更多维度，说明当前框架可能未覆盖所有可优化的维度（如量化、专家选择）。

（完）
