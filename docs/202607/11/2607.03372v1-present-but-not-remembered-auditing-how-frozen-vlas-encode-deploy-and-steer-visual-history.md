---
title: "Present but Not Remembered: Auditing How Frozen VLAs Encode, Deploy, and Steer Visual History"
title_zh: 存在但未被记住：审计冻结VLA如何编码、部署和操控视觉历史
authors: "Chih-Ting Liao, Xin Cao"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03372v1"
tags: ["query:vl"]
score: 7.0
evidence: 审计冻结视觉语言动作模型如何编码时间历史
tldr: 本文研究冻结视觉-语言-动作模型（VLA）如何编码和利用历史观察。通过层解析线性探测和因果干预分析三个VLA，发现过去帧内容可解码，但历史信息几乎冗余于当前帧；历史仅在当前帧严重退化时被因果使用，且不同架构的部署策略不同。重新注入历史无法修复遮挡或明确动作，表明可操纵性取决于历史部署方式而非编码。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03372v1/fig-001.webp\", \"caption\": \"Figure 1: The temporal-deployment audit at a glance (Octo-Small unless noted [25], n=200). Rows group the story: encoding and the central dissociation (a,b), fallback deployment (c,d), cross-architecture (e,f). (a) Decode R2(t−1) (teal) stays above 0.30 at every depth while the causal deploy gap (rose, occ black − markov, 95% band) is significant only through L4 and collapses at L6. (b) History is decodable (A1) but its unique information (A4, gray) is ≈ 0 in every model and both families: a redundant copy of the present. (c) On the degradation ladder only near-total occlusion (occ black) deploys history; milder degradations are n.s. (d) Interchange (rose) and attention-knockout (plum) both collapse at L6, ruling out a method artifact. (e) Under the same occlusion Octo’s reliance on history rises (fallback) while CronusVLA’s falls (standing), encoding identical (A4≈0 in both). (f) CronusVLA’s contribution is positive when clean, ≈0 under temporal shuffling (order-blind), and falls under occlusion, over three seeds.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 993}, {\"url\": \"assets/figures/arxiv/2607.03372v1/fig-002.webp\", \"caption\": \"Table 1: Positioning: VLA × mechanistic interpretability. Prior work localizes the within-frame modality axis, steers within-frame motion features, or adds memory without a diagnosis; we localize the orthogonal time axis and characterize how history is encoded, deployed, and steered, reaching an architecture-conditional verdict.\", \"page\": 3, \"index\": 2, \"width\": 736, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2607.03372v1/fig-003.webp\", \"caption\": \"Table 16: Per-leg reproduction map (expected headline numbers). All legs read the same frozen, hashed stimulus set; runs are on one consumer GPU with frozen public checkpoints.\", \"page\": 19, \"index\": 3, \"width\": 892, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2607.03372v1/fig-004.webp\", \"caption\": \"Table 12: Notation.\", \"page\": 17, \"index\": 4, \"width\": 652, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2607.03372v1/fig-005.webp\", \"caption\": \"Figure 4: Population and behavioral evidence (Octo-Small [25]). (a) Mean per-DoF |∆| under masking vs. swapping t−1: history influences the continuous pose dimensions throughout, not just the gripper. (b) Per-pair lost fraction of the frame across the frozen set; the distribution sits far below the near-total-occlusion band that triggers deployment, so only 3/250 pairs are naturally occluded, the fallback is seldom triggered in distribution.\", \"page\": 16, \"index\": 5, \"width\": 825, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2607.03372v1/fig-006.webp\", \"caption\": \"Figure 3: Supporting panels (Octo-Small unless noted [25]): (a) decode by depth, (b) per-layer interchange contribution, (c) five-seed deploy-gap hardening with a permutation test, (d) inverse scaling across model size. These expand the summary panels of Figure 1.\", \"page\": 16, \"index\": 6, \"width\": 824, \"height\": 620}]"
motivation: 现有工作关注添加记忆，但忽视了冻结VLA中历史信息如何表示和使用。
method: 采用层解析线性探测和因果干预分析三个VLA（来自两个架构系列）的历史信息编码与因果作用。
result: 历史帧内容可解码，但历史独特信息几乎缺失；历史仅在当前帧严重遮挡时被因果使用，且不同架构部署策略不同（一个增加依赖，一个减少）。
conclusion: VLA没有遗忘过去，但未能将历史表示为与当前不同的信息；可操纵性取决于历史如何部署而非是否编码。
---

## 摘要
一个冻结的视觉-语言-动作模型（VLA）在每个决策步骤接收最近的观测，然而先前的工作侧重于增加记忆，而不是询问现有历史如何被表示和使用。我们使用层解析线性探测和因果交换干预，在来自两个架构家族的三个VLA上研究这一时间轴。我们发现了一个三部分的分离。首先，过去帧的内容在整个网络中保持线性可解码。其次，超出当前帧的历史特有信息几乎不存在，表明存储的历史很大程度上是当前帧的冗余副本。第三，仅当当前帧严重退化时，历史才会被因果性地部署，而动作读取通过网络逐渐丧失对历史的依赖。尽管所有模型都类似地编码历史，但它们的部署策略不同：在相同的遮挡下，一个架构越来越依赖历史作为后备，而另一个则较少依赖。我们进一步引入了一种无需训练的时间部署审计，以区分这些模式。在后备模式中，重新注入历史既不能修复遮挡也不能消除动作歧义，证实了存储表示的冗余性。在另一种模式中，相同的干预可靠地将预测动作导向供体历史。这些结果表明，可操控性取决于历史如何被部署，而不是它是否被编码。VLA不会忘记过去；它们在很大程度上未能将历史表示为与当前不同的信息。我们的发现表明，未来的记忆增强应注入过去特有的信息，而不仅仅是更多的历史。

## Abstract
A frozen vision-language-action model (VLA) receives recent observations at every decision step, yet prior work has focused on adding memory rather than asking how existing history is represented and used. We study this temporal axis using layer-resolved linear probing and causal interchange interventions across three VLAs from two architecture families. We find a three-part dissociation. First, past-frame content remains linearly decodable throughout the network. Second, information unique to history beyond the current frame is nearly absent, indicating that stored history is largely a redundant copy of the present. Third, history is causally deployed only when the current frame is heavily degraded, while the action readout progressively loses dependence on history through the network. Although all models encode history similarly, their deployment strategies differ: under the same occlusion, one architecture increasingly relies on history as a fallback, whereas the other relies on it less. We further introduce a training-free temporal deployment audit that distinguishes these regimes. In the fallback regime, re-injecting history neither repairs occlusion nor disambiguates actions, confirming the redundancy of the stored representation. In the other regime, the same intervention reliably steers the predicted action toward the donor history. These results show that steerability depends on how history is deployed rather than whether it is encoded. VLAs do not forget the past; they largely fail to represent it as information distinct from the present. Our findings suggest that future memory augmentation should inject information unique to the past rather than simply more history.