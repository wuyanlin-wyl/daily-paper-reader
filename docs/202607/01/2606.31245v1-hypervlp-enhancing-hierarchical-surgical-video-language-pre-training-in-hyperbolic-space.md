---
title: "HyperVLP: Enhancing Hierarchical Surgical Video-Language Pre-training in Hyperbolic Space"
title_zh: HyperVLP：在双曲空间中增强分层手术视频-语言预训练
authors: "Yaojun Hu, Kun Yuan, Nassir Navab, Haochao Ying, Jian Wu, Nicolas Padoy"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31245v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 层次化手术视频语言预训练
tldr: 手术视觉-语言预训练模型通常将教学视频中的多层级知识（动作、步骤、阶段）编码到扁平空间，忽略了层级间的语义包含关系。本文提出HyperVLP，在双曲空间中进行预训练，通过缓解结构假负例和强制父阶段与子步骤间的语义一致性来保留层次结构。在多个手术基准上，零样本和少样本阶段识别任务均取得一致提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31245v1/fig-001.webp\", \"caption\": \"Table 2: Linear-probing evaluation results. V: supervision is from visual frames. L: supervision is from natural languages. VL: supervision is from both visual and language entities.\", \"page\": 7, \"index\": 1, \"width\": 721, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2606.31245v1/fig-002.webp\", \"caption\": \"Table 1: Zero-shot Phase Recognition across Procedures and Centers.\", \"page\": 6, \"index\": 2, \"width\": 704, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2606.31245v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of HyperVLP. Red dashed boxes denote hierarchical units (V, T, v, t), which are mapped to hyperbolic embeddings (Vc, Tc, vc, tc) shown on the right. Green boxes indicate structurally related samples S(i) used in geometry-aware contrastive learning.\", \"page\": 3, \"index\": 3, \"width\": 716, \"height\": 273}]"
motivation: 现有方法将手术知识中多层级、层次化的语义信号压缩到单一扁平空间，忽略了跨层级的语义包含关系和长程依赖建模。
method: 提出双曲空间下的手术视频-语言预训练框架，利用双曲几何显式保留层次结构，通过结构假负例缓解和父子语义一致性约束进行优化。
result: 在多个手术基准的零样本和少样本阶段识别任务上取得一致且显著的性能提升。
conclusion: 在双曲空间中建模手术视频-语言层次结构能有效提升跨过程、跨机构的阶段识别能力。
---

## 摘要
手术视觉-语言基础模型通常采用教育材料（如手术讲座视频）来将语言中编码的手术知识转化为视觉表示。这些知识是多维且分层的：细粒度的动作线索出现在叙述中，中级关键步骤被总结在子节标题中，而全局过程上下文（如患者病史和手术策略）则在抽象文本中描述。先前的工作大多将这些异质信号压缩到单一平坦的嵌入空间中，隐式假设各层级之间独立。然而，这是次优的，因为它忽略了跨层级的语义包含关系（例如，动作属于步骤，步骤构成阶段），削弱了长距离依赖建模。为此，我们提出了一种双曲手术视频-语言预训练框架，通过缓解由过程上下文引起的结构假阴性，并强制执行父阶段与其组成子步骤之间的语义一致性，显式地保留了分层结构。在多个手术基准上的大量实验表明，在不同手术和机构中的零样本和少样本阶段识别上取得了持续的性能提升。

## Abstract
Surgical vision-language foundation models typically adopt educational materials, such as surgical lecture videos, to transfer surgical knowledge encoded in language into visual representations. These knowledge are multi-dimensional and hierarchical: fine-grained action cues appear in narration, mid-level key steps are summarized in subsection headings, and global procedural context, such as patient history and surgical strategy, is described in abstract texts. Prior work largely collapses these heterogeneous signals into a single flat embedding space, implicitly assuming independence across hierarchy levels. However, this is suboptimal because it ignores cross-level semantic containment, e.g., actions belong to steps, steps compose phases, weakens long-range dependency modeling. To this end, we propose a hyperbolic surgical video-language pre-training framework that explicitly preserves the hierarchical structure by mitigating structural false negatives induced by procedural context and enforcing semantic consistency between parent phases and their constituent child steps. Extensive experiments on multiple surgical benchmarks show consistent gains in zero- and few-shot phase recognition across procedures and institutions.

---

## 论文详细总结（自动生成）

# 论文总结：HyperVLP：在双曲空间中增强分层手术视频-语言预训练

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：手术视频-语言预训练模型通常从教学视频中学习，但手术知识天然具有层次结构（细粒度动作→中级步骤→全局阶段）。现有方法（如SurgVLP、HecVL、PeskaVLP）将这些异质信号压缩到单一的欧几里得嵌入空间中，忽略层级间的语义包含关系（如动作属于步骤、步骤构成阶段），导致跨层级的依赖建模不足，且引入“结构假负例”（将语义上相近的样本错误地视为负样本）。
- **意义**：提出一种在双曲空间中显式建模手术工作流层次结构的预训练框架，旨在提升零样本和少样本下的手术阶段识别性能，特别是跨手术类型和跨机构的泛化能力。

## 2. 方法论
### 2.1 核心思想
- 将视频和文本表示投影到共享的Lorentz双曲流形中，利用双曲空间的负曲率和指数容量自然保持层次包含关系。
- 设计**几何感知双曲对比损失**，缓解结构假负例。
- 引入**基于锥的双曲蕴含目标**，在跨模态和模态内同时施加父子语义一致性约束。
- 采用**两阶段渐进优化策略**稳定训练。

### 2.2 关键技术细节
- **层次嵌入**：使用ResNet-50（视觉编码器）和BioClinicalBERT（文本编码器），提取特征后通过指数映射将欧几里得特征投影到Lorentz流形。
- **几何感知双曲对比损失**：
  - 将候选样本分为三类：结构相关样本（S(i)，同过程内时间相邻或层级包含）、正样本对（P(i)）、负样本（N(i)，跨过程样本）。
  - 损失函数中引入自适应权重α_ik，抑制结构相关样本的过度分离，保留层次拓扑。
- **层次蕴含学习**：
  - 采用Lorentz空间中的双曲蕴含锥，通过角度惩罚强制子节点落在父节点的锥区域内。
  - 跨模态蕴含：文本概念包含其对应视频（如“切割”包含所有切割剪辑）。
  - 模态内蕴含：强制父级（如阶段）包含其子级（如步骤）嵌入。
- **两阶段训练**：
  - 第一阶段仅使用几何感知对比损失，建立鲁棒的跨模态对齐。
  - 第二阶段冻结编码器，仅微调映射层，联合优化对比损失和蕴含损失。

## 3. 实验设计
### 3.1 数据集与基准
- **训练数据集**：Surgical Video Lectures (SVL) 数据集，包含大规模手术讲座视频及ASR转录、关键步骤描述和视频级摘要。显式构建了三级层次单元（细粒度片段、阶段级段落、完整视频）。
- **下游数据集**（评估通用性）：
  - Cholec80（胆囊切除术）
  - AutoLaparo（子宫切除术）
  - MultiBypass140（胃绕道手术），包含两个中心子集：StrasBypass70（Stras70）和BernBypass70（Bern70），用于跨中心评估。
- **评估设置**：零样本阶段识别（Acc/F1）、少样本/全样本线性探测（10%和100%标签）。

### 3.2 对比方法
- 通用模型：MIL-NCE、CLIP（ImageNet/400M预训练）、MoCo。
- 手术VLP基线：SurgVLP、HecVL、PeskaVLP（均基于SVL数据集，相同骨干网络）。

## 4. 资源与算力
- 论文明确提到：第一阶段使用8块NVIDIA A100 GPU，全局batch size 256；第二阶段使用单GPU，batch size 128。
- 训练时长未明确报告，仅提及使用AdamW优化器，初始学习率5e-5（第一阶段）和1e-5（第二阶段）。
- 其余细节（如总迭代步数、训练时间）未给出。

## 5. 实验数量与充分性
- **主要实验**：
  - 零样本阶段识别：在4个数据集上对比6种方法，报告Acc/F1。
  - 少样本/全样本线性探测：在4个数据集上，不同监督方式（V/L/VL），包含10%和100%两种设置，对比5种基线。
  - 消融实验：逐步添加损失组件（L_GeoHCL、L_inter、L_intra），在4个数据集上验证效果。
  - 定性分析：展示双曲空间中距离原点的径向分布，验证层次分离。
- **充分性评价**：实验覆盖了跨手术类型、跨中心、零样本/少样本/全样本多种场景，消融实验完整，对比基线包含通用模型和当前最优手术VLP方法，公平性合理（均使用相同SVL数据和骨干）。但未报告统计显著性检验或多次重复实验的方差。

## 6. 主要结论与发现
- HyperVLP在所有基准上取得最佳结果，尤其在跨中心（Stras70和Bern70）和跨过程（AutoLaparo）上提升显著（例如零样本Stras70上Acc达52.7%，远超PeskaVLP的46.7%）。
- 双曲空间中的层次建模优于欧几里得空间中的层次方法（如HecVL、PeskaVLP），表明几何归纳偏置是关键。
- 几何感知对比损失（L_GeoHCL）和跨模态蕴含损失（L_inter）贡献最大，模态内蕴含（L_intra）进一步稳定层次结构。
- 径向距离分布显示双曲嵌入自然区分不同语义粒度，且跨数据集保持稳定。

## 7. 优点
- **创新性**：首次将双曲几何应用于手术视频-语言预训练，解决了欧几里得空间无法自然建模层次包含的固有限制。
- **技术设计**：巧妙识别了手术场景中的“结构假负例”并设计自适应抑制机制，理论动机清晰。
- **实验充分性**：覆盖多种下游场景（跨手术、跨中心），对比了现有最优算法，消融实验完整，定性分析直观。
- **实用性**：在零样本和少样本设置下显著提升，降低了对密集标注的依赖，具有临床推广潜力。

## 8. 不足与局限
- **算力与训练细节不透明**：未报告总训练时长、迭代次数、收敛曲线等，不利于复现和资源评估。
- **数据集局限**：所有下游数据集均来自腹腔镜手术（胆囊、子宫、胃绕道），未验证其他手术类型（如眼科、骨科）的泛化性。
- **骨干网络较老**：视觉编码器使用ResNet-50，文本使用BioClinicalBERT，可能未充分利用近期更强的基础模型（如ViT、LLM）。
- **缺乏统计检验**：未报告多次实验的均值和标准差，结果置信度未知。
- **层次结构构建依赖人工注释**：训练数据中父子关系的构建依赖于已有的结构化标注（如关键步骤划分），在缺乏此类标注的场景下应用受限。
- **可能的数据偏差**：训练数据来自教学视频，与真实手术视频在分布上可能存在差异（如叙事结构、镜头切换频率不同）。

（完）
