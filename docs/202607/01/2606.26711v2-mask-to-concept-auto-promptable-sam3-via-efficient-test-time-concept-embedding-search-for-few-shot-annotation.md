---
title: "Mask to Concept: Auto-Promptable SAM3 via Efficient Test-Time Concept Embedding Search for Few-Shot Annotation"
title_zh: 掩码到概念：通过高效测试时概念嵌入搜索实现自动可提示的SAM3用于小样本标注
authors: "Quan Zhou, Shaoqing Zhai, Qiang Hu, Jia Chen, Qiang Li, Zhiwei Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26711v2"
tags: ["query:vlmmed"]
score: 9.0
evidence: 自动提示SAM3的医学少样本分割
tldr: 将基础分割模型从人工提示工具转变为自动可提示的标注器对医学数据标注至关重要。本文提出Mask to Concept (M2C)框架，无需外部模块、参数重训练或手动文本工程，仅用少量标注图像即可使SAM3自动搜索可转移视觉概念。通过可学习概念嵌入和混合不确定性估计模块，实现高效少样本标注，在医学分割基准上达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26711v2/fig-001.webp\", \"caption\": \"Fig. 3. Annotation efficiency curves on Kvasir-SEG and ISIC-2017 datasets.\", \"page\": 7, \"index\": 1, \"width\": 718, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-002.webp\", \"caption\": \"Table 1. Quantitative comparison with SOTA FSS methods on Kvasir-SEG and ISIC2017 datasets. We report the mean and standard deviation (µ±σ) for the Dice metric as percentages(%). Methods are divided into training-based (top) and training-free (bottom). The best results are highlighted in bold, and the second-best are underlined.\", \"page\": 6, \"index\": 2, \"width\": 725, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-003.webp\", \"caption\": \"Fig. 2. Visualization of 1-shot results on Kvasir-SEG (top) and ISIC-2017 (bottom).\", \"page\": 6, \"index\": 3, \"width\": 725, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed human-in-the-loop annotation framework. It consists of two core iterative modules: Mask to Concept (M2C) and Hybrid Uncertainty Estimation (HUE). During each iteration, given a few human-labeled samples from the previous iteration, M2C employs a continuous searching on the dataset-specific concept embedding to automatically segment the remaining unlabeled samples. Following this, HUE estimates the uncertainties of these predictions and applies tailored strategies to the samples based on the uncertainty as they transition into the next iteration.\", \"page\": 4, \"index\": 4, \"width\": 725, \"height\": 281}]"
motivation: 将SAM3从人工提示工具转变为自动提示标注器，解决医学图像中缺乏细粒度临床知识和人工描述模糊的问题。
method: 提出M2C框架，通过可学习概念嵌入搜索和混合不确定性估计，在冻结SAM3架构内自动寻找可转移视觉概念。
result: 在医学分割基准上实现少样本分割SOTA性能和卓越的标注效率。
conclusion: 提供了一种可扩展的医学图像标注实用高效途径。
---

## 摘要
将基础分割模型从人工提示工具转变为自动可提示的标注器对于可扩展的医学数据标注至关重要。当前方法通常依赖外部特征匹配器或辅助网络来自动化几何提示，但引入了架构开销并限制了性能可扩展性。尽管SAM3通过可重用文本提示原生支持概念分割，但它在医学影像中的直接使用受到缺乏细粒度临床知识和人工描述模糊性的阻碍。在这项工作中，我们提出了Mask to Concept（M2C），一种高效的框架，无需外部模块、参数重新训练或手动文本工程，即可使SAM3适应医学小样本标注。仅使用少量标注图像，M2C使SAM3能够在完全冻结的架构内自动搜索可迁移的视觉概念：它初始化一个可学习的概念嵌入，用它来提示分割，并通过最小化概念分割误差的梯度来更新嵌入。我们进一步引入混合不确定性估计（HUE）模块，该模块计算预测熵并将概念预测映射回框提示，衡量概念-几何提示不一致性。高不确定性的样本会被主动标记以供人工纠正，纠正后的掩码反馈给M2C以持续搜索更精确的概念嵌入，从而形成最小化专家努力的自我增强标注循环。在医学分割基准上的实验表明，我们的方法实现了最先进的小样本分割性能和出色的标注效率，为可扩展的医学图像标注提供了实用且高效的途径。代码位于https://github.com/Huster-Hq/M2C。

## Abstract
Transforming foundation segmentation models from human-prompted tools into auto-promptable annotators is critical for scalable medical data annotation. Current methods commonly depend on external feature matchers or auxiliary networks to automate geometric prompting, but introducing architectural overhead and limiting performance scalability. Although SAM3 natively supports concept segmentation via reusable text prompts, its direct use in medical imaging is hindered by a lack of fine-grained clinical knowledge and the ambiguity of human-written descriptions. In this work, we propose Mask to Concept (M2C), an efficient framework that adapts SAM3 for medical few-shot annotation without external modules, parameter retraining, or manual text engineering. Using only a few labeled images, M2C enables SAM3 to automatically search for transferable visual concepts entirely within its frozen architecture: it initializes a learnable concept embedding, uses it to prompt segmentation, and updates the embedding by gradients of minimizing the concept segmentation error. We further introduce a Hybrid Uncertainty Estimation (HUE) module that calculates the prediction entropy and maps concept predictions back to the box prompts, measuring concept-geometry prompting inconsistency. Highly uncertain samples are flagged actively for human correction, and the corrected masks are then fed back to M2C to continuously search for more precise concept embeddings, forming a self-enhancing annotation loop with minimal expert effort. Experiments on medical segmentation benchmarks show that our method achieves SOTA few-shot segmentation performance and outstanding annotation efficiency, offering a practical and efficient pathway toward scalable medical image labeling. Codes are at https://github.com/Huster-Hq/M2C.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇名为《Mask to Concept: Auto-Promptable SAM3 via Efficient Test-Time Concept Embedding Search for Few-Shot Annotation》的论文进行结构化、深入、客观的总结。

---

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：基础分割模型（如 SAM、SAM2、SAM3）在医学图像标注中通常作为需要人工提示的交互式工具，难以实现大规模、低成本的自动化标注。现有的自动标注方法要么依赖外部特征匹配或辅助网络，增加了系统复杂度；要么需要在小样本分割（FSS）范式下进行预训练，泛化性差。
- **整体背景**：数据标注瓶颈严重制约了医学AI的发展。付费的人工标注昂贵且耗时，而人工提示模型需要为每张图像提供提示，无法摊销人力成本。当前研究趋势是将“人工提示工具”转变为“自动提示标注器”。
- **研究动机**：最新提出的SAM3模型原生支持通过“文本提示”进行“概念分割”，这为构建跨实例的、可复用的概念驱动自动标注器提供了可能。然而，直接使用文本提示在医学领域面临挑战：（1）SAM3缺乏细粒度临床概念知识；（2）人工书写的文本描述（如分类名称）往往过于模糊，无法可靠地指导自动化标注。因此，本文旨在探索如何在**不依赖外部模块、不进行FSS预训练、不重新训练SAM3**的前提下，解锁并适配SAM3的概念分割能力，用于高效的医学小样本标注。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出 **Mask to Concept (M2C)** 框架。在一个**完全冻结**的SAM3模型内部，通过仅从**少量已标注的掩码**中学习一个“**可学习的概念嵌入向量**”，使SAM3能够自动搜索并精炼一个适用于当前数据集的、可迁移的视觉概念，从而替代模糊的文本提示。
- **关键技术细节**：
    1.  **Mask-to-Concept (M2C) 概念搜索**：
        - **初始化**：初始化一个可学习的向量 `E`（通常为全零向量），用于修正原文本编码器输出的概念嵌入。即：`y_pred = SAM(x, TEnc(T) + E)`。
        - **优化过程**：冻结整个SAM3模型，仅优化向量 `E`。利用已标注样本 `(x_label, y_label)`，通过最小化分割损失（IoU损失+交叉熵损失）的梯度来迭代更新 `E`。
        - **公式**：`E_t = E_{t-1} - η * ∇L_seg`。这个过程是“持续搜索”的，随着注释样本的增加，`E` 会被不断精炼，从而自然支持增量式小样本学习。
    2.  **混合不确定性估计 (Hybrid Uncertainty Estimation, HUE)**：
        - 目的：在主动学习循环中，智能识别哪些自动化分割结果需要人工校正。
        - **子指标1：预测熵 (Prediction Entropy, U_ENT)**：计算预测掩码中模糊区域（熵 > 0.8）的平均香农熵，衡量模型在决策边界上的不确定性。
        - **子指标2：概念-几何提示不一致性 (Concept-Geometry Prompting Inconsistency, U_CGPI)**：将概念驱动的预测掩码转换成一个边界框，并将其作为几何提示重新输入SAM3，得到另一个几何驱动的掩码。两个掩码之间的 IoU 分数越低（即 `1-IoU` 越高），表示两种提示方式的结果越不一致，不确定性越高。
        - **综合不确定性**：`U_HU = U_ENT + U_CGPI`。
    3.  **人机协同标注循环**：
        - 将不确定性得分排序，高不确定性样本被路由给人修正。修正后的掩码反馈给M2C，进一步优化概念嵌入 `E`。
        - 低不确定性样本可选择提前退出（early-exit），直接作为最终标注结果。这形成了一个自我增强的闭环。

### 3. 实验设计
- **使用数据集**：
    - **Kvasir-SEG**：内窥镜息肉分割数据集（1000张图像）。
    - **ISIC-2017**：皮肤病变分割数据集（使用官方600张测试集，避免数据泄露）。
- **Benchmark与对比方法**：
    - 对比了六种最先进的小样本分割方法，分为两类：
        - **基于训练的方法**：`Multiverseg`、`Tyche`。
        - **训练无关的方法**：`ProtoSAM`、`Matcher`、`SPFS-SAM`、`MAUP`。
    - 为保证公平，所有基于SAM的基线方法（如 ProtoSAM, SPFS-SAM）均使用 SAM-H 作为骨干。
- **评估设置**：
    - **小样本分割 (FSS)**：将每个数据集按照 1:9 分为支持集和查询集。在1-shot、5-shot、10-shot设置下评估，每个查询样本与K个支持样本配对，进行5次独立测试取平均和标准差。
    - **标注效率**：模拟自动化标注流程，以**不同人工标注样本量**为x轴，**整体数据集标注质量**（Dice）为y轴，绘制效率曲线。对比方法包括 Multiverseg 和 SPFS-SAM。

### 4. 资源与算力
- **文中明确提到**：M2C的嵌入优化过程在**单张NVIDIA RTX 4090 GPU**上执行。
- **优化器**：AdamW。
- **未明确说明**：论文没有报告整个方法的训练时长（如从开始到完全收敛需要多少分钟或小时），也没有说明是否需要在多GPU上进行并行训练。由于M2C本身是测试时优化，其单次迭代的时间效率未被量化。

### 5. 实验数量与充分性
- **实验数量**：论文进行了多组实验，包括：
    - **核心FSS实验**：在两个数据集（Kvasir-SEG, ISIC-2017）上的3种shot设置（1, 5, 10-shot），与6种方法对比，有定量和定性结果。
    - **标注效率实验**：在两个数据集上，对比了3种方法（M2C, Multiverseg, SPFS-SAM）在不同人工标注预算下的表现。
    - **消融实验**：
        - **HUE有效性消融**：对比随机选择、仅HUE（无early-exit）、完整HUE三种配置。
        - **HUE子指标消融**：对比单独使用 U_ENT、U_CGPI 以及两者结合时，达到90% Dice所需的人工标注成本。
- **充分性与客观性**：
    - **优点**：实验设计比较全面。FSS实验对比了多种主流方法，并报告了均值±标准差，体现了统计稳定性。标注效率实验直接模拟了实际应用场景，非常实用。消融实验清晰地验证了每个模块的有效性。
    - **不足**：
        - 仅使用两个数据集，领域多样性有限（仅息肉和皮肤病变）。
        - 对比方法中，部分方法（如Multiverseg, Tyche）在1-shot设置下性能较差，可能削弱了对比的冲击力。
        - 在标注效率实验中，仅与Multiverseg和SPFS-SAM进行了对比，而没有与基于特征匹配的方法（如Matcher, ProtoSAM）进行普遍比较，可能不够全面。

### 6. 论文的主要结论与发现
- **M2C在FSS任务上达到SOTA**：在1-shot设置下，M2C在Kvasir-SEG和ISIC-2017上分别比最先进的 ProtoSAM 高 **4.2%** 和 **11.8%** 的Dice分数，证明了其从极少样本中学习有效概念表示的强大能力。
- **M2C的标注效率卓越**：在自动化标注场景下，M2C在所有数据集上均以最少的人工标注量达到了最佳的总体标注质量，且优势随着标注预算的增加而扩大。
- **HUE模块有效提升效率**：HUE的混合不确定性机制（预测熵+概念-几何不一致性）能有效区分“困难样本”和“简单样本”。结合early-exit策略，可以大幅减少不必要的人工修正，将有限的人力集中在最有价值的样本上。
- **方法论高效**：无需外部模块、无需预训练、无需微调SAM3，仅通过优化一个轻量级嵌入向量即可实现领域适配，计算成本极低。

### 7. 优点
- **高度创新性**：首次成功解锁了SAM3原生的文本概念分割能力用于医学领域，并巧妙地将其转化为一种测试时间搜索问题，跳出了以往依赖几何提示或外部特征匹配器的范式。
- **完全冻结与低训练成本**：方法不修改SAM3的任何参数，仅优化一个很小的嵌入向量，避免了昂贵的模型重新训练/微调，且收敛极快。这一特性对于在资源受限环境下部署或频繁更换数据集（医疗中很常见）具有极大吸引力。
- **系统性框架**：不只是提出了一个孤立的M2C模块，而是将其整合到HUE和人工反馈的主动学习循环中，构建了一套完整、实用、高效的人机协同标注系统。
- **可解释性强**：HUE模块提供了一个明确的不确定性度量，用于指导何时需要人工介入，使得整个过程的透明度和可控性较高。

### 8. 不足与局限
- **实验覆盖有限**：如前所述，验证仅在两个公开数据集上进行，尚未在更广泛、更复杂的多器官、病理亚型等医学数据集上验证，其泛化性和鲁棒性有待进一步检验。
- **对比基线选择**：与部分基线方法（如Matcher）的对比中，M2C的优势巨大，这可能部分归因于基线方法本身在特定数据集上表现不佳；与更多近期方法（如通过语义适配器增强的SAM2）的对比可以进一步增强结论的普适性。
- **关于人工投入**：虽然减少了总人工量，但系统依然依赖“少数”初始手动标注和后续的不确定性校正。论文未讨论初始样本的选择策略（如是否需要多样化/代表性样本？），以及如果初始样本质量不佳或分布偏移，系统性能会如何下降。
- **失败案例分析**：论文没有提供任何失败案例的分析（例如，哪些类型的病变或图像背景会导致HUE失效或M2C搜索失败）。缺乏对方法边界的探讨，可能使得实际部署中的风险被低估。
- **计算成本分析不充分**：虽然资源需求低，但论文没有量化解码时间或嵌入优化的时间成本，这使得无法与其他方法进行严格的效率比较。

（完）
