# 创新点总结 · 2026-07-01

- 生成时间：2026-07-01 22:15:52 UTC
- 当日论文数：36

## 单篇方法拆解

### 1. DataComp-VLM: Improved Open Datasets for Vision-Language Models
- 区域：deep；分数：10.0
- 原文链接：https://arxiv.org/abs/2606.28551v1
- 方法流程：
  - 数据池构建：收集160个公开数据集，分为图像-标题对、多模态交错文档、纯文本、指令微调四类，经去污染处理（图像用SSCD嵌入余弦相似度>0.75，文本用MinHash Jaccard相似度>0.55）避免与测试集重叠。
  - 数据策展：参与者可对数据池应用过滤策略（全局或局部）或混合策略（调整各类数据比例），在固定池/训练token比例下选择指定规模。
  - 模型训练：固定架构（InternViT-300M视觉编码器 + 2层MLP投影器 + Qwen2.5-Base语言模型），使用AnyRes图像分块（448×448 tiles，每tile 256视觉token），AdamW优化器，3%线性预热，余弦学习率衰减（峰值2e-5）。
  - 评估：在52个基准的嵌套套件上评估，报告Validation（13任务）、Core（33任务）和Extended（52任务）平均准确率。
- 关键模块/机制：
  - 数据池构建与去污染：确保训练与测试数据无重叠，避免评估偏差。；对多模态样本使用ResNet-50 SSCD嵌入模型，余弦相似度>0.75则剔除；对纯文本样本使用MinHash Jaccard相似度>0.55剔除。
  - AnyRes图像分块：动态将图像分割为448×448的tiles，支持高分辨率输入。；每张图被分割为多个448×448的tiles，每个tile经像素混洗后编码为256个视觉token，最后拼接所有token。
  - 嵌套评估套件：提供从快速验证到全面分析的层级化评估。；从65个候选基准中筛选：先去除高方差基准（稳定性），再去除从小规模到大规模不单调提升的基准（单调性），最终形成Validation（13任务）、Core（33任务）、Extended（52任务）三层。
  - 数据混合策略：对比不同数据类型的混合比例对最终性能的影响。；固定训练token预算，调整图像-标题对、多模态交错文档、纯文本、指令微调数据的相对比例，发现增加指令微调数据比例（甚至超过50%）带来显著性能提升，且随模型规模扩大优势增加。
- 算法细节：
  - 过滤策略：对图像-标题对使用CLIP分数（OpenAI-CLIP, SigLIP-2等）全局排序保留高分数样本；对文本数据使用文本质量分类器（DCLM FastText, NVIDIA Mixtral等）过滤；对多模态数据使用条件互信息、多模态困惑度等度量过滤。所有过滤均设定阈值（小规模保留top-10%，中规模保留top-40%）。
  - 混合策略：在固定token预算下，按比例从各类数据中采样，例如指令微调数据占总token的20%、40%等，其余由图像-标题对和多模态文档补充。
  - 去污染算法：图像用SSCD特征余弦相似度阈值0.75；文本用MinHash Jaccard相似度阈值0.55，确保训练集与52个评估基准无重叠。
- 训练/推理细节：
  - 训练使用AdamW优化器，初始学习率2e-5，采用余弦退火调度，前3%步数线性预热。
  - 图像编码器InternViT-300M，语言模型使用Qwen2.5-Base系列（0.5B/1.5B/3B/7B对应四个规模）。
  - 训练采用ChatML格式（<|im_start|>和<|im_end|>分隔角色），所有数据类型统一格式化。
  - 训练token预算：small 6.25B, medium 25B, large 100B, x-large 200B。模型参数：1B/2B/4B/8B。

### 2. RASC+: Retrieval-Constrained LLM Adjudication for Clinical Value Set Authoring
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.23992v1
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 3. IV-CoT: Implicit Visual Chain-of-Thought for Structure-Aware Text-to-Image Generation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.24849v1
- 方法流程：
  - 输入：文本描述
  - 多模态大模型处理文本，生成视觉条件查询
  - 视觉条件查询分解为结构查询和语义查询
  - 结构查询形成隐式视觉计划（结构规划）
  - 语义查询以结构计划为条件渲染外观（语义渲染）
  - 输出：最终图像
- 关键模块/机制：
  - 结构查询：捕获对象计数、空间关系等结构信息；通过训练时草图监督学习从草图中提取结构，推理时仅依赖文本。
  - 语义查询：根据结构计划渲染外观细节；以结构查询的输出为条件，进行外观渲染。
  - 训练时草图监督：引导结构查询学习结构表示；训练过程中使用草图作为监督信号，推理时不使用草图或中间解码。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式
- 算法细节：
  - IV-CoT在单次前向传播中执行隐式CoT推理，无需自回归解码。
  - 结构查询和语义查询分解与级联构成核心算法。
- 训练/推理细节：
  - 训练时使用草图监督结构查询；
  - 推理时仅需文本输入，单次前向传播生成图像。

### 4. SSMNBench: Diagnosing Image-based Cross-View Human-Object Understanding via Single-View Sufficiency and Multi-View Necessity
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.25634v1
- 方法流程：
  - 1. 数据收集与预处理：从Core4D等8个多视图数据集中选取密集遮挡场景，提取4个最小重叠视图
  - 2. 任务定义与标注：定义11个任务（5个SVS、6个MVN），专家为每个QA对标注黄金视图（SVS）或最小必需视图子集（MVN）
  - 3. 干扰项生成：为每个问题生成3个结构化干扰选项（实体干扰、空间邻近干扰等），减少语言先验
  - 4. 质量控制：通过盲审验证答案可回答性、唯一性、无常识依赖
  - 5. 视图扰动设置：对每个QA对生成Normal（原始4视图）、+1/+2/+3（增加冗余视图）、-1（删除1视图）共5种输入配置
  - 6. 评估：报告每种配置下的准确率，并计算SVS任务的Distraction Decay（δ_dis = acc(Normal) - acc(+k)）
- 关键模块/机制：
  - SVS/MVN分类模块：将任务按所需视觉证据的视图分布分类；SVS任务：存在至少一个黄金视角可独立回答；MVN任务：无单视图可回答，需融合多视图互补证据。专家根据场景标注每个QA对的黄金视图（SVS）或最小必需视图子集（MVN）。
  - 视图扰动协议：系统改变输入视图数量，诊断模型行为变化；基于原始4视图，构造Normal（4视图）、+1/+2/+3（按顺序插入额外视图）、-1（随机移除1视图）共5种输入变体。视图顺序随机化以消除位置偏好。
  - Distraction Decay指标：量化冗余视图对SVS任务的干扰程度；δ_dis = acc(Normal) - acc(+k)，其中k=1,2,3。正值表示添加冗余视图导致准确率下降，反映模型注意力被分散。
- 公式/目标函数/指标定义：
  - Distraction Decay (δ_dis) = accuracy(Normal) - accuracy(+k), for k=1,2,3 in SVS tasks
- 算法细节：
  - SVS黄金视图标注：专家选取一个可独立回答问题的视图作为V_GT，并确保其他视图可能冗余
  - MVN最小视图子集标注：专家确定至少需要哪几个视图的组合才能回答问题，排除单视图可答的情况
  - 干扰项生成：基于场景语义和空间关系生成3个错误选项，包括实体干扰（类似物体）、空间邻近干扰（位置接近但属性不同）等
  - 视图顺序随机化：在输入给MLLM前，打乱视图顺序以消除相机位置偏见
- 训练/推理细节：
  - 不需要训练，直接评估现成MLLM的零样本推理能力
  - 输入为多张图像（数量可变）和对应文本问题，输出为从4个选项中选择答案
  - 评估17个MLLM，包括GPT-4o、Gemini 1.5 Pro、Qwen2-VL-72B等，均使用官方推理接口或开源模型默认设置

### 5. Steering Vision-Language Models with Joint Sparse Autoencoders
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.25657v1
- 方法流程：
  - 阶段1: 层定位——通过探针实验识别视觉-语言对齐分离性最强的层范围（文中未详述但提及）
  - 阶段2: JSAE训练——输入平均池化的视觉和文本激活(Av, At)，经独立编码器(Ev, Et)得到稀疏编码(zv, zt)，通过复合损失(重建+稀疏+余弦对齐)训练，输出跨模态共享特征
  - 阶段3: 方向聚类——使用Algorithm 1对高方差神经元进行L2归一化激活谱K-means聚类，选择top-K语义簇作为最终对齐特征(F_aligned)
  - 阶段4: 干预——根据公式(5)聚合原子特征文本解码器列得到宏观方向v_steer，在图像token位置乘以掩码m进行加法或减法干预
- 关键模块/机制：
  - 序列池化：将变长视觉和文本token序列转换为固定尺寸的宏观表示；对目标层隐藏状态分别按模态平均池化：Av = mean(h_v), At = mean(h_t)，消除序列长度影响
  - JSAE编码器-解码器对：独立编码每个模态的池化激活到稀疏潜空间，并重建；每个模态使用独立编码器E(·)=ReLU(W_e·+b_e)映射到k维(k≫d)，解码器D重建输入；共享潜空间但保持各自参数
  - 余弦对齐损失：迫使配对视觉和文本稀疏编码方向一致，鼓励共激活；L_align = 1 - cos(z_v, z_t) = 1 - (sum_{i∈S}(z_v_i z_t_i))/(||z_v||_2 ||z_t||_2)，其中S为两模态非零索引交集
  - 方向聚类(Algorithm 1)：从原子特征中聚合宏观语义方向；过滤方差<1e-5的神经元，对每个神经元构建L2归一化激活剖面a_i（N样本），K-means聚类，按轮廓分数排序，人工检查丢弃多义簇，保留top-K
  - 双向干预机制：通过加法引导或抑制测试特征的因果作用；加法：H' = H + α·v_steer ⊙ m，抑制：H' = H - α·v_steer ⊙ m，其中v_steer为聚合归一化文本解码器列，m仅限于图像token位置，α通过网格搜索校准
- 公式/目标函数/指标定义：
  - 池化公式: Av = (1/N) Σ h_v,i, At = (1/M) Σ h_t,j
  - 总损失: L_total = λ_r L_recon + λ_s L_sparse + λ_a L_align
  - 重建损失: L_recon = 0.5(||Âv - Av||^2 + ||Ât - At||^2)
  - 稀疏损失: L_sparse = 0.5(||z_v||_1 + ||z_t||_1)
  - 对齐损失: L_align = (1/B) Σ (1 - cosine_sim(z_v^(b), z_t^(b)))
  - 余弦相似度: cos(z_v, z_t) = (Σ_{i∈S} z_v,i z_t,i)/(||z_v||_2 ||z_t||_2)，S为两模态非零索引交集
- 算法细节：
  - Algorithm 1: 直接语义特征发现——输入N个样本的配对潜激活Z_v, Z_t ∈ R^{N×d_latent}，计算每个神经元方差σ_i^2，保留>1e-5的候选索引；对每个候选神经元构建L2归一化剖面向量a_i ∈ R^N；对所有剖面执行K-means聚类；按平均轮廓分数排序簇；人工检查丢弃多义簇，保留top-K作为最终对齐特征F_aligned
  - 干预向量聚合：对属于同一语义的高维原子特征，将其对应的文本解码器列w_t,i归一化后求和得到宏观方向v_steer
  - 干预操作：仅在图像token位置添加/减去宏概念方向，通过模态掩码m实现
- 训练/推理细节：
  - 训练：JSAE使用复合损失L_total，其中λ_r, λ_s, λ_a为超参数；编码器采用ReLU激活，潜空间维度k≫d；优化器未详述但为梯度下降变体
  - 推理：干预时，通过网格搜索校准缩放因子α以平衡语义效果和流畅度；方向向量来自文本解码器列，而非视觉解码器

### 6. Mask to Concept: Auto-Promptable SAM3 via Efficient Test-Time Concept Embedding Search for Few-Shot Annotation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.26711v2
- 方法流程：
  - 输入：少量标注图像（支持集）和未标注图像；
  - M2C阶段：从标注图像学习概念嵌入，更新后用于分割未标注图像；
  - HUE阶段：对预测mask计算混合不确定性分数，排序后高不确定性样本送人工修正，低不确定性样本可选提前退出；
  - 迭代：将修正后的mask反馈给M2C继续搜索更好的概念嵌入，直到所有图像标注完成。
- 关键模块/机制：
  - M2C (Mask to Concept)：学习数据集特定的概念嵌入；引入可学习向量E，与文本编码器的概念嵌入相加，输入SAM3。从支持集图像中，冻结SAM3参数，仅优化E，最小化分割损失（IoU损失+交叉熵损失），通过梯度下降迭代更新。
  - HUE (Hybrid Uncertainty Estimation)：估计预测不确定性，区分难易样本；计算两个子指标：(1) 预测熵：对预测mask中像素熵>0.8的像素取平均熵；(2) 概念-几何提示不一致性：将概念预测mask转为box，用box几何提示SAM3得到几何mask，计算两者IoU，分数=1-IoU。最终不确定性为两者之和。根据阈值τ（0.6）将预测分为难易样本。
- 公式/目标函数/指标定义：
  - L_seg = IoU loss + cross-entropy loss（原生SAM3损失）
  - E_t = E_{t-1} - η ∇_{E_{t-1}} (1/|D| Σ L_seg(y_pred, y_label))
  - U_ENT = (1/|Ω|) Σ_{i∈Ω} H(p_i)，其中Ω={i|H(p_i)>0.8}，H(p_i) = -[p_i log p_i + (1-p_i) log(1-p_i)]
  - U_CGPI = 1 - IoU(y_pred, y_box)
  - U_HU = U_ENT + U_CGPI
- 算法细节：
  - 持续搜索：每次迭代使用当前已标注样本集，从上一轮嵌入E_{t-1}初始化，在标注样本上梯度下降优化若干步；优化器AdamW，学习率5e-4余弦退火至5e-6。
  - 概念嵌入初始化：全零向量。
  - HUE中，将预测mask转为box：使用预测mask的最小和最大坐标生成外接矩形边界框。
  - 主动选择：根据不确定性排序，选择top-|D_t|个硬样本进行人工修正，其余硬样本继续等待下一轮，易样本早期退出。
- 训练/推理细节：
  - 训练：冻结SAM3全部参数，仅优化概念嵌入向量E；优化器AdamW；学习率5e-4余弦退火至5e-6。
  - 推理：使用优化后的E与文本编码器输出相加，输入SAM3得到预测mask；HUE在推理后计算。

### 7. KARLA: Knowledge-base Augmented Retrieval for Language Models
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.26807v1
- 方法流程：
  - 阶段1: 语料生成 - 从知识库中采样事实（贪心迭代满足目标关系频率），为每个主体实体选择k个事实，调用教师LLM生成带内联查询的百科风格段落（约250 tokens），并验证文本中对象匹配和关系覆盖。
  - 阶段2: 模型微调 - 基于生成语料微调预训练LLM，使用掩码下一个token预测损失，对检索结果span（⟨KB⟩ ... ⟨/KB⟩）内的token梯度屏蔽，并随机替换10%的查询结果为 ⟨KB_FAIL⟩。
  - 阶段3: 推理 - 预处理阶段将输入中的实体提及通过候选检索（FAISS + 双编码器）和上下文重排序（小语言模型）映射到规范KB实体并替换为⟨KB⟩ e*: e*_desc ⟨/KB⟩；自回归生成过程中检测到序列⟨r⟩ ⟨subj⟩ s ⟨/subj⟩时触发KB查询，将结果替换为⟨KB⟩ o: o_desc ⟨/KB⟩。
- 关键模块/机制：
  - 关系特殊token ⟨r⟩：触发知识库查询的原子令牌，每个关系对应一个，防止生成不存在的关系，降低推理成本。；初始化时取关系文本标签子令牌嵌入的均值，添加到模型词表中，引入 |R|·d 个新嵌入参数。
  - 内联查询格式：模型与KB交互的接口，由关系token、主体标记和KB响应标记组成。；序列为 ⟨r⟩ ⟨subj⟩ s ⟨/subj⟩ ⟨KB⟩ o: o_desc ⟨/KB⟩，其中o为查询结果，o_desc为KB提供的简短描述。
  - 掩码下一个token预测损失：训练目标，强制模型依赖检索结果而非参数记忆事实。；损失函数 L(θ) = -∑_t m_t log p_θ(x̃_t | x̃_{<t})，其中m_t对检索结果span内的token设为0，其他token为1。
  - 失败令牌 ⟨KB_FAIL⟩：模拟KB查询无结果的情况，训练模型回退到参数知识。；在训练时随机替换10%的成功查询结果为该令牌，模型在推理时遇到此令牌则启用参数知识。
  - 实体链接预处理：将输入文本中的实体提及映射为规范KB实体，替换为标准化格式。；两阶段：bi-encoder候选检索（all-MiniLM-L6-v2 + FAISS）→ Qwen3 1.7B上下文重排序，得到规范实体e*，替换为 ⟨KB⟩ e*: e*_desc ⟨/KB⟩。
- 公式/目标函数/指标定义：
  - L(θ) = -∑_{t=1}^{N} m_t · log p_θ( x̃_t | x̃_{<t} )，其中m_t = 0 若 x̃_t 属于 ⟨KB⟩ ... ⟨/KB⟩ 范围，否则m_t = 1。
- 算法细节：
  - 采样算法：迭代贪心，每次选择当前使用最少的关系r*，均匀采样该关系的一个实体e，获取其所有事实F(e)，剔除已出现2×T次的关系的事实，从中采样k个事实。
  - 推理触发检测：在自回归生成过程中，实时检测是否生成序列 ⟨r⟩ ⟨subj⟩ s ⟨/subj⟩，一旦检测到则暂停生成，执行KB查询（以s为主语、r为关系查询三元组），若存在则返回随机一个对象o，替换为⟨KB⟩ o: o_desc ⟨/KB⟩；若不存在则返回⟨KB_FAIL⟩，模型继续基于参数知识生成。
- 训练/推理细节：
  - 训练语料由教师LLM（GPT-5 mini）根据每个实体子图生成，验证包括对象文本精确匹配和所有采样关系均出现，失败重试最多2次。
  - 训练损失仅对非检索span的token计算梯度，检索span内的token不受监督。
  - 训练时10%的查询结果被替换为⟨KB_FAIL⟩以增强鲁棒性。
  - 推理前对输入进行实体链接预处理，将实体提及替换为规范KB实体及其描述。

### 8. GAVEL: Grounded Caption Error Verification and Localization
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.26923v1
- 方法流程：
  - 数据准备：使用VLM（如Qwen2, Llama-4等）生成图像到文本或文本到图像对，用GPT-5 mini评估对齐分数，筛选低分对作为候选负样本。
  - 人工标注：将候选对提交给标注者，要求对不一致文本提供自然语言解释，并绘制对应的边界框；经过三阶段审核保证质量。
  - 训练基线：采用VisionLLM框架，在35k训练对上微调7个epoch，得到监督基线模型。
  - 评估：在5k测试对上进行评估，使用IoU@0.3/0.5/0.7衡量定位，GPT-5自动评分（0-5）衡量解释质量。
- 关键模块/机制：
  - 半自动数据生成模块：生成候选不一致对，降低人工标注成本；使用VLM生成初始图像-文本对，再用GPT-5 mini评估对齐概率，将低概率对送入人工标注流程。
  - 人工标注流程：提供高质量的解释和边界框标注；每个样本分配给一位标注者和一位审阅者，审阅修正后由额外审阅者随机抽查，采用三阶段验证。
  - 监督微调基线：验证数据集的可学习性；基于VisionLLM框架，在35k训练对上微调，使用标准交叉熵损失优化解释生成和边界框回归。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式
- 算法细节：
  - 候选对筛选：使用GPT-5 mini对每个图像-文本对输出对齐分数，选取分数最低的N个对作为负样本候选。
  - 训练时，VisionLLM同时输出解释的token序列和边界框坐标，利用多任务损失联合优化。
- 训练/推理细节：
  - 训练数据：35k人工标注的图像-文本-解释-边界框对。
  - 训练框架：VisionLLM，微调7个epoch。
  - 推理时：输入图像和文本，模型输出解释文本和边界框坐标。

### 9. ViQ: Text-Aligned Visual Quantized Representations at Any Resolution
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.27313v2
- 方法流程：
  - 输入：任意分辨率图像。
  - 预处理：将图像缩放到最大面积（如768^2），保持原始宽高比，并划分为不重叠的patch（如patch size=16）。
  - 特征提取：使用SigLIP2-g视觉编码器（输出特征维度C=1536）提取每个patch的视觉特征。
  - 降维：通过瓶颈层将特征维度从C=1536降至D=128，再降至d=6。
  - 近端表示学习：逐步对特征空间进行正则化：首先应用BatchNorm+L2，然后BatchNorm+L∞，最后使用FSQ量化。FSQ使用levels=[8,8,8,5,5,5]，总码本大小8^3*5^3=64,000。
  - 位置感知头级量化：在量化前，为每个空间位置（patch）添加可学习的位置编码，每个量化头独立编码位置信息，使得不同位置的离散令牌具有位置感知性。
  - 输出：每个patch被量化成一个d维离散令牌（d=6），所有令牌组成图像的离散表示，可以投影到LLM latent空间或传递给解码器重建图像。
- 关键模块/机制：
  - 视觉编码器（SigLIP2-g）：提取图像特征，支持任意分辨率输入。；使用预训练的SigLIP2-g模型（参数量1.1B），输出特征维度C=1536。通过文本对齐预训练（利用语言模型Qwen2.5-VL-0.5B作为文本监督）将其语义能力增强，并使其能够处理原生分辨率输入（通过位置插值等适配）。
  - 瓶颈层（Bottleneck）：降维，将高维视觉特征压缩到低维空间以便量化。；包含两个全连接层：第一层将C=1536降至D=128，第二层将D=128降至d=6。中间使用ReLU激活。
  - 近端表示学习（Proximal Representation Learning）：逐步将连续特征空间压缩到离散空间，避免信息剧烈损失。；训练过程中，先使用连续表示+BatchNorm+L2正则，然后增加BatchNorm+L∞约束，最后使用FSQ强制离散化。通过逐渐增强正则，使模型适应离散化。
  - 位置感知头级量化（Position-aware Head-wise Quantization）：对每个patch进行量化，并赋予位置信息，支持任意分辨率。；对每个量化头，为每个patch学习一个可学习的位置编码向量（维度为d），与patch特征相加后再通过FSQ量化。不同头的编码独立学习，使得离散令牌携带空间位置信息。
  - FSQ量化器（Finite Scalar Quantization）：将连续特征映射到离散代码。；使用FSQ，levels=[8,8,8,5,5,5]，即每个维度分别量化为8、8、8、5、5、5个水平，总组合数8^3*5^3=64,000。量化后每个patch得到一个整数索引（0~63999）。
  - 解码器（用于重建）：将ViQ的离散令牌重建为图像。；解码器基于VAE架构，训练损失包括L_KL、L_MSE、L_LPIPS、L_GAN以及L_REPA（使用DINOv2进行语义对齐）。系数λ_REPA=1.5。使用AdamW优化器，学习率6e-4，batch size 4096，训练50k步。
- 公式/目标函数/指标定义：
  - VAE训练损失：L_VAE = L_KL + L_MSE + L_LPIPS + L_GAN + λ_REPA * L_REPA
  - FSQ量化：每个维度分别量化到指定levels，如[8,8,8,5,5,5]，总码本大小∏ L_i = 64,000
  - 压缩比：原始RGB图像大小=3HW字节，ViQ代码大小=HW/32字节，压缩比=96:1
- 算法细节：
  - 文本对齐预训练阶段：使用Qwen2.5-VL-0.5B作为语言模型，Qwen-Image作为低层视觉监督（固定参数），通过对比学习使视觉编码器与文本表示对齐，同时通过位置插值使编码器适应任意分辨率。
  - 特征离散化阶段：训练时逐步增加正则化约束：①初始使用连续特征+BN+l2；②然后使用BN+l∞；③最后使用FSQ量化。每个阶段训练一定步数，使特征空间平滑过渡。
  - 位置感知量化：对图像划分非重叠patch，每个patch具有空间坐标。在量化前，每个头（head）为每个位置学习一个可学习位置编码，与patch特征相加后输入FSQ。多头量化后，所有头的离散索引拼接作为该patch的表示。
  - 离线代码提取：训练VLM时，预先对图像提取ViQ离散代码并存储，训练时直接加载代码代替原始图像，减少计算量。
- 训练/推理细节：
  - 训练阶段1（文本对齐预训练）：使用128张NVIDIA A100 GPU，数据集为图像-文本对。
  - 训练阶段2（量化训练）：使用256张NVIDIA A100 GPU。
  - VLM SFT训练：将ViQ与Qwen2.5系列模型集成，使用LLaVA-NeXT的任意分辨率训练pipeline。训练时固定视觉编码器和LLM，仅训练投影层。
  - 推理时：对输入图像离线提取ViQ离散代码，然后与文本嵌入一起送入LLM，无需在线处理图像。
  - VLM训练效率：对于0.5B模型，ViQ相比SigLIP2-g在4k/16k训练中前向时间加速70%/78%；对于7B模型，加速46%/65%。步时间加速超过20%-40%。

### 10. MVPruner: Dynamic Token Pruning for Accelerating Multi-view Vision-Language Models in Autonomous Driving
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.27660v1
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 11. Why Trust Your Agent? Empirical Security Gains from TRiSM-Guided Agentic Workflows in Healthcare
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.28666v1
- 方法流程：
  - 1. 输入：医院就诊信息和患者数据（合成数据），攻击场景（RAG投毒、数据字段注入、客户端网络注入）。
  - 2. 预处理：使用Mendix Studio Pro开发应用，通过Mendix Agent Commons集成LLM API（Anthropic、OpenAI、Google）。
  - 3. 安全agentic工作流：a) 患者搜索agent：检索患者记录，输出JSON格式的所需信息。b) 文档生成agent：接收医院就诊和患者信息，根据就诊类型应用模板生成PDF报告。c)（仅Covid-19就诊）相似X光agent：查询X光知识库，并运行Covid-Net模型预测感染概率。
  - 4. 安全检查：嵌入分析、策略JSON强制执行。
  - 5. 输出：生成的医疗报告PDF。
- 关键模块/机制：
  - 患者搜索agent：检索和隔离敏感数据；从数据库中检索与医院就诊相关的患者记录，仅输出下游agent所需的JSON格式信息，减少敏感数据暴露。
  - 文档生成agent：生成医疗报告PDF；接收患者信息和就诊信息，从外部MCP服务器获取模板，根据就诊类型（常规或Covid-19）应用适当模板生成报告。
  - 相似X光agent：辅助Covid-19诊断；仅用于Covid-19就诊。查询X光知识库获取相似X光的诊断状态，同时运行一个小型判别性Covid-Net模型生成感染概率，结合两者结果减少对生成式AI的依赖。
  - 嵌入分析模块：检测异常输入；在入口点对输入进行嵌入分析，识别可能的攻击向量，如RAG投毒或数据字段注入。
  - 策略JSON：定义agent权限和操作范围；为每个agent定义明确的策略JSON文件，限制其可访问的工具、资源和操作，实施最小权限和防御深度。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式
  - 攻击成功率：统计攻击导致模型输出偏离无攻击真实输出的方向，包括改变/抑制Covid-19判定、包含攻击者指定的数据或字段、执行注入的工具调用指令、替换报告内容。报告准确率：基于工作流是否正确调用工具、填写所有必填字段、没有幻觉、准确表示Covid相关工具结果。
- 算法细节：
  - 多智能体分解算法：将原始单agent任务树拆分为多个子任务，每个子任务由专用agent执行，agent之间通过JSON数据传递。
  - 服务器端提示构建：所有prompt在服务器端组装，避免客户端篡改。
  - 数据分区：患者搜索agent输出仅包含下游所需的最小字段集，其他字段不传递。
  - 入口点嵌入分析：对用户输入进行嵌入相似度计算与异常检测。
  - 策略JSON执行：每个agent调用工具前检查策略JSON，拒绝未授权操作。
- 训练/推理细节：
  - 所有模型调用使用各提供商默认的解码参数（Anthropic、OpenAI、Google），无额外微调。
  - Covid-Net模型为预训练的小型判别模型，用于推理基于X光的感染概率，未提及训练过程。
  - 实验在2026年6-7月进行，使用指定的模型版本。

### 12. Personalizing MLLMs via Reinforced Multimodal Reference Game
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.28845v1
- 方法流程：
  - 输入：用户概念图像及查询图像；
  - 处理：MLLM作为speaker生成概念描述；
  - 处理：MLLM作为listener根据描述从候选图像（含硬正例和硬负例）中匹配目标；
  - 处理：计算基于匹配结果的对比奖励；
  - 输出：优化后的描述生成策略，用于个性化响应。
- 关键模块/机制：
  - 强化参考游戏框架：核心训练范式；MLLM同时扮演说话者和听者，在对比游戏中通过交互学习生成能区分目标概念与视觉相似概念（硬负例）以及同一概念不同视角（硬正例）的描述。
  - 对比奖励机制：优化信号；基于硬正例（同一概念不同视角）和硬负例（视觉相似不同概念）定义可验证的对比奖励，鼓励描述在区分正负例时表现优异，通过强化学习更新模型。
- 训练/推理细节：
  - 训练采用强化学习范式，参考游戏提供自监督对比信号，无需人工标注

### 13. HKVLM: Faithful Reasoning Grounding by Binding Language Queries to a Frozen Detector
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.28862v1
- 方法流程：
  - 输入图像I和自由形式查询q
  - 冻结检测器（Grounding DINO）输出M个类无关区域提议R={ (b_j, e_j) }，其中b_j为框坐标，e_j为区域特征
  - 冻结语言模型（如Qwen2.5-VL）接收图像和查询，输出K个参考查询嵌入{u_k}（从指定查询槽读取）
  - 对齐钩子：通过投影网络f_theta和g_phi将区域特征和查询嵌入映射到共享空间，计算亲和力s_kj=<u~_k, e~_j>/κ
  - 训练：使用对比检索损失L_ret和二分图匹配（匈牙利算法）将查询与正区域配对，并训练框残差和忠实性否决头
  - 推理：对齐钩子为每个查询选择最佳匹配区域，忠实否决头v(·)判断绑定是否真实，若通过且亲和力超阈值则输出类别-框对，否则弃权
- 关键模块/机制：
  - 冻结感知模块：生成类无关的区域提议，提供视觉候选；使用预训练并冻结的语言对齐检测器（如Grounding DINO），输出M个区域提议，每个提议包含框坐标b_j和视觉-语言融合区域特征e_j∈R^d，保证高召回且不与语言生成耦合。
  - 参考查询嵌入生成模块：将语言请求编码为连续查询向量，保留推理意图；冻结的语言模型（如Qwen2.5-VL）在输入中附加查询槽，输出K个连续向量{u_k}，每个向量代表查询隐含的一个实体。可附加LoRA微调。
  - 对齐钩子（Alignment Hook）：将查询嵌入与区域特征映射到共享空间并进行绑定；包含两个轻量投影网络f_θ和g_ϕ，分别将区域特征e_j和查询嵌入u_k投影到d维空间得到e~_j和u~_k；计算亲和力s_kj=<u~_k,e~_j>/κ；训练时使用对比检索损失（查询-区域匹配）和二分图匹配（匈牙利算法分配查询至区域），同时预测框残差Δb以轻微调整框位置。
  - 感知忠实否决头：验证绑定是否真实，防止幻觉；一个小型二元分类器v(·)，输入查询和对应最佳区域的投影对(u~_k, e~_σ(k))，输出[0,1]指示绑定是否真实；训练用BCE损失L_faith；推理时若v(·)<γ_v或最大亲和力<γ_s则弃权，不输出类别。
- 公式/目标函数/指标定义：
  - SeeErr = 1 - |{g: ∃r∈R, IoU(r,g)≥τ}| / |G|
  - SayErr = Pr( ĉ ≠ c⋆ | correct region ∈ R⋆ )
  - 亲和力: s_kj = <u~_k, e~_j> / κ
  - 对比检索损失: L_ret = -Σ_k log( Σ_{j∈P(k)} exp(s_kj) / Σ_{j=1}^M exp(s_kj) )
  - 二分图匹配成本: C(u_k, r_σ(k)) = -s_kσ(k) + λ1||b_σ(k)-b*_k||1 + λ2 L_giou(b_σ(k), b*_k)
  - 框残差损失: L_box = Σ_k ||Δb_σ(k) - (b*_k - b_σ(k))||1
- 算法细节：
  - 对比检索: 每个查询k的正区域集合P(k)由匹配区域定义（可扩展为IoU≥τ的区域），用交叉熵损失拉近查询与正区域、推离其它区域
  - 二分图匹配: 使用匈牙利算法在K个查询和M个区域间寻找最小代价匹配，代价为负亲和力与框损失（L1和GIoU）的加权和
  - 框残差: 冻结检测器框不动，对齐钩子仅预测一个小的残差Δb加到原始框上，保持定位能力在检测器
  - 忠实性投票: 训练一个二元分类器，输入为（查询投影+最佳区域投影），输出概率，推理时结合阈值γ_v和γ_s决定是否输出
  - 数据效率: 仅在少量数据（几百到几千表达）上训练对齐钩子，不训练检测器和语言模型
- 训练/推理细节：
  - 训练参数: 仅训练对齐钩子中的投影网络f_θ、g_ϕ、框残差预测和忠实性否决头，语言模型可附加LoRA，检测器始终冻结
  - 冷启动训练: 适用于小数据场景，如0.5k/1k/5k图像，避免整体VLM微调
  - 推理过程: 1) 检测器生成M个区域提议; 2) 语言模型生成K个查询嵌入; 3) 对齐钩子计算亲和力，匈牙利匹配选择最佳区域; 4) 忠实否决头评估，若通过且最大亲和力≥γ_s则输出类别-框，否则弃权
  - 阈值选择: γ_v和γ_s从验证集上根据召回/忠实权衡选取

### 14. Covering the Unseen: Information Demand Coverage Optimization for Retrieval-Augmented Generation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.29328v1
- 方法流程：
  - 输入查询Q，使用LLM生成M个多样性子查询（两阶段生成：首先生成初步子查询，再通过微调增加多样性）
  - 对每个子查询进行反向验证质量加权，抑制语义漂移或冗余的子查询，构建归一化的需求分布P_Q
  - 计算每个候选文档c与每个子查询的余弦相似度，构造覆盖度量函数μ_S（S为选定集合）
  - 采用贪心算法，每次选择使Sinkhorn-Wasserstein距离减少最多的文档c加入S，迭代k次
  - 输出最终的上下文集合S（包含k个文档）
- 关键模块/机制：
  - 两阶段多样化子查询生成：近似多维需求结构，构建需求分布的基础；第一阶段使用LLM根据原始查询生成多个初步子查询；第二阶段对初步子查询进行重组、扩展或过滤以增加多样性，确保覆盖查询的不同语义维度。
  - 反向验证质量加权：抑制语义漂移或冗余的子查询，提高需求分布的准确性；对每个子查询，使用LLM进行自验证：检查子查询是否忠于原始查询且不冗余；根据验证分数赋予权重，低质量子查询权重降低，最终对权重归一化形成需求分布P_Q。
  - 需求加权设施选址目标：形式化覆盖优化目标，提供理论保证；定义目标函数为最小化Sinkhorn-Wasserstein距离d_SW(P_Q, μ_S)；该目标是单调次模函数，具有(1-1/e)的贪心近似保证。
  - Sinkhorn边际增益代理：近似贪心选择的边际增益，降低计算复杂度；使用Sinkhorn算法迭代求解熵正则化最优传输距离，计算每次添加候选文档c后d_SW的减少量Δ(c)；选择Δ(c)最大的文档，避免精确O(n^3)计算。
- 公式/目标函数/指标定义：
  - 目标函数：min_{S, |S|=k} W_ε(P_Q, μ_S)，其中W_ε是熵正则化Sinkhorn距离，P_Q为需求分布，μ_S为选定集合S的覆盖度量。
- 算法细节：
  - 1. 输入查询Q，调用LLM生成M个多样性子查询（例如M=20）
  - 2. 对每个子查询s_i，用LLM评估其质量并赋予权重w_i（反向验证），形成需求分布P_Q = {w_i / sum(w_j)}
  - 3. 计算每个候选文档c与每个子查询的余弦相似度sim(c, s_i)，构造覆盖度量μ_S(c) = max_{c' in S} sim(c', c) 或更复杂的形式
  - 4. 初始化空集S = ∅，迭代k次：
  - a. 对每个c not in S，计算添加c后的新覆盖度量μ_{S∪{c}}
  - b. 使用Sinkhorn算法计算d_SW(P_Q, μ_{S∪{c}})与d_SW(P_Q, μ_S)的差值Δ(c)
  - c. 选择c* = argmax Δ(c)，将c*加入S
  - 5. 输出S作为最终上下文
- 训练/推理细节：
  - 无训练过程，完全推理时运行
  - LLM子查询生成需一次调用（预先生成，可缓存）
  - Sinkhorn算法迭代次数固定（如20次），计算复杂度O(k * (N * M))，其中N为候选池大小，M为子查询数量

### 15. H-GRPO: Permutation-Invariant Reinforcement Learning for Grounded Visual Reasoning
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.29915v1
- 方法流程：
  - 1. 数据集构造：通过多阶段流水线生成GVRS合成数据集，包含图像、问题、最终答案和结构化推理轨迹（三元组序列）
  - 2. 有监督微调（SFT）：在GVRS上训练一个epoch，使模型学会输出分解式接地推理格式
  - 3. GRPO强化学习：以SFT模型为初始化，使用组相对策略优化（G=8 rollouts）进一步优化，奖励函数基于匈牙利匹配得分
  - 4. 推理与评估：模型生成预测三元组，通过匈牙利匹配与参考三元组比较，计算总匹配得分S_HS
- 关键模块/机制：
  - 分解式接地推理生成器：将全局查询分解为原子子问题，每个子问题生成子答案和证据边界框，输出三元组序列 (q_i, a_i, b_i)；模型在训练中学习按照特定格式输出一系列三元组，每个三元组包含一个子问题、对应的子答案以及一个定位该证据的边界框，从而强制中间步骤视觉接地。
  - 匈牙利匹配评估器：计算预测推理轨迹与参考轨迹之间的相似度得分，解决三元组顺序不一致问题；构建预测三元组与参考三元组之间的相似度矩阵S，每个元素S_ij = 1/4 (E(b_i^pred, b_j_ref) + sim_q(q_i^pred, q_j_ref) + sim_a(a_i^pred, a_j_ref) + IoU(b_i^pred, b_j_ref))，然后求解带一对一约束的二分匹配问题，得到最优匹配M，最终得分S_HS = 1/min(m,n) * sum_{(i,j)∈M} S_ij。
  - GRPO训练模块：通过组相对优势更新策略，优化模型生成高奖励的接地推理轨迹；基于GRPO算法，每组样本使用G=8个rollouts，计算相对优势，以匈牙利匹配得分为奖励信号，更新模型参数以鼓励生成语义对齐且视觉准确的推理步骤。
- 公式/目标函数/指标定义：
  - 相似度矩阵 S_ij = 1/4 [E(预测边界框, 参考边界框) + sim_q(预测子问题, 参考子问题) + sim_a(预测子答案, 参考子答案) + IoU(预测边界框, 参考边界框)]
  - 匹配优化目标：X* = argmax_X Σ_i Σ_j S_ij x_ij，约束为每行/列之和≤1
  - 总匹配得分 S_HS = 1/min(m,n) * Σ_{(i,j)∈M} S_ij
- 算法细节：
  - 匈牙利匹配算法：将预测三元组与参考三元组视为二分图节点，基于相似度矩阵S，使用匈牙利算法求解最大权重匹配
  - GRPO训练：每组样本生成G个轨迹，计算每个轨迹的奖励（匈牙利匹配得分），然后组内归一化得到优势，更新策略
  - 边界框存在性检查：函数E(·,·)判断预测框是否存在且与参考框兼容
- 训练/推理细节：
  - 两阶段训练：1）SFT在GVRS上训练1个epoch，学习率5e-6，warm-up 0.03，weight decay 0.01；2）GRPO使用相同的超参数，每样本G=8个rollouts
  - 推理时，模型自回归生成三元组序列，然后使用匈牙利匹配与参考三元组计算评估得分

### 16. LETT-NeXt: A Lightweight RECIST-Guided Model for 3D CT Lesion Segmentation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.30108v1
- 方法流程：
  - 预处理阶段：将所有CT体素重采样至2.4×1.0×1.0 mm间距，提取以RECIST标记为中心的72×160×160体素裁剪，CT强度裁剪至[-999,255]HU后减去训练集均值(69.86)并除以标准差(200.88)得到归一化裁剪图Ĩ_CT
  - 提示编码阶段：生成RECIST线掩码Pline（在RECIST轴向切片上连接两端点并膨胀半径2体素）和端点热图Pendpoints（高斯热图，σ_end=2.0），形成三通道输入X=[Ĩ_CT, Pline, Pendpoints]
  - 网络预测阶段：将X输入MedNeXt-v2 f32编码器-解码器（5编码器阶段、4解码器阶段、跳跃连接，通道数32/64/128/256/512，约6.92M参数量），输出病变概率图
  - 后处理阶段：对Sigmoid输出阈值0.35获得候选体素；计算连通分量，选择与RECIST标记区域重叠最大的分量（若无重叠则选择距离标记中心最近的分量）；将裁剪空间掩码映射回原始图像网格
  - AutoZoom推理：若第一次预测掩码触及裁剪边界，则提取更大物理视野的裁剪（缩放至固定输入尺寸）重新推理，覆盖初始预测
- 关键模块/机制：
  - RECIST提示通道编码：将RECIST标注转换为模型可处理的提示信息，引导模型关注目标病变位置和走向；生成两个通道：Pline为二元掩码，在轴向切面上连接两个RECIST端点并以半径2体素膨胀；Pendpoints为高斯热图，端点处值最大，σ_end=2.0控制空间扩散。两通道与归一化CT拼接为三通道输入。
  - MedNeXt-v2 f32编码器-解码器：从三通道输入中预测病变分割掩码；采用3D U-Net拓扑，包含5个编码阶段（通道数32,64,128,256,512）和4个解码阶段（通道数256,128,64,32），每个阶段包含若干深度可分离卷积层（depth），使用加性跳跃连接，总参数约6.92M。
  - 辅助解剖监督头（训练时）：通过PanTS数据的解剖-肿瘤标签提供多类监督，促进解剖感知特征学习；在解码器最终特征图上附加1×1×1卷积输出局部解剖-肿瘤图，与多类交叉熵和Dice损失组合（Laux=0.5L_CE+0.5L_multiDice），仅在有效标签上计算。推理时该头被移除。
  - 提示感知组件选择：确保每个RECIST标记只对应一个病变分量，去除假阳性或非目标分量；对阈值后的掩码计算连通分量，优先选择与RECIST标记区域（线端点膨胀区域）重叠最大的分量；若无重叠，则选择距离标记中心最近的分量。
  - 自适应AutoZoom推理：解决初始裁剪可能截断病变的问题，通过第二次大视野推理补全；当第一次预测掩码触及裁剪边界时，提取更大物理视野的裁剪（保持纵横比缩放到固定输入大小），再次通过同一模型推理，得到的预测替换初始预测，后续按相同后处理流程。
- 公式/目标函数/指标定义：
  - Pendpoints(v) = max_{i∈{1,2}} exp(-‖v-p_i‖₂²/(2σ_end²))，其中σ_end=2.0体素
  - 网络输入 X = [Ĩ_CT, Pline, Pendpoints]
  - 主病变损失: Lmain = LDice + 2 LBCE
  - 辅助损失: Laux = 0.5 L_CE(Ŷaux, Yaux) + 0.5 L_multiDice(Ŷaux, Yaux)
  - 总损失: Ltotal = Lmain + 0.25 Laux
  - 评价指标: Score = 0.5·DSC + 0.5·NSD
- 算法细节：
  - 裁剪采样策略：60%标准RECIST中心裁剪、30%扩大视野裁剪、≤10%边界框拟合裁剪（避免截断病变），所有裁剪强制包含RECIST端点且留有15mm边缘
  - 后处理阈值0.35通过验证集阈值扫描确定
  - 连通分量选择：首先寻找与RECIST标记区域（线掩码膨胀区域）重叠最大的分量；若无重叠则取距离RECIST中心最近的分量
  - AutoZoom条件：第一次预测掩码的任意体素触碰裁剪边界则触发第二次推理
  - 推理时每个RECIST标记独立处理，最终合并为每个CT案例的带标签输出掩码
- 训练/推理细节：
  - 训练数据：9,968个案例共25,112条记录（含801条PanTS辅助记录），每条记录对应一个CT与一个目标病变及RECIST标记
  - 训练协议：25个epoch，AdamW优化器，初始学习率1e-3，批大小6每GPU（全局18），使用强度偏移、缩放和高斯噪声数据增强
  - 推理配置：CPU完成每例平均6.9秒，峰值内存3.6GB，阈值0.35，边界触发AutoZoom
  - 验证时：每个CT案例包含多个RECIST标记则分别推理后合并为一张标签图

### 17. Dynamo: Dynamic Skill-Tool Evolution for Vision-Language Agents
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.30185v1
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 18. SHOVIR: A Benchmark for Evaluating Vision Shortcut Learning in Radiology Report Generation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.30201v1
- 方法流程：
  - 阶段1：构建带空间标注的数据集。对MIMIC-CXR使用ChestImaGenome的边界框，通过规则映射和一致性过滤得到2160张图像；对PadChest-GR使用其自带边界框，经CheXbert验证过滤得到1390张图像。
  - 阶段2：设计遮挡实验。图像级实验：用匹配均值和标准差的高斯噪声替换整个图像。疾病级实验：针对每个目标类c，定义疾病框Bc和共病框B¯c，以随机遮挡RO为对照，分别遮挡Bc（OCO）和B¯c（DOCO），遮挡强度p∈{0%,20%,40%,60%,80%,100%}。
  - 阶段3：统一推理。对8个VLM，采用作者默认配置，仅输入正面胸片，生成报告。
  - 阶段4：评估。计算基线、全噪声、RO、OCO、DOCO条件下的NLP指标（BLEU, ROUGE-L）、临床指标（F1-CheXbert, F1-RadGraph, GREEN）和捷径指标（ΔFN, ΔOCO, ΔDOCO）。
- 关键模块/机制：
  - 数据集映射与过滤模块：统一两个数据集的标签空间，确保每框CheXpert标签的准确性。；对MIMIC-CXR，通过规则映射将ImaGenome属性转为CheXpert类别，进行交叉源一致性检查（F1=0.935），并严格保留每张图像所有阳性标签均有对应ImaGenome属性的样本。对PadChest-GR，将154个发现标签中的69个映射到CheXpert，并用CheXbert验证每个框的句子，剔除不一致框（移除35.3%），最后保留类别样本数≥40的图像。
  - 遮挡生成模块：生成不同空间尺度的扰动图像，包括全噪声、随机遮挡、目标类遮挡、不同类遮挡。；噪声生成：对原图在1st-99th百分位像素计算均值和标准差，采样高斯噪声，使噪声图像保留强度分布但无结构。遮挡强度p通过线性插值控制原图与噪声的混合比例。RO：随机放置与原图边界框分布相同数量的随机框，且不重叠Bc或B¯c。OCO：遮挡所有疾病框Bc。DOCO：遮挡所有共病框B¯c（IoU<0.15）。固定随机种子使所有模型接收相同扰动输入。
  - 捷径指标模块：量化模型对视觉依赖的敏感度。；图像级：ΔFN = F1(Baseline) - F1(Full Noise Image)，越大表示视觉依赖越强。疾病级：先计算各条件加权平均F1（μ-F1），再计算ΔOCO = μ-F1(RO) - μ-F1(OCO)和ΔDOCO = μ-F1(RO) - μ-F1(DOCO)。ΔOCO大表明模型依赖目标区域，ΔDOCO大表明模型依赖上下文区域（共病捷径）。
- 公式/目标函数/指标定义：
  - Bc = { b ∈ B | c ∈ Db }, B¯c = { b ∈ B | c ∉ Db ∧ max_{b'∈Bc} IoU(b, b') < 0.15 } （公式1）
  - ΔFN = F1(Baseline) - F1(Full Noise Image) （公式2）
  - ΔOCO = μ-F1(RO) - μ-F1(OCO), ΔDOCO = μ-F1(RO) - μ-F1(DOCO) （公式3）
  - μ-F1 = Σ (w_c * F1_c) / Σ w_c，其中w_c为类别c的标注图像数（加权平均F1）
  - 噪声生成：pixel_noise ~ N(μ, σ)，其中μ和σ为原图第1-99百分位像素的均值和标准差。
- 算法细节：
  - 数据集构建算法：对每张图像，取所有带框区域，通过查找表将原始标签映射为CheXpert类；对于PadChest-GR，额外用CheXbert对每个框关联句子预测标签，仅保留映射与预测一致的框。
  - 遮挡实验执行算法：对每张图像，先确定所有框B。对于目标类c，按公式1划分Bc和B¯c。RO：从B中随机选择1到|B|个框，尺寸从所有框尺寸分布中采样，位置随机但避开Bc和B¯c区域。OCO：直接遮挡Bc中的所有框。DOCO：遮挡B¯c所有框。每个框内像素按p比例线性混合噪声。
  - 推理协议：所有模型只接受正面胸片，使用作者提供的默认提示和采样配置。
  - 评估算法：对生成的报告，用CheXbert提取14个类别的标签，计算每类的F1；F1-RadGraph通过比较关系图；GREEN使用微调LLM评分。
- 训练/推理细节：
  - 所有8个VLM采用作者发布的默认推理配置（文本提示、图像预处理、解码策略），无额外微调。
  - 仅使用正面胸片（PA/AP）作为输入，忽略侧位片。
  - 对每个模型，输入均通过其官方预处理流程（如resize、padding、normalize）。
  - 解码使用贪婪或beam search（依模型默认），生成报告最大长度遵循原始设置。

### 19. APRIL-MedSeg: A Modular Medical Image Segmentation Toolbox Embracing Modern Paradigms
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.30577v2
- 方法流程：
  - 输入图像x经Encoder提取多尺度特征金字塔{f1,...,fL}，从低层空间细节到高层语义表示
  - Bottleneck模块仅处理最深特征图fL，捕获全局上下文和长程依赖
  - Skip Connection模块选择性传输并精炼中间层特征f1...L-1，缓解空间信息损失
  - Decoder模块逐步上采样并融合Bottleneck的全局上下文特征与Skip Connection的空间特征，生成密集像素分割图ŷ
- 关键模块/机制：
  - Encoder：逐级下采样输入图像，提取多尺度特征，支持CNN、Transformer、Mamba、基础模型等177种实现；通过timm_前缀动态解析PyTorch Image Models中的任意骨干；基础模型通过统一DPT头从中间ViT层提取多块多尺度特征
  - Bottleneck：对最深特征图进行上下文建模和特征精炼，17种插件模块；涵盖Identity、ASPP、PPM、SE、ECA、CBAM、Self-Attention、MoE、LLM4Seg等，可组合不同策略
  - Skip Connection：可控路由机制，传递并可选精炼中间编码器特征；实现如concat、attention等连接方式，增强多尺度信息流
  - Decoder：逐步上采样并融合特征，重建分割图，45种实现；分为Basic Upsampling、Dense Connection、Cascade Refinement、Pyramid Aggregation、MLP、Transformer、Attention、Mamba、Network-specific等九类
  - Registry & Configuration System：统一注册和YAML配置，实现组件可组合和实验管理；六个全局注册表（架构、损失、增强等）；继承机制允许子配置仅覆盖差异字段；支持损失递归组合和深度监督注入
- 公式/目标函数/指标定义：
  - 预测管道形式化：ŷ = D(B(fL), S(f1...L-1))，其中{f1,...,fL}=E(x)为编码器提取的多尺度特征金字塔
- 算法细节：
  - 注册机制：通过@REGISTRY.register()装饰器注册模块，YAML配置中通过name字段指定并实例化
  - 动态timm解析：encoder.name以timm_为前缀时，自动调用timm.create_model()获取预训练模型
  - 损失递归组合：losses列表支持任意嵌套，每个子损失有weight参数，自动生成深度监督损失
  - 配置继承：子配置通过inherits字段继承父配置，仅覆盖需要修改的字段，减少冗余
- 训练/推理细节：
  - 单命令启动：python train.py --config config.yaml 实现完整训练流程，自动处理模型构建、数据加载、分布式训练、损失组合、checkpoint和评估
  - 不同范式共享统一命令行接口，只需更改配置文件（如supervised, semi-supervised, domain adaptation等）
  - 支持推理时TTA（Test-Time Augmentation）：python test.py --config config.yaml --tta
  - 基础模型支持多种微调策略：冻结编码器、部分微调、层学习率衰减、全微调

### 20. Dense Structural Priors for Sparse Functional Landmark Localization in Surgical Videos
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.31007v1
- 方法流程：
  - 输入：以中心帧It为中心的连续帧序列{It−k,...,It,...,It+k}。
  - 粗网络预测：多帧热图定位骨干网络预测中心帧的尖端和锚点粗热图Ĥc。
  - 解码点提示：从粗热图解码尖端和锚点位置作为SAM3的正点提示。
  - SAM3生成掩码：输入SAM3生成非oracle器械掩码Mc。
  - 特征融合：将中心帧视觉特征、粗热图特征和掩码先验特征输入细化网络。
  - 输出：细化网络预测精化热图Ĥr。
- 关键模块/机制：
  - 粗多帧网络：预测粗热图，提供初始点提示；基于MFCNet的骨干网络，输入多帧帧序列，输出中心帧的尖端和锚点粗热图Ĥc。
  - SAM3结构先验：零样本生成器械掩码，提供结构上下文；接收来自粗热图解码的点提示（尖端和锚点位置），输出器械掩码Mc，无需微调。
  - 细化网络：融合多特征，输出精化热图；接收中心帧视觉特征、粗热图特征和掩码先验特征，通过融合机制（如拼接）预测精化热图Ĥr。
  - 辅助掩码分支：可选分支，增强掩码形状信息学习；连接在共享细化特征之后，预测SAM3生成的掩码Mc，辅助训练时引入额外掩码损失。
- 公式/目标函数/指标定义：
  - 主损失函数：L = ||Ĥr - Hgt||_2^2 + λaux ||M̂ - Mc||_2^2，其中Hgt为高斯真值热图，M̂为预测辅助掩码，λaux为权重。
  - 直接掩码增强监督损失：L_direct = G ⊙ (Ĥd - H̃)⊙2，其中H̃为掩码增强目标，G为门控权重矩阵。
- 算法细节：
  - 点提示生成：从粗热图Ĥc中通过argmax或软argmax解码尖端和锚点位置。
  - SAM3零样本推理：直接使用预训练SAM3模型，无需任何手术视频领域微调。
  - 特征融合：细化网络将中心帧图像特征、粗热图特征和SAM3掩码特征沿通道维度拼接或加权融合。
  - 门控回归：在直接掩码增强监督中，使用G将热图区域分为关键点区域、掩码区域和背景区域，并赋予不同损失权重（关键点区域权重最大）。
- 训练/推理细节：
  - 训练：使用关键点热图损失训练粗网络和细化网络；当使用辅助掩码分支时，加入额外掩码损失，λaux调节权重。
  - 推理流程：粗网络预测粗热图→解码点提示→SAM3生成掩码→细化网络输出精化热图。
  - 数据集：7867个剪辑，来自60个视频（YouTube、Cholec80、HeiChole、SurgVU、CRCD），按视频级划分训练/验证/测试（6403/79/1385）。
  - 评估指标：在15像素匹配容差下计算精度、召回率、F1分数和平均欧氏距离误差。

### 21. HyperVLP: Enhancing Hierarchical Surgical Video-Language Pre-training in Hyperbolic Space
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.31245v1
- 方法流程：
  - 数据组织：将手术讲座视频按层次结构组织为父-子单元（视频/阶段/动作），父段由子剪辑时间组成。
  - 特征提取：分别用视觉编码器F_v和文本编码器F_t提取特征，经线性投影映射到洛伦兹流形的切空间，再通过指数映射exp_c_o得到双曲嵌入V_c, T_c, v_c, t_c。
  - 阶段I - 几何感知双曲对比学习：仅优化对比损失L_GeoHCL，在父级和子级分别对齐视觉与文本嵌入，利用自适应权重α_ik降低结构相关样本的负贡献。
  - 阶段II - 层次蕴含学习：冻结编码器，微调映射层，联合优化对比损失和蕴含损失（跨模态L_inter和模态内L_intra），强制父子节点间的锥形包含关系。
  - 下游任务：将预训练的双曲视觉编码器迁移到阶段识别任务，通过线性分类头进行零样本或微调评估。
- 关键模块/机制：
  - 几何感知双曲对比学习 (Geo-aware Hyperbolic Contrastive Learning)：在双曲空间中对齐视觉和文本表示，同时减轻结构假负例的影响；对于锚点i，将样本分为结构相关集S(i)、正集P(i)和负集N(i)。对比损失中，对S(i)中的样本赋予自适应权重α_ik = σ(-d_ik / (∑_{k∈P(i)} d_ik + ε))，其中d_ik为双曲相似度（负距离）。权重随相似度增高而增大，从而抑制结构相关样本被推开。损失函数应用于父级和子级，对称计算，总损失为四个方向的平均。
  - 锥形双曲蕴含学习 (Cone-based Hyperbolic Entailment Learning)：强制父阶段嵌入包含子步骤嵌入，维持层次语义一致性；在洛伦兹空间中定义锥形区域R_x，半孔径θ(x)=sin^{-1}(2K√c/||x_n||)。子嵌入y需落在父嵌入x的锥内，通过惩罚外角φ(y,x)实现，损失L_ent(y,x)=max(0, φ(y,x)-ηθ(x))。跨模态：文本概念包含对应视频（L_inter = L_ent(v_c,t_c)+L_ent(V_c,T_c)）。模态内：父文本包含子文本、父视频包含子视频（L_intra = L_ent(t_c,T_c)+L_ent(v_c,V_c)）。
  - 双曲嵌入映射 (Hyperbolic Embedding via Lorentz Model)：将欧氏特征投影到双曲流形，利用其几何性质表示层次结构；采用洛伦兹模型L^n = {x∈R^{n+1}: ⟨x,x⟩_L=-1/c, x_0>0}。欧氏特征经线性投影到切空间（在原点o=[√(1/c),0]），再通过指数映射exp_c_o(x)=cosh(√c||x||_L)o + (sinh(√c||x||_L)/(√c||x||_L))x得到双曲向量。可学习的缩放因子稳定优化。
  - 两阶段优化策略 (Two-stage Training)：分步优化，先对齐后约束层次，以稳定训练；阶段I：仅用几何感知双曲对比损失L_GeoHCL优化所有参数。阶段II：冻结视觉和文本编码器，只训练双曲映射层，联合优化L_GeoHCL和蕴含损失（L_inter+L_intra），强化层次包含关系。
- 公式/目标函数/指标定义：
  - 双曲空间定义：L^n = {x∈R^{n+1}: ⟨x,x⟩_L=-1/c, x_0=√(1/c+||x_n||^2), c>0}
  - 洛伦兹内积：⟨x,y⟩_L = ⟨x_n,y_n⟩_E - x_0 y_0
  - 指数映射：exp_c_o(x) = cosh(√c ||x||_L) o + (sinh(√c ||x||_L) / (√c ||x||_L)) x
  - 几何感知对比损失：L_geoHC(V_c,T_c) = -1/B ∑_{i=1}^B log [ ∑_{k∈P(i)} d_ik / (∑_{k∈P(i)} d_ik + ∑_{k∈S(i)} α_ik d_ik + ∑_{k∈N(i)} d_ik) ]，其中d_ik = -d_L(V_i^c, T_k^c)/τ
  - 自适应权重：α_ik = σ( -d_ik / (∑_{k∈P(i)} d_ik + ε) )
  - 锥形半孔径：θ(x) = sin^{-1}(2K√c / ||x_n||)
- 算法细节：
  - 结构负例分区：对于每个锚点i，根据时间包含关系构造S(i)，包括同阶段内相邻步骤的剪辑和层次包含的子/父样本。P(i)包含锚点的正配对（同一层次匹配的视觉-文本对）。N(i)为其他不相关的跨过程样本。
  - 对比损失中的自适应权重计算：先计算锚点与S(i)中每个样本的双曲相似度d_ik，然后用softmax-like形式归一化，经sigmoid映射到[0,1]，权重越大表示该结构相关样本与正样本越相似，从而降低其负贡献。
  - 锥形约束实现：通过计算子嵌入相对于父嵌入的方位角与外角，若子嵌入落在锥外则产生惩罚，阈值η控制锥的宽松度。
  - 两阶段训练：阶段I使用全部训练数据，优化L_GeoHCL，学习率等标准设置。阶段II固定编码器权重，只更新双曲映射层的线性投影和指数映射相关参数，联合三个损失。
- 训练/推理细节：
  - 训练数据集：Surgical Video Lectures (SVL)，由作者提供重构为层次化视频-文本语料，包含三个层级：细粒度剪辑-局部描述、阶段-程序摘要、完整视频-全局摘要。
  - 数据采样：训练时每次采样两个相邻层级构成父-子对（如阶段-步骤、步骤-动作）。
  - 对比损失中的批次设置：批次大小B，每个批次包含多个父-子单元。
  - 模型架构：视觉编码器采用Video Transformer或类似结构（未具体说明），文本编码器采用Transformer。双曲映射层为线性投影+指数映射。
  - 优化器：AdamW，学习率、权重衰减等超参数遵循SurgVLP设置。
  - 推理：采用零样本评估（直接使用预训练编码器提取特征，余弦相似度分类）和少样本微调（在目标数据集上线性探针或全微调）。

### 22. Revisiting Parameter Redundancy in Vision-Language-Action Models: Insights from VLM-to-VLA Adaptation
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.31382v1
- 方法流程：
  - 1. 选择预训练的VLM和对应的VLA模型对（如Prismatic→OpenVLA，PaLI-Gemma→π0.5）。
  - 2. 计算共享骨干子空间内的参数相对差异ΔW_rel = ||W_VLA - W_VLM||_2 / ||W_VLM||_2。
  - 3. 分析ΔW_rel在不同模块（LLM注意力头/FFN通道、视觉主干注意力/FFN、投影器FFN）中的空间分布模式。
  - 4. 对每个目标模块，基于|ΔW|构建两种受控剪枝掩码：M_high（剪除差异最高的r%）和M_low（剪除差异最低的r%），直接应用剪枝后评估性能，无需微调。
  - 5. 根据诊断结果识别冗余（低差异参数）和关键（高差异参数）的模块，设计多模块联合剪枝方案，在保留关键参数的前提下剪除冗余参数。
  - 6. 在剪枝后直接推理评估任务成功率，不进行任何性能恢复。
- 关键模块/机制：
  - 参数差异量化模块：计算VLM到VLA适应过程中参数的相对变化幅度，作为结构信号；对每个模块的子参数（注意力头或FFN通道），计算ΔW_rel = ||W_VLA - W_VLM||_2 / (||W_VLM||_2 + ε)，并可视化揭示结构化分布。
  - 受控剪枝诊断模块：通过剪除最高或最低差异参数，建立差异信号与功能贡献的因果关系；对于目标模块，根据参数差异幅度排名，生成M_high（Top-r%）和M_low（Bottom-r%）掩码，直接应用剪枝后评估性能，无需微调。
  - 多模块联合剪枝方案：基于模块异质性选择冗余参数，实现恢复无关剪枝；结合LLM、视觉主干和投影器的差异分布，对低差异区域（如LLM中间层FFN通道、视觉主干浅层注意力）进行剪枝，同时对高差异区域（如LLM初始和末端层、投影器后期）保持较少的剪枝率，通过全局阈值确定剪枝掩码。
  - 掩码构建算法（Algorithm 1）：系统化生成模块感知的VLA剪枝掩码；遍历LLM、视觉、投影器模块，对每个子参数计算基于L2范数的通道级或头级重要性，聚合后全局排序，按Lowest或Highest策略确定阈值，生成单元掩码并扩展到参数级掩码，同时处理FFN的输入输出对齐。
- 公式/目标函数/指标定义：
  - 参数剪枝目标（传统）：max_M S(f(·; P(W; M))) s.t. ||M||_0 ≤ k，其中S是任务成功率，k是稀疏度预算。
  - 相对参数差异：ΔW_rel = ||W_VLA - W_VLM||_2 / ||W_VLM||_2，用于量化适应过程中的参数变化。
  - 最高差异剪枝掩码：M_high(r) = Top-r% parameters ranked by |ΔW|
  - 最低差异剪枝掩码：M_low(r) = Bottom-r% parameters ranked by |ΔW|
- 算法细节：
  - Algorithm 1: 模块感知的VLA掩码构建。输入：W_VLM, W_VLA, 剪枝率r, 策略（Lowest/Highest）。输出：二进制掩码M。步骤：初始化重要性池S为空；遍历LLM、视觉、投影器模块；对每个子参数计算ΔW；根据聚合维度d计算通道/头重要性v = ||ΔW||_{L2,dim=d} / (||W_VLM||_{L2,dim=d} + ε)；对于LLM FFN，将对gate/up/down投影的v累加以对齐FFN通道；对于注意力头，通过均值操作聚合到头级重要性；将聚合后的v加入S；全局排序，根据策略找到阈值τ；生成单元掩码M_unit；通过模块内广播扩展到参数级掩码（例如FFN中同时掩码fc1/up/ga...
  - 在诊断过程中，直接应用剪枝掩码进行推理，不经过任何微调或低秩适应。
  - 多模块联合剪枝时，从LLM、视觉、投影器中分别选择低差异参数进行剪除，整体掩码由各模块掩码拼接得到，剪枝比例根据模块异质性调整。
- 训练/推理细节：
  - 诊断阶段：在剪枝后直接进行推理，无任何微调或性能恢复步骤。
  - 对比实验中的恢复控制：使用LoRA微调（FSDP分布式策略）作为性能恢复手段，以验证假设。
  - 所有实验在NVIDIA A100 GPU上执行。
  - 评估指标：任务成功率（SR）。

### 23. Visual Semantic Entropy: Do Vision Language Models Recognize Visual Ambiguity?
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.31407v1
- 方法流程：
  - 输入图像v和问题q
  - 对v进行N次扰动（如随机裁剪、颜色抖动）得到{v_i}，保持q不变
  - 对每个扰动图像v_i，使用VLM生成回答a_i
  - 将所有回答{a_i}通过句子编码器嵌入，使用DPC聚类得到K个原型
  - 计算每个原型的权重w_i = 簇样本数 / 总样本数
  - 计算所有原型嵌入之间的加权成对余弦距离: U = Σ_{i<j} w_i w_j * dist(proto_i, proto_j)
  - 输出U作为不确定性得分
- 关键模块/机制：
  - 图像扰动模块：生成图像的局部变化视图，保持语义不变，探测视觉模糊性；应用随机数据增强（如随机裁剪、颜色抖动等）生成N个扰动图像
  - 答案生成模块：对每个扰动图像，使用固定文本查询生成回答；使用VLM对每个扰动图像进行解码（如采样或贪婪解码）得到回答
  - 语义聚类模块：将语义相似的答案归为原型，减少词汇变化影响；使用句子编码器（如all-MiniLM-L6-v2）嵌入答案，然后使用DPC（密度峰值聚类）算法聚类
  - 不确定性计算模块：根据原型权重和距离计算最终不确定性得分；计算每个原型的权重（簇样本比例），然后计算所有原型嵌入之间的加权成对余弦距离作为不确定性
- 公式/目标函数/指标定义：
  - VSE = Σ_{i<j} w_i w_j * dist(proto_i, proto_j)，其中w_i是原型i的权重（簇样本数/总样本数），dist为余弦距离
- 算法细节：
  - 扰动次数N为超参数（典型值5），聚类使用DPC基于余弦相似度确定密度和距离
  - 权重计算为簇样本数除以总样本数
  - 距离度量使用余弦距离
- 训练/推理细节：
  - 方法用于推理时的不确定性估计，无需训练
  - VLM为预训练模型，不更新参数
  - 图像扰动次数N为超参数（如5或10）

### 24. Token-Sparse Medical Multimodal Reasoning via Dual-Stream Reinforcement Learning
- 区域：deep；分数：9.0
- 原文链接：https://arxiv.org/abs/2606.31599v1
- 方法流程：
  - 输入：图像和文本指令；VLM编码得到视觉token序列E ∈ R^{N×D}。
  - 定位分支：策略模型π_θ生成G个候选输出，每个包含推理轨迹、边界框坐标和最终预测。
  - 对每个候选边界框，执行grounding-aware视觉token剪枝（GTP）：将视觉token划分为前景和背景子集，计算背景token与所有前景token的归一化余弦相似度，将每个背景token分配给最相似的前景token并通过加权聚合融合，得到稀疏视觉表示Ŝ^{(g)}_kept。
  - token稀疏推理分支：将稀疏视觉表示输入解码器进行组rollout，每个稀疏表示生成G个推理轨迹，产生G^2个输出。
  - rollout对齐：将定位分支的每个输出重复G次，与推理分支输出一一对应。
  - 计算复合奖励（格式、IoU、准确率、交叉反馈），并采用顺序优化：先优化定位分支（式1），冻结后优化推理分支（式2），使用GRPO目标（式3）。
- 关键模块/机制：
  - 定位分支：生成候选边界框，定位视觉证据区域；基于VLM策略模型π_θ，输入指令X，生成G个rollout，每个包含推理、坐标（边界框b_pred）和输出。坐标用于后续token剪枝。
  - Token稀疏推理分支（GTP）：基于定位分支的边界框进行视觉token剪枝与融合，执行稀疏推理；视觉编码器输出token E，根据边界框分为前景（框内）和背景（框外）。计算每个背景token与所有前景token的归一化余弦相似度，将背景token分配给最相似的前景token，通过加权聚合融合，得到前景中心的稀疏表示Ŝ_kept。然后分配旋转位置编码（RoPE），输入解码器进行推理。
  - 交叉反馈奖励：提供分支间的互监督信号，协调优化；定位分支的总奖励RL_total = 3*(1-λ)*RL_format + λ*RL_IoU + λ*RL_acc + λ*RS_acc，其中RS_acc来自推理分支的准确率。推理分支的总奖励RS_total = 2*(1-λ)*RS_format + λ*RS_acc + λ*RL_acc。交叉反馈项使定位准确时推理正确则给予额外奖励。
  - 顺序优化：解耦分支梯度，避免冲突，稳定训练；先仅优化定位分支（使用RL_total），待定位能力收敛后冻结定位分支，再优化推理分支（使用RS_total）。使用GRPO算法进行策略优化，目标函数包含策略比、裁剪项和KL正则化。
- 公式/目标函数/指标定义：
  - RL_total = 3·(1-λ)·RL_format + λ·RL_IoU + λ·RL_acc + λ·RS_acc（定位分支总奖励）
  - RS_total = 2·(1-λ)·RS_format + λ·RS_acc + λ·RL_acc（推理分支总奖励）
  - L_GRPO(θ) = E_{X~D}[1/G Σ_{g=1}^G min(π_θ/π_{θ_old} A_g, clip(π_θ/π_{θ_old}, 1-ε, 1+ε) A_g) - β D_KL(π_θ || π_ref)]
- 算法细节：
  - 视觉token剪枝：计算背景token与前景token的归一化余弦相似度，分配并加权融合。
  - 组rollout：对每个候选边界框生成G个稀疏表示，每个再生成G个推理轨迹，共G^2个。
  - rollout对齐：定位输出重复G次，与推理输出建立一一对应。
  - 顺序优化阶段：阶段1优化定位分支，阶段2冻结定位分支仅优化推理分支。
- 训练/推理细节：
  - 训练：8块NVIDIA H200 GPU，使用veRL框架，约8小时。
  - 推理：启用GTP（grounding-aware token pruning），与vLLM框架兼容，实现约4.5倍（7B）和3.4倍（32B）加速。
  - 训练数据：医学VQA基准，使用定位标注（边界框）和答案。
  - 优化器：GRPO，超参数λ控制奖励权重，ε裁剪范围，β KL系数。

### 25. G$^3$VLA: Geometric inductive bias for Vision-Language-Action Models
- 区域：quick；分数：8.0
- 原文链接：https://arxiv.org/abs/2606.24472v1
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 26. Vision-Language Model Reasoning for Contextual Semantic Mapping in Intralogistics
- 区域：quick；分数：8.0
- 原文链接：https://arxiv.org/abs/2606.24814v1
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 27. MedGuards: Multi-Agent System for Reliable Medical Error Detection and Correction
- 区域：quick；分数：8.0
- 原文链接：https://arxiv.org/abs/2606.25651v2
- 方法流程：
  - 输入临床笔记x
  - 步骤1（错误检测）：两个检测智能体独立分析x，输出二元预测d和推理轨迹、置信度；若一致则直接输出，否则仲裁智能体综合所有信息做出最终决策d
  - 步骤2（错误定位）：两个定位智能体独立输出错误候选句子s及推理轨迹、置信度；若一致则直接输出，否则仲裁智能体综合输出最终s；通过字符级相似度（SequenceMatcher）将候选句子与原始句子对齐得到s*
  - 步骤3（错误纠正）：单个纠正智能体基于原始笔记x和定位的句子s*生成纠正版本ŝ
  - 输出纠正句子集ŝ（若检测到错误）
- 关键模块/机制：
  - 错误检测智能体（Agent_Det）：判断输入临床笔记中是否存在错误，输出二元标签、推理轨迹和置信度；两个基智能体分别分析输入x，产生(d_i, r_det_i, c_det_i)；若d1≠d2，激活仲裁智能体Agent_Det_3，其输入为x拼接(d1, r_det_1, c_det_1)和(d2, r_det_2, c_det_2)，通过ICL做出最终决策。
  - 错误定位智能体（Agent_Loc）：从输入笔记中识别包含错误的句子，输出句子、推理轨迹和置信度；两个基智能体独立输出候选句子si；若s1≠s2，激活仲裁智能体Agent_Loc_3，其输入为x拼接(s1, r_loc_1, c_loc_1)和(s2, r_loc_2, c_loc_2)；最终定位结果通过字符级相似度（公式4）对齐到原始文本中的句子s*。
  - 错误纠正智能体（Agent_Corr）：基于原始笔记和定位的句子生成正确版本；单个智能体，输入s*和x，输出纠正后的句子ŝ。
  - 仲裁智能体（Arbitration Agent）：在检测或定位步骤中，当两个基智能体输出不一致时，基于ICL解决分歧；仲裁智能体不进行额外训练，而是将原始输入、候选输出、推理轨迹和置信度分数作为上下文提示，合成最终决策。
  - 关键字优先纠正分数（KPCS）：评估纠正输出质量，兼顾关键字存在性和语义相似度；三步计算：①关键字检查K(Sc,Sr)=n/m；②语义与词汇相似度M=平均(ROUGE-1, BERTScore, BLEURT)；③加权组合KPCS=α·K + (1-α)·M。
- 公式/目标函数/指标定义：
  - 字符级相似度：sim(xi, s) = 2·M / (|xi|+|s|)，其中M是最长公共子序列中匹配字符数，xi为生成句子，s为参考句子。
  - 检测任务：d = f_Det(x)，d∈{0,1}
  - 定位任务：s = f_Loc(x | d)
  - 纠正任务：ŝ = f_Corr(s | x)
  - 错误检测仲裁：d = Agent_Det_3( x ∥ (d1, r_Det_1, c_Det_1), (d2, r_Det_2, c_Det_2) )
  - 错误定位仲裁：s = Agent_Loc_3( x ∥ (s1, r_Loc_1, c_Loc_1), (s2, r_Loc_2, c_Loc_2) )
- 算法细节：
  - 多智能体自一致性：对于检测和定位子任务，运行两个独立智能体，只有当输出一致时才接受；否则触发仲裁智能体，仲裁智能体以ICL方式综合所有信息做出最终决策。
  - 字符级句子对齐：定位步骤中，使用Python的SequenceMatcher计算字符级相似度，从原始笔记中选出与候选句子最匹配的句子作为最终错误句子，避免语义偏离。
  - ICL仲裁：仲裁智能体不更新参数，而是将两个基智能体的输出、推理轨迹和置信度作为上下文拼接输入，依赖LLM的上下文学习能力解决争议。
- 训练/推理细节：
  - 无需额外训练：MedGuards完全基于预训练LLM，通过多智能体系统进行推理，不进行微调或参数更新。
  - 推理时运行三个步骤：检测、定位、纠正，每个步骤可能调用多个LLM实例（基智能体+仲裁智能体）。
  - 置信度分数：基智能体在输出时附带置信度（[0,1]），用于仲裁时提供参考。

### 28. GeMoE: Gating Entropy is All You Need for Uncertainty-aware Adaptive Routing in MoE-based Large Vision-Language Models
- 区域：quick；分数：8.0
- 原文链接：https://arxiv.org/abs/2606.26287v1
- 方法流程：
  - 输入token经过路由层得到路由权重分布，计算门控熵H(x)。
  - 将门控熵输入专家数量预测器P，得到软预测ksoft，再舍入为整数k。
  - 根据k激活Top-k专家，计算加权输出。
- 关键模块/机制：
  - 门控熵计算：评估token的复杂性/不确定性；对路由权重分布Rnorm(x)计算熵：H(x)= -Σ wi(x) log2 wi(x)。
  - 专家数量预测器P：从门控熵映射到所需专家数量；线性层+Softmax得到专家数的软分布，加权求和得ksoft，再经直通估计器舍入为整数k。
  - 单调性损失Lmono：强制专家数量随门控熵单调递增；对任意两个token (xi, xj)，若H(xi)>H(xj)则要求ksoft_i > ksoft_j + margin，否则惩罚。
  - 整体训练损失：联合优化任务性能、单调性约束和负载均衡；L = Lce + α Lmono + β Llb，其中Llb是负载均衡损失。
- 公式/目标函数/指标定义：
  - 门控熵: H(x|R) = - Σ_i wi(x) log2 wi(x)
  - 专家数量预测: N(x)_i = exp(P(x)_i) / Σ_j exp(P(x)_j), ksoft = Σ_i i * N(x)_i, k = Round(ksoft)
  - 单调性损失: Lmono = Σ_{(xi,xj)} max(0, margin - (ksoft_i - ksoft_j)) if H(xi) > H(xj)
  - 总损失: L = Lce + α Lmono + β Llb
  - MDL代理不等式: 若log(P(y|x,E'_new)/P(y|x,E')) > c，则添加专家；门控熵可近似信息增益。
- 算法细节：
  - 每个token通过路由层得到K专家上的权重分布，计算门控熵。
  - 门控熵作为输入，通过线性层预测专家数量分布，得到软值ksoft。
  - 使用直通估计器将ksoft舍入为整数k，前向传播时使用k进行Top-k路由，反向传播时使用ksoft的梯度更新预测器。
  - 训练时加入单调性损失，迫使同一batch内门控熵高的token获得更大的ksoft。
  - 负载均衡损失Llb鼓励专家间负载均衡。
- 训练/推理细节：
  - 训练时冻结专家参数，仅微调路由器和专家数量预测器。
  - 使用直通估计器使舍入操作可微。
  - α和β为损失权重超参数（文中未给出具体数值）。
  - 推理时，计算门控熵，通过预测器得到k，执行Top-k路由，无需额外计算。

### 29. Towards Fast and Effective Long Video Understanding of Multimodal Large Language Models via Adaptive Quasi-Gaussian Sampling
- 区域：quick；分数：7.0
- 原文链接：https://arxiv.org/abs/2606.24187v2
- 方法流程：
  - 摘要级信息不足：建议结合论文方法部分补充输入、核心处理流程和输出。
- 关键模块/机制：
  - 待确认核心模块：占位；当前信息不足，需从论文方法图、算法框或代码中确认。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式。
- 算法细节：
  - 摘要级信息不足，暂不能确认完整算法细节。
- 训练/推理细节：
  - 摘要级信息不足，暂不能确认训练或推理细节。

### 30. Toward Low-Latency Vision-Language Models with Doubly-Correct Predictions in Egocentric Visual Understanding
- 区域：quick；分数：7.0
- 原文链接：https://arxiv.org/abs/2606.25160v1
- 方法流程：
  - 输入: 自我中心视频帧序列，使用预训练VLM（如CLIP变体）提取帧嵌入和rationale热图
  - 阶段1: 对每个视频样本，通过VLM获取预测标签和空间-时间注意力权重作为rationale证据
  - 阶段2: 计算每层参数的权重幅度，并结合rationale重要性（如注意力权重高的帧/区域对应的层更关键）生成非均匀剪枝比率
  - 阶段3: 基于剪枝比率移除低重要性参数，保留与正确rationale强相关的参数
  - 输出: 剪枝后的轻量VLM，其预测同时依赖正确类别和有效证据
- 关键模块/机制：
  - Spatio-temporal Doubly-Correct Evaluation Protocol：评估剪枝后模型是否同时输出正确标签和有效证据（rationale热图与真实动作区域一致）；对每个视频样本，定义双重正确样本：预测类别正确且rationale热图定位到包含动作的关键帧/空间区域。使用该协议对比不同剪枝方法。
  - Rationale-Informed Pruning Strategy：利用模型自身rationale指导非均匀剪枝，保留对证据识别重要的参数；基于权重幅度（magnitude）计算各层参数的基础重要性；再结合模型生成的rationale热图（如注意力权重），对与高rationale响应相关的层赋予更低剪枝率，从而保留证据传递路径。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式
- 算法细节：
  - 非均匀剪枝比率分配：对第l层，基础剪枝率p_l ∝ 1/|W_l|（权重幅度逆相关），然后根据该层参数对rationale热图贡献度加权调整，关键层剪枝率降低。
  - rationale热图生成：通过VLM的注意力层输出与正/负文本提示的余弦相似度加权求和得到时空定位图（见图2示意）。
- 训练/推理细节：
  - 无需额外训练，剪枝过程基于预训练VLM的权重和一次前向传播得到的rationale信息
  - 剪枝后可直接用于推理，未提及需要微调

### 31. TriViewBench: Controlled Complexity Scaling for Multi-View Structural Reasoning in MLLMs
- 区域：quick；分数：7.0
- 原文链接：https://arxiv.org/abs/2606.26029v1
- 方法流程：
  - 场景生成：使用合成引擎创建3D场景，参数化控制对象数量（1-10个）和遮挡程度（通过物体间随机遮挡生成不同可见性比例）。
  - 视图渲染：为每个场景生成三个正交视图（前、侧、俯视图），构成多视图输入。
  - QA对构建：针对每个场景生成14K+ QA对，分为4个复杂度级别和3个推理类别（Local Decision: 两物体空间关系判断; Object Counting: 指定视角下物体计数; Global Recovery: 场景全局属性恢复如塔高有序列表）。
  - 评估协议：对18个MLLMs采用统一提示协议，包括直接提示（128 tokens限制）和思维链提示（2048 tokens限制），使用贪婪解码（temperature=0），准确性通过精确字符串匹配评估。
  - 错误分析：对计数任务进行过计数/欠计数分类，分析单一视角与多视角下的错误模式差异；按对象数量、遮挡程度分层分析性能退化曲线。
- 关键模块/机制：
  - 场景参数化生成器：生成可控复杂度的3D场景作为评估材料；使用合成引擎随机放置物体，参数化控制物体数量（1-10）和遮挡程度（通过物体间碰撞生成随机遮挡），每个场景生成三个正交视图（前、侧、俯）。总共1923个场景。
  - 复杂度级别定义：将场景按结构复杂度分为4个级别，用于测试性能单调退化；根据场景中物体数量和遮挡程度组合，定义Level 1（简单，少物体低遮挡）到Level 4（复杂，多物体高遮挡）。每个级别对应不同难度的QA对。
  - 推理类别体系：分离三种不同层次的推理能力：局部决策、目标计数、全局恢复；Local Decision: 比较两个指定物体的属性（如高度/距离）；Object Counting: 指定视角或全局的物体数量；Global Recovery: 恢复全局场景结构（如所有塔高度排序列表）。
  - 跨视图身份混淆分析：识别多视图计数错误中的过计数机制；通过比较单视图与三视图下计数错误的方向分布，发现模型在多视图下倾向于将同一物体在不同视角下重复计数，导致过计数。
- 公式/目标函数/指标定义：
  - 准确率指标：对于大多数问题，采用精确字符串匹配；对于Global Recovery中的tower_height_list，预测列表和真实列表均降序排列，完全一致才正确。
  - 相对性能退化率：例如Local Decision相对下降12.11%，Object Counting相对下降59.14%，Global Recovery相对下降80.02%。
- 算法细节：
  - 评估协议：每个模型在相同提示下运行，直接提示限制输出128 tokens，CoT提示限制2048 tokens，使用贪婪解码。
  - 错误分类：对Object Counting任务，将输出数值与真实数值比较，分为过计数（>真实值）、正确、欠计数（<真实值）。
  - 遮挡分析：计算场景级平均可见性比率（所有物体可见部分的平均比例），按阈值分为三个遮挡级别（>0.90, 0.75-0.90, 0.50-0.75），统计每个级别的准确率。
- 训练/推理细节：
  - 推理配置：所有模型使用温度=0的贪婪解码以确保可重复性。直接提示最多128 tokens，CoT提示最多2048 tokens。
  - 统一提示协议：模型仅接受图像和问题文本，输出简洁答案。

### 32. Reducing Redundancy in Whole-Slide Image Patching for Scalable Indexing and Retrieval
- 区域：quick；分数：7.0
- 原文链接：https://arxiv.org/abs/2606.26157v1
- 方法流程：
  - 输入：带标签的WSI集合{(Wi, Li)}
  - 对每个WSI应用无监督图块选择算法（如Yottixel, SPLICE, SDM）提取代表性子图块集合Pi
  - 通过基础模型（如病理基础模型）将每个图块映射为嵌入向量
  - 构建冗余仓库：遍历所有不同类别WSI对，计算所有图块嵌入对的相似度，收集分布S；设定q分位数阈值Sq；移除相似度低于Sq的图块对，将这些图块嵌入加入冗余仓库R，并从原集合中删除，更新得到图块嵌入集合ˆPi
  - 将ˆPi中的嵌入转换为BOB条形码ˆBi
  - 推理：对新WSI，同样提取图块嵌入P，对每个嵌入Pik，检查其与R中所有嵌入的相似度，若有低于Sq的，则移除该嵌入，得到更新嵌入˜Pi，再转换为BOB条形码˜Bi
  - 在已有BOB条形码仓库（ˆBi, Li, Gi）中检索与˜Bi最相似的k个（排除相同患者ID），通过多数投票预测标签
- 关键模块/机制：
  - 冗余仓库构建模块（Algorithm 1）：离线构建冗余仓库，存储跨类别中高度相似的冗余图块嵌入，并更新原始图块嵌入集合；输入所有WSI的图块嵌入和标签，输出冗余仓库R和更新后的图块嵌入。核心：跨类别两两比较，计算所有图块对相似度，取q分位数作为阈值，移除低于阈值的图块对并加入R。
  - 查询时冗余移除模块（Algorithm 2）：对查询WSI的每个图块嵌入，检查其与R中嵌入的相似度，移除冗余嵌入；对查询WSI的嵌入集合Pi，遍历每个Pik，与R中所有嵌入计算相似度，若存在f(Pik, Rj) < Sq，则从Pi中移除Pik并跳出内层循环。
  - BOB编码转换模块：将浮点嵌入转换为二值条形码，降低存储和距离计算开销；采用BOB（Binary Omni Barcode）方法将高维嵌入转换为固定长度的二进制码，后续检索使用汉明距离，计算速度快。
- 公式/目标函数/指标定义：
  - 输入信息未提供明确公式
  - 相似度函数f（如余弦相似度）
  - q分位数阈值Sq：根据所有跨类别图块对相似度分布的分位数确定
- 算法细节：
  - Algorithm 1：遍历所有WSI i和j (i≠j)，对每个Pi中的嵌入Pik和Pj中的Pjm，计算f(Pik, Pjm)加入列表S；然后得到S的q分位数Sq；再遍历所有i≠j，对每个Pik和Pjm，若f(Pik, Pjm) < Sq，则将Pik和Pjm加入R，并从原集合中移除
  - Algorithm 2：对每个查询WSI的嵌入集合Pi，遍历每个Pik，遍历R中所有嵌入Rj，若f(Pik, Rj) < Sq，则从Pi中移除Pik并break；然后得到更新嵌入˜Pi，转换为BOB ˜Bi；然后在仓库中找k个最近邻（排除相同患者ID），多数投票得标签
- 训练/推理细节：
  - 无传统训练过程：冗余仓库构建基于无监督的跨类别比较，不需要训练
  - 推理时使用预训练基础模型提取图块嵌入，然后执行冗余移除和kNN分类

### 33. Spectral Evolution-Guided Token Pruning in Multimodal Large Language Models
- 区域：quick；分数：6.0
- 原文链接：https://arxiv.org/abs/2606.24165v1
- 方法流程：
  - 输入：视觉token序列X_ℓ和X_{ℓ+1}来自LLM解码器的相邻Transformer层
  - 步骤1：对每个token的d维向量进行FFT，得到频域表示F(X_ℓ)和F(X_{ℓ+1})
  - 步骤2：对频域表示应用高斯高通滤波G，抑制低频分量，保留高频结构信息
  - 步骤3：计算滤波后频域表示之间的L2距离，得到每个token的跨层频谱演化得分s_i
  - 步骤4：对所有视觉token的得分进行归一化，得分高的token被保留，得分低的被剪枝
  - 输出：剪枝后的视觉token子集，供后续Transformer层处理
- 关键模块/机制：
  - FFT变换模块：将token从空间域投影到频域，分离高频和低频成分；对每个视觉token的d维向量独立施加2D FFT（若为空间结构）或1D FFT，将表示分解为复数频域系数
  - 高斯高通滤波器：抑制低频全局信息，突出高频细节及语义变化；在频域中构建高斯型高通掩码G，对低频区域（中心）赋予小权重，高频区域赋予大权重，与频域表示逐元素相乘
  - 跨层频谱演化得分计算：量化Token从低层到高层的表示变化强度；对相邻两层（如ℓ和ℓ+1）的滤波后频域表示计算L2范数差异，得分s_i = ||G⊙F(X_i^{ℓ}) - G⊙F(X_i^{ℓ+1})||_2，再减去均值除以标准差归一化
  - 剪枝选择模块：根据演化得分筛选保留的视觉token；对得分排序，保留前K个得分最高的token（K为预设保留数），其余丢弃；得分计算在预填充阶段的早期层进行
- 公式/目标函数/指标定义：
  - 2D离散傅里叶变换： \hat{X}_{u,v} = \sum_{h=0}^{H-1} \sum_{w=0}^{W-1} X_{h,w} e^{-j2\pi(\frac{uh}{H}+\frac{vw}{W})}
  - 跨层频谱演化得分（未显式给出公式，但可推断为：s_i = \| G \odot \mathcal{F}(X_i^{\ell}) - G \odot \mathcal{F}(X_i^{\ell+1}) \|_2 ，其中G为高斯高通滤波器）
- 算法细节：
  - 预填充阶段选择两个连续Transformer层（如第1层和第2层）计算视觉token的演化得分
  - 对每个token的d维向量分别进行FFT（按空间维度若为2D则展平为1D），得到复数向量
  - 构造高斯高通滤波器：中心低频区域权重0.2，边缘高频区域权重0.8（具体参数根据实验调整）
  - 逐元素乘法后计算L2距离，得到原始得分；然后对所有token得分减去均值并除以标准差进行标准化
  - 标准化后得分高于阈值或排名前N×保留率的token被保留
- 训练/推理细节：
  - 无需重新训练，直接应用于已训练好的MLLM推理阶段
  - 剪枝发生在LLM解码器的早期层（如第1层和第2层之间），后续层只处理保留的token
  - 与token合并方法（如ToMe）兼容：先剪枝后合并，或交替使用

### 34. Boosting Text-Driven Video Segmentation via Geometry-Aware Distillation
- 区域：quick；分数：6.0
- 原文链接：https://arxiv.org/abs/2606.24464v1
- 方法流程：
  - 输入：单帧RGB图像和文本描述
  - MGP阶段：使用单目几何模型（如π3、VGGT）估计深度，随机缩放并添加偏置，将像素投影到3D空间；通过连续相机轨迹生成多视角新视图，经inpainting填充遮挡区域，形成伪视频序列。
  - 伪视频输入SAM2图像编码器和冻结文本编码器，通过可训练跨模态适配器对齐特征，再经记忆注意力模块生成记忆增强特征。
  - 使用VFM编码器（如DINOv3）提取语义特征，通过投影头将记忆特征映射到VFM空间并计算余弦相似度损失作为辅助正则。
  - GAD阶段：在真实视频数据上微调，使用3D感知编码器（如π3）和VFM编码器双蒸馏，通过两个投影头分别对齐记忆特征，蒸馏损失与分割损失联合优化。
  - 输出：预测的视频分割掩码序列
- 关键模块/机制：
  - 单目几何预训练（MGP）：从单图像学习跨视角几何一致性；使用单目几何模型（π3/VGGT）预测深度，随机缩放后投影到3D，沿连续相机轨迹生成多视角图像，经inpainting修复遮挡，形成伪视频。
  - 跨模态适配器（Aθ）：对齐视觉与文本特征；在每层编码器后插入可训练适配器，接收视觉特征和文本特征，输出对齐后的特征，保持编码器冻结。
  - 特征投影模块：将记忆注意力特征映射到VFM/3D特征空间；两层MLP（GELU激活），训练时用于计算蒸馏损失，推理时不使用。
  - 几何感知蒸馏（GAD）：注入3D几何先验和语义先验；在真实视频上，用3D感知编码器和VFM编码器分别提取特征，通过投影头与记忆特征计算余弦相似度损失，与分割损失联合优化。
- 公式/目标函数/指标定义：
  - 分割函数：M = G(V, W)
  - 视觉文本特征：F_{t,l} = E_{img}(I_t), P_l = E_{txt}(W)
  - 跨模态对齐：F'_{t,l} = A_θ(F_{t,l}, P_l)
  - 总损失：L_total = L_seg + L_sim
  - 相似度损失：L_sim = 1 - Sim(F_proj, F_VFM) = 1 - (F_proj·F_VFM)/(||F_proj||·||F_VFM||)
  - 蒸馏损失：L_distill = L_3D + L_VFM = Σ_{k∈{3D,VFM}} [1 - Sim(F^{(k)}_{proj}, F_k)]
- 算法细节：
  - 新视图合成：从单图估计深度，投影到3D点云，沿线性/分段/曲线轨迹生成连续相机位姿，重投影并inpainting
  - 记忆注意力模块：冻结SAM2的记忆注意力，处理伪视频帧序列，特征经投影后与VFM特征对齐
  - 双分支蒸馏：3D感知特征与VFM特征分别通过独立投影头对齐记忆特征，损失权重未明确但联合优化
  - 训练时投影头使用，推理时丢弃
- 训练/推理细节：
  - MGP阶段：在大规模单图像数据集上训练，使用合成伪视频，冻结SAM2图像编码器和文本编码器，仅训练跨模态适配器和投影头
  - GAD阶段：在真实视频分割数据集上微调，冻结SAM2和3D/VFM编码器，训练适配器和两个投影头
  - 推理时仅使用SAM2编码器和记忆注意力、提示编码器、掩码解码器，无额外计算开销

### 35. Are We There Yet? Exploring the Capabilities of MLLMs in Assistive AI Applications
- 区域：quick；分数：6.0
- 原文链接：https://arxiv.org/abs/2606.25084v1
- 方法流程：
  - 数据采集阶段：使用头戴GoPro以1 FPS采样，录制自我中心视频（总时长133.31分钟），提取帧图像（8,025张场景文本图像、150张货币图像、37张故事书页面图像、72张菜单图像等）
  - 任务构建阶段：为每个图像生成自然语言问答对，覆盖场景文本识别、货币识别、导航帮助、多语言菜单阅读、长篇内容阅读五类任务
  - 推理评估阶段：向MLLMs输入图像和问题，模型生成答案；使用ANLS（平均归一化Levenshtein相似度）和LLM-as-a-judge评分评估答案准确性，同时记录推理延迟
- 关键模块/机制：
  - NetraLink数据采集系统：获取真实世界自我中心视角的多模态数据，模拟视觉障碍者使用场景；头戴GoPro相机以30fps录制视频，后处理时以1 FPS抽帧，生成高分辨率静态图像；覆盖城市多种场景（校园、居民区、商业市场）
  - 多模态大语言模型（MLLM）推理引擎：同时处理视觉输入和文本问题，生成自然语言回答；模型包括视觉编码器（如CLIP、QwenViT）和大语言模型（如Gemma、LLaMA），将图像编码为视觉token后与文本token拼接，经自回归解码生成答案。
  - VQA评估框架：统一不同任务的评估格式；每个任务均构造为问题-答案对，问题类型多样性（如货币识别含四种标准问题）；评估时使用ANLS计算生成答案与参考答案的字符级相似度，并使用LLM-as-a-judge进行语义正确性评分。
- 公式/目标函数/指标定义：
  - ANLS = 平均每个样本的1 - Levenshtein距离/最大字符串长度
  - LLM-as-a-judge评分：使用大语言模型评估生成答案的正确性，输出0-1分数

### 36. Intracranial Aneurysm Classification and Segmentation via Tri-Axial ROI and Multi-Task Learning
- 区域：quick；分数：6.0
- 原文链接：https://arxiv.org/abs/2606.26706v1
- 方法流程：
  - 阶段1 - 2D三轴ROI提取：输入C×Z×Y×X体积，在每轴1/4、1/2、3/4位置提取3个切片（最多9个），非空切片重采样至[0.55,0.5]mm间距，用nnU-Net 2D二值分割模型预测，对每个轴约束两个空间维度，通过坐标平均得到3D边界框坐标。输出裁剪后的ROI体积。
  - 阶段2 - 3D多任务学习：将裁剪体积调整至224×224×224，送入共享6阶段残差编码器，经双解码器（Decoder1输出14通道：13血管+1合并动脉瘤；Decoder2输出13通道：每个血管与其动脉瘤位置配对）和三分类头（模态、动脉瘤存在、位置）。训练后通过合并规则恢复26类分割：若Decoder1预测为动脉瘤类（14）且Decoder2预测血管解剖类k，则最终标签为动脉瘤位置类k+13，否则保留Decoder2的血管标签。
  - 推理：单次前向，使用8个TTA增强（所有轴翻转组合），对分类logits平均并应用sigmoid，左右翻转的logits进行标签映射校正。
- 关键模块/机制：
  - 2D三轴ROI提取模块：快速生成血管ROI边界框，避免3D滑动窗口计算开销；对输入体积在三个正交轴（z,y,x）的1/4、1/2、3/4位置各取1切片（共最多9张），非空切片重采样至[0.55,0.5]mm，用nnU-Net 2D二值分割预测。轴向切片约束(y,x)，矢状面约束(z,y)，冠状面约束(z,x)。跨轴预测通过坐标平均得到最终边界框坐标：z_min = (1/|S_Y∪S_X|) ∑_{i∈S_Y∪S_X} z_i_min，类似计算z_max、y和x坐标。
  - 共享残差编码器：提取多尺度特征，作为分割解码器和分类头的共同基础；6阶段Residual Encoder，输出瓶颈特征图大小为B×320×7×7×7。
  - 双分割解码器：分别输出14类（13血管+1合并动脉瘤）和13类（血管-动脉瘤配对），缓解体积不平衡；Decoder1输出14通道，其中动脉瘤类合并为通道14；Decoder2输出13通道，每对对应解剖对称对（如{1,26}）。两个解码器共享前3阶段，最后2阶段独立。训练时Decoder1的动脉瘤类使用10×CE权重，Decoder2的动脉瘤相关类使用5×CE权重。后处理合并：若Decoder1预测动脉瘤类（通道14）且Decoder2预测血管类k，则最终输出动脉瘤位置类k+13，否则保留Decoder2血管标签。
  - 跨注意力池化：将编码器瓶颈特征池化为固定维度的特征向量，用于分类头；以瓶颈特征（7×7×7×320）作为键（K）和值（V），分类头提供查询（Q），通过交叉注意力机制聚合特征。三个分类头分别使用Q=4（模态）、Q=2（存在）、Q=16（位置）。输出维度为B×Q×320，再展平为B×Q*320并送入全连接层。
  - 多任务损失：联合优化五个输出头；总损失L = (1/Z)[λ_seg ∑_{d=1}^2 L_seg_d + L_presence_BCE + L_location_BCE + L_modality_CE]，其中λ_seg=0.5, Z=3.5。每个L_seg_d为Dice和加权CE之和。Decoder1动脉瘤类CE权重10×，Decoder2动脉瘤相关类CE权重5×，背景和血管类为1×。BCE使用逆类别频率加权。
- 公式/目标函数/指标定义：
  - ROI坐标公式：z_min = (1/|S_Y ∪ S_X|) ∑_{i∈S_Y∪S_X} z_i_min, z_max类似，y在轴向和矢状面预测上平均，x在轴向和冠状面预测上平均。
  - 总损失函数：L = (1/Z)[λ_seg ∑_{d=1}^2 L_seg_d + L_presence_BCE + L_location_BCE + L_modality_CE], λ_seg=0.5, Z=3.5。
  - 分割损失L_seg_d = Dice损失 + 加权交叉熵损失（CE）。
  - 官方评估指标：平均AUC = 0.5*(AUC_AP + (1/13)∑_{i=1}^{13} AUC_i)。
- 算法细节：
  - 标签重映射：原始26类（13血管+13动脉瘤位置）通过索引集I1（将13血管单独映射，动脉瘤合并为一类）和I2（按左右对称配对血管和动脉瘤）重新组织。
  - 后处理合并：对每个体素，若Decoder1预测为动脉瘤类（通道14）且Decoder2预测为血管类k，则最终输出动脉瘤位置类k+13，否则保留Decoder2的血管标签。
  - 左右标签翻转增强：训练时水平翻转图像并交换左右结构标签（如左MCA变成右MCA），使数据集翻倍。推理时对左翻转的logits应用逆标签映射校正。
  - TTA：8种轴翻转组合，分类logits取平均后经sigmoid得到概率。
  - 多折集成：选择最佳两个折叠，对分类logits平均。
- 训练/推理细节：
  - 训练优化器：SGD，初始学习率0.01，权重衰减3×10^{-5}，batch size=2，在单张A100上训练1000个epoch。
  - 微调：在100或250个epoch时降低学习率至4×10^{-3}。
  - 推理：单个前向，8倍TTA（所有轴翻转组合），分类logits平均后sigmoid，左右翻转logits做标签校正。
  - 两折集成：Fold 0微调100 epoch，Fold 1微调250 epoch。
  - 数据准备：通过主动学习生成动脉瘤掩膜和血管解剖标签，包括手动校正。
