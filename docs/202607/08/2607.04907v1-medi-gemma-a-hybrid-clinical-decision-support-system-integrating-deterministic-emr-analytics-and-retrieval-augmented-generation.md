---
title: "Medi-Gemma: A Hybrid Clinical Decision Support System Integrating Deterministic EMR Analytics and Retrieval-Augmented Generation"
title_zh: Medi-Gemma：一种整合确定性EMR分析与检索增强生成的混合临床决策支持系统
authors: "Mohammed Saim Ahmed Quadri, Yunzhe Xue, Justin W. Ady, Usman Roshan"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04907v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学临床决策支持中的RAG
tldr: 针对大语言模型在临床决策中存在结构幻觉、确定性推理弱及向量检索遗漏等问题，提出Medi-Gemma混合临床决策支持系统。通过解耦框架分离临床感知与数据编排，结合确定性EMR分析、检索增强生成及真实注入模块，实现可追溯推理与安全合规。验证表明该架构消除了语义漂移，提升了事实遵循度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04907v1/fig-001.webp\", \"caption\": \"Fig. 2. Medical Director dashboard after ingestion of encounter and demographic datasets. The dashboard presents automated patient triage, wound prioritization, and longitudinal patient summaries generated by the deterministic analytics pipeline.\", \"page\": 6, \"index\": 1, \"width\": 1085, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2607.04907v1/fig-002.webp\", \"caption\": \"Fig. 3. Representative multimodal clinical workflow of the proposed Medi-Gemma Clinical Decision Support System. The clinician links a patient record, uploads a wound image for automatic visual analysis, and interacts with the system through natural language queries. The Clinical Orchestrator integrates deterministic patient-state retrieval, multimodal wound understanding, retrieval-augmented generation (RAG), and evidence-based protocol guidance to produce clinically grounded recommendations while maintaining factual consistency with the electronic medical record.\", \"page\": 7, \"index\": 2, \"width\": 1079, \"height\": 783}, {\"url\": \"assets/figures/arxiv/2607.04907v1/fig-003.webp\", \"caption\": \"Fig. 1. Overall architecture of the proposed Medi-Gemma Clinical Decision Support System. Incoming clinician queries are dynamically routed to deterministic analytics or patient-specific reasoning pipelines. The Clinical Orchestrator performs Ground Truth State Injection before retrieval-augmented generation, while the Protocol Manager and Safety Verifier enforce evidence-based clinical safety prior to response generation.\", \"page\": 4, \"index\": 3, \"width\": 648, \"height\": 956}]"
motivation: 现有LLM在临床场景中因结构幻觉、弱确定性推理及检索遗漏而受限，亟需一种兼顾结构化数据保真与生成安全性的混合方案。
method: 构建多阶段管道，由ClinicalOrchestrator协调；通过DataManager处理非结构化EMR，IntentRouter路由至确定性分析或检索增强推理，并利用Ground Truth Injection模块注入最新临床状态以覆盖生成上下文。
result: 该架构消除了语义上下文漂移，防止数据库崩溃，并显著提升对后端临床知识库的事实遵循度。
conclusion: Medi-Gemma为LLM临床决策支持提供了更安全的设计模式，尤其适用于需结构化数据保真与确定性保障的场景。
---

## 摘要
在高风险临床环境中部署大型语言模型（LLM）仍受到结构性幻觉、对表格化患者数据的弱确定性推理以及向量检索遗漏的限制。本文介绍了Medi-Gemma的架构与验证，这是一个用于伤口病理分诊和工作流程自动化的临床决策支持系统（CDSS）。该平台引入了一种解耦框架，将临床感知与数据编排分离，同时保持可追溯的推理。Medi-Gemma使用由集中式ClinicalOrchestrator协调的多阶段流水线。数据请求由DataManager通过类型强制清理非结构化电子病历（EMR）文件，而不进行生成式推理。自然语言查询由分层IntentRouter处理，将请求路由到由PandasQueryEngine执行的确定性分析路径，或由使用CPU优化向量存储的ClinicalRAGEngine管理的患者特定推理。一个关键贡献是Ground Truth Injection模块，它拦截患者特定查询，提取数字识别令牌，通过Pandas查询结构化数据框，检索最新的验证临床状态，并在生成前将该快照作为覆盖上下文块嵌入LLM提示中。安全合规性由确定性ProtocolManager强制执行，它将临床术语映射到固定的循证风险路径，而SafetyVerifier短语过滤器防止输出规则违规。验证表明，该架构消除了语义上下文漂移，防止了数据库编译崩溃，并提高了对后端临床存储库的事实遵守。这些结果支持Medi-Gemma作为基于LLM的临床决策支持的一种更安全的模式，其中结构化数据保真度、检索基础和确定性保障至关重要。

## Abstract
Deploying Large Language Models (LLMs) in high-stakes clinical settings remains limited by structural hallucinations, weak deterministic reasoning over tabular patient data, and omissions in vector retrieval. This paper presents the architecture and validation of Medi-Gemma, a Clinical Decision Support System (CDSS) for wound pathology triage and workflow automation. The platform introduces a decoupled framework that separates clinical perception from data orchestration while preserving traceable reasoning. Medi-Gemma uses a multi-stage pipeline coordinated by a centralized ClinicalOrchestrator. Data requests are handled without generative inference by a DataManager that cleans unstructured Electronic Medical Record (EMR) files through type coercion. Natural language queries are processed by a hierarchical IntentRouter, which routes requests to deterministic analytics paths executed by a PandasQueryEngine or to patient-specific reasoning managed by a ClinicalRAGEngine using a CPU-optimized vector store. A key contribution is the Ground Truth Injection Module, which intercepts patient-specific queries, extracts numeric identification tokens, queries the structured dataframe via Pandas, retrieves the latest validated clinical state, and embeds this snapshot as an overriding context block in the LLM prompt before generation. Safety compliance is enforced by a deterministic ProtocolManager that maps clinical terminology to fixed evidence-based risk pathways, while a SafetyVerifier phrase filter prevents output rule violations. Validation shows that this architecture eliminates semantic context drift, prevents database compilation crashes, and improves factual adherence to backend clinical repositories. These results support Medi-Gemma as a safer pattern for LLM-based clinical decision support where structured data fidelity, retrieval grounding, and deterministic safeguards are essential.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）在临床决策支持系统（CDSS）中具有巨大潜力，如减少医生文书负担、生成结构化病历、加速患者风险分层。但标准生成式文本架构在高风险临床环境中面临严重缺陷，主要表现为：
  - **向量检索遗漏**：传统RAG将文本文件随机分块，基于语义相似度检索，容易忽略关键历史上下文或优先使用过时的病历。
  - **表格幻觉**：生成模型对结构化表格（如生命体征趋势、实验室结果、伤口尺寸）进行精确数值运算时，常产生虚构指标或处理错误。
- **整体含义**：现有LLM无法可靠处理非结构化叙述笔记与结构化纵向数据集结合的临床场景，需要一种能将确定性计算与生成式推理安全结合的混合架构。

## 2. 论文提出的方法论

### 核心思想
- 提出**Medi-Gemma**混合临床决策支持系统，通过解耦框架明确分离**确定性计算**（结构化EMR分析）与**生成式推理**（自然语言理解与响应生成），同时保持推理的可追溯性。
- 核心创新：**Ground Truth State Injection（真实状态注入）**机制——在语言模型推理前，从结构化数据框中确定性地提取患者最新验证的临床状态，并以覆盖指令嵌入LLM提示中。

### 关键技术细节
1. **数据管理层（DataManager）**：通过类型强制（type coercion）清洗非结构化EMR文件，标准化列名，转换数值字段为浮动类型，处理缺失值（数值缺失填0.0，文本缺失保留标记）。
2. **动态意图路由（IntentRouter）**：基于规则优先级分类自然语言查询：
   - 检测患者标识（如“patient id 123”）→ 路由至临床路线（Clinical Route）。
   - 检测聚合关键词（如“how many”“average”）→ 路由至数据路线（Data Route）。
   - 依赖上下文引用（如“this wound”）且存在图像上传 → 路由至临床路线。
3. **确定性分析管道（PandasQueryEngine）**：对分类为数据分析的查询，由本地Gemma模型解释自然语言并生成可执行Python代码，在结构化数据框上执行精确运算（如`.mean()`、`.count()`），避免数值幻觉。
4. **Ground Truth注入流程**（由ClinicalOrchestrator协调）：
   - 提取患者数字标识 → 验证存在性；
   - 在结构化数据框中按Encounter_Date排序，获取该患者最新一次就诊记录；
   - 提取日期、伤口尺寸、组织成分、护士笔记等参数；
   - 构建验证上下文块，注入到LLM提示中，并附带强制执行指令（“必须使用此日期作为当前状态”）。
5. **语义向量检索（ClinicalRAGEngine）**：使用本地CPU部署的BAAI/bge-small-en-v1.5模型，通过LlamaIndex实现向量索引，利用MetadataFilters按患者ID精确过滤，防止跨患者上下文污染。
6. **确定性临床安全层**：
   - **ProtocolManager**：将检测到的病理术语映射到预定义的循证风险等级（如critical infection → Critical Risk，diabetic foot ulcer → High Risk）。
   - **SafetyVerifier**：关键词过滤器，扫描生成响应中的禁止或危险短语，违规时替换为安全警告。

### 算法流程（文字说明）
1. 医生上传结构化患者与就诊数据集。
2. 医生输入自然语言临床查询。
3. IntentRouter分类查询 → 若为统计类，直接由PandasQueryEngine执行代码生成结果；若为患者特定，则继续：
4. ClinicalOrchestrator执行Ground Truth State Injection：从数据框获取最新患者状态，构建上下文块（带“系统注入真实状态，优先级高于检索”的指令）。
5. ClinicalRAGEngine（可选）检索相关叙述上下文。
6. ProtocolManager应用协议映射。
7. SafetyVerifier进行输出安全过滤。
8. 返回最终推荐给医生。

## 3. 实验设计

- **使用数据集/场景**：采用代表性**去标识化EMR数据集**（包含结构化数值测量和 unstructured 临床文档）及**模拟临床工作流**。具体为伤口护理相关的就诊记录和患者人口学表。
- **Benchmark**：文中未明确列出标准基准数据集或对比基线。验证目的是检查各架构组件是否按设计正确运行，而非与现有方法进行定量指标比较。
- **对比方法**：仅在相关工作部分以表格形式将Medi-Gemma与“Conventional Clinical RAG”进行定性能力对比（如语义检索、结构化EMR分析、动态意图路由、真实状态注入、协议推理、安全验证等），但无定量实验对比。

## 4. 资源与算力

- 论文中**未明确提及**使用的GPU型号、数量、训练时长或推理算力需求。
- 仅提到：向量检索引擎使用CPU优化的`BAAI/bge-small-en-v1.5`模型，本地部署；LLM推理使用本地Gemma模型（通过Ollama），但未说明硬件配置。
- 因此，**无法评估训练或推理的具体资源消耗**。

## 5. 实验数量与充分性

- **实验数量**：论文未报告多组不同数据集或消融实验的定量结果。验证部分主要描述了四个能力方面的定性验证：
  - EMR数据预处理验证（数据清洗成功）。
  - 动态意图路由验证（示例查询分类正确）。
  - Ground Truth State Injection验证（描述机制，未给出数值评估）。
  - 混合临床决策管道展示（通过流程图和代表性工作流演示）。
- **充分性与客观性**：实验设计**不充分**。缺少：
  - 与基线方法（如标准RAG、纯LLM）的严格定量比较。
  - 基于可靠指标（如事实一致性、幻觉率、准确率、召回率）的评测。
  - 消融实验（如移除Ground Truth Injection后的表现差异）。
  - 真实临床环境下的前瞻性评估。
- 论文本身也承认“系统已使用代表性去标识化EMR数据集和模拟临床工作流验证，而非在医院现场部署”，因此验证主要**作为概念验证**存在，客观性有限。

## 6. 主要结论与发现

- Medi-Gemma架构**消除了语义上下文漂移**：通过确定性注入最新患者状态，覆盖向量检索可能引入的过时上下文。
- **防止了数据库编译崩溃**：DataManager的类型强制处理确保数值字段的完整性，避免下游处理错误。
- **提高了对后端临床知识库的事实遵循度**：数据分析完全由Pandas直接执行，不依赖LLM进行数值推理；生成部分则结合注入的真实状态和检索的叙述上下文。
- 论文学者认为该架构为LLM临床决策支持提供了更安全的设计模式，尤其适用于需要结构化数据保真度、检索基础与确定性保障的场景。

## 7. 优点

- **明确的解耦设计**：将确定性计算（数据分析、协议映射、安全过滤）与生成式推理分开，提高了可靠性和可审计性。
- **Ground Truth State Injection创新**：从根本上解决RAG中患者状态过时的问题，且不增加模型复杂度。
- **动态意图路由的优先级规则**：简单高效，避免语言模型处理不当查询，降低延迟和错误风险。
- **严格的临床安全层**：协议映射和关键词过滤提供了一层确定性保护，防止生成不当建议（如非法剂量指令）。
- **模块化可扩展**：各组件可独立升级或替换（如更换不同的向量模型或LLM），适应不同临床场景。
- **支持数据隐私**：向量检索和LLM推理均在本地进行（CPU部署小模型），减少数据外泄风险。

## 8. 不足与局限

- **缺乏严格实验验证**：仅展示架构可行性，未与基准方法（如标准RAG、纯LLM）进行定量对比；无消融研究以评估每个组件的贡献。
- **仅限伤口护理领域**：当前实现聚焦于伤口病理分诊和EMR文档，虽然声称可推广，但需额外协议库和领域本体才能应用于其他临床科室。
- **依赖EMR数据质量**：Ground Truth Injection的有效性依赖于底层EMR数据的完整性和准确性；缺失或过时记录仍会限制推理。
- **无真实临床部署评估**：未在活医院环境中测试，缺乏对工作流效率、医生信任度、诊断一致性等实际指标的评估。
- **规则路由与协议映射的局限性**：当前采用基于规则的方法，虽透明但可能无法处理复杂或模糊的查询；未来若引入学习路由，需注意保持安全关键操作的确定性。
- **未报告算力与延迟**：缺乏对系统实时性的评估（如端到端查询响应时间），这对于临床应用至关重要。

（完）
