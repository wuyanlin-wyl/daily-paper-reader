---
title: Do Medical Vision Language Models Actually See? A Counterfactual Grounding Framework and Hard-Negative Contrastive Training for Visually-Reliant Medical VLMs
title_zh: 医学视觉语言模型真的“看到”了吗？一个反事实基础框架和针对视觉依赖型医学VLM的硬负例对比训练
authors: "Anas Zafar, Leema Krishna Murali, Siddhant Bharadwaj, Ashish Vashist, Jia Wu"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03647v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 医学视觉语言模型的反事实评估和对比训练
tldr: 针对医疗视觉语言模型可能依赖文本捷径而非视觉证据的问题，提出反事实评估框架量化视觉依赖度与幻觉率，并设计CORAL方法通过对比目标微调Qwen2.5-VL-7B，在四个医学VQA基准上显著提升准确率（+6.7pp）并降低幻觉率（-8.0pp），揭示了医疗领域视觉依赖弱于通用领域的差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03647v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed counterfactual grounding framework. Given an image and question, the framework constructs multiple counterfactual image conditions, including real, blank, shuffled, no-image, and CLIP-retrieved hard-negative images. Each condition is evaluated under identical prompts and decoding settings across VLMs. The resulting answers and reasoning traces are used to compute accuracy, visual reliance, hallucinated visual reasoning, visual benefit/harm, and hard-negative discrimination metrics. The same hard-negative mechanism also provides the contrastive signal used in CORAL training.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2607.03647v1/fig-002.webp\", \"caption\": \"Table 1: Hard-negative pools used to train CORAL.\", \"page\": 3, \"index\": 2, \"width\": 466, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2607.03647v1/fig-003.webp\", \"caption\": \"Table 4: Per-benchmark CORAL vs. Qwen2.5-VL-7B base, item-level bootstrap CIs. Bold marks CIs that exclude zero.\", \"page\": 7, \"index\": 3, \"width\": 440, \"height\": 1101}]"
motivation: 现有医疗VLM被怀疑利用文本捷径而非真实视觉推理，缺乏系统评估视觉依赖度的方法。
method: "提出反事实评估框架（VRS, VHR）和CORAL训练方法，通过对比目标函数惩罚硬负例图像交换下的答案不变性。"
result: CORAL在四个医学VQA基准上准确率提升6.7个百分点，幻觉率降低8.0个百分点，显著优于基线。
conclusion: 医疗VLM存在视觉依赖不足的问题，CORAL有效增强视觉推理能力但仍存在领域差距，需进一步研究评估与训练改进。
---

## 摘要
大型视觉语言模型在医学问答上报告了较强的准确性，但仍不清楚它们是从视觉证据中推理还是利用文本捷径。我们引入了一个反事实评估框架，通过用受控替代图像（空白、像素打乱、图像缺失以及CLIP检索的硬负例）替换输入图像来解耦视觉和文本贡献，并推导出一组基础指标，包括视觉依赖评分和视觉幻觉率。我们进一步引入了CORAL（对比检索增强学习），这是一个基于Qwen2.5-VL-7B的7B参数LoRA微调模型，使用对比基础目标进行训练，该目标惩罚在硬负例图像交换下的答案不变性。在四个封闭式医学VQA基准测试（PathVQA, PMC-VQA, SLAKE, VQA-RAD；总计n=400）的配对对照评估中，CORAL相比基础Qwen2.5-VL-7B模型，宏平均准确率提高了6.7个百分点（P(Δ>0)=0.988），VHR降低了8.0个百分点（P<0.001）；MedVLThinker RL变体在两个指标上均未获得显著提升。跨领域诊断进一步揭示，图像替换在医学基准上仅造成≤6.5个百分点的损失，而在通用领域任务上则造成48-61个百分点的损失，这定位了CGO所针对的基础差距。我们公开讨论了评估局限（包括训练/评估基准重叠和次要指标统计效力不足），并发布我们的框架、训练代码和模型权重，以支持医学VLM的可重复基础审计。

## Abstract
Large vision language models (VLMs) report strong accuracy on medical question-answering, yet it remains unclear whether they reason from visual evidence or exploit textual shortcuts. We introduce a counterfactual evaluation framework that decouples visual and textual contributions by substituting input images with controlled surrogates blank, pixel-shuffled, image-absent, and CLIP-retrieved hard negatives and derive a suite of grounding metrics including the Visual Reliance Score (VRS) and Visual Hallucination Rate (VHR). We further introduce CORAL (COntrastive Retrieval-Augmented Learning), a 7B-parameter LoRA fine-tune of Qwen2.5-VL-7B trained with a Contrastive Grounding Objective (CGO) that penalises answer invariance under hard-negative image swaps. On a paired controlled evaluation across four closed-form medical VQA benchmarks (PathVQA, PMC-VQA, SLAKE, VQA-RAD; n=400 total), CORAL improves macro accuracy by +6.7 pp (P(Delta>0)=0.988) and reduces VHR by 8.0 pp (P<0.001) over the matched Qwen2.5-VL-7B base; neither MedVLThinker RL variant achieves a significant gain on either metric. Cross-domain diagnostics further reveal that image substitution costs only <=6.5 pp on medical benchmarks versus 48-61 pp on general-domain tasks, situating the grounding gap that CGO targets. We discuss evaluation limitations openly including train/eval benchmark overlap and underpowered secondary metrics and release our framework, training code, and model weights to support reproducible grounding audits of medical VLMs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义
- **研究动机**：当前医疗视觉语言模型（VLM）在标准医学问答基准上报告了高准确率，但这一指标无法区分模型是真正基于视觉证据推理，还是利用文本捷径（如数据集中的共现模式、问题措辞偏差）。这种隐藏的“语言先验”依赖性在临床部署中构成安全风险，因为模型可能在不看图像的情况下给出正确答案。
- **整体含义**：提出一套系统化、可复现的框架来量化医疗VLM的视觉依赖程度，并设计训练干预鼓励模型“真正看到”图像，从而提高临床可靠性。

## 2. 方法论
### 核心思想
- **反事实评估**：通过构造一系列图像条件（真实、空白、像素打乱、无图像、CLIP检索硬负例），观察模型在不同条件下答案的变化，从而解耦视觉与文本贡献。
- **对比训练**：引入对比基础目标（Contrastive Grounding Objective, CGO），在训练中惩罚模型在面对视觉相似但标签不同的硬负例图像时保持答案不变的行为。

### 关键技术细节
- **图像条件构造**：
  - `x_real`：原始图像（基准）
  - `x_blank`：均匀灰色图像
  - `x_shuffle`：32×32 像素块随机排列
  - `x_∅`：无图像（仅文本）
  - `x_HN`：CLIP-ViT-L/14 检索到的与真实图像最相似但标签不同的硬负例
  - `x_cor`：其他退化（高斯噪声、模糊、JPEG压缩、遮盖）
- **评估指标**：
  - VRS（视觉依赖评分）= Acc_real - Acc_shuffle
  - VHR（视觉幻觉率）= 模型在打乱图像上正确、但在真实图像上错误的样本比例
  - VBR（视觉获益率）、IS（图像敏感性）、NVCR/HVRR（针对思维链模型）等共8项指标
- **CORAL模型训练**（对比检索增强学习）：
  - 基础模型：Qwen2.5-VL-7B-Instruct（7B参数）
  - 训练目标：CGO = `r_acc` + λ * 1[Ans(x_real) ≠ Ans(x_HN)]，λ=0.5
  - 硬负例检索：使用FAISS对训练池内（共5,778对）进行CLIP嵌入的近似最近邻搜索，要求标签不同
  - 训练方式：LoRA（r=16, α=32）微调，冻结基础权重
  - 优化器：AdamW，学习率1e-4，余弦调度，5% warmup，2000步
  - 批量大小：8（4×80GB H100，梯度累积4）

## 3. 实验设计
- **数据集与基准**：
  - **Phase 2（主要配对评估）**：PathVQA, PMC-VQA, SLAKE, VQA-RAD（各100样本，共400样本）
  - **跨域诊断（T5-T10）**：同上医学集 + ChartQA, ScienceQA, VQAv2, GQA, MedXpertQA-MM, MMMU-Medical（各500样本）
- **对比方法**：
  - Qwen2.5-VL-7B（基础）
  - RL(text)（MedVLThinker-7B-RL_m23k）：GRPO在23K纯文本医学QA上微调
  - RL(image)（MedVLThinker-7B-RL_PMC）：GRPO在23K图像-问题-答案（PMC-VQA）上微调
  - CORAL（本文方法，基于Qwen2.5-VL-7B + CGO）
- **评估协议**：所有模型使用相同的提示、贪婪解码（temperature=0）、max_new_tokens=2048，bfloat16，FlashAttention-2，H100 GPU。

## 4. 资源与算力
- **GPU型号与数量**：4 × 80GB H100
- **训练设置**：LoRA微调，2000步，batch=8，梯度累积4，bfloat16
- **训练时长**：文中未明确给出具体训练小时数，但可推算：2000步，batch=8（有效batch=32），在4×H100上预计数小时内完成（未提及精确时间）。
- **评估**：所有模型在同一批固定样本上运行，使用相同GPU配置。

## 5. 实验数量与充分性
- **实验组数**：
  - Phase 2：4个模型 × 4个基准 × 6种图像条件 = 96项条件-模型组合，但核心对比是宏平均后的统计检验
  - 跨域诊断：4个模型（含3B/7B变体） × 10个基准 = 40组，部分可视化如雷达图
  - 其他消融：硬负例池统计、不同退化条件、合成校准信号等（文中提及但未全部详细列出）
- **充分性评价**：
  - **客观公平**：所有模型在相同样本、相同硬件、相同解码参数下评估；统计采用配对bootstrap（B=10000）和McNemar检验，置信区间报告详实
  - **局限**：Phase 2仅400样本，导致部分指标（如VRS、BD）统计效力不足，95%CI跨越零点；仅对比了Qwen2.5-VL系列的7B模型，未覆盖LLaVA-Med、BiomedGPT等其他架构；训练集（PMC-VQA、PathVQA等）和评估集存在重叠
  - **充分性**：作者公开承认上述局限，并合理控制结论范围，实验设计在学术规范内较为充分

## 6. 主要结论与发现
- **CORAL显著提升准确率**：宏平均准确率提高+6.7个百分点（95%CI [0.75, 12.5]，P(Δ>0)=0.988）
- **CORAL显著降低视觉幻觉率**：VHR降低8.0个百分点（95%CI [-11.75, -4.25]，P<0.001），相对减少57%
- **RL变体无显著改善**：RL(text)和RL(image)在准确率和各基础指标上均未超过基础模型；RL(image)在无图像条件下准确率最高（0.548），表明其依赖文本先验而非视觉
- **医疗领域视觉依赖差距**：图像替换在医疗基准上仅损失≤6.5个百分点，而通用领域损失48-61个百分点，说明医疗VLM存在严重的基础差距
- **CORAL的副作用**：在提取答案图像敏感性（IS_pred）上，CORAL表现更差（0.698 vs 基础0.518），这是作者承认的待改进方向

## 7. 优点
- **评估框架系统全面**：首次为医疗VLM提供解耦视觉/文本贡献的完整反事实评估套件，指标覆盖准确率、依赖度、幻觉率、敏感度等多个维度
- **训练目标直接针对安全风险**：CGO惩罚语言捷径，奖励在视觉混淆下改变答案，对应临床中应避免的“错误且自信”场景
- **统计严谨**：使用配对bootstrap置信区间、McNemar检验、方向性P(Δ>0)，避免p值滥用
- **可复现性**：公开框架代码、训练代码、模型权重，便于社区审计
- **揭示重要现象**：RL(image)在无图像时准确率最高，这一反直觉发现突显了仅靠准确率评估的局限性

## 8. 不足与局限
- **评估规模有限**：Phase 2每基准仅100样本，导致VRS、BD等次要指标统计效力不足，无法得出强结论
- **图像敏感性反效果**：CORAL在IS_pred上更差（70%情况下给定打乱图像仍给出相同答案），说明CGO未能充分鼓励对一般图像扰动的敏感性，可能只对硬负例有效
- **训练/评估基准重叠**：PMC-VQA既用于训练又用于评估，可能存在过拟合风险
- **模型架构覆盖不足**：仅评估了Qwen2.5-VL系列，未涉及LLaVA-Med、BiomedGPT、HuatuoGPT-Vision等其他常用医疗VLM
- **输出格式约束**：CORAL训练时强制直接答案（从PMC-VQA格式），牺牲了思维链能力，无法与CoT模型公平比较HVRR等指标
- **硬负例检索域外泛化**：硬负例仅来自训练池内，未测试跨模态或跨数据集的泛化性

（完）
