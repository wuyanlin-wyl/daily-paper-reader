---
title: "Dual-Adaptive SAM3: Hierarchical Routing over Low-Rank Expert Layers for Parameter-Efficient Medical Image Segmentation"
title_zh: 双自适应SAM3：基于低秩专家层的层次路由用于参数高效医学图像分割
authors: "Ying Chen, Jinyue Li, Kun Wang, Qiankun Li, Yang Liu"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.02571v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: SAM3参数高效适配医学图像分割
tldr: "针对SAM3模型在医学图像分割中参数效率低的问题，提出Dual-Adaptive SAM3框架，通过动态专家路由（DER）与分解参数化专家（DPE）实现任务感知和参数感知的双自适应。DER基于视觉输入和文本提示稀疏激活最相关专家，DPE将专家分解为共享冻结基座和轻量可训练低秩增量，减少80%以上参数。在多个医学分割基准上，DA-SAM3匹配或超越全微调SAM3，并较当前最优方法提升5%，验证了其有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02571v1/fig-001.webp\", \"caption\": \"Fig. 1. (a) The architecture of the Dual-Adaptive SAM3 framework. (b) The architecture of trainable Transformer. (c) The Dual-Adaptive MoE Layer including Dynamic Expert Router (DER) and Decomposed Parameterized Expert (DPE)\", \"page\": 3, \"index\": 1, \"width\": 673, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2607.02571v1/fig-002.webp\", \"caption\": \"Table 1. Comparison with state-of-the-art methods on Synapse CT, MMWHS, BTCV, and ACDC.\", \"page\": 7, \"index\": 2, \"width\": 560, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2607.02571v1/fig-003.webp\", \"caption\": \"Fig. 2. Visual comparison of segmentation results.\", \"page\": 8, \"index\": 3, \"width\": 571, \"height\": 433}]"
motivation: 现有SAM3微调参数效率低，标准MoE计算开销大，难以适应多样化的医学图像分割任务。
method: 提出双自适应机制：任务感知的Dynamic Expert Router（DER）根据视觉和文本提示稀疏选择专家，参数感知的Decomposed Parameterized Experts（DPE）用共享冻结基座加低秩增量表示专家。
result: "在多个公开医学分割基准上，DA-SAM3匹配或超越全微调SAM3和标准MoE基线，且较当前SOTA方法提升5%，参数开销降低80%以上。"
conclusion: DA-SAM3通过双自适应机制有效平衡了医学图像分割的精度与参数效率，具有临床实用潜力。
---

## 摘要
基于概念的任意分割模型（SAM3）通过自然语言交互开创了开放词汇分割的新范式，在医学图像分析中具有巨大潜力。然而，如何有效适配这一强大的视觉-语言模型到多样且细微的医学影像领域仍是一个关键挑战。朴素微调参数效率低下，而标准的混合专家（MoE）方法引入了过高的计算开销，限制了其临床适用性。为解决这一问题，我们提出了双自适应SAM3（DA-SAM3），一种通过双自适应特化机制实现高分割精度与极端参数效率的新型框架。我们的第一个自适应是任务感知的：动态专家路由器（DER）通过联合推理视觉输入与文本概念提示，稀疏激活最相关的专家，模拟临床会诊过程。第二个自适应是参数感知的：分解参数化专家（DPE）设计将每个专家表示为共享的冻结基（继承自预训练SAM3）和轻量级可训练低秩增量，减少了超过80%的MoE参数开销。在多个公开医学分割基准上的广泛实验表明，双自适应SAM3不仅匹配或超越了全微调SAM3和标准MoE基线的精度，而且相较于当前最先进方法实现了5%的显著提升，可解释的结果验证了其有效性。代码已开源：https://github.com/Reconsider80/DA-SAM3。

## Abstract
The Segment Anything Model with Concepts (SAM3) heralds a new paradigm for open-vocabulary segmentation through natural language interaction, offering significant potential for medical image analysis. However, effectively adapting such a powerful vision-language model to the diverse and nuanced domain of medical imaging remains a key challenge. Naive fine-tuning is parameter-inefficient, while standard Mixture-of-Experts (MoE) methods introduce prohibitive computational overhead, limiting their clinical applicability. To address this, we propose Dual-Adaptive SAM3 (DA-SAM3), a novel framework that achieves both high segmentation accuracy and extreme parameter efficiency via a dual-adaptive specialization mechanism. Our first adaptation is task-aware: a Dynamic Expert Router (DER) that sparsely activates the most relevant experts by jointly reasoning about the visual input and the textual concept prompt, mimicking a clinical consultation process. Our second adaptation is parameter-aware: a Decomposed Parameterized Experts (DPE) design that represents each expert as a shared frozen base (inherited from the pretrained SAM3) and a lightweight trainable low-rank delta, reducing MoE parameter overhead by over 80\%. Extensive experiments on multiple public medical segmentation benchmarks demonstrate that Dual-Adaptive SAM3 not only matches or exceeds the accuracy of fully fine-tuned SAM3 and standard MoE baselines, but also achieves a notable 5\% gain over current state-of-the-art methods, with interpretable results validating its effectiveness. The code is available at: https://github.com/Reconsider80/DA-SAM3.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何高效地将大型视觉-语言基础模型（如SAM3）适配到医学图像分割领域，同时兼顾**高精度**与**参数效率**。现有方法要么全参数微调导致参数量巨大且易灾难性遗忘，要么标准混合专家（MoE）带来过高的计算和存储开销，难以在资源受限的临床环境中部署。
- **背景**：SAM3通过自然语言交互实现开放词汇分割，在医学图像分析中有巨大潜力。但医学影像模态多样、解剖结构复杂、病理表现多变，需要对模型进行**任务感知和参数感知**的特化适配。现有参数高效微调方法（如LoRA）通常静态且统一应用，无法适应医学概念的动态层次性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 提出**双自适应SAM3（DA-SAM3）**，通过**任务感知的动态专家路由**和**参数感知的低秩专家分解**，实现输入条件化的粗到细推理，模拟临床决策过程。

### 关键技术细节
- **整体架构**：冻结SAM3的图像编码器和文本编码器，仅在融合模块中替换部分前馈网络（FFN）为**双自适应MoE层**，其余MHSA和MSCA层保持冻结。
- **层次化融合解码器**：在融合模块的关键深度（`L/6, L/4, L/2`）放置DA-MoE层，分别对应**早期粗对齐**、**中期语义识别**和**晚期边界精修**，模拟放射科医生多阶段读片过程。
- **动态专家路由器（DER）**：
  - 计算**领域上下文向量** `h_ctx`：概念标记 `C_tok` 与池化视觉特征做交叉注意力，捕捉全局图像-概念相关性。
  - 路由得分：融合当前token特征、`h_ctx`、冻结的概念嵌入 `e_c`，通过可学习投影 `W_r` 得到得分，再经**稀疏Top-k门控**（k=2）选择专家。
- **分解参数化专家（DPE）**：
  - 每个专家 `E_i` 由共享冻结基座 `W_0`（继承自预训练SAM3的FFN权重）加上轻量低秩增量 `ΔW_i = A_i B_i^T`（秩 `r << D`）构成。
  - 输出：`E_i(x) = (W_0 + ΔW_i)x`，最终输出为选中的专家输出加权和（经GELU激活）。
- **两阶段训练**：
  - **预热阶段**：初始化低秩增量为零，路由器随机，训练分割损失 `L_seg` + 负载均衡损失 `L_balance`，使专家特化。
  - **微调阶段**：冻结专家参数，仅微调路由器，增强开放词汇泛化能力。
  - 总损失：`L = L_seg + λ1 L_balance + λ2 L_sparse`（`L_sparse`是路由稀疏正则化项，λ1=0.01, λ2=0.001）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - **Synapse CT**：30例腹部CT，8个器官标签，训练18/测试12，图像resize到224×224。
  - **MMWHS**：20例心脏CT，训练16/测试4。
  - **BTCV**：30例CT，13个腹部结构，训练24/测试6。
  - **ACDC**：150例心脏MRI，使用官方划分。
- **评价指标**：Dice相似系数（DSC↑）和Hausdorff距离（HD↓）。
- **对比方法**：
  - **任务特定网络**：nnU-Net、TransUNet、Swin-UNETR、MedNeXt、Swin-UMamba。
  - **Prompt-free SAM变体**：SAMed、AutoSAM、H-SAM、MoE-SAM。
  - **Prompt-based SAM变体**：SAM、MedSAM、MSA、SAMUS、DeSAM、SAM-Med2D、SAM3。
- **训练细节**：batch size=8，AdamW优化器，weight decay=0.1，学习率0.0005，warmup策略。MoE：专家数=4，top-k=特征总数的一半。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量或训练时长**。仅提及训练配置（batch size、优化器等），但未提供具体硬件信息。这一点在总结中需明确指出。

## 5. 实验数量与充分性

- **实验数量**：
  - **主表（Table 1）**：在四个标准数据集上对比了14种方法，覆盖多种SAM变体和任务特定网络。
  - **消融实验（Table 2）**：在Synapse CT、MMWHS、ACDC三个数据集上测试了以下变体：
    - 基准（SAM3）
    - SAM3+Concat（简单拼接）
    - SAM3+LoRA
    - SAM3+Standard MoE
    - 去除DER（随机路由）
    - 去除DPE（全参数专家）
    - 完整DA-SAM3
  - **可视化对比**（Fig. 2）展示了多个案例的分割结果。
- **充分性与公平性**：
  - 数据集覆盖腹部CT、心脏CT和心脏MRI，模态多样但以CT为主（MRI仅有ACDC）。
  - 对比方法包括近期SOTA，但**缺少同类基于MoE的低秩高效方法**（如仅对比了标准MoE，未对比DeRS等）——尽管DeRS被引用为灵感来源。
  - 消融实验设计合理，逐步验证各组件贡献。
  - 所有实验遵循官方数据划分，保证公平。

## 6. 论文的主要结论与发现

- **性能**：DA-SAM3在四个数据集上均达到最优或次优的DSC和HD，较当前SOTA方法（如MoE-SAM）提升约5%（例如Synapse CT上DSC 85.12 vs 84.71）。
- **参数效率**：DPE设计使MoE参数开销降低80%以上，同时精度超过全微调SAM3和标准MoE。
- **层次路由有效性**：在解码器不同深度放置DA-MoE层，对应粗对齐、语义识别和边界精修，可视化结果验证了这种模拟临床推理设计的有效性。
- **关键组件贡献**：DER和DPE缺一不可，去除任一都会导致显著性能下降（DER缺失时Synapse CT的DSC下降2.37%，DPE缺失时下降1.57%）。

## 7. 优点

1. **双自适应机制新颖**：首次将动态多模态路由与参数高效专家分解集成到视觉-语言基础模型适配中。
2. **参数效率极高**：通过低秩增量+冻结共享基座，MoE参数量减少80%以上，便于临床部署。
3. **层次路由设计**：模仿放射科医生从粗到细的临床推理过程，可解释性强。
4. **两阶段训练策略**：先专家特化后路由校准，避免路由崩溃并提升泛化性。
5. **全面实验验证**：在四个数据集上对比大量方法，消融实验充分，代码开源。

## 8. 不足与局限

1. **模态覆盖不足**：仅包括CT和MRI（且MRI只有一个ACDC数据集），未实验X光、超声等常见医学模态，泛化性有待验证。
2. **计算资源未报告**：未提及GPU型号、数量及训练耗时，难以判断实际部署成本。
3. **推理速度未评估**：虽然参数减少，但动态路由可能引入额外计算延迟，文中未分析推理时间。
4. **专家数量与Top-k选择缺乏敏感性分析**：仅固定专家数=4、top-k=2，未探索不同配置对性能的影响。
5. **与同类高效MoE方法对比不足**：仅对比标准MoE，未与DeRS等同样采用低秩分解的MoE方法直接比较（尽管引用了DeRS）。
6. **开放词汇泛化测试有限**：仅在四个标准数据集上评估，概念类别有限，未挑战未见过的复杂概念或长尾医学实体。

（完）
