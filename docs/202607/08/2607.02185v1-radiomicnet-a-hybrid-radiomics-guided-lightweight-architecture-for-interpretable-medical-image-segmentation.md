---
title: "RadiomicNet: A Hybrid Radiomics-Guided Lightweight Architecture for Interpretable Medical Image Segmentation"
title_zh: "RadiomicNet: 一种混合放射组学引导的轻量级可解释医学图像分割架构"
authors: Mohammad Amanour Rahman
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02185v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 放射组学引导的轻量级医学图像分割架构
tldr: 提出RadiomicNet，一种结合手工影像组学特征的轻量级混合架构，通过Radiomics Attention Gate（RAG）模块将GLCM和LBP特征融入MobileNetV2编码-解码器的跳跃连接注意力中，实现前向可解释性。使用影像组学一致性损失降低校准误差，在BUSI和Kvasir-SEG数据集上分别取得0.763和0.854的DSC，参数量仅3.27M，优于U-KAN。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02185v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative segmentation results on BUSI test set. Columns: input image, ground truth, prediction, RAG attention map, overlay. Green=TP, Red=FP, Blue=FN. High attention regions correlate with textural lesion boundaries, demonstrating clinically coherent focus.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2607.02185v1/fig-002.webp\", \"caption\": \"Fig. 1. RadiomicNet architecture. The Radiomics Attention Gate (RAG) fuses handcrafted texture features with deep skip connections at each decoder stage, providing ante-hoc interpretability.\", \"page\": 2, \"index\": 2, \"width\": 529, \"height\": 408}]"
motivation: 解决深度学习在医学图像分割中数学不可解释、参数量大和缺乏临床可解释性的问题。
method: 提出RadiomicNet，包含双流混合架构：利用GLCM和LBP影像组学特征通过Radiomics Attention Gate（RAG）调制跳跃连接注意力，并引入影像组学一致性损失对齐纹理复杂度与预测不确定性。
result: "在BUSI数据集上DSC达0.763±0.231，Kvasir-SEG达0.854±0.112，分别超过U-KAN 1.2%和1.8%，参数量仅为3.27M（比U-Net少9.5倍，比U-KAN少4.3倍），ECE从0.142降至0.118。"
conclusion: 紧凑、可解释的基于领域知识的模型能够以大幅降低的计算开销实现最先进的分割性能，且GLCM差异度、能量和LBP熵是主要影像组学线索，提供临床有意义的解释。
---

## 摘要
深度学习在医学图像分割中取得了显著性能，但仍存在关键局限性：数学难解性、大量参数需求以及缺乏临床可解释性。我们提出RadiomicNet，一种新颖的双流混合架构，通过将手工制作的放射组学特征直接集成到分割学习过程中，增强了标准深度学习。关键贡献是放射组学注意力门（RAG），它利用灰度共生矩阵（GLCM）和局部二进制模式（LBP）特征来调节基于轻量级MobileNetV2的编码器-解码器中的跳跃连接注意力，提供事前可解释性，无需事后近似。一种新颖的放射组学一致性损失进一步强制纹理复杂度与预测不确定性之间的对齐，将期望校准误差（ECE）从0.142降低到0.118。RadiomicNet在乳腺超声图像（BUSI）数据集上达到0.763±0.231的Dice相似系数（DSC），在Kvasir-SEG上达到0.854±0.112，分别比U-KAN高出1.2%和1.8%（p<0.05，Wilcoxon符号秩检验），仅需3.27M参数，是标准U-Net的9.5倍和U-KAN的4.3倍。基于梯度的特征重要性分析表明，GLCM不相似性（15.24%）、GLCM能量（14.56%）和LBP熵（11.49%）是主要的放射组学线索，为分割决策提供了临床有意义的解释。所提出的方法表明，基于领域知识的紧凑、可解释模型可以在大幅降低计算开销的情况下提供最先进的分割性能。

## Abstract
Deep learning has achieved remarkable performance in medical image segmentation, yet it suffers from critical limitations: mathematical intractability, substantial parameter requirements, and lack of clinical interpretability. We propose RadiomicNet, a novel two-stream hybrid architecture that enhances standard deep learning by integrating handcrafted radiomics features directly into the segmentation learning process. The key contribution is the Radiomics Attention Gate (RAG), which leverages Gray-Level Co-occurrence Matrix (GLCM) and Local Binary Pattern (LBP) features to modulate skip-connection attention in a lightweight MobileNetV2-based encoder-decoder, providing ante-hoc interpretability without post-hoc approximations. A novel Radiomics Consistency Loss further enforces alignment between texture complexity and prediction uncertainty, reducing Expected Calibration Error (ECE) from 0.142 to 0.118. RadiomicNet achieves a Dice Similarity Coefficient (DSC) of 0.763 +/- 0.231 on the Breast Ultrasound Images (BUSI) dataset and 0.854 +/- 0.112 on Kvasir-SEG, outperforming U-KAN by 1.2% and 1.8%, respectively (p < 0.05, Wilcoxon signed-rank test), with only 3.27M parameters, 9.5x fewer than standard U-Net and 4.3x fewer than U-KAN. Gradient-based feature importance analysis reveals that GLCM dissimilarity (15.24%), GLCM energy (14.56%), and LBP entropy (11.49%) are the dominant radiomics cues, providing clinically meaningful explanations for segmentation decisions. The proposed approach demonstrates that compact, interpretable models grounded in domain knowledge can deliver state-of-the-art segmentation performance with substantially reduced computational overhead.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：深度学习在医学图像分割中虽然性能优异，但存在三个根本性局限：
  - **数学不可解释性**：预测结果无法追溯至人类可理解的特征。
  - **参数量过大**：标准 U-Net 需 31M 参数，难以部署在边缘设备。
  - **数据饥饿**：在标注数据有限时性能显著下降。
- **研究动机**：响应 LBDL（Learning Beyond Deep Learning）倡议，寻求一种结合领域先验知识、同时保持轻量和可解释的范式。
- **整体含义**：提出一种混合架构，将基于手工设计的放射组学特征（如 GLCM、LBP）直接嵌入分割网络的注意力机制和损失函数中，在不牺牲性能的前提下实现**事前可解释性**（ante-hoc interpretability），并大幅降低计算开销。

### 2. 论文提出的方法论

- **核心思想**：构建双流混合架构（Two-Stream Hybrid Architecture）。
  - **流1**：基于 MobileNetV2 的编码器-解码器，处理 RGB 图像。
  - **流2**：轻量 MLP，处理 13 维手工放射组学特征。
  - 通过 **Radiomics Attention Gate (RAG)** 在每个解码器层级将两流融合，使网络能利用临床已知的纹理结构来引导注意力。
- **关键技术细节**：
  - **放射组学特征提取**：从每个输入图像中预先提取 13 个特征，分为三类：
    - GLCM 纹理统计（对比度、不相似性、同质性、能量、相关性）。
    - LBP 结构描述子（均值、标准差、熵、均匀性）。
    - 全局强度统计（均值、标准差、偏度、峰度）。
  - **Radiomics Attention Gate (RAG)**：
    - 通道注意力（来自放射组学嵌入）: α_c = σ(W_c2·ReLU(W_c1 r̂))。
    - 空间注意力（来自跳跃连接特征）: α_s = σ(Conv_7×7(F_l))。
    - 门控融合：F'_l = F_l ⊙ [γ·α_c·α_s + (1-γ)·α_s]，其中 γ 为可学习标量，反映放射组学注意力的相对贡献。
  - **复合损失函数**：
    - L = λ1 L_BCE + λ2 L_Dice + λ3 L_bnd + λ4 L_RC。
    - L_bnd：边界加权 BCE（膨胀因子 α=3）。
    - **Radiomics Consistency Loss (L_RC)**：强制对齐高纹理复杂度（高 GLCM 对比度）区域与高预测不确定性（高熵），即 L_RC = MSE( H̄(p), ẽf1 )，其中 ẽf1 为归一化的 GLCM 对比度。该损失嵌入临床先验：纹理复杂度与诊断不确定性相关。
- **算法流程**（文字说明）：
  1. 输入 RGB 图像，提取 13 维放射组学特征 r。
  2. r 经 MLP 映射为 32 维嵌入 r̂。
  3. MobileNetV2 编码器提取多尺度特征。
  4. 在每个解码器层，RAG 接收跳跃连接特征 F_l 和 r̂，计算 α_c、α_s 和 γ，生成调制后的特征图。
  5. 解码器逐步还原至原图分辨率，输出分割概率图。
  6. 训练时使用复合损失（含 L_RC）进行端到端优化。

### 3. 实验设计

- **数据集**：
  - **BUSI**（乳腺超声图像）：647 幅图像，437 良性/210 恶性，带像素级病变掩膜。划分：452 训练/97 验证/98 测试，按类别分层。
  - **Kvasir-SEG**（结肠镜息肉分割）：1000 幅图像，采用官方划分并保留测试集。
- **Benchmark**：分别采用标准 U-Net、UNet++、TransUNet、U-KAN 作为基线方法，比较 DSC、IoU、Precision、Recall、Specificity。
- **对比方法**：
  - U-Net（31M 参数）
  - UNet++（9M 参数）
  - TransUNet（93M 参数）
  - U-KAN（14M 参数）
  - RadiomicNet（3.27M 参数，提出方法）

### 4. 资源与算力

- **算力说明**：论文中仅提及“All experiments run on a single NVIDIA GPU”，**未说明具体 GPU 型号、显存、训练时长**。因此无法量化具体算力消耗，仅可知单卡训练。
- **其他资源**：采用 AdamW 优化器，batch size 8，梯度裁剪 norm 1.0，早停 patience 20。

### 5. 实验数量与充分性

- **实验组数**：
  - 两个数据集（BUSI、Kvasir-SEG）上独立测试。
  - 消融实验（见表 III）：
    - 组件消融：去除 RAG、去除 L_RC、去除 L_bnd。
    - RAG 流分解：仅空间注意力、仅通道注意力、全 RAG。
  - 校准分析（表 IV）：比较有无 L_RC 的 ECE。
  - 梯度特征重要性分析（图 2）。
  - 定性可视化（图 3）。
  - 模型复杂度对比（表 V）。
- **充分性与公平性**：
  - 对比基线均使用相同数据划分和增强协议，**统计显著性**采用 Wilcoxon 符号秩检验（p<0.05），保证结果可信。
  - 消融实验覆盖了关键组件，验证了 RAG 和 L_RC 的独立贡献。
  - **不足**：仅覆盖两个数据集（超声和结肠镜），未在 CT、MRI 等其他模态验证，泛化性有限；未进行多中心或跨域测试。

### 6. 论文的主要结论与发现

- 提出 RadiomicNet，仅 **3.27M 参数**（比 U-Net 少 9.5×，比 U-KAN 少 4.3×），在 BUSI 上 DSC 达 0.763±0.231，在 Kvasir-SEG 上 DSC 达 0.854±0.112，分别比 U-KAN 提升 1.2% 和 1.8%（p<0.05）。
- 引入 **Radiomics Consistency Loss** 将 ECE 从 0.142 降至 0.118（相对降幅 16.9%），改善校准性能。
- **可解释性**：通过 RAG 实现事前可解释性，梯度分析证实 GLCM 不相似性（15.24%）、GLCM 能量（14.56%）和 LBP 熵（11.49%）是主导特征，符合乳腺病变纹理异质性的临床知识。
- 推断速度达 **8.85 ms/幅**（约 113 fps），满足实时临床部署需求。

### 7. 优点

- **轻量高效**：总参数量仅 3.27M，模型大小 13.1 MB，适合边缘部署。
- **事前可解释性**：RAG 中的注意力权重直接由数学定义、临床验证的放射组学特征决定，无需事后近似。
- **创新损失函数**：L_RC 嵌入临床先验（纹理复杂度→不确定性），在提升 DSC 的同时改善概率校准。
- **跨域验证**：在乳腺超声和结肠镜两个差异较大的任务上均取得最优，表明泛化性。
- **统计严谨**：采用 Wilcoxon 符号秩检验验证显著性，对比基线覆盖 CNN、Transformer、KAN 三大范式。

### 8. 不足与局限

- **固定特征集**：仅使用 13 个预定义放射组学特征，无法自适应优化；未来可探索可微分特征选择。
- **仅 2D 处理**：当前仅支持 2D 切片，未扩展到 3D CT/MRI 体积，限制应用场景。
- **模态覆盖不足**：仅在 BUSI 和 Kvasir-SEG 上验证，未在 CT、MRI 等常见模态测试，泛化性有待进一步验证。
- **算力细节缺失**：未报告 GPU 型号、训练时长，影响实验可复现性。
- **单中心数据**：两个数据集均来自公开单一中心，存在潜在偏差风险；未进行多中心或外部验证。
- **可解释性边界**：RAG 的注意力机制虽能解释特征重要性，但决策逻辑仍依赖深度网络的非线性组合，完全的因果可解释性尚未实现。

（完）
