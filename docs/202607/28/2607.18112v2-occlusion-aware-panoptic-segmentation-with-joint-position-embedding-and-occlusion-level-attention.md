---
title: Occlusion-Aware Panoptic Segmentation with Joint Position Embedding and Occlusion-Level Attention
title_zh: 基于联合位置嵌入与遮挡级别注意力的遮挡感知全景分割
authors: "Wenbo Wei, Jun Wang, Shan Raza, Abhir Bhalerao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18112v2"
tags: ["query:vlmmed"]
score: 8.0
evidence: 新颖的遮挡感知注意力用于分割
tldr: 针对全景分割中遮挡问题，提出PEMOLA模块，通过训练遮挡分类器获得遮挡级别注意力，并结合位置嵌入调制，显著提升分割质量。在COCO-OLAC和Cityscapes-OLAC数据集上验证了有效性和跨数据集泛化能力，计算开销小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18112v2/fig-001.webp\", \"caption\": \"Fig. 3. Visualisation of position embedding before and after PEMOLA. From top to bottom, every two rows show low-, medium-, and high-occlusion samples, respectively. PE denotes the original position embedding. Mod denotes the modulation map generated by PEMOLA. PM denotes the modulated position embedding. 1st and last denote the first and last channels. PM-1st and PM-last reveal that the modulated position embedding incorporate occlusion priors, with each channel emphasizing distinct spatial representations.\", \"page\": 5, \"index\": 1, \"width\": 520, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2607.18112v2/fig-002.webp\", \"caption\": \"TABLE III QUANTITATIVE COMPARISONS ACROSS LOW, MID, AND HIGH OCCLUSION LEVELS ON THE COCO-OLAC DATASET [11].\", \"page\": 4, \"index\": 2, \"width\": 518, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2607.18112v2/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the proposed PEMOLA module. PEMOLA modulates position embedding using two occlusion priors: the occlusion-level attention extracted from the occlusion classifier highlights spatial regions affected by occlusion, while the learnable occlusion label embeddings encode global occlusion-level semantics to guide modulation. These two priors jointly adjust the positional representation in a residual manner, allowing the model to preserve spatial consistency while adaptively enhancing representations in occluded regions.\", \"page\": 3, \"index\": 3, \"width\": 965, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2607.18112v2/fig-004.webp\", \"caption\": \"Fig. 1. Examples of occlusion-induced segmentation failures from COCO val2017 [12] using Mask2Former [6]. The left column shows the original input images, and the right column shows the segmentation predictions. In the top row, the brown chair in the center is actually two chairs, but the rear one is heavily occluded and is mistakenly merged with the front one in the prediction. In the bottom row, the person to the right of the blue baseball bat is missed due to severe occlusion, leaving only a dark region visible.\", \"page\": 1, \"index\": 4, \"width\": 500, \"height\": 364}]"
motivation: 现有全景分割方法忽视遮挡建模，导致复杂场景性能受限。
method: 提出PEMOLA模块，利用遮挡分类器生成遮挡级别注意力作为空间指导，并通过可学习嵌入调制位置嵌入，将遮挡先验引入网络。
result: 在COCO-OLAC和Cityscapes-OLAC上，PEMOLA一致提升全景分割质量，且仅增加极少计算开销。
conclusion: 遮挡建模对全景分割至关重要，PEMOLA通过联合位置嵌入和遮挡级别注意力实现了鲁棒的分割性能。
---

## 摘要
在复杂场景中，由于遮挡的存在，全景分割仍然具有挑战性，然而现代方法往往忽略遮挡建模。在本文中，我们提出了位置嵌入调制与遮挡级别注意力（PEMOLA），一种新颖的遮挡感知模块，可以无缝集成到基于Transformer的全景分割中。为了获取遮挡线索，我们在COCO-OLAC数据集上训练了一个遮挡分类器。该分类器推导出遮挡级别注意力，作为空间指导，同时遮挡标签被编码为可学习的嵌入以生成通道权重。通过联合调制，PEMOLA优雅地将遮挡先验引入位置嵌入，从而改进遮挡建模。我们进一步为Cityscapes数据集标注了遮挡级别，称为Cityscapes遮挡标签用于所有计算机视觉任务（Cityscapes-OLAC），遵循与COCO-OLAC相同的标注协议，以评估PEMOLA的跨数据集泛化能力。在COCO-OLAC和Cityscapes-OLAC上的大量实验表明，PEMOLA在引入最小计算开销的同时，持续提高了全景分割质量。这些结果突显了遮挡建模的重要性，其中融入遮挡级别注意力有助于在遮挡下实现鲁棒的全景分割。代码和数据集可在https://github.com/wenbo-wei/PEMOLA获取。

## Abstract
Panoptic segmentation in complex scenes remains challenging because of occlusions, yet modern approaches often neglect occlusion modelling. In this paper, we propose Position Embedding Modulation with Occlusion Level Attention (PEMOLA), a novel occlusion-aware module that can be seamlessly integrated into transformer-based panoptic segmentation. To obtain occlusion cues, we train an occlusion classifier on the COCO-OLAC dataset. The classifier derives the occlusion-level attention, which serves as spatial guidance, while the occlusion labels are encoded into a learnable embedding to produce channel-wise weights. Through joint modulation, PEMOLA elegantly introduces the occlusion priors into the position embedding, thereby improving the occlusion modelling. We further annotate the Cityscapes dataset with occlusion levels, termed Cityscapes Occlusion Labels for All Computer Vision Tasks (Cityscapes-OLAC), following the same labelling protocol as COCO-OLAC, to evaluate the cross-dataset generalisation ability of PEMOLA. Extensive experiments on COCO-OLAC and Cityscapes-OLAC demonstrate that PEMOLA consistently improves panoptic segmentation quality while introducing minimal computational overhead. These results highlight the importance of occlusion modelling, where incorporating occlusion-level attention helps deliver robust panoptic segmentation under occlusion. Code and dataset are available at https://github.com/wenbo-wei/PEMOLA.