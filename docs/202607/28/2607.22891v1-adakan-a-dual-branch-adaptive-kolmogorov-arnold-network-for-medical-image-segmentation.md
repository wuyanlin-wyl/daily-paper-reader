---
title: "AdaKAN: A dual-branch adaptive Kolmogorov-Arnold network for medical image segmentation"
title_zh: AdaKAN：用于医学图像分割的双分支自适应Kolmogorov-Arnold网络
authors: "Dalia Alzu'bi, Deep Bhattacharyya, Ali Ayub, A. Ben Hamza"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22891v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于医学图像分割的新型自适应KAN
tldr: 针对医学图像分割中解剖结构复杂和成像模态差异大的挑战，本文提出AdaKAN，一种融合卷积与高效KAN块的双分支自适应Kolmogorov-Arnold网络。通过Bernstein多项式激活和通道细化设计，结合U型架构与跳跃连接，在多个医学图像数据集上取得最先进的分割精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22891v1/fig-001.webp\", \"caption\": \"Figure 3: Comparison of model efficiency and performance of AdaKAN with baselines in terms of DSC, FLOPs, and parameter count (in millions) on BUSI. The lower number of FLOPs and parameter count indicates better efficiency.\", \"page\": 7, \"index\": 1, \"width\": 517, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2607.22891v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison of our method and baselines on the BUSI, GlaS, and CVC datasets. The top-performing and second-best results are highlighted in bold and underlined, respectively.\", \"page\": 6, \"index\": 2, \"width\": 973, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2607.22891v1/fig-003.webp\", \"caption\": \"Figure 2: Visual comparison of segmentation heatmaps between the proposed AdaKAN and baselines on the BUSI dataset.\", \"page\": 6, \"index\": 3, \"width\": 876, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2607.22891v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of AdaKAN framework. The proposed model adopts a symmetric encoder-decoder architecture, comprising a convolution stage and an efficient KAN stage. The encoder downsamples the input image through three convolution blocks followed by two EffiKAN blocks, progressively reducing spatial resolution while expanding feature channels to capture hierarchical representations. The decoder mirrors this design, upsampling features via bilinear interpolation and integrating high-resolution details from the encoder via skip connections to produce the segmentation mask.\", \"page\": 3, \"index\": 4, \"width\": 881, \"height\": 765}]"
motivation: 现有卷积和Transformer分割模型难以同时捕获长程依赖与局部细节，且KAN的应用在医学图像分割中尚未充分探索。
method: 提出AdaKAN，采用双分支AdaptKAN模块：一支用Bernstein多项式KAN层进行全局平滑近似，另一支通过投影与自适应缩放进行通道细化，整体U型结构融合卷积与高效注意力。
result: 在多个医学图像数据集上，AdaKAN分割精度达到当前最优（state-of-the-art）。
conclusion: AdaKAN有效结合了KAN的全局逼近能力与卷积的局部特征提取，为医学图像分割提供了一种新的高性能方案。
---

## 摘要
医学图像分割是计算机辅助诊断中的一项基本任务，但由于解剖结构的复杂性和成像模态之间的差异，它仍然具有挑战性。在本文中，我们提出了AdaKAN，一种自适应Kolmogorov-Arnold网络（KAN），它协同地将卷积操作与一种新颖的高效KAN（EffiKAN）模块相结合，该模块由高效注意力机制和自适应KAN（AdaptKAN）模块组成。该模块采用双分支设计：一个分支使用带有Bernstein多项式激活的KAN层进行全局平滑和稳定的函数逼近，而另一个分支通过投影操作和自适应缩放执行通道级细化。AdaKAN采用U形架构，有效捕获长距离依赖和细粒度局部特征，克服了传统卷积和基于Transformer的分割模型的局限性。使用跳跃连接来在编码期间保留空间细节，并在解码期间促进准确重建。在不同医学成像数据集上进行的大量实验表明，AdaKAN在分割准确性方面达到了最先进的性能。

## Abstract
Medical image segmentation is a fundamental task in computer-aided diagnosis, yet it remains challenging due to the complexity of anatomical structures and the variability across imaging modalities. In this paper, we propose AdaKAN, an Adaptive Kolmogorov-Arnold Network (KAN) that synergistically integrates convolutional operations with a novel efficient KAN (EffiKAN) block, comprised of an efficient attention mechanism and an adaptive KAN (AdaptKAN) module. This module features a dual-branch design: one branch employs a KAN layer with Bernstein polynomial activations for globally smooth and stable function approximation, while the other branch performs channel-wise refinement through projection operations and adaptive scaling. AdaKAN adopts a U-shaped architecture that effectively captures both long-range dependencies and fine-grained local features, overcoming the limitations of conventional convolutional and Transformer-based segmentation models. Skip connections are employed to preserve spatial details during encoding and facilitate accurate reconstruction during decoding. Extensive experiments conducted on diverse medical imaging datasets demonstrate that AdaKAN achieves state-of-the-art performance in segmentation accuracy.

---

## 论文详细总结（自动生成）

# AdaKAN: 用于医学图像分割的双分支自适应Kolmogorov-Arnold网络 — 详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：医学图像分割面临解剖结构复杂、成像模态差异大的挑战。现有方法中，全卷积网络(FCN)因卷积的局部性难以捕获长距离空间依赖，导致边界模糊或不完整分割；Transformer模型虽能建模全局上下文，但计算复杂度为二次方；轻量级MLP方法虽高效但表达力有限。
- **背景**：Kolmogorov-Arnold网络(KAN)作为MLP的替代方案，将可学习参数从固定激活节点转移到边缘一元激活函数，提高了可解释性和非线性建模能力。但已有KAN变体（如U-KAN）基于B样条激活，全局建模能力有限。
- **整体含义**：作者提出AdaKAN，通过融合卷积、高效注意力和自适应KAN模块，在单一U形框架内同时实现局部精度、全局感知和计算效率，为医学图像分割提供新的平衡方案。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：采用对称的U形编码器-解码器结构，包含卷积阶段和EffiKAN阶段。关键组件是EffiKAN块，它由高效注意力(EA)和自适应KAN模块(AdaptKAN)组成。
- **关键技术细节**：
  - **卷积阶段**：三个连续的卷积块（Conv+BN+ReLU），逐步将空间分辨率降为H/2、H/4、H/8，通道数依次为C1=32、C2=64、C3=256。
  - **EffiKAN阶段**：两个EffiKAN块，每个块包含：
    - **Patch Embedding**：将卷积输出的特征图转化为token序列（n个patch，嵌入维度d）。
    - **LayerNorm**：稳定特征分布。
    - **高效注意力(EA)**：使用行/列softmax重排乘法顺序，将标准注意力的O(n²)复杂度降为O(n)。公式：EA(LN(Y)) = σ_row(Q)(σ_col(K)ᵀV)。
    - **残差连接**：Z = EA(LN(Y)) + Y。
    - **AdaptKAN模块**：双分支设计。
      - 左分支：KAN层 + LayerNorm，激活函数为Bernstein多项式（公式：˜φ(z)=˜w_b SiLU(z)+˜w_s Bern(z)，其中Bern(z)=Σ θ_r P_{r,R}(z)，P_{r,R}为r阶Bernstein基函数，R=3）。
      - 右分支：通道细化瓶颈——DownProject→ReLU→UpProject，输出乘以可学习缩放系数s（初始化为0.1），用于稳定训练并控制分支贡献。
      - 输出：AdaptKAN(Z) = KAN(LN(Z)) + s·˜Z，其中˜Z = ReLU(Z W_dn)W_up。
    - 最终EffiKAN块输出：EffiKAN(Y) = AdaptKAN(Z) + Z。
  - **解码器**：镜像结构，使用双线性上采样和跳跃连接恢复空间分辨率，最后通过1×1卷积+Softmax输出分割掩码。
- **公式简要说明**：所有关键操作已用文字描述，核心公式在文中明确给出（EA公式、Bernstein多项式、AdaptKAN输出等）。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：共5个，涵盖不同模态和任务。
  - BUSI（乳腺超声，二分类）
  - GlaS（结肠组织学腺体分割，二分类）
  - CVC（结肠息肉分割，二分类）
  - ISIC 2018（皮肤病变分割，二分类）
  - ACDC（心脏MRI多结构分割：右心室RV、心肌Myo、左心室LV，多分类）
- **基准与评估指标**：IoU（交并比）和Dice相似系数(DSC)。
- **对比方法**：Att-UNet、MedT、UNeXt、Rolling-Unet、U-KAN、PDS-UKAN、ResU-KAN、SET、U-Net V2、PVT-EMCAD-B2等，均为近年SOTA方法。

## 4. 资源与算力
- **硬件**：单块NVIDIA A4500 GPU（未说明数量，通常为1块）。
- **框架**：PyTorch。
- **训练设置**：
  - 批量大小：BUSI/CVC/ISIC=8，GlaS=6，ACDC=16。
  - 输入尺寸：BUSI等为256×256，GlaS为512×512。
  - 学习率：1e-4，余弦退火至1e-5。
  - 优化器：Adam。
  - 训练轮数：400个epoch。
  - 每次实验使用5个随机种子取均值±标准差。
- **参数与FLOPs**：AdaKAN模型参数量14.21M，FLOPs 8.12G（见表1）。作者未明确报告总训练时间。

## 5. 实验数量与充分性
- **实验组数**：
  - 定量评估：5个数据集（BUSI、GlaS、CVC、ISIC、ACDC）分别与7-8种方法对比，每个结果报告5次独立运行的均值和标准差。
  - 消融实验：在BUSI数据集上进行了4种消融配置（移除EA、移除Up-Projection、移除Down-Projection等），见表4。
  - 参数敏感性：在BUSI上测试了Bernstein多项式阶数R=2~5对性能的影响，见表5。
  - 可视化定性比较：4个BUSI样本的预测热力图对比（图2）。
  - 效率分析：以散点图（DSC vs FLOPs，气泡大小表示参数量）对比多种方法（图3）。
- **充分性评价**：实验覆盖了多个模态（超声、组织学、内镜、皮肤镜、心脏MRI），包括二分类和多分类任务，对比方法全面且为近年SOTA，消融和参数分析合理，结果报告了标准差，统计可信度高。实验设计较为充分、客观。

## 6. 论文的主要结论与发现
- AdaKAN在5个数据集上均优于所有对比方法，在BUSI上DSC达80.95%（较ResU-KAN提升1.03%），在GlaS上DSC达93.94%，在CVC上DSC达92.26%，在ISIC上DSC达91.71%，在ACDC上平均DSC达93.00%。
- 消融证实高效注意力和AdaptKAN模块均对性能有显著贡献，缺少任一组件均导致性能下降。
- Bernstein多项式阶数R=3为最优平衡点，兼顾表达力与稳定性。
- AdaKAN在参数量和FLOPs方面具有竞争力（14.21M参数、8.12G FLOPs），优于参数量更大的ResU-KAN（20.06M参数）。

## 7. 优点
- **方法创新**：首次将Bernstein多项式基的KAN层与高效注意力结合于医学图像分割，双分支AdaptKAN设计新颖，左分支提供全局平滑近似，右分支实现轻量通道细化，并通过可学习缩放系数稳定融合。
- **计算高效**：高效注意力将复杂度降至线性，整体模型参数量适中（14.21M），FLOPs仅8.12G，优于多数SOTA。
- **实验结果扎实**：涵盖5个不同数据集、多模态、二分类与多分类，与众多方法对比，统计报告完整。
- **消融与分析全面**：验证了各组件贡献，并探讨了Bernstein阶数的影响。

## 8. 不足与局限
- **实验覆盖局限**：未在更大的公开数据集（如Synapse、MSD）或无标注场景（半监督/弱监督）下验证，也未讨论模型对噪声或遮挡的鲁棒性。
- **偏差风险**：所有实验使用80/20训练-验证随机划分，未报告交叉验证或独立测试集上的性能；仅报告5次随机种子结果，未使用正式统计检验（如t检验）证明显著差异。
- **应用限制**：Bernstein多项式定义域为[0,1]，可能限制输入特征范围的泛化；模型在极低分辨率或极端长尾类别下的表现未知。
- **资源报告不足**：未提供完整的训练时间、GPU内存占用等细节，难以复现算力成本。
- **方法对比公平性存疑**：部分基线（如PDS-UKAN）未报告参数量和FLOPs，无法在同等计算开销下比较；AdaKAN的通道配置（C3=256, D1=320等）是否经过网格搜索优化未说明。

（完）
