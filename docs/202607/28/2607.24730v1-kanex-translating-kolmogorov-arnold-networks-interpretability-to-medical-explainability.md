---
title: "KANEx: Translating Kolmogorov-Arnold Networks' Interpretability to Medical Explainability"
title_zh: KANEx：将Kolmogorov-Arnold网络的可解释性转化为医学可解释性
authors: "Krithi Shailya, Ananya Lakshmi Ravi, Venkatanathan K. V., Sowmya S. Sundaram, Gokul S. Krishnan, Aditi Anand, Balaraman Ravindran"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24730v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 基于VLM的胸片分类器可解释性
tldr: 针对医学影像模型黑箱问题，提出KANEx框架，利用Kolmogorov-Arnold Networks（KANs）的符号可解释性增强视觉-语言模型（VLM）推理，并设计KAN-Map热力图生成方法。在MIMIC-CXR数据集上，KAN架构在保持语义相似度的同时，显著提升定位和推理质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24730v1/fig-001.webp\", \"caption\": \"Fig. 2: Ablation Studies for KAN-Map: Localization methods for Rational KAN (Grad-CAM, Attn-R, KAN-Map) with ground truth bounding boxes (top) and KAN-Map across VanillaKAN, GroupKAN, RationalKAN (bottom)\", \"page\": 8, \"index\": 1, \"width\": 688, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-002.webp\", \"caption\": \"Fig. 3: LExT bar plot comparing baseline, RN/ViT, and KAN variants; corresponding explanations reveal sub-par baseline explanation (top-right) vs. improved KAN-Map explanation (bottom-right).\", \"page\": 8, \"index\": 2, \"width\": 725, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-003.webp\", \"caption\": \"Table 2: Visual explainability comparison across RN50, ViT, and their KAN variants. Bold values indicate the best performance within each backbone family and explanation method.\", \"page\": 7, \"index\": 3, \"width\": 724, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-004.webp\", \"caption\": \"Table 1: Definitions of evaluation metrics used in the proposed fraemwork\", \"page\": 6, \"index\": 4, \"width\": 737, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-005.webp\", \"caption\": \"Fig. 1: Overview of the proposed pipeline KANEx : An input image x is processed by a KAN backbone f(·) to produce labels ŷ and heatmaps {hk(x)}Kk=1. These outputs are provided to a VLM which generates textual explanations.\", \"page\": 3, \"index\": 5, \"width\": 620, \"height\": 354}]"
motivation: 现有胸部X光分类器结合VLM生成的文本解释缺乏对底层视觉模型的透明度，难以赢得临床信任。
method: 提出KANEx框架，利用KANs的符号可解释性为VLM提供可解释上下文，并设计KAN-Map直接从KAN模型生成热力图而非梯度近似。
result: "在MIMIC-CXR数据集上，KAN架构在ResNet/ViT基线下提升语义相似度，显著提高热力图忠实度，视觉定位和下游推理质量提升10%。"
conclusion: 将语言解释和视觉归因建立在数学可解释单元上是迈向可信医学AI的必要步骤。
---

## 摘要
计算机视觉模型在医学应用中已变得非常有效，但其黑箱性质仍持续削弱临床医生的信任。在临床工作流中，胸部X光分类器越来越多地与视觉语言模型（VLM）结合，以生成自然语言解释。然而，这些系统增加了语言流畅性，却未解决视觉模型底层的不透明性。随着Kolmogorov-Arnold网络（KAN）的出现，其基于样条的分量提供了内在可解释的功能单元，我们探究是否可以利用这种架构透明度来产生更可信的文本解释。我们引入KANEx，这是首个利用KAN的符号透明度来支撑VLM推理的框架。这种可解释性也使得设计KAN-Map成为可能，这是一种直接从KAN模型而非梯度近似导出的新型热力图生成方法。我们将这些有依据的上下文输入下游VLM，以增强可解释性。在MIMIC-CXR数据集上进行基准测试，我们证明基于KAN的架构（以ResNet/ViT为基线）在提高语义相似度的同时，生成了显著更可信的显著性图。KAN架构将视觉定位和下游推理质量提升了10%。我们的发现表明，将语言解释和视觉归因建立在数学可解释单元上是迈向可信医学AI的必要步骤。

## Abstract
Computer vision models have become highly effective for medical applications, yet their black-box nature continues to undermine clinician trust. In clinical workflows, chest X-ray classifiers are increasingly paired with Vision-Language Models (VLMs) to generate natural-language explanations. However, these systems add linguistic fluency without addressing the underlying opacity of the visual model. With the emergence of Kolmogorov-Arnold Networks (KANs), whose spline-based components provide inherently interpretable functional units, we investigate whether this architectural transparency can be leveraged to produce more trustworthy textual explanations. We introduce KANEx, the first ever framework that leverages the symbolic transparency of KANs to ground VLM reasoning. This interpretability also made it possible to design KAN-Map, a novel heatmap generation method derived directly from KAN models rather than gradient approximations. We feed these grounded contexts into downstream VLMs for enhanced explainability. Benchmarked on the MIMIC-CXR dataset, we demonstrate that KAN-based architectures with ResNet/ViT baselines demonstrate improved semantic similarity while producing significantly more faithful saliency maps. KAN architectures improve visual localization and downstream reasoning quality by 10%. Our findings suggest that grounding linguistic explanations and visual attributions in mathematically interpretable units is a necessary step toward trustworthy medical AI.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文信息生成的结构化、深入、客观的中文总结。

## KANEx：将Kolmogorov-Arnold网络的可解释性转化为医学可解释性

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：虽然计算机视觉模型（特别是与视觉语言模型VLM结合的胸部X光分类器）在医学诊断辅助中表现出色，但其“黑箱”特性严重削弱了临床医生的信任。现有的VLM系统虽然能生成流畅的自然语言解释，但其底层视觉模型的不透明性并未得到解决，导致解释可能基于不可靠的“幻觉”或模糊推理。
- **研究动机**：Kolmogorov-Arnold网络（KAN）作为一种新兴的神经网络架构，其基于样条（spline）的组成部分具有内在的可解释性。本文旨在探索如何利用KAN这种架构上的透明度，来提升VLM生成的文本和视觉解释的可信度。
- **整体含义**：本文提出，将语言解释和视觉归因建立在数学上可解释的单元（如KAN的样条函数）之上，是构建值得信赖的医学AI的关键一步。这标志着从“事后解释”（如Grad-CAM）向“内在可解释性”支撑解释的范式转变。

### 2. 论文提出的方法论：核心思想、关键技术细节与流程

- **核心思想**：提出 KANEx 框架，一个统一的胸部X光诊断与解释流水线。该流水线利用KAN的内在可解释性，为下游的视觉语言模型（VLM）提供更有依据的输入，从而生成更忠实、更精确的视觉和文本解释。
- **关键技术细节与流程**：
    1.  **可解释的视觉模型**：将预训练的ResNet或ViT主干网络的最终MLP分类头替换为KAN层，形成混合架构。论文比较了三种KAN变体：
        - **VanillaKAN**: 使用标准的B-样条作为边缘函数。
        - **GroupKAN**: 对样条进行分组，以提高高维数据下的效率。
        - **RationalKAN**: 用有理函数替代样条，以增强表达力。
        - KAN的核心公式为：`x(l+1)_i = Φ_i( Σ_j ϕ(l)_i,j(x(l)_j) )`，其中 `ϕ_i,j` 是参数化的B-样条函数。
    2.  **KAN-Map热力图生成**：这是论文的核心创新之一。与依赖梯度（如Grad-CAM）不同，KAN-Map通过直接分析KAN样条函数的前向激活值来生成热力图。具体流程：
        - 将最终卷积特征图 `F ∈ R^(C×H×W)` 中的每个空间位置的特征向量 `F_x,y` 独立地送入训练好的KAN分类器。
        - 对于类别k，热力图在该位置的值为 `h_k(x,y) = ϕ2_k(ϕ1(F_x,y))`，其中 `ϕ1` 和 `ϕ2` 是KAN的样条层。
        - 这一过程无需反向传播，直接反映了每个空间位置对模型决策的贡献，理论上更忠实。
    3.  **引导VLM生成解释**：KANEx将原始X光图像、KAN-Map生成的热力图以及模型预测的诊断标签一同输入到一个冻结的VLM（实验中使用了LLaVA）。通过精心设计的提示，要求VLM以放射科医生的口吻生成包含“发现”和“解释”的结构化放射学报告。

### 3. 实验设计

- **数据集**：
    - **主数据集**：从MIMIC-CXR（胸部X光）数据库中取出一子集，并与MIMIC-IV的诊断代码和MIMIC-IV-Note的病程记录进行匹配，构建了一个包含约20k规模的自定义多模态数据集，用于训练和评估。每个样本包含X光图像、诊断标签、一个综合的“医生式”文本解释。
    - **评估子集**：使用MS-CXR数据集提供的边界框，对热力图的定位性能进行评估。
- **Benchmark**：论文主要在**视觉可解释性**和**解释质量**两个维度上进行评估。
- **对比方法**：
    - **视觉主干**：ResNet (RN50) 和 Vision Transformer (ViT) 作为基线，并与它们的KAN变体（RN50(KAN), ViT(rKAN)等）进行对比。
    - **热力图生成**：KAN-Map 与 **Grad-CAM** 和 **注意力展开（Attn-R, 仅用于ViT）** 进行比较。
    - **文本生成**：使用了LLaVA模型，但并未与其他文本生成方法进行对比。

### 4. 资源与算力

- 论文正文中**未明确说明**所使用的具体GPU型号、数量以及训练时长。

### 5. 实验数量与充分性

- **实验数量**：论文进行了一系列控制变量实验，总组数较多。
    - **主干与KAN变体组合**：对RN50和ViT两个主干，结合VanillaKAN、GroupKAN、RationalKAN三种变体，共6种模型配置。
    - **热力图方法比较**：对每种模型配置，使用了Grad-CAM、KAN-Map（以及ViT的Attn-R）三种方法生成热力图，并比较了IoU, Energy@10, Area@50, Faithfulness等指标。
    - **消融实验**：文本中明确提到了对KAN-Map的消融研究（Figure 2），展示了不同热力图方法和不同KAN变体生成的视觉热力图对比。
- **实验充分性与客观性**：
    - **优点**：实验设计较为公平。为了隔离KAN头部的影响，实验冻结了所有主干网络的权重，仅替换和训练最终的KAN分类头，这确保了性能差异完全来自KAN组件。使用了多种、多维度的评估指标（IoU, Faithfulness, LExT-C等），对解释的不同方面进行了评估。Friedman检验也确认了模型间IoU分布的显著差异。
    - **缺点**：文本生成的实验较弱。没有与其他专门的医学文本生成模型或不同的提示策略进行比较，这使得“KAN架构能改进文本解释质量”这一结论的泛化性有限。整个实验评估是在一个未与临床医生合作的自定义数据集上进行的。

### 6. 论文的主要结论与发现

1.  **KAN-Map优于梯度方法**：KAN-Map在定位精度（IoU提升约25%）和解释忠实度（Faithfulness提升>20%）上一致优于基于梯度的Grad-CAM和Attn-R方法，证明了利用KAN内在可解释性的有效性。
2.  **KAN架构提升整体性能**：使用KAN分类头替代传统MLP头后，模型在视觉定位（IoU）和下游推理质量（LExT分数提升约20%）上均有显著提升（~10%）。
3.  **不同KAN变体各有所长**：
    - **RationalKAN** 在热力图的定位准确性（IoU）上表现最好。
    - **GroupKAN** 在解释的忠实度和文本解释质量（LExT）上取得最佳成绩。
    - **VanillaKAN / GroupKAN** 生成的热力图更紧凑、更聚焦（Area@50更低，Energy@10更高）。
    - 这表明存在一个可解释性内部的权衡：最精确的空间定位不一定等同于最集中的解释模式。

### 7. 优点

1.  **首创性**：首次系统性地探索了KAN的内在可解释性如何被用来支撑并改进VLM的推理，提出了一个完整的、统一的KANEx流水线。
2.  **方法创新**：提出的KAN-Map热力图生成方法，彻底摆脱了对梯度近似和反向传播的依赖，直接从前向传播中获取信息，这既是计算上的高效，也是逻辑上的严谨，是模型驱动型解释的一个进步。
3.  **实验设计严谨**：通过冻结主干网络来隔离KAN头部的贡献，使对比实验非常公平和具有说服力。评估指标覆盖全面，从像素级定位到语义级忠实度，再到临床文本质量。
4.  **清晰的trade-off分析**：论文不回避不同KAN变体在解释清晰度和准确性上的权衡，提供了有洞察力的分析。

### 8. 不足与局限

1.  **文本生成评估不充分**：论文未与其他医学报告生成或基于Grad-CAM的VLM基线进行对比，削弱了“KAN架构改进文本解释”这一核心结论的可信度。文本质量评估仅依赖自动化的LExT指标，缺乏人工评估（如放射科医生评测）。
2.  **数据集的局限性**：所有实验均基于MIMIC-CXR一个数据库，且是自定义的一个子集。在更大规模、更多样化的数据集（如不同疾病分布、不同设备来源的X光）上的泛化性尚未验证。
3.  **缺乏临床验证**：论文在结论中提到了“计划用临床医生进行评估”，但在当前工作中并未实施。所有评估都是通过自动化的指标进行的，没有量化这些解释对临床医生的实际决策质量、效率或信任度的影响。
4.  **KAN混合架构的限制**：论文仅在分类器头部替换了KAN，而骨干网络仍然是“黑箱”的ResNet或ViT。这种“浅层KAN”的改进能多大程度上解决根本性的模型透明度问题，值得商榷。一个完全透明的KAN骨干网络或许更为理想。
5.  **资源与复现细节缺失**：未报告计算资源（GPU类型、数量、训练时间），这给其他研究者复现结果带来了困难。

（完）
