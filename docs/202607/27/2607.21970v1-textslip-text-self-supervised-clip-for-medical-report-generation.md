---
title: "TextSLIP: Text Self-Supervised CLIP for Medical Report Generation"
title_zh: "TextSLIP: 用于医学报告生成的文本自监督CLIP"
authors: "Haoyu Jiang, Ziping Cong"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21970v1"
tags: ["query:vlmmed"]
score: 9.0
evidence: 用于报告生成的医学视觉语言预训练
tldr: 针对自动生成放射学报告中CLIP模型细粒度语义监督不足的问题，本文提出TextSLIP框架，通过增加文本模态内自监督对比学习来提升文本嵌入判别性，从而为视觉编码器提供更细粒度的语言监督。在700万脑MRI图像-文本对上预训练后，报告生成指标一致优于CLIP基线，表明文本级对比学习是改进医学视觉-文本对齐的有效方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21970v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of TextSLIP\", \"page\": 3, \"index\": 1, \"width\": 708, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2607.21970v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on visual encoder initialization. Comparison of TextSLIP, CLIP (pretrained on the same 7M brain MRI dataset), and the original unpretrained ResNet-101 backbone from R2Gen [4]. The best results are in bold.\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2607.21970v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of different pretrained CLIP-style models. The best results are in bold.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 306}]"
motivation: 标准CLIP主要优化跨模态对齐，缺乏对文本嵌入空间的显式结构，限制了细粒度语义监督。
method: 在CLIP基础上引入文本模态内自监督对比学习，通过增强文本对的判别性来提供更细致的语言监督到视觉编码器。
result: 在7百万脑MRI图像-文本数据集上预训练，报告生成指标相比CLIP基线获得一致提升。
conclusion: 文本级对比学习有助于改善医学视觉-文本对齐，但仍需在更多医学领域进行广泛验证。
---

## 摘要
自动化放射学报告生成对于提高报告一致性和临床工作流程至关重要。尽管对比语言-图像预训练（CLIP）推动了医学视觉语言建模的发展，但现有的CLIP风格方法可能仍无法为复杂的报告生成提供足够细粒度的语义监督。标准CLIP主要优化跨模态对齐，而没有明确构建指导视觉表示学习的文本嵌入空间。为了解决这一局限性，我们提出了TextSLIP，一种通用的医学视觉语言预训练框架，它通过模态内文本对比学习增强CLIP。通过自监督增强文本对提高文本嵌入的区分性，TextSLIP旨在为视觉编码器提供更细粒度的语言监督。作为初步验证，我们在一个包含700万脑部MRI图像-文本对的精选数据集上预训练TextSLIP，并在报告生成架构中微调预训练的视觉编码器。在与CLIP风格基线的受控比较中，TextSLIP在报告生成指标上表现出一致的改进。消融研究进一步表明，文本侧的自监督对观察到的增益有所贡献。这些结果表明，文本级对比学习是改进医学视觉-文本对齐的一个有前景的方向，而在更多医学领域进行更广泛的验证仍然是重要的下一步。

## Abstract
Automating radiology report generation is important for improving reporting consistency and clinical workflows . While Contrastive Language--Image Pretraining (CLIP) has advanced medical vision language modeling, existing CLIP-style approaches may still provide insufficient fine-grained semantic supervision for complex report generation. Standard CLIP primarily optimizes cross-modal alignment, without explicitly structuring the textual embedding space that guides visual representation learning. To address this limitation, we propose TextSLIP, a general medical vision-language pretraining framework that augments CLIP with intra-modal text contrastive learning. By improving textual embedding discriminability through self-supervised augmented text pairs, TextSLIP is designed to provide finer-grained linguistic supervision to the visual encoder. As an initial validation, we pretrain TextSLIP on a curated dataset of 7 million brain MRI image-text pairs and fine-tune the pretrained visual encoder within a report generation architecture. In controlled comparisons with CLIP-style baselines, TextSLIP shows consistent improvements on report generation metrics. Ablation studies further suggest that text-side self-supervision contributes to the observed gains. These results indicate that text-level contrastive learning is a promising direction for improving medical visual-textual alignment, while broader validation across additional medical domains remains an important next step.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有基于CLIP的医学视觉-语言预训练方法主要优化跨模态对齐，未能显式构建文本嵌入空间的判别结构，导致视觉编码器从文本侧获得的语义监督不够细粒度，限制了医学报告生成的质量。
- **研究动机**：自动化放射学报告生成可减轻放射科医生负担并提高报告一致性。CLIP虽在医学图像分类中表现优异，但在生成复杂文本描述的报告任务中，文本嵌入缺乏精细区分，制约了视觉表示学习。
- **整体含义**：通过引入文本模态内的自监督对比学习，增强文本嵌入的判别性，从而提供更细粒度的语言监督给视觉编码器，以提升报告生成性能。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：在CLIP框架基础上，增加文本自监督对比学习（采用ESimCSE），形成“双对比”策略：跨模态图像-文本对比 + 文本模态内对比。
- **关键技术细节**：
  - **视觉编码器**：ViT-B-16-quickgelu
  - **文本编码器**：BioMed-BERT
  - **动量文本编码器**：参数由文本编码器指数移动平均更新（α=0.99），用于生成负样本嵌入并存入FIFO队列。
  - **文本增强**：采用单词重复（Word Repetition）策略，对输入文本随机重复部分单词构造正样本对。
  - **损失函数**：
    - CLIP损失（双向InfoNCE）：
      - 图像→文本损失 L_I→T
      - 文本→图像损失 L_T→I
      - L_CLIP = (L_I→T + L_T→I)/2
    - ESimCSE损失 L_ESimCSE：基于正样本（增强文本）与负样本（队列中的原始文本嵌入）的InfoNCE。
    - 总损失：L = L_CLIP + λ L_ESimCSE（λ为权重超参数）
- **算法流程**（文字说明）：
  1. 对每个batch，图像通过视觉编码器，原始文本通过文本编码器，获得对齐嵌入。
  2. 对原始文本应用单词重复增强，得到正样本，通过同一文本编码器获得正嵌入。
  3. 原始文本同时通过动量文本编码器，获得负样本嵌入，并维护一个FIFO队列。
  4. 计算CLIP损失和ESimCSE损失，联合优化。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - **预训练**：从MedTrinity-25M中提取约700万脑MRI图像-文本对，作为预训练语料。
  - **下游微调与测试**：BRATS2023-GLI（1,251例多中心术前胶质瘤MRI，四个模态：T1n, T1c, T2w, T2f）。将3D体积沿三个轴提取2D切片，并用AutoRG Brain + GPT-4生成结构化临床报告。按7:1:2划分训练/验证/测试。
- **基准与对比方法**：
  - 报告生成框架：R2Gen，将其视觉提取器替换为预训练编码器。
  - 对比的CLIP风格模型：UniMed-CLIP、BioMed-CLIP（均为开源医学视觉-语言模型）。
  - 消融对照：
    - 同一7M数据上训练的纯CLIP
    - 原始R2Gen中未预训练的ResNet-101
- **评估指标**：BLEU-1~4（n-gram精度）、ROUGE-L（最长公共子序列，侧重召回）。

## 4. 资源与算力
- **预训练阶段**：单节点6块A100 80GB GPU，训练20个epoch，学习率4e-5，预热5000步，有效batch size 1536，总训练时间约80小时。
- **微调阶段**：单块A100 80GB GPU，训练30个epoch，采用R2Gen默认配置。

## 5. 实验数量与充分性
- **实验数量**：
  - 主对比实验（Table 1）：在4个MRI模态（T1n, T1c, T2w, T2f）上比较TextSLIP与UniMed-CLIP、BioMed-CLIP。
  - 消融实验（Table 2）：在同一数据集上对比TextSLIP、纯CLIP、未预训练ResNet-101。
- **充分性评估**：
  - **优点**：控制变量严格（预训练数据、微调框架均一致），在多个模态上验证，消融实验有效分离了文本自监督的贡献。
  - **不足**：仅在一个数据集（BRATS2023-GLI）和一个任务（脑MRI报告生成）上评估；仅使用自动指标（BLEU/ROUGE），缺乏临床专家评估或更医学相关的指标（如Chexbert、RadGraph）；未在更广泛的医学领域（如胸片、CT）验证。因此实验覆盖范围较窄，公平性（对比模型使用不同预训练数据）在Table 1中可能不完全对等（UniMed-CLIP和BioMed-CLIP预训练数据不同），但Table 2在同一数据上训练纯CLIP，提供了更公平的消融对比。

## 6. 论文的主要结论与发现
- TextSLIP在所有四个MRI模态上，BLEU-1至BLEU-4及ROUGE-L均优于UniMed-CLIP和BioMed-CLIP（BLEU-1提升约1-2%）。
- 与同一数据上训练的纯CLIP相比，TextSLIP在多数模态上BLEU-4提升约3%，证实文本自监督目标带来了实质增益。
- 文本侧自监督对比学习能有效提高文本嵌入判别性，从而为视觉编码器提供更细粒度的语义监督，改善报告生成质量。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：在CLIP中引入文本模态内自监督对比（ESimCSE），构思简洁，与跨模态对比互补，可即插即用。
- **控制实验严谨**：在消融实验中，纯CLIP与TextSLIP使用完全相同的预训练数据和视觉架构，仅损失函数不同，从而直接归因于文本自监督的效果。
- **大规模预训练**：使用700万脑MRI图像-文本对，数据量充足，确保预训练有效性。
- **公开代码**：便于复现和后续研究。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅验证了脑MRI单一模态，未在胸部X光、CT等其他医学图像上测试，通用性存疑。
  - 下游数据集BRATS2023-GLI较小（约1251例），且报告由自动化流程生成，可能与真实临床报告存在差异。
- **评估指标单一**：仅使用基于n-gram的BLEU和ROUGE-L，缺乏临床语义指标（如F1 Chexpert、RadGraph）或人工评估，无法衡量生成内容的医学准确性和安全性。
- **文本增强策略简单**：仅使用单词重复，可能未充分利用医学文本中的专业结构（如实体、否定、不确定性），未来可探索医学语义感知的增强。
- **资源消耗高**：预训练需80小时6×A100，对算力要求较高。
- **未与最新医学报告生成SOTA方法（如基于Transformer的专用模型）系统比较**：仅比较了预训练编码器，未对比完整的报告生成方法（如R2Gen本身有不同变体）。

（完）
