---
title: "FabriVLA: A Lightweight Vision-Language-Action Model for Precise Multi-Task Manipulation"
title_zh: "FabriVLA: 一种用于精确多任务操作的轻量级视觉-语言-动作模型"
authors: "Shiyuan Yang, Borong Zhang, Jizheng Zhang, Zhijia Tao, Junfei Guo, Donglai Ran, Xu Bian, Qingbiao Li"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08575v2"
tags: ["query:vl"]
score: 9.0
evidence: FabriVLA使用InternVL3.5视觉语言骨干
tldr: "FabriVLA是一种轻量级视觉-语言-动作模型，用于精确多任务操作。它结合InternVL3.5视觉语言骨干与flow-matching动作头，通过门控自注意力和浅层VLM融合增强空间上下文，采用单阶段联合优化。在Meta-World MT50基准上，平均成功率达90.0%，表明1B参数的紧凑VLA即可实现强性能，无需数十亿参数。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08575v2/fig-001.webp\", \"caption\": \"Figure 4: Meta-World MT50 success rate versus training step. Colored lines are the four difficulty tiers; the solid black and dashed gray lines are the tier-average and overall episodelevel success rates. The 93k checkpoint corresponds to the selected result in Table 1.\", \"page\": 6, \"index\": 1, \"width\": 469, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2607.08575v2/fig-002.webp\", \"caption\": \"Figure 2: Overview of FabriVLA. RGB observations and the task instruction are encoded by an InternVL3.5-1B vision-language backbone (14 retained layers) into fused context tokens. A shallow VLM layer (layer 6) is additionally extracted and fused with the deep features via a learned linear projection to enrich spatial detail. A flow-matching action head composed of a stack of L=8 gated cross-attention blocks denoises an action chunk conditioned on these tokens, the robot state, and the flow timestep. The entire model is trained jointly from a pretrained VLM and randomly initialized action head.\", \"page\": 2, \"index\": 2, \"width\": 968, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2607.08575v2/fig-003.webp\", \"caption\": \"Figure 3: Design space of optional modules in the FabriVLA action head. All three variants share a common base head—action encoder → L=8 cross-attention blocks → LayerNorm → sequence-pooled MLP head → velocity—and each adds a single zero-initialized gated residual module, so at initialization (gate = 0) every variant reduces exactly to the base head; they differ only in where temporal or per-step information is injected. (a) Gated self-attention mixes the 50 action tokens inside every block before cross-attention (the learnable gate g of Section 2). (b) A token residual head adds a per-step linear map R1024→R24 in parallel with the pooled MLP head. (c) A depthwise temporal convolution (k=3) smooths adjacent steps along the horizon after the block stack. The released FabriVLA enables variant (a) only.\", \"page\": 3, \"index\": 3, \"width\": 964, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2607.08575v2/fig-004.webp\", \"caption\": \"Table 1: Comparison on Meta-World MT50 against recent VLA models. “Params” is the parameter count and “Robo-Pre.” indicates robotic data pretraining or the reported pretraining recipe. Bold and underline denote the best and second best per column; for “Params” they denote the smallest and second smallest. Easy/Medium/Hard/Very Hard are per tier success rates and Tier Avg. is their mean. Baseline numbers are extracted from the corresponding papers.\", \"page\": 5, \"index\": 4, \"width\": 918, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2607.08575v2/fig-005.webp\", \"caption\": \"Table 2: MT50 diagnostic results by task grouping. Manipulation demand buckets form a mutually exclusive partition of all 50 tasks; precision oriented subsets are non-exclusive diagnostics. Scores are means of per-task success rates with 10 episodes per task.\", \"page\": 5, \"index\": 5, \"width\": 968, \"height\": 275}]"
motivation: 为实现精确多任务操作，需要轻量级且高效的VLA模型。
method: 结合InternVL3.5视觉语言骨干与flow-matching动作头，采用门控自注意力和浅层VLM融合，单阶段联合优化。
result: "在Meta-World MT50基准上，tier平均成功率达90.0%。"
conclusion: 1B规模的紧凑VLA无需数十亿参数即可在复杂多任务中达到高成功率。
---

## 摘要
我们提出了FabriVLA，一种用于精确多任务操作的轻量级视觉-语言-动作模型。FabriVLA将InternVL3.5视觉-语言骨干网络与一个流匹配动作头相结合，该动作头具有跨动作令牌的门控自注意力机制和浅层VLM层融合以增强空间上下文。该模型通过单阶段联合优化进行训练，基于预训练的VLM和随机初始化的动作头。在涵盖50个不同操作任务的Meta-World MT50基准测试中，FabriVLA达到了90.0%的分层平均成功率，表明基于1B规模的VLM构建的紧凑型VLA可以在不依赖数十亿参数VLA骨干网络的情况下实现强劲性能。

## Abstract
We present FabriVLA, a lightweight Vision-Language-Action model for Precise Multi-Task Manipulation. FabriVLA combines an InternVL3.5 vision-language backbone with a flow-matching action head featuring gated self-attention across action tokens and shallow VLM layer fusion for enriched spatial context. The model is trained via single stage joint optimization from a pretrained VLM and randomly initialized action head. On the Meta-World MT50 benchmark spanning 50 diverse manipulation tasks, FabriVLA achieves a tier-average success rate of 90.0%, demonstrating that a compact VLA built on a 1B scale VLM can achieve strong performance without relying on multi billion parameter VLA backbones.

---

## 论文详细总结（自动生成）

# FabriVLA 论文详细中文总结

## 1. 核心问题与整体含义

**研究动机**：视觉-语言-动作（VLA）模型在通用机器人操作领域展现潜力，但现有大规模VLA（数十亿参数）计算成本高、推理延迟大，难以满足实时机器人控制需求。因此，需要开发轻量级VLA架构，在保持高性能的同时提高效率。

**研究意义**：本文证明，基于 **1B 规模** 的视觉-语言模型构建的紧凑型VLA，无需依赖数十亿参数的VLA骨干网络，即可在复杂多任务操作（Meta-World MT50基准）上达到 **90.0%的分层平均成功率**，为低资源场景下的实时机器人控制提供了可行方案。

## 2. 方法论

### 核心思想
将轻量级视觉-语言骨干（InternVL3.5-1B）与流匹配动作头结合，通过门控自注意力机制建模动作序列的时序依赖，并利用浅层VLM层融合增强空间细节信息。模型采用 **单阶段联合优化**（同时更新VLM骨干和随机初始化的动作头）。

### 关键技术细节
- **视觉-语言骨干**：InternVL3.5-1B（保留14层transformer层），输入图像448×448，任务文本指令与视觉令牌拼接。
- **状态与动作编码**：
  - 机器人状态（24维：末端位姿、夹爪状态、物体位置）通过两层MLP编码为状态令牌（1024维）。
  - 含噪动作（50步×24维）通过“多体动作编码器”投影为动作令牌序列（50×1024），并添加位置编码。
- **动作头模块**：
  - **门控自注意力**：每个transformer块中，动作令牌先经过自注意力，再通过可学习门控参数 *g*（初始化为0）抑制或增强自注意力。训练中门控逐渐打开，模型逐步学习步骤间依赖。
  - **跨注意力**：动作令牌与VLM上下文令牌（状态令牌 + VLM输出的多模态令牌）进行交叉注意力。
  - **时间条件前馈**：加入流匹配时间步 *t* 的正弦编码，通过前馈网络（扩展至4096维，GELU激活，投影回1024维）。
  - **浅层VLM层融合**：将VLM第6层（浅层、富含空间细节）与第14层（深层、语义）的特征拼接，再通过可学习线性投影（2048→1024）融合，初始化为单位矩阵，保证初始化时与纯深层基线一致。此举仅增加约2.1M参数。
- **流匹配训练**：
  - 线性插值：噪声 *ϵ*（均匀分布[-1,1]）与真值动作 *a* 插值：xₜ = (1-t)·ϵ + t·a。
  - 训练目标：预测速度场 vₜ = a - ϵ，通过期望平方误差损失优化。
  - 时间 *t* 从 Beta(2,2) 分布采样，集中在中间区间（t≈0.5），使训练更有效。
  - 推理：从纯噪声开始，欧拉积分50步得到动作。

## 3. 实验设计

- **数据集与基准**：Meta-World MT50，共50个多样化操作任务，分为四个难度层级（easy、medium、hard、very hard）。使用公开的Evo-1数据集（每任务50条轨迹，共2500条轨迹）。
- **对比方法**：TinyVLA、π0、SmolVLA、RoboTron-Mani、Evo-1、Evo-Depth、LA4VLA。其中Evo-1、Evo-Depth、LA4VLA使用了相同的Meta-World数据集。
- **评估协议**：
  - 每任务10个episode，随机初始化（种子4042），400步内成功记为成功。
  - 报告 **分层平均成功率**（四层难度的均值）和 **总episode级成功率**（500个episode中成功比例）。
  - 另外按操作需求分桶和精度导向子集进行诊断分析。

## 4. 资源与算力

- **硬件**：5块 NVIDIA RTX PRO 6000 GPU。
- **训练配置**：
  - 训练步数：100,000步（最终选择93,000步的checkpoint）。
  - Batch size：40（跨5GPU）。
  - 学习率：2.0×10⁻⁵，线性warmup 1,000步后余弦衰减。
  - 精度：BF16混合精度，采用DeepSpeed ZeRO-2 + FP32主权重（确保VLM骨干梯度更新有效）。
  - 优化器：AdamW（权重衰减1e-4，梯度裁剪1.0）。
- **训练时长**：未明确给出总时间。

## 5. 实验数量与充分性

- **主要实验**：
  1. **MT50主结果**（表1）：与7种方法对比，报告四层成功率及分层平均。
  2. **任务需求诊断**（表2）：按操作需求分7个桶（互斥）和精度导向子集（非互斥），共19个分组。
  3. **训练动态分析**（图4）：不同训练步数（20k~100k）的成功率曲线。
  4. **消融实验一**（图5）：浅层VLM层融合（layer 6 vs 无）对比，使用完整训练设置。
  5. **消融实验二**（图6）：三种可选动作头模块的消融（门控自注意力SA、令牌残差头TR、时序卷积TC），使用冻结VLM、50k步简略设置。
- **充分性**：
  - 主实验覆盖全部50个任务，每任务10 episode，共500 episode，随机种子固定，结果可重现。
  - 消融实验对比了各组件的贡献，但受限于计算资源，动作头消融采用冻结VLM和较短步数，绝对性能较低，但相对比较有效。
  - 任务需求诊断提供了细致分析，找出模型强弱项。
  - 未进行交叉验证或多随机种子重复（每个任务仅10 episode），但Meta-World标准评估通常采用此类协议。
  - 对比方法引用自各自论文，未在完全相同设置下复现，可能存在实现细节差异，但已尽量控制。

## 6. 主要结论与发现

- FabriVLA在Meta-World MT50上达到 **90.0%分层平均成功率** 和 **92.0%总episode级成功率**，超越所有对比的轻量级VLA（包括Evo-1 80.6%、LA4VLA 87.5%等）。
- **门控自注意力** 是性能提升的关键组件（图6中提高9.2个百分点分层平均），能够有效建模50步动作序列的跨步依赖。
- **浅层VLM层融合** 显著提升性能（图5中分层平均提高7.1个百分点），尤其在需要精细空间定位的任务中效果明显。
- 单阶段联合训练（VLM骨干不冻结）对性能至关重要，浅层融合初始化保持与纯深层基线一致，保证了训练稳定性。
- 模型在“平面滑动/接触驱动/插入装配”类任务上表现突出（>93%），但在“工具操作/粗放抓取运输”类任务上较弱（83.3%~86%），说明长距离运输和时序工具使用仍是挑战。

## 7. 优点

- **轻量高效**：仅0.89B参数，推理延迟低，适合实时机器人控制。
- **创新设计**：
  - 门控自注意力平滑引入时序依赖，避免训练不稳定。
  - 浅层VLM融合采用初始化与深层对齐的策略，使附加信息可被有效学习而不影响初始行为。
  - 单阶段联合训练简化流程，无需多阶段预训练。
- **实验全面**：在标准基准上对比多种方法，并进行了详细的任务诊断和消融，验证了各设计选择。
- **开源友好**：数据集和骨干模型均为公开资源，有利于复现。

## 8. 不足与局限

- **实验范围有限**：仅在仿真环境Meta-World MT50上评估，未涉及真实机器人或更多样化的仿真环境（如Libero、CALVIN等），泛化性有待验证。
- **单次评估**：每任务仅10 episode，可能受随机性影响；未报告置信区间或多次重复。
- **消融实验设置不一致**：动作头模块消融采用冻结VLM和50k步训练，而主模型采用完整训练；冻结VLM可能限制了模块的潜力，相对比较结果可能未反映真实表现。
- **训练稳定性问题**：论文提到100k步checkpoint性能下降（93k最优），存在晚期过拟合或训练退化风险，未讨论解决策略。
- **缺失部分复现信息**：训练总时长未给出；对比方法的评估环境（如种子、episode数量）是否完全对齐未明确。
- **应用限制**：轻量模型在未见任务上的泛化能力未知；依赖特定VLM骨干，更换骨干可能需要重新调参；动作空间仅24维，对更高维或多模态动作（如力控制）未涉及。
- **未见任务零样本评估**：未测试模型在未见任务或物体上的零样本表现，当前结果仅覆盖训练集包含的任务。

（完）
