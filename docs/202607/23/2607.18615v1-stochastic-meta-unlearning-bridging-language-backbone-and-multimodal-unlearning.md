---
title: "Stochastic Meta-Unlearning: Bridging Language Backbone and Multimodal Unlearning"
title_zh: 随机元遗忘：连接语言主干与多模态遗忘
authors: "Zijie Liu, Jinhao Duan, Gaowen Liu, Sijia Liu, Tianlong Chen"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18615v1"
tags: ["query:vl"]
score: 9.0
evidence: 面向视觉语言模型的随机元遗忘
tldr: 针对视觉-语言模型（VLM）中的机器遗忘问题，发现仅对语言主干进行文本遗忘不足以阻止多模态恢复，为此提出随机元遗忘（SMU）框架。SMU采用双层优化，内环在文本数据上对语言主干进行遗忘，外环利用VLM反馈评估遗忘效果，从而学习对多模态遗忘鲁棒的初始化。实验表明SMU在遗忘效果和模型保留能力上均优于基线，且具有迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18615v1/fig-001.webp\", \"caption\": \"Figure 1: Text-side unlearning alone does not guarantee multimodal forgetting. Across Qwen and InternVL on Covid and Hateful Memes, LM-side unlearning can reduce Forget Text accuracy, but the target often remains recoverable under image-conditioned VLM queries.\", \"page\": 2, \"index\": 1, \"width\": 516, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2607.18615v1/fig-002.webp\", \"caption\": \"Figure 4: SMU transfers to new forgetting targets. When the meta-test forget target differs from the meta-training target, SMU usually preserves low Forget accuracy while maintaining Retain and Test utility across models and datasets.\", \"page\": 7, \"index\": 2, \"width\": 491, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2607.18615v1/fig-003.webp\", \"caption\": \"Table 1: Unlearning performance on Hateful Memes and Covid Memes with VLM baselines. Results are accuracy (%). Text→ Text evaluates text-only forgetting, and Text→Multi evaluates forgetting after VLM recomposition. Lower Forget is better (↓), and higher Retain/Test is better (↑). SMU achieves the strongest overall forget-retain trade-off across models and datasets.\", \"page\": 7, \"index\": 3, \"width\": 822, \"height\": 270}]"
motivation: 单模态语言遗忘不足以阻止VLM中多模态信息恢复，文本反馈不可靠，需要VLM级反馈指导遗忘。
method: 提出随机元遗忘（SMU），一种双层优化框架：内环对语言主干进行文本级遗忘，外环通过重组VLM评估遗忘质量并更新初始化。
result: SMU在遗忘准确率上降低10.52点，保留准确率提升20.10点，测试准确率提升17.01点，并具备对新目标的迁移能力。
conclusion: VLM级反馈能使语言主干遗忘更可靠且更具迁移性，为多模态遗忘提供了有效方案。
---

## 摘要
机器遗忘在视觉-语言模型（VLM）中仍未被充分探索。与语言模型不同，VLM将语言主干与视觉组件相结合，这使得遗忘更加复杂。当从单模态遗忘转向VLM遗忘时，出现了一个令人惊讶的现象：由独立语言主干遗忘的目标，当图像信息提供给完整的VLM时，仍然可以被恢复。这表明仅文本反馈不足以实现可靠的VLM遗忘。基于这一观察，我们提出了随机元遗忘（SMU），这是一个双层框架，利用VLM级反馈来学习一个准备好遗忘的初始化。在内循环中，SMU使用文本数据对语言主干应用少量遗忘步骤。在外循环中，SMU将更新后的主干与冻结的VLM重新组合，并在VLM级别评估遗忘和效用。这种设计使得遗忘更新能够感知最终的多模态行为，同时仍将更新限制在语言主干上。在两个VLM、两个多模态模因数据集以及三个基线模型上的实验表明，SMU实现了最佳的遗忘-保留权衡。与每个指标的最强基线相比，SMU平均降低了10.52个遗忘准确率点，并分别提高了20.10和17.01个保留准确率和测试准确率点。更重要的是，SMU还能迁移到新的遗忘目标和不同的元测试遗忘方法。这些结果表明，VLM级反馈可以使语言主干的遗忘更加可靠，并且对VLM更具迁移性。

## Abstract
Machine unlearning for vision-language models (VLMs) remains underexplored. Unlike language models, VLMs combine a language backbone with visual components, which makes unlearning more complex. There is a surprising phenomenon when moving from single-modality unlearning to VLM unlearning: a target forgotten by the standalone language backbone can still be recovered when image information is given to the full VLM. This shows that text-only feedback is not enough for reliable VLM unlearning. Motivated by this observation, we propose Stochastic Meta-Unlearning (SMU), a bilevel framework that uses VLM-level feedback to learn an unlearning-ready initialization. In the inner loop, SMU applies a few unlearning steps to the language backbone using text data. In the outer loop, SMU recomposes the updated backbone with the frozen VLM and evaluates forgetting and utility at the VLM level. This design makes the unlearning update aware of the final multimodal behavior, while still keeping the update local to the language backbone. Experiments on two VLMs, two multimodal meme datasets, and three baselines show that SMU achieves the best overall forget-retain trade-off. Compared with the strongest baseline for each metric, SMU reduces average Forget accuracy by 10.52 points and improves average Retain and Test accuracy by 20.10 and 17.01 points, respectively. More importantly, SMU also transfers to new forgetting targets and to different meta-test unlearning methods. These results suggest that VLM-level feedback can make language-backbone unlearning more reliable and more transferable for VLMs.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：视觉-语言模型（VLM）的机器遗忘问题尚未被充分探索。VLM由语言主干、视觉编码器和投影模块组成，结构复杂。现有单模态语言遗忘方法在VLM中失效：即便语言主干成功忘记了某个概念，一旦结合图像信息进行多模态查询，该概念依然能被恢复（图1）。这表明仅依靠文本反馈无法保证VLM级别的可靠遗忘。
- **整体目标**：提出一种框架，使得语言主干的遗忘操作在重新组合成完整VLM后仍然有效，同时保持对其他任务的效用。

## 2. 论文提出的方法论

- **核心思想**：采用双层元优化（bilevel meta-optimization）框架——**Stochastic Meta-Unlearning (SMU)**。
- **关键技术细节**：
    - **内循环（inner loop）**：仅对语言主干执行K步文本侧遗忘（使用GradDiff、NPO、RMU等操作），更新主干参数。
    - **外循环（outer loop）**：将更新后的语言主干与冻结的VLM组件（视觉编码器、投影模块）重组，计算多模态遗忘损失和保留损失，通过梯度回传更新语言主干的**初始参数**。
    - **随机任务采样**：在元训练阶段，随机采样不同的遗忘目标、内循环操作符和查询条件，提高初始化对未知遗忘目标的泛化能力。
    - **元测试**：从元学习得到的初始化出发，仅执行少量文本侧遗忘步骤，即可用于新的遗忘请求。
- **公式/算法流程**（文字说明）：
    - 每个元训练episode采样环境e=(z,m,c)，包含遗忘目标、操作符、查询条件。
    - 内循环：θ_{T,k+1} = θ_{T,k} - α ∇L_in(f_{θ_{T,k}}; 文本遗忘/保留集)。
    - 外循环：计算损失 L_outer = λ_f L_mm_forget + λ_r L_mm_retain + 可选文本损失。
    - 元更新：θ_T ← θ_T - β ∇L_outer(θ_T; e)。
    - 随机元遗忘：对多个环境求平均损失，更新初始化。

## 3. 实验设计

- **数据集与模型**：
    - 两个VLM：**Qwen2.5-VL-7B-Instruct**、**InternVL3-8B-hf**。
    - 两个多模态模因数据集：**Hateful Memes**（二分类）、**COVID Memes**（三分类）。
- **评测设置**：
    - **Text→Text**：仅文本评估遗忘与保留。
    - **Text→Multi**：重组VLM后多模态评估。
    - 指标：Forget准确率（越低越好）、Retain准确率、Test准确率（越高越好）。
- **对比方法**：三个直接VLM遗忘基线——**GradDiff**、**NPO**、**RMU**。
- **遗忘/保留集构建**：对训练集特征进行PCA降维后KMeans聚类（k=10），选择目标簇作为遗忘集，其余作为保留集。

## 4. 资源与算力

- 论文明确说明：实验在**NVIDIA A100-SXM4-80GB** GPU上运行，大多数训练和评估使用单卡。
- 未报告完整的总计算时长，但提到元遗忘训练比单次遗忘方法更昂贵。
- 训练超参数：LoRA rank=8，bf16，fine-tuning 3 epochs；直接遗忘1 epoch；元遗忘内步长1e-5，元步长1e-5，K=4内步，5外epochs。

## 5. 实验数量与充分性

- **主要实验**：表1报告了2个模型 × 2个数据集 × 4种方法 × 2种评测（Text→Text/Text→Multi）共32个主结果，并展示了SMU在多数设定下最优综合权衡。
- **迁移实验**：图4测试了目标迁移（元训练目标不同于元测试目标），图5测试了操作符迁移（元训练用GradDiff，元测试换为RMU/NPO）。
- **额外实验**：附录B进行了图像侧遗忘（表2），证明图像侧不稳定；附录C给出了消融和超参数细节。
- **充分性与公平性**：实验覆盖了不同模型、数据集、评测方式，并验证了泛化性；但未做更多模型、更大规模数据集或更复杂的攻击测试。基线方法使用了官方/文献中的标准超参数，比较基本公平。

## 6. 论文的主要结论与发现

- SMU实现了当前最强的遗忘-保留权衡：平均Forget准确率9.16%（比最强基线低10.52点），Retain 75.23%（高20.10点），Test 68.45%（高17.01点）。
- SMU能够迁移到新的遗忘目标（图4），在大部分设定下仍保持低遗忘率和高保留率。
- SMU对元测试操作符的变化具有鲁棒性（图5），即使用GradDiff训练，测试时换为RMU/NPO仍有效。
- 图像侧遗忘不稳定（表2），容易导致效用崩溃，支持将遗忘操作放在语言主干上的设计。

## 7. 优点

- **方法论创新**：首次将元学习引入VLM遗忘，解决了“组件-系统”不匹配问题（语言主干更新 vs 多模态系统评估）。
- **双层优化设计**：内环保留低计算成本（仅文本），外环提供多模态反馈，不更新视觉组件。
- **随机任务采样**：提高了对未知遗忘目标及操作符的泛化能力。
- **实验扎实**：在多个模型和数据集上验证，并进行了迁移性测试，结果显著优于三种强基线。

## 8. 不足与局限

- **计算成本**：元训练相比直接遗忘更昂贵（需展开内循环和元梯度计算）。
- **视觉组件冻结**：当目标信息主要存在于视觉编码器或投影模块时，可能遗忘不充分。
- **实验覆盖有限**：仅测试了两个7B/8B级别的VLM和两个模因数据集，未在更大模型（如13B+）或更多样化视觉领域（如自然图像、医学影像）上评估。
- **缺少正式安全保证**：仅凭经验评估遗忘，无法保证目标知识无法被更强的对抗提示或未见过的多模态上下文恢复。
- **可能被滥用**：遗忘技术可用于隐藏不良模型行为或逃避问责。

（完）
